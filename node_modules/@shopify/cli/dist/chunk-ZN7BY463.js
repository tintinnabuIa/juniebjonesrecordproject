import {
  format2,
  init_prettier,
  resolveConfig
} from "./chunk-USGISM5H.js";
import {
  require_globby
} from "./chunk-56ELYGPH.js";
import {
  base_command_default
} from "./chunk-KQU2IAXJ.js";
import {
  businessPlatformRequest,
  ensureAuthenticatedAdmin,
  ensureAuthenticatedBusinessPlatform,
  fanoutHooks,
  graphqlRequest,
  logout,
  normalizeStoreFqdn
} from "./chunk-ZVE6VSBX.js";
import {
  findUpAndReadPackageJson,
  getPackageManager,
  installNodeModules,
  packageManagerFromUserAgent,
  readAndParsePackageJson,
  writePackageJSON
} from "./chunk-O3GDENDY.js";
import {
  AbortError,
  BugError,
  ansi_escapes_default,
  camelize,
  capitalize,
  copyFile,
  currentProcessIsGlobal,
  fileExists,
  findPathUp,
  formatPackageManagerCommand,
  glob,
  hasGit,
  hyphenate,
  isDirectory,
  isTerminalInteractive,
  linesToColumns,
  mkdir,
  outputContent,
  outputDebug,
  outputInfo,
  outputToken,
  readFile,
  removeFile,
  renderConfirmationPrompt,
  renderFatalError,
  renderInfo,
  renderSelectPrompt,
  renderSuccess,
  renderTasks,
  renderTextPrompt,
  renderWarning,
  require_cross_spawn,
  require_source,
  require_supports_hyperlinks,
  rmdir,
  runWithTimer,
  source_default,
  writeFile
} from "./chunk-6O2ILC2V.js";
import {
  require_ansi_escapes,
  require_ansi_styles,
  require_clean_stack,
  require_color_convert,
  require_color_name,
  require_ejs,
  require_get_package_type,
  require_is_fullwidth_code_point,
  require_string_width,
  require_strip_ansi,
  require_typescript,
  require_widest_line,
  require_wordwrap,
  require_wrap_ansi
} from "./chunk-EZZRDRCA.js";
import {
  require_source_map_support
} from "./chunk-KUJQ4AB6.js";
import {
  require_src,
  require_supports_color
} from "./chunk-UMUTXITN.js";
import {
  require_indent_string
} from "./chunk-UV5N2VL7.js";
import {
  require_is_wsl
} from "./chunk-G2ZZKGSV.js";
import {
  basename,
  cwd,
  dirname,
  extname,
  joinPath,
  relativePath,
  relativizePath,
  resolvePath
} from "./chunk-H4D3T5OG.js";
import {
  __commonJS,
  __require,
  __toESM,
  init_cjs_shims
} from "./chunk-PKR7KJ6P.js";

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/write.js
var require_write = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/write.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var stdout = (msg) => {
      process.stdout.write(msg);
    }, stderr = (msg) => {
      process.stderr.write(msg);
    };
    exports.default = {
      stderr,
      stdout
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/fs.js
var require_fs = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/fs.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.existsSync = exports.safeReadJson = exports.readJsonSync = exports.readJson = exports.fileExists = exports.dirExists = void 0;
    var node_fs_1 = __require("node:fs"), promises_1 = __require("node:fs/promises"), dirExists = async (input) => {
      let dirStat;
      try {
        dirStat = await (0, promises_1.stat)(input);
      } catch {
        throw new Error(`No directory found at ${input}`);
      }
      if (!dirStat.isDirectory())
        throw new Error(`${input} exists but is not a directory`);
      return input;
    };
    exports.dirExists = dirExists;
    var fileExists2 = async (input) => {
      let fileStat;
      try {
        fileStat = await (0, promises_1.stat)(input);
      } catch {
        throw new Error(`No file found at ${input}`);
      }
      if (!fileStat.isFile())
        throw new Error(`${input} exists but is not a file`);
      return input;
    };
    exports.fileExists = fileExists2;
    async function readJson(path3) {
      let contents = await (0, promises_1.readFile)(path3, "utf8");
      return JSON.parse(contents);
    }
    exports.readJson = readJson;
    function readJsonSync(path3, parse2 = !0) {
      let contents = (0, node_fs_1.readFileSync)(path3, "utf8");
      return parse2 ? JSON.parse(contents) : contents;
    }
    exports.readJsonSync = readJsonSync;
    async function safeReadJson(path3) {
      try {
        return await readJson(path3);
      } catch {
      }
    }
    exports.safeReadJson = safeReadJson;
    function existsSync2(path3) {
      return (0, node_fs_1.existsSync)(path3);
    }
    exports.existsSync = existsSync2;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/util.js
var require_util = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/util.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.mergeNestedObjects = exports.mapValues = exports.uniq = exports.isNotFalsy = exports.isTruthy = exports.capitalize = exports.sumBy = exports.maxBy = exports.isProd = exports.castArray = exports.sortBy = exports.last = exports.uniqBy = exports.compact = exports.pickBy = void 0;
    function pickBy(obj, fn) {
      return Object.entries(obj).reduce((o, [k, v]) => (fn(v) && (o[k] = v), o), {});
    }
    exports.pickBy = pickBy;
    function compact(a) {
      return a.filter((a2) => !!a2);
    }
    exports.compact = compact;
    function uniqBy(arr, fn) {
      return arr.filter((a, i) => {
        let aVal = fn(a);
        return !arr.some((b, j) => j > i && fn(b) === aVal);
      });
    }
    exports.uniqBy = uniqBy;
    function last2(arr) {
      if (arr)
        return arr.at(-1);
    }
    exports.last = last2;
    function compare(a, b) {
      if (a = a === void 0 ? 0 : a, b = b === void 0 ? 0 : b, Array.isArray(a) && Array.isArray(b)) {
        if (a.length === 0 && b.length === 0)
          return 0;
        let diff = compare(a[0], b[0]);
        return diff !== 0 ? diff : compare(a.slice(1), b.slice(1));
      }
      return a < b ? -1 : a > b ? 1 : 0;
    }
    function sortBy(arr, fn) {
      return arr.sort((a, b) => compare(fn(a), fn(b)));
    }
    exports.sortBy = sortBy;
    function castArray(input) {
      return input === void 0 ? [] : Array.isArray(input) ? input : [input];
    }
    exports.castArray = castArray;
    function isProd() {
      return !["development", "test"].includes(process.env.NODE_ENV ?? "");
    }
    exports.isProd = isProd;
    function maxBy(arr, fn) {
      if (arr.length !== 0)
        return arr.reduce((maxItem, i) => {
          let curr = fn(i), max = fn(maxItem);
          return curr > max ? i : maxItem;
        });
    }
    exports.maxBy = maxBy;
    function sumBy(arr, fn) {
      return arr.reduce((sum, i) => sum + fn(i), 0);
    }
    exports.sumBy = sumBy;
    function capitalize2(s) {
      return s ? s.charAt(0).toUpperCase() + s.slice(1).toLowerCase() : "";
    }
    exports.capitalize = capitalize2;
    function isTruthy(input) {
      return ["1", "true", "y", "yes"].includes(input.toLowerCase());
    }
    exports.isTruthy = isTruthy;
    function isNotFalsy(input) {
      return !["0", "false", "n", "no"].includes(input.toLowerCase());
    }
    exports.isNotFalsy = isNotFalsy;
    function uniq(arr) {
      return [...new Set(arr)].sort();
    }
    exports.uniq = uniq;
    function mapValues(obj, fn) {
      return Object.entries(obj).reduce((o, [k, v]) => (o[k] = fn(v, k), o), {});
    }
    exports.mapValues = mapValues;
    function get(obj, path3) {
      return path3.split(".").reduce((o, p) => o?.[p], obj);
    }
    function mergeNestedObjects(objs, path3) {
      return Object.fromEntries(objs.flatMap((o) => Object.entries(get(o, path3) ?? {})).reverse());
    }
    exports.mergeNestedObjects = mergeNestedObjects;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/args.js
var require_args = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/args.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.string = exports.url = exports.file = exports.directory = exports.integer = exports.boolean = exports.custom = void 0;
    var node_url_1 = __require("node:url"), fs_1 = require_fs(), util_1 = require_util();
    function custom(defaults) {
      return (options = {}) => ({
        parse: async (i, _context, _opts) => i,
        ...defaults,
        ...options,
        input: [],
        type: "option"
      });
    }
    exports.custom = custom;
    exports.boolean = custom({
      parse: async (b) => !!b && (0, util_1.isNotFalsy)(b)
    });
    exports.integer = custom({
      async parse(input, _, opts) {
        if (!/^-?\d+$/.test(input))
          throw new Error(`Expected an integer but received: ${input}`);
        let num = Number.parseInt(input, 10);
        if (opts.min !== void 0 && num < opts.min)
          throw new Error(`Expected an integer greater than or equal to ${opts.min} but received: ${input}`);
        if (opts.max !== void 0 && num > opts.max)
          throw new Error(`Expected an integer less than or equal to ${opts.max} but received: ${input}`);
        return num;
      }
    });
    exports.directory = custom({
      async parse(input, _, opts) {
        return opts.exists ? (0, fs_1.dirExists)(input) : input;
      }
    });
    exports.file = custom({
      async parse(input, _, opts) {
        return opts.exists ? (0, fs_1.fileExists)(input) : input;
      }
    });
    exports.url = custom({
      async parse(input) {
        try {
          return new node_url_1.URL(input);
        } catch {
          throw new Error(`Expected a valid url but received: ${input}`);
        }
      }
    });
    var stringArg = custom({});
    exports.string = stringArg;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/package.json
var require_package = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/package.json"(exports, module) {
    module.exports = {
      name: "@oclif/core",
      description: "base library for oclif CLIs",
      version: "3.26.5",
      author: "Salesforce",
      bugs: "https://github.com/oclif/core/issues",
      dependencies: {
        "@types/cli-progress": "^3.11.5",
        "ansi-escapes": "^4.3.2",
        "ansi-styles": "^4.3.0",
        cardinal: "^2.1.1",
        chalk: "^4.1.2",
        "clean-stack": "^3.0.1",
        "cli-progress": "^3.12.0",
        color: "^4.2.3",
        debug: "^4.3.4",
        ejs: "^3.1.10",
        "get-package-type": "^0.1.0",
        globby: "^11.1.0",
        hyperlinker: "^1.0.0",
        "indent-string": "^4.0.0",
        "is-wsl": "^2.2.0",
        "js-yaml": "^3.14.1",
        minimatch: "^9.0.4",
        "natural-orderby": "^2.0.3",
        "object-treeify": "^1.1.33",
        "password-prompt": "^1.1.3",
        "slice-ansi": "^4.0.0",
        "string-width": "^4.2.3",
        "strip-ansi": "^6.0.1",
        "supports-color": "^8.1.1",
        "supports-hyperlinks": "^2.2.0",
        "widest-line": "^3.1.0",
        wordwrap: "^1.0.0",
        "wrap-ansi": "^7.0.0"
      },
      devDependencies: {
        "@commitlint/config-conventional": "^17.8.1",
        "@oclif/plugin-help": "^6",
        "@oclif/plugin-plugins": "^4",
        "@oclif/prettier-config": "^0.2.1",
        "@oclif/test": "^3.2.11",
        "@types/ansi-styles": "^3.2.1",
        "@types/benchmark": "^2.1.5",
        "@types/chai": "^4.3.11",
        "@types/chai-as-promised": "^7.1.8",
        "@types/clean-stack": "^2.1.1",
        "@types/color": "^3.0.6",
        "@types/debug": "^4.1.10",
        "@types/ejs": "^3.1.5",
        "@types/indent-string": "^4.0.1",
        "@types/js-yaml": "^3.12.7",
        "@types/mocha": "^10.0.6",
        "@types/node": "^18",
        "@types/node-notifier": "^8.0.5",
        "@types/pnpapi": "^0.0.5",
        "@types/slice-ansi": "^4.0.0",
        "@types/strip-ansi": "^5.2.1",
        "@types/supports-color": "^8.1.1",
        "@types/wordwrap": "^1.0.3",
        "@types/wrap-ansi": "^3.0.0",
        benchmark: "^2.1.4",
        chai: "^4.4.1",
        "chai-as-promised": "^7.1.1",
        commitlint: "^17.8.1",
        "cross-env": "^7.0.3",
        eslint: "^8.57.0",
        "eslint-config-oclif": "^5.1.3",
        "eslint-config-oclif-typescript": "^3.1.6",
        "eslint-config-prettier": "^9.1.0",
        "fancy-test": "^3.0.14",
        globby: "^11.1.0",
        husky: "^8",
        "lint-staged": "^14.0.1",
        madge: "^6.1.0",
        mocha: "^10.4.0",
        nyc: "^15.1.0",
        prettier: "^3.2.5",
        shx: "^0.3.4",
        sinon: "^16.1.3",
        "ts-node": "^10.9.2",
        tsd: "^0.31.0",
        typescript: "^5"
      },
      engines: {
        node: ">=18.0.0"
      },
      files: [
        "/lib",
        "/flush.js",
        "/flush.d.ts",
        "/handle.js"
      ],
      homepage: "https://github.com/oclif/core",
      keywords: [
        "oclif",
        "cli",
        "command",
        "command line",
        "parser",
        "args",
        "argv"
      ],
      license: "MIT",
      main: "./lib/index.js",
      repository: "oclif/core",
      oclif: {
        bin: "oclif",
        devPlugins: [
          "@oclif/plugin-help",
          "@oclif/plugin-plugins"
        ]
      },
      publishConfig: {
        access: "public"
      },
      scripts: {
        build: "shx rm -rf lib && tsc",
        commitlint: "commitlint",
        compile: "tsc",
        format: 'prettier --write "+(src|test)/**/*.+(ts|js|json)"',
        lint: "eslint . --ext .ts",
        posttest: "yarn lint && yarn test:circular-deps",
        prepack: "yarn run build",
        prepare: "husky install",
        pretest: "yarn build && tsc -p test --noEmit --skipLibCheck",
        "test:circular-deps": "madge lib/ -c",
        "test:debug": 'nyc mocha --debug-brk --inspect "test/**/*.test.ts"',
        "test:integration": 'mocha --forbid-only "test/**/*.integration.ts" --parallel --timeout 1200000',
        "test:interoperability": "cross-env DEBUG=integration:* ts-node test/integration/interop.ts",
        "test:perf": "ts-node test/perf/parser.perf.ts",
        test: 'nyc mocha --forbid-only "test/**/*.test.ts"'
      },
      types: "lib/index.d.ts"
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cache.js
var require_cache = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cache.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var node_fs_1 = __require("node:fs"), node_path_1 = __require("node:path"), Cache = class _Cache extends Map {
      static instance;
      constructor() {
        super(), this.set("@oclif/core", this.getOclifCoreMeta());
      }
      static getInstance() {
        return _Cache.instance || (_Cache.instance = new _Cache()), _Cache.instance;
      }
      get(key) {
        return super.get(key);
      }
      getOclifCoreMeta() {
        try {
          return { name: "@oclif/core", version: require_package().version };
        } catch {
          try {
            return {
              name: "@oclif/core",
              version: JSON.parse((0, node_fs_1.readFileSync)((0, node_path_1.join)(__dirname, "..", "package.json"), "utf8"))
            };
          } catch {
            return { name: "@oclif/core", version: "unknown" };
          }
        }
      }
    };
    exports.default = Cache;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/settings.js
var require_settings = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/settings.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.settings = void 0;
    global.oclif || (global.oclif = {});
    exports.settings = global.oclif;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/screen.js
var require_screen = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/screen.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.errtermwidth = exports.stdtermwidth = void 0;
    var settings_1 = require_settings();
    function termwidth(stream) {
      if (!stream.isTTY)
        return 80;
      let width = stream.getWindowSize()[0];
      return width < 1 ? 80 : width < 40 ? 40 : width;
    }
    var columns = Number.parseInt(process.env.OCLIF_COLUMNS, 10) || settings_1.settings.columns;
    exports.stdtermwidth = columns || termwidth(process.stdout);
    exports.errtermwidth = columns || termwidth(process.stderr);
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/logger.js
var require_logger = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/logger.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.Logger = void 0;
    var promises_1 = __require("node:fs/promises"), node_path_1 = __require("node:path"), stripAnsi = require_strip_ansi(), timestamp = () => (/* @__PURE__ */ new Date()).toISOString(), timer, wait = (ms) => new Promise((resolve2) => {
      timer && timer.unref(), timer = setTimeout(() => resolve2(null), ms);
    });
    function chomp(s) {
      return s.endsWith(`
`) ? s.replace(/\n$/, "") : s;
    }
    var Logger = class {
      file;
      buffer = [];
      flushing = Promise.resolve();
      constructor(file) {
        this.file = file;
      }
      async flush(waitForMs = 0) {
        await wait(waitForMs), this.flushing = this.flushing.then(async () => {
          if (this.buffer.length === 0)
            return;
          let mylines = this.buffer;
          this.buffer = [], await (0, promises_1.mkdir)((0, node_path_1.dirname)(this.file), { recursive: !0 }), await (0, promises_1.appendFile)(this.file, mylines.join(`
`) + `
`);
        }), await this.flushing;
      }
      log(msg) {
        msg = stripAnsi(chomp(msg));
        let lines = msg.split(`
`).map((l) => `${timestamp()} ${l}`.trimEnd());
        this.buffer.push(...lines), this.flush(50).catch(console.error);
      }
    };
    exports.Logger = Logger;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/config.js
var require_config = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/config.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.config = void 0;
    var settings_1 = require_settings(), logger_1 = require_logger();
    function displayWarnings() {
      process.listenerCount("warning") > 1 || process.on("warning", (warning) => {
        console.error(warning.stack), warning.detail && console.error(warning.detail);
      });
    }
    exports.config = {
      get debug() {
        return !!settings_1.settings.debug;
      },
      set debug(enabled) {
        settings_1.settings.debug = enabled, enabled && displayWarnings();
      },
      get errlog() {
        return settings_1.settings.errlog;
      },
      set errlog(errlog) {
        errlog ? (this.errorLogger = new logger_1.Logger(errlog), settings_1.settings.errlog = errlog) : (delete this.errorLogger, delete settings_1.settings.errlog);
      },
      errorLogger: void 0
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/cli.js
var require_cli = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/cli.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.CLIError = exports.addOclifExitCode = void 0;
    var chalk_1 = __importDefault(require_source()), clean_stack_1 = __importDefault(require_clean_stack()), indent_string_1 = __importDefault(require_indent_string()), wrap_ansi_1 = __importDefault(require_wrap_ansi()), cache_1 = __importDefault(require_cache()), screen_1 = require_screen(), config_1 = require_config();
    function addOclifExitCode(error, options) {
      return "oclif" in error || (error.oclif = {}), error.oclif.exit = options?.exit === void 0 ? cache_1.default.getInstance().get("exitCodes")?.default ?? 2 : options.exit, error;
    }
    exports.addOclifExitCode = addOclifExitCode;
    var CLIError = class extends Error {
      code;
      oclif = {};
      skipOclifErrorHandling;
      suggestions;
      constructor(error, options = {}) {
        super(error instanceof Error ? error.message : error), addOclifExitCode(this, options), this.code = options.code, this.suggestions = options.suggestions;
      }
      get bang() {
        try {
          return chalk_1.default.red(process.platform === "win32" ? "\xBB" : "\u203A");
        } catch {
        }
      }
      get stack() {
        return (0, clean_stack_1.default)(super.stack, { pretty: !0 });
      }
      /**
       * @deprecated `render` Errors display should be handled by display function, like pretty-print
       * @return {string} returns a string representing the dispay of the error
       */
      render() {
        if (config_1.config.debug)
          return this.stack;
        let output = `${this.name}: ${this.message}`;
        return output = (0, wrap_ansi_1.default)(output, screen_1.errtermwidth - 6, { hard: !0, trim: !1 }), output = (0, indent_string_1.default)(output, 3), output = (0, indent_string_1.default)(output, 1, { includeEmptyLines: !0, indent: this.bang }), output = (0, indent_string_1.default)(output, 1), output;
      }
    };
    exports.CLIError = CLIError;
    (function(CLIError2) {
      class Warn extends CLIError2 {
        constructor(err) {
          super(err instanceof Error ? err.message : err), this.name = "Warning";
        }
        get bang() {
          try {
            return chalk_1.default.yellow(process.platform === "win32" ? "\xBB" : "\u203A");
          } catch {
          }
        }
      }
      CLIError2.Warn = Warn;
    })(CLIError || (exports.CLIError = CLIError = {}));
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/exit.js
var require_exit = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/exit.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.ExitError = void 0;
    var cli_1 = require_cli(), ExitError = class extends cli_1.CLIError {
      code = "EEXIT";
      constructor(exitCode = 1) {
        super(`EEXIT: ${exitCode}`, { exit: exitCode });
      }
      render() {
        return "";
      }
    };
    exports.ExitError = ExitError;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/pretty-print.js
var require_pretty_print = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/pretty-print.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.applyPrettyPrintOptions = void 0;
    var indent_string_1 = __importDefault(require_indent_string()), wrap_ansi_1 = __importDefault(require_wrap_ansi()), screen_1 = require_screen(), config_1 = require_config();
    function applyPrettyPrintOptions(error, options) {
      let prettyErrorKeys = ["message", "code", "ref", "suggestions"];
      for (let key of prettyErrorKeys)
        !(key in error) && options[key] && (error[key] = options[key]);
      return error;
    }
    exports.applyPrettyPrintOptions = applyPrettyPrintOptions;
    var formatSuggestions = (suggestions) => {
      let label = "Try this:";
      if (!suggestions || suggestions.length === 0)
        return;
      if (suggestions.length === 1)
        return `${label} ${suggestions[0]}`;
      let multiple = suggestions.map((suggestion) => `* ${suggestion}`).join(`
`);
      return `${label}
${(0, indent_string_1.default)(multiple, 2)}`;
    };
    function prettyPrint(error) {
      if (config_1.config.debug)
        return error.stack;
      let { bang, code, message, name: errorSuffix, ref, suggestions } = error, formattedHeader = message ? `${errorSuffix || "Error"}: ${message}` : void 0, formattedCode = code ? `Code: ${code}` : void 0, formattedSuggestions = formatSuggestions(suggestions), formattedReference = ref ? `Reference: ${ref}` : void 0, formatted = [formattedHeader, formattedCode, formattedSuggestions, formattedReference].filter(Boolean).join(`
`), output = (0, wrap_ansi_1.default)(formatted, screen_1.errtermwidth - 6, { hard: !0, trim: !1 });
      return output = (0, indent_string_1.default)(output, 3), output = (0, indent_string_1.default)(output, 1, { includeEmptyLines: !0, indent: bang || "" }), output = (0, indent_string_1.default)(output, 1), output;
    }
    exports.default = prettyPrint;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/error.js
var require_error = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/error.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.error = void 0;
    var write_1 = __importDefault(require_write()), config_1 = require_config(), cli_1 = require_cli(), pretty_print_1 = __importStar(require_pretty_print());
    function error(input, options = {}) {
      let err;
      if (typeof input == "string")
        err = new cli_1.CLIError(input, options);
      else if (input instanceof Error)
        err = (0, cli_1.addOclifExitCode)(input, options);
      else
        throw new TypeError("first argument must be a string or instance of Error");
      if (err = (0, pretty_print_1.applyPrettyPrintOptions)(err, options), options.exit === !1) {
        let message = (0, pretty_print_1.default)(err);
        message && write_1.default.stderr(message + `
`), config_1.config.errorLogger && config_1.config.errorLogger.log(err?.stack ?? "");
      } else
        throw err;
    }
    exports.error = error;
    exports.default = error;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/module-load.js
var require_module_load = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/errors/module-load.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.ModuleLoadError = void 0;
    var cli_1 = require_cli(), ModuleLoadError = class extends cli_1.CLIError {
      code = "MODULE_NOT_FOUND";
      constructor(message) {
        super(`[MODULE_NOT_FOUND] ${message}`, { exit: 1 }), this.name = "ModuleLoadError";
      }
    };
    exports.ModuleLoadError = ModuleLoadError;
  }
});

// ../../node_modules/.pnpm/is-arrayish@0.3.4/node_modules/is-arrayish/index.js
var require_is_arrayish = __commonJS({
  "../../node_modules/.pnpm/is-arrayish@0.3.4/node_modules/is-arrayish/index.js"(exports, module) {
    init_cjs_shims();
    module.exports = function(obj) {
      return !obj || typeof obj == "string" ? !1 : obj instanceof Array || Array.isArray(obj) || obj.length >= 0 && (obj.splice instanceof Function || Object.getOwnPropertyDescriptor(obj, obj.length - 1) && obj.constructor.name !== "String");
    };
  }
});

// ../../node_modules/.pnpm/simple-swizzle@0.2.4/node_modules/simple-swizzle/index.js
var require_simple_swizzle = __commonJS({
  "../../node_modules/.pnpm/simple-swizzle@0.2.4/node_modules/simple-swizzle/index.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var isArrayish = require_is_arrayish(), concat = Array.prototype.concat, slice = Array.prototype.slice, swizzle = module.exports = function(args) {
      for (var results = [], i = 0, len = args.length; i < len; i++) {
        var arg = args[i];
        isArrayish(arg) ? results = concat.call(results, slice.call(arg)) : results.push(arg);
      }
      return results;
    };
    swizzle.wrap = function(fn) {
      return function() {
        return fn(swizzle(arguments));
      };
    };
  }
});

// ../../node_modules/.pnpm/color-string@1.9.1/node_modules/color-string/index.js
var require_color_string = __commonJS({
  "../../node_modules/.pnpm/color-string@1.9.1/node_modules/color-string/index.js"(exports, module) {
    init_cjs_shims();
    var colorNames = require_color_name(), swizzle = require_simple_swizzle(), hasOwnProperty = Object.hasOwnProperty, reverseNames = /* @__PURE__ */ Object.create(null);
    for (name in colorNames)
      hasOwnProperty.call(colorNames, name) && (reverseNames[colorNames[name]] = name);
    var name, cs = module.exports = {
      to: {},
      get: {}
    };
    cs.get = function(string) {
      var prefix = string.substring(0, 3).toLowerCase(), val, model;
      switch (prefix) {
        case "hsl":
          val = cs.get.hsl(string), model = "hsl";
          break;
        case "hwb":
          val = cs.get.hwb(string), model = "hwb";
          break;
        default:
          val = cs.get.rgb(string), model = "rgb";
          break;
      }
      return val ? { model, value: val } : null;
    };
    cs.get.rgb = function(string) {
      if (!string)
        return null;
      var abbr = /^#([a-f0-9]{3,4})$/i, hex = /^#([a-f0-9]{6})([a-f0-9]{2})?$/i, rgba = /^rgba?\(\s*([+-]?\d+)(?=[\s,])\s*(?:,\s*)?([+-]?\d+)(?=[\s,])\s*(?:,\s*)?([+-]?\d+)\s*(?:[,|\/]\s*([+-]?[\d\.]+)(%?)\s*)?\)$/, per = /^rgba?\(\s*([+-]?[\d\.]+)\%\s*,?\s*([+-]?[\d\.]+)\%\s*,?\s*([+-]?[\d\.]+)\%\s*(?:[,|\/]\s*([+-]?[\d\.]+)(%?)\s*)?\)$/, keyword = /^(\w+)$/, rgb = [0, 0, 0, 1], match, i, hexAlpha;
      if (match = string.match(hex)) {
        for (hexAlpha = match[2], match = match[1], i = 0; i < 3; i++) {
          var i2 = i * 2;
          rgb[i] = parseInt(match.slice(i2, i2 + 2), 16);
        }
        hexAlpha && (rgb[3] = parseInt(hexAlpha, 16) / 255);
      } else if (match = string.match(abbr)) {
        for (match = match[1], hexAlpha = match[3], i = 0; i < 3; i++)
          rgb[i] = parseInt(match[i] + match[i], 16);
        hexAlpha && (rgb[3] = parseInt(hexAlpha + hexAlpha, 16) / 255);
      } else if (match = string.match(rgba)) {
        for (i = 0; i < 3; i++)
          rgb[i] = parseInt(match[i + 1], 0);
        match[4] && (match[5] ? rgb[3] = parseFloat(match[4]) * 0.01 : rgb[3] = parseFloat(match[4]));
      } else if (match = string.match(per)) {
        for (i = 0; i < 3; i++)
          rgb[i] = Math.round(parseFloat(match[i + 1]) * 2.55);
        match[4] && (match[5] ? rgb[3] = parseFloat(match[4]) * 0.01 : rgb[3] = parseFloat(match[4]));
      } else return (match = string.match(keyword)) ? match[1] === "transparent" ? [0, 0, 0, 0] : hasOwnProperty.call(colorNames, match[1]) ? (rgb = colorNames[match[1]], rgb[3] = 1, rgb) : null : null;
      for (i = 0; i < 3; i++)
        rgb[i] = clamp(rgb[i], 0, 255);
      return rgb[3] = clamp(rgb[3], 0, 1), rgb;
    };
    cs.get.hsl = function(string) {
      if (!string)
        return null;
      var hsl = /^hsla?\(\s*([+-]?(?:\d{0,3}\.)?\d+)(?:deg)?\s*,?\s*([+-]?[\d\.]+)%\s*,?\s*([+-]?[\d\.]+)%\s*(?:[,|\/]\s*([+-]?(?=\.\d|\d)(?:0|[1-9]\d*)?(?:\.\d*)?(?:[eE][+-]?\d+)?)\s*)?\)$/, match = string.match(hsl);
      if (match) {
        var alpha = parseFloat(match[4]), h = (parseFloat(match[1]) % 360 + 360) % 360, s = clamp(parseFloat(match[2]), 0, 100), l = clamp(parseFloat(match[3]), 0, 100), a = clamp(isNaN(alpha) ? 1 : alpha, 0, 1);
        return [h, s, l, a];
      }
      return null;
    };
    cs.get.hwb = function(string) {
      if (!string)
        return null;
      var hwb = /^hwb\(\s*([+-]?\d{0,3}(?:\.\d+)?)(?:deg)?\s*,\s*([+-]?[\d\.]+)%\s*,\s*([+-]?[\d\.]+)%\s*(?:,\s*([+-]?(?=\.\d|\d)(?:0|[1-9]\d*)?(?:\.\d*)?(?:[eE][+-]?\d+)?)\s*)?\)$/, match = string.match(hwb);
      if (match) {
        var alpha = parseFloat(match[4]), h = (parseFloat(match[1]) % 360 + 360) % 360, w = clamp(parseFloat(match[2]), 0, 100), b = clamp(parseFloat(match[3]), 0, 100), a = clamp(isNaN(alpha) ? 1 : alpha, 0, 1);
        return [h, w, b, a];
      }
      return null;
    };
    cs.to.hex = function() {
      var rgba = swizzle(arguments);
      return "#" + hexDouble(rgba[0]) + hexDouble(rgba[1]) + hexDouble(rgba[2]) + (rgba[3] < 1 ? hexDouble(Math.round(rgba[3] * 255)) : "");
    };
    cs.to.rgb = function() {
      var rgba = swizzle(arguments);
      return rgba.length < 4 || rgba[3] === 1 ? "rgb(" + Math.round(rgba[0]) + ", " + Math.round(rgba[1]) + ", " + Math.round(rgba[2]) + ")" : "rgba(" + Math.round(rgba[0]) + ", " + Math.round(rgba[1]) + ", " + Math.round(rgba[2]) + ", " + rgba[3] + ")";
    };
    cs.to.rgb.percent = function() {
      var rgba = swizzle(arguments), r = Math.round(rgba[0] / 255 * 100), g = Math.round(rgba[1] / 255 * 100), b = Math.round(rgba[2] / 255 * 100);
      return rgba.length < 4 || rgba[3] === 1 ? "rgb(" + r + "%, " + g + "%, " + b + "%)" : "rgba(" + r + "%, " + g + "%, " + b + "%, " + rgba[3] + ")";
    };
    cs.to.hsl = function() {
      var hsla = swizzle(arguments);
      return hsla.length < 4 || hsla[3] === 1 ? "hsl(" + hsla[0] + ", " + hsla[1] + "%, " + hsla[2] + "%)" : "hsla(" + hsla[0] + ", " + hsla[1] + "%, " + hsla[2] + "%, " + hsla[3] + ")";
    };
    cs.to.hwb = function() {
      var hwba = swizzle(arguments), a = "";
      return hwba.length >= 4 && hwba[3] !== 1 && (a = ", " + hwba[3]), "hwb(" + hwba[0] + ", " + hwba[1] + "%, " + hwba[2] + "%" + a + ")";
    };
    cs.to.keyword = function(rgb) {
      return reverseNames[rgb.slice(0, 3)];
    };
    function clamp(num, min, max) {
      return Math.min(Math.max(min, num), max);
    }
    function hexDouble(num) {
      var str = Math.round(num).toString(16).toUpperCase();
      return str.length < 2 ? "0" + str : str;
    }
  }
});

// ../../node_modules/.pnpm/color@4.2.3/node_modules/color/index.js
var require_color = __commonJS({
  "../../node_modules/.pnpm/color@4.2.3/node_modules/color/index.js"(exports, module) {
    init_cjs_shims();
    var colorString = require_color_string(), convert = require_color_convert(), skippedModels = [
      // To be honest, I don't really feel like keyword belongs in color convert, but eh.
      "keyword",
      // Gray conflicts with some method names, and has its own method defined.
      "gray",
      // Shouldn't really be in color-convert either...
      "hex"
    ], hashedModelKeys = {};
    for (let model of Object.keys(convert))
      hashedModelKeys[[...convert[model].labels].sort().join("")] = model;
    var limiters = {};
    function Color(object, model) {
      if (!(this instanceof Color))
        return new Color(object, model);
      if (model && model in skippedModels && (model = null), model && !(model in convert))
        throw new Error("Unknown model: " + model);
      let i, channels;
      if (object == null)
        this.model = "rgb", this.color = [0, 0, 0], this.valpha = 1;
      else if (object instanceof Color)
        this.model = object.model, this.color = [...object.color], this.valpha = object.valpha;
      else if (typeof object == "string") {
        let result = colorString.get(object);
        if (result === null)
          throw new Error("Unable to parse color from string: " + object);
        this.model = result.model, channels = convert[this.model].channels, this.color = result.value.slice(0, channels), this.valpha = typeof result.value[channels] == "number" ? result.value[channels] : 1;
      } else if (object.length > 0) {
        this.model = model || "rgb", channels = convert[this.model].channels;
        let newArray = Array.prototype.slice.call(object, 0, channels);
        this.color = zeroArray(newArray, channels), this.valpha = typeof object[channels] == "number" ? object[channels] : 1;
      } else if (typeof object == "number")
        this.model = "rgb", this.color = [
          object >> 16 & 255,
          object >> 8 & 255,
          object & 255
        ], this.valpha = 1;
      else {
        this.valpha = 1;
        let keys = Object.keys(object);
        "alpha" in object && (keys.splice(keys.indexOf("alpha"), 1), this.valpha = typeof object.alpha == "number" ? object.alpha : 0);
        let hashedKeys = keys.sort().join("");
        if (!(hashedKeys in hashedModelKeys))
          throw new Error("Unable to parse color from object: " + JSON.stringify(object));
        this.model = hashedModelKeys[hashedKeys];
        let { labels } = convert[this.model], color = [];
        for (i = 0; i < labels.length; i++)
          color.push(object[labels[i]]);
        this.color = zeroArray(color);
      }
      if (limiters[this.model])
        for (channels = convert[this.model].channels, i = 0; i < channels; i++) {
          let limit = limiters[this.model][i];
          limit && (this.color[i] = limit(this.color[i]));
        }
      this.valpha = Math.max(0, Math.min(1, this.valpha)), Object.freeze && Object.freeze(this);
    }
    Color.prototype = {
      toString() {
        return this.string();
      },
      toJSON() {
        return this[this.model]();
      },
      string(places) {
        let self = this.model in colorString.to ? this : this.rgb();
        self = self.round(typeof places == "number" ? places : 1);
        let args = self.valpha === 1 ? self.color : [...self.color, this.valpha];
        return colorString.to[self.model](args);
      },
      percentString(places) {
        let self = this.rgb().round(typeof places == "number" ? places : 1), args = self.valpha === 1 ? self.color : [...self.color, this.valpha];
        return colorString.to.rgb.percent(args);
      },
      array() {
        return this.valpha === 1 ? [...this.color] : [...this.color, this.valpha];
      },
      object() {
        let result = {}, { channels } = convert[this.model], { labels } = convert[this.model];
        for (let i = 0; i < channels; i++)
          result[labels[i]] = this.color[i];
        return this.valpha !== 1 && (result.alpha = this.valpha), result;
      },
      unitArray() {
        let rgb = this.rgb().color;
        return rgb[0] /= 255, rgb[1] /= 255, rgb[2] /= 255, this.valpha !== 1 && rgb.push(this.valpha), rgb;
      },
      unitObject() {
        let rgb = this.rgb().object();
        return rgb.r /= 255, rgb.g /= 255, rgb.b /= 255, this.valpha !== 1 && (rgb.alpha = this.valpha), rgb;
      },
      round(places) {
        return places = Math.max(places || 0, 0), new Color([...this.color.map(roundToPlace(places)), this.valpha], this.model);
      },
      alpha(value) {
        return value !== void 0 ? new Color([...this.color, Math.max(0, Math.min(1, value))], this.model) : this.valpha;
      },
      // Rgb
      red: getset("rgb", 0, maxfn(255)),
      green: getset("rgb", 1, maxfn(255)),
      blue: getset("rgb", 2, maxfn(255)),
      hue: getset(["hsl", "hsv", "hsl", "hwb", "hcg"], 0, (value) => (value % 360 + 360) % 360),
      saturationl: getset("hsl", 1, maxfn(100)),
      lightness: getset("hsl", 2, maxfn(100)),
      saturationv: getset("hsv", 1, maxfn(100)),
      value: getset("hsv", 2, maxfn(100)),
      chroma: getset("hcg", 1, maxfn(100)),
      gray: getset("hcg", 2, maxfn(100)),
      white: getset("hwb", 1, maxfn(100)),
      wblack: getset("hwb", 2, maxfn(100)),
      cyan: getset("cmyk", 0, maxfn(100)),
      magenta: getset("cmyk", 1, maxfn(100)),
      yellow: getset("cmyk", 2, maxfn(100)),
      black: getset("cmyk", 3, maxfn(100)),
      x: getset("xyz", 0, maxfn(95.047)),
      y: getset("xyz", 1, maxfn(100)),
      z: getset("xyz", 2, maxfn(108.833)),
      l: getset("lab", 0, maxfn(100)),
      a: getset("lab", 1),
      b: getset("lab", 2),
      keyword(value) {
        return value !== void 0 ? new Color(value) : convert[this.model].keyword(this.color);
      },
      hex(value) {
        return value !== void 0 ? new Color(value) : colorString.to.hex(this.rgb().round().color);
      },
      hexa(value) {
        if (value !== void 0)
          return new Color(value);
        let rgbArray = this.rgb().round().color, alphaHex = Math.round(this.valpha * 255).toString(16).toUpperCase();
        return alphaHex.length === 1 && (alphaHex = "0" + alphaHex), colorString.to.hex(rgbArray) + alphaHex;
      },
      rgbNumber() {
        let rgb = this.rgb().color;
        return (rgb[0] & 255) << 16 | (rgb[1] & 255) << 8 | rgb[2] & 255;
      },
      luminosity() {
        let rgb = this.rgb().color, lum = [];
        for (let [i, element] of rgb.entries()) {
          let chan = element / 255;
          lum[i] = chan <= 0.04045 ? chan / 12.92 : ((chan + 0.055) / 1.055) ** 2.4;
        }
        return 0.2126 * lum[0] + 0.7152 * lum[1] + 0.0722 * lum[2];
      },
      contrast(color2) {
        let lum1 = this.luminosity(), lum2 = color2.luminosity();
        return lum1 > lum2 ? (lum1 + 0.05) / (lum2 + 0.05) : (lum2 + 0.05) / (lum1 + 0.05);
      },
      level(color2) {
        let contrastRatio = this.contrast(color2);
        return contrastRatio >= 7 ? "AAA" : contrastRatio >= 4.5 ? "AA" : "";
      },
      isDark() {
        let rgb = this.rgb().color;
        return (rgb[0] * 2126 + rgb[1] * 7152 + rgb[2] * 722) / 1e4 < 128;
      },
      isLight() {
        return !this.isDark();
      },
      negate() {
        let rgb = this.rgb();
        for (let i = 0; i < 3; i++)
          rgb.color[i] = 255 - rgb.color[i];
        return rgb;
      },
      lighten(ratio) {
        let hsl = this.hsl();
        return hsl.color[2] += hsl.color[2] * ratio, hsl;
      },
      darken(ratio) {
        let hsl = this.hsl();
        return hsl.color[2] -= hsl.color[2] * ratio, hsl;
      },
      saturate(ratio) {
        let hsl = this.hsl();
        return hsl.color[1] += hsl.color[1] * ratio, hsl;
      },
      desaturate(ratio) {
        let hsl = this.hsl();
        return hsl.color[1] -= hsl.color[1] * ratio, hsl;
      },
      whiten(ratio) {
        let hwb = this.hwb();
        return hwb.color[1] += hwb.color[1] * ratio, hwb;
      },
      blacken(ratio) {
        let hwb = this.hwb();
        return hwb.color[2] += hwb.color[2] * ratio, hwb;
      },
      grayscale() {
        let rgb = this.rgb().color, value = rgb[0] * 0.3 + rgb[1] * 0.59 + rgb[2] * 0.11;
        return Color.rgb(value, value, value);
      },
      fade(ratio) {
        return this.alpha(this.valpha - this.valpha * ratio);
      },
      opaquer(ratio) {
        return this.alpha(this.valpha + this.valpha * ratio);
      },
      rotate(degrees) {
        let hsl = this.hsl(), hue = hsl.color[0];
        return hue = (hue + degrees) % 360, hue = hue < 0 ? 360 + hue : hue, hsl.color[0] = hue, hsl;
      },
      mix(mixinColor, weight) {
        if (!mixinColor || !mixinColor.rgb)
          throw new Error('Argument to "mix" was not a Color instance, but rather an instance of ' + typeof mixinColor);
        let color1 = mixinColor.rgb(), color2 = this.rgb(), p = weight === void 0 ? 0.5 : weight, w = 2 * p - 1, a = color1.alpha() - color2.alpha(), w1 = ((w * a === -1 ? w : (w + a) / (1 + w * a)) + 1) / 2, w2 = 1 - w1;
        return Color.rgb(
          w1 * color1.red() + w2 * color2.red(),
          w1 * color1.green() + w2 * color2.green(),
          w1 * color1.blue() + w2 * color2.blue(),
          color1.alpha() * p + color2.alpha() * (1 - p)
        );
      }
    };
    for (let model of Object.keys(convert)) {
      if (skippedModels.includes(model))
        continue;
      let { channels } = convert[model];
      Color.prototype[model] = function(...args) {
        return this.model === model ? new Color(this) : args.length > 0 ? new Color(args, model) : new Color([...assertArray(convert[this.model][model].raw(this.color)), this.valpha], model);
      }, Color[model] = function(...args) {
        let color = args[0];
        return typeof color == "number" && (color = zeroArray(args, channels)), new Color(color, model);
      };
    }
    function roundTo(number, places) {
      return Number(number.toFixed(places));
    }
    function roundToPlace(places) {
      return function(number) {
        return roundTo(number, places);
      };
    }
    function getset(model, channel, modifier) {
      model = Array.isArray(model) ? model : [model];
      for (let m of model)
        (limiters[m] || (limiters[m] = []))[channel] = modifier;
      return model = model[0], function(value) {
        let result;
        return value !== void 0 ? (modifier && (value = modifier(value)), result = this[model](), result.color[channel] = value, result) : (result = this[model]().color[channel], modifier && (result = modifier(result)), result);
      };
    }
    function maxfn(max) {
      return function(v) {
        return Math.max(0, Math.min(max, v));
      };
    }
    function assertArray(value) {
      return Array.isArray(value) ? value : [value];
    }
    function zeroArray(array, length) {
      for (let i = 0; i < length; i++)
        typeof array[i] != "number" && (array[i] = 0);
      return array;
    }
    module.exports = Color;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/interfaces/theme.js
var require_theme = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/interfaces/theme.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.THEME_KEYS = exports.STANDARD_CHALK = void 0;
    exports.STANDARD_CHALK = [
      "white",
      "black",
      "blue",
      "yellow",
      "green",
      "red",
      "magenta",
      "cyan",
      "gray",
      "blackBright",
      "redBright",
      "greenBright",
      "yellowBright",
      "blueBright",
      "magentaBright",
      "cyanBright",
      "whiteBright",
      "bgBlack",
      "bgRed",
      "bgGreen",
      "bgYellow",
      "bgBlue",
      "bgMagenta",
      "bgCyan",
      "bgWhite",
      "bgGray",
      "bgBlackBright",
      "bgRedBright",
      "bgGreenBright",
      "bgYellowBright",
      "bgBlueBright",
      "bgMagentaBright",
      "bgCyanBright",
      "bgWhiteBright",
      "bold",
      "underline",
      "dim",
      "italic",
      "strikethrough"
    ];
    exports.THEME_KEYS = [
      "alias",
      "bin",
      "command",
      "commandSummary",
      "dollarSign",
      "flag",
      "flagDefaultValue",
      "flagOptions",
      "flagRequired",
      "flagSeparator",
      "sectionDescription",
      "sectionHeader",
      "topic",
      "version"
    ];
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/theme.js
var require_theme2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/theme.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.getColor = exports.parseTheme = exports.colorize = void 0;
    var chalk_1 = __importDefault(require_source()), Color = __importStar(require_color()), theme_1 = require_theme();
    function isStandardChalk(color) {
      return theme_1.STANDARD_CHALK.includes(color);
    }
    function colorize(color, text) {
      return isStandardChalk(color) ? chalk_1.default[color](text) : color ? chalk_1.default.hex(color)(text) : text;
    }
    exports.colorize = colorize;
    function parseTheme(theme) {
      return Object.fromEntries(Object.entries(theme).map(([key, value]) => [key, getColor(value)]).filter(([_, value]) => value));
    }
    exports.parseTheme = parseTheme;
    function getColor(color) {
      try {
        return isStandardChalk(color) ? color : new Color.default(color).hex();
      } catch {
      }
    }
    exports.getColor = getColor;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/warn.js
var require_warn = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/warn.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.memoizedWarn = exports.warn = void 0;
    var write_1 = __importDefault(require_write()), config_1 = require_config(), cli_1 = require_cli(), pretty_print_1 = __importDefault(require_pretty_print());
    function warn(input) {
      let err;
      if (typeof input == "string")
        err = new cli_1.CLIError.Warn(input);
      else if (input instanceof Error)
        err = (0, cli_1.addOclifExitCode)(input);
      else
        throw new TypeError("first argument must be a string or instance of Error");
      let message = (0, pretty_print_1.default)(err);
      message && write_1.default.stderr(message + `
`), config_1.config.errorLogger && config_1.config.errorLogger.log(err?.stack ?? "");
    }
    exports.warn = warn;
    var WARNINGS = /* @__PURE__ */ new Set();
    function memoizedWarn(input) {
      WARNINGS.has(input) || warn(input), WARNINGS.add(input);
    }
    exports.memoizedWarn = memoizedWarn;
    exports.default = warn;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/read-tsconfig.js
var require_read_tsconfig = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/read-tsconfig.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.readTSConfig = void 0;
    var debug_1 = __importDefault(require_src()), promises_1 = __require("node:fs/promises"), node_path_1 = __require("node:path"), warn_1 = require_warn(), util_1 = require_util(), debug2 = (0, debug_1.default)("read-tsconfig");
    function resolve2(root, name) {
      try {
        return __require.resolve(name, { paths: [root] });
      } catch {
      }
    }
    async function upUntil(path3, test) {
      let result;
      try {
        result = await test(path3);
      } catch {
        result = !1;
      }
      if (result)
        return path3;
      let parent = (0, node_path_1.dirname)(path3);
      if (parent !== path3)
        return upUntil(parent, test);
    }
    async function readTSConfig(root, tsconfigName = "tsconfig.json") {
      let found = [], typescript;
      try {
        typescript = require_typescript();
      } catch {
        try {
          typescript = __require(root + "/node_modules/typescript");
        } catch {
        }
      }
      if (!typescript) {
        (0, warn_1.memoizedWarn)("Could not find typescript. Please ensure that typescript is a devDependency. Falling back to compiled source.");
        return;
      }
      let read = async (path3) => {
        let localRoot = await upUntil(path3, async (p) => (
          // eslint-disable-next-line unicorn/no-await-expression-member
          (await (0, promises_1.readdir)(p)).includes("package.json")
        ));
        if (localRoot)
          try {
            let contents = await (0, promises_1.readFile)(path3, "utf8"), parsed = typescript?.parseConfigFileTextToJson(path3, contents).config;
            if (found.push(parsed), parsed.extends) {
              if (parsed.extends.startsWith(".")) {
                let nextPath = resolve2(localRoot, parsed.extends);
                return nextPath ? read(nextPath) : void 0;
              }
              let resolved = resolve2(localRoot, parsed.extends);
              if (resolved)
                return read(resolved);
            }
            return parsed;
          } catch (error) {
            debug2(error);
          }
      };
      return await read((0, node_path_1.join)(root, tsconfigName)), {
        compilerOptions: (0, util_1.mergeNestedObjects)(found, "compilerOptions"),
        "ts-node": (0, util_1.mergeNestedObjects)(found, "ts-node")
      };
    }
    exports.readTSConfig = readTSConfig;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/util.js
var require_util2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/util.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.collectUsableIds = exports.getCommandIdPermutations = exports.getPermutations = exports.Debug = void 0;
    var debug2 = require_src();
    function displayWarnings() {
      process.listenerCount("warning") > 1 || process.on("warning", (warning) => {
        console.error(warning.stack), warning.detail && console.error(warning.detail);
      });
    }
    function Debug(...scope) {
      if (!debug2)
        return (..._) => {
        };
      let d = debug2(["config", ...scope].join(":"));
      return d.enabled && displayWarnings(), (...args) => d(...args);
    }
    exports.Debug = Debug;
    function getPermutations(arr) {
      if (arr.length === 0)
        return [];
      if (arr.length === 1)
        return [arr];
      let output = [], partialPermutations = getPermutations(arr.slice(1)), first2 = arr[0];
      for (let i = 0, len = partialPermutations.length; i < len; i++) {
        let partial = partialPermutations[i];
        for (let j = 0, len2 = partial.length; j <= len2; j++) {
          let start = partial.slice(0, j), end = partial.slice(j), merged = [...start, first2, ...end];
          output.push(merged);
        }
      }
      return output;
    }
    exports.getPermutations = getPermutations;
    function getCommandIdPermutations(commandId) {
      return getPermutations(commandId.split(":")).flatMap((c) => c.join(":"));
    }
    exports.getCommandIdPermutations = getCommandIdPermutations;
    var collectUsableIds = (commandIds) => new Set(commandIds.flatMap((id) => id.split(":").map((_, i, a) => a.slice(0, i + 1).join(":"))));
    exports.collectUsableIds = collectUsableIds;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/ts-path.js
var require_ts_path = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/ts-path.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.tsPath = exports.TS_CONFIGS = void 0;
    var promises_1 = __require("node:fs/promises"), node_path_1 = __require("node:path"), cache_1 = __importDefault(require_cache()), warn_1 = require_warn(), settings_1 = require_settings(), fs_1 = require_fs(), read_tsconfig_1 = require_read_tsconfig(), util_1 = require_util(), util_2 = require_util2(), debug2 = (0, util_2.Debug)("ts-path");
    exports.TS_CONFIGS = {};
    var REGISTERED = /* @__PURE__ */ new Set();
    function determineRuntime() {
      return process.execPath.split(node_path_1.sep).includes("bun") ? "bun" : process.execArgv.length === 0 ? "node" : process.execArgv[0] === "--require" && process.execArgv[1].split(node_path_1.sep).includes("ts-node") || process.execArgv[0].split(node_path_1.sep).includes("ts-node") ? "ts-node" : process.execArgv[0] === "--require" && process.execArgv[1].split(node_path_1.sep).includes("tsx") ? "tsx" : "node";
    }
    var RUN_TIME = determineRuntime();
    function isErrno(error) {
      return "code" in error && error.code === "ENOENT";
    }
    async function loadTSConfig(root) {
      try {
        if (exports.TS_CONFIGS[root])
          return exports.TS_CONFIGS[root];
        let tsconfig = await (0, read_tsconfig_1.readTSConfig)(root);
        return tsconfig ? (debug2("tsconfig: %O", tsconfig), exports.TS_CONFIGS[root] = tsconfig, exports.TS_CONFIGS[root]) : void 0;
      } catch (error) {
        if (isErrno(error))
          return;
        debug2(`Could not parse tsconfig.json. Skipping typescript path lookup for ${root}.`), (0, warn_1.memoizedWarn)(`Could not parse tsconfig.json for ${root}. Falling back to compiled source.`);
      }
    }
    async function registerTSNode(root, tsconfig) {
      if (REGISTERED.has(root))
        return;
      debug2("registering ts-node at", root);
      let tsNodePath = __require.resolve("ts-node", { paths: [root, __dirname] });
      debug2("ts-node path:", tsNodePath);
      let tsNode;
      try {
        tsNode = __require(tsNodePath);
      } catch {
        debug2(`Could not find ts-node at ${tsNodePath}. Skipping ts-node registration for ${root}.`), (0, warn_1.memoizedWarn)(`Could not find ts-node at ${tsNodePath}. Please ensure that ts-node is a devDependency. Falling back to compiled source.`);
        return;
      }
      let typeRoots = [(0, node_path_1.join)(root, "node_modules", "@types")], rootDirs = [];
      if (tsconfig.compilerOptions.rootDirs)
        for (let r of tsconfig.compilerOptions.rootDirs)
          rootDirs.push((0, node_path_1.join)(root, r));
      else tsconfig.compilerOptions.rootDir ? rootDirs.push((0, node_path_1.join)(root, tsconfig.compilerOptions.rootDir)) : tsconfig.compilerOptions.baseUrl ? rootDirs.push((0, node_path_1.join)(root, tsconfig.compilerOptions.baseUrl)) : rootDirs.push((0, node_path_1.join)(root, "src"));
      let { baseUrl, rootDir, ...rest } = tsconfig.compilerOptions, conf = {
        compilerOptions: {
          ...rest,
          rootDirs,
          typeRoots
        },
        ...tsconfig["ts-node"],
        cwd: root,
        esm: tsconfig["ts-node"]?.esm ?? !0,
        experimentalSpecifierResolution: tsconfig["ts-node"]?.experimentalSpecifierResolution ?? "explicit",
        scope: !0,
        scopeDir: root,
        skipProject: !0,
        transpileOnly: !0
      };
      debug2("ts-node options: %O", conf), tsNode.register(conf), REGISTERED.add(root);
    }
    function cannotTranspileEsm(rootPlugin, plugin, isProduction) {
      return (isProduction || rootPlugin?.moduleType === "commonjs") && plugin?.moduleType === "module";
    }
    function cannotUseTsNode(root, plugin, isProduction) {
      if (plugin?.moduleType !== "module" || isProduction)
        return !1;
      let nodeMajor = Number.parseInt(process.version.replace("v", "").split(".")[0], 10);
      return RUN_TIME === "ts-node" && nodeMajor >= 20;
    }
    async function determinePath(root, orig) {
      let tsconfig = await loadTSConfig(root);
      if (!tsconfig)
        return orig;
      debug2(`Determining path for ${orig}`), RUN_TIME === "tsx" || RUN_TIME === "bun" ? debug2(`Skipping ts-node registration for ${root} because the runtime is: ${RUN_TIME}`) : await registerTSNode(root, tsconfig);
      let { baseUrl, outDir, rootDir, rootDirs } = tsconfig.compilerOptions, rootDirPath = rootDir ?? (rootDirs ?? [])[0] ?? baseUrl;
      if (!rootDirPath)
        return debug2(`no rootDir, rootDirs, or baseUrl specified in tsconfig.json. Returning default path ${orig}`), orig;
      if (!outDir)
        return debug2(`no outDir specified in tsconfig.json. Returning default path ${orig}`), orig;
      let lib = (0, node_path_1.join)(root, outDir), src = (0, node_path_1.join)(root, rootDirPath), relative = (0, node_path_1.relative)(lib, orig), out = (0, node_path_1.join)(src, relative).replace(/\.js$/, "");
      return debug2(`lib dir: ${lib}`), debug2(`src dir: ${src}`), debug2(`src directory to find: ${out}`), (0, fs_1.existsSync)(out) ? (debug2(`Found source directory for ${orig} at ${out}`), out) : (await Promise.all([
        (0, promises_1.access)(`${out}.ts`).then(() => `${out}.ts`).catch(() => !1),
        (0, promises_1.access)(`${out}.tsx`).then(() => `${out}.tsx`).catch(() => !1)
      ])).some(Boolean) ? (debug2(`Found source file for ${orig} at ${out}`), out) : (debug2(`No source file found. Returning default path ${orig}`), (0, util_1.isProd)() || (0, warn_1.memoizedWarn)(`Could not find source for ${orig} based on tsconfig. Defaulting to compiled source.`), orig);
    }
    async function tsPath(root, orig, plugin) {
      let rootPlugin = plugin?.options.isRoot ? plugin : cache_1.default.getInstance().get("rootPlugin");
      if (!orig)
        return orig;
      orig = orig.startsWith(root) ? orig : (0, node_path_1.join)(root, orig);
      let enableAutoTranspile = settings_1.settings.enableAutoTranspile ?? settings_1.settings.tsnodeEnabled;
      if (enableAutoTranspile === !1)
        return debug2(`Skipping typescript path lookup for ${root} because enableAutoTranspile is explicitly set to false`), orig;
      let isProduction = (0, util_1.isProd)();
      if (enableAutoTranspile === void 0 && isProduction && plugin?.type !== "link")
        return debug2(`Skipping typescript path lookup for ${root} because NODE_ENV is NOT "test" or "development"`), orig;
      if (cannotTranspileEsm(rootPlugin, plugin, isProduction))
        return debug2(`Skipping typescript path lookup for ${root} because it's an ESM module (NODE_ENV: ${process.env.NODE_ENV}, root plugin module type: ${rootPlugin?.moduleType})`), plugin?.type === "link" && (0, warn_1.memoizedWarn)(`${plugin?.name} is a linked ESM module and cannot be auto-transpiled. Existing compiled source will be used instead.`), orig;
      if (cannotUseTsNode(root, plugin, isProduction))
        return debug2(`Skipping typescript path lookup for ${root} because ts-node is run in node version ${process.version}"`), (0, warn_1.memoizedWarn)("ts-node executable cannot transpile ESM in Node 20. Existing compiled source will be used instead. See https://github.com/oclif/core/issues/817."), orig;
      try {
        return await determinePath(root, orig);
      } catch (error) {
        return debug2(error), orig;
      }
    }
    exports.tsPath = tsPath;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/module-loader.js
var require_module_loader = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/module-loader.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.isPathModule = exports.loadWithDataFromManifest = exports.loadWithData = exports.load = void 0;
    var node_fs_1 = __require("node:fs"), node_path_1 = __require("node:path"), node_url_1 = __require("node:url"), ts_path_1 = require_ts_path(), module_load_1 = require_module_load(), fs_1 = require_fs(), getPackageType = require_get_package_type(), SUPPORTED_EXTENSIONS = [".ts", ".js", ".mjs", ".cjs", ".mts", ".cts", ".tsx", ".jsx"], isPlugin = (config) => config.type !== void 0;
    function handleError(error, isESM, path3) {
      throw error.code === "MODULE_NOT_FOUND" || error.code === "ERR_MODULE_NOT_FOUND" ? new module_load_1.ModuleLoadError(`${isESM ? "import()" : "require"} failed to load ${path3}: ${error.message}`) : error;
    }
    async function load(config, modulePath) {
      let filePath, isESM;
      try {
        return { filePath, isESM } = await resolvePath2(config, modulePath), isESM ? await import((0, node_url_1.pathToFileURL)(filePath).href) : __require(filePath);
      } catch (error) {
        handleError(error, isESM, filePath ?? modulePath);
      }
    }
    exports.load = load;
    async function loadWithData(config, modulePath) {
      let filePath, isESM;
      try {
        ({ filePath, isESM } = await resolvePath2(config, modulePath));
        let module2 = isESM ? await import((0, node_url_1.pathToFileURL)(filePath).href) : __require(filePath);
        return { filePath, isESM, module: module2 };
      } catch (error) {
        handleError(error, isESM, filePath ?? modulePath);
      }
    }
    exports.loadWithData = loadWithData;
    async function loadWithDataFromManifest(cached, modulePath) {
      let { id, isESM, relativePath: relativePath2 } = cached;
      if (!relativePath2)
        throw new module_load_1.ModuleLoadError(`Cached command ${id} does not have a relative path`);
      if (isESM === void 0)
        throw new module_load_1.ModuleLoadError(`Cached command ${id} does not have the isESM property set`);
      let filePath = (0, node_path_1.join)(modulePath, relativePath2.join(node_path_1.sep));
      try {
        let module2 = isESM ? await import((0, node_url_1.pathToFileURL)(filePath).href) : __require(filePath);
        return { filePath, isESM, module: module2 };
      } catch (error) {
        handleError(error, isESM, filePath ?? modulePath);
      }
    }
    exports.loadWithDataFromManifest = loadWithDataFromManifest;
    function isPathModule(filePath) {
      switch ((0, node_path_1.extname)(filePath).toLowerCase()) {
        case ".js":
        case ".jsx":
        case ".ts":
        case ".tsx":
          return getPackageType.sync(filePath) === "module";
        case ".mjs":
        case ".mts":
          return !0;
        default:
          return !1;
      }
    }
    exports.isPathModule = isPathModule;
    async function resolvePath2(config, modulePath) {
      let isESM, filePath;
      try {
        filePath = __require.resolve(modulePath), isESM = isPathModule(filePath);
      } catch {
        filePath = (isPlugin(config) ? await (0, ts_path_1.tsPath)(config.root, modulePath, config) : await (0, ts_path_1.tsPath)(config.root, modulePath)) ?? modulePath;
        let fileExists2 = !1, isDirectory2 = !1;
        if ((0, fs_1.existsSync)(filePath)) {
          fileExists2 = !0;
          try {
            (0, node_fs_1.lstatSync)(filePath)?.isDirectory?.() && (fileExists2 = !1, isDirectory2 = !0);
          } catch {
          }
        }
        if (!fileExists2) {
          let foundPath = findFile(filePath);
          !foundPath && isDirectory2 && (foundPath = findFile((0, node_path_1.join)(filePath, "index"))), foundPath && (filePath = foundPath);
        }
        isESM = isPathModule(filePath);
      }
      return { filePath, isESM };
    }
    function findFile(filePath) {
      for (let extension of SUPPORTED_EXTENSIONS) {
        let testPath = `${filePath}${extension}`;
        if ((0, fs_1.existsSync)(testPath))
          return testPath;
      }
      return null;
    }
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/symbols.js
var require_symbols = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/symbols.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.SINGLE_COMMAND_CLI_SYMBOL = void 0;
    exports.SINGLE_COMMAND_CLI_SYMBOL = (/* @__PURE__ */ Symbol("SINGLE_COMMAND_CLI")).toString();
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/cache-default-value.js
var require_cache_default_value = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/cache-default-value.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.cacheDefaultValue = void 0;
    var cacheDefaultValue = async (flagOrArg, respectNoCacheDefault) => {
      if (!(respectNoCacheDefault && flagOrArg.noCacheDefault)) {
        if (typeof flagOrArg.defaultHelp == "function")
          try {
            return await flagOrArg.defaultHelp({ flags: {}, options: flagOrArg });
          } catch {
            return;
          }
        if (typeof flagOrArg.default == "function")
          try {
            return await flagOrArg.default({ flags: {}, options: flagOrArg });
          } catch {
          }
        else
          return flagOrArg.default;
      }
    };
    exports.cacheDefaultValue = cacheDefaultValue;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/ids.js
var require_ids = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/ids.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.toConfiguredId = exports.toStandardizedId = void 0;
    function toStandardizedId(commandID, config) {
      return commandID.replaceAll(new RegExp(config.topicSeparator, "g"), ":");
    }
    exports.toStandardizedId = toStandardizedId;
    function toConfiguredId(commandID, config) {
      return commandID.replaceAll(new RegExp(":", "g"), config.topicSeparator || ":");
    }
    exports.toConfiguredId = toConfiguredId;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/ensure-arg-object.js
var require_ensure_arg_object = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/ensure-arg-object.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.ensureArgObject = void 0;
    function ensureArgObject(args) {
      return Array.isArray(args) ? (args ?? []).reduce((x, y) => ({ ...x, [y.name]: y }), {}) : args ?? {};
    }
    exports.ensureArgObject = ensureArgObject;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/docopts.js
var require_docopts = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/docopts.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.DocOpts = void 0;
    var ensure_arg_object_1 = require_ensure_arg_object(), DocOpts = class _DocOpts {
      cmd;
      flagList;
      flagMap;
      constructor(cmd) {
        this.cmd = cmd, this.flagMap = {}, this.flagList = Object.entries(cmd.flags || {}).filter(([_, flag]) => !flag.hidden).map(([name, flag]) => (this.flagMap[name] = flag, flag));
      }
      static generate(cmd) {
        return new _DocOpts(cmd).toString();
      }
      toString() {
        let opts = ["<%= command.id %>"];
        if (this.cmd.args) {
          let suffix = this.cmd.strict === !1 ? "..." : "", a = Object.values((0, ensure_arg_object_1.ensureArgObject)(this.cmd.args)).map((arg) => arg.required ? `${arg.name.toUpperCase()}${suffix}` : `[${arg.name.toUpperCase()}${suffix}]`) || [];
          opts.push(...a);
        }
        try {
          opts.push(...Object.values(this.groupFlagElements()));
        } catch {
          opts.push(...this.flagList.map((flag) => {
            let name = flag.char ? `-${flag.char}` : `--${flag.name}`;
            return flag.type === "boolean" ? name : `${name}=<value>`;
          }));
        }
        return opts.join(" ");
      }
      combineElementsToFlag(elementMap, flagName, flagNames, unionString) {
        if (!this.flagMap[flagName])
          return;
        let isRequired = this.flagMap[flagName]?.required;
        (typeof isRequired != "boolean" || !isRequired) && (isRequired = flagNames.reduce((required, toCombine) => required || this.flagMap[toCombine]?.required || !1, !1));
        for (let toCombine of flagNames)
          elementMap[flagName] = `${elementMap[flagName] || ""}${unionString}${elementMap[toCombine] || ""}`, delete elementMap[toCombine], delete this.flagMap[toCombine];
        elementMap[flagName] = isRequired ? `(${elementMap[flagName] || ""})` : `[${elementMap[flagName] || ""}]`, delete this.flagMap[flagName];
      }
      generateElements(elementMap = {}, flagGroups = []) {
        let elementStrs = [];
        for (let flag of flagGroups) {
          let type = "", flagName = flag.char ? `-${flag.char}` : `--${flag.name}`;
          flag.type === "option" && (type = flag.options ? ` ${flag.options.join("|")}` : " <value>");
          let element = `${flagName}${type}`;
          elementMap[flag.name] = element, elementStrs.push(element);
        }
        return elementStrs;
      }
      groupFlagElements() {
        let elementMap = {};
        this.generateElements(elementMap, this.flagList.filter((flag) => flag.required)), this.generateElements(elementMap, this.flagList.filter((flag) => !flag.required));
        for (let flag of this.flagList)
          Array.isArray(flag.dependsOn) && this.combineElementsToFlag(elementMap, flag.name, flag.dependsOn, " "), Array.isArray(flag.exclusive) && this.combineElementsToFlag(elementMap, flag.name, flag.exclusive, " | ");
        for (let remainingFlagName of Object.keys(this.flagMap)) {
          let remainingFlag = this.flagMap[remainingFlagName] || {};
          remainingFlag.required || (elementMap[remainingFlag.name] = `[${elementMap[remainingFlag.name] || ""}]`);
        }
        return elementMap;
      }
    };
    exports.DocOpts = DocOpts;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/util.js
var require_util3 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/util.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.normalizeArgv = exports.formatCommandDeprecationWarning = exports.formatFlagDeprecationWarning = exports.getHelpFlagAdditions = exports.standardizeIDFromArgv = exports.template = void 0;
    var ejs = __importStar(require_ejs()), util_1 = require_util2(), ids_1 = require_ids();
    function template(context) {
      function render(t) {
        return ejs.render(t, context);
      }
      return render;
    }
    exports.template = template;
    var isFlag = (s) => s.startsWith("-"), isArgWithValue = (s) => s.includes("=");
    function collateSpacedCmdIDFromArgs(argv, config) {
      if (argv.length === 1)
        return argv;
      let id = ((argv2) => {
        let ids = (0, util_1.collectUsableIds)(config.commandIDs), final = [], idPresent = (id2) => ids.has(id2), finalizeId = (s) => (s ? [...final, s] : final).filter(Boolean).join(":"), hasArgs = () => {
          let id2 = finalizeId();
          if (!id2)
            return !1;
          let cmd = config.findCommand(id2);
          return !!(cmd && (cmd.strict === !1 || Object.keys(cmd.args ?? {}).length > 0));
        };
        for (let arg of argv2)
          if (idPresent(finalizeId(arg)))
            final.push(arg);
          else {
            if (isArgWithValue(arg) || isFlag(arg) || hasArgs())
              break;
            final.push(arg);
          }
        return finalizeId();
      })(argv);
      if (id) {
        let argvSlice = argv.slice(id.split(":").length);
        return [id, ...argvSlice];
      }
      return argv;
    }
    function standardizeIDFromArgv(argv, config) {
      return argv.length === 0 || (config.topicSeparator === " " ? argv = collateSpacedCmdIDFromArgs(argv, config) : config.topicSeparator !== ":" && (argv[0] = (0, ids_1.toStandardizedId)(argv[0], config))), argv;
    }
    exports.standardizeIDFromArgv = standardizeIDFromArgv;
    function getHelpFlagAdditions(config) {
      let helpFlags = ["--help"], additionalHelpFlags = config.pjson.oclif.additionalHelpFlags ?? [];
      return [...(/* @__PURE__ */ new Set([...helpFlags, ...additionalHelpFlags])).values()];
    }
    exports.getHelpFlagAdditions = getHelpFlagAdditions;
    function formatFlagDeprecationWarning(flag, opts) {
      let message = `The "${flag}" flag has been deprecated`;
      return opts === !0 ? `${message}.` : opts.message ? opts.message : (opts.version && (message += ` and will be removed in version ${opts.version}`), message += opts.to ? `. Use "${opts.to}" instead.` : ".", message);
    }
    exports.formatFlagDeprecationWarning = formatFlagDeprecationWarning;
    function formatCommandDeprecationWarning(command, opts) {
      let message = `The "${command}" command has been deprecated`;
      return opts ? opts.message ? opts.message : (opts.version && (message += ` and will be removed in version ${opts.version}`), message += opts.to ? `. Use "${opts.to}" instead.` : ".", message) : `${message}.`;
    }
    exports.formatCommandDeprecationWarning = formatCommandDeprecationWarning;
    function normalizeArgv(config, argv = process.argv.slice(2)) {
      return config.topicSeparator !== ":" && !argv[0]?.includes(":") && (argv = standardizeIDFromArgv(argv, config)), argv;
    }
    exports.normalizeArgv = normalizeArgv;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/formatter.js
var require_formatter = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/formatter.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.HelpFormatter = void 0;
    var chalk_1 = __importDefault(require_source()), indent_string_1 = __importDefault(require_indent_string()), string_width_1 = __importDefault(require_string_width()), strip_ansi_1 = __importDefault(require_strip_ansi()), widest_line_1 = __importDefault(require_widest_line()), wrap_ansi_1 = __importDefault(require_wrap_ansi()), theme_1 = require_theme2(), screen_1 = require_screen(), util_1 = require_util3(), HelpFormatter = class {
      config;
      indentSpacing = 2;
      opts;
      /**
       * Takes a string and replaces `<%= prop =>` with the value of prop, where prop is anything on
       * `config=Interfaces.Config` or `opts=Interface.HelpOptions`.
       *
       * ```javascript
       * `<%= config.bin =>` // will resolve to the bin defined in `pjson.oclif`.
       * ```
       */
      render;
      constructor(config, opts = {}) {
        this.config = config, this.opts = { maxWidth: screen_1.stdtermwidth, ...opts }, this.render = (0, util_1.template)(this);
      }
      /**
       * Indent by `this.indentSpacing`. The text should be wrap based on terminal width before indented.
       *
       * In order to call indent multiple times on the same set or text, the caller must wrap based on
       * the number of times the text has been indented. For example.
       *
       * ```javascript
       * const body = `main line\n${indent(wrap('indented example line', 4))}`
       * const header = 'SECTION'
       * console.log(`${header}\n${indent(wrap(body))}`
       * ```
       * will output
       * ```
       * SECTION
       *   main line
       *     indented example line
       * ```
       *
       * If the terminal width was 24 and the `4` was not provided in the first wrap, it would like like the following.
       * ```
       * SECTION
       *   main line
       *     indented example
       *   line
       * ```
       * @param body the text to indent
       * @param spacing the final number of spaces this text will be indented
       * @return the formatted indented text
       */
      indent(body, spacing = this.indentSpacing) {
        return (0, indent_string_1.default)(body, spacing);
      }
      renderList(input, opts) {
        if (input.length === 0)
          return "";
        let renderMultiline = () => {
          let output2 = "";
          for (let [left, right] of input)
            !left && !right || (left && (opts.stripAnsi && (left = (0, strip_ansi_1.default)(left)), output2 += this.wrap(left.trim(), opts.indentation)), right && (opts.stripAnsi && (right = (0, strip_ansi_1.default)(right)), output2 += `
`, output2 += this.indent(this.wrap(right.trim(), opts.indentation + 2), 4)), output2 += `

`);
          return output2.trim();
        };
        if (opts.multiline)
          return renderMultiline();
        let maxLength = (0, widest_line_1.default)(input.map((i) => i[0]).join(`
`)), output = "", spacer = opts.spacer || `
`, cur = "";
        for (let [left, r] of input) {
          let right = r;
          if (cur && (output += spacer, output += cur), cur = left || "", opts.stripAnsi && (cur = (0, strip_ansi_1.default)(cur)), !right) {
            cur = cur.trim();
            continue;
          }
          opts.stripAnsi && (right = (0, strip_ansi_1.default)(right)), right = this.wrap(right.trim(), opts.indentation + maxLength + 2);
          let [first2, ...lines] = right.split(`
`).map((s) => s.trim());
          if (cur += " ".repeat(maxLength - (0, string_width_1.default)(cur) + 2), cur += first2, lines.length !== 0) {
            if (lines.length > 4)
              return renderMultiline();
            opts.spacer || (spacer = `
`), cur += `
`, cur += this.indent(lines.join(`
`), maxLength + 2);
          }
        }
        return cur && (output += spacer, output += cur), output.trim();
      }
      section(header, body) {
        let newBody;
        if (typeof body == "string")
          newBody = this.render(body);
        else if (Array.isArray(body))
          newBody = body.map((entry) => {
            if ("name" in entry) {
              let tableEntry = entry;
              return [this.render(tableEntry.name), this.render(tableEntry.description)];
            }
            let [left, right] = entry;
            return [this.render(left), right && this.render(right)];
          });
        else {
          if ("header" in body)
            return this.section(body.header, body.body);
          newBody = body.map((entry) => [entry.name, entry.description]).map(([left, right]) => [this.render(left), right && this.render(right)]);
        }
        let output = [
          (0, theme_1.colorize)(this.config?.theme?.sectionHeader, chalk_1.default.bold(header)),
          (0, theme_1.colorize)(this.config?.theme?.sectionDescription, this.indent(Array.isArray(newBody) ? this.renderList(newBody, { indentation: 2, stripAnsi: this.opts.stripAnsi }) : newBody))
        ].join(`
`);
        return this.opts.stripAnsi ? (0, strip_ansi_1.default)(output) : output;
      }
      /**
       * Wrap text according to `opts.maxWidth` which is typically set to the terminal width. All text
       * will be rendered before bring wrapped, otherwise it could mess up the lengths.
       *
       * A terminal will automatically wrap text, so this method is primarily used for indented
       * text. For indented text, specify the indentation so it is taken into account during wrapping.
       *
       * Here is an example of wrapping with indentation.
       * ```
       * <------ terminal window width ------>
       * <---------- no indentation --------->
       * This is my text that will be wrapped
       * once it passes maxWidth.
       *
       * <- indent -><------ text space ----->
       *             This is my text that will
       *             be wrapped once it passes
       *             maxWidth.
       *
       * <-- indent not taken into account ->
       *             This is my text that will
       * be wrapped
       *             once it passes maxWidth.
       * ```
       * @param body the text to wrap
       * @param spacing the indentation size to subtract from the terminal width
       * @returns the formatted wrapped text
       */
      wrap(body, spacing = this.indentSpacing) {
        return (0, wrap_ansi_1.default)(this.render(body), this.opts.maxWidth - spacing, { hard: !0 });
      }
    };
    exports.HelpFormatter = HelpFormatter;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/command.js
var require_command = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/command.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.CommandHelp = void 0;
    var chalk_1 = __importDefault(require_source()), strip_ansi_1 = __importDefault(require_strip_ansi()), theme_1 = require_theme2(), ensure_arg_object_1 = require_ensure_arg_object(), ids_1 = require_ids(), util_1 = require_util(), docopts_1 = require_docopts(), formatter_1 = require_formatter(), POSSIBLE_LINE_FEED = /\r\n|\n/;
    function determineSortOrder(flagSortOrder) {
      return flagSortOrder === "alphabetical" ? "alphabetical" : flagSortOrder === "none" ? "none" : "alphabetical";
    }
    var CommandHelp = class extends formatter_1.HelpFormatter {
      command;
      config;
      opts;
      constructor(command, config, opts) {
        super(config, opts), this.command = command, this.config = config, this.opts = opts;
      }
      aliases(aliases) {
        return !aliases || aliases.length === 0 ? void 0 : aliases.map((a) => [
          (0, theme_1.colorize)(this.config?.theme?.dollarSign, "$"),
          (0, theme_1.colorize)(this.config?.theme?.bin, this.config.bin),
          (0, theme_1.colorize)(this.config?.theme?.alias, a)
        ].join(" ")).join(`
`);
      }
      arg(arg) {
        let name = arg.name.toUpperCase();
        return arg.required ? `${name}` : `[${name}]`;
      }
      args(args) {
        if (args.filter((a) => a.description).length !== 0)
          return args.map((a) => {
            let name = this.command.strict === !1 ? `${a.name.toUpperCase()}...` : a.name.toUpperCase(), description = a.description || "";
            return a.default && (description = `${(0, theme_1.colorize)(this.config?.theme?.flagDefaultValue, `[default: ${a.default}]`)} ${description}`), a.options && (description = `${(0, theme_1.colorize)(this.config?.theme?.flagOptions, `(${a.options.join("|")})`)} ${description}`), [
              (0, theme_1.colorize)(this.config?.theme?.flag, name),
              description ? (0, theme_1.colorize)(this.config?.theme?.sectionDescription, description) : void 0
            ];
          });
      }
      defaultUsage() {
        return this.opts.docopts === void 0 || this.opts.docopts ? docopts_1.DocOpts.generate(this.command) : (0, util_1.compact)([
          this.command.id,
          Object.values(this.command.args ?? {})?.filter((a) => !a.hidden).map((a) => this.arg(a)).join(" ")
        ]).join(" ");
      }
      description() {
        let cmd = this.command, description;
        if (this.opts.hideCommandSummaryInDescription)
          description = (cmd.description || "").split(POSSIBLE_LINE_FEED).slice(1);
        else if (cmd.description) {
          let summary = cmd.summary ? `${cmd.summary}
` : null;
          description = summary ? [...summary.split(POSSIBLE_LINE_FEED), ...(cmd.description || "").split(POSSIBLE_LINE_FEED)] : (cmd.description || "").split(POSSIBLE_LINE_FEED);
        }
        if (description)
          return this.wrap(description.join(`
`));
      }
      examples(examples) {
        return !examples || examples.length === 0 ? void 0 : (0, util_1.castArray)(examples).map((a) => {
          let description, commands;
          if (typeof a == "string") {
            let lines = a.split(POSSIBLE_LINE_FEED).filter(Boolean);
            if (lines.length >= 2 && !this.isCommand(lines[0]) && lines.slice(1).every((i) => this.isCommand(i)))
              description = lines[0], commands = lines.slice(1);
            else
              return lines.map((line) => this.formatIfCommand(line)).join(`
`);
          } else
            description = a.description, commands = [a.command];
          let multilineSeparator = this.config.platform === "win32" ? this.config.shell.includes("powershell") ? "`" : "^" : "\\", finalIndentedSpacing = this.indentSpacing * 2, multilineCommands = commands.map((c) => (
            // First indent keeping room for escaped newlines
            this.indent(this.wrap(this.formatIfCommand(c), finalIndentedSpacing + 4)).split(POSSIBLE_LINE_FEED).join(` ${multilineSeparator}
  `)
          )).join(`
`);
          return `${this.wrap(description, finalIndentedSpacing)}

${multilineCommands}`;
        }).join(`

`);
      }
      flagHelpLabel(flag, showOptions = !1) {
        let label = flag.helpLabel;
        if (!label) {
          let labels = [];
          labels.push(flag.char ? `-${flag.char[0]}` : "  "), flag.name && (flag.type === "boolean" && flag.allowNo ? labels.push(`--[no-]${flag.name.trim()}`) : labels.push(`--${flag.name.trim()}`)), label = labels.join(flag.char ? (0, theme_1.colorize)(this.config?.theme?.flagSeparator, ", ") : "  ");
        }
        if (flag.type === "option") {
          let value = flag.helpValue || (this.opts.showFlagNameInTitle ? flag.name : "<value>");
          !flag.helpValue && flag.options && (value = showOptions || this.opts.showFlagOptionsInTitle ? `${flag.options.join("|")}` : "<option>"), flag.multiple && (value += "..."), value.includes("|") || (value = chalk_1.default.underline(value)), label += `=${value}`;
        }
        return (0, theme_1.colorize)(this.config.theme?.flag, label);
      }
      flags(flags) {
        if (flags.length === 0)
          return;
        let noChar = flags.reduce((previous, current) => previous && current.char === void 0, !0);
        return flags.map((flag) => {
          let left = this.flagHelpLabel(flag);
          noChar && (left = left.replace("    ", ""));
          let right = flag.summary || flag.description || "";
          return flag.type === "option" && flag.default && (right = `${(0, theme_1.colorize)(this.config?.theme?.flagDefaultValue, `[default: ${flag.default}]`)} ${right}`), flag.required && (right = `${(0, theme_1.colorize)(this.config?.theme?.flagRequired, "(required)")} ${right}`), flag.type === "option" && flag.options && !flag.helpValue && !this.opts.showFlagOptionsInTitle && (right += (0, theme_1.colorize)(this.config?.theme?.flagOptions, `
<options: ${flag.options.join("|")}>`)), [left, (0, theme_1.colorize)(this.config?.theme?.sectionDescription, right.trim())];
        });
      }
      flagsDescriptions(flags) {
        let flagsWithExtendedDescriptions = flags.filter((flag) => flag.summary && flag.description);
        return flagsWithExtendedDescriptions.length === 0 ? void 0 : flagsWithExtendedDescriptions.map((flag) => {
          let summary = flag.summary || "", flagHelp = this.flagHelpLabel(flag, !0);
          return flag.char || (flagHelp = flagHelp.replace("    ", "")), flagHelp += flagHelp.length + summary.length + 2 < this.opts.maxWidth ? "  " + summary : `

` + this.indent(this.wrap(summary, this.indentSpacing * 2)), `${flagHelp}

${this.indent(this.wrap(flag.description || "", this.indentSpacing * 2))}`;
        }).join(`

`);
      }
      generate() {
        let cmd = this.command, unsortedFlags = Object.entries(cmd.flags || {}).filter(([, v]) => !v.hidden).map(([k, v]) => (v.name = k, v)), flags = determineSortOrder(this.opts.flagSortOrder) === "alphabetical" ? (0, util_1.sortBy)(unsortedFlags, (f) => [!f.char, f.char, f.name]) : unsortedFlags, args = Object.values((0, ensure_arg_object_1.ensureArgObject)(cmd.args)).filter((a) => !a.hidden);
        return (0, util_1.compact)(this.sections().map(({ generate, header }) => {
          let body = generate({ args, cmd, flags }, header);
          return Array.isArray(body) ? body.map((helpSection) => helpSection && helpSection.body && this.section(helpSection.header, helpSection.body)).join(`

`) : body && this.section(header, body);
        })).join(`

`);
      }
      groupFlags(flags) {
        let mainFlags = [], flagGroups = {};
        for (let flag of flags) {
          let group = flag.helpGroup;
          group ? (flagGroups[group] || (flagGroups[group] = []), flagGroups[group].push(flag)) : mainFlags.push(flag);
        }
        return { flagGroups, mainFlags };
      }
      sections() {
        let sections = [
          {
            generate: () => this.usage(),
            header: this.opts.usageHeader || "USAGE"
          },
          {
            generate: ({ args }, header) => [{ body: this.args(args), header }],
            header: "ARGUMENTS"
          },
          {
            generate: ({ flags }, header) => {
              let { flagGroups, mainFlags } = this.groupFlags(flags), flagSections = [], mainFlagBody = this.flags(mainFlags);
              mainFlagBody && flagSections.push({ body: mainFlagBody, header });
              for (let [name, flags2] of Object.entries(flagGroups)) {
                let body = this.flags(flags2);
                body && flagSections.push({ body, header: `${name.toUpperCase()} ${header}` });
              }
              return (0, util_1.compact)(flagSections);
            },
            header: "FLAGS"
          },
          {
            generate: () => this.description(),
            header: "DESCRIPTION"
          },
          {
            generate: ({ cmd }) => this.aliases(cmd.aliases),
            header: "ALIASES"
          },
          {
            generate: ({ cmd }) => {
              let examples = cmd.examples || cmd.example;
              return this.examples(examples);
            },
            header: "EXAMPLES"
          },
          {
            generate: ({ flags }) => this.flagsDescriptions(flags),
            header: "FLAG DESCRIPTIONS"
          }
        ], allowedSections = this.opts.sections?.map((s) => s.toLowerCase());
        return sections.filter(({ header }) => !allowedSections || allowedSections.includes(header.toLowerCase()));
      }
      usage() {
        let { id, usage } = this.command, standardId = (0, ids_1.toStandardizedId)(id, this.config), configuredId = (0, ids_1.toConfiguredId)(id, this.config);
        return (usage ? (0, util_1.castArray)(usage) : [this.defaultUsage()]).map((u) => {
          let allowedSpacing = this.opts.maxWidth - this.indentSpacing, dollarSign = (0, theme_1.colorize)(this.config?.theme?.dollarSign, "$"), bin = (0, theme_1.colorize)(this.config?.theme?.bin, this.config.bin), command = (0, theme_1.colorize)(this.config?.theme?.command, "<%= command.id %>"), commandDescription = (0, theme_1.colorize)(this.config?.theme?.sectionDescription, u.replace("<%= command.id %>", "").replace(standardId, "").replace(configuredId, "").trim()), line = `${dollarSign} ${bin} ${command} ${commandDescription}`.trim();
          if (line.length > allowedSpacing) {
            let splitIndex = line.slice(0, Math.max(0, allowedSpacing)).lastIndexOf(" ");
            return line.slice(0, Math.max(0, splitIndex)) + `
` + this.indent(this.wrap(line.slice(Math.max(0, splitIndex)), this.indentSpacing * 2));
          }
          return this.wrap(line);
        }).join(`
`);
      }
      formatIfCommand(example) {
        example = this.render(example);
        let dollarSign = (0, theme_1.colorize)(this.config?.theme?.dollarSign, "$");
        return example.startsWith(this.config.bin) ? `${dollarSign} ${example}` : example.startsWith(`$ ${this.config.bin}`) ? `${dollarSign}${example.replace("$", "")}` : example;
      }
      isCommand(example) {
        return (0, strip_ansi_1.default)(this.formatIfCommand(example)).startsWith(`${(0, theme_1.colorize)(this.config?.theme?.dollarSign, "$")} ${this.config.bin}`);
      }
    };
    exports.CommandHelp = CommandHelp;
    exports.default = CommandHelp;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/root.js
var require_root = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/root.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var strip_ansi_1 = __importDefault(require_strip_ansi()), theme_1 = require_theme2(), util_1 = require_util(), formatter_1 = require_formatter(), RootHelp = class extends formatter_1.HelpFormatter {
      config;
      opts;
      constructor(config, opts) {
        super(config, opts), this.config = config, this.opts = opts;
      }
      description() {
        let description = this.config.pjson.oclif.description || this.config.pjson.description || "";
        if (description = this.render(description), description = description.split(`
`).slice(1).join(`
`), !!description)
          return this.section("DESCRIPTION", this.wrap((0, theme_1.colorize)(this.config?.theme?.sectionDescription, description)));
      }
      root() {
        let description = this.config.pjson.oclif.description || this.config.pjson.description || "";
        description = this.render(description), description = description.split(`
`)[0];
        let output = (0, util_1.compact)([
          (0, theme_1.colorize)(this.config?.theme?.commandSummary, description),
          this.version(),
          this.usage(),
          this.description()
        ]).join(`

`);
        return this.opts.stripAnsi && (output = (0, strip_ansi_1.default)(output)), output;
      }
      usage() {
        return this.section(this.opts.usageHeader || "USAGE", this.wrap(`${(0, theme_1.colorize)(this.config?.theme?.dollarSign, "$")} ${(0, theme_1.colorize)(this.config?.theme?.bin, this.config.bin)} ${(0, theme_1.colorize)(this.config?.theme?.sectionDescription, "[COMMAND]")}`));
      }
      version() {
        return this.section("VERSION", this.wrap((0, theme_1.colorize)(this.config?.theme?.version, this.config.userAgent)));
      }
    };
    exports.default = RootHelp;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/index.js
var require_help = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/help/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.loadHelpClass = exports.Help = exports.HelpBase = exports.standardizeIDFromArgv = exports.normalizeArgv = exports.getHelpFlagAdditions = exports.CommandHelp = void 0;
    var node_util_1 = __require("node:util"), strip_ansi_1 = __importDefault(require_strip_ansi()), theme_1 = require_theme2(), write_1 = __importDefault(require_write()), ts_path_1 = require_ts_path(), error_1 = require_error(), module_loader_1 = require_module_loader(), symbols_1 = require_symbols(), cache_default_value_1 = require_cache_default_value(), ids_1 = require_ids(), util_1 = require_util(), command_1 = require_command(), formatter_1 = require_formatter(), root_1 = __importDefault(require_root()), util_2 = require_util3(), command_2 = require_command();
    Object.defineProperty(exports, "CommandHelp", { enumerable: !0, get: function() {
      return command_2.CommandHelp;
    } });
    var util_3 = require_util3();
    Object.defineProperty(exports, "getHelpFlagAdditions", { enumerable: !0, get: function() {
      return util_3.getHelpFlagAdditions;
    } });
    Object.defineProperty(exports, "normalizeArgv", { enumerable: !0, get: function() {
      return util_3.normalizeArgv;
    } });
    Object.defineProperty(exports, "standardizeIDFromArgv", { enumerable: !0, get: function() {
      return util_3.standardizeIDFromArgv;
    } });
    function getHelpSubject(args, config) {
      let mergedHelpFlags = (0, util_2.getHelpFlagAdditions)(config);
      for (let arg of args) {
        if (arg === "--")
          return;
        if (!(mergedHelpFlags.includes(arg) || arg === "help"))
          return arg.startsWith("-") ? void 0 : arg;
      }
    }
    var HelpBase = class extends formatter_1.HelpFormatter {
      constructor(config, opts = {}) {
        super(config, opts), config.topicSeparator || (config.topicSeparator = ":");
      }
    };
    exports.HelpBase = HelpBase;
    var Help = class extends HelpBase {
      CommandHelpClass = command_1.CommandHelp;
      constructor(config, opts = {}) {
        super(config, opts);
      }
      get sortedCommands() {
        let { commands } = this.config;
        return commands = commands.filter((c) => this.opts.all || !c.hidden), commands = (0, util_1.sortBy)(commands, (c) => c.id), commands = (0, util_1.uniqBy)(commands, (c) => c.id), commands;
      }
      get sortedTopics() {
        let topics = this._topics;
        return topics = topics.filter((t) => this.opts.all || !t.hidden), topics = (0, util_1.sortBy)(topics, (t) => t.name), topics = (0, util_1.uniqBy)(topics, (t) => t.name), topics;
      }
      command(command) {
        return this.formatCommand(command);
      }
      description(c) {
        let description = this.render(c.description || "");
        return c.summary ? description : description.split(`
`).slice(1).join(`
`);
      }
      formatCommand(command) {
        return this.config.topicSeparator !== ":" && (command.id = command.id.replaceAll(":", this.config.topicSeparator), command.aliases = command.aliases && command.aliases.map((a) => a.replaceAll(":", this.config.topicSeparator))), this.getCommandHelpClass(command).generate();
      }
      formatCommands(commands) {
        if (commands.length === 0)
          return "";
        let body = this.renderList(commands.filter((c) => this.opts.hideAliasesFromRoot ? !c.aliases?.includes(c.id) : !0).map((c) => {
          this.config.topicSeparator !== ":" && (c.id = c.id.replaceAll(":", this.config.topicSeparator));
          let summary = this.summary(c);
          return [
            (0, theme_1.colorize)(this.config?.theme?.command, c.id),
            summary && (0, theme_1.colorize)(this.config?.theme?.sectionDescription, (0, strip_ansi_1.default)(summary))
          ];
        }), {
          indentation: 2,
          spacer: `
`,
          stripAnsi: this.opts.stripAnsi
        });
        return this.section("COMMANDS", body);
      }
      formatRoot() {
        return new root_1.default(this.config, this.opts).root();
      }
      formatTopic(topic) {
        let description = this.render(topic.description || ""), summary = description.split(`
`)[0];
        description = description.split(`
`).slice(1).join(`
`);
        let topicID = `${topic.name}:COMMAND`;
        this.config.topicSeparator !== ":" && (topicID = topicID.replaceAll(":", this.config.topicSeparator));
        let output = (0, util_1.compact)([
          (0, theme_1.colorize)(this.config?.theme?.commandSummary, summary),
          this.section(this.opts.usageHeader || "USAGE", `${(0, theme_1.colorize)(this.config?.theme?.dollarSign, "$")} ${(0, theme_1.colorize)(this.config?.theme?.bin, this.config.bin)} ${topicID}`),
          description && this.section("DESCRIPTION", this.wrap((0, theme_1.colorize)(this.config?.theme?.sectionDescription, description)))
        ]).join(`

`);
        return this.opts.stripAnsi && (output = (0, strip_ansi_1.default)(output)), output + `
`;
      }
      formatTopics(topics) {
        if (topics.length === 0)
          return "";
        let body = this.renderList(topics.map((c) => (this.config.topicSeparator !== ":" && (c.name = c.name.replaceAll(":", this.config.topicSeparator)), [
          (0, theme_1.colorize)(this.config?.theme?.topic, c.name),
          c.description && this.render((0, theme_1.colorize)(this.config?.theme?.sectionDescription, c.description.split(`
`)[0]))
        ])), {
          indentation: 2,
          spacer: `
`,
          stripAnsi: this.opts.stripAnsi
        });
        return this.section("TOPICS", body);
      }
      getCommandHelpClass(command) {
        return new this.CommandHelpClass(command, this.config, this.opts);
      }
      log(...args) {
        this.opts.sendToStderr ? write_1.default.stderr(node_util_1.format.apply(this, args) + `
`) : write_1.default.stdout(node_util_1.format.apply(this, args) + `
`);
      }
      async showCommandHelp(command) {
        let name = command.id, depth = name.split(":").length, subTopics = this.sortedTopics.filter((t) => t.name.startsWith(name + ":") && t.name.split(":").length === depth + 1), subCommands = this.sortedCommands.filter((c) => c.id.startsWith(name + ":") && c.id.split(":").length === depth + 1), plugin = this.config.plugins.get(command.pluginName), state = this.config.pjson?.oclif?.state || plugin?.pjson?.oclif?.state || command.state;
        if (state && this.log(state === "deprecated" ? `${(0, util_2.formatCommandDeprecationWarning)((0, ids_1.toConfiguredId)(name, this.config), command.deprecationOptions)}
` : `This command is in ${state}.
`), command.deprecateAliases && command.aliases.includes(name)) {
          let actualCmd = this.config.commands.find((c) => c.aliases.includes(name)), opts = { ...command.deprecationOptions, ...actualCmd ? { to: actualCmd.id } : {} };
          this.log(`${(0, util_2.formatCommandDeprecationWarning)((0, ids_1.toConfiguredId)(name, this.config), opts)}
`);
        }
        let summary = this.summary(command);
        if (summary && this.log(summary + `
`), this.log(this.formatCommand(command)), this.log(""), subTopics.length > 0 && (this.log(this.formatTopics(subTopics)), this.log("")), subCommands.length > 0) {
          let aliases = [], uniqueSubCommands = subCommands.filter((p) => (aliases.push(...p.aliases), !aliases.includes(p.id)));
          this.log(this.formatCommands(uniqueSubCommands)), this.log("");
        }
      }
      async showHelp(argv) {
        let originalArgv = argv.slice(1);
        argv = argv.filter((arg) => !(0, util_2.getHelpFlagAdditions)(this.config).includes(arg)), this.config.topicSeparator !== ":" && (argv = (0, util_2.standardizeIDFromArgv)(argv, this.config));
        let subject = getHelpSubject(argv, this.config);
        if (!subject) {
          if (this.config.isSingleCommandCLI) {
            let rootCmd = this.config.findCommand(symbols_1.SINGLE_COMMAND_CLI_SYMBOL);
            if (rootCmd) {
              await this.showCommandHelp(rootCmd);
              return;
            }
          }
          await this.showRootHelp();
          return;
        }
        let command = this.config.findCommand(subject);
        if (command) {
          if (command.id === symbols_1.SINGLE_COMMAND_CLI_SYMBOL && (command.id = ""), command.hasDynamicHelp && command.pluginType !== "jit") {
            let loaded = await command.load();
            for (let [name, flag] of Object.entries(loaded.flags ?? {}))
              flag.type !== "boolean" && (command.flags[name].default = await (0, cache_default_value_1.cacheDefaultValue)(flag, !1));
            await this.showCommandHelp(command);
          } else
            await this.showCommandHelp(command);
          return;
        }
        let topic = this.config.findTopic(subject);
        if (topic) {
          await this.showTopicHelp(topic);
          return;
        }
        if (this.config.flexibleTaxonomy) {
          let matches = this.config.findMatches(subject, originalArgv);
          if (matches.length > 0 && (await this.config.runHook("command_incomplete", {
            argv: originalArgv.filter((o) => !subject.split(":").includes(o)),
            id: subject,
            matches
          })).successes.length > 0)
            return;
        }
        (0, error_1.error)(`Command ${subject} not found.`);
      }
      async showRootHelp() {
        let rootTopics = this.sortedTopics, rootCommands = this.sortedCommands, state = this.config.pjson?.oclif?.state;
        state && this.log(state === "deprecated" ? `${this.config.bin} is deprecated` : `${this.config.bin} is in ${state}.
`), this.log(this.formatRoot()), this.log(""), this.opts.all || (rootTopics = rootTopics.filter((t) => !t.name.includes(":")), rootCommands = rootCommands.filter((c) => !c.id.includes(":"))), rootTopics.length > 0 && (this.log(this.formatTopics(rootTopics)), this.log("")), rootCommands.length > 0 && (rootCommands = rootCommands.filter((c) => c.id), this.log(this.formatCommands(rootCommands)), this.log(""));
      }
      async showTopicHelp(topic) {
        let { name } = topic, depth = name.split(":").length, subTopics = this.sortedTopics.filter((t) => t.name.startsWith(name + ":") && t.name.split(":").length === depth + 1), commands = this.sortedCommands.filter((c) => c.id.startsWith(name + ":") && c.id.split(":").length === depth + 1), state = this.config.pjson?.oclif?.state;
        state && this.log(`This topic is in ${state}.
`), this.log(this.formatTopic(topic)), subTopics.length > 0 && (this.log(this.formatTopics(subTopics)), this.log("")), commands.length > 0 && (this.log(this.formatCommands(commands)), this.log(""));
      }
      summary(c) {
        if (!(this.opts.sections && !this.opts.sections.map((s) => s.toLowerCase()).includes("summary")))
          return c.summary ? (0, theme_1.colorize)(this.config?.theme?.commandSummary, this.render(c.summary.split(`
`)[0])) : c.description && (0, theme_1.colorize)(this.config?.theme?.commandSummary, this.render(c.description).split(`
`)[0]);
      }
      /*
       * _topics is to work around Interfaces.topics mistakenly including commands that do
       * not have children, as well as topics. A topic has children, either commands or other topics. When
       * this is fixed upstream config.topics should return *only* topics with children,
       * and this can be removed.
       */
      get _topics() {
        return this.config.topics.filter((topic) => this.config.topics.some((subTopic) => subTopic.name.includes(`${topic.name}:`)));
      }
    };
    exports.Help = Help;
    function extractClass(exported) {
      return exported && exported.default ? exported.default : exported;
    }
    async function loadHelpClass(config) {
      let { pjson } = config, configuredClass = pjson.oclif?.helpClass;
      if (configuredClass)
        try {
          let path3 = await (0, ts_path_1.tsPath)(config.root, configuredClass) ?? configuredClass, exported = await (0, module_loader_1.load)(config, path3);
          return extractClass(exported);
        } catch (error) {
          throw new Error(`Unable to load configured help class "${configuredClass}", failed with message:
${error.message}`);
        }
      return Help;
    }
    exports.loadHelpClass = loadHelpClass;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/handle.js
var require_handle = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/handle.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.handle = exports.Exit = void 0;
    var clean_stack_1 = __importDefault(require_clean_stack()), cache_1 = __importDefault(require_cache()), index_1 = require_help(), config_1 = require_config(), cli_1 = require_cli(), exit_1 = require_exit(), pretty_print_1 = __importDefault(require_pretty_print());
    exports.Exit = {
      exit(code = 0) {
        process.exit(code);
      }
    };
    async function handle(err) {
      try {
        err || (err = new cli_1.CLIError("no error?")), err.message === "SIGINT" && exports.Exit.exit(1);
        let shouldPrint = !(err instanceof exit_1.ExitError) && !err.skipOclifErrorHandling, pretty = (0, pretty_print_1.default)(err), stack = (0, clean_stack_1.default)(err.stack || "", { pretty: !0 });
        if (shouldPrint) {
          console.error(pretty ?? stack);
          let config = cache_1.default.getInstance().get("config");
          if (err.showHelp && err.parse?.input?.argv && config) {
            let options = {
              ...config.pjson.oclif.helpOptions ?? config.pjson.helpOptions,
              sections: ["flags", "usage", "arguments"],
              sendToStderr: !0
            }, help = new index_1.Help(config, options);
            console.error(), await help.showHelp(process.argv.slice(2));
          }
        }
        let exitCode = err.oclif?.exit ?? 1;
        config_1.config.errorLogger && err.code !== "EEXIT" ? (stack && config_1.config.errorLogger.log(stack), await config_1.config.errorLogger.flush().then(() => exports.Exit.exit(exitCode)).catch(console.error)) : exports.Exit.exit(exitCode);
      } catch (error) {
        console.error(err.stack), console.error(error.stack), exports.Exit.exit(1);
      }
    }
    exports.handle = handle;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/index.js
var require_errors = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/errors/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.warn = exports.exit = exports.Logger = exports.handle = exports.ModuleLoadError = exports.ExitError = exports.CLIError = exports.error = exports.config = void 0;
    var exit_1 = require_exit(), config_1 = require_config();
    Object.defineProperty(exports, "config", { enumerable: !0, get: function() {
      return config_1.config;
    } });
    var error_1 = require_error();
    Object.defineProperty(exports, "error", { enumerable: !0, get: function() {
      return error_1.error;
    } });
    var cli_1 = require_cli();
    Object.defineProperty(exports, "CLIError", { enumerable: !0, get: function() {
      return cli_1.CLIError;
    } });
    var exit_2 = require_exit();
    Object.defineProperty(exports, "ExitError", { enumerable: !0, get: function() {
      return exit_2.ExitError;
    } });
    var module_load_1 = require_module_load();
    Object.defineProperty(exports, "ModuleLoadError", { enumerable: !0, get: function() {
      return module_load_1.ModuleLoadError;
    } });
    var handle_1 = require_handle();
    Object.defineProperty(exports, "handle", { enumerable: !0, get: function() {
      return handle_1.handle;
    } });
    var logger_1 = require_logger();
    Object.defineProperty(exports, "Logger", { enumerable: !0, get: function() {
      return logger_1.Logger;
    } });
    function exit(code = 0) {
      throw new exit_1.ExitError(code);
    }
    exports.exit = exit;
    var warn_1 = require_warn();
    Object.defineProperty(exports, "warn", { enumerable: !0, get: function() {
      return warn_1.warn;
    } });
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/base.js
var require_base = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/base.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.ActionBase = void 0;
    var node_util_1 = __require("node:util"), util_1 = require_util(), ActionBase = class {
      std = "stderr";
      stdmocks;
      type;
      stdmockOrigs = {
        stderr: process.stderr.write,
        stdout: process.stdout.write
      };
      get output() {
        return this.globals.output;
      }
      set output(output) {
        this.globals.output = output;
      }
      get running() {
        return !!this.task;
      }
      get status() {
        return this.task ? this.task.status : void 0;
      }
      set status(status) {
        let { task } = this;
        task && task.status !== status && (this._updateStatus(status, task.status), task.status = status);
      }
      get task() {
        return this.globals.action.task;
      }
      set task(task) {
        this.globals.action.task = task;
      }
      pause(fn, icon) {
        let { task } = this, active = task && task.active;
        task && active && (this._pause(icon), this._stdout(!1), task.active = !1);
        let ret = fn();
        return task && active && this._resume(), ret;
      }
      async pauseAsync(fn, icon) {
        let { task } = this, active = task && task.active;
        task && active && (this._pause(icon), this._stdout(!1), task.active = !1);
        let ret = await fn();
        return task && active && this._resume(), ret;
      }
      start(action, status, opts = {}) {
        this.std = opts.stdout ? "stdout" : "stderr";
        let task = { action, active: !!(this.task && this.task.active), status };
        this.task = task, this._start(opts), task.active = !0, this._stdout(!0);
      }
      stop(msg = "done") {
        let { task } = this;
        task && (this._stop(msg), task.active = !1, this.task = void 0, this._stdout(!1));
      }
      // flush mocked stdout/stderr
      _flushStdout() {
        try {
          let output = "", std;
          for (; this.stdmocks && this.stdmocks.length > 0; ) {
            let cur = this.stdmocks.shift();
            std = cur[0], this._write(std, cur[1]), output += cur[1][0].toString("utf8");
          }
          output && std && output.at(-1) !== `
` && this._write(std, `
`);
        } catch (error) {
          this._write("stderr", (0, node_util_1.inspect)(error));
        }
      }
      _pause(_) {
        throw new Error("not implemented");
      }
      _resume() {
        this.task && this.start(this.task.action, this.task.status);
      }
      _start(_opts) {
        throw new Error("not implemented");
      }
      // mock out stdout/stderr so it doesn't screw up the rendering
      _stdout(toggle) {
        try {
          if (toggle) {
            if (this.stdmocks)
              return;
            this.stdmockOrigs = {
              stderr: process.stderr.write,
              stdout: process.stdout.write
            }, this.stdmocks = [], process.stdout.write = (...args) => (this.stdmocks.push(["stdout", args]), !0), process.stderr.write = (...args) => (this.stdmocks.push(["stderr", args]), !0);
          } else {
            if (!this.stdmocks)
              return;
            delete this.stdmocks, process.stdout.write = this.stdmockOrigs.stdout, process.stderr.write = this.stdmockOrigs.stderr;
          }
        } catch (error) {
          this._write("stderr", (0, node_util_1.inspect)(error));
        }
      }
      _stop(_) {
        throw new Error("not implemented");
      }
      _updateStatus(_, __) {
      }
      // write to the real stdout/stderr
      _write(std, s) {
        switch (std) {
          case "stdout": {
            this.stdmockOrigs.stdout.apply(process.stdout, (0, util_1.castArray)(s));
            break;
          }
          case "stderr": {
            this.stdmockOrigs.stderr.apply(process.stderr, (0, util_1.castArray)(s));
            break;
          }
          default:
            throw new Error(`invalid std: ${std}`);
        }
      }
      get globals() {
        global.ux = global.ux || {};
        let globals = global.ux;
        return globals.action = globals.action || {}, globals;
      }
    };
    exports.ActionBase = ActionBase;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/simple.js
var require_simple = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/simple.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var base_1 = require_base(), SimpleAction = class extends base_1.ActionBase {
      type = "simple";
      _flush() {
        this._write(this.std, `
`), this._flushStdout();
      }
      _pause(icon) {
        icon ? this._updateStatus(icon) : this._flush();
      }
      _render(action, status) {
        this.task && (this.task.active && this._flush(), this._write(this.std, status ? `${action}... ${status}` : `${action}...`));
      }
      _resume() {
      }
      _start() {
        this.task && this._render(this.task.action, this.task.status);
      }
      _stop(status) {
        this.task && this._updateStatus(status, this.task.status, !0);
      }
      _updateStatus(status, prevStatus, newline = !1) {
        this.task && (this.task.active && !prevStatus ? this._write(this.std, ` ${status}`) : this._write(this.std, `${this.task.action}... ${status}`), (newline || !prevStatus) && this._flush());
      }
    };
    exports.default = SimpleAction;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/spinners.js
var require_spinners = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/spinners.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.default = {
      arc: {
        frames: ["\u25DC", "\u25E0", "\u25DD", "\u25DE", "\u25E1", "\u25DF"],
        interval: 100
      },
      arrow: {
        frames: ["\u2190", "\u2196", "\u2191", "\u2197", "\u2192", "\u2198", "\u2193", "\u2199"],
        interval: 100
      },
      arrow2: {
        frames: ["\u2B06\uFE0F ", "\u2197\uFE0F ", "\u27A1\uFE0F ", "\u2198\uFE0F ", "\u2B07\uFE0F ", "\u2199\uFE0F ", "\u2B05\uFE0F ", "\u2196\uFE0F "],
        interval: 80
      },
      arrow3: {
        frames: ["\u25B9\u25B9\u25B9\u25B9\u25B9", "\u25B8\u25B9\u25B9\u25B9\u25B9", "\u25B9\u25B8\u25B9\u25B9\u25B9", "\u25B9\u25B9\u25B8\u25B9\u25B9", "\u25B9\u25B9\u25B9\u25B8\u25B9", "\u25B9\u25B9\u25B9\u25B9\u25B8"],
        interval: 120
      },
      balloon: {
        frames: [" ", ".", "o", "O", "@", "*", " "],
        interval: 140
      },
      balloon2: {
        frames: [".", "o", "O", "\xB0", "O", "o", "."],
        interval: 120
      },
      bounce: {
        frames: ["\u2801", "\u2802", "\u2804", "\u2802"],
        interval: 120
      },
      bouncingBall: {
        frames: [
          "( \u25CF    )",
          "(  \u25CF   )",
          "(   \u25CF  )",
          "(    \u25CF )",
          "(     \u25CF)",
          "(    \u25CF )",
          "(   \u25CF  )",
          "(  \u25CF   )",
          "( \u25CF    )",
          "(\u25CF     )"
        ],
        interval: 80
      },
      bouncingBar: {
        frames: ["[    ]", "[   =]", "[  ==]", "[ ===]", "[====]", "[=== ]", "[==  ]", "[=   ]"],
        interval: 80
      },
      boxBounce: {
        frames: ["\u2596", "\u2598", "\u259D", "\u2597"],
        interval: 120
      },
      boxBounce2: {
        frames: ["\u258C", "\u2580", "\u2590", "\u2584"],
        interval: 100
      },
      circle: {
        frames: ["\u25E1", "\u2299", "\u25E0"],
        interval: 120
      },
      circleHalves: {
        frames: ["\u25D0", "\u25D3", "\u25D1", "\u25D2"],
        interval: 50
      },
      circleQuarters: {
        frames: ["\u25F4", "\u25F7", "\u25F6", "\u25F5"],
        interval: 120
      },
      clock: {
        frames: ["\u{1F550} ", "\u{1F551} ", "\u{1F552} ", "\u{1F553} ", "\u{1F554} ", "\u{1F555} ", "\u{1F556} ", "\u{1F557} ", "\u{1F558} ", "\u{1F559} ", "\u{1F55A} "],
        interval: 100
      },
      dots: {
        frames: ["\u280B", "\u2819", "\u2839", "\u2838", "\u283C", "\u2834", "\u2826", "\u2827", "\u2807", "\u280F"],
        interval: 80
      },
      dots2: {
        frames: ["\u28FE", "\u28FD", "\u28FB", "\u28BF", "\u287F", "\u28DF", "\u28EF", "\u28F7"],
        interval: 80
      },
      dots3: {
        frames: ["\u280B", "\u2819", "\u281A", "\u281E", "\u2816", "\u2826", "\u2834", "\u2832", "\u2833", "\u2813"],
        interval: 80
      },
      dots4: {
        frames: ["\u2804", "\u2806", "\u2807", "\u280B", "\u2819", "\u2838", "\u2830", "\u2820", "\u2830", "\u2838", "\u2819", "\u280B", "\u2807", "\u2806"],
        interval: 80
      },
      dots5: {
        frames: ["\u280B", "\u2819", "\u281A", "\u2812", "\u2802", "\u2802", "\u2812", "\u2832", "\u2834", "\u2826", "\u2816", "\u2812", "\u2810", "\u2810", "\u2812", "\u2813", "\u280B"],
        interval: 80
      },
      dots6: {
        frames: [
          "\u2801",
          "\u2809",
          "\u2819",
          "\u281A",
          "\u2812",
          "\u2802",
          "\u2802",
          "\u2812",
          "\u2832",
          "\u2834",
          "\u2824",
          "\u2804",
          "\u2804",
          "\u2824",
          "\u2834",
          "\u2832",
          "\u2812",
          "\u2802",
          "\u2802",
          "\u2812",
          "\u281A",
          "\u2819",
          "\u2809",
          "\u2801"
        ],
        interval: 80
      },
      dots7: {
        frames: [
          "\u2808",
          "\u2809",
          "\u280B",
          "\u2813",
          "\u2812",
          "\u2810",
          "\u2810",
          "\u2812",
          "\u2816",
          "\u2826",
          "\u2824",
          "\u2820",
          "\u2820",
          "\u2824",
          "\u2826",
          "\u2816",
          "\u2812",
          "\u2810",
          "\u2810",
          "\u2812",
          "\u2813",
          "\u280B",
          "\u2809",
          "\u2808"
        ],
        interval: 80
      },
      dots8: {
        frames: [
          "\u2801",
          "\u2801",
          "\u2809",
          "\u2819",
          "\u281A",
          "\u2812",
          "\u2802",
          "\u2802",
          "\u2812",
          "\u2832",
          "\u2834",
          "\u2824",
          "\u2804",
          "\u2804",
          "\u2824",
          "\u2820",
          "\u2820",
          "\u2824",
          "\u2826",
          "\u2816",
          "\u2812",
          "\u2810",
          "\u2810",
          "\u2812",
          "\u2813",
          "\u280B",
          "\u2809",
          "\u2808",
          "\u2808"
        ],
        interval: 80
      },
      dots9: {
        frames: ["\u28B9", "\u28BA", "\u28BC", "\u28F8", "\u28C7", "\u2867", "\u2857", "\u284F"],
        interval: 80
      },
      dots10: {
        frames: ["\u2884", "\u2882", "\u2881", "\u2841", "\u2848", "\u2850", "\u2860"],
        interval: 80
      },
      dots11: {
        frames: ["\u2801", "\u2802", "\u2804", "\u2840", "\u2880", "\u2820", "\u2810", "\u2808"],
        interval: 100
      },
      earth: {
        frames: ["\u{1F30D} ", "\u{1F30E} ", "\u{1F30F} "],
        interval: 180
      },
      flip: {
        frames: ["_", "_", "_", "-", "`", "`", "'", "\xB4", "-", "_", "_", "_"],
        interval: 70
      },
      growHorizontal: {
        frames: ["\u258F", "\u258E", "\u258D", "\u258C", "\u258B", "\u258A", "\u2589", "\u258A", "\u258B", "\u258C", "\u258D", "\u258E"],
        interval: 120
      },
      growVertical: {
        frames: ["\u2581", "\u2583", "\u2584", "\u2585", "\u2586", "\u2587", "\u2586", "\u2585", "\u2584", "\u2583"],
        interval: 120
      },
      hamburger: {
        frames: ["\u2631", "\u2632", "\u2634"],
        interval: 100
      },
      hearts: {
        frames: ["\u{1F49B} ", "\u{1F499} ", "\u{1F49C} ", "\u{1F49A} ", "\u2764\uFE0F "],
        interval: 100
      },
      hexagon: {
        frames: ["\u2B21", "\u2B22"],
        interval: 400
      },
      line: {
        frames: ["-", "\\", "|", "/"],
        interval: 130
      },
      line2: {
        frames: ["\u2802", "-", "\u2013", "\u2014", "\u2013", "-"],
        interval: 100
      },
      monkey: {
        frames: ["\u{1F648} ", "\u{1F648} ", "\u{1F649} ", "\u{1F64A} "],
        interval: 300
      },
      moon: {
        frames: ["\u{1F311} ", "\u{1F312} ", "\u{1F313} ", "\u{1F314} ", "\u{1F315} ", "\u{1F316} ", "\u{1F317} ", "\u{1F318} "],
        interval: 80
      },
      noise: {
        frames: ["\u2593", "\u2592", "\u2591"],
        interval: 100
      },
      pipe: {
        frames: ["\u2524", "\u2518", "\u2534", "\u2514", "\u251C", "\u250C", "\u252C", "\u2510"],
        interval: 100
      },
      pong: {
        frames: [
          "\u2590\u2802       \u258C",
          "\u2590\u2808       \u258C",
          "\u2590 \u2802      \u258C",
          "\u2590 \u2820      \u258C",
          "\u2590  \u2840     \u258C",
          "\u2590  \u2820     \u258C",
          "\u2590   \u2802    \u258C",
          "\u2590   \u2808    \u258C",
          "\u2590    \u2802   \u258C",
          "\u2590    \u2820   \u258C",
          "\u2590     \u2840  \u258C",
          "\u2590     \u2820  \u258C",
          "\u2590      \u2802 \u258C",
          "\u2590      \u2808 \u258C",
          "\u2590       \u2802\u258C",
          "\u2590       \u2820\u258C",
          "\u2590       \u2840\u258C",
          "\u2590      \u2820 \u258C",
          "\u2590      \u2802 \u258C",
          "\u2590     \u2808  \u258C",
          "\u2590     \u2802  \u258C",
          "\u2590    \u2820   \u258C",
          "\u2590    \u2840   \u258C",
          "\u2590   \u2820    \u258C",
          "\u2590   \u2802    \u258C",
          "\u2590  \u2808     \u258C",
          "\u2590  \u2802     \u258C",
          "\u2590 \u2820      \u258C",
          "\u2590 \u2840      \u258C",
          "\u2590\u2820       \u258C"
        ],
        interval: 80
      },
      runner: {
        frames: ["\u{1F6B6} ", "\u{1F3C3} "],
        interval: 140
      },
      simpleDots: {
        frames: [".  ", ".. ", "...", "   "],
        interval: 400
      },
      simpleDotsScrolling: {
        frames: [".  ", ".. ", "...", " ..", "  .", "   "],
        interval: 200
      },
      smiley: {
        frames: ["\u{1F604} ", "\u{1F61D} "],
        interval: 200
      },
      squareCorners: {
        frames: ["\u25F0", "\u25F3", "\u25F2", "\u25F1"],
        interval: 180
      },
      squish: {
        frames: ["\u256B", "\u256A"],
        interval: 100
      },
      star: {
        frames: ["\u2736", "\u2738", "\u2739", "\u273A", "\u2739", "\u2737"],
        interval: 70
      },
      star2: {
        frames: ["+", "x", "*"],
        interval: 80
      },
      toggle: {
        frames: ["\u22B6", "\u22B7"],
        interval: 250
      },
      toggle2: {
        frames: ["\u25AB", "\u25AA"],
        interval: 80
      },
      toggle3: {
        frames: ["\u25A1", "\u25A0"],
        interval: 120
      },
      toggle4: {
        frames: ["\u25A0", "\u25A1", "\u25AA", "\u25AB"],
        interval: 100
      },
      toggle5: {
        frames: ["\u25AE", "\u25AF"],
        interval: 100
      },
      toggle6: {
        frames: ["\u101D", "\u1040"],
        interval: 300
      },
      toggle7: {
        frames: ["\u29BE", "\u29BF"],
        interval: 80
      },
      toggle8: {
        frames: ["\u25CD", "\u25CC"],
        interval: 100
      },
      toggle9: {
        frames: ["\u25C9", "\u25CE"],
        interval: 100
      },
      toggle10: {
        frames: ["\u3282", "\u3280", "\u3281"],
        interval: 100
      },
      toggle11: {
        frames: ["\u29C7", "\u29C6"],
        interval: 50
      },
      toggle12: {
        frames: ["\u2617", "\u2616"],
        interval: 120
      },
      toggle13: {
        frames: ["=", "*", "-"],
        interval: 80
      },
      triangle: {
        frames: ["\u25E2", "\u25E3", "\u25E4", "\u25E5"],
        interval: 50
      }
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/spinner.js
var require_spinner = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/action/spinner.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var ansi_styles_1 = __importDefault(require_ansi_styles()), chalk_1 = __importDefault(require_source()), strip_ansi_1 = __importDefault(require_strip_ansi()), supportsColor = __importStar(require_supports_color()), screen_1 = require_screen(), base_1 = require_base(), spinners_1 = __importDefault(require_spinners()), ansiEscapes = require_ansi_escapes();
    function color(s) {
      return supportsColor ? (supportsColor.stdout ? supportsColor.stdout.has256 : (process.env.TERM || "").includes("256")) ? `\x1B[38;5;104m${s}${ansi_styles_1.default.reset.open}` : chalk_1.default.magenta(s) : s;
    }
    var SpinnerAction = class extends base_1.ActionBase {
      frameIndex;
      frames;
      spinner;
      type = "spinner";
      constructor() {
        super(), this.frames = this.getFrames(), this.frameIndex = 0;
      }
      _frame() {
        let frame = this.frames[this.frameIndex];
        return this.frameIndex = ++this.frameIndex % this.frames.length, color(frame);
      }
      _lines(s) {
        return (0, strip_ansi_1.default)(s).split(`
`).map((l) => Math.ceil(l.length / screen_1.errtermwidth)).reduce((c, i) => c + i, 0);
      }
      _pause(icon) {
        this.spinner && clearInterval(this.spinner), this._reset(), icon && this._render(` ${icon}`), this.output = void 0;
      }
      _render(icon) {
        if (!this.task)
          return;
        this._reset(), this._flushStdout();
        let frame = icon === "spinner" ? ` ${this._frame()}` : icon || "", status = this.task.status ? ` ${this.task.status}` : "";
        this.output = `${this.task.action}...${frame}${status}
`, this._write(this.std, this.output);
      }
      _reset() {
        if (!this.output)
          return;
        let lines = this._lines(this.output);
        this._write(this.std, ansiEscapes.cursorLeft + ansiEscapes.cursorUp(lines) + ansiEscapes.eraseDown), this.output = void 0;
      }
      _start(opts) {
        opts.style && (this.frames = this.getFrames(opts)), this._reset(), this.spinner && clearInterval(this.spinner), this._render(), this.spinner = setInterval((icon) => this._render.bind(this)(icon), process.platform === "win32" ? 500 : 100, "spinner"), this.spinner.unref();
      }
      _stop(status) {
        this.task && (this.task.status = status), this.spinner && clearInterval(this.spinner), this._render(), this.output = void 0;
      }
      getFrames(opts) {
        return opts?.style ? spinners_1.default[opts.style].frames : spinners_1.default[process.platform === "win32" ? "line" : "dots2"].frames;
      }
    };
    exports.default = SpinnerAction;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/config.js
var require_config2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/config.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.config = exports.Config = void 0;
    var cache_1 = __importDefault(require_cache()), simple_1 = __importDefault(require_simple()), spinner_1 = __importDefault(require_spinner()), g = global, globals = g.ux || (g.ux = {}), actionType = !!process.stderr.isTTY && !process.env.CI && !["dumb", "emacs-color"].includes(process.env.TERM) && "spinner" || "simple", Action = actionType === "spinner" ? spinner_1.default : simple_1.default, Config = class {
      action = new Action();
      errorsHandled = !1;
      outputLevel = "info";
      showStackTrace = !0;
      get context() {
        return globals.context || {};
      }
      set context(v) {
        globals.context = v;
      }
      get debug() {
        return globals.debug || process.env.DEBUG === "*";
      }
      set debug(v) {
        globals.debug = v;
      }
    };
    exports.Config = Config;
    function fetch() {
      let major = cache_1.default.getInstance().get("@oclif/core")?.version.split(".")[0] || "unknown";
      return globals[major] || (globals[major] = new Config()), globals[major];
    }
    exports.config = fetch();
    exports.default = exports.config;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/flush.js
var require_flush = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/flush.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.flush = void 0;
    var errors_1 = require_errors();
    function timeout(p, ms) {
      function wait(ms2, unref = !1) {
        return new Promise((resolve2) => {
          let t = setTimeout(() => resolve2(null), ms2);
          unref && t.unref();
        });
      }
      return Promise.race([p, wait(ms, !0).then(() => (0, errors_1.error)("timed out"))]);
    }
    async function _flush() {
      let p = new Promise((resolve2) => {
        process.stdout.once("drain", () => resolve2(null));
      });
      if (!process.stdout.write(""))
        return p;
    }
    async function flush(ms = 1e4) {
      await timeout(_flush(), ms);
    }
    exports.flush = flush;
  }
});

// ../../node_modules/.pnpm/password-prompt@1.1.3/node_modules/password-prompt/index.js
var require_password_prompt = __commonJS({
  "../../node_modules/.pnpm/password-prompt@1.1.3/node_modules/password-prompt/index.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var stdin = process.stdin, stderr = process.stderr, read = {
      hide: (ask, options = {}) => read.raw(ask, !1, options),
      mask: (ask, options = {}) => read.raw(ask, !0, options),
      raw: (ask, maskAfter, options = {}) => !stdin.setRawMode || process.env.TERM === "dumb" ? read.notty(ask) : new Promise(function(resolve2, reject) {
        let ansi = require_ansi_escapes(), input = "";
        stderr.write(ansi.eraseLine), stderr.write(ansi.cursorLeft), stderr.write(ask), stdin.resume(), stdin.setRawMode(!0);
        function stop() {
          maskAfter ? stderr.write(ansi.cursorHide + ansi.cursorLeft + ask + input.replace(/./g, "*") + `
` + ansi.cursorShow) : stderr.write(`
`), stdin.removeListener("data", fn), stdin.setRawMode(!1), stdin.pause();
        }
        function enter() {
          options.required && input.length === 0 || (stop(), input = input.replace(/\r$/, ""), input = input || options.default, resolve2(input));
        }
        function ctrlc() {
          reject(new Error("SIGINT")), stop();
        }
        function backspace() {
          input.length !== 0 && (input = input.substr(0, input.length - 1), stderr.write(ansi.cursorBackward(1)), stderr.write(ansi.eraseEndLine));
        }
        function newchar(c) {
          input += c, stderr.write(maskAfter ? c : "*".repeat(c.length));
        }
        let fn = function(c) {
          switch (c) {
            case "":
            // Ctrl-d
            case "\r":
            case `
`:
              return enter();
            case "":
              return ctrlc();
            default:
              return c.charCodeAt(0) === 127 ? backspace() : newchar(c);
          }
        };
        stdin.on("data", fn);
      }),
      notty: (ask) => new Promise((resolve2, reject) => {
        let spawn2 = require_cross_spawn();
        stderr.write(ask);
        let output = spawn2.sync("sh", ["-c", "read -s PASS && echo $PASS"], {
          stdio: ["inherit", "pipe", "inherit"],
          encoding: "utf8"
        });
        if (stderr.write(`
`), output.error) return reject(output.error);
        resolve2(output.stdout.trim());
      })
    };
    function prompt(ask, options = {}) {
      return options = Object.assign(
        {
          method: "mask",
          required: options.default === void 0,
          default: ""
        },
        options
      ), stdin.setEncoding("utf8"), read[options.method](ask, options).then((input) => input || (options.required ? prompt(ask) : ""));
    }
    module.exports = prompt;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/prompt.js
var require_prompt = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/prompt.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.anykey = exports.confirm = exports.prompt = void 0;
    var chalk_1 = __importDefault(require_source()), Errors = __importStar(require_errors()), config_1 = require_config2();
    function normal(options, retries = 100) {
      if (retries < 0)
        throw new Error("no input");
      return new Promise((resolve2, reject) => {
        let timer;
        options.timeout && (timer = setTimeout(() => {
          process.stdin.pause(), reject(new Error("Prompt timeout"));
        }, options.timeout), timer.unref()), process.stdin.setEncoding("utf8"), process.stderr.write(options.prompt), process.stdin.resume(), process.stdin.once("data", (b) => {
          timer && clearTimeout(timer), process.stdin.pause();
          let data = (typeof b == "string" ? b : b.toString()).trim();
          !options.default && options.required && data === "" ? resolve2(normal(options, retries - 1)) : resolve2(data || options.default);
        });
      });
    }
    function getPrompt(name, type, defaultValue) {
      let prompt2 = "> ";
      return defaultValue && type === "hide" && (defaultValue = "*".repeat(defaultValue.length)), name && defaultValue ? prompt2 = name + " " + chalk_1.default.yellow("[" + defaultValue + "]") + ": " : name && (prompt2 = `${name}: `), prompt2;
    }
    async function single(options) {
      let raw = process.stdin.isRaw;
      process.stdin.setRawMode && process.stdin.setRawMode(!0), options.required = options.required ?? !1;
      let response = await normal(options);
      return process.stdin.setRawMode && process.stdin.setRawMode(!!raw), response;
    }
    function replacePrompt(prompt2) {
      let ansiEscapes = require_ansi_escapes();
      process.stderr.write(ansiEscapes.cursorHide + ansiEscapes.cursorUp(1) + ansiEscapes.cursorLeft + prompt2 + ansiEscapes.cursorDown(1) + ansiEscapes.cursorLeft + ansiEscapes.cursorShow);
    }
    async function _prompt(name, inputOptions = {}) {
      let prompt2 = getPrompt(name, inputOptions.type, inputOptions.default), options = {
        default: "",
        isTTY: !!(process.env.TERM !== "dumb" && process.stdin.isTTY),
        name,
        prompt: prompt2,
        required: !0,
        type: "normal",
        ...inputOptions
      }, passwordPrompt = require_password_prompt();
      switch (options.type) {
        case "normal":
          return normal(options);
        case "single":
          return single(options);
        case "mask":
          return passwordPrompt(options.prompt, {
            default: options.default,
            method: options.type,
            required: options.required
          }).then((value) => (replacePrompt(getPrompt(name, "hide", inputOptions.default)), value));
        case "hide":
          return passwordPrompt(options.prompt, {
            default: options.default,
            method: options.type,
            required: options.required
          });
        default:
          throw new Error(`unexpected type ${options.type}`);
      }
    }
    async function prompt(name, options = {}) {
      return config_1.config.action.pauseAsync(() => _prompt(name, options), chalk_1.default.cyan("?"));
    }
    exports.prompt = prompt;
    function confirm(message) {
      return config_1.config.action.pauseAsync(async () => {
        let confirm2 = async () => {
          let response = (await _prompt(message)).toLowerCase();
          return ["n", "no"].includes(response) ? !1 : ["y", "yes"].includes(response) ? !0 : confirm2();
        };
        return confirm2();
      }, chalk_1.default.cyan("?"));
    }
    exports.confirm = confirm;
    async function anykey(message) {
      let tty = !!process.stdin.setRawMode;
      message || (message = tty ? `Press any key to continue or ${chalk_1.default.yellow("q")} to exit` : `Press enter to continue or ${chalk_1.default.yellow("q")} to exit`);
      let char = await prompt(message, { required: !1, type: "single" });
      return tty && process.stderr.write(`
`), char === "q" && Errors.error("quit"), char === "" && Errors.error("ctrl-c"), char;
    }
    exports.anykey = anykey;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/object.js
var require_object = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/object.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var chalk_1 = __importDefault(require_source()), node_util_1 = __require("node:util");
    function styledObject(obj, keys) {
      let output = [], keyLengths = Object.keys(obj).map((key) => key.toString().length), maxKeyLength = Math.max(...keyLengths) + 2;
      function pp(obj2) {
        return typeof obj2 == "string" || typeof obj2 == "number" ? obj2 : typeof obj2 == "object" ? Object.keys(obj2).map((k) => k + ": " + (0, node_util_1.inspect)(obj2[k])).join(", ") : (0, node_util_1.inspect)(obj2);
      }
      let logKeyValue = (key, value) => `${chalk_1.default.blue(key)}:` + " ".repeat(maxKeyLength - key.length - 1) + pp(value);
      for (let key of keys || Object.keys(obj).sort()) {
        let value = obj[key];
        if (Array.isArray(value)) {
          if (value.length > 0) {
            output.push(logKeyValue(key, value[0]));
            for (let e of value.slice(1))
              output.push(" ".repeat(maxKeyLength) + pp(e));
          }
        } else value != null && output.push(logKeyValue(key, value));
      }
      return output.join(`
`);
    }
    exports.default = styledObject;
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/eta.js
var require_eta = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/eta.js"(exports, module) {
    init_cjs_shims();
    var ETA = class {
      constructor(length, initTime, initValue) {
        this.etaBufferLength = length || 100, this.valueBuffer = [initValue], this.timeBuffer = [initTime], this.eta = "0";
      }
      // add new values to calculation buffer
      update(time, value, total) {
        this.valueBuffer.push(value), this.timeBuffer.push(time), this.calculate(total - value);
      }
      // fetch estimated time
      getTime() {
        return this.eta;
      }
      // eta calculation - request number of remaining events
      calculate(remaining) {
        let currentBufferSize = this.valueBuffer.length, buffer = Math.min(this.etaBufferLength, currentBufferSize), v_diff = this.valueBuffer[currentBufferSize - 1] - this.valueBuffer[currentBufferSize - buffer], t_diff = this.timeBuffer[currentBufferSize - 1] - this.timeBuffer[currentBufferSize - buffer], vt_rate = v_diff / t_diff;
        this.valueBuffer = this.valueBuffer.slice(-this.etaBufferLength), this.timeBuffer = this.timeBuffer.slice(-this.etaBufferLength);
        let eta = Math.ceil(remaining / vt_rate / 1e3);
        isNaN(eta) ? this.eta = "NULL" : isFinite(eta) ? eta > 1e7 ? this.eta = "INF" : eta < 0 ? this.eta = 0 : this.eta = eta : this.eta = "INF";
      }
    };
    module.exports = ETA;
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/terminal.js
var require_terminal = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/terminal.js"(exports, module) {
    init_cjs_shims();
    var _readline = __require("readline"), Terminal = class {
      constructor(outputStream) {
        this.stream = outputStream, this.linewrap = !0, this.dy = 0;
      }
      // save cursor position + settings
      cursorSave() {
        this.stream.isTTY && this.stream.write("\x1B7");
      }
      // restore last cursor position + settings
      cursorRestore() {
        this.stream.isTTY && this.stream.write("\x1B8");
      }
      // show/hide cursor
      cursor(enabled) {
        this.stream.isTTY && (enabled ? this.stream.write("\x1B[?25h") : this.stream.write("\x1B[?25l"));
      }
      // change cursor positionn
      cursorTo(x = null, y = null) {
        this.stream.isTTY && _readline.cursorTo(this.stream, x, y);
      }
      // change relative cursor position
      cursorRelative(dx = null, dy = null) {
        this.stream.isTTY && (this.dy = this.dy + dy, _readline.moveCursor(this.stream, dx, dy));
      }
      // relative reset
      cursorRelativeReset() {
        this.stream.isTTY && (_readline.moveCursor(this.stream, 0, -this.dy), _readline.cursorTo(this.stream, 0, null), this.dy = 0);
      }
      // clear to the right from cursor
      clearRight() {
        this.stream.isTTY && _readline.clearLine(this.stream, 1);
      }
      // clear the full line
      clearLine() {
        this.stream.isTTY && _readline.clearLine(this.stream, 0);
      }
      // clear everyting beyond the current line
      clearBottom() {
        this.stream.isTTY && _readline.clearScreenDown(this.stream);
      }
      // add new line; increment counter
      newline() {
        this.stream.write(`
`), this.dy++;
      }
      // write content to output stream
      // @TODO use string-width to strip length
      write(s, rawWrite = !1) {
        this.linewrap === !0 && rawWrite === !1 ? this.stream.write(s.substr(0, this.getWidth())) : this.stream.write(s);
      }
      // control line wrapping
      lineWrapping(enabled) {
        this.stream.isTTY && (this.linewrap = enabled, enabled ? this.stream.write("\x1B[?7h") : this.stream.write("\x1B[?7l"));
      }
      // tty environment ?
      isTTY() {
        return this.stream.isTTY === !0;
      }
      // get terminal width
      getWidth() {
        return this.stream.columns || (this.stream.isTTY ? 80 : 200);
      }
    };
    module.exports = Terminal;
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/format-value.js
var require_format_value = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/format-value.js"(exports, module) {
    init_cjs_shims();
    module.exports = function(v, options, type) {
      if (options.autopadding !== !0)
        return v;
      function autopadding(value, length) {
        return (options.autopaddingChar + value).slice(-length);
      }
      return type === "percentage" ? autopadding(v, 3) : v;
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/format-bar.js
var require_format_bar = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/format-bar.js"(exports, module) {
    init_cjs_shims();
    module.exports = function(progress, options) {
      let completeSize = Math.round(progress * options.barsize), incompleteSize = options.barsize - completeSize;
      return options.barCompleteString.substr(0, completeSize) + options.barGlue + options.barIncompleteString.substr(0, incompleteSize);
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/format-time.js
var require_format_time = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/format-time.js"(exports, module) {
    init_cjs_shims();
    module.exports = function(t, options, roundToMultipleOf) {
      function round(input) {
        return roundToMultipleOf ? roundToMultipleOf * Math.round(input / roundToMultipleOf) : input;
      }
      function autopadding(v) {
        return (options.autopaddingChar + v).slice(-2);
      }
      return t > 3600 ? autopadding(Math.floor(t / 3600)) + "h" + autopadding(round(t % 3600 / 60)) + "m" : t > 60 ? autopadding(Math.floor(t / 60)) + "m" + autopadding(round(t % 60)) + "s" : t > 10 ? autopadding(round(t)) + "s" : autopadding(t) + "s";
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/formatter.js
var require_formatter2 = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/formatter.js"(exports, module) {
    init_cjs_shims();
    var _stringWidth = require_string_width(), _defaultFormatValue = require_format_value(), _defaultFormatBar = require_format_bar(), _defaultFormatTime = require_format_time();
    module.exports = function(options, params, payload) {
      let s = options.format, formatTime = options.formatTime || _defaultFormatTime, formatValue = options.formatValue || _defaultFormatValue, formatBar = options.formatBar || _defaultFormatBar, percentage = Math.floor(params.progress * 100) + "", stopTime = params.stopTime || Date.now(), elapsedTime = Math.round((stopTime - params.startTime) / 1e3), context = Object.assign({}, payload, {
        bar: formatBar(params.progress, options),
        percentage: formatValue(percentage, options, "percentage"),
        total: formatValue(params.total, options, "total"),
        value: formatValue(params.value, options, "value"),
        eta: formatValue(params.eta, options, "eta"),
        eta_formatted: formatTime(params.eta, options, 5),
        duration: formatValue(elapsedTime, options, "duration"),
        duration_formatted: formatTime(elapsedTime, options, 1)
      });
      s = s.replace(/\{(\w+)\}/g, function(match, key) {
        return typeof context[key] < "u" ? context[key] : match;
      });
      let fullMargin = Math.max(0, params.maxWidth - _stringWidth(s) - 2), halfMargin = Math.floor(fullMargin / 2);
      switch (options.align) {
        // fill start-of-line with whitespaces
        case "right":
          s = fullMargin > 0 ? " ".repeat(fullMargin) + s : s;
          break;
        // distribute whitespaces to left+right
        case "center":
          s = halfMargin > 0 ? " ".repeat(halfMargin) + s : s;
          break;
        default:
          break;
      }
      return s;
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/options.js
var require_options = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/options.js"(exports, module) {
    init_cjs_shims();
    function mergeOption(v, defaultValue) {
      return typeof v > "u" || v === null ? defaultValue : v;
    }
    module.exports = {
      // set global options
      parse: function(rawOptions, preset) {
        let options = {}, opt = Object.assign({}, preset, rawOptions);
        return options.throttleTime = 1e3 / mergeOption(opt.fps, 10), options.stream = mergeOption(opt.stream, process.stderr), options.terminal = mergeOption(opt.terminal, null), options.clearOnComplete = mergeOption(opt.clearOnComplete, !1), options.stopOnComplete = mergeOption(opt.stopOnComplete, !1), options.barsize = mergeOption(opt.barsize, 40), options.align = mergeOption(opt.align, "left"), options.hideCursor = mergeOption(opt.hideCursor, !1), options.linewrap = mergeOption(opt.linewrap, !1), options.barGlue = mergeOption(opt.barGlue, ""), options.barCompleteChar = mergeOption(opt.barCompleteChar, "="), options.barIncompleteChar = mergeOption(opt.barIncompleteChar, "-"), options.format = mergeOption(opt.format, "progress [{bar}] {percentage}% | ETA: {eta}s | {value}/{total}"), options.formatTime = mergeOption(opt.formatTime, null), options.formatValue = mergeOption(opt.formatValue, null), options.formatBar = mergeOption(opt.formatBar, null), options.etaBufferLength = mergeOption(opt.etaBuffer, 10), options.etaAsynchronousUpdate = mergeOption(opt.etaAsynchronousUpdate, !1), options.progressCalculationRelative = mergeOption(opt.progressCalculationRelative, !1), options.synchronousUpdate = mergeOption(opt.synchronousUpdate, !0), options.noTTYOutput = mergeOption(opt.noTTYOutput, !1), options.notTTYSchedule = mergeOption(opt.notTTYSchedule, 2e3), options.emptyOnZero = mergeOption(opt.emptyOnZero, !1), options.forceRedraw = mergeOption(opt.forceRedraw, !1), options.autopadding = mergeOption(opt.autopadding, !1), options.gracefulExit = mergeOption(opt.gracefulExit, !1), options;
      },
      // derived options: instance specific, has to be created for every bar element
      assignDerivedOptions: function(options) {
        return options.barCompleteString = options.barCompleteChar.repeat(options.barsize + 1), options.barIncompleteString = options.barIncompleteChar.repeat(options.barsize + 1), options.autopaddingChar = options.autopadding ? mergeOption(options.autopaddingChar, "   ") : "", options;
      }
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/generic-bar.js
var require_generic_bar = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/generic-bar.js"(exports, module) {
    init_cjs_shims();
    var _ETA = require_eta(), _Terminal = require_terminal(), _formatter = require_formatter2(), _options = require_options(), _EventEmitter = __require("events");
    module.exports = class extends _EventEmitter {
      constructor(options) {
        super(), this.options = _options.assignDerivedOptions(options), this.terminal = this.options.terminal ? this.options.terminal : new _Terminal(this.options.stream), this.value = 0, this.startValue = 0, this.total = 100, this.lastDrawnString = null, this.startTime = null, this.stopTime = null, this.lastRedraw = Date.now(), this.eta = new _ETA(this.options.etaBufferLength, 0, 0), this.payload = {}, this.isActive = !1, this.formatter = typeof this.options.format == "function" ? this.options.format : _formatter;
      }
      // internal render function
      render(forceRendering = !1) {
        let params = {
          progress: this.getProgress(),
          eta: this.eta.getTime(),
          startTime: this.startTime,
          stopTime: this.stopTime,
          total: this.total,
          value: this.value,
          maxWidth: this.terminal.getWidth()
        };
        this.options.etaAsynchronousUpdate && this.updateETA();
        let s = this.formatter(this.options, params, this.payload);
        (forceRendering || this.options.forceRedraw || this.options.noTTYOutput && !this.terminal.isTTY() || this.lastDrawnString != s) && (this.emit("redraw-pre"), this.terminal.cursorTo(0, null), this.terminal.write(s), this.terminal.clearRight(), this.lastDrawnString = s, this.lastRedraw = Date.now(), this.emit("redraw-post"));
      }
      // start the progress bar
      start(total, startValue, payload) {
        this.value = startValue || 0, this.total = typeof total < "u" && total >= 0 ? total : 100, this.startValue = startValue || 0, this.payload = payload || {}, this.startTime = Date.now(), this.stopTime = null, this.lastDrawnString = "", this.eta = new _ETA(this.options.etaBufferLength, this.startTime, this.value), this.isActive = !0, this.emit("start", total, startValue);
      }
      // stop the bar
      stop() {
        this.isActive = !1, this.stopTime = Date.now(), this.emit("stop", this.total, this.value);
      }
      // update the bar value
      // update(value, payload)
      // update(payload)
      update(arg0, arg1 = {}) {
        typeof arg0 == "number" && (this.value = arg0, this.eta.update(Date.now(), arg0, this.total));
        let payloadData = (typeof arg0 == "object" ? arg0 : arg1) || {};
        this.emit("update", this.total, this.value);
        for (let key in payloadData)
          this.payload[key] = payloadData[key];
        this.value >= this.getTotal() && this.options.stopOnComplete && this.stop();
      }
      // calculate the actual progress value
      getProgress() {
        let progress = this.value / this.total;
        return this.options.progressCalculationRelative && (progress = (this.value - this.startValue) / (this.total - this.startValue)), isNaN(progress) && (progress = this.options && this.options.emptyOnZero ? 0 : 1), progress = Math.min(Math.max(progress, 0), 1), progress;
      }
      // update the bar value
      // increment(delta, payload)
      // increment(payload)
      increment(arg0 = 1, arg1 = {}) {
        typeof arg0 == "object" ? this.update(this.value + 1, arg0) : this.update(this.value + arg0, arg1);
      }
      // get the total (limit) value
      getTotal() {
        return this.total;
      }
      // set the total (limit) value
      setTotal(total) {
        typeof total < "u" && total >= 0 && (this.total = total);
      }
      // force eta calculation update (long running processes)
      updateETA() {
        this.eta.update(Date.now(), this.value, this.total);
      }
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/single-bar.js
var require_single_bar = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/single-bar.js"(exports, module) {
    init_cjs_shims();
    var _GenericBar = require_generic_bar(), _options = require_options();
    module.exports = class extends _GenericBar {
      constructor(options, preset) {
        super(_options.parse(options, preset)), this.timer = null, this.options.noTTYOutput && this.terminal.isTTY() === !1 && (this.options.synchronousUpdate = !1), this.schedulingRate = this.terminal.isTTY() ? this.options.throttleTime : this.options.notTTYSchedule, this.sigintCallback = null;
      }
      // internal render function
      render() {
        this.timer && (clearTimeout(this.timer), this.timer = null), super.render(), this.options.noTTYOutput && this.terminal.isTTY() === !1 && this.terminal.newline(), this.timer = setTimeout(this.render.bind(this), this.schedulingRate);
      }
      update(current, payload) {
        this.timer && (super.update(current, payload), this.options.synchronousUpdate && this.lastRedraw + this.options.throttleTime * 2 < Date.now() && this.render());
      }
      // start the progress bar
      start(total, startValue, payload) {
        this.options.noTTYOutput === !1 && this.terminal.isTTY() === !1 || (this.sigintCallback === null && this.options.gracefulExit && (this.sigintCallback = this.stop.bind(this), process.once("SIGINT", this.sigintCallback), process.once("SIGTERM", this.sigintCallback)), this.terminal.cursorSave(), this.options.hideCursor === !0 && this.terminal.cursor(!1), this.options.linewrap === !1 && this.terminal.lineWrapping(!1), super.start(total, startValue, payload), this.render());
      }
      // stop the bar
      stop() {
        this.timer && (this.sigintCallback && (process.removeListener("SIGINT", this.sigintCallback), process.removeListener("SIGTERM", this.sigintCallback), this.sigintCallback = null), this.render(), super.stop(), clearTimeout(this.timer), this.timer = null, this.options.hideCursor === !0 && this.terminal.cursor(!0), this.options.linewrap === !1 && this.terminal.lineWrapping(!0), this.terminal.cursorRestore(), this.options.clearOnComplete ? (this.terminal.cursorTo(0, null), this.terminal.clearLine()) : this.terminal.newline());
      }
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/multi-bar.js
var require_multi_bar = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/lib/multi-bar.js"(exports, module) {
    init_cjs_shims();
    var _Terminal = require_terminal(), _BarElement = require_generic_bar(), _options = require_options(), _EventEmitter = __require("events");
    module.exports = class extends _EventEmitter {
      constructor(options, preset) {
        super(), this.bars = [], this.options = _options.parse(options, preset), this.options.synchronousUpdate = !1, this.terminal = this.options.terminal ? this.options.terminal : new _Terminal(this.options.stream), this.timer = null, this.isActive = !1, this.schedulingRate = this.terminal.isTTY() ? this.options.throttleTime : this.options.notTTYSchedule, this.loggingBuffer = [], this.sigintCallback = null;
      }
      // add a new bar to the stack
      create(total, startValue, payload, barOptions = {}) {
        let bar = new _BarElement(Object.assign(
          {},
          // global options
          this.options,
          // terminal instance
          {
            terminal: this.terminal
          },
          // overrides
          barOptions
        ));
        return this.bars.push(bar), this.options.noTTYOutput === !1 && this.terminal.isTTY() === !1 || (this.sigintCallback === null && this.options.gracefulExit && (this.sigintCallback = this.stop.bind(this), process.once("SIGINT", this.sigintCallback), process.once("SIGTERM", this.sigintCallback)), this.isActive || (this.options.hideCursor === !0 && this.terminal.cursor(!1), this.options.linewrap === !1 && this.terminal.lineWrapping(!1), this.timer = setTimeout(this.update.bind(this), this.schedulingRate)), this.isActive = !0, bar.start(total, startValue, payload), this.emit("start")), bar;
      }
      // remove a bar from the stack
      remove(bar) {
        let index = this.bars.indexOf(bar);
        return index < 0 ? !1 : (this.bars.splice(index, 1), this.update(), this.terminal.newline(), this.terminal.clearBottom(), !0);
      }
      // internal update routine
      update() {
        if (this.timer && (clearTimeout(this.timer), this.timer = null), this.emit("update-pre"), this.terminal.cursorRelativeReset(), this.emit("redraw-pre"), this.loggingBuffer.length > 0)
          for (this.terminal.clearLine(); this.loggingBuffer.length > 0; )
            this.terminal.write(this.loggingBuffer.shift(), !0);
        for (let i = 0; i < this.bars.length; i++)
          i > 0 && this.terminal.newline(), this.bars[i].render();
        this.emit("redraw-post"), this.options.noTTYOutput && this.terminal.isTTY() === !1 && (this.terminal.newline(), this.terminal.newline()), this.timer = setTimeout(this.update.bind(this), this.schedulingRate), this.emit("update-post"), this.options.stopOnComplete && !this.bars.find((bar) => bar.isActive) && this.stop();
      }
      stop() {
        if (clearTimeout(this.timer), this.timer = null, this.sigintCallback && (process.removeListener("SIGINT", this.sigintCallback), process.removeListener("SIGTERM", this.sigintCallback), this.sigintCallback = null), this.isActive = !1, this.options.hideCursor === !0 && this.terminal.cursor(!0), this.options.linewrap === !1 && this.terminal.lineWrapping(!0), this.terminal.cursorRelativeReset(), this.emit("stop-pre-clear"), this.options.clearOnComplete)
          this.terminal.clearBottom();
        else {
          for (let i = 0; i < this.bars.length; i++)
            i > 0 && this.terminal.newline(), this.bars[i].render(), this.bars[i].stop();
          this.terminal.newline();
        }
        this.emit("stop");
      }
      log(s) {
        this.loggingBuffer.push(s);
      }
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/legacy.js
var require_legacy = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/legacy.js"(exports, module) {
    init_cjs_shims();
    module.exports = {
      format: "progress [{bar}] {percentage}% | ETA: {eta}s | {value}/{total}",
      barCompleteChar: "=",
      barIncompleteChar: "-"
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/shades-classic.js
var require_shades_classic = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/shades-classic.js"(exports, module) {
    init_cjs_shims();
    module.exports = {
      format: " {bar} {percentage}% | ETA: {eta}s | {value}/{total}",
      barCompleteChar: "\u2588",
      barIncompleteChar: "\u2591"
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/shades-grey.js
var require_shades_grey = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/shades-grey.js"(exports, module) {
    init_cjs_shims();
    module.exports = {
      format: " \x1B[90m{bar}\x1B[0m {percentage}% | ETA: {eta}s | {value}/{total}",
      barCompleteChar: "\u2588",
      barIncompleteChar: "\u2591"
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/rect.js
var require_rect = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/rect.js"(exports, module) {
    init_cjs_shims();
    module.exports = {
      format: " {bar}\u25A0 {percentage}% | ETA: {eta}s | {value}/{total}",
      barCompleteChar: "\u25A0",
      barIncompleteChar: " "
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/index.js
var require_presets = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/presets/index.js"(exports, module) {
    init_cjs_shims();
    var _legacy = require_legacy(), _shades_classic = require_shades_classic(), _shades_grey = require_shades_grey(), _rect = require_rect();
    module.exports = {
      legacy: _legacy,
      shades_classic: _shades_classic,
      shades_grey: _shades_grey,
      rect: _rect
    };
  }
});

// ../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/cli-progress.js
var require_cli_progress = __commonJS({
  "../../node_modules/.pnpm/cli-progress@3.12.0/node_modules/cli-progress/cli-progress.js"(exports, module) {
    init_cjs_shims();
    var _SingleBar = require_single_bar(), _MultiBar = require_multi_bar(), _Presets = require_presets(), _Formatter = require_formatter2(), _defaultFormatValue = require_format_value(), _defaultFormatBar = require_format_bar(), _defaultFormatTime = require_format_time();
    module.exports = {
      Bar: _SingleBar,
      SingleBar: _SingleBar,
      MultiBar: _MultiBar,
      Presets: _Presets,
      Format: {
        Formatter: _Formatter,
        BarFormat: _defaultFormatBar,
        ValueFormat: _defaultFormatValue,
        TimeFormat: _defaultFormatTime
      }
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/progress.js
var require_progress = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/progress.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var cli_progress_1 = require_cli_progress();
    function progress(options = {}) {
      return new cli_progress_1.SingleBar({ noTTYOutput: process.env.TERM === "dumb" || !process.stdin.isTTY, ...options });
    }
    exports.default = progress;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/common.js
var require_common = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/common.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    function isNothing(subject) {
      return typeof subject > "u" || subject === null;
    }
    function isObject(subject) {
      return typeof subject == "object" && subject !== null;
    }
    function toArray(sequence) {
      return Array.isArray(sequence) ? sequence : isNothing(sequence) ? [] : [sequence];
    }
    function extend(target, source) {
      var index, length, key, sourceKeys;
      if (source)
        for (sourceKeys = Object.keys(source), index = 0, length = sourceKeys.length; index < length; index += 1)
          key = sourceKeys[index], target[key] = source[key];
      return target;
    }
    function repeat(string, count) {
      var result = "", cycle;
      for (cycle = 0; cycle < count; cycle += 1)
        result += string;
      return result;
    }
    function isNegativeZero(number) {
      return number === 0 && Number.NEGATIVE_INFINITY === 1 / number;
    }
    module.exports.isNothing = isNothing;
    module.exports.isObject = isObject;
    module.exports.toArray = toArray;
    module.exports.repeat = repeat;
    module.exports.isNegativeZero = isNegativeZero;
    module.exports.extend = extend;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/exception.js
var require_exception = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/exception.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    function YAMLException(reason, mark) {
      Error.call(this), this.name = "YAMLException", this.reason = reason, this.mark = mark, this.message = (this.reason || "(unknown reason)") + (this.mark ? " " + this.mark.toString() : ""), Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = new Error().stack || "";
    }
    YAMLException.prototype = Object.create(Error.prototype);
    YAMLException.prototype.constructor = YAMLException;
    YAMLException.prototype.toString = function(compact) {
      var result = this.name + ": ";
      return result += this.reason || "(unknown reason)", !compact && this.mark && (result += " " + this.mark.toString()), result;
    };
    module.exports = YAMLException;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/mark.js
var require_mark = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/mark.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var common = require_common();
    function Mark(name, buffer, position, line, column) {
      this.name = name, this.buffer = buffer, this.position = position, this.line = line, this.column = column;
    }
    Mark.prototype.getSnippet = function(indent, maxLength) {
      var head, start, tail, end, snippet;
      if (!this.buffer) return null;
      for (indent = indent || 4, maxLength = maxLength || 75, head = "", start = this.position; start > 0 && `\0\r
\x85\u2028\u2029`.indexOf(this.buffer.charAt(start - 1)) === -1; )
        if (start -= 1, this.position - start > maxLength / 2 - 1) {
          head = " ... ", start += 5;
          break;
        }
      for (tail = "", end = this.position; end < this.buffer.length && `\0\r
\x85\u2028\u2029`.indexOf(this.buffer.charAt(end)) === -1; )
        if (end += 1, end - this.position > maxLength / 2 - 1) {
          tail = " ... ", end -= 5;
          break;
        }
      return snippet = this.buffer.slice(start, end), common.repeat(" ", indent) + head + snippet + tail + `
` + common.repeat(" ", indent + this.position - start + head.length) + "^";
    };
    Mark.prototype.toString = function(compact) {
      var snippet, where = "";
      return this.name && (where += 'in "' + this.name + '" '), where += "at line " + (this.line + 1) + ", column " + (this.column + 1), compact || (snippet = this.getSnippet(), snippet && (where += `:
` + snippet)), where;
    };
    module.exports = Mark;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type.js
var require_type = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var YAMLException = require_exception(), TYPE_CONSTRUCTOR_OPTIONS = [
      "kind",
      "resolve",
      "construct",
      "instanceOf",
      "predicate",
      "represent",
      "defaultStyle",
      "styleAliases"
    ], YAML_NODE_KINDS = [
      "scalar",
      "sequence",
      "mapping"
    ];
    function compileStyleAliases(map) {
      var result = {};
      return map !== null && Object.keys(map).forEach(function(style) {
        map[style].forEach(function(alias) {
          result[String(alias)] = style;
        });
      }), result;
    }
    function Type(tag, options) {
      if (options = options || {}, Object.keys(options).forEach(function(name) {
        if (TYPE_CONSTRUCTOR_OPTIONS.indexOf(name) === -1)
          throw new YAMLException('Unknown option "' + name + '" is met in definition of "' + tag + '" YAML type.');
      }), this.tag = tag, this.kind = options.kind || null, this.resolve = options.resolve || function() {
        return !0;
      }, this.construct = options.construct || function(data) {
        return data;
      }, this.instanceOf = options.instanceOf || null, this.predicate = options.predicate || null, this.represent = options.represent || null, this.defaultStyle = options.defaultStyle || null, this.styleAliases = compileStyleAliases(options.styleAliases || null), YAML_NODE_KINDS.indexOf(this.kind) === -1)
        throw new YAMLException('Unknown kind "' + this.kind + '" is specified for "' + tag + '" YAML type.');
    }
    module.exports = Type;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema.js
var require_schema = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var common = require_common(), YAMLException = require_exception(), Type = require_type();
    function compileList(schema, name, result) {
      var exclude = [];
      return schema.include.forEach(function(includedSchema) {
        result = compileList(includedSchema, name, result);
      }), schema[name].forEach(function(currentType) {
        result.forEach(function(previousType, previousIndex) {
          previousType.tag === currentType.tag && previousType.kind === currentType.kind && exclude.push(previousIndex);
        }), result.push(currentType);
      }), result.filter(function(type, index) {
        return exclude.indexOf(index) === -1;
      });
    }
    function compileMap() {
      var result = {
        scalar: {},
        sequence: {},
        mapping: {},
        fallback: {}
      }, index, length;
      function collectType(type) {
        result[type.kind][type.tag] = result.fallback[type.tag] = type;
      }
      for (index = 0, length = arguments.length; index < length; index += 1)
        arguments[index].forEach(collectType);
      return result;
    }
    function Schema(definition) {
      this.include = definition.include || [], this.implicit = definition.implicit || [], this.explicit = definition.explicit || [], this.implicit.forEach(function(type) {
        if (type.loadKind && type.loadKind !== "scalar")
          throw new YAMLException("There is a non-scalar type in the implicit list of a schema. Implicit resolving of such types is not supported.");
      }), this.compiledImplicit = compileList(this, "implicit", []), this.compiledExplicit = compileList(this, "explicit", []), this.compiledTypeMap = compileMap(this.compiledImplicit, this.compiledExplicit);
    }
    Schema.DEFAULT = null;
    Schema.create = function() {
      var schemas, types;
      switch (arguments.length) {
        case 1:
          schemas = Schema.DEFAULT, types = arguments[0];
          break;
        case 2:
          schemas = arguments[0], types = arguments[1];
          break;
        default:
          throw new YAMLException("Wrong number of arguments for Schema.create function");
      }
      if (schemas = common.toArray(schemas), types = common.toArray(types), !schemas.every(function(schema) {
        return schema instanceof Schema;
      }))
        throw new YAMLException("Specified list of super schemas (or a single Schema object) contains a non-Schema object.");
      if (!types.every(function(type) {
        return type instanceof Type;
      }))
        throw new YAMLException("Specified list of YAML types (or a single Type object) contains a non-Type object.");
      return new Schema({
        include: schemas,
        explicit: types
      });
    };
    module.exports = Schema;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/str.js
var require_str = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/str.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    module.exports = new Type("tag:yaml.org,2002:str", {
      kind: "scalar",
      construct: function(data) {
        return data !== null ? data : "";
      }
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/seq.js
var require_seq = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/seq.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    module.exports = new Type("tag:yaml.org,2002:seq", {
      kind: "sequence",
      construct: function(data) {
        return data !== null ? data : [];
      }
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/map.js
var require_map = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/map.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    module.exports = new Type("tag:yaml.org,2002:map", {
      kind: "mapping",
      construct: function(data) {
        return data !== null ? data : {};
      }
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/failsafe.js
var require_failsafe = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/failsafe.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Schema = require_schema();
    module.exports = new Schema({
      explicit: [
        require_str(),
        require_seq(),
        require_map()
      ]
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/null.js
var require_null = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/null.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    function resolveYamlNull(data) {
      if (data === null) return !0;
      var max = data.length;
      return max === 1 && data === "~" || max === 4 && (data === "null" || data === "Null" || data === "NULL");
    }
    function constructYamlNull() {
      return null;
    }
    function isNull(object) {
      return object === null;
    }
    module.exports = new Type("tag:yaml.org,2002:null", {
      kind: "scalar",
      resolve: resolveYamlNull,
      construct: constructYamlNull,
      predicate: isNull,
      represent: {
        canonical: function() {
          return "~";
        },
        lowercase: function() {
          return "null";
        },
        uppercase: function() {
          return "NULL";
        },
        camelcase: function() {
          return "Null";
        }
      },
      defaultStyle: "lowercase"
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/bool.js
var require_bool = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/bool.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    function resolveYamlBoolean(data) {
      if (data === null) return !1;
      var max = data.length;
      return max === 4 && (data === "true" || data === "True" || data === "TRUE") || max === 5 && (data === "false" || data === "False" || data === "FALSE");
    }
    function constructYamlBoolean(data) {
      return data === "true" || data === "True" || data === "TRUE";
    }
    function isBoolean(object) {
      return Object.prototype.toString.call(object) === "[object Boolean]";
    }
    module.exports = new Type("tag:yaml.org,2002:bool", {
      kind: "scalar",
      resolve: resolveYamlBoolean,
      construct: constructYamlBoolean,
      predicate: isBoolean,
      represent: {
        lowercase: function(object) {
          return object ? "true" : "false";
        },
        uppercase: function(object) {
          return object ? "TRUE" : "FALSE";
        },
        camelcase: function(object) {
          return object ? "True" : "False";
        }
      },
      defaultStyle: "lowercase"
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/int.js
var require_int = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/int.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var common = require_common(), Type = require_type();
    function isHexCode(c) {
      return 48 <= c && c <= 57 || 65 <= c && c <= 70 || 97 <= c && c <= 102;
    }
    function isOctCode(c) {
      return 48 <= c && c <= 55;
    }
    function isDecCode(c) {
      return 48 <= c && c <= 57;
    }
    function resolveYamlInteger(data) {
      if (data === null) return !1;
      var max = data.length, index = 0, hasDigits = !1, ch;
      if (!max) return !1;
      if (ch = data[index], (ch === "-" || ch === "+") && (ch = data[++index]), ch === "0") {
        if (index + 1 === max) return !0;
        if (ch = data[++index], ch === "b") {
          for (index++; index < max; index++)
            if (ch = data[index], ch !== "_") {
              if (ch !== "0" && ch !== "1") return !1;
              hasDigits = !0;
            }
          return hasDigits && ch !== "_";
        }
        if (ch === "x") {
          for (index++; index < max; index++)
            if (ch = data[index], ch !== "_") {
              if (!isHexCode(data.charCodeAt(index))) return !1;
              hasDigits = !0;
            }
          return hasDigits && ch !== "_";
        }
        for (; index < max; index++)
          if (ch = data[index], ch !== "_") {
            if (!isOctCode(data.charCodeAt(index))) return !1;
            hasDigits = !0;
          }
        return hasDigits && ch !== "_";
      }
      if (ch === "_") return !1;
      for (; index < max; index++)
        if (ch = data[index], ch !== "_") {
          if (ch === ":") break;
          if (!isDecCode(data.charCodeAt(index)))
            return !1;
          hasDigits = !0;
        }
      return !hasDigits || ch === "_" ? !1 : ch !== ":" ? !0 : /^(:[0-5]?[0-9])+$/.test(data.slice(index));
    }
    function constructYamlInteger(data) {
      var value = data, sign = 1, ch, base, digits = [];
      return value.indexOf("_") !== -1 && (value = value.replace(/_/g, "")), ch = value[0], (ch === "-" || ch === "+") && (ch === "-" && (sign = -1), value = value.slice(1), ch = value[0]), value === "0" ? 0 : ch === "0" ? value[1] === "b" ? sign * parseInt(value.slice(2), 2) : value[1] === "x" ? sign * parseInt(value, 16) : sign * parseInt(value, 8) : value.indexOf(":") !== -1 ? (value.split(":").forEach(function(v) {
        digits.unshift(parseInt(v, 10));
      }), value = 0, base = 1, digits.forEach(function(d) {
        value += d * base, base *= 60;
      }), sign * value) : sign * parseInt(value, 10);
    }
    function isInteger(object) {
      return Object.prototype.toString.call(object) === "[object Number]" && object % 1 === 0 && !common.isNegativeZero(object);
    }
    module.exports = new Type("tag:yaml.org,2002:int", {
      kind: "scalar",
      resolve: resolveYamlInteger,
      construct: constructYamlInteger,
      predicate: isInteger,
      represent: {
        binary: function(obj) {
          return obj >= 0 ? "0b" + obj.toString(2) : "-0b" + obj.toString(2).slice(1);
        },
        octal: function(obj) {
          return obj >= 0 ? "0" + obj.toString(8) : "-0" + obj.toString(8).slice(1);
        },
        decimal: function(obj) {
          return obj.toString(10);
        },
        /* eslint-disable max-len */
        hexadecimal: function(obj) {
          return obj >= 0 ? "0x" + obj.toString(16).toUpperCase() : "-0x" + obj.toString(16).toUpperCase().slice(1);
        }
      },
      defaultStyle: "decimal",
      styleAliases: {
        binary: [2, "bin"],
        octal: [8, "oct"],
        decimal: [10, "dec"],
        hexadecimal: [16, "hex"]
      }
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/float.js
var require_float = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/float.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var common = require_common(), Type = require_type(), YAML_FLOAT_PATTERN = new RegExp(
      // 2.5e4, 2.5 and integers
      "^(?:[-+]?(?:0|[1-9][0-9_]*)(?:\\.[0-9_]*)?(?:[eE][-+]?[0-9]+)?|\\.[0-9_]+(?:[eE][-+]?[0-9]+)?|[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+\\.[0-9_]*|[-+]?\\.(?:inf|Inf|INF)|\\.(?:nan|NaN|NAN))$"
    );
    function resolveYamlFloat(data) {
      return !(data === null || !YAML_FLOAT_PATTERN.test(data) || // Quick hack to not allow integers end with `_`
      // Probably should update regexp & check speed
      data[data.length - 1] === "_");
    }
    function constructYamlFloat(data) {
      var value, sign, base, digits;
      return value = data.replace(/_/g, "").toLowerCase(), sign = value[0] === "-" ? -1 : 1, digits = [], "+-".indexOf(value[0]) >= 0 && (value = value.slice(1)), value === ".inf" ? sign === 1 ? Number.POSITIVE_INFINITY : Number.NEGATIVE_INFINITY : value === ".nan" ? NaN : value.indexOf(":") >= 0 ? (value.split(":").forEach(function(v) {
        digits.unshift(parseFloat(v, 10));
      }), value = 0, base = 1, digits.forEach(function(d) {
        value += d * base, base *= 60;
      }), sign * value) : sign * parseFloat(value, 10);
    }
    var SCIENTIFIC_WITHOUT_DOT = /^[-+]?[0-9]+e/;
    function representYamlFloat(object, style) {
      var res;
      if (isNaN(object))
        switch (style) {
          case "lowercase":
            return ".nan";
          case "uppercase":
            return ".NAN";
          case "camelcase":
            return ".NaN";
        }
      else if (Number.POSITIVE_INFINITY === object)
        switch (style) {
          case "lowercase":
            return ".inf";
          case "uppercase":
            return ".INF";
          case "camelcase":
            return ".Inf";
        }
      else if (Number.NEGATIVE_INFINITY === object)
        switch (style) {
          case "lowercase":
            return "-.inf";
          case "uppercase":
            return "-.INF";
          case "camelcase":
            return "-.Inf";
        }
      else if (common.isNegativeZero(object))
        return "-0.0";
      return res = object.toString(10), SCIENTIFIC_WITHOUT_DOT.test(res) ? res.replace("e", ".e") : res;
    }
    function isFloat(object) {
      return Object.prototype.toString.call(object) === "[object Number]" && (object % 1 !== 0 || common.isNegativeZero(object));
    }
    module.exports = new Type("tag:yaml.org,2002:float", {
      kind: "scalar",
      resolve: resolveYamlFloat,
      construct: constructYamlFloat,
      predicate: isFloat,
      represent: representYamlFloat,
      defaultStyle: "lowercase"
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/json.js
var require_json = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/json.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Schema = require_schema();
    module.exports = new Schema({
      include: [
        require_failsafe()
      ],
      implicit: [
        require_null(),
        require_bool(),
        require_int(),
        require_float()
      ]
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/core.js
var require_core = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/core.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Schema = require_schema();
    module.exports = new Schema({
      include: [
        require_json()
      ]
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/timestamp.js
var require_timestamp = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/timestamp.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type(), YAML_DATE_REGEXP = new RegExp(
      "^([0-9][0-9][0-9][0-9])-([0-9][0-9])-([0-9][0-9])$"
    ), YAML_TIMESTAMP_REGEXP = new RegExp(
      "^([0-9][0-9][0-9][0-9])-([0-9][0-9]?)-([0-9][0-9]?)(?:[Tt]|[ \\t]+)([0-9][0-9]?):([0-9][0-9]):([0-9][0-9])(?:\\.([0-9]*))?(?:[ \\t]*(Z|([-+])([0-9][0-9]?)(?::([0-9][0-9]))?))?$"
    );
    function resolveYamlTimestamp(data) {
      return data === null ? !1 : YAML_DATE_REGEXP.exec(data) !== null || YAML_TIMESTAMP_REGEXP.exec(data) !== null;
    }
    function constructYamlTimestamp(data) {
      var match, year, month, day, hour, minute, second, fraction = 0, delta = null, tz_hour, tz_minute, date;
      if (match = YAML_DATE_REGEXP.exec(data), match === null && (match = YAML_TIMESTAMP_REGEXP.exec(data)), match === null) throw new Error("Date resolve error");
      if (year = +match[1], month = +match[2] - 1, day = +match[3], !match[4])
        return new Date(Date.UTC(year, month, day));
      if (hour = +match[4], minute = +match[5], second = +match[6], match[7]) {
        for (fraction = match[7].slice(0, 3); fraction.length < 3; )
          fraction += "0";
        fraction = +fraction;
      }
      return match[9] && (tz_hour = +match[10], tz_minute = +(match[11] || 0), delta = (tz_hour * 60 + tz_minute) * 6e4, match[9] === "-" && (delta = -delta)), date = new Date(Date.UTC(year, month, day, hour, minute, second, fraction)), delta && date.setTime(date.getTime() - delta), date;
    }
    function representYamlTimestamp(object) {
      return object.toISOString();
    }
    module.exports = new Type("tag:yaml.org,2002:timestamp", {
      kind: "scalar",
      resolve: resolveYamlTimestamp,
      construct: constructYamlTimestamp,
      instanceOf: Date,
      represent: representYamlTimestamp
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/merge.js
var require_merge = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/merge.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    function resolveYamlMerge(data) {
      return data === "<<" || data === null;
    }
    module.exports = new Type("tag:yaml.org,2002:merge", {
      kind: "scalar",
      resolve: resolveYamlMerge
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/binary.js
var require_binary = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/binary.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var NodeBuffer;
    try {
      _require = __require, NodeBuffer = _require("buffer").Buffer;
    } catch {
    }
    var _require, Type = require_type(), BASE64_MAP = `ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=
\r`;
    function resolveYamlBinary(data) {
      if (data === null) return !1;
      var code, idx, bitlen = 0, max = data.length, map = BASE64_MAP;
      for (idx = 0; idx < max; idx++)
        if (code = map.indexOf(data.charAt(idx)), !(code > 64)) {
          if (code < 0) return !1;
          bitlen += 6;
        }
      return bitlen % 8 === 0;
    }
    function constructYamlBinary(data) {
      var idx, tailbits, input = data.replace(/[\r\n=]/g, ""), max = input.length, map = BASE64_MAP, bits = 0, result = [];
      for (idx = 0; idx < max; idx++)
        idx % 4 === 0 && idx && (result.push(bits >> 16 & 255), result.push(bits >> 8 & 255), result.push(bits & 255)), bits = bits << 6 | map.indexOf(input.charAt(idx));
      return tailbits = max % 4 * 6, tailbits === 0 ? (result.push(bits >> 16 & 255), result.push(bits >> 8 & 255), result.push(bits & 255)) : tailbits === 18 ? (result.push(bits >> 10 & 255), result.push(bits >> 2 & 255)) : tailbits === 12 && result.push(bits >> 4 & 255), NodeBuffer ? NodeBuffer.from ? NodeBuffer.from(result) : new NodeBuffer(result) : result;
    }
    function representYamlBinary(object) {
      var result = "", bits = 0, idx, tail, max = object.length, map = BASE64_MAP;
      for (idx = 0; idx < max; idx++)
        idx % 3 === 0 && idx && (result += map[bits >> 18 & 63], result += map[bits >> 12 & 63], result += map[bits >> 6 & 63], result += map[bits & 63]), bits = (bits << 8) + object[idx];
      return tail = max % 3, tail === 0 ? (result += map[bits >> 18 & 63], result += map[bits >> 12 & 63], result += map[bits >> 6 & 63], result += map[bits & 63]) : tail === 2 ? (result += map[bits >> 10 & 63], result += map[bits >> 4 & 63], result += map[bits << 2 & 63], result += map[64]) : tail === 1 && (result += map[bits >> 2 & 63], result += map[bits << 4 & 63], result += map[64], result += map[64]), result;
    }
    function isBinary(object) {
      return NodeBuffer && NodeBuffer.isBuffer(object);
    }
    module.exports = new Type("tag:yaml.org,2002:binary", {
      kind: "scalar",
      resolve: resolveYamlBinary,
      construct: constructYamlBinary,
      predicate: isBinary,
      represent: representYamlBinary
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/omap.js
var require_omap = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/omap.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type(), _hasOwnProperty = Object.prototype.hasOwnProperty, _toString = Object.prototype.toString;
    function resolveYamlOmap(data) {
      if (data === null) return !0;
      var objectKeys = [], index, length, pair, pairKey, pairHasKey, object = data;
      for (index = 0, length = object.length; index < length; index += 1) {
        if (pair = object[index], pairHasKey = !1, _toString.call(pair) !== "[object Object]") return !1;
        for (pairKey in pair)
          if (_hasOwnProperty.call(pair, pairKey))
            if (!pairHasKey) pairHasKey = !0;
            else return !1;
        if (!pairHasKey) return !1;
        if (objectKeys.indexOf(pairKey) === -1) objectKeys.push(pairKey);
        else return !1;
      }
      return !0;
    }
    function constructYamlOmap(data) {
      return data !== null ? data : [];
    }
    module.exports = new Type("tag:yaml.org,2002:omap", {
      kind: "sequence",
      resolve: resolveYamlOmap,
      construct: constructYamlOmap
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/pairs.js
var require_pairs = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/pairs.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type(), _toString = Object.prototype.toString;
    function resolveYamlPairs(data) {
      if (data === null) return !0;
      var index, length, pair, keys, result, object = data;
      for (result = new Array(object.length), index = 0, length = object.length; index < length; index += 1) {
        if (pair = object[index], _toString.call(pair) !== "[object Object]" || (keys = Object.keys(pair), keys.length !== 1)) return !1;
        result[index] = [keys[0], pair[keys[0]]];
      }
      return !0;
    }
    function constructYamlPairs(data) {
      if (data === null) return [];
      var index, length, pair, keys, result, object = data;
      for (result = new Array(object.length), index = 0, length = object.length; index < length; index += 1)
        pair = object[index], keys = Object.keys(pair), result[index] = [keys[0], pair[keys[0]]];
      return result;
    }
    module.exports = new Type("tag:yaml.org,2002:pairs", {
      kind: "sequence",
      resolve: resolveYamlPairs,
      construct: constructYamlPairs
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/set.js
var require_set = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/set.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type(), _hasOwnProperty = Object.prototype.hasOwnProperty;
    function resolveYamlSet(data) {
      if (data === null) return !0;
      var key, object = data;
      for (key in object)
        if (_hasOwnProperty.call(object, key) && object[key] !== null)
          return !1;
      return !0;
    }
    function constructYamlSet(data) {
      return data !== null ? data : {};
    }
    module.exports = new Type("tag:yaml.org,2002:set", {
      kind: "mapping",
      resolve: resolveYamlSet,
      construct: constructYamlSet
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/default_safe.js
var require_default_safe = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/default_safe.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Schema = require_schema();
    module.exports = new Schema({
      include: [
        require_core()
      ],
      implicit: [
        require_timestamp(),
        require_merge()
      ],
      explicit: [
        require_binary(),
        require_omap(),
        require_pairs(),
        require_set()
      ]
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/js/undefined.js
var require_undefined = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/js/undefined.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    function resolveJavascriptUndefined() {
      return !0;
    }
    function constructJavascriptUndefined() {
    }
    function representJavascriptUndefined() {
      return "";
    }
    function isUndefined(object) {
      return typeof object > "u";
    }
    module.exports = new Type("tag:yaml.org,2002:js/undefined", {
      kind: "scalar",
      resolve: resolveJavascriptUndefined,
      construct: constructJavascriptUndefined,
      predicate: isUndefined,
      represent: representJavascriptUndefined
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/js/regexp.js
var require_regexp = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/js/regexp.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Type = require_type();
    function resolveJavascriptRegExp(data) {
      if (data === null || data.length === 0) return !1;
      var regexp = data, tail = /\/([gim]*)$/.exec(data), modifiers = "";
      return !(regexp[0] === "/" && (tail && (modifiers = tail[1]), modifiers.length > 3 || regexp[regexp.length - modifiers.length - 1] !== "/"));
    }
    function constructJavascriptRegExp(data) {
      var regexp = data, tail = /\/([gim]*)$/.exec(data), modifiers = "";
      return regexp[0] === "/" && (tail && (modifiers = tail[1]), regexp = regexp.slice(1, regexp.length - modifiers.length - 1)), new RegExp(regexp, modifiers);
    }
    function representJavascriptRegExp(object) {
      var result = "/" + object.source + "/";
      return object.global && (result += "g"), object.multiline && (result += "m"), object.ignoreCase && (result += "i"), result;
    }
    function isRegExp(object) {
      return Object.prototype.toString.call(object) === "[object RegExp]";
    }
    module.exports = new Type("tag:yaml.org,2002:js/regexp", {
      kind: "scalar",
      resolve: resolveJavascriptRegExp,
      construct: constructJavascriptRegExp,
      predicate: isRegExp,
      represent: representJavascriptRegExp
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/js/function.js
var require_function = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/type/js/function.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var esprima;
    try {
      _require = __require, esprima = _require("esprima");
    } catch {
      typeof window < "u" && (esprima = window.esprima);
    }
    var _require, Type = require_type();
    function resolveJavascriptFunction(data) {
      if (data === null) return !1;
      try {
        var source = "(" + data + ")", ast = esprima.parse(source, { range: !0 });
        return !(ast.type !== "Program" || ast.body.length !== 1 || ast.body[0].type !== "ExpressionStatement" || ast.body[0].expression.type !== "ArrowFunctionExpression" && ast.body[0].expression.type !== "FunctionExpression");
      } catch {
        return !1;
      }
    }
    function constructJavascriptFunction(data) {
      var source = "(" + data + ")", ast = esprima.parse(source, { range: !0 }), params = [], body;
      if (ast.type !== "Program" || ast.body.length !== 1 || ast.body[0].type !== "ExpressionStatement" || ast.body[0].expression.type !== "ArrowFunctionExpression" && ast.body[0].expression.type !== "FunctionExpression")
        throw new Error("Failed to resolve function");
      return ast.body[0].expression.params.forEach(function(param) {
        params.push(param.name);
      }), body = ast.body[0].expression.body.range, ast.body[0].expression.body.type === "BlockStatement" ? new Function(params, source.slice(body[0] + 1, body[1] - 1)) : new Function(params, "return " + source.slice(body[0], body[1]));
    }
    function representJavascriptFunction(object) {
      return object.toString();
    }
    function isFunction(object) {
      return Object.prototype.toString.call(object) === "[object Function]";
    }
    module.exports = new Type("tag:yaml.org,2002:js/function", {
      kind: "scalar",
      resolve: resolveJavascriptFunction,
      construct: constructJavascriptFunction,
      predicate: isFunction,
      represent: representJavascriptFunction
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/default_full.js
var require_default_full = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/schema/default_full.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var Schema = require_schema();
    module.exports = Schema.DEFAULT = new Schema({
      include: [
        require_default_safe()
      ],
      explicit: [
        require_undefined(),
        require_regexp(),
        require_function()
      ]
    });
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/loader.js
var require_loader = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/loader.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var common = require_common(), YAMLException = require_exception(), Mark = require_mark(), DEFAULT_SAFE_SCHEMA = require_default_safe(), DEFAULT_FULL_SCHEMA = require_default_full(), _hasOwnProperty = Object.prototype.hasOwnProperty, CONTEXT_FLOW_IN = 1, CONTEXT_FLOW_OUT = 2, CONTEXT_BLOCK_IN = 3, CONTEXT_BLOCK_OUT = 4, CHOMPING_CLIP = 1, CHOMPING_STRIP = 2, CHOMPING_KEEP = 3, PATTERN_NON_PRINTABLE = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x84\x86-\x9F\uFFFE\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]/, PATTERN_NON_ASCII_LINE_BREAKS = /[\x85\u2028\u2029]/, PATTERN_FLOW_INDICATORS = /[,\[\]\{\}]/, PATTERN_TAG_HANDLE = /^(?:!|!!|![a-z\-]+!)$/i, PATTERN_TAG_URI = /^(?:!|[^,\[\]\{\}])(?:%[0-9a-f]{2}|[0-9a-z\-#;\/\?:@&=\+\$,_\.!~\*'\(\)\[\]])*$/i;
    function _class(obj) {
      return Object.prototype.toString.call(obj);
    }
    function is_EOL(c) {
      return c === 10 || c === 13;
    }
    function is_WHITE_SPACE(c) {
      return c === 9 || c === 32;
    }
    function is_WS_OR_EOL(c) {
      return c === 9 || c === 32 || c === 10 || c === 13;
    }
    function is_FLOW_INDICATOR(c) {
      return c === 44 || c === 91 || c === 93 || c === 123 || c === 125;
    }
    function fromHexCode(c) {
      var lc;
      return 48 <= c && c <= 57 ? c - 48 : (lc = c | 32, 97 <= lc && lc <= 102 ? lc - 97 + 10 : -1);
    }
    function escapedHexLen(c) {
      return c === 120 ? 2 : c === 117 ? 4 : c === 85 ? 8 : 0;
    }
    function fromDecimalCode(c) {
      return 48 <= c && c <= 57 ? c - 48 : -1;
    }
    function simpleEscapeSequence(c) {
      return c === 48 ? "\0" : c === 97 ? "\x07" : c === 98 ? "\b" : c === 116 || c === 9 ? "	" : c === 110 ? `
` : c === 118 ? "\v" : c === 102 ? "\f" : c === 114 ? "\r" : c === 101 ? "\x1B" : c === 32 ? " " : c === 34 ? '"' : c === 47 ? "/" : c === 92 ? "\\" : c === 78 ? "\x85" : c === 95 ? "\xA0" : c === 76 ? "\u2028" : c === 80 ? "\u2029" : "";
    }
    function charFromCodepoint(c) {
      return c <= 65535 ? String.fromCharCode(c) : String.fromCharCode(
        (c - 65536 >> 10) + 55296,
        (c - 65536 & 1023) + 56320
      );
    }
    function setProperty(object, key, value) {
      key === "__proto__" ? Object.defineProperty(object, key, {
        configurable: !0,
        enumerable: !0,
        writable: !0,
        value
      }) : object[key] = value;
    }
    var simpleEscapeCheck = new Array(256), simpleEscapeMap = new Array(256);
    for (i = 0; i < 256; i++)
      simpleEscapeCheck[i] = simpleEscapeSequence(i) ? 1 : 0, simpleEscapeMap[i] = simpleEscapeSequence(i);
    var i;
    function State(input, options) {
      this.input = input, this.filename = options.filename || null, this.schema = options.schema || DEFAULT_FULL_SCHEMA, this.onWarning = options.onWarning || null, this.legacy = options.legacy || !1, this.json = options.json || !1, this.listener = options.listener || null, this.implicitTypes = this.schema.compiledImplicit, this.typeMap = this.schema.compiledTypeMap, this.length = input.length, this.position = 0, this.line = 0, this.lineStart = 0, this.lineIndent = 0, this.documents = [];
    }
    function generateError(state, message) {
      return new YAMLException(
        message,
        new Mark(state.filename, state.input, state.position, state.line, state.position - state.lineStart)
      );
    }
    function throwError(state, message) {
      throw generateError(state, message);
    }
    function throwWarning(state, message) {
      state.onWarning && state.onWarning.call(null, generateError(state, message));
    }
    var directiveHandlers = {
      YAML: function(state, name, args) {
        var match, major, minor;
        state.version !== null && throwError(state, "duplication of %YAML directive"), args.length !== 1 && throwError(state, "YAML directive accepts exactly one argument"), match = /^([0-9]+)\.([0-9]+)$/.exec(args[0]), match === null && throwError(state, "ill-formed argument of the YAML directive"), major = parseInt(match[1], 10), minor = parseInt(match[2], 10), major !== 1 && throwError(state, "unacceptable YAML version of the document"), state.version = args[0], state.checkLineBreaks = minor < 2, minor !== 1 && minor !== 2 && throwWarning(state, "unsupported YAML version of the document");
      },
      TAG: function(state, name, args) {
        var handle, prefix;
        args.length !== 2 && throwError(state, "TAG directive accepts exactly two arguments"), handle = args[0], prefix = args[1], PATTERN_TAG_HANDLE.test(handle) || throwError(state, "ill-formed tag handle (first argument) of the TAG directive"), _hasOwnProperty.call(state.tagMap, handle) && throwError(state, 'there is a previously declared suffix for "' + handle + '" tag handle'), PATTERN_TAG_URI.test(prefix) || throwError(state, "ill-formed tag prefix (second argument) of the TAG directive"), state.tagMap[handle] = prefix;
      }
    };
    function captureSegment(state, start, end, checkJson) {
      var _position, _length, _character, _result;
      if (start < end) {
        if (_result = state.input.slice(start, end), checkJson)
          for (_position = 0, _length = _result.length; _position < _length; _position += 1)
            _character = _result.charCodeAt(_position), _character === 9 || 32 <= _character && _character <= 1114111 || throwError(state, "expected valid JSON character");
        else PATTERN_NON_PRINTABLE.test(_result) && throwError(state, "the stream contains non-printable characters");
        state.result += _result;
      }
    }
    function mergeMappings(state, destination, source, overridableKeys) {
      var sourceKeys, key, index, quantity;
      for (common.isObject(source) || throwError(state, "cannot merge mappings; the provided source object is unacceptable"), sourceKeys = Object.keys(source), index = 0, quantity = sourceKeys.length; index < quantity; index += 1)
        key = sourceKeys[index], _hasOwnProperty.call(destination, key) || (setProperty(destination, key, source[key]), overridableKeys[key] = !0);
    }
    function storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, valueNode, startLine, startPos) {
      var index, quantity;
      if (Array.isArray(keyNode))
        for (keyNode = Array.prototype.slice.call(keyNode), index = 0, quantity = keyNode.length; index < quantity; index += 1)
          Array.isArray(keyNode[index]) && throwError(state, "nested arrays are not supported inside keys"), typeof keyNode == "object" && _class(keyNode[index]) === "[object Object]" && (keyNode[index] = "[object Object]");
      if (typeof keyNode == "object" && _class(keyNode) === "[object Object]" && (keyNode = "[object Object]"), keyNode = String(keyNode), _result === null && (_result = {}), keyTag === "tag:yaml.org,2002:merge")
        if (Array.isArray(valueNode))
          for (index = 0, quantity = valueNode.length; index < quantity; index += 1)
            mergeMappings(state, _result, valueNode[index], overridableKeys);
        else
          mergeMappings(state, _result, valueNode, overridableKeys);
      else
        !state.json && !_hasOwnProperty.call(overridableKeys, keyNode) && _hasOwnProperty.call(_result, keyNode) && (state.line = startLine || state.line, state.position = startPos || state.position, throwError(state, "duplicated mapping key")), setProperty(_result, keyNode, valueNode), delete overridableKeys[keyNode];
      return _result;
    }
    function readLineBreak(state) {
      var ch;
      ch = state.input.charCodeAt(state.position), ch === 10 ? state.position++ : ch === 13 ? (state.position++, state.input.charCodeAt(state.position) === 10 && state.position++) : throwError(state, "a line break is expected"), state.line += 1, state.lineStart = state.position;
    }
    function skipSeparationSpace(state, allowComments, checkIndent) {
      for (var lineBreaks = 0, ch = state.input.charCodeAt(state.position); ch !== 0; ) {
        for (; is_WHITE_SPACE(ch); )
          ch = state.input.charCodeAt(++state.position);
        if (allowComments && ch === 35)
          do
            ch = state.input.charCodeAt(++state.position);
          while (ch !== 10 && ch !== 13 && ch !== 0);
        if (is_EOL(ch))
          for (readLineBreak(state), ch = state.input.charCodeAt(state.position), lineBreaks++, state.lineIndent = 0; ch === 32; )
            state.lineIndent++, ch = state.input.charCodeAt(++state.position);
        else
          break;
      }
      return checkIndent !== -1 && lineBreaks !== 0 && state.lineIndent < checkIndent && throwWarning(state, "deficient indentation"), lineBreaks;
    }
    function testDocumentSeparator(state) {
      var _position = state.position, ch;
      return ch = state.input.charCodeAt(_position), !!((ch === 45 || ch === 46) && ch === state.input.charCodeAt(_position + 1) && ch === state.input.charCodeAt(_position + 2) && (_position += 3, ch = state.input.charCodeAt(_position), ch === 0 || is_WS_OR_EOL(ch)));
    }
    function writeFoldedLines(state, count) {
      count === 1 ? state.result += " " : count > 1 && (state.result += common.repeat(`
`, count - 1));
    }
    function readPlainScalar(state, nodeIndent, withinFlowCollection) {
      var preceding, following, captureStart, captureEnd, hasPendingContent, _line, _lineStart, _lineIndent, _kind = state.kind, _result = state.result, ch;
      if (ch = state.input.charCodeAt(state.position), is_WS_OR_EOL(ch) || is_FLOW_INDICATOR(ch) || ch === 35 || ch === 38 || ch === 42 || ch === 33 || ch === 124 || ch === 62 || ch === 39 || ch === 34 || ch === 37 || ch === 64 || ch === 96 || (ch === 63 || ch === 45) && (following = state.input.charCodeAt(state.position + 1), is_WS_OR_EOL(following) || withinFlowCollection && is_FLOW_INDICATOR(following)))
        return !1;
      for (state.kind = "scalar", state.result = "", captureStart = captureEnd = state.position, hasPendingContent = !1; ch !== 0; ) {
        if (ch === 58) {
          if (following = state.input.charCodeAt(state.position + 1), is_WS_OR_EOL(following) || withinFlowCollection && is_FLOW_INDICATOR(following))
            break;
        } else if (ch === 35) {
          if (preceding = state.input.charCodeAt(state.position - 1), is_WS_OR_EOL(preceding))
            break;
        } else {
          if (state.position === state.lineStart && testDocumentSeparator(state) || withinFlowCollection && is_FLOW_INDICATOR(ch))
            break;
          if (is_EOL(ch))
            if (_line = state.line, _lineStart = state.lineStart, _lineIndent = state.lineIndent, skipSeparationSpace(state, !1, -1), state.lineIndent >= nodeIndent) {
              hasPendingContent = !0, ch = state.input.charCodeAt(state.position);
              continue;
            } else {
              state.position = captureEnd, state.line = _line, state.lineStart = _lineStart, state.lineIndent = _lineIndent;
              break;
            }
        }
        hasPendingContent && (captureSegment(state, captureStart, captureEnd, !1), writeFoldedLines(state, state.line - _line), captureStart = captureEnd = state.position, hasPendingContent = !1), is_WHITE_SPACE(ch) || (captureEnd = state.position + 1), ch = state.input.charCodeAt(++state.position);
      }
      return captureSegment(state, captureStart, captureEnd, !1), state.result ? !0 : (state.kind = _kind, state.result = _result, !1);
    }
    function readSingleQuotedScalar(state, nodeIndent) {
      var ch, captureStart, captureEnd;
      if (ch = state.input.charCodeAt(state.position), ch !== 39)
        return !1;
      for (state.kind = "scalar", state.result = "", state.position++, captureStart = captureEnd = state.position; (ch = state.input.charCodeAt(state.position)) !== 0; )
        if (ch === 39)
          if (captureSegment(state, captureStart, state.position, !0), ch = state.input.charCodeAt(++state.position), ch === 39)
            captureStart = state.position, state.position++, captureEnd = state.position;
          else
            return !0;
        else is_EOL(ch) ? (captureSegment(state, captureStart, captureEnd, !0), writeFoldedLines(state, skipSeparationSpace(state, !1, nodeIndent)), captureStart = captureEnd = state.position) : state.position === state.lineStart && testDocumentSeparator(state) ? throwError(state, "unexpected end of the document within a single quoted scalar") : (state.position++, captureEnd = state.position);
      throwError(state, "unexpected end of the stream within a single quoted scalar");
    }
    function readDoubleQuotedScalar(state, nodeIndent) {
      var captureStart, captureEnd, hexLength, hexResult, tmp, ch;
      if (ch = state.input.charCodeAt(state.position), ch !== 34)
        return !1;
      for (state.kind = "scalar", state.result = "", state.position++, captureStart = captureEnd = state.position; (ch = state.input.charCodeAt(state.position)) !== 0; ) {
        if (ch === 34)
          return captureSegment(state, captureStart, state.position, !0), state.position++, !0;
        if (ch === 92) {
          if (captureSegment(state, captureStart, state.position, !0), ch = state.input.charCodeAt(++state.position), is_EOL(ch))
            skipSeparationSpace(state, !1, nodeIndent);
          else if (ch < 256 && simpleEscapeCheck[ch])
            state.result += simpleEscapeMap[ch], state.position++;
          else if ((tmp = escapedHexLen(ch)) > 0) {
            for (hexLength = tmp, hexResult = 0; hexLength > 0; hexLength--)
              ch = state.input.charCodeAt(++state.position), (tmp = fromHexCode(ch)) >= 0 ? hexResult = (hexResult << 4) + tmp : throwError(state, "expected hexadecimal character");
            state.result += charFromCodepoint(hexResult), state.position++;
          } else
            throwError(state, "unknown escape sequence");
          captureStart = captureEnd = state.position;
        } else is_EOL(ch) ? (captureSegment(state, captureStart, captureEnd, !0), writeFoldedLines(state, skipSeparationSpace(state, !1, nodeIndent)), captureStart = captureEnd = state.position) : state.position === state.lineStart && testDocumentSeparator(state) ? throwError(state, "unexpected end of the document within a double quoted scalar") : (state.position++, captureEnd = state.position);
      }
      throwError(state, "unexpected end of the stream within a double quoted scalar");
    }
    function readFlowCollection(state, nodeIndent) {
      var readNext = !0, _line, _tag = state.tag, _result, _anchor = state.anchor, following, terminator, isPair, isExplicitPair, isMapping, overridableKeys = {}, keyNode, keyTag, valueNode, ch;
      if (ch = state.input.charCodeAt(state.position), ch === 91)
        terminator = 93, isMapping = !1, _result = [];
      else if (ch === 123)
        terminator = 125, isMapping = !0, _result = {};
      else
        return !1;
      for (state.anchor !== null && (state.anchorMap[state.anchor] = _result), ch = state.input.charCodeAt(++state.position); ch !== 0; ) {
        if (skipSeparationSpace(state, !0, nodeIndent), ch = state.input.charCodeAt(state.position), ch === terminator)
          return state.position++, state.tag = _tag, state.anchor = _anchor, state.kind = isMapping ? "mapping" : "sequence", state.result = _result, !0;
        readNext || throwError(state, "missed comma between flow collection entries"), keyTag = keyNode = valueNode = null, isPair = isExplicitPair = !1, ch === 63 && (following = state.input.charCodeAt(state.position + 1), is_WS_OR_EOL(following) && (isPair = isExplicitPair = !0, state.position++, skipSeparationSpace(state, !0, nodeIndent))), _line = state.line, composeNode(state, nodeIndent, CONTEXT_FLOW_IN, !1, !0), keyTag = state.tag, keyNode = state.result, skipSeparationSpace(state, !0, nodeIndent), ch = state.input.charCodeAt(state.position), (isExplicitPair || state.line === _line) && ch === 58 && (isPair = !0, ch = state.input.charCodeAt(++state.position), skipSeparationSpace(state, !0, nodeIndent), composeNode(state, nodeIndent, CONTEXT_FLOW_IN, !1, !0), valueNode = state.result), isMapping ? storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, valueNode) : isPair ? _result.push(storeMappingPair(state, null, overridableKeys, keyTag, keyNode, valueNode)) : _result.push(keyNode), skipSeparationSpace(state, !0, nodeIndent), ch = state.input.charCodeAt(state.position), ch === 44 ? (readNext = !0, ch = state.input.charCodeAt(++state.position)) : readNext = !1;
      }
      throwError(state, "unexpected end of the stream within a flow collection");
    }
    function readBlockScalar(state, nodeIndent) {
      var captureStart, folding, chomping = CHOMPING_CLIP, didReadContent = !1, detectedIndent = !1, textIndent = nodeIndent, emptyLines = 0, atMoreIndented = !1, tmp, ch;
      if (ch = state.input.charCodeAt(state.position), ch === 124)
        folding = !1;
      else if (ch === 62)
        folding = !0;
      else
        return !1;
      for (state.kind = "scalar", state.result = ""; ch !== 0; )
        if (ch = state.input.charCodeAt(++state.position), ch === 43 || ch === 45)
          CHOMPING_CLIP === chomping ? chomping = ch === 43 ? CHOMPING_KEEP : CHOMPING_STRIP : throwError(state, "repeat of a chomping mode identifier");
        else if ((tmp = fromDecimalCode(ch)) >= 0)
          tmp === 0 ? throwError(state, "bad explicit indentation width of a block scalar; it cannot be less than one") : detectedIndent ? throwError(state, "repeat of an indentation width identifier") : (textIndent = nodeIndent + tmp - 1, detectedIndent = !0);
        else
          break;
      if (is_WHITE_SPACE(ch)) {
        do
          ch = state.input.charCodeAt(++state.position);
        while (is_WHITE_SPACE(ch));
        if (ch === 35)
          do
            ch = state.input.charCodeAt(++state.position);
          while (!is_EOL(ch) && ch !== 0);
      }
      for (; ch !== 0; ) {
        for (readLineBreak(state), state.lineIndent = 0, ch = state.input.charCodeAt(state.position); (!detectedIndent || state.lineIndent < textIndent) && ch === 32; )
          state.lineIndent++, ch = state.input.charCodeAt(++state.position);
        if (!detectedIndent && state.lineIndent > textIndent && (textIndent = state.lineIndent), is_EOL(ch)) {
          emptyLines++;
          continue;
        }
        if (state.lineIndent < textIndent) {
          chomping === CHOMPING_KEEP ? state.result += common.repeat(`
`, didReadContent ? 1 + emptyLines : emptyLines) : chomping === CHOMPING_CLIP && didReadContent && (state.result += `
`);
          break;
        }
        for (folding ? is_WHITE_SPACE(ch) ? (atMoreIndented = !0, state.result += common.repeat(`
`, didReadContent ? 1 + emptyLines : emptyLines)) : atMoreIndented ? (atMoreIndented = !1, state.result += common.repeat(`
`, emptyLines + 1)) : emptyLines === 0 ? didReadContent && (state.result += " ") : state.result += common.repeat(`
`, emptyLines) : state.result += common.repeat(`
`, didReadContent ? 1 + emptyLines : emptyLines), didReadContent = !0, detectedIndent = !0, emptyLines = 0, captureStart = state.position; !is_EOL(ch) && ch !== 0; )
          ch = state.input.charCodeAt(++state.position);
        captureSegment(state, captureStart, state.position, !1);
      }
      return !0;
    }
    function readBlockSequence(state, nodeIndent) {
      var _line, _tag = state.tag, _anchor = state.anchor, _result = [], following, detected = !1, ch;
      for (state.anchor !== null && (state.anchorMap[state.anchor] = _result), ch = state.input.charCodeAt(state.position); ch !== 0 && !(ch !== 45 || (following = state.input.charCodeAt(state.position + 1), !is_WS_OR_EOL(following))); ) {
        if (detected = !0, state.position++, skipSeparationSpace(state, !0, -1) && state.lineIndent <= nodeIndent) {
          _result.push(null), ch = state.input.charCodeAt(state.position);
          continue;
        }
        if (_line = state.line, composeNode(state, nodeIndent, CONTEXT_BLOCK_IN, !1, !0), _result.push(state.result), skipSeparationSpace(state, !0, -1), ch = state.input.charCodeAt(state.position), (state.line === _line || state.lineIndent > nodeIndent) && ch !== 0)
          throwError(state, "bad indentation of a sequence entry");
        else if (state.lineIndent < nodeIndent)
          break;
      }
      return detected ? (state.tag = _tag, state.anchor = _anchor, state.kind = "sequence", state.result = _result, !0) : !1;
    }
    function readBlockMapping(state, nodeIndent, flowIndent) {
      var following, allowCompact, _line, _pos, _tag = state.tag, _anchor = state.anchor, _result = {}, overridableKeys = {}, keyTag = null, keyNode = null, valueNode = null, atExplicitKey = !1, detected = !1, ch;
      for (state.anchor !== null && (state.anchorMap[state.anchor] = _result), ch = state.input.charCodeAt(state.position); ch !== 0; ) {
        if (following = state.input.charCodeAt(state.position + 1), _line = state.line, _pos = state.position, (ch === 63 || ch === 58) && is_WS_OR_EOL(following))
          ch === 63 ? (atExplicitKey && (storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, null), keyTag = keyNode = valueNode = null), detected = !0, atExplicitKey = !0, allowCompact = !0) : atExplicitKey ? (atExplicitKey = !1, allowCompact = !0) : throwError(state, "incomplete explicit mapping pair; a key node is missed; or followed by a non-tabulated empty line"), state.position += 1, ch = following;
        else if (composeNode(state, flowIndent, CONTEXT_FLOW_OUT, !1, !0))
          if (state.line === _line) {
            for (ch = state.input.charCodeAt(state.position); is_WHITE_SPACE(ch); )
              ch = state.input.charCodeAt(++state.position);
            if (ch === 58)
              ch = state.input.charCodeAt(++state.position), is_WS_OR_EOL(ch) || throwError(state, "a whitespace character is expected after the key-value separator within a block mapping"), atExplicitKey && (storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, null), keyTag = keyNode = valueNode = null), detected = !0, atExplicitKey = !1, allowCompact = !1, keyTag = state.tag, keyNode = state.result;
            else if (detected)
              throwError(state, "can not read an implicit mapping pair; a colon is missed");
            else
              return state.tag = _tag, state.anchor = _anchor, !0;
          } else if (detected)
            throwError(state, "can not read a block mapping entry; a multiline key may not be an implicit key");
          else
            return state.tag = _tag, state.anchor = _anchor, !0;
        else
          break;
        if ((state.line === _line || state.lineIndent > nodeIndent) && (composeNode(state, nodeIndent, CONTEXT_BLOCK_OUT, !0, allowCompact) && (atExplicitKey ? keyNode = state.result : valueNode = state.result), atExplicitKey || (storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, valueNode, _line, _pos), keyTag = keyNode = valueNode = null), skipSeparationSpace(state, !0, -1), ch = state.input.charCodeAt(state.position)), state.lineIndent > nodeIndent && ch !== 0)
          throwError(state, "bad indentation of a mapping entry");
        else if (state.lineIndent < nodeIndent)
          break;
      }
      return atExplicitKey && storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, null), detected && (state.tag = _tag, state.anchor = _anchor, state.kind = "mapping", state.result = _result), detected;
    }
    function readTagProperty(state) {
      var _position, isVerbatim = !1, isNamed = !1, tagHandle, tagName, ch;
      if (ch = state.input.charCodeAt(state.position), ch !== 33) return !1;
      if (state.tag !== null && throwError(state, "duplication of a tag property"), ch = state.input.charCodeAt(++state.position), ch === 60 ? (isVerbatim = !0, ch = state.input.charCodeAt(++state.position)) : ch === 33 ? (isNamed = !0, tagHandle = "!!", ch = state.input.charCodeAt(++state.position)) : tagHandle = "!", _position = state.position, isVerbatim) {
        do
          ch = state.input.charCodeAt(++state.position);
        while (ch !== 0 && ch !== 62);
        state.position < state.length ? (tagName = state.input.slice(_position, state.position), ch = state.input.charCodeAt(++state.position)) : throwError(state, "unexpected end of the stream within a verbatim tag");
      } else {
        for (; ch !== 0 && !is_WS_OR_EOL(ch); )
          ch === 33 && (isNamed ? throwError(state, "tag suffix cannot contain exclamation marks") : (tagHandle = state.input.slice(_position - 1, state.position + 1), PATTERN_TAG_HANDLE.test(tagHandle) || throwError(state, "named tag handle cannot contain such characters"), isNamed = !0, _position = state.position + 1)), ch = state.input.charCodeAt(++state.position);
        tagName = state.input.slice(_position, state.position), PATTERN_FLOW_INDICATORS.test(tagName) && throwError(state, "tag suffix cannot contain flow indicator characters");
      }
      return tagName && !PATTERN_TAG_URI.test(tagName) && throwError(state, "tag name cannot contain such characters: " + tagName), isVerbatim ? state.tag = tagName : _hasOwnProperty.call(state.tagMap, tagHandle) ? state.tag = state.tagMap[tagHandle] + tagName : tagHandle === "!" ? state.tag = "!" + tagName : tagHandle === "!!" ? state.tag = "tag:yaml.org,2002:" + tagName : throwError(state, 'undeclared tag handle "' + tagHandle + '"'), !0;
    }
    function readAnchorProperty(state) {
      var _position, ch;
      if (ch = state.input.charCodeAt(state.position), ch !== 38) return !1;
      for (state.anchor !== null && throwError(state, "duplication of an anchor property"), ch = state.input.charCodeAt(++state.position), _position = state.position; ch !== 0 && !is_WS_OR_EOL(ch) && !is_FLOW_INDICATOR(ch); )
        ch = state.input.charCodeAt(++state.position);
      return state.position === _position && throwError(state, "name of an anchor node must contain at least one character"), state.anchor = state.input.slice(_position, state.position), !0;
    }
    function readAlias(state) {
      var _position, alias, ch;
      if (ch = state.input.charCodeAt(state.position), ch !== 42) return !1;
      for (ch = state.input.charCodeAt(++state.position), _position = state.position; ch !== 0 && !is_WS_OR_EOL(ch) && !is_FLOW_INDICATOR(ch); )
        ch = state.input.charCodeAt(++state.position);
      return state.position === _position && throwError(state, "name of an alias node must contain at least one character"), alias = state.input.slice(_position, state.position), _hasOwnProperty.call(state.anchorMap, alias) || throwError(state, 'unidentified alias "' + alias + '"'), state.result = state.anchorMap[alias], skipSeparationSpace(state, !0, -1), !0;
    }
    function composeNode(state, parentIndent, nodeContext, allowToSeek, allowCompact) {
      var allowBlockStyles, allowBlockScalars, allowBlockCollections, indentStatus = 1, atNewLine = !1, hasContent = !1, typeIndex, typeQuantity, type, flowIndent, blockIndent;
      if (state.listener !== null && state.listener("open", state), state.tag = null, state.anchor = null, state.kind = null, state.result = null, allowBlockStyles = allowBlockScalars = allowBlockCollections = CONTEXT_BLOCK_OUT === nodeContext || CONTEXT_BLOCK_IN === nodeContext, allowToSeek && skipSeparationSpace(state, !0, -1) && (atNewLine = !0, state.lineIndent > parentIndent ? indentStatus = 1 : state.lineIndent === parentIndent ? indentStatus = 0 : state.lineIndent < parentIndent && (indentStatus = -1)), indentStatus === 1)
        for (; readTagProperty(state) || readAnchorProperty(state); )
          skipSeparationSpace(state, !0, -1) ? (atNewLine = !0, allowBlockCollections = allowBlockStyles, state.lineIndent > parentIndent ? indentStatus = 1 : state.lineIndent === parentIndent ? indentStatus = 0 : state.lineIndent < parentIndent && (indentStatus = -1)) : allowBlockCollections = !1;
      if (allowBlockCollections && (allowBlockCollections = atNewLine || allowCompact), (indentStatus === 1 || CONTEXT_BLOCK_OUT === nodeContext) && (CONTEXT_FLOW_IN === nodeContext || CONTEXT_FLOW_OUT === nodeContext ? flowIndent = parentIndent : flowIndent = parentIndent + 1, blockIndent = state.position - state.lineStart, indentStatus === 1 ? allowBlockCollections && (readBlockSequence(state, blockIndent) || readBlockMapping(state, blockIndent, flowIndent)) || readFlowCollection(state, flowIndent) ? hasContent = !0 : (allowBlockScalars && readBlockScalar(state, flowIndent) || readSingleQuotedScalar(state, flowIndent) || readDoubleQuotedScalar(state, flowIndent) ? hasContent = !0 : readAlias(state) ? (hasContent = !0, (state.tag !== null || state.anchor !== null) && throwError(state, "alias node should not have any properties")) : readPlainScalar(state, flowIndent, CONTEXT_FLOW_IN === nodeContext) && (hasContent = !0, state.tag === null && (state.tag = "?")), state.anchor !== null && (state.anchorMap[state.anchor] = state.result)) : indentStatus === 0 && (hasContent = allowBlockCollections && readBlockSequence(state, blockIndent))), state.tag !== null && state.tag !== "!")
        if (state.tag === "?") {
          for (state.result !== null && state.kind !== "scalar" && throwError(state, 'unacceptable node kind for !<?> tag; it should be "scalar", not "' + state.kind + '"'), typeIndex = 0, typeQuantity = state.implicitTypes.length; typeIndex < typeQuantity; typeIndex += 1)
            if (type = state.implicitTypes[typeIndex], type.resolve(state.result)) {
              state.result = type.construct(state.result), state.tag = type.tag, state.anchor !== null && (state.anchorMap[state.anchor] = state.result);
              break;
            }
        } else _hasOwnProperty.call(state.typeMap[state.kind || "fallback"], state.tag) ? (type = state.typeMap[state.kind || "fallback"][state.tag], state.result !== null && type.kind !== state.kind && throwError(state, "unacceptable node kind for !<" + state.tag + '> tag; it should be "' + type.kind + '", not "' + state.kind + '"'), type.resolve(state.result) ? (state.result = type.construct(state.result), state.anchor !== null && (state.anchorMap[state.anchor] = state.result)) : throwError(state, "cannot resolve a node with !<" + state.tag + "> explicit tag")) : throwError(state, "unknown tag !<" + state.tag + ">");
      return state.listener !== null && state.listener("close", state), state.tag !== null || state.anchor !== null || hasContent;
    }
    function readDocument(state) {
      var documentStart = state.position, _position, directiveName, directiveArgs, hasDirectives = !1, ch;
      for (state.version = null, state.checkLineBreaks = state.legacy, state.tagMap = {}, state.anchorMap = {}; (ch = state.input.charCodeAt(state.position)) !== 0 && (skipSeparationSpace(state, !0, -1), ch = state.input.charCodeAt(state.position), !(state.lineIndent > 0 || ch !== 37)); ) {
        for (hasDirectives = !0, ch = state.input.charCodeAt(++state.position), _position = state.position; ch !== 0 && !is_WS_OR_EOL(ch); )
          ch = state.input.charCodeAt(++state.position);
        for (directiveName = state.input.slice(_position, state.position), directiveArgs = [], directiveName.length < 1 && throwError(state, "directive name must not be less than one character in length"); ch !== 0; ) {
          for (; is_WHITE_SPACE(ch); )
            ch = state.input.charCodeAt(++state.position);
          if (ch === 35) {
            do
              ch = state.input.charCodeAt(++state.position);
            while (ch !== 0 && !is_EOL(ch));
            break;
          }
          if (is_EOL(ch)) break;
          for (_position = state.position; ch !== 0 && !is_WS_OR_EOL(ch); )
            ch = state.input.charCodeAt(++state.position);
          directiveArgs.push(state.input.slice(_position, state.position));
        }
        ch !== 0 && readLineBreak(state), _hasOwnProperty.call(directiveHandlers, directiveName) ? directiveHandlers[directiveName](state, directiveName, directiveArgs) : throwWarning(state, 'unknown document directive "' + directiveName + '"');
      }
      if (skipSeparationSpace(state, !0, -1), state.lineIndent === 0 && state.input.charCodeAt(state.position) === 45 && state.input.charCodeAt(state.position + 1) === 45 && state.input.charCodeAt(state.position + 2) === 45 ? (state.position += 3, skipSeparationSpace(state, !0, -1)) : hasDirectives && throwError(state, "directives end mark is expected"), composeNode(state, state.lineIndent - 1, CONTEXT_BLOCK_OUT, !1, !0), skipSeparationSpace(state, !0, -1), state.checkLineBreaks && PATTERN_NON_ASCII_LINE_BREAKS.test(state.input.slice(documentStart, state.position)) && throwWarning(state, "non-ASCII line breaks are interpreted as content"), state.documents.push(state.result), state.position === state.lineStart && testDocumentSeparator(state)) {
        state.input.charCodeAt(state.position) === 46 && (state.position += 3, skipSeparationSpace(state, !0, -1));
        return;
      }
      if (state.position < state.length - 1)
        throwError(state, "end of the stream or a document separator is expected");
      else
        return;
    }
    function loadDocuments(input, options) {
      input = String(input), options = options || {}, input.length !== 0 && (input.charCodeAt(input.length - 1) !== 10 && input.charCodeAt(input.length - 1) !== 13 && (input += `
`), input.charCodeAt(0) === 65279 && (input = input.slice(1)));
      var state = new State(input, options), nullpos = input.indexOf("\0");
      for (nullpos !== -1 && (state.position = nullpos, throwError(state, "null byte is not allowed in input")), state.input += "\0"; state.input.charCodeAt(state.position) === 32; )
        state.lineIndent += 1, state.position += 1;
      for (; state.position < state.length - 1; )
        readDocument(state);
      return state.documents;
    }
    function loadAll(input, iterator, options) {
      iterator !== null && typeof iterator == "object" && typeof options > "u" && (options = iterator, iterator = null);
      var documents = loadDocuments(input, options);
      if (typeof iterator != "function")
        return documents;
      for (var index = 0, length = documents.length; index < length; index += 1)
        iterator(documents[index]);
    }
    function load(input, options) {
      var documents = loadDocuments(input, options);
      if (documents.length !== 0) {
        if (documents.length === 1)
          return documents[0];
        throw new YAMLException("expected a single document in the stream, but found more");
      }
    }
    function safeLoadAll(input, iterator, options) {
      return typeof iterator == "object" && iterator !== null && typeof options > "u" && (options = iterator, iterator = null), loadAll(input, iterator, common.extend({ schema: DEFAULT_SAFE_SCHEMA }, options));
    }
    function safeLoad(input, options) {
      return load(input, common.extend({ schema: DEFAULT_SAFE_SCHEMA }, options));
    }
    module.exports.loadAll = loadAll;
    module.exports.load = load;
    module.exports.safeLoadAll = safeLoadAll;
    module.exports.safeLoad = safeLoad;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/dumper.js
var require_dumper = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml/dumper.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var common = require_common(), YAMLException = require_exception(), DEFAULT_FULL_SCHEMA = require_default_full(), DEFAULT_SAFE_SCHEMA = require_default_safe(), _toString = Object.prototype.toString, _hasOwnProperty = Object.prototype.hasOwnProperty, CHAR_TAB = 9, CHAR_LINE_FEED = 10, CHAR_CARRIAGE_RETURN = 13, CHAR_SPACE = 32, CHAR_EXCLAMATION = 33, CHAR_DOUBLE_QUOTE = 34, CHAR_SHARP = 35, CHAR_PERCENT = 37, CHAR_AMPERSAND = 38, CHAR_SINGLE_QUOTE = 39, CHAR_ASTERISK = 42, CHAR_COMMA = 44, CHAR_MINUS = 45, CHAR_COLON = 58, CHAR_EQUALS = 61, CHAR_GREATER_THAN = 62, CHAR_QUESTION = 63, CHAR_COMMERCIAL_AT = 64, CHAR_LEFT_SQUARE_BRACKET = 91, CHAR_RIGHT_SQUARE_BRACKET = 93, CHAR_GRAVE_ACCENT = 96, CHAR_LEFT_CURLY_BRACKET = 123, CHAR_VERTICAL_LINE = 124, CHAR_RIGHT_CURLY_BRACKET = 125, ESCAPE_SEQUENCES = {};
    ESCAPE_SEQUENCES[0] = "\\0";
    ESCAPE_SEQUENCES[7] = "\\a";
    ESCAPE_SEQUENCES[8] = "\\b";
    ESCAPE_SEQUENCES[9] = "\\t";
    ESCAPE_SEQUENCES[10] = "\\n";
    ESCAPE_SEQUENCES[11] = "\\v";
    ESCAPE_SEQUENCES[12] = "\\f";
    ESCAPE_SEQUENCES[13] = "\\r";
    ESCAPE_SEQUENCES[27] = "\\e";
    ESCAPE_SEQUENCES[34] = '\\"';
    ESCAPE_SEQUENCES[92] = "\\\\";
    ESCAPE_SEQUENCES[133] = "\\N";
    ESCAPE_SEQUENCES[160] = "\\_";
    ESCAPE_SEQUENCES[8232] = "\\L";
    ESCAPE_SEQUENCES[8233] = "\\P";
    var DEPRECATED_BOOLEANS_SYNTAX = [
      "y",
      "Y",
      "yes",
      "Yes",
      "YES",
      "on",
      "On",
      "ON",
      "n",
      "N",
      "no",
      "No",
      "NO",
      "off",
      "Off",
      "OFF"
    ];
    function compileStyleMap(schema, map) {
      var result, keys, index, length, tag, style, type;
      if (map === null) return {};
      for (result = {}, keys = Object.keys(map), index = 0, length = keys.length; index < length; index += 1)
        tag = keys[index], style = String(map[tag]), tag.slice(0, 2) === "!!" && (tag = "tag:yaml.org,2002:" + tag.slice(2)), type = schema.compiledTypeMap.fallback[tag], type && _hasOwnProperty.call(type.styleAliases, style) && (style = type.styleAliases[style]), result[tag] = style;
      return result;
    }
    function encodeHex(character) {
      var string, handle, length;
      if (string = character.toString(16).toUpperCase(), character <= 255)
        handle = "x", length = 2;
      else if (character <= 65535)
        handle = "u", length = 4;
      else if (character <= 4294967295)
        handle = "U", length = 8;
      else
        throw new YAMLException("code point within a string may not be greater than 0xFFFFFFFF");
      return "\\" + handle + common.repeat("0", length - string.length) + string;
    }
    function State(options) {
      this.schema = options.schema || DEFAULT_FULL_SCHEMA, this.indent = Math.max(1, options.indent || 2), this.noArrayIndent = options.noArrayIndent || !1, this.skipInvalid = options.skipInvalid || !1, this.flowLevel = common.isNothing(options.flowLevel) ? -1 : options.flowLevel, this.styleMap = compileStyleMap(this.schema, options.styles || null), this.sortKeys = options.sortKeys || !1, this.lineWidth = options.lineWidth || 80, this.noRefs = options.noRefs || !1, this.noCompatMode = options.noCompatMode || !1, this.condenseFlow = options.condenseFlow || !1, this.implicitTypes = this.schema.compiledImplicit, this.explicitTypes = this.schema.compiledExplicit, this.tag = null, this.result = "", this.duplicates = [], this.usedDuplicates = null;
    }
    function indentString(string, spaces) {
      for (var ind = common.repeat(" ", spaces), position = 0, next = -1, result = "", line, length = string.length; position < length; )
        next = string.indexOf(`
`, position), next === -1 ? (line = string.slice(position), position = length) : (line = string.slice(position, next + 1), position = next + 1), line.length && line !== `
` && (result += ind), result += line;
      return result;
    }
    function generateNextLine(state, level) {
      return `
` + common.repeat(" ", state.indent * level);
    }
    function testImplicitResolving(state, str) {
      var index, length, type;
      for (index = 0, length = state.implicitTypes.length; index < length; index += 1)
        if (type = state.implicitTypes[index], type.resolve(str))
          return !0;
      return !1;
    }
    function isWhitespace(c) {
      return c === CHAR_SPACE || c === CHAR_TAB;
    }
    function isPrintable(c) {
      return 32 <= c && c <= 126 || 161 <= c && c <= 55295 && c !== 8232 && c !== 8233 || 57344 <= c && c <= 65533 && c !== 65279 || 65536 <= c && c <= 1114111;
    }
    function isNsChar(c) {
      return isPrintable(c) && !isWhitespace(c) && c !== 65279 && c !== CHAR_CARRIAGE_RETURN && c !== CHAR_LINE_FEED;
    }
    function isPlainSafe(c, prev) {
      return isPrintable(c) && c !== 65279 && c !== CHAR_COMMA && c !== CHAR_LEFT_SQUARE_BRACKET && c !== CHAR_RIGHT_SQUARE_BRACKET && c !== CHAR_LEFT_CURLY_BRACKET && c !== CHAR_RIGHT_CURLY_BRACKET && c !== CHAR_COLON && (c !== CHAR_SHARP || prev && isNsChar(prev));
    }
    function isPlainSafeFirst(c) {
      return isPrintable(c) && c !== 65279 && !isWhitespace(c) && c !== CHAR_MINUS && c !== CHAR_QUESTION && c !== CHAR_COLON && c !== CHAR_COMMA && c !== CHAR_LEFT_SQUARE_BRACKET && c !== CHAR_RIGHT_SQUARE_BRACKET && c !== CHAR_LEFT_CURLY_BRACKET && c !== CHAR_RIGHT_CURLY_BRACKET && c !== CHAR_SHARP && c !== CHAR_AMPERSAND && c !== CHAR_ASTERISK && c !== CHAR_EXCLAMATION && c !== CHAR_VERTICAL_LINE && c !== CHAR_EQUALS && c !== CHAR_GREATER_THAN && c !== CHAR_SINGLE_QUOTE && c !== CHAR_DOUBLE_QUOTE && c !== CHAR_PERCENT && c !== CHAR_COMMERCIAL_AT && c !== CHAR_GRAVE_ACCENT;
    }
    function needIndentIndicator(string) {
      var leadingSpaceRe = /^\n* /;
      return leadingSpaceRe.test(string);
    }
    var STYLE_PLAIN = 1, STYLE_SINGLE = 2, STYLE_LITERAL = 3, STYLE_FOLDED = 4, STYLE_DOUBLE = 5;
    function chooseScalarStyle(string, singleLineOnly, indentPerLevel, lineWidth, testAmbiguousType) {
      var i, char, prev_char, hasLineBreak = !1, hasFoldableLine = !1, shouldTrackWidth = lineWidth !== -1, previousLineBreak = -1, plain = isPlainSafeFirst(string.charCodeAt(0)) && !isWhitespace(string.charCodeAt(string.length - 1));
      if (singleLineOnly)
        for (i = 0; i < string.length; i++) {
          if (char = string.charCodeAt(i), !isPrintable(char))
            return STYLE_DOUBLE;
          prev_char = i > 0 ? string.charCodeAt(i - 1) : null, plain = plain && isPlainSafe(char, prev_char);
        }
      else {
        for (i = 0; i < string.length; i++) {
          if (char = string.charCodeAt(i), char === CHAR_LINE_FEED)
            hasLineBreak = !0, shouldTrackWidth && (hasFoldableLine = hasFoldableLine || // Foldable line = too long, and not more-indented.
            i - previousLineBreak - 1 > lineWidth && string[previousLineBreak + 1] !== " ", previousLineBreak = i);
          else if (!isPrintable(char))
            return STYLE_DOUBLE;
          prev_char = i > 0 ? string.charCodeAt(i - 1) : null, plain = plain && isPlainSafe(char, prev_char);
        }
        hasFoldableLine = hasFoldableLine || shouldTrackWidth && i - previousLineBreak - 1 > lineWidth && string[previousLineBreak + 1] !== " ";
      }
      return !hasLineBreak && !hasFoldableLine ? plain && !testAmbiguousType(string) ? STYLE_PLAIN : STYLE_SINGLE : indentPerLevel > 9 && needIndentIndicator(string) ? STYLE_DOUBLE : hasFoldableLine ? STYLE_FOLDED : STYLE_LITERAL;
    }
    function writeScalar(state, string, level, iskey) {
      state.dump = (function() {
        if (string.length === 0)
          return "''";
        if (!state.noCompatMode && DEPRECATED_BOOLEANS_SYNTAX.indexOf(string) !== -1)
          return "'" + string + "'";
        var indent = state.indent * Math.max(1, level), lineWidth = state.lineWidth === -1 ? -1 : Math.max(Math.min(state.lineWidth, 40), state.lineWidth - indent), singleLineOnly = iskey || state.flowLevel > -1 && level >= state.flowLevel;
        function testAmbiguity(string2) {
          return testImplicitResolving(state, string2);
        }
        switch (chooseScalarStyle(string, singleLineOnly, state.indent, lineWidth, testAmbiguity)) {
          case STYLE_PLAIN:
            return string;
          case STYLE_SINGLE:
            return "'" + string.replace(/'/g, "''") + "'";
          case STYLE_LITERAL:
            return "|" + blockHeader(string, state.indent) + dropEndingNewline(indentString(string, indent));
          case STYLE_FOLDED:
            return ">" + blockHeader(string, state.indent) + dropEndingNewline(indentString(foldString(string, lineWidth), indent));
          case STYLE_DOUBLE:
            return '"' + escapeString(string, lineWidth) + '"';
          default:
            throw new YAMLException("impossible error: invalid scalar style");
        }
      })();
    }
    function blockHeader(string, indentPerLevel) {
      var indentIndicator = needIndentIndicator(string) ? String(indentPerLevel) : "", clip = string[string.length - 1] === `
`, keep = clip && (string[string.length - 2] === `
` || string === `
`), chomp = keep ? "+" : clip ? "" : "-";
      return indentIndicator + chomp + `
`;
    }
    function dropEndingNewline(string) {
      return string[string.length - 1] === `
` ? string.slice(0, -1) : string;
    }
    function foldString(string, width) {
      for (var lineRe = /(\n+)([^\n]*)/g, result = (function() {
        var nextLF = string.indexOf(`
`);
        return nextLF = nextLF !== -1 ? nextLF : string.length, lineRe.lastIndex = nextLF, foldLine(string.slice(0, nextLF), width);
      })(), prevMoreIndented = string[0] === `
` || string[0] === " ", moreIndented, match; match = lineRe.exec(string); ) {
        var prefix = match[1], line = match[2];
        moreIndented = line[0] === " ", result += prefix + (!prevMoreIndented && !moreIndented && line !== "" ? `
` : "") + foldLine(line, width), prevMoreIndented = moreIndented;
      }
      return result;
    }
    function foldLine(line, width) {
      if (line === "" || line[0] === " ") return line;
      for (var breakRe = / [^ ]/g, match, start = 0, end, curr = 0, next = 0, result = ""; match = breakRe.exec(line); )
        next = match.index, next - start > width && (end = curr > start ? curr : next, result += `
` + line.slice(start, end), start = end + 1), curr = next;
      return result += `
`, line.length - start > width && curr > start ? result += line.slice(start, curr) + `
` + line.slice(curr + 1) : result += line.slice(start), result.slice(1);
    }
    function escapeString(string) {
      for (var result = "", char, nextChar, escapeSeq, i = 0; i < string.length; i++) {
        if (char = string.charCodeAt(i), char >= 55296 && char <= 56319 && (nextChar = string.charCodeAt(i + 1), nextChar >= 56320 && nextChar <= 57343)) {
          result += encodeHex((char - 55296) * 1024 + nextChar - 56320 + 65536), i++;
          continue;
        }
        escapeSeq = ESCAPE_SEQUENCES[char], result += !escapeSeq && isPrintable(char) ? string[i] : escapeSeq || encodeHex(char);
      }
      return result;
    }
    function writeFlowSequence(state, level, object) {
      var _result = "", _tag = state.tag, index, length;
      for (index = 0, length = object.length; index < length; index += 1)
        writeNode(state, level, object[index], !1, !1) && (index !== 0 && (_result += "," + (state.condenseFlow ? "" : " ")), _result += state.dump);
      state.tag = _tag, state.dump = "[" + _result + "]";
    }
    function writeBlockSequence(state, level, object, compact) {
      var _result = "", _tag = state.tag, index, length;
      for (index = 0, length = object.length; index < length; index += 1)
        writeNode(state, level + 1, object[index], !0, !0) && ((!compact || index !== 0) && (_result += generateNextLine(state, level)), state.dump && CHAR_LINE_FEED === state.dump.charCodeAt(0) ? _result += "-" : _result += "- ", _result += state.dump);
      state.tag = _tag, state.dump = _result || "[]";
    }
    function writeFlowMapping(state, level, object) {
      var _result = "", _tag = state.tag, objectKeyList = Object.keys(object), index, length, objectKey, objectValue, pairBuffer;
      for (index = 0, length = objectKeyList.length; index < length; index += 1)
        pairBuffer = "", index !== 0 && (pairBuffer += ", "), state.condenseFlow && (pairBuffer += '"'), objectKey = objectKeyList[index], objectValue = object[objectKey], writeNode(state, level, objectKey, !1, !1) && (state.dump.length > 1024 && (pairBuffer += "? "), pairBuffer += state.dump + (state.condenseFlow ? '"' : "") + ":" + (state.condenseFlow ? "" : " "), writeNode(state, level, objectValue, !1, !1) && (pairBuffer += state.dump, _result += pairBuffer));
      state.tag = _tag, state.dump = "{" + _result + "}";
    }
    function writeBlockMapping(state, level, object, compact) {
      var _result = "", _tag = state.tag, objectKeyList = Object.keys(object), index, length, objectKey, objectValue, explicitPair, pairBuffer;
      if (state.sortKeys === !0)
        objectKeyList.sort();
      else if (typeof state.sortKeys == "function")
        objectKeyList.sort(state.sortKeys);
      else if (state.sortKeys)
        throw new YAMLException("sortKeys must be a boolean or a function");
      for (index = 0, length = objectKeyList.length; index < length; index += 1)
        pairBuffer = "", (!compact || index !== 0) && (pairBuffer += generateNextLine(state, level)), objectKey = objectKeyList[index], objectValue = object[objectKey], writeNode(state, level + 1, objectKey, !0, !0, !0) && (explicitPair = state.tag !== null && state.tag !== "?" || state.dump && state.dump.length > 1024, explicitPair && (state.dump && CHAR_LINE_FEED === state.dump.charCodeAt(0) ? pairBuffer += "?" : pairBuffer += "? "), pairBuffer += state.dump, explicitPair && (pairBuffer += generateNextLine(state, level)), writeNode(state, level + 1, objectValue, !0, explicitPair) && (state.dump && CHAR_LINE_FEED === state.dump.charCodeAt(0) ? pairBuffer += ":" : pairBuffer += ": ", pairBuffer += state.dump, _result += pairBuffer));
      state.tag = _tag, state.dump = _result || "{}";
    }
    function detectType(state, object, explicit) {
      var _result, typeList, index, length, type, style;
      for (typeList = explicit ? state.explicitTypes : state.implicitTypes, index = 0, length = typeList.length; index < length; index += 1)
        if (type = typeList[index], (type.instanceOf || type.predicate) && (!type.instanceOf || typeof object == "object" && object instanceof type.instanceOf) && (!type.predicate || type.predicate(object))) {
          if (state.tag = explicit ? type.tag : "?", type.represent) {
            if (style = state.styleMap[type.tag] || type.defaultStyle, _toString.call(type.represent) === "[object Function]")
              _result = type.represent(object, style);
            else if (_hasOwnProperty.call(type.represent, style))
              _result = type.represent[style](object, style);
            else
              throw new YAMLException("!<" + type.tag + '> tag resolver accepts not "' + style + '" style');
            state.dump = _result;
          }
          return !0;
        }
      return !1;
    }
    function writeNode(state, level, object, block, compact, iskey) {
      state.tag = null, state.dump = object, detectType(state, object, !1) || detectType(state, object, !0);
      var type = _toString.call(state.dump);
      block && (block = state.flowLevel < 0 || state.flowLevel > level);
      var objectOrArray = type === "[object Object]" || type === "[object Array]", duplicateIndex, duplicate;
      if (objectOrArray && (duplicateIndex = state.duplicates.indexOf(object), duplicate = duplicateIndex !== -1), (state.tag !== null && state.tag !== "?" || duplicate || state.indent !== 2 && level > 0) && (compact = !1), duplicate && state.usedDuplicates[duplicateIndex])
        state.dump = "*ref_" + duplicateIndex;
      else {
        if (objectOrArray && duplicate && !state.usedDuplicates[duplicateIndex] && (state.usedDuplicates[duplicateIndex] = !0), type === "[object Object]")
          block && Object.keys(state.dump).length !== 0 ? (writeBlockMapping(state, level, state.dump, compact), duplicate && (state.dump = "&ref_" + duplicateIndex + state.dump)) : (writeFlowMapping(state, level, state.dump), duplicate && (state.dump = "&ref_" + duplicateIndex + " " + state.dump));
        else if (type === "[object Array]") {
          var arrayLevel = state.noArrayIndent && level > 0 ? level - 1 : level;
          block && state.dump.length !== 0 ? (writeBlockSequence(state, arrayLevel, state.dump, compact), duplicate && (state.dump = "&ref_" + duplicateIndex + state.dump)) : (writeFlowSequence(state, arrayLevel, state.dump), duplicate && (state.dump = "&ref_" + duplicateIndex + " " + state.dump));
        } else if (type === "[object String]")
          state.tag !== "?" && writeScalar(state, state.dump, level, iskey);
        else {
          if (state.skipInvalid) return !1;
          throw new YAMLException("unacceptable kind of an object to dump " + type);
        }
        state.tag !== null && state.tag !== "?" && (state.dump = "!<" + state.tag + "> " + state.dump);
      }
      return !0;
    }
    function getDuplicateReferences(object, state) {
      var objects = [], duplicatesIndexes = [], index, length;
      for (inspectNode(object, objects, duplicatesIndexes), index = 0, length = duplicatesIndexes.length; index < length; index += 1)
        state.duplicates.push(objects[duplicatesIndexes[index]]);
      state.usedDuplicates = new Array(length);
    }
    function inspectNode(object, objects, duplicatesIndexes) {
      var objectKeyList, index, length;
      if (object !== null && typeof object == "object")
        if (index = objects.indexOf(object), index !== -1)
          duplicatesIndexes.indexOf(index) === -1 && duplicatesIndexes.push(index);
        else if (objects.push(object), Array.isArray(object))
          for (index = 0, length = object.length; index < length; index += 1)
            inspectNode(object[index], objects, duplicatesIndexes);
        else
          for (objectKeyList = Object.keys(object), index = 0, length = objectKeyList.length; index < length; index += 1)
            inspectNode(object[objectKeyList[index]], objects, duplicatesIndexes);
    }
    function dump(input, options) {
      options = options || {};
      var state = new State(options);
      return state.noRefs || getDuplicateReferences(input, state), writeNode(state, 0, input, !0, !0) ? state.dump + `
` : "";
    }
    function safeDump(input, options) {
      return dump(input, common.extend({ schema: DEFAULT_SAFE_SCHEMA }, options));
    }
    module.exports.dump = dump;
    module.exports.safeDump = safeDump;
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml.js
var require_js_yaml = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/lib/js-yaml.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var loader = require_loader(), dumper = require_dumper();
    function deprecated(name) {
      return function() {
        throw new Error("Function " + name + " is deprecated and cannot be used.");
      };
    }
    module.exports.Type = require_type();
    module.exports.Schema = require_schema();
    module.exports.FAILSAFE_SCHEMA = require_failsafe();
    module.exports.JSON_SCHEMA = require_json();
    module.exports.CORE_SCHEMA = require_core();
    module.exports.DEFAULT_SAFE_SCHEMA = require_default_safe();
    module.exports.DEFAULT_FULL_SCHEMA = require_default_full();
    module.exports.load = loader.load;
    module.exports.loadAll = loader.loadAll;
    module.exports.safeLoad = loader.safeLoad;
    module.exports.safeLoadAll = loader.safeLoadAll;
    module.exports.dump = dumper.dump;
    module.exports.safeDump = dumper.safeDump;
    module.exports.YAMLException = require_exception();
    module.exports.MINIMAL_SCHEMA = require_failsafe();
    module.exports.SAFE_SCHEMA = require_default_safe();
    module.exports.DEFAULT_SCHEMA = require_default_full();
    module.exports.scan = deprecated("scan");
    module.exports.parse = deprecated("parse");
    module.exports.compose = deprecated("compose");
    module.exports.addConstructor = deprecated("addConstructor");
  }
});

// ../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/index.js
var require_js_yaml2 = __commonJS({
  "../../node_modules/.pnpm/js-yaml@3.14.2/node_modules/js-yaml/index.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var yaml = require_js_yaml();
    module.exports = yaml;
  }
});

// ../../node_modules/.pnpm/natural-orderby@2.0.3/node_modules/natural-orderby/cjs/natural-orderby.js
var require_natural_orderby = __commonJS({
  "../../node_modules/.pnpm/natural-orderby@2.0.3/node_modules/natural-orderby/cjs/natural-orderby.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var compareNumbers = function(numberA, numberB) {
      return numberA < numberB ? -1 : numberA > numberB ? 1 : 0;
    }, RE_NUMBERS = /(^0x[\da-fA-F]+$|^([+-]?(?:\d+(?:\.\d*)?|\.\d+)(?:[eE][+-]?\d+)?(?!\.\d+)(?=\D|\s|$))|\d+)/g, RE_LEADING_OR_TRAILING_WHITESPACES = /^\s+|\s+$/g, RE_WHITESPACES = /\s+/g, RE_INT_OR_FLOAT = /^[+-]?(?:\d+(?:\.\d*)?|\.\d+)(?:[eE][+-]?\d+)?$/, RE_DATE = /(^([\w ]+,?[\w ]+)?[\w ]+,?[\w ]+\d+:\d+(:\d+)?[\w ]?|^\d{1,4}[/-]\d{1,4}[/-]\d{1,4}|^\w+, \w+ \d+, \d{4})/, RE_LEADING_ZERO = /^0+[1-9]{1}[0-9]*$/, RE_UNICODE_CHARACTERS = /[^\x00-\x80]/, compareUnicode = function(stringA, stringB) {
      var result = stringA.localeCompare(stringB);
      return result ? result / Math.abs(result) : 0;
    }, stringCompare = function(stringA, stringB) {
      return stringA < stringB ? -1 : stringA > stringB ? 1 : 0;
    }, compareChunks = function(chunksA, chunksB) {
      for (var lengthA = chunksA.length, lengthB = chunksB.length, size = Math.min(lengthA, lengthB), i = 0; i < size; i++) {
        var chunkA = chunksA[i], chunkB = chunksB[i];
        if (chunkA.normalizedString !== chunkB.normalizedString) {
          if (chunkA.normalizedString === "" != (chunkB.normalizedString === ""))
            return chunkA.normalizedString === "" ? -1 : 1;
          if (chunkA.parsedNumber !== void 0 && chunkB.parsedNumber !== void 0) {
            var result = compareNumbers(chunkA.parsedNumber, chunkB.parsedNumber);
            return result === 0 ? stringCompare(chunkA.normalizedString, chunkB.normalizedString) : result;
          } else return chunkA.parsedNumber !== void 0 || chunkB.parsedNumber !== void 0 ? chunkA.parsedNumber !== void 0 ? -1 : 1 : RE_UNICODE_CHARACTERS.test(chunkA.normalizedString + chunkB.normalizedString) && chunkA.normalizedString.localeCompare ? compareUnicode(chunkA.normalizedString, chunkB.normalizedString) : stringCompare(chunkA.normalizedString, chunkB.normalizedString);
        }
      }
      return lengthA > size || lengthB > size ? lengthA <= size ? -1 : 1 : 0;
    }, compareOtherTypes = function(valueA, valueB) {
      return (valueA.chunks ? !valueB.chunks : valueB.chunks) ? valueA.chunks ? -1 : 1 : (valueA.isNaN ? !valueB.isNaN : valueB.isNaN) ? valueA.isNaN ? -1 : 1 : (valueA.isSymbol ? !valueB.isSymbol : valueB.isSymbol) ? valueA.isSymbol ? -1 : 1 : (valueA.isObject ? !valueB.isObject : valueB.isObject) ? valueA.isObject ? -1 : 1 : (valueA.isArray ? !valueB.isArray : valueB.isArray) ? valueA.isArray ? -1 : 1 : (valueA.isFunction ? !valueB.isFunction : valueB.isFunction) ? valueA.isFunction ? -1 : 1 : (valueA.isNull ? !valueB.isNull : valueB.isNull) ? valueA.isNull ? -1 : 1 : 0;
    }, compareValues = function(valueA, valueB) {
      return valueA.value === valueB.value ? 0 : valueA.parsedNumber !== void 0 && valueB.parsedNumber !== void 0 ? compareNumbers(valueA.parsedNumber, valueB.parsedNumber) : valueA.chunks && valueB.chunks ? compareChunks(valueA.chunks, valueB.chunks) : compareOtherTypes(valueA, valueB);
    }, compareMultiple = function(recordA, recordB, orders) {
      for (var indexA = recordA.index, valuesA = recordA.values, indexB = recordB.index, valuesB = recordB.values, length = valuesA.length, ordersLength = orders.length, i = 0; i < length; i++) {
        var order = i < ordersLength ? orders[i] : null;
        if (order && typeof order == "function") {
          var result = order(valuesA[i].value, valuesB[i].value);
          if (result)
            return result;
        } else {
          var _result = compareValues(valuesA[i], valuesB[i]);
          if (_result)
            return _result * (order === "desc" ? -1 : 1);
        }
      }
      return indexA - indexB;
    }, createIdentifierFn = function(identifier) {
      return typeof identifier == "function" ? identifier : function(value) {
        if (Array.isArray(value)) {
          var index = Number(identifier);
          if (Number.isInteger(index))
            return value[index];
        } else if (value && typeof value == "object" && typeof identifier != "function")
          return value[identifier];
        return value;
      };
    }, stringify = function(value) {
      return typeof value == "boolean" || value instanceof Boolean ? Number(value).toString() : typeof value == "number" || value instanceof Number ? value.toString() : value instanceof Date ? value.getTime().toString() : typeof value == "string" || value instanceof String ? value.toLowerCase().replace(RE_LEADING_OR_TRAILING_WHITESPACES, "") : "";
    }, parseNumber = function(value) {
      if (value.length !== 0) {
        var parsedNumber = Number(value);
        if (!Number.isNaN(parsedNumber))
          return parsedNumber;
      }
    }, parseDate = function(value) {
      if (RE_DATE.test(value)) {
        var parsedDate = Date.parse(value);
        if (!Number.isNaN(parsedDate))
          return parsedDate;
      }
    }, numberify = function(value) {
      var parsedNumber = parseNumber(value);
      return parsedNumber !== void 0 ? parsedNumber : parseDate(value);
    }, createChunks = function(value) {
      return value.replace(RE_NUMBERS, "\0$1\0").replace(/\0$/, "").replace(/^\0/, "").split("\0");
    }, normalizeAlphaChunk = function(chunk) {
      return chunk.replace(RE_WHITESPACES, " ").replace(RE_LEADING_OR_TRAILING_WHITESPACES, "");
    }, normalizeNumericChunk = function(chunk, index, chunks) {
      if (RE_INT_OR_FLOAT.test(chunk) && (!RE_LEADING_ZERO.test(chunk) || index === 0 || chunks[index - 1] !== "."))
        return parseNumber(chunk) || 0;
    }, createChunkMap = function(chunk, index, chunks) {
      return {
        parsedNumber: normalizeNumericChunk(chunk, index, chunks),
        normalizedString: normalizeAlphaChunk(chunk)
      };
    }, createChunkMaps = function(value) {
      var chunksMaps = createChunks(value).map(createChunkMap);
      return chunksMaps;
    }, isFunction = function(value) {
      return typeof value == "function";
    }, isNaN2 = function(value) {
      return Number.isNaN(value) || value instanceof Number && Number.isNaN(value.valueOf());
    }, isNull = function(value) {
      return value === null;
    }, isObject = function(value) {
      return value !== null && typeof value == "object" && !Array.isArray(value) && !(value instanceof Number) && !(value instanceof String) && !(value instanceof Boolean) && !(value instanceof Date);
    }, isSymbol = function(value) {
      return typeof value == "symbol";
    }, isUndefined = function(value) {
      return value === void 0;
    }, getMappedValueRecord = function(value) {
      if (typeof value == "string" || value instanceof String || (typeof value == "number" || value instanceof Number) && !isNaN2(value) || typeof value == "boolean" || value instanceof Boolean || value instanceof Date) {
        var stringValue = stringify(value), parsedNumber = numberify(stringValue), chunks = createChunkMaps(parsedNumber ? "" + parsedNumber : stringValue);
        return {
          parsedNumber,
          chunks,
          value
        };
      }
      return {
        isArray: Array.isArray(value),
        isFunction: isFunction(value),
        isNaN: isNaN2(value),
        isNull: isNull(value),
        isObject: isObject(value),
        isSymbol: isSymbol(value),
        isUndefined: isUndefined(value),
        value
      };
    }, getValueByIdentifier = function(value, getValue) {
      return getValue(value);
    }, getElementByIndex = function(collection, index) {
      return collection[index];
    }, baseOrderBy = function(collection, identifiers, orders) {
      var identifierFns = identifiers.length ? identifiers.map(createIdentifierFn) : [function(value) {
        return value;
      }], mappedCollection = collection.map(function(element, index) {
        var values = identifierFns.map(function(identifier) {
          return getValueByIdentifier(element, identifier);
        }).map(getMappedValueRecord);
        return {
          index,
          values
        };
      });
      return mappedCollection.sort(function(recordA, recordB) {
        return compareMultiple(recordA, recordB, orders);
      }), mappedCollection.map(function(element) {
        return getElementByIndex(collection, element.index);
      });
    }, getIdentifiers = function(identifiers) {
      if (!identifiers)
        return [];
      var identifierList = Array.isArray(identifiers) ? [].concat(identifiers) : [identifiers];
      return identifierList.some(function(identifier) {
        return typeof identifier != "string" && typeof identifier != "number" && typeof identifier != "function";
      }) ? [] : identifierList;
    }, getOrders = function(orders) {
      if (!orders)
        return [];
      var orderList = Array.isArray(orders) ? [].concat(orders) : [orders];
      return orderList.some(function(order) {
        return order !== "asc" && order !== "desc" && typeof order != "function";
      }) ? [] : orderList;
    };
    function orderBy(collection, identifiers, orders) {
      if (!collection || !Array.isArray(collection))
        return [];
      var validatedIdentifiers = getIdentifiers(identifiers), validatedOrders = getOrders(orders);
      return baseOrderBy(collection, validatedIdentifiers, validatedOrders);
    }
    var baseCompare = function(options) {
      return function(valueA, valueB) {
        var a = getMappedValueRecord(valueA), b = getMappedValueRecord(valueB), result = compareValues(a, b);
        return result * (options.order === "desc" ? -1 : 1);
      };
    }, isValidOrder = function(value) {
      return typeof value == "string" && (value === "asc" || value === "desc");
    }, getOptions = function(customOptions) {
      var order = "asc";
      return typeof customOptions == "string" && isValidOrder(customOptions) ? order = customOptions : customOptions && typeof customOptions == "object" && customOptions.order && isValidOrder(customOptions.order) && (order = customOptions.order), {
        order
      };
    };
    function compare(options) {
      var validatedOptions = getOptions(options);
      return baseCompare(validatedOptions);
    }
    exports.orderBy = orderBy;
    exports.compare = compare;
  }
});

// ../../node_modules/.pnpm/astral-regex@2.0.0/node_modules/astral-regex/index.js
var require_astral_regex = __commonJS({
  "../../node_modules/.pnpm/astral-regex@2.0.0/node_modules/astral-regex/index.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var regex = "[\uD800-\uDBFF][\uDC00-\uDFFF]", astralRegex = (options) => options && options.exact ? new RegExp(`^${regex}$`) : new RegExp(regex, "g");
    module.exports = astralRegex;
  }
});

// ../../node_modules/.pnpm/slice-ansi@4.0.0/node_modules/slice-ansi/index.js
var require_slice_ansi = __commonJS({
  "../../node_modules/.pnpm/slice-ansi@4.0.0/node_modules/slice-ansi/index.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var isFullwidthCodePoint = require_is_fullwidth_code_point(), astralRegex = require_astral_regex(), ansiStyles = require_ansi_styles(), ESCAPES = [
      "\x1B",
      "\x9B"
    ], wrapAnsi = (code) => `${ESCAPES[0]}[${code}m`, checkAnsi = (ansiCodes, isEscapes, endAnsiCode) => {
      let output = [];
      ansiCodes = [...ansiCodes];
      for (let ansiCode of ansiCodes) {
        let ansiCodeOrigin = ansiCode;
        ansiCode.includes(";") && (ansiCode = ansiCode.split(";")[0][0] + "0");
        let item = ansiStyles.codes.get(Number.parseInt(ansiCode, 10));
        if (item) {
          let indexEscape = ansiCodes.indexOf(item.toString());
          indexEscape === -1 ? output.push(wrapAnsi(isEscapes ? item : ansiCodeOrigin)) : ansiCodes.splice(indexEscape, 1);
        } else if (isEscapes) {
          output.push(wrapAnsi(0));
          break;
        } else
          output.push(wrapAnsi(ansiCodeOrigin));
      }
      if (isEscapes && (output = output.filter((element, index) => output.indexOf(element) === index), endAnsiCode !== void 0)) {
        let fistEscapeCode = wrapAnsi(ansiStyles.codes.get(Number.parseInt(endAnsiCode, 10)));
        output = output.reduce((current, next) => next === fistEscapeCode ? [next, ...current] : [...current, next], []);
      }
      return output.join("");
    };
    module.exports = (string, begin, end) => {
      let characters = [...string], ansiCodes = [], stringEnd = typeof end == "number" ? end : characters.length, isInsideEscape = !1, ansiCode, visible = 0, output = "";
      for (let [index, character] of characters.entries()) {
        let leftEscape = !1;
        if (ESCAPES.includes(character)) {
          let code = /\d[^m]*/.exec(string.slice(index, index + 18));
          ansiCode = code && code.length > 0 ? code[0] : void 0, visible < stringEnd && (isInsideEscape = !0, ansiCode !== void 0 && ansiCodes.push(ansiCode));
        } else isInsideEscape && character === "m" && (isInsideEscape = !1, leftEscape = !0);
        if (!isInsideEscape && !leftEscape && visible++, !astralRegex({ exact: !0 }).test(character) && isFullwidthCodePoint(character.codePointAt()) && (visible++, typeof end != "number" && stringEnd++), visible > begin && visible <= stringEnd)
          output += character;
        else if (visible === begin && !isInsideEscape && ansiCode !== void 0)
          output = checkAnsi(ansiCodes);
        else if (visible >= stringEnd) {
          output += checkAnsi(ansiCodes, !0, ansiCode);
          break;
        }
      }
      return output;
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/flags.js
var require_flags = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/flags.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.option = exports.help = exports.version = exports.string = exports.url = exports.file = exports.directory = exports.integer = exports.boolean = exports.custom = void 0;
    var node_url_1 = __require("node:url"), errors_1 = require_errors(), help_1 = require_help(), fs_1 = require_fs();
    function custom(defaults) {
      return (options = {}) => ({
        parse: async (input, _ctx, _opts) => input,
        ...defaults,
        ...options,
        input: [],
        multiple: !!(options.multiple === void 0 ? defaults?.multiple ?? !1 : options.multiple),
        type: "option"
      });
    }
    exports.custom = custom;
    function boolean(options = {}) {
      return {
        parse: async (b, _) => b,
        ...options,
        allowNo: !!options.allowNo,
        type: "boolean"
      };
    }
    exports.boolean = boolean;
    exports.integer = custom({
      async parse(input, _, opts) {
        if (!/^-?\d+$/.test(input))
          throw new errors_1.CLIError(`Expected an integer but received: ${input}`);
        let num = Number.parseInt(input, 10);
        if (opts.min !== void 0 && num < opts.min)
          throw new errors_1.CLIError(`Expected an integer greater than or equal to ${opts.min} but received: ${input}`);
        if (opts.max !== void 0 && num > opts.max)
          throw new errors_1.CLIError(`Expected an integer less than or equal to ${opts.max} but received: ${input}`);
        return num;
      }
    });
    exports.directory = custom({
      async parse(input, _, opts) {
        return opts.exists ? (0, fs_1.dirExists)(input) : input;
      }
    });
    exports.file = custom({
      async parse(input, _, opts) {
        return opts.exists ? (0, fs_1.fileExists)(input) : input;
      }
    });
    exports.url = custom({
      async parse(input) {
        try {
          return new node_url_1.URL(input);
        } catch {
          throw new errors_1.CLIError(`Expected a valid url but received: ${input}`);
        }
      }
    });
    exports.string = custom();
    var version = (opts = {}) => boolean({
      description: "Show CLI version.",
      ...opts,
      async parse(_, ctx) {
        ctx.log(ctx.config.userAgent), ctx.exit(0);
      }
    });
    exports.version = version;
    var help = (opts = {}) => boolean({
      description: "Show CLI help.",
      ...opts,
      async parse(_, cmd) {
        let Help = await (0, help_1.loadHelpClass)(cmd.config);
        await new Help(cmd.config, cmd.config.pjson.oclif.helpOptions ?? cmd.config.pjson.helpOptions).showHelp(cmd.id ? [cmd.id, ...cmd.argv] : cmd.argv), cmd.exit(0);
      }
    });
    exports.help = help;
    function option(defaults) {
      return (options = {}) => ({
        parse: async (input, _ctx, _opts) => input,
        ...defaults,
        ...options,
        input: [],
        multiple: !!(options.multiple === void 0 ? defaults.multiple : options.multiple),
        type: "option"
      });
    }
    exports.option = option;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/table.js
var require_table = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/table.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.table = void 0;
    var chalk_1 = __importDefault(require_source()), js_yaml_1 = require_js_yaml2(), natural_orderby_1 = require_natural_orderby(), node_util_1 = __require("node:util"), slice_ansi_1 = __importDefault(require_slice_ansi()), string_width_1 = __importDefault(require_string_width()), F = __importStar(require_flags()), screen_1 = require_screen(), util_1 = require_util(), write_1 = __importDefault(require_write()), Table = class {
      data;
      columns;
      options;
      constructor(data, columns, options = {}) {
        this.data = data, this.columns = Object.keys(columns).map((key) => {
          let col = columns[key], extended2 = col.extended ?? !1, get = col.get ?? ((row) => row[key] ?? ""), header = typeof col.header == "string" ? col.header : (0, util_1.capitalize)(key.replaceAll("_", " ")), minWidth = Math.max(col.minWidth ?? 0, (0, string_width_1.default)(header) + 1);
          return {
            extended: extended2,
            get,
            header,
            key,
            minWidth
          };
        });
        let { columns: cols, csv, extended, filter, output, printLine, sort, title } = options;
        this.options = {
          columns: cols,
          extended,
          filter,
          "no-header": options["no-header"] ?? !1,
          "no-truncate": options["no-truncate"] ?? !1,
          output: csv ? "csv" : output,
          printLine: printLine ?? ((s) => write_1.default.stdout(s + `
`)),
          rowStart: " ",
          sort,
          title
        };
      }
      display() {
        let rows = this.data.map((d) => {
          let row = {};
          for (let col of this.columns) {
            let val = col.get(d);
            typeof val != "string" && (val = (0, node_util_1.inspect)(val, { breakLength: Number.POSITIVE_INFINITY })), row[col.key] = val;
          }
          return row;
        });
        if (this.options.filter) {
          let [header, regex] = this.options.filter.split("="), isNot = header[0] === "-";
          isNot && (header = header.slice(1));
          let col = this.findColumnFromHeader(header);
          if (!col || !regex)
            throw new Error("Filter flag has an invalid value");
          rows = rows.filter((d) => {
            let re = new RegExp(regex), match = d[col.key].match(re);
            return isNot ? !match : match;
          });
        }
        if (this.options.sort) {
          let sorters = this.options.sort.split(","), sortHeaders = sorters.map((k) => k[0] === "-" ? k.slice(1) : k), sortKeys = this.filterColumnsFromHeaders(sortHeaders).map((c) => (v) => v[c.key]), sortKeysOrder = sorters.map((k) => k[0] === "-" ? "desc" : "asc");
          rows = (0, natural_orderby_1.orderBy)(rows, sortKeys, sortKeysOrder);
        }
        if (this.options.columns) {
          let filters = this.options.columns.split(",");
          this.columns = this.filterColumnsFromHeaders(filters);
        } else this.options.extended || (this.columns = this.columns.filter((c) => !c.extended));
        switch (this.data = rows, this.options.output) {
          case "csv": {
            this.outputCSV();
            break;
          }
          case "json": {
            this.outputJSON();
            break;
          }
          case "yaml": {
            this.outputYAML();
            break;
          }
          default:
            this.outputTable();
        }
      }
      filterColumnsFromHeaders(filters) {
        filters = [...new Set(filters)];
        let cols = [];
        for (let f of filters) {
          let c = this.columns.find((c2) => c2.header.toLowerCase() === f.toLowerCase());
          c && cols.push(c);
        }
        return cols;
      }
      findColumnFromHeader(header) {
        return this.columns.find((c) => c.header.toLowerCase() === header.toLowerCase());
      }
      getCSVRow(d) {
        let values = this.columns.map((col) => d[col.key] || ""), lineToBeEscaped = values.find((e) => e.includes('"') || e.includes(`
`) || e.includes(`\r
`) || e.includes("\r") || e.includes(","));
        return values.map((e) => lineToBeEscaped ? `"${e.replaceAll('"', '""')}"` : e);
      }
      outputCSV() {
        let { columns, data, options } = this;
        options["no-header"] || options.printLine(columns.map((c) => c.header).join(","));
        for (let d of data) {
          let row = this.getCSVRow(d);
          options.printLine(row.join(","));
        }
      }
      outputJSON() {
        this.options.printLine(JSON.stringify(this.resolveColumnsToObjectArray(), void 0, 2));
      }
      outputTable() {
        let { data, options } = this, columns = this.columns.map((c) => {
          let maxWidth2 = Math.max((0, string_width_1.default)(".".padEnd(c.minWidth - 1)), (0, string_width_1.default)(c.header), getWidestColumnWith(data, c.key)) + 1;
          return {
            ...c,
            maxWidth: maxWidth2,
            width: maxWidth2
          };
        }), maxWidth = screen_1.stdtermwidth - 2;
        if ((() => {
          if (options["no-truncate"] || !process.stdout.isTTY && !process.env.CLI_UX_SKIP_TTY_CHECK || (0, util_1.sumBy)(columns, (c) => c.width) - maxWidth <= 0)
            return;
          for (let col of columns)
            col.width = col.minWidth;
          let dataMinWidth = (0, util_1.sumBy)(columns, (c) => c.minWidth);
          if (dataMinWidth >= maxWidth)
            return;
          let wiggleRoom = maxWidth - dataMinWidth, needyCols = columns.map((c) => ({ key: c.key, needs: c.maxWidth - c.width })).sort((a, b) => a.needs - b.needs);
          for (let { key, needs } of needyCols) {
            if (!needs)
              continue;
            let col = columns.find((c) => key === c.key);
            col && (wiggleRoom > needs ? (col.width = col.width + needs, wiggleRoom -= needs) : wiggleRoom && (col.width = col.width + wiggleRoom, wiggleRoom = 0));
          }
        })(), options.title && (options.printLine(options.title), options.printLine("".padEnd(columns.reduce((sum, col) => sum + col.width, 1), "=")), options.rowStart = "| "), !options["no-header"]) {
          let headers = options.rowStart;
          for (let col of columns) {
            let header = col.header;
            headers += header.padEnd(col.width);
          }
          options.printLine(chalk_1.default.bold(headers));
          let dividers = options.rowStart;
          for (let col of columns) {
            let divider = "".padEnd(col.width - 1, "\u2500") + " ";
            dividers += divider.padEnd(col.width);
          }
          options.printLine(chalk_1.default.bold(dividers));
        }
        for (let row of data) {
          let numOfLines = 1;
          for (let col of columns) {
            let lines = row[col.key].split(`
`).length;
            lines > numOfLines && (numOfLines = lines);
          }
          let linesIndexess = [...new Array(numOfLines).keys()];
          for (let i of linesIndexess) {
            let l = options.rowStart;
            for (let col of columns) {
              let width = col.width, d = row[col.key];
              d = d.split(`
`)[i] || "";
              let visualWidth = (0, string_width_1.default)(d), colorWidth = d.length - visualWidth, cell = d.padEnd(width + colorWidth);
              (cell.length - colorWidth > width || visualWidth === width) && (cell = (0, slice_ansi_1.default)(cell, 0, width - 2) + "\u2026 ", cell += " ".repeat(width - (0, string_width_1.default)(cell))), l += cell;
            }
            options.printLine(l);
          }
        }
      }
      outputYAML() {
        this.options.printLine((0, js_yaml_1.safeDump)(this.resolveColumnsToObjectArray()));
      }
      resolveColumnsToObjectArray() {
        let { columns, data } = this;
        return data.map((d) => Object.fromEntries(columns.map((col) => [col.key, d[col.key] ?? ""])));
      }
    };
    function table(data, columns, options = {}) {
      new Table(data, columns, options).display();
    }
    exports.table = table;
    (function(table2) {
      table2.Flags = {
        columns: F.string({ description: "only show provided columns (comma-separated)", exclusive: ["extended"] }),
        csv: F.boolean({ description: "output is csv format [alias: --output=csv]", exclusive: ["no-truncate"] }),
        extended: F.boolean({ char: "x", description: "show extra columns", exclusive: ["columns"] }),
        filter: F.string({ description: "filter property by partial string matching, ex: name=foo" }),
        "no-header": F.boolean({ description: "hide table header from output", exclusive: ["csv"] }),
        "no-truncate": F.boolean({ description: "do not truncate output to fit screen", exclusive: ["csv"] }),
        output: F.string({
          description: "output in a more machine friendly format",
          exclusive: ["no-truncate", "csv"],
          options: ["csv", "json", "yaml"]
        }),
        sort: F.string({ description: "property to sort by (prepend '-' for descending)" })
      };
      function flags(opts) {
        if (opts) {
          let f = {}, o = (opts.only && typeof opts.only == "string" ? [opts.only] : opts.only) || Object.keys(table2.Flags), e = (opts.except && typeof opts.except == "string" ? [opts.except] : opts.except) || [];
          for (let key of o)
            e.includes(key) || (f[key] = table2.Flags[key]);
          return f;
        }
        return table2.Flags;
      }
      table2.flags = flags;
    })(table || (exports.table = table = {}));
    var getWidestColumnWith = (data, columnKey) => data.reduce((previous, current) => {
      let d = current[columnKey], manyLines = d.split(`
`);
      return Math.max(previous, manyLines.length > 1 ? Math.max(...manyLines.map((r) => (0, string_width_1.default)(r))) : (0, string_width_1.default)(d));
    }, 0);
  }
});

// ../../node_modules/.pnpm/object-treeify@1.1.33/node_modules/object-treeify/lib/index.js
var require_lib = __commonJS({
  "../../node_modules/.pnpm/object-treeify@1.1.33/node_modules/object-treeify/lib/index.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var assert = console.assert, buildCtx = (opts) => {
      let ctx = {
        joined: !0,
        spacerNoNeighbour: "   ",
        spacerNeighbour: "\u2502  ",
        keyNoNeighbour: "\u2514\u2500 ",
        keyNeighbour: "\u251C\u2500 ",
        sortFn: null,
        ...opts
      };
      return assert(Object.keys(ctx).length === 6, "Unexpected Option(s) provided"), assert(typeof ctx.joined == "boolean", 'Option "joined" has invalid format'), assert(typeof ctx.spacerNoNeighbour == "string", 'Option "spacerNoNeighbour" has invalid format'), assert(typeof ctx.spacerNeighbour == "string", 'Option "spacerNeighbour" has invalid format'), assert(typeof ctx.keyNoNeighbour == "string", 'Option "keyNoNeighbour" has invalid format'), assert(typeof ctx.keyNeighbour == "string", 'Option "keyNeighbour" has invalid format'), assert(typeof ctx.sortFn == "function" || ctx.sortFn === null, 'Option "sortFn" has invalid format'), ctx;
    };
    module.exports = (tree, opts = {}) => {
      let ctx = buildCtx(opts), result = [], sort = (input) => ctx.sortFn === null ? input.reverse() : input.sort((a, b) => ctx.sortFn(b, a)), neighbours = [], keys = sort(Object.keys(tree)).map((k) => [k]), lookup = [tree];
      for (; keys.length !== 0; ) {
        let key = keys.pop(), node = lookup[key.length - 1][key[key.length - 1]];
        neighbours[key.length - 1] = keys.length !== 0 && keys[keys.length - 1].length === key.length, result.push([neighbours.slice(0, key.length - 1).map((n) => n ? ctx.spacerNeighbour : ctx.spacerNoNeighbour).join(""), neighbours[key.length - 1] ? ctx.keyNeighbour : ctx.keyNoNeighbour, key[key.length - 1], ["boolean", "string", "number"].includes(typeof node) ? `: ${node}` : ""].join("")), node instanceof Object && !Array.isArray(node) && (keys.push(...sort(Object.keys(node)).map((k) => key.concat(k))), lookup[key.length] = node);
      }
      return ctx.joined === !0 ? result.join(`
`) : result;
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/tree.js
var require_tree = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/tree.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.Tree = void 0;
    var treeify = require_lib(), Tree = class _Tree {
      nodes = {};
      display(logger = console.log) {
        let addNodes = function(nodes) {
          let tree3 = {};
          for (let p of Object.keys(nodes))
            tree3[p] = addNodes(nodes[p].nodes);
          return tree3;
        }, tree2 = addNodes(this.nodes);
        logger(treeify(tree2));
      }
      insert(child, value = new _Tree()) {
        return this.nodes[child] = value, this;
      }
      search(key) {
        for (let child of Object.keys(this.nodes)) {
          if (child === key)
            return this.nodes[child];
          let c = this.nodes[child].search(key);
          if (c)
            return c;
        }
      }
    };
    exports.Tree = Tree;
    function tree() {
      return new Tree();
    }
    exports.default = tree;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/index.js
var require_styled = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/styled/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.tree = exports.Table = exports.progress = exports.styledObject = void 0;
    var object_1 = require_object();
    Object.defineProperty(exports, "styledObject", { enumerable: !0, get: function() {
      return __importDefault(object_1).default;
    } });
    var progress_1 = require_progress();
    Object.defineProperty(exports, "progress", { enumerable: !0, get: function() {
      return __importDefault(progress_1).default;
    } });
    exports.Table = __importStar(require_table());
    var tree_1 = require_tree();
    Object.defineProperty(exports, "tree", { enumerable: !0, get: function() {
      return __importDefault(tree_1).default;
    } });
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/wait.js
var require_wait = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/wait.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.default = (ms = 1e3) => new Promise((resolve2) => {
      setTimeout(resolve2, ms);
    });
  }
});

// ../../node_modules/.pnpm/hyperlinker@1.0.0/node_modules/hyperlinker/index.js
var require_hyperlinker = __commonJS({
  "../../node_modules/.pnpm/hyperlinker@1.0.0/node_modules/hyperlinker/index.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    module.exports = (text, uri, params) => (params = params || {}, [
      "\x1B]",
      "8",
      ";",
      Object.keys(params).map((key) => key + "=" + params[key]).join(":"),
      ";",
      uri,
      "\x07",
      text,
      "\x1B]",
      "8",
      ";",
      ";",
      "\x07"
    ].join(""));
  }
});

// ../../node_modules/.pnpm/esprima@4.0.1/node_modules/esprima/dist/esprima.js
var require_esprima = __commonJS({
  "../../node_modules/.pnpm/esprima@4.0.1/node_modules/esprima/dist/esprima.js"(exports, module) {
    init_cjs_shims();
    (function(root, factory) {
      typeof exports == "object" && typeof module == "object" ? module.exports = factory() : typeof define == "function" && define.amd ? define([], factory) : typeof exports == "object" ? exports.esprima = factory() : root.esprima = factory();
    })(exports, function() {
      return (
        /******/
        (function(modules) {
          var installedModules = {};
          function __webpack_require__(moduleId) {
            if (installedModules[moduleId])
              return installedModules[moduleId].exports;
            var module2 = installedModules[moduleId] = {
              /******/
              exports: {},
              /******/
              id: moduleId,
              /******/
              loaded: !1
              /******/
            };
            return modules[moduleId].call(module2.exports, module2, module2.exports, __webpack_require__), module2.loaded = !0, module2.exports;
          }
          return __webpack_require__.m = modules, __webpack_require__.c = installedModules, __webpack_require__.p = "", __webpack_require__(0);
        })([
          /* 0 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var comment_handler_1 = __webpack_require__(1), jsx_parser_1 = __webpack_require__(3), parser_1 = __webpack_require__(8), tokenizer_1 = __webpack_require__(15);
            function parse2(code, options, delegate) {
              var commentHandler = null, proxyDelegate = function(node, metadata) {
                delegate && delegate(node, metadata), commentHandler && commentHandler.visit(node, metadata);
              }, parserDelegate = typeof delegate == "function" ? proxyDelegate : null, collectComment = !1;
              if (options) {
                collectComment = typeof options.comment == "boolean" && options.comment;
                var attachComment = typeof options.attachComment == "boolean" && options.attachComment;
                (collectComment || attachComment) && (commentHandler = new comment_handler_1.CommentHandler(), commentHandler.attach = attachComment, options.comment = !0, parserDelegate = proxyDelegate);
              }
              var isModule = !1;
              options && typeof options.sourceType == "string" && (isModule = options.sourceType === "module");
              var parser4;
              options && typeof options.jsx == "boolean" && options.jsx ? parser4 = new jsx_parser_1.JSXParser(code, options, parserDelegate) : parser4 = new parser_1.Parser(code, options, parserDelegate);
              var program = isModule ? parser4.parseModule() : parser4.parseScript(), ast = program;
              return collectComment && commentHandler && (ast.comments = commentHandler.comments), parser4.config.tokens && (ast.tokens = parser4.tokens), parser4.config.tolerant && (ast.errors = parser4.errorHandler.errors), ast;
            }
            exports2.parse = parse2;
            function parseModule(code, options, delegate) {
              var parsingOptions = options || {};
              return parsingOptions.sourceType = "module", parse2(code, parsingOptions, delegate);
            }
            exports2.parseModule = parseModule;
            function parseScript(code, options, delegate) {
              var parsingOptions = options || {};
              return parsingOptions.sourceType = "script", parse2(code, parsingOptions, delegate);
            }
            exports2.parseScript = parseScript;
            function tokenize(code, options, delegate) {
              var tokenizer = new tokenizer_1.Tokenizer(code, options), tokens;
              tokens = [];
              try {
                for (; ; ) {
                  var token = tokenizer.getNextToken();
                  if (!token)
                    break;
                  delegate && (token = delegate(token)), tokens.push(token);
                }
              } catch (e) {
                tokenizer.errorHandler.tolerate(e);
              }
              return tokenizer.errorHandler.tolerant && (tokens.errors = tokenizer.errors()), tokens;
            }
            exports2.tokenize = tokenize;
            var syntax_1 = __webpack_require__(2);
            exports2.Syntax = syntax_1.Syntax, exports2.version = "4.0.1";
          },
          /* 1 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var syntax_1 = __webpack_require__(2), CommentHandler = (function() {
              function CommentHandler2() {
                this.attach = !1, this.comments = [], this.stack = [], this.leading = [], this.trailing = [];
              }
              return CommentHandler2.prototype.insertInnerComments = function(node, metadata) {
                if (node.type === syntax_1.Syntax.BlockStatement && node.body.length === 0) {
                  for (var innerComments = [], i = this.leading.length - 1; i >= 0; --i) {
                    var entry = this.leading[i];
                    metadata.end.offset >= entry.start && (innerComments.unshift(entry.comment), this.leading.splice(i, 1), this.trailing.splice(i, 1));
                  }
                  innerComments.length && (node.innerComments = innerComments);
                }
              }, CommentHandler2.prototype.findTrailingComments = function(metadata) {
                var trailingComments = [];
                if (this.trailing.length > 0) {
                  for (var i = this.trailing.length - 1; i >= 0; --i) {
                    var entry_1 = this.trailing[i];
                    entry_1.start >= metadata.end.offset && trailingComments.unshift(entry_1.comment);
                  }
                  return this.trailing.length = 0, trailingComments;
                }
                var entry = this.stack[this.stack.length - 1];
                if (entry && entry.node.trailingComments) {
                  var firstComment = entry.node.trailingComments[0];
                  firstComment && firstComment.range[0] >= metadata.end.offset && (trailingComments = entry.node.trailingComments, delete entry.node.trailingComments);
                }
                return trailingComments;
              }, CommentHandler2.prototype.findLeadingComments = function(metadata) {
                for (var leadingComments = [], target; this.stack.length > 0; ) {
                  var entry = this.stack[this.stack.length - 1];
                  if (entry && entry.start >= metadata.start.offset)
                    target = entry.node, this.stack.pop();
                  else
                    break;
                }
                if (target) {
                  for (var count = target.leadingComments ? target.leadingComments.length : 0, i = count - 1; i >= 0; --i) {
                    var comment = target.leadingComments[i];
                    comment.range[1] <= metadata.start.offset && (leadingComments.unshift(comment), target.leadingComments.splice(i, 1));
                  }
                  return target.leadingComments && target.leadingComments.length === 0 && delete target.leadingComments, leadingComments;
                }
                for (var i = this.leading.length - 1; i >= 0; --i) {
                  var entry = this.leading[i];
                  entry.start <= metadata.start.offset && (leadingComments.unshift(entry.comment), this.leading.splice(i, 1));
                }
                return leadingComments;
              }, CommentHandler2.prototype.visitNode = function(node, metadata) {
                if (!(node.type === syntax_1.Syntax.Program && node.body.length > 0)) {
                  this.insertInnerComments(node, metadata);
                  var trailingComments = this.findTrailingComments(metadata), leadingComments = this.findLeadingComments(metadata);
                  leadingComments.length > 0 && (node.leadingComments = leadingComments), trailingComments.length > 0 && (node.trailingComments = trailingComments), this.stack.push({
                    node,
                    start: metadata.start.offset
                  });
                }
              }, CommentHandler2.prototype.visitComment = function(node, metadata) {
                var type = node.type[0] === "L" ? "Line" : "Block", comment = {
                  type,
                  value: node.value
                };
                if (node.range && (comment.range = node.range), node.loc && (comment.loc = node.loc), this.comments.push(comment), this.attach) {
                  var entry = {
                    comment: {
                      type,
                      value: node.value,
                      range: [metadata.start.offset, metadata.end.offset]
                    },
                    start: metadata.start.offset
                  };
                  node.loc && (entry.comment.loc = node.loc), node.type = type, this.leading.push(entry), this.trailing.push(entry);
                }
              }, CommentHandler2.prototype.visit = function(node, metadata) {
                node.type === "LineComment" ? this.visitComment(node, metadata) : node.type === "BlockComment" ? this.visitComment(node, metadata) : this.attach && this.visitNode(node, metadata);
              }, CommentHandler2;
            })();
            exports2.CommentHandler = CommentHandler;
          },
          /* 2 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 }), exports2.Syntax = {
              AssignmentExpression: "AssignmentExpression",
              AssignmentPattern: "AssignmentPattern",
              ArrayExpression: "ArrayExpression",
              ArrayPattern: "ArrayPattern",
              ArrowFunctionExpression: "ArrowFunctionExpression",
              AwaitExpression: "AwaitExpression",
              BlockStatement: "BlockStatement",
              BinaryExpression: "BinaryExpression",
              BreakStatement: "BreakStatement",
              CallExpression: "CallExpression",
              CatchClause: "CatchClause",
              ClassBody: "ClassBody",
              ClassDeclaration: "ClassDeclaration",
              ClassExpression: "ClassExpression",
              ConditionalExpression: "ConditionalExpression",
              ContinueStatement: "ContinueStatement",
              DoWhileStatement: "DoWhileStatement",
              DebuggerStatement: "DebuggerStatement",
              EmptyStatement: "EmptyStatement",
              ExportAllDeclaration: "ExportAllDeclaration",
              ExportDefaultDeclaration: "ExportDefaultDeclaration",
              ExportNamedDeclaration: "ExportNamedDeclaration",
              ExportSpecifier: "ExportSpecifier",
              ExpressionStatement: "ExpressionStatement",
              ForStatement: "ForStatement",
              ForOfStatement: "ForOfStatement",
              ForInStatement: "ForInStatement",
              FunctionDeclaration: "FunctionDeclaration",
              FunctionExpression: "FunctionExpression",
              Identifier: "Identifier",
              IfStatement: "IfStatement",
              ImportDeclaration: "ImportDeclaration",
              ImportDefaultSpecifier: "ImportDefaultSpecifier",
              ImportNamespaceSpecifier: "ImportNamespaceSpecifier",
              ImportSpecifier: "ImportSpecifier",
              Literal: "Literal",
              LabeledStatement: "LabeledStatement",
              LogicalExpression: "LogicalExpression",
              MemberExpression: "MemberExpression",
              MetaProperty: "MetaProperty",
              MethodDefinition: "MethodDefinition",
              NewExpression: "NewExpression",
              ObjectExpression: "ObjectExpression",
              ObjectPattern: "ObjectPattern",
              Program: "Program",
              Property: "Property",
              RestElement: "RestElement",
              ReturnStatement: "ReturnStatement",
              SequenceExpression: "SequenceExpression",
              SpreadElement: "SpreadElement",
              Super: "Super",
              SwitchCase: "SwitchCase",
              SwitchStatement: "SwitchStatement",
              TaggedTemplateExpression: "TaggedTemplateExpression",
              TemplateElement: "TemplateElement",
              TemplateLiteral: "TemplateLiteral",
              ThisExpression: "ThisExpression",
              ThrowStatement: "ThrowStatement",
              TryStatement: "TryStatement",
              UnaryExpression: "UnaryExpression",
              UpdateExpression: "UpdateExpression",
              VariableDeclaration: "VariableDeclaration",
              VariableDeclarator: "VariableDeclarator",
              WhileStatement: "WhileStatement",
              WithStatement: "WithStatement",
              YieldExpression: "YieldExpression"
            };
          },
          /* 3 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            var __extends = this && this.__extends || (function() {
              var extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
                d.__proto__ = b;
              } || function(d, b) {
                for (var p in b) b.hasOwnProperty(p) && (d[p] = b[p]);
              };
              return function(d, b) {
                extendStatics(d, b);
                function __() {
                  this.constructor = d;
                }
                d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
              };
            })();
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var character_1 = __webpack_require__(4), JSXNode = __webpack_require__(5), jsx_syntax_1 = __webpack_require__(6), Node = __webpack_require__(7), parser_1 = __webpack_require__(8), token_1 = __webpack_require__(13), xhtml_entities_1 = __webpack_require__(14);
            token_1.TokenName[
              100
              /* Identifier */
            ] = "JSXIdentifier", token_1.TokenName[
              101
              /* Text */
            ] = "JSXText";
            function getQualifiedElementName(elementName) {
              var qualifiedName;
              switch (elementName.type) {
                case jsx_syntax_1.JSXSyntax.JSXIdentifier:
                  var id = elementName;
                  qualifiedName = id.name;
                  break;
                case jsx_syntax_1.JSXSyntax.JSXNamespacedName:
                  var ns = elementName;
                  qualifiedName = getQualifiedElementName(ns.namespace) + ":" + getQualifiedElementName(ns.name);
                  break;
                case jsx_syntax_1.JSXSyntax.JSXMemberExpression:
                  var expr = elementName;
                  qualifiedName = getQualifiedElementName(expr.object) + "." + getQualifiedElementName(expr.property);
                  break;
                /* istanbul ignore next */
                default:
                  break;
              }
              return qualifiedName;
            }
            var JSXParser = (function(_super) {
              __extends(JSXParser2, _super);
              function JSXParser2(code, options, delegate) {
                return _super.call(this, code, options, delegate) || this;
              }
              return JSXParser2.prototype.parsePrimaryExpression = function() {
                return this.match("<") ? this.parseJSXRoot() : _super.prototype.parsePrimaryExpression.call(this);
              }, JSXParser2.prototype.startJSX = function() {
                this.scanner.index = this.startMarker.index, this.scanner.lineNumber = this.startMarker.line, this.scanner.lineStart = this.startMarker.index - this.startMarker.column;
              }, JSXParser2.prototype.finishJSX = function() {
                this.nextToken();
              }, JSXParser2.prototype.reenterJSX = function() {
                this.startJSX(), this.expectJSX("}"), this.config.tokens && this.tokens.pop();
              }, JSXParser2.prototype.createJSXNode = function() {
                return this.collectComments(), {
                  index: this.scanner.index,
                  line: this.scanner.lineNumber,
                  column: this.scanner.index - this.scanner.lineStart
                };
              }, JSXParser2.prototype.createJSXChildNode = function() {
                return {
                  index: this.scanner.index,
                  line: this.scanner.lineNumber,
                  column: this.scanner.index - this.scanner.lineStart
                };
              }, JSXParser2.prototype.scanXHTMLEntity = function(quote) {
                for (var result = "&", valid = !0, terminated = !1, numeric = !1, hex = !1; !this.scanner.eof() && valid && !terminated; ) {
                  var ch = this.scanner.source[this.scanner.index];
                  if (ch === quote)
                    break;
                  if (terminated = ch === ";", result += ch, ++this.scanner.index, !terminated)
                    switch (result.length) {
                      case 2:
                        numeric = ch === "#";
                        break;
                      case 3:
                        numeric && (hex = ch === "x", valid = hex || character_1.Character.isDecimalDigit(ch.charCodeAt(0)), numeric = numeric && !hex);
                        break;
                      default:
                        valid = valid && !(numeric && !character_1.Character.isDecimalDigit(ch.charCodeAt(0))), valid = valid && !(hex && !character_1.Character.isHexDigit(ch.charCodeAt(0)));
                        break;
                    }
                }
                if (valid && terminated && result.length > 2) {
                  var str = result.substr(1, result.length - 2);
                  numeric && str.length > 1 ? result = String.fromCharCode(parseInt(str.substr(1), 10)) : hex && str.length > 2 ? result = String.fromCharCode(parseInt("0" + str.substr(1), 16)) : !numeric && !hex && xhtml_entities_1.XHTMLEntities[str] && (result = xhtml_entities_1.XHTMLEntities[str]);
                }
                return result;
              }, JSXParser2.prototype.lexJSX = function() {
                var cp = this.scanner.source.charCodeAt(this.scanner.index);
                if (cp === 60 || cp === 62 || cp === 47 || cp === 58 || cp === 61 || cp === 123 || cp === 125) {
                  var value = this.scanner.source[this.scanner.index++];
                  return {
                    type: 7,
                    value,
                    lineNumber: this.scanner.lineNumber,
                    lineStart: this.scanner.lineStart,
                    start: this.scanner.index - 1,
                    end: this.scanner.index
                  };
                }
                if (cp === 34 || cp === 39) {
                  for (var start = this.scanner.index, quote = this.scanner.source[this.scanner.index++], str = ""; !this.scanner.eof(); ) {
                    var ch = this.scanner.source[this.scanner.index++];
                    if (ch === quote)
                      break;
                    ch === "&" ? str += this.scanXHTMLEntity(quote) : str += ch;
                  }
                  return {
                    type: 8,
                    value: str,
                    lineNumber: this.scanner.lineNumber,
                    lineStart: this.scanner.lineStart,
                    start,
                    end: this.scanner.index
                  };
                }
                if (cp === 46) {
                  var n1 = this.scanner.source.charCodeAt(this.scanner.index + 1), n2 = this.scanner.source.charCodeAt(this.scanner.index + 2), value = n1 === 46 && n2 === 46 ? "..." : ".", start = this.scanner.index;
                  return this.scanner.index += value.length, {
                    type: 7,
                    value,
                    lineNumber: this.scanner.lineNumber,
                    lineStart: this.scanner.lineStart,
                    start,
                    end: this.scanner.index
                  };
                }
                if (cp === 96)
                  return {
                    type: 10,
                    value: "",
                    lineNumber: this.scanner.lineNumber,
                    lineStart: this.scanner.lineStart,
                    start: this.scanner.index,
                    end: this.scanner.index
                  };
                if (character_1.Character.isIdentifierStart(cp) && cp !== 92) {
                  var start = this.scanner.index;
                  for (++this.scanner.index; !this.scanner.eof(); ) {
                    var ch = this.scanner.source.charCodeAt(this.scanner.index);
                    if (character_1.Character.isIdentifierPart(ch) && ch !== 92)
                      ++this.scanner.index;
                    else if (ch === 45)
                      ++this.scanner.index;
                    else
                      break;
                  }
                  var id = this.scanner.source.slice(start, this.scanner.index);
                  return {
                    type: 100,
                    value: id,
                    lineNumber: this.scanner.lineNumber,
                    lineStart: this.scanner.lineStart,
                    start,
                    end: this.scanner.index
                  };
                }
                return this.scanner.lex();
              }, JSXParser2.prototype.nextJSXToken = function() {
                this.collectComments(), this.startMarker.index = this.scanner.index, this.startMarker.line = this.scanner.lineNumber, this.startMarker.column = this.scanner.index - this.scanner.lineStart;
                var token = this.lexJSX();
                return this.lastMarker.index = this.scanner.index, this.lastMarker.line = this.scanner.lineNumber, this.lastMarker.column = this.scanner.index - this.scanner.lineStart, this.config.tokens && this.tokens.push(this.convertToken(token)), token;
              }, JSXParser2.prototype.nextJSXText = function() {
                this.startMarker.index = this.scanner.index, this.startMarker.line = this.scanner.lineNumber, this.startMarker.column = this.scanner.index - this.scanner.lineStart;
                for (var start = this.scanner.index, text = ""; !this.scanner.eof(); ) {
                  var ch = this.scanner.source[this.scanner.index];
                  if (ch === "{" || ch === "<")
                    break;
                  ++this.scanner.index, text += ch, character_1.Character.isLineTerminator(ch.charCodeAt(0)) && (++this.scanner.lineNumber, ch === "\r" && this.scanner.source[this.scanner.index] === `
` && ++this.scanner.index, this.scanner.lineStart = this.scanner.index);
                }
                this.lastMarker.index = this.scanner.index, this.lastMarker.line = this.scanner.lineNumber, this.lastMarker.column = this.scanner.index - this.scanner.lineStart;
                var token = {
                  type: 101,
                  value: text,
                  lineNumber: this.scanner.lineNumber,
                  lineStart: this.scanner.lineStart,
                  start,
                  end: this.scanner.index
                };
                return text.length > 0 && this.config.tokens && this.tokens.push(this.convertToken(token)), token;
              }, JSXParser2.prototype.peekJSXToken = function() {
                var state = this.scanner.saveState();
                this.scanner.scanComments();
                var next = this.lexJSX();
                return this.scanner.restoreState(state), next;
              }, JSXParser2.prototype.expectJSX = function(value) {
                var token = this.nextJSXToken();
                (token.type !== 7 || token.value !== value) && this.throwUnexpectedToken(token);
              }, JSXParser2.prototype.matchJSX = function(value) {
                var next = this.peekJSXToken();
                return next.type === 7 && next.value === value;
              }, JSXParser2.prototype.parseJSXIdentifier = function() {
                var node = this.createJSXNode(), token = this.nextJSXToken();
                return token.type !== 100 && this.throwUnexpectedToken(token), this.finalize(node, new JSXNode.JSXIdentifier(token.value));
              }, JSXParser2.prototype.parseJSXElementName = function() {
                var node = this.createJSXNode(), elementName = this.parseJSXIdentifier();
                if (this.matchJSX(":")) {
                  var namespace = elementName;
                  this.expectJSX(":");
                  var name_1 = this.parseJSXIdentifier();
                  elementName = this.finalize(node, new JSXNode.JSXNamespacedName(namespace, name_1));
                } else if (this.matchJSX("."))
                  for (; this.matchJSX("."); ) {
                    var object = elementName;
                    this.expectJSX(".");
                    var property = this.parseJSXIdentifier();
                    elementName = this.finalize(node, new JSXNode.JSXMemberExpression(object, property));
                  }
                return elementName;
              }, JSXParser2.prototype.parseJSXAttributeName = function() {
                var node = this.createJSXNode(), attributeName, identifier = this.parseJSXIdentifier();
                if (this.matchJSX(":")) {
                  var namespace = identifier;
                  this.expectJSX(":");
                  var name_2 = this.parseJSXIdentifier();
                  attributeName = this.finalize(node, new JSXNode.JSXNamespacedName(namespace, name_2));
                } else
                  attributeName = identifier;
                return attributeName;
              }, JSXParser2.prototype.parseJSXStringLiteralAttribute = function() {
                var node = this.createJSXNode(), token = this.nextJSXToken();
                token.type !== 8 && this.throwUnexpectedToken(token);
                var raw = this.getTokenRaw(token);
                return this.finalize(node, new Node.Literal(token.value, raw));
              }, JSXParser2.prototype.parseJSXExpressionAttribute = function() {
                var node = this.createJSXNode();
                this.expectJSX("{"), this.finishJSX(), this.match("}") && this.tolerateError("JSX attributes must only be assigned a non-empty expression");
                var expression = this.parseAssignmentExpression();
                return this.reenterJSX(), this.finalize(node, new JSXNode.JSXExpressionContainer(expression));
              }, JSXParser2.prototype.parseJSXAttributeValue = function() {
                return this.matchJSX("{") ? this.parseJSXExpressionAttribute() : this.matchJSX("<") ? this.parseJSXElement() : this.parseJSXStringLiteralAttribute();
              }, JSXParser2.prototype.parseJSXNameValueAttribute = function() {
                var node = this.createJSXNode(), name = this.parseJSXAttributeName(), value = null;
                return this.matchJSX("=") && (this.expectJSX("="), value = this.parseJSXAttributeValue()), this.finalize(node, new JSXNode.JSXAttribute(name, value));
              }, JSXParser2.prototype.parseJSXSpreadAttribute = function() {
                var node = this.createJSXNode();
                this.expectJSX("{"), this.expectJSX("..."), this.finishJSX();
                var argument = this.parseAssignmentExpression();
                return this.reenterJSX(), this.finalize(node, new JSXNode.JSXSpreadAttribute(argument));
              }, JSXParser2.prototype.parseJSXAttributes = function() {
                for (var attributes = []; !this.matchJSX("/") && !this.matchJSX(">"); ) {
                  var attribute = this.matchJSX("{") ? this.parseJSXSpreadAttribute() : this.parseJSXNameValueAttribute();
                  attributes.push(attribute);
                }
                return attributes;
              }, JSXParser2.prototype.parseJSXOpeningElement = function() {
                var node = this.createJSXNode();
                this.expectJSX("<");
                var name = this.parseJSXElementName(), attributes = this.parseJSXAttributes(), selfClosing = this.matchJSX("/");
                return selfClosing && this.expectJSX("/"), this.expectJSX(">"), this.finalize(node, new JSXNode.JSXOpeningElement(name, selfClosing, attributes));
              }, JSXParser2.prototype.parseJSXBoundaryElement = function() {
                var node = this.createJSXNode();
                if (this.expectJSX("<"), this.matchJSX("/")) {
                  this.expectJSX("/");
                  var name_3 = this.parseJSXElementName();
                  return this.expectJSX(">"), this.finalize(node, new JSXNode.JSXClosingElement(name_3));
                }
                var name = this.parseJSXElementName(), attributes = this.parseJSXAttributes(), selfClosing = this.matchJSX("/");
                return selfClosing && this.expectJSX("/"), this.expectJSX(">"), this.finalize(node, new JSXNode.JSXOpeningElement(name, selfClosing, attributes));
              }, JSXParser2.prototype.parseJSXEmptyExpression = function() {
                var node = this.createJSXChildNode();
                return this.collectComments(), this.lastMarker.index = this.scanner.index, this.lastMarker.line = this.scanner.lineNumber, this.lastMarker.column = this.scanner.index - this.scanner.lineStart, this.finalize(node, new JSXNode.JSXEmptyExpression());
              }, JSXParser2.prototype.parseJSXExpressionContainer = function() {
                var node = this.createJSXNode();
                this.expectJSX("{");
                var expression;
                return this.matchJSX("}") ? (expression = this.parseJSXEmptyExpression(), this.expectJSX("}")) : (this.finishJSX(), expression = this.parseAssignmentExpression(), this.reenterJSX()), this.finalize(node, new JSXNode.JSXExpressionContainer(expression));
              }, JSXParser2.prototype.parseJSXChildren = function() {
                for (var children = []; !this.scanner.eof(); ) {
                  var node = this.createJSXChildNode(), token = this.nextJSXText();
                  if (token.start < token.end) {
                    var raw = this.getTokenRaw(token), child = this.finalize(node, new JSXNode.JSXText(token.value, raw));
                    children.push(child);
                  }
                  if (this.scanner.source[this.scanner.index] === "{") {
                    var container = this.parseJSXExpressionContainer();
                    children.push(container);
                  } else
                    break;
                }
                return children;
              }, JSXParser2.prototype.parseComplexJSXElement = function(el) {
                for (var stack = []; !this.scanner.eof(); ) {
                  el.children = el.children.concat(this.parseJSXChildren());
                  var node = this.createJSXChildNode(), element = this.parseJSXBoundaryElement();
                  if (element.type === jsx_syntax_1.JSXSyntax.JSXOpeningElement) {
                    var opening = element;
                    if (opening.selfClosing) {
                      var child = this.finalize(node, new JSXNode.JSXElement(opening, [], null));
                      el.children.push(child);
                    } else
                      stack.push(el), el = { node, opening, closing: null, children: [] };
                  }
                  if (element.type === jsx_syntax_1.JSXSyntax.JSXClosingElement) {
                    el.closing = element;
                    var open_1 = getQualifiedElementName(el.opening.name), close_1 = getQualifiedElementName(el.closing.name);
                    if (open_1 !== close_1 && this.tolerateError("Expected corresponding JSX closing tag for %0", open_1), stack.length > 0) {
                      var child = this.finalize(el.node, new JSXNode.JSXElement(el.opening, el.children, el.closing));
                      el = stack[stack.length - 1], el.children.push(child), stack.pop();
                    } else
                      break;
                  }
                }
                return el;
              }, JSXParser2.prototype.parseJSXElement = function() {
                var node = this.createJSXNode(), opening = this.parseJSXOpeningElement(), children = [], closing = null;
                if (!opening.selfClosing) {
                  var el = this.parseComplexJSXElement({ node, opening, closing, children });
                  children = el.children, closing = el.closing;
                }
                return this.finalize(node, new JSXNode.JSXElement(opening, children, closing));
              }, JSXParser2.prototype.parseJSXRoot = function() {
                this.config.tokens && this.tokens.pop(), this.startJSX();
                var element = this.parseJSXElement();
                return this.finishJSX(), element;
              }, JSXParser2.prototype.isStartOfExpression = function() {
                return _super.prototype.isStartOfExpression.call(this) || this.match("<");
              }, JSXParser2;
            })(parser_1.Parser);
            exports2.JSXParser = JSXParser;
          },
          /* 4 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var Regex = {
              // Unicode v8.0.0 NonAsciiIdentifierStart:
              NonAsciiIdentifierStart: /[\xAA\xB5\xBA\xC0-\xD6\xD8-\xF6\xF8-\u02C1\u02C6-\u02D1\u02E0-\u02E4\u02EC\u02EE\u0370-\u0374\u0376\u0377\u037A-\u037D\u037F\u0386\u0388-\u038A\u038C\u038E-\u03A1\u03A3-\u03F5\u03F7-\u0481\u048A-\u052F\u0531-\u0556\u0559\u0561-\u0587\u05D0-\u05EA\u05F0-\u05F2\u0620-\u064A\u066E\u066F\u0671-\u06D3\u06D5\u06E5\u06E6\u06EE\u06EF\u06FA-\u06FC\u06FF\u0710\u0712-\u072F\u074D-\u07A5\u07B1\u07CA-\u07EA\u07F4\u07F5\u07FA\u0800-\u0815\u081A\u0824\u0828\u0840-\u0858\u08A0-\u08B4\u0904-\u0939\u093D\u0950\u0958-\u0961\u0971-\u0980\u0985-\u098C\u098F\u0990\u0993-\u09A8\u09AA-\u09B0\u09B2\u09B6-\u09B9\u09BD\u09CE\u09DC\u09DD\u09DF-\u09E1\u09F0\u09F1\u0A05-\u0A0A\u0A0F\u0A10\u0A13-\u0A28\u0A2A-\u0A30\u0A32\u0A33\u0A35\u0A36\u0A38\u0A39\u0A59-\u0A5C\u0A5E\u0A72-\u0A74\u0A85-\u0A8D\u0A8F-\u0A91\u0A93-\u0AA8\u0AAA-\u0AB0\u0AB2\u0AB3\u0AB5-\u0AB9\u0ABD\u0AD0\u0AE0\u0AE1\u0AF9\u0B05-\u0B0C\u0B0F\u0B10\u0B13-\u0B28\u0B2A-\u0B30\u0B32\u0B33\u0B35-\u0B39\u0B3D\u0B5C\u0B5D\u0B5F-\u0B61\u0B71\u0B83\u0B85-\u0B8A\u0B8E-\u0B90\u0B92-\u0B95\u0B99\u0B9A\u0B9C\u0B9E\u0B9F\u0BA3\u0BA4\u0BA8-\u0BAA\u0BAE-\u0BB9\u0BD0\u0C05-\u0C0C\u0C0E-\u0C10\u0C12-\u0C28\u0C2A-\u0C39\u0C3D\u0C58-\u0C5A\u0C60\u0C61\u0C85-\u0C8C\u0C8E-\u0C90\u0C92-\u0CA8\u0CAA-\u0CB3\u0CB5-\u0CB9\u0CBD\u0CDE\u0CE0\u0CE1\u0CF1\u0CF2\u0D05-\u0D0C\u0D0E-\u0D10\u0D12-\u0D3A\u0D3D\u0D4E\u0D5F-\u0D61\u0D7A-\u0D7F\u0D85-\u0D96\u0D9A-\u0DB1\u0DB3-\u0DBB\u0DBD\u0DC0-\u0DC6\u0E01-\u0E30\u0E32\u0E33\u0E40-\u0E46\u0E81\u0E82\u0E84\u0E87\u0E88\u0E8A\u0E8D\u0E94-\u0E97\u0E99-\u0E9F\u0EA1-\u0EA3\u0EA5\u0EA7\u0EAA\u0EAB\u0EAD-\u0EB0\u0EB2\u0EB3\u0EBD\u0EC0-\u0EC4\u0EC6\u0EDC-\u0EDF\u0F00\u0F40-\u0F47\u0F49-\u0F6C\u0F88-\u0F8C\u1000-\u102A\u103F\u1050-\u1055\u105A-\u105D\u1061\u1065\u1066\u106E-\u1070\u1075-\u1081\u108E\u10A0-\u10C5\u10C7\u10CD\u10D0-\u10FA\u10FC-\u1248\u124A-\u124D\u1250-\u1256\u1258\u125A-\u125D\u1260-\u1288\u128A-\u128D\u1290-\u12B0\u12B2-\u12B5\u12B8-\u12BE\u12C0\u12C2-\u12C5\u12C8-\u12D6\u12D8-\u1310\u1312-\u1315\u1318-\u135A\u1380-\u138F\u13A0-\u13F5\u13F8-\u13FD\u1401-\u166C\u166F-\u167F\u1681-\u169A\u16A0-\u16EA\u16EE-\u16F8\u1700-\u170C\u170E-\u1711\u1720-\u1731\u1740-\u1751\u1760-\u176C\u176E-\u1770\u1780-\u17B3\u17D7\u17DC\u1820-\u1877\u1880-\u18A8\u18AA\u18B0-\u18F5\u1900-\u191E\u1950-\u196D\u1970-\u1974\u1980-\u19AB\u19B0-\u19C9\u1A00-\u1A16\u1A20-\u1A54\u1AA7\u1B05-\u1B33\u1B45-\u1B4B\u1B83-\u1BA0\u1BAE\u1BAF\u1BBA-\u1BE5\u1C00-\u1C23\u1C4D-\u1C4F\u1C5A-\u1C7D\u1CE9-\u1CEC\u1CEE-\u1CF1\u1CF5\u1CF6\u1D00-\u1DBF\u1E00-\u1F15\u1F18-\u1F1D\u1F20-\u1F45\u1F48-\u1F4D\u1F50-\u1F57\u1F59\u1F5B\u1F5D\u1F5F-\u1F7D\u1F80-\u1FB4\u1FB6-\u1FBC\u1FBE\u1FC2-\u1FC4\u1FC6-\u1FCC\u1FD0-\u1FD3\u1FD6-\u1FDB\u1FE0-\u1FEC\u1FF2-\u1FF4\u1FF6-\u1FFC\u2071\u207F\u2090-\u209C\u2102\u2107\u210A-\u2113\u2115\u2118-\u211D\u2124\u2126\u2128\u212A-\u2139\u213C-\u213F\u2145-\u2149\u214E\u2160-\u2188\u2C00-\u2C2E\u2C30-\u2C5E\u2C60-\u2CE4\u2CEB-\u2CEE\u2CF2\u2CF3\u2D00-\u2D25\u2D27\u2D2D\u2D30-\u2D67\u2D6F\u2D80-\u2D96\u2DA0-\u2DA6\u2DA8-\u2DAE\u2DB0-\u2DB6\u2DB8-\u2DBE\u2DC0-\u2DC6\u2DC8-\u2DCE\u2DD0-\u2DD6\u2DD8-\u2DDE\u3005-\u3007\u3021-\u3029\u3031-\u3035\u3038-\u303C\u3041-\u3096\u309B-\u309F\u30A1-\u30FA\u30FC-\u30FF\u3105-\u312D\u3131-\u318E\u31A0-\u31BA\u31F0-\u31FF\u3400-\u4DB5\u4E00-\u9FD5\uA000-\uA48C\uA4D0-\uA4FD\uA500-\uA60C\uA610-\uA61F\uA62A\uA62B\uA640-\uA66E\uA67F-\uA69D\uA6A0-\uA6EF\uA717-\uA71F\uA722-\uA788\uA78B-\uA7AD\uA7B0-\uA7B7\uA7F7-\uA801\uA803-\uA805\uA807-\uA80A\uA80C-\uA822\uA840-\uA873\uA882-\uA8B3\uA8F2-\uA8F7\uA8FB\uA8FD\uA90A-\uA925\uA930-\uA946\uA960-\uA97C\uA984-\uA9B2\uA9CF\uA9E0-\uA9E4\uA9E6-\uA9EF\uA9FA-\uA9FE\uAA00-\uAA28\uAA40-\uAA42\uAA44-\uAA4B\uAA60-\uAA76\uAA7A\uAA7E-\uAAAF\uAAB1\uAAB5\uAAB6\uAAB9-\uAABD\uAAC0\uAAC2\uAADB-\uAADD\uAAE0-\uAAEA\uAAF2-\uAAF4\uAB01-\uAB06\uAB09-\uAB0E\uAB11-\uAB16\uAB20-\uAB26\uAB28-\uAB2E\uAB30-\uAB5A\uAB5C-\uAB65\uAB70-\uABE2\uAC00-\uD7A3\uD7B0-\uD7C6\uD7CB-\uD7FB\uF900-\uFA6D\uFA70-\uFAD9\uFB00-\uFB06\uFB13-\uFB17\uFB1D\uFB1F-\uFB28\uFB2A-\uFB36\uFB38-\uFB3C\uFB3E\uFB40\uFB41\uFB43\uFB44\uFB46-\uFBB1\uFBD3-\uFD3D\uFD50-\uFD8F\uFD92-\uFDC7\uFDF0-\uFDFB\uFE70-\uFE74\uFE76-\uFEFC\uFF21-\uFF3A\uFF41-\uFF5A\uFF66-\uFFBE\uFFC2-\uFFC7\uFFCA-\uFFCF\uFFD2-\uFFD7\uFFDA-\uFFDC]|\uD800[\uDC00-\uDC0B\uDC0D-\uDC26\uDC28-\uDC3A\uDC3C\uDC3D\uDC3F-\uDC4D\uDC50-\uDC5D\uDC80-\uDCFA\uDD40-\uDD74\uDE80-\uDE9C\uDEA0-\uDED0\uDF00-\uDF1F\uDF30-\uDF4A\uDF50-\uDF75\uDF80-\uDF9D\uDFA0-\uDFC3\uDFC8-\uDFCF\uDFD1-\uDFD5]|\uD801[\uDC00-\uDC9D\uDD00-\uDD27\uDD30-\uDD63\uDE00-\uDF36\uDF40-\uDF55\uDF60-\uDF67]|\uD802[\uDC00-\uDC05\uDC08\uDC0A-\uDC35\uDC37\uDC38\uDC3C\uDC3F-\uDC55\uDC60-\uDC76\uDC80-\uDC9E\uDCE0-\uDCF2\uDCF4\uDCF5\uDD00-\uDD15\uDD20-\uDD39\uDD80-\uDDB7\uDDBE\uDDBF\uDE00\uDE10-\uDE13\uDE15-\uDE17\uDE19-\uDE33\uDE60-\uDE7C\uDE80-\uDE9C\uDEC0-\uDEC7\uDEC9-\uDEE4\uDF00-\uDF35\uDF40-\uDF55\uDF60-\uDF72\uDF80-\uDF91]|\uD803[\uDC00-\uDC48\uDC80-\uDCB2\uDCC0-\uDCF2]|\uD804[\uDC03-\uDC37\uDC83-\uDCAF\uDCD0-\uDCE8\uDD03-\uDD26\uDD50-\uDD72\uDD76\uDD83-\uDDB2\uDDC1-\uDDC4\uDDDA\uDDDC\uDE00-\uDE11\uDE13-\uDE2B\uDE80-\uDE86\uDE88\uDE8A-\uDE8D\uDE8F-\uDE9D\uDE9F-\uDEA8\uDEB0-\uDEDE\uDF05-\uDF0C\uDF0F\uDF10\uDF13-\uDF28\uDF2A-\uDF30\uDF32\uDF33\uDF35-\uDF39\uDF3D\uDF50\uDF5D-\uDF61]|\uD805[\uDC80-\uDCAF\uDCC4\uDCC5\uDCC7\uDD80-\uDDAE\uDDD8-\uDDDB\uDE00-\uDE2F\uDE44\uDE80-\uDEAA\uDF00-\uDF19]|\uD806[\uDCA0-\uDCDF\uDCFF\uDEC0-\uDEF8]|\uD808[\uDC00-\uDF99]|\uD809[\uDC00-\uDC6E\uDC80-\uDD43]|[\uD80C\uD840-\uD868\uD86A-\uD86C\uD86F-\uD872][\uDC00-\uDFFF]|\uD80D[\uDC00-\uDC2E]|\uD811[\uDC00-\uDE46]|\uD81A[\uDC00-\uDE38\uDE40-\uDE5E\uDED0-\uDEED\uDF00-\uDF2F\uDF40-\uDF43\uDF63-\uDF77\uDF7D-\uDF8F]|\uD81B[\uDF00-\uDF44\uDF50\uDF93-\uDF9F]|\uD82C[\uDC00\uDC01]|\uD82F[\uDC00-\uDC6A\uDC70-\uDC7C\uDC80-\uDC88\uDC90-\uDC99]|\uD835[\uDC00-\uDC54\uDC56-\uDC9C\uDC9E\uDC9F\uDCA2\uDCA5\uDCA6\uDCA9-\uDCAC\uDCAE-\uDCB9\uDCBB\uDCBD-\uDCC3\uDCC5-\uDD05\uDD07-\uDD0A\uDD0D-\uDD14\uDD16-\uDD1C\uDD1E-\uDD39\uDD3B-\uDD3E\uDD40-\uDD44\uDD46\uDD4A-\uDD50\uDD52-\uDEA5\uDEA8-\uDEC0\uDEC2-\uDEDA\uDEDC-\uDEFA\uDEFC-\uDF14\uDF16-\uDF34\uDF36-\uDF4E\uDF50-\uDF6E\uDF70-\uDF88\uDF8A-\uDFA8\uDFAA-\uDFC2\uDFC4-\uDFCB]|\uD83A[\uDC00-\uDCC4]|\uD83B[\uDE00-\uDE03\uDE05-\uDE1F\uDE21\uDE22\uDE24\uDE27\uDE29-\uDE32\uDE34-\uDE37\uDE39\uDE3B\uDE42\uDE47\uDE49\uDE4B\uDE4D-\uDE4F\uDE51\uDE52\uDE54\uDE57\uDE59\uDE5B\uDE5D\uDE5F\uDE61\uDE62\uDE64\uDE67-\uDE6A\uDE6C-\uDE72\uDE74-\uDE77\uDE79-\uDE7C\uDE7E\uDE80-\uDE89\uDE8B-\uDE9B\uDEA1-\uDEA3\uDEA5-\uDEA9\uDEAB-\uDEBB]|\uD869[\uDC00-\uDED6\uDF00-\uDFFF]|\uD86D[\uDC00-\uDF34\uDF40-\uDFFF]|\uD86E[\uDC00-\uDC1D\uDC20-\uDFFF]|\uD873[\uDC00-\uDEA1]|\uD87E[\uDC00-\uDE1D]/,
              // Unicode v8.0.0 NonAsciiIdentifierPart:
              NonAsciiIdentifierPart: /[\xAA\xB5\xB7\xBA\xC0-\xD6\xD8-\xF6\xF8-\u02C1\u02C6-\u02D1\u02E0-\u02E4\u02EC\u02EE\u0300-\u0374\u0376\u0377\u037A-\u037D\u037F\u0386-\u038A\u038C\u038E-\u03A1\u03A3-\u03F5\u03F7-\u0481\u0483-\u0487\u048A-\u052F\u0531-\u0556\u0559\u0561-\u0587\u0591-\u05BD\u05BF\u05C1\u05C2\u05C4\u05C5\u05C7\u05D0-\u05EA\u05F0-\u05F2\u0610-\u061A\u0620-\u0669\u066E-\u06D3\u06D5-\u06DC\u06DF-\u06E8\u06EA-\u06FC\u06FF\u0710-\u074A\u074D-\u07B1\u07C0-\u07F5\u07FA\u0800-\u082D\u0840-\u085B\u08A0-\u08B4\u08E3-\u0963\u0966-\u096F\u0971-\u0983\u0985-\u098C\u098F\u0990\u0993-\u09A8\u09AA-\u09B0\u09B2\u09B6-\u09B9\u09BC-\u09C4\u09C7\u09C8\u09CB-\u09CE\u09D7\u09DC\u09DD\u09DF-\u09E3\u09E6-\u09F1\u0A01-\u0A03\u0A05-\u0A0A\u0A0F\u0A10\u0A13-\u0A28\u0A2A-\u0A30\u0A32\u0A33\u0A35\u0A36\u0A38\u0A39\u0A3C\u0A3E-\u0A42\u0A47\u0A48\u0A4B-\u0A4D\u0A51\u0A59-\u0A5C\u0A5E\u0A66-\u0A75\u0A81-\u0A83\u0A85-\u0A8D\u0A8F-\u0A91\u0A93-\u0AA8\u0AAA-\u0AB0\u0AB2\u0AB3\u0AB5-\u0AB9\u0ABC-\u0AC5\u0AC7-\u0AC9\u0ACB-\u0ACD\u0AD0\u0AE0-\u0AE3\u0AE6-\u0AEF\u0AF9\u0B01-\u0B03\u0B05-\u0B0C\u0B0F\u0B10\u0B13-\u0B28\u0B2A-\u0B30\u0B32\u0B33\u0B35-\u0B39\u0B3C-\u0B44\u0B47\u0B48\u0B4B-\u0B4D\u0B56\u0B57\u0B5C\u0B5D\u0B5F-\u0B63\u0B66-\u0B6F\u0B71\u0B82\u0B83\u0B85-\u0B8A\u0B8E-\u0B90\u0B92-\u0B95\u0B99\u0B9A\u0B9C\u0B9E\u0B9F\u0BA3\u0BA4\u0BA8-\u0BAA\u0BAE-\u0BB9\u0BBE-\u0BC2\u0BC6-\u0BC8\u0BCA-\u0BCD\u0BD0\u0BD7\u0BE6-\u0BEF\u0C00-\u0C03\u0C05-\u0C0C\u0C0E-\u0C10\u0C12-\u0C28\u0C2A-\u0C39\u0C3D-\u0C44\u0C46-\u0C48\u0C4A-\u0C4D\u0C55\u0C56\u0C58-\u0C5A\u0C60-\u0C63\u0C66-\u0C6F\u0C81-\u0C83\u0C85-\u0C8C\u0C8E-\u0C90\u0C92-\u0CA8\u0CAA-\u0CB3\u0CB5-\u0CB9\u0CBC-\u0CC4\u0CC6-\u0CC8\u0CCA-\u0CCD\u0CD5\u0CD6\u0CDE\u0CE0-\u0CE3\u0CE6-\u0CEF\u0CF1\u0CF2\u0D01-\u0D03\u0D05-\u0D0C\u0D0E-\u0D10\u0D12-\u0D3A\u0D3D-\u0D44\u0D46-\u0D48\u0D4A-\u0D4E\u0D57\u0D5F-\u0D63\u0D66-\u0D6F\u0D7A-\u0D7F\u0D82\u0D83\u0D85-\u0D96\u0D9A-\u0DB1\u0DB3-\u0DBB\u0DBD\u0DC0-\u0DC6\u0DCA\u0DCF-\u0DD4\u0DD6\u0DD8-\u0DDF\u0DE6-\u0DEF\u0DF2\u0DF3\u0E01-\u0E3A\u0E40-\u0E4E\u0E50-\u0E59\u0E81\u0E82\u0E84\u0E87\u0E88\u0E8A\u0E8D\u0E94-\u0E97\u0E99-\u0E9F\u0EA1-\u0EA3\u0EA5\u0EA7\u0EAA\u0EAB\u0EAD-\u0EB9\u0EBB-\u0EBD\u0EC0-\u0EC4\u0EC6\u0EC8-\u0ECD\u0ED0-\u0ED9\u0EDC-\u0EDF\u0F00\u0F18\u0F19\u0F20-\u0F29\u0F35\u0F37\u0F39\u0F3E-\u0F47\u0F49-\u0F6C\u0F71-\u0F84\u0F86-\u0F97\u0F99-\u0FBC\u0FC6\u1000-\u1049\u1050-\u109D\u10A0-\u10C5\u10C7\u10CD\u10D0-\u10FA\u10FC-\u1248\u124A-\u124D\u1250-\u1256\u1258\u125A-\u125D\u1260-\u1288\u128A-\u128D\u1290-\u12B0\u12B2-\u12B5\u12B8-\u12BE\u12C0\u12C2-\u12C5\u12C8-\u12D6\u12D8-\u1310\u1312-\u1315\u1318-\u135A\u135D-\u135F\u1369-\u1371\u1380-\u138F\u13A0-\u13F5\u13F8-\u13FD\u1401-\u166C\u166F-\u167F\u1681-\u169A\u16A0-\u16EA\u16EE-\u16F8\u1700-\u170C\u170E-\u1714\u1720-\u1734\u1740-\u1753\u1760-\u176C\u176E-\u1770\u1772\u1773\u1780-\u17D3\u17D7\u17DC\u17DD\u17E0-\u17E9\u180B-\u180D\u1810-\u1819\u1820-\u1877\u1880-\u18AA\u18B0-\u18F5\u1900-\u191E\u1920-\u192B\u1930-\u193B\u1946-\u196D\u1970-\u1974\u1980-\u19AB\u19B0-\u19C9\u19D0-\u19DA\u1A00-\u1A1B\u1A20-\u1A5E\u1A60-\u1A7C\u1A7F-\u1A89\u1A90-\u1A99\u1AA7\u1AB0-\u1ABD\u1B00-\u1B4B\u1B50-\u1B59\u1B6B-\u1B73\u1B80-\u1BF3\u1C00-\u1C37\u1C40-\u1C49\u1C4D-\u1C7D\u1CD0-\u1CD2\u1CD4-\u1CF6\u1CF8\u1CF9\u1D00-\u1DF5\u1DFC-\u1F15\u1F18-\u1F1D\u1F20-\u1F45\u1F48-\u1F4D\u1F50-\u1F57\u1F59\u1F5B\u1F5D\u1F5F-\u1F7D\u1F80-\u1FB4\u1FB6-\u1FBC\u1FBE\u1FC2-\u1FC4\u1FC6-\u1FCC\u1FD0-\u1FD3\u1FD6-\u1FDB\u1FE0-\u1FEC\u1FF2-\u1FF4\u1FF6-\u1FFC\u200C\u200D\u203F\u2040\u2054\u2071\u207F\u2090-\u209C\u20D0-\u20DC\u20E1\u20E5-\u20F0\u2102\u2107\u210A-\u2113\u2115\u2118-\u211D\u2124\u2126\u2128\u212A-\u2139\u213C-\u213F\u2145-\u2149\u214E\u2160-\u2188\u2C00-\u2C2E\u2C30-\u2C5E\u2C60-\u2CE4\u2CEB-\u2CF3\u2D00-\u2D25\u2D27\u2D2D\u2D30-\u2D67\u2D6F\u2D7F-\u2D96\u2DA0-\u2DA6\u2DA8-\u2DAE\u2DB0-\u2DB6\u2DB8-\u2DBE\u2DC0-\u2DC6\u2DC8-\u2DCE\u2DD0-\u2DD6\u2DD8-\u2DDE\u2DE0-\u2DFF\u3005-\u3007\u3021-\u302F\u3031-\u3035\u3038-\u303C\u3041-\u3096\u3099-\u309F\u30A1-\u30FA\u30FC-\u30FF\u3105-\u312D\u3131-\u318E\u31A0-\u31BA\u31F0-\u31FF\u3400-\u4DB5\u4E00-\u9FD5\uA000-\uA48C\uA4D0-\uA4FD\uA500-\uA60C\uA610-\uA62B\uA640-\uA66F\uA674-\uA67D\uA67F-\uA6F1\uA717-\uA71F\uA722-\uA788\uA78B-\uA7AD\uA7B0-\uA7B7\uA7F7-\uA827\uA840-\uA873\uA880-\uA8C4\uA8D0-\uA8D9\uA8E0-\uA8F7\uA8FB\uA8FD\uA900-\uA92D\uA930-\uA953\uA960-\uA97C\uA980-\uA9C0\uA9CF-\uA9D9\uA9E0-\uA9FE\uAA00-\uAA36\uAA40-\uAA4D\uAA50-\uAA59\uAA60-\uAA76\uAA7A-\uAAC2\uAADB-\uAADD\uAAE0-\uAAEF\uAAF2-\uAAF6\uAB01-\uAB06\uAB09-\uAB0E\uAB11-\uAB16\uAB20-\uAB26\uAB28-\uAB2E\uAB30-\uAB5A\uAB5C-\uAB65\uAB70-\uABEA\uABEC\uABED\uABF0-\uABF9\uAC00-\uD7A3\uD7B0-\uD7C6\uD7CB-\uD7FB\uF900-\uFA6D\uFA70-\uFAD9\uFB00-\uFB06\uFB13-\uFB17\uFB1D-\uFB28\uFB2A-\uFB36\uFB38-\uFB3C\uFB3E\uFB40\uFB41\uFB43\uFB44\uFB46-\uFBB1\uFBD3-\uFD3D\uFD50-\uFD8F\uFD92-\uFDC7\uFDF0-\uFDFB\uFE00-\uFE0F\uFE20-\uFE2F\uFE33\uFE34\uFE4D-\uFE4F\uFE70-\uFE74\uFE76-\uFEFC\uFF10-\uFF19\uFF21-\uFF3A\uFF3F\uFF41-\uFF5A\uFF66-\uFFBE\uFFC2-\uFFC7\uFFCA-\uFFCF\uFFD2-\uFFD7\uFFDA-\uFFDC]|\uD800[\uDC00-\uDC0B\uDC0D-\uDC26\uDC28-\uDC3A\uDC3C\uDC3D\uDC3F-\uDC4D\uDC50-\uDC5D\uDC80-\uDCFA\uDD40-\uDD74\uDDFD\uDE80-\uDE9C\uDEA0-\uDED0\uDEE0\uDF00-\uDF1F\uDF30-\uDF4A\uDF50-\uDF7A\uDF80-\uDF9D\uDFA0-\uDFC3\uDFC8-\uDFCF\uDFD1-\uDFD5]|\uD801[\uDC00-\uDC9D\uDCA0-\uDCA9\uDD00-\uDD27\uDD30-\uDD63\uDE00-\uDF36\uDF40-\uDF55\uDF60-\uDF67]|\uD802[\uDC00-\uDC05\uDC08\uDC0A-\uDC35\uDC37\uDC38\uDC3C\uDC3F-\uDC55\uDC60-\uDC76\uDC80-\uDC9E\uDCE0-\uDCF2\uDCF4\uDCF5\uDD00-\uDD15\uDD20-\uDD39\uDD80-\uDDB7\uDDBE\uDDBF\uDE00-\uDE03\uDE05\uDE06\uDE0C-\uDE13\uDE15-\uDE17\uDE19-\uDE33\uDE38-\uDE3A\uDE3F\uDE60-\uDE7C\uDE80-\uDE9C\uDEC0-\uDEC7\uDEC9-\uDEE6\uDF00-\uDF35\uDF40-\uDF55\uDF60-\uDF72\uDF80-\uDF91]|\uD803[\uDC00-\uDC48\uDC80-\uDCB2\uDCC0-\uDCF2]|\uD804[\uDC00-\uDC46\uDC66-\uDC6F\uDC7F-\uDCBA\uDCD0-\uDCE8\uDCF0-\uDCF9\uDD00-\uDD34\uDD36-\uDD3F\uDD50-\uDD73\uDD76\uDD80-\uDDC4\uDDCA-\uDDCC\uDDD0-\uDDDA\uDDDC\uDE00-\uDE11\uDE13-\uDE37\uDE80-\uDE86\uDE88\uDE8A-\uDE8D\uDE8F-\uDE9D\uDE9F-\uDEA8\uDEB0-\uDEEA\uDEF0-\uDEF9\uDF00-\uDF03\uDF05-\uDF0C\uDF0F\uDF10\uDF13-\uDF28\uDF2A-\uDF30\uDF32\uDF33\uDF35-\uDF39\uDF3C-\uDF44\uDF47\uDF48\uDF4B-\uDF4D\uDF50\uDF57\uDF5D-\uDF63\uDF66-\uDF6C\uDF70-\uDF74]|\uD805[\uDC80-\uDCC5\uDCC7\uDCD0-\uDCD9\uDD80-\uDDB5\uDDB8-\uDDC0\uDDD8-\uDDDD\uDE00-\uDE40\uDE44\uDE50-\uDE59\uDE80-\uDEB7\uDEC0-\uDEC9\uDF00-\uDF19\uDF1D-\uDF2B\uDF30-\uDF39]|\uD806[\uDCA0-\uDCE9\uDCFF\uDEC0-\uDEF8]|\uD808[\uDC00-\uDF99]|\uD809[\uDC00-\uDC6E\uDC80-\uDD43]|[\uD80C\uD840-\uD868\uD86A-\uD86C\uD86F-\uD872][\uDC00-\uDFFF]|\uD80D[\uDC00-\uDC2E]|\uD811[\uDC00-\uDE46]|\uD81A[\uDC00-\uDE38\uDE40-\uDE5E\uDE60-\uDE69\uDED0-\uDEED\uDEF0-\uDEF4\uDF00-\uDF36\uDF40-\uDF43\uDF50-\uDF59\uDF63-\uDF77\uDF7D-\uDF8F]|\uD81B[\uDF00-\uDF44\uDF50-\uDF7E\uDF8F-\uDF9F]|\uD82C[\uDC00\uDC01]|\uD82F[\uDC00-\uDC6A\uDC70-\uDC7C\uDC80-\uDC88\uDC90-\uDC99\uDC9D\uDC9E]|\uD834[\uDD65-\uDD69\uDD6D-\uDD72\uDD7B-\uDD82\uDD85-\uDD8B\uDDAA-\uDDAD\uDE42-\uDE44]|\uD835[\uDC00-\uDC54\uDC56-\uDC9C\uDC9E\uDC9F\uDCA2\uDCA5\uDCA6\uDCA9-\uDCAC\uDCAE-\uDCB9\uDCBB\uDCBD-\uDCC3\uDCC5-\uDD05\uDD07-\uDD0A\uDD0D-\uDD14\uDD16-\uDD1C\uDD1E-\uDD39\uDD3B-\uDD3E\uDD40-\uDD44\uDD46\uDD4A-\uDD50\uDD52-\uDEA5\uDEA8-\uDEC0\uDEC2-\uDEDA\uDEDC-\uDEFA\uDEFC-\uDF14\uDF16-\uDF34\uDF36-\uDF4E\uDF50-\uDF6E\uDF70-\uDF88\uDF8A-\uDFA8\uDFAA-\uDFC2\uDFC4-\uDFCB\uDFCE-\uDFFF]|\uD836[\uDE00-\uDE36\uDE3B-\uDE6C\uDE75\uDE84\uDE9B-\uDE9F\uDEA1-\uDEAF]|\uD83A[\uDC00-\uDCC4\uDCD0-\uDCD6]|\uD83B[\uDE00-\uDE03\uDE05-\uDE1F\uDE21\uDE22\uDE24\uDE27\uDE29-\uDE32\uDE34-\uDE37\uDE39\uDE3B\uDE42\uDE47\uDE49\uDE4B\uDE4D-\uDE4F\uDE51\uDE52\uDE54\uDE57\uDE59\uDE5B\uDE5D\uDE5F\uDE61\uDE62\uDE64\uDE67-\uDE6A\uDE6C-\uDE72\uDE74-\uDE77\uDE79-\uDE7C\uDE7E\uDE80-\uDE89\uDE8B-\uDE9B\uDEA1-\uDEA3\uDEA5-\uDEA9\uDEAB-\uDEBB]|\uD869[\uDC00-\uDED6\uDF00-\uDFFF]|\uD86D[\uDC00-\uDF34\uDF40-\uDFFF]|\uD86E[\uDC00-\uDC1D\uDC20-\uDFFF]|\uD873[\uDC00-\uDEA1]|\uD87E[\uDC00-\uDE1D]|\uDB40[\uDD00-\uDDEF]/
            };
            exports2.Character = {
              /* tslint:disable:no-bitwise */
              fromCodePoint: function(cp) {
                return cp < 65536 ? String.fromCharCode(cp) : String.fromCharCode(55296 + (cp - 65536 >> 10)) + String.fromCharCode(56320 + (cp - 65536 & 1023));
              },
              // https://tc39.github.io/ecma262/#sec-white-space
              isWhiteSpace: function(cp) {
                return cp === 32 || cp === 9 || cp === 11 || cp === 12 || cp === 160 || cp >= 5760 && [5760, 8192, 8193, 8194, 8195, 8196, 8197, 8198, 8199, 8200, 8201, 8202, 8239, 8287, 12288, 65279].indexOf(cp) >= 0;
              },
              // https://tc39.github.io/ecma262/#sec-line-terminators
              isLineTerminator: function(cp) {
                return cp === 10 || cp === 13 || cp === 8232 || cp === 8233;
              },
              // https://tc39.github.io/ecma262/#sec-names-and-keywords
              isIdentifierStart: function(cp) {
                return cp === 36 || cp === 95 || cp >= 65 && cp <= 90 || cp >= 97 && cp <= 122 || cp === 92 || cp >= 128 && Regex.NonAsciiIdentifierStart.test(exports2.Character.fromCodePoint(cp));
              },
              isIdentifierPart: function(cp) {
                return cp === 36 || cp === 95 || cp >= 65 && cp <= 90 || cp >= 97 && cp <= 122 || cp >= 48 && cp <= 57 || cp === 92 || cp >= 128 && Regex.NonAsciiIdentifierPart.test(exports2.Character.fromCodePoint(cp));
              },
              // https://tc39.github.io/ecma262/#sec-literals-numeric-literals
              isDecimalDigit: function(cp) {
                return cp >= 48 && cp <= 57;
              },
              isHexDigit: function(cp) {
                return cp >= 48 && cp <= 57 || cp >= 65 && cp <= 70 || cp >= 97 && cp <= 102;
              },
              isOctalDigit: function(cp) {
                return cp >= 48 && cp <= 55;
              }
            };
          },
          /* 5 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var jsx_syntax_1 = __webpack_require__(6), JSXClosingElement = /* @__PURE__ */ (function() {
              function JSXClosingElement2(name) {
                this.type = jsx_syntax_1.JSXSyntax.JSXClosingElement, this.name = name;
              }
              return JSXClosingElement2;
            })();
            exports2.JSXClosingElement = JSXClosingElement;
            var JSXElement = /* @__PURE__ */ (function() {
              function JSXElement2(openingElement, children, closingElement) {
                this.type = jsx_syntax_1.JSXSyntax.JSXElement, this.openingElement = openingElement, this.children = children, this.closingElement = closingElement;
              }
              return JSXElement2;
            })();
            exports2.JSXElement = JSXElement;
            var JSXEmptyExpression = /* @__PURE__ */ (function() {
              function JSXEmptyExpression2() {
                this.type = jsx_syntax_1.JSXSyntax.JSXEmptyExpression;
              }
              return JSXEmptyExpression2;
            })();
            exports2.JSXEmptyExpression = JSXEmptyExpression;
            var JSXExpressionContainer = /* @__PURE__ */ (function() {
              function JSXExpressionContainer2(expression) {
                this.type = jsx_syntax_1.JSXSyntax.JSXExpressionContainer, this.expression = expression;
              }
              return JSXExpressionContainer2;
            })();
            exports2.JSXExpressionContainer = JSXExpressionContainer;
            var JSXIdentifier = /* @__PURE__ */ (function() {
              function JSXIdentifier2(name) {
                this.type = jsx_syntax_1.JSXSyntax.JSXIdentifier, this.name = name;
              }
              return JSXIdentifier2;
            })();
            exports2.JSXIdentifier = JSXIdentifier;
            var JSXMemberExpression = /* @__PURE__ */ (function() {
              function JSXMemberExpression2(object, property) {
                this.type = jsx_syntax_1.JSXSyntax.JSXMemberExpression, this.object = object, this.property = property;
              }
              return JSXMemberExpression2;
            })();
            exports2.JSXMemberExpression = JSXMemberExpression;
            var JSXAttribute = /* @__PURE__ */ (function() {
              function JSXAttribute2(name, value) {
                this.type = jsx_syntax_1.JSXSyntax.JSXAttribute, this.name = name, this.value = value;
              }
              return JSXAttribute2;
            })();
            exports2.JSXAttribute = JSXAttribute;
            var JSXNamespacedName = /* @__PURE__ */ (function() {
              function JSXNamespacedName2(namespace, name) {
                this.type = jsx_syntax_1.JSXSyntax.JSXNamespacedName, this.namespace = namespace, this.name = name;
              }
              return JSXNamespacedName2;
            })();
            exports2.JSXNamespacedName = JSXNamespacedName;
            var JSXOpeningElement = /* @__PURE__ */ (function() {
              function JSXOpeningElement2(name, selfClosing, attributes) {
                this.type = jsx_syntax_1.JSXSyntax.JSXOpeningElement, this.name = name, this.selfClosing = selfClosing, this.attributes = attributes;
              }
              return JSXOpeningElement2;
            })();
            exports2.JSXOpeningElement = JSXOpeningElement;
            var JSXSpreadAttribute = /* @__PURE__ */ (function() {
              function JSXSpreadAttribute2(argument) {
                this.type = jsx_syntax_1.JSXSyntax.JSXSpreadAttribute, this.argument = argument;
              }
              return JSXSpreadAttribute2;
            })();
            exports2.JSXSpreadAttribute = JSXSpreadAttribute;
            var JSXText = /* @__PURE__ */ (function() {
              function JSXText2(value, raw) {
                this.type = jsx_syntax_1.JSXSyntax.JSXText, this.value = value, this.raw = raw;
              }
              return JSXText2;
            })();
            exports2.JSXText = JSXText;
          },
          /* 6 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 }), exports2.JSXSyntax = {
              JSXAttribute: "JSXAttribute",
              JSXClosingElement: "JSXClosingElement",
              JSXElement: "JSXElement",
              JSXEmptyExpression: "JSXEmptyExpression",
              JSXExpressionContainer: "JSXExpressionContainer",
              JSXIdentifier: "JSXIdentifier",
              JSXMemberExpression: "JSXMemberExpression",
              JSXNamespacedName: "JSXNamespacedName",
              JSXOpeningElement: "JSXOpeningElement",
              JSXSpreadAttribute: "JSXSpreadAttribute",
              JSXText: "JSXText"
            };
          },
          /* 7 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var syntax_1 = __webpack_require__(2), ArrayExpression = /* @__PURE__ */ (function() {
              function ArrayExpression2(elements) {
                this.type = syntax_1.Syntax.ArrayExpression, this.elements = elements;
              }
              return ArrayExpression2;
            })();
            exports2.ArrayExpression = ArrayExpression;
            var ArrayPattern = /* @__PURE__ */ (function() {
              function ArrayPattern2(elements) {
                this.type = syntax_1.Syntax.ArrayPattern, this.elements = elements;
              }
              return ArrayPattern2;
            })();
            exports2.ArrayPattern = ArrayPattern;
            var ArrowFunctionExpression = /* @__PURE__ */ (function() {
              function ArrowFunctionExpression2(params, body, expression) {
                this.type = syntax_1.Syntax.ArrowFunctionExpression, this.id = null, this.params = params, this.body = body, this.generator = !1, this.expression = expression, this.async = !1;
              }
              return ArrowFunctionExpression2;
            })();
            exports2.ArrowFunctionExpression = ArrowFunctionExpression;
            var AssignmentExpression = /* @__PURE__ */ (function() {
              function AssignmentExpression2(operator, left, right) {
                this.type = syntax_1.Syntax.AssignmentExpression, this.operator = operator, this.left = left, this.right = right;
              }
              return AssignmentExpression2;
            })();
            exports2.AssignmentExpression = AssignmentExpression;
            var AssignmentPattern = /* @__PURE__ */ (function() {
              function AssignmentPattern2(left, right) {
                this.type = syntax_1.Syntax.AssignmentPattern, this.left = left, this.right = right;
              }
              return AssignmentPattern2;
            })();
            exports2.AssignmentPattern = AssignmentPattern;
            var AsyncArrowFunctionExpression = /* @__PURE__ */ (function() {
              function AsyncArrowFunctionExpression2(params, body, expression) {
                this.type = syntax_1.Syntax.ArrowFunctionExpression, this.id = null, this.params = params, this.body = body, this.generator = !1, this.expression = expression, this.async = !0;
              }
              return AsyncArrowFunctionExpression2;
            })();
            exports2.AsyncArrowFunctionExpression = AsyncArrowFunctionExpression;
            var AsyncFunctionDeclaration = /* @__PURE__ */ (function() {
              function AsyncFunctionDeclaration2(id, params, body) {
                this.type = syntax_1.Syntax.FunctionDeclaration, this.id = id, this.params = params, this.body = body, this.generator = !1, this.expression = !1, this.async = !0;
              }
              return AsyncFunctionDeclaration2;
            })();
            exports2.AsyncFunctionDeclaration = AsyncFunctionDeclaration;
            var AsyncFunctionExpression = /* @__PURE__ */ (function() {
              function AsyncFunctionExpression2(id, params, body) {
                this.type = syntax_1.Syntax.FunctionExpression, this.id = id, this.params = params, this.body = body, this.generator = !1, this.expression = !1, this.async = !0;
              }
              return AsyncFunctionExpression2;
            })();
            exports2.AsyncFunctionExpression = AsyncFunctionExpression;
            var AwaitExpression = /* @__PURE__ */ (function() {
              function AwaitExpression2(argument) {
                this.type = syntax_1.Syntax.AwaitExpression, this.argument = argument;
              }
              return AwaitExpression2;
            })();
            exports2.AwaitExpression = AwaitExpression;
            var BinaryExpression = /* @__PURE__ */ (function() {
              function BinaryExpression2(operator, left, right) {
                var logical = operator === "||" || operator === "&&";
                this.type = logical ? syntax_1.Syntax.LogicalExpression : syntax_1.Syntax.BinaryExpression, this.operator = operator, this.left = left, this.right = right;
              }
              return BinaryExpression2;
            })();
            exports2.BinaryExpression = BinaryExpression;
            var BlockStatement = /* @__PURE__ */ (function() {
              function BlockStatement2(body) {
                this.type = syntax_1.Syntax.BlockStatement, this.body = body;
              }
              return BlockStatement2;
            })();
            exports2.BlockStatement = BlockStatement;
            var BreakStatement = /* @__PURE__ */ (function() {
              function BreakStatement2(label) {
                this.type = syntax_1.Syntax.BreakStatement, this.label = label;
              }
              return BreakStatement2;
            })();
            exports2.BreakStatement = BreakStatement;
            var CallExpression = /* @__PURE__ */ (function() {
              function CallExpression2(callee, args) {
                this.type = syntax_1.Syntax.CallExpression, this.callee = callee, this.arguments = args;
              }
              return CallExpression2;
            })();
            exports2.CallExpression = CallExpression;
            var CatchClause = /* @__PURE__ */ (function() {
              function CatchClause2(param, body) {
                this.type = syntax_1.Syntax.CatchClause, this.param = param, this.body = body;
              }
              return CatchClause2;
            })();
            exports2.CatchClause = CatchClause;
            var ClassBody = /* @__PURE__ */ (function() {
              function ClassBody2(body) {
                this.type = syntax_1.Syntax.ClassBody, this.body = body;
              }
              return ClassBody2;
            })();
            exports2.ClassBody = ClassBody;
            var ClassDeclaration = /* @__PURE__ */ (function() {
              function ClassDeclaration2(id, superClass, body) {
                this.type = syntax_1.Syntax.ClassDeclaration, this.id = id, this.superClass = superClass, this.body = body;
              }
              return ClassDeclaration2;
            })();
            exports2.ClassDeclaration = ClassDeclaration;
            var ClassExpression = /* @__PURE__ */ (function() {
              function ClassExpression2(id, superClass, body) {
                this.type = syntax_1.Syntax.ClassExpression, this.id = id, this.superClass = superClass, this.body = body;
              }
              return ClassExpression2;
            })();
            exports2.ClassExpression = ClassExpression;
            var ComputedMemberExpression = /* @__PURE__ */ (function() {
              function ComputedMemberExpression2(object, property) {
                this.type = syntax_1.Syntax.MemberExpression, this.computed = !0, this.object = object, this.property = property;
              }
              return ComputedMemberExpression2;
            })();
            exports2.ComputedMemberExpression = ComputedMemberExpression;
            var ConditionalExpression = /* @__PURE__ */ (function() {
              function ConditionalExpression2(test, consequent, alternate) {
                this.type = syntax_1.Syntax.ConditionalExpression, this.test = test, this.consequent = consequent, this.alternate = alternate;
              }
              return ConditionalExpression2;
            })();
            exports2.ConditionalExpression = ConditionalExpression;
            var ContinueStatement = /* @__PURE__ */ (function() {
              function ContinueStatement2(label) {
                this.type = syntax_1.Syntax.ContinueStatement, this.label = label;
              }
              return ContinueStatement2;
            })();
            exports2.ContinueStatement = ContinueStatement;
            var DebuggerStatement = /* @__PURE__ */ (function() {
              function DebuggerStatement2() {
                this.type = syntax_1.Syntax.DebuggerStatement;
              }
              return DebuggerStatement2;
            })();
            exports2.DebuggerStatement = DebuggerStatement;
            var Directive = /* @__PURE__ */ (function() {
              function Directive2(expression, directive) {
                this.type = syntax_1.Syntax.ExpressionStatement, this.expression = expression, this.directive = directive;
              }
              return Directive2;
            })();
            exports2.Directive = Directive;
            var DoWhileStatement = /* @__PURE__ */ (function() {
              function DoWhileStatement2(body, test) {
                this.type = syntax_1.Syntax.DoWhileStatement, this.body = body, this.test = test;
              }
              return DoWhileStatement2;
            })();
            exports2.DoWhileStatement = DoWhileStatement;
            var EmptyStatement = /* @__PURE__ */ (function() {
              function EmptyStatement2() {
                this.type = syntax_1.Syntax.EmptyStatement;
              }
              return EmptyStatement2;
            })();
            exports2.EmptyStatement = EmptyStatement;
            var ExportAllDeclaration = /* @__PURE__ */ (function() {
              function ExportAllDeclaration2(source) {
                this.type = syntax_1.Syntax.ExportAllDeclaration, this.source = source;
              }
              return ExportAllDeclaration2;
            })();
            exports2.ExportAllDeclaration = ExportAllDeclaration;
            var ExportDefaultDeclaration = /* @__PURE__ */ (function() {
              function ExportDefaultDeclaration2(declaration) {
                this.type = syntax_1.Syntax.ExportDefaultDeclaration, this.declaration = declaration;
              }
              return ExportDefaultDeclaration2;
            })();
            exports2.ExportDefaultDeclaration = ExportDefaultDeclaration;
            var ExportNamedDeclaration = /* @__PURE__ */ (function() {
              function ExportNamedDeclaration2(declaration, specifiers, source) {
                this.type = syntax_1.Syntax.ExportNamedDeclaration, this.declaration = declaration, this.specifiers = specifiers, this.source = source;
              }
              return ExportNamedDeclaration2;
            })();
            exports2.ExportNamedDeclaration = ExportNamedDeclaration;
            var ExportSpecifier = /* @__PURE__ */ (function() {
              function ExportSpecifier2(local, exported) {
                this.type = syntax_1.Syntax.ExportSpecifier, this.exported = exported, this.local = local;
              }
              return ExportSpecifier2;
            })();
            exports2.ExportSpecifier = ExportSpecifier;
            var ExpressionStatement = /* @__PURE__ */ (function() {
              function ExpressionStatement2(expression) {
                this.type = syntax_1.Syntax.ExpressionStatement, this.expression = expression;
              }
              return ExpressionStatement2;
            })();
            exports2.ExpressionStatement = ExpressionStatement;
            var ForInStatement = /* @__PURE__ */ (function() {
              function ForInStatement2(left, right, body) {
                this.type = syntax_1.Syntax.ForInStatement, this.left = left, this.right = right, this.body = body, this.each = !1;
              }
              return ForInStatement2;
            })();
            exports2.ForInStatement = ForInStatement;
            var ForOfStatement = /* @__PURE__ */ (function() {
              function ForOfStatement2(left, right, body) {
                this.type = syntax_1.Syntax.ForOfStatement, this.left = left, this.right = right, this.body = body;
              }
              return ForOfStatement2;
            })();
            exports2.ForOfStatement = ForOfStatement;
            var ForStatement = /* @__PURE__ */ (function() {
              function ForStatement2(init, test, update, body) {
                this.type = syntax_1.Syntax.ForStatement, this.init = init, this.test = test, this.update = update, this.body = body;
              }
              return ForStatement2;
            })();
            exports2.ForStatement = ForStatement;
            var FunctionDeclaration = /* @__PURE__ */ (function() {
              function FunctionDeclaration2(id, params, body, generator) {
                this.type = syntax_1.Syntax.FunctionDeclaration, this.id = id, this.params = params, this.body = body, this.generator = generator, this.expression = !1, this.async = !1;
              }
              return FunctionDeclaration2;
            })();
            exports2.FunctionDeclaration = FunctionDeclaration;
            var FunctionExpression = /* @__PURE__ */ (function() {
              function FunctionExpression2(id, params, body, generator) {
                this.type = syntax_1.Syntax.FunctionExpression, this.id = id, this.params = params, this.body = body, this.generator = generator, this.expression = !1, this.async = !1;
              }
              return FunctionExpression2;
            })();
            exports2.FunctionExpression = FunctionExpression;
            var Identifier = /* @__PURE__ */ (function() {
              function Identifier2(name) {
                this.type = syntax_1.Syntax.Identifier, this.name = name;
              }
              return Identifier2;
            })();
            exports2.Identifier = Identifier;
            var IfStatement = /* @__PURE__ */ (function() {
              function IfStatement2(test, consequent, alternate) {
                this.type = syntax_1.Syntax.IfStatement, this.test = test, this.consequent = consequent, this.alternate = alternate;
              }
              return IfStatement2;
            })();
            exports2.IfStatement = IfStatement;
            var ImportDeclaration = /* @__PURE__ */ (function() {
              function ImportDeclaration2(specifiers, source) {
                this.type = syntax_1.Syntax.ImportDeclaration, this.specifiers = specifiers, this.source = source;
              }
              return ImportDeclaration2;
            })();
            exports2.ImportDeclaration = ImportDeclaration;
            var ImportDefaultSpecifier = /* @__PURE__ */ (function() {
              function ImportDefaultSpecifier2(local) {
                this.type = syntax_1.Syntax.ImportDefaultSpecifier, this.local = local;
              }
              return ImportDefaultSpecifier2;
            })();
            exports2.ImportDefaultSpecifier = ImportDefaultSpecifier;
            var ImportNamespaceSpecifier = /* @__PURE__ */ (function() {
              function ImportNamespaceSpecifier2(local) {
                this.type = syntax_1.Syntax.ImportNamespaceSpecifier, this.local = local;
              }
              return ImportNamespaceSpecifier2;
            })();
            exports2.ImportNamespaceSpecifier = ImportNamespaceSpecifier;
            var ImportSpecifier = /* @__PURE__ */ (function() {
              function ImportSpecifier2(local, imported) {
                this.type = syntax_1.Syntax.ImportSpecifier, this.local = local, this.imported = imported;
              }
              return ImportSpecifier2;
            })();
            exports2.ImportSpecifier = ImportSpecifier;
            var LabeledStatement = /* @__PURE__ */ (function() {
              function LabeledStatement2(label, body) {
                this.type = syntax_1.Syntax.LabeledStatement, this.label = label, this.body = body;
              }
              return LabeledStatement2;
            })();
            exports2.LabeledStatement = LabeledStatement;
            var Literal = /* @__PURE__ */ (function() {
              function Literal2(value, raw) {
                this.type = syntax_1.Syntax.Literal, this.value = value, this.raw = raw;
              }
              return Literal2;
            })();
            exports2.Literal = Literal;
            var MetaProperty = /* @__PURE__ */ (function() {
              function MetaProperty2(meta, property) {
                this.type = syntax_1.Syntax.MetaProperty, this.meta = meta, this.property = property;
              }
              return MetaProperty2;
            })();
            exports2.MetaProperty = MetaProperty;
            var MethodDefinition = /* @__PURE__ */ (function() {
              function MethodDefinition2(key, computed, value, kind, isStatic) {
                this.type = syntax_1.Syntax.MethodDefinition, this.key = key, this.computed = computed, this.value = value, this.kind = kind, this.static = isStatic;
              }
              return MethodDefinition2;
            })();
            exports2.MethodDefinition = MethodDefinition;
            var Module = /* @__PURE__ */ (function() {
              function Module2(body) {
                this.type = syntax_1.Syntax.Program, this.body = body, this.sourceType = "module";
              }
              return Module2;
            })();
            exports2.Module = Module;
            var NewExpression = /* @__PURE__ */ (function() {
              function NewExpression2(callee, args) {
                this.type = syntax_1.Syntax.NewExpression, this.callee = callee, this.arguments = args;
              }
              return NewExpression2;
            })();
            exports2.NewExpression = NewExpression;
            var ObjectExpression = /* @__PURE__ */ (function() {
              function ObjectExpression2(properties) {
                this.type = syntax_1.Syntax.ObjectExpression, this.properties = properties;
              }
              return ObjectExpression2;
            })();
            exports2.ObjectExpression = ObjectExpression;
            var ObjectPattern = /* @__PURE__ */ (function() {
              function ObjectPattern2(properties) {
                this.type = syntax_1.Syntax.ObjectPattern, this.properties = properties;
              }
              return ObjectPattern2;
            })();
            exports2.ObjectPattern = ObjectPattern;
            var Property = /* @__PURE__ */ (function() {
              function Property2(kind, key, computed, value, method, shorthand) {
                this.type = syntax_1.Syntax.Property, this.key = key, this.computed = computed, this.value = value, this.kind = kind, this.method = method, this.shorthand = shorthand;
              }
              return Property2;
            })();
            exports2.Property = Property;
            var RegexLiteral = /* @__PURE__ */ (function() {
              function RegexLiteral2(value, raw, pattern, flags) {
                this.type = syntax_1.Syntax.Literal, this.value = value, this.raw = raw, this.regex = { pattern, flags };
              }
              return RegexLiteral2;
            })();
            exports2.RegexLiteral = RegexLiteral;
            var RestElement = /* @__PURE__ */ (function() {
              function RestElement2(argument) {
                this.type = syntax_1.Syntax.RestElement, this.argument = argument;
              }
              return RestElement2;
            })();
            exports2.RestElement = RestElement;
            var ReturnStatement = /* @__PURE__ */ (function() {
              function ReturnStatement2(argument) {
                this.type = syntax_1.Syntax.ReturnStatement, this.argument = argument;
              }
              return ReturnStatement2;
            })();
            exports2.ReturnStatement = ReturnStatement;
            var Script = /* @__PURE__ */ (function() {
              function Script2(body) {
                this.type = syntax_1.Syntax.Program, this.body = body, this.sourceType = "script";
              }
              return Script2;
            })();
            exports2.Script = Script;
            var SequenceExpression = /* @__PURE__ */ (function() {
              function SequenceExpression2(expressions) {
                this.type = syntax_1.Syntax.SequenceExpression, this.expressions = expressions;
              }
              return SequenceExpression2;
            })();
            exports2.SequenceExpression = SequenceExpression;
            var SpreadElement = /* @__PURE__ */ (function() {
              function SpreadElement2(argument) {
                this.type = syntax_1.Syntax.SpreadElement, this.argument = argument;
              }
              return SpreadElement2;
            })();
            exports2.SpreadElement = SpreadElement;
            var StaticMemberExpression = /* @__PURE__ */ (function() {
              function StaticMemberExpression2(object, property) {
                this.type = syntax_1.Syntax.MemberExpression, this.computed = !1, this.object = object, this.property = property;
              }
              return StaticMemberExpression2;
            })();
            exports2.StaticMemberExpression = StaticMemberExpression;
            var Super = /* @__PURE__ */ (function() {
              function Super2() {
                this.type = syntax_1.Syntax.Super;
              }
              return Super2;
            })();
            exports2.Super = Super;
            var SwitchCase = /* @__PURE__ */ (function() {
              function SwitchCase2(test, consequent) {
                this.type = syntax_1.Syntax.SwitchCase, this.test = test, this.consequent = consequent;
              }
              return SwitchCase2;
            })();
            exports2.SwitchCase = SwitchCase;
            var SwitchStatement = /* @__PURE__ */ (function() {
              function SwitchStatement2(discriminant, cases) {
                this.type = syntax_1.Syntax.SwitchStatement, this.discriminant = discriminant, this.cases = cases;
              }
              return SwitchStatement2;
            })();
            exports2.SwitchStatement = SwitchStatement;
            var TaggedTemplateExpression = /* @__PURE__ */ (function() {
              function TaggedTemplateExpression2(tag, quasi) {
                this.type = syntax_1.Syntax.TaggedTemplateExpression, this.tag = tag, this.quasi = quasi;
              }
              return TaggedTemplateExpression2;
            })();
            exports2.TaggedTemplateExpression = TaggedTemplateExpression;
            var TemplateElement = /* @__PURE__ */ (function() {
              function TemplateElement2(value, tail) {
                this.type = syntax_1.Syntax.TemplateElement, this.value = value, this.tail = tail;
              }
              return TemplateElement2;
            })();
            exports2.TemplateElement = TemplateElement;
            var TemplateLiteral = /* @__PURE__ */ (function() {
              function TemplateLiteral2(quasis, expressions) {
                this.type = syntax_1.Syntax.TemplateLiteral, this.quasis = quasis, this.expressions = expressions;
              }
              return TemplateLiteral2;
            })();
            exports2.TemplateLiteral = TemplateLiteral;
            var ThisExpression = /* @__PURE__ */ (function() {
              function ThisExpression2() {
                this.type = syntax_1.Syntax.ThisExpression;
              }
              return ThisExpression2;
            })();
            exports2.ThisExpression = ThisExpression;
            var ThrowStatement = /* @__PURE__ */ (function() {
              function ThrowStatement2(argument) {
                this.type = syntax_1.Syntax.ThrowStatement, this.argument = argument;
              }
              return ThrowStatement2;
            })();
            exports2.ThrowStatement = ThrowStatement;
            var TryStatement = /* @__PURE__ */ (function() {
              function TryStatement2(block, handler, finalizer) {
                this.type = syntax_1.Syntax.TryStatement, this.block = block, this.handler = handler, this.finalizer = finalizer;
              }
              return TryStatement2;
            })();
            exports2.TryStatement = TryStatement;
            var UnaryExpression = /* @__PURE__ */ (function() {
              function UnaryExpression2(operator, argument) {
                this.type = syntax_1.Syntax.UnaryExpression, this.operator = operator, this.argument = argument, this.prefix = !0;
              }
              return UnaryExpression2;
            })();
            exports2.UnaryExpression = UnaryExpression;
            var UpdateExpression = /* @__PURE__ */ (function() {
              function UpdateExpression2(operator, argument, prefix) {
                this.type = syntax_1.Syntax.UpdateExpression, this.operator = operator, this.argument = argument, this.prefix = prefix;
              }
              return UpdateExpression2;
            })();
            exports2.UpdateExpression = UpdateExpression;
            var VariableDeclaration = /* @__PURE__ */ (function() {
              function VariableDeclaration2(declarations, kind) {
                this.type = syntax_1.Syntax.VariableDeclaration, this.declarations = declarations, this.kind = kind;
              }
              return VariableDeclaration2;
            })();
            exports2.VariableDeclaration = VariableDeclaration;
            var VariableDeclarator = /* @__PURE__ */ (function() {
              function VariableDeclarator2(id, init) {
                this.type = syntax_1.Syntax.VariableDeclarator, this.id = id, this.init = init;
              }
              return VariableDeclarator2;
            })();
            exports2.VariableDeclarator = VariableDeclarator;
            var WhileStatement = /* @__PURE__ */ (function() {
              function WhileStatement2(test, body) {
                this.type = syntax_1.Syntax.WhileStatement, this.test = test, this.body = body;
              }
              return WhileStatement2;
            })();
            exports2.WhileStatement = WhileStatement;
            var WithStatement = /* @__PURE__ */ (function() {
              function WithStatement2(object, body) {
                this.type = syntax_1.Syntax.WithStatement, this.object = object, this.body = body;
              }
              return WithStatement2;
            })();
            exports2.WithStatement = WithStatement;
            var YieldExpression = /* @__PURE__ */ (function() {
              function YieldExpression2(argument, delegate) {
                this.type = syntax_1.Syntax.YieldExpression, this.argument = argument, this.delegate = delegate;
              }
              return YieldExpression2;
            })();
            exports2.YieldExpression = YieldExpression;
          },
          /* 8 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var assert_1 = __webpack_require__(9), error_handler_1 = __webpack_require__(10), messages_1 = __webpack_require__(11), Node = __webpack_require__(7), scanner_1 = __webpack_require__(12), syntax_1 = __webpack_require__(2), token_1 = __webpack_require__(13), ArrowParameterPlaceHolder = "ArrowParameterPlaceHolder", Parser = (function() {
              function Parser2(code, options, delegate) {
                options === void 0 && (options = {}), this.config = {
                  range: typeof options.range == "boolean" && options.range,
                  loc: typeof options.loc == "boolean" && options.loc,
                  source: null,
                  tokens: typeof options.tokens == "boolean" && options.tokens,
                  comment: typeof options.comment == "boolean" && options.comment,
                  tolerant: typeof options.tolerant == "boolean" && options.tolerant
                }, this.config.loc && options.source && options.source !== null && (this.config.source = String(options.source)), this.delegate = delegate, this.errorHandler = new error_handler_1.ErrorHandler(), this.errorHandler.tolerant = this.config.tolerant, this.scanner = new scanner_1.Scanner(code, this.errorHandler), this.scanner.trackComment = this.config.comment, this.operatorPrecedence = {
                  ")": 0,
                  ";": 0,
                  ",": 0,
                  "=": 0,
                  "]": 0,
                  "||": 1,
                  "&&": 2,
                  "|": 3,
                  "^": 4,
                  "&": 5,
                  "==": 6,
                  "!=": 6,
                  "===": 6,
                  "!==": 6,
                  "<": 7,
                  ">": 7,
                  "<=": 7,
                  ">=": 7,
                  "<<": 8,
                  ">>": 8,
                  ">>>": 8,
                  "+": 9,
                  "-": 9,
                  "*": 11,
                  "/": 11,
                  "%": 11
                }, this.lookahead = {
                  type: 2,
                  value: "",
                  lineNumber: this.scanner.lineNumber,
                  lineStart: 0,
                  start: 0,
                  end: 0
                }, this.hasLineTerminator = !1, this.context = {
                  isModule: !1,
                  await: !1,
                  allowIn: !0,
                  allowStrictDirective: !0,
                  allowYield: !0,
                  firstCoverInitializedNameError: null,
                  isAssignmentTarget: !1,
                  isBindingElement: !1,
                  inFunctionBody: !1,
                  inIteration: !1,
                  inSwitch: !1,
                  labelSet: {},
                  strict: !1
                }, this.tokens = [], this.startMarker = {
                  index: 0,
                  line: this.scanner.lineNumber,
                  column: 0
                }, this.lastMarker = {
                  index: 0,
                  line: this.scanner.lineNumber,
                  column: 0
                }, this.nextToken(), this.lastMarker = {
                  index: this.scanner.index,
                  line: this.scanner.lineNumber,
                  column: this.scanner.index - this.scanner.lineStart
                };
              }
              return Parser2.prototype.throwError = function(messageFormat) {
                for (var values = [], _i = 1; _i < arguments.length; _i++)
                  values[_i - 1] = arguments[_i];
                var args = Array.prototype.slice.call(arguments, 1), msg = messageFormat.replace(/%(\d)/g, function(whole, idx) {
                  return assert_1.assert(idx < args.length, "Message reference must be in range"), args[idx];
                }), index = this.lastMarker.index, line = this.lastMarker.line, column = this.lastMarker.column + 1;
                throw this.errorHandler.createError(index, line, column, msg);
              }, Parser2.prototype.tolerateError = function(messageFormat) {
                for (var values = [], _i = 1; _i < arguments.length; _i++)
                  values[_i - 1] = arguments[_i];
                var args = Array.prototype.slice.call(arguments, 1), msg = messageFormat.replace(/%(\d)/g, function(whole, idx) {
                  return assert_1.assert(idx < args.length, "Message reference must be in range"), args[idx];
                }), index = this.lastMarker.index, line = this.scanner.lineNumber, column = this.lastMarker.column + 1;
                this.errorHandler.tolerateError(index, line, column, msg);
              }, Parser2.prototype.unexpectedTokenError = function(token, message) {
                var msg = message || messages_1.Messages.UnexpectedToken, value;
                if (token ? (message || (msg = token.type === 2 ? messages_1.Messages.UnexpectedEOS : token.type === 3 ? messages_1.Messages.UnexpectedIdentifier : token.type === 6 ? messages_1.Messages.UnexpectedNumber : token.type === 8 ? messages_1.Messages.UnexpectedString : token.type === 10 ? messages_1.Messages.UnexpectedTemplate : messages_1.Messages.UnexpectedToken, token.type === 4 && (this.scanner.isFutureReservedWord(token.value) ? msg = messages_1.Messages.UnexpectedReserved : this.context.strict && this.scanner.isStrictModeReservedWord(token.value) && (msg = messages_1.Messages.StrictReservedWord))), value = token.value) : value = "ILLEGAL", msg = msg.replace("%0", value), token && typeof token.lineNumber == "number") {
                  var index = token.start, line = token.lineNumber, lastMarkerLineStart = this.lastMarker.index - this.lastMarker.column, column = token.start - lastMarkerLineStart + 1;
                  return this.errorHandler.createError(index, line, column, msg);
                } else {
                  var index = this.lastMarker.index, line = this.lastMarker.line, column = this.lastMarker.column + 1;
                  return this.errorHandler.createError(index, line, column, msg);
                }
              }, Parser2.prototype.throwUnexpectedToken = function(token, message) {
                throw this.unexpectedTokenError(token, message);
              }, Parser2.prototype.tolerateUnexpectedToken = function(token, message) {
                this.errorHandler.tolerate(this.unexpectedTokenError(token, message));
              }, Parser2.prototype.collectComments = function() {
                if (!this.config.comment)
                  this.scanner.scanComments();
                else {
                  var comments = this.scanner.scanComments();
                  if (comments.length > 0 && this.delegate)
                    for (var i = 0; i < comments.length; ++i) {
                      var e = comments[i], node = void 0;
                      node = {
                        type: e.multiLine ? "BlockComment" : "LineComment",
                        value: this.scanner.source.slice(e.slice[0], e.slice[1])
                      }, this.config.range && (node.range = e.range), this.config.loc && (node.loc = e.loc);
                      var metadata = {
                        start: {
                          line: e.loc.start.line,
                          column: e.loc.start.column,
                          offset: e.range[0]
                        },
                        end: {
                          line: e.loc.end.line,
                          column: e.loc.end.column,
                          offset: e.range[1]
                        }
                      };
                      this.delegate(node, metadata);
                    }
                }
              }, Parser2.prototype.getTokenRaw = function(token) {
                return this.scanner.source.slice(token.start, token.end);
              }, Parser2.prototype.convertToken = function(token) {
                var t = {
                  type: token_1.TokenName[token.type],
                  value: this.getTokenRaw(token)
                };
                if (this.config.range && (t.range = [token.start, token.end]), this.config.loc && (t.loc = {
                  start: {
                    line: this.startMarker.line,
                    column: this.startMarker.column
                  },
                  end: {
                    line: this.scanner.lineNumber,
                    column: this.scanner.index - this.scanner.lineStart
                  }
                }), token.type === 9) {
                  var pattern = token.pattern, flags = token.flags;
                  t.regex = { pattern, flags };
                }
                return t;
              }, Parser2.prototype.nextToken = function() {
                var token = this.lookahead;
                this.lastMarker.index = this.scanner.index, this.lastMarker.line = this.scanner.lineNumber, this.lastMarker.column = this.scanner.index - this.scanner.lineStart, this.collectComments(), this.scanner.index !== this.startMarker.index && (this.startMarker.index = this.scanner.index, this.startMarker.line = this.scanner.lineNumber, this.startMarker.column = this.scanner.index - this.scanner.lineStart);
                var next = this.scanner.lex();
                return this.hasLineTerminator = token.lineNumber !== next.lineNumber, next && this.context.strict && next.type === 3 && this.scanner.isStrictModeReservedWord(next.value) && (next.type = 4), this.lookahead = next, this.config.tokens && next.type !== 2 && this.tokens.push(this.convertToken(next)), token;
              }, Parser2.prototype.nextRegexToken = function() {
                this.collectComments();
                var token = this.scanner.scanRegExp();
                return this.config.tokens && (this.tokens.pop(), this.tokens.push(this.convertToken(token))), this.lookahead = token, this.nextToken(), token;
              }, Parser2.prototype.createNode = function() {
                return {
                  index: this.startMarker.index,
                  line: this.startMarker.line,
                  column: this.startMarker.column
                };
              }, Parser2.prototype.startNode = function(token, lastLineStart) {
                lastLineStart === void 0 && (lastLineStart = 0);
                var column = token.start - token.lineStart, line = token.lineNumber;
                return column < 0 && (column += lastLineStart, line--), {
                  index: token.start,
                  line,
                  column
                };
              }, Parser2.prototype.finalize = function(marker, node) {
                if (this.config.range && (node.range = [marker.index, this.lastMarker.index]), this.config.loc && (node.loc = {
                  start: {
                    line: marker.line,
                    column: marker.column
                  },
                  end: {
                    line: this.lastMarker.line,
                    column: this.lastMarker.column
                  }
                }, this.config.source && (node.loc.source = this.config.source)), this.delegate) {
                  var metadata = {
                    start: {
                      line: marker.line,
                      column: marker.column,
                      offset: marker.index
                    },
                    end: {
                      line: this.lastMarker.line,
                      column: this.lastMarker.column,
                      offset: this.lastMarker.index
                    }
                  };
                  this.delegate(node, metadata);
                }
                return node;
              }, Parser2.prototype.expect = function(value) {
                var token = this.nextToken();
                (token.type !== 7 || token.value !== value) && this.throwUnexpectedToken(token);
              }, Parser2.prototype.expectCommaSeparator = function() {
                if (this.config.tolerant) {
                  var token = this.lookahead;
                  token.type === 7 && token.value === "," ? this.nextToken() : token.type === 7 && token.value === ";" ? (this.nextToken(), this.tolerateUnexpectedToken(token)) : this.tolerateUnexpectedToken(token, messages_1.Messages.UnexpectedToken);
                } else
                  this.expect(",");
              }, Parser2.prototype.expectKeyword = function(keyword) {
                var token = this.nextToken();
                (token.type !== 4 || token.value !== keyword) && this.throwUnexpectedToken(token);
              }, Parser2.prototype.match = function(value) {
                return this.lookahead.type === 7 && this.lookahead.value === value;
              }, Parser2.prototype.matchKeyword = function(keyword) {
                return this.lookahead.type === 4 && this.lookahead.value === keyword;
              }, Parser2.prototype.matchContextualKeyword = function(keyword) {
                return this.lookahead.type === 3 && this.lookahead.value === keyword;
              }, Parser2.prototype.matchAssign = function() {
                if (this.lookahead.type !== 7)
                  return !1;
                var op = this.lookahead.value;
                return op === "=" || op === "*=" || op === "**=" || op === "/=" || op === "%=" || op === "+=" || op === "-=" || op === "<<=" || op === ">>=" || op === ">>>=" || op === "&=" || op === "^=" || op === "|=";
              }, Parser2.prototype.isolateCoverGrammar = function(parseFunction) {
                var previousIsBindingElement = this.context.isBindingElement, previousIsAssignmentTarget = this.context.isAssignmentTarget, previousFirstCoverInitializedNameError = this.context.firstCoverInitializedNameError;
                this.context.isBindingElement = !0, this.context.isAssignmentTarget = !0, this.context.firstCoverInitializedNameError = null;
                var result = parseFunction.call(this);
                return this.context.firstCoverInitializedNameError !== null && this.throwUnexpectedToken(this.context.firstCoverInitializedNameError), this.context.isBindingElement = previousIsBindingElement, this.context.isAssignmentTarget = previousIsAssignmentTarget, this.context.firstCoverInitializedNameError = previousFirstCoverInitializedNameError, result;
              }, Parser2.prototype.inheritCoverGrammar = function(parseFunction) {
                var previousIsBindingElement = this.context.isBindingElement, previousIsAssignmentTarget = this.context.isAssignmentTarget, previousFirstCoverInitializedNameError = this.context.firstCoverInitializedNameError;
                this.context.isBindingElement = !0, this.context.isAssignmentTarget = !0, this.context.firstCoverInitializedNameError = null;
                var result = parseFunction.call(this);
                return this.context.isBindingElement = this.context.isBindingElement && previousIsBindingElement, this.context.isAssignmentTarget = this.context.isAssignmentTarget && previousIsAssignmentTarget, this.context.firstCoverInitializedNameError = previousFirstCoverInitializedNameError || this.context.firstCoverInitializedNameError, result;
              }, Parser2.prototype.consumeSemicolon = function() {
                this.match(";") ? this.nextToken() : this.hasLineTerminator || (this.lookahead.type !== 2 && !this.match("}") && this.throwUnexpectedToken(this.lookahead), this.lastMarker.index = this.startMarker.index, this.lastMarker.line = this.startMarker.line, this.lastMarker.column = this.startMarker.column);
              }, Parser2.prototype.parsePrimaryExpression = function() {
                var node = this.createNode(), expr, token, raw;
                switch (this.lookahead.type) {
                  case 3:
                    (this.context.isModule || this.context.await) && this.lookahead.value === "await" && this.tolerateUnexpectedToken(this.lookahead), expr = this.matchAsyncFunction() ? this.parseFunctionExpression() : this.finalize(node, new Node.Identifier(this.nextToken().value));
                    break;
                  case 6:
                  case 8:
                    this.context.strict && this.lookahead.octal && this.tolerateUnexpectedToken(this.lookahead, messages_1.Messages.StrictOctalLiteral), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1, token = this.nextToken(), raw = this.getTokenRaw(token), expr = this.finalize(node, new Node.Literal(token.value, raw));
                    break;
                  case 1:
                    this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1, token = this.nextToken(), raw = this.getTokenRaw(token), expr = this.finalize(node, new Node.Literal(token.value === "true", raw));
                    break;
                  case 5:
                    this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1, token = this.nextToken(), raw = this.getTokenRaw(token), expr = this.finalize(node, new Node.Literal(null, raw));
                    break;
                  case 10:
                    expr = this.parseTemplateLiteral();
                    break;
                  case 7:
                    switch (this.lookahead.value) {
                      case "(":
                        this.context.isBindingElement = !1, expr = this.inheritCoverGrammar(this.parseGroupExpression);
                        break;
                      case "[":
                        expr = this.inheritCoverGrammar(this.parseArrayInitializer);
                        break;
                      case "{":
                        expr = this.inheritCoverGrammar(this.parseObjectInitializer);
                        break;
                      case "/":
                      case "/=":
                        this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1, this.scanner.index = this.startMarker.index, token = this.nextRegexToken(), raw = this.getTokenRaw(token), expr = this.finalize(node, new Node.RegexLiteral(token.regex, raw, token.pattern, token.flags));
                        break;
                      default:
                        expr = this.throwUnexpectedToken(this.nextToken());
                    }
                    break;
                  case 4:
                    !this.context.strict && this.context.allowYield && this.matchKeyword("yield") ? expr = this.parseIdentifierName() : !this.context.strict && this.matchKeyword("let") ? expr = this.finalize(node, new Node.Identifier(this.nextToken().value)) : (this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1, this.matchKeyword("function") ? expr = this.parseFunctionExpression() : this.matchKeyword("this") ? (this.nextToken(), expr = this.finalize(node, new Node.ThisExpression())) : this.matchKeyword("class") ? expr = this.parseClassExpression() : expr = this.throwUnexpectedToken(this.nextToken()));
                    break;
                  default:
                    expr = this.throwUnexpectedToken(this.nextToken());
                }
                return expr;
              }, Parser2.prototype.parseSpreadElement = function() {
                var node = this.createNode();
                this.expect("...");
                var arg = this.inheritCoverGrammar(this.parseAssignmentExpression);
                return this.finalize(node, new Node.SpreadElement(arg));
              }, Parser2.prototype.parseArrayInitializer = function() {
                var node = this.createNode(), elements = [];
                for (this.expect("["); !this.match("]"); )
                  if (this.match(","))
                    this.nextToken(), elements.push(null);
                  else if (this.match("...")) {
                    var element = this.parseSpreadElement();
                    this.match("]") || (this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1, this.expect(",")), elements.push(element);
                  } else
                    elements.push(this.inheritCoverGrammar(this.parseAssignmentExpression)), this.match("]") || this.expect(",");
                return this.expect("]"), this.finalize(node, new Node.ArrayExpression(elements));
              }, Parser2.prototype.parsePropertyMethod = function(params) {
                this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                var previousStrict = this.context.strict, previousAllowStrictDirective = this.context.allowStrictDirective;
                this.context.allowStrictDirective = params.simple;
                var body = this.isolateCoverGrammar(this.parseFunctionSourceElements);
                return this.context.strict && params.firstRestricted && this.tolerateUnexpectedToken(params.firstRestricted, params.message), this.context.strict && params.stricted && this.tolerateUnexpectedToken(params.stricted, params.message), this.context.strict = previousStrict, this.context.allowStrictDirective = previousAllowStrictDirective, body;
              }, Parser2.prototype.parsePropertyMethodFunction = function() {
                var isGenerator = !1, node = this.createNode(), previousAllowYield = this.context.allowYield;
                this.context.allowYield = !0;
                var params = this.parseFormalParameters(), method = this.parsePropertyMethod(params);
                return this.context.allowYield = previousAllowYield, this.finalize(node, new Node.FunctionExpression(null, params.params, method, isGenerator));
              }, Parser2.prototype.parsePropertyMethodAsyncFunction = function() {
                var node = this.createNode(), previousAllowYield = this.context.allowYield, previousAwait = this.context.await;
                this.context.allowYield = !1, this.context.await = !0;
                var params = this.parseFormalParameters(), method = this.parsePropertyMethod(params);
                return this.context.allowYield = previousAllowYield, this.context.await = previousAwait, this.finalize(node, new Node.AsyncFunctionExpression(null, params.params, method));
              }, Parser2.prototype.parseObjectPropertyKey = function() {
                var node = this.createNode(), token = this.nextToken(), key;
                switch (token.type) {
                  case 8:
                  case 6:
                    this.context.strict && token.octal && this.tolerateUnexpectedToken(token, messages_1.Messages.StrictOctalLiteral);
                    var raw = this.getTokenRaw(token);
                    key = this.finalize(node, new Node.Literal(token.value, raw));
                    break;
                  case 3:
                  case 1:
                  case 5:
                  case 4:
                    key = this.finalize(node, new Node.Identifier(token.value));
                    break;
                  case 7:
                    token.value === "[" ? (key = this.isolateCoverGrammar(this.parseAssignmentExpression), this.expect("]")) : key = this.throwUnexpectedToken(token);
                    break;
                  default:
                    key = this.throwUnexpectedToken(token);
                }
                return key;
              }, Parser2.prototype.isPropertyKey = function(key, value) {
                return key.type === syntax_1.Syntax.Identifier && key.name === value || key.type === syntax_1.Syntax.Literal && key.value === value;
              }, Parser2.prototype.parseObjectProperty = function(hasProto) {
                var node = this.createNode(), token = this.lookahead, kind, key = null, value = null, computed = !1, method = !1, shorthand = !1, isAsync = !1;
                if (token.type === 3) {
                  var id = token.value;
                  this.nextToken(), computed = this.match("["), isAsync = !this.hasLineTerminator && id === "async" && !this.match(":") && !this.match("(") && !this.match("*") && !this.match(","), key = isAsync ? this.parseObjectPropertyKey() : this.finalize(node, new Node.Identifier(id));
                } else this.match("*") ? this.nextToken() : (computed = this.match("["), key = this.parseObjectPropertyKey());
                var lookaheadPropertyKey = this.qualifiedPropertyName(this.lookahead);
                if (token.type === 3 && !isAsync && token.value === "get" && lookaheadPropertyKey)
                  kind = "get", computed = this.match("["), key = this.parseObjectPropertyKey(), this.context.allowYield = !1, value = this.parseGetterMethod();
                else if (token.type === 3 && !isAsync && token.value === "set" && lookaheadPropertyKey)
                  kind = "set", computed = this.match("["), key = this.parseObjectPropertyKey(), value = this.parseSetterMethod();
                else if (token.type === 7 && token.value === "*" && lookaheadPropertyKey)
                  kind = "init", computed = this.match("["), key = this.parseObjectPropertyKey(), value = this.parseGeneratorMethod(), method = !0;
                else if (key || this.throwUnexpectedToken(this.lookahead), kind = "init", this.match(":") && !isAsync)
                  !computed && this.isPropertyKey(key, "__proto__") && (hasProto.value && this.tolerateError(messages_1.Messages.DuplicateProtoProperty), hasProto.value = !0), this.nextToken(), value = this.inheritCoverGrammar(this.parseAssignmentExpression);
                else if (this.match("("))
                  value = isAsync ? this.parsePropertyMethodAsyncFunction() : this.parsePropertyMethodFunction(), method = !0;
                else if (token.type === 3) {
                  var id = this.finalize(node, new Node.Identifier(token.value));
                  if (this.match("=")) {
                    this.context.firstCoverInitializedNameError = this.lookahead, this.nextToken(), shorthand = !0;
                    var init = this.isolateCoverGrammar(this.parseAssignmentExpression);
                    value = this.finalize(node, new Node.AssignmentPattern(id, init));
                  } else
                    shorthand = !0, value = id;
                } else
                  this.throwUnexpectedToken(this.nextToken());
                return this.finalize(node, new Node.Property(kind, key, computed, value, method, shorthand));
              }, Parser2.prototype.parseObjectInitializer = function() {
                var node = this.createNode();
                this.expect("{");
                for (var properties = [], hasProto = { value: !1 }; !this.match("}"); )
                  properties.push(this.parseObjectProperty(hasProto)), this.match("}") || this.expectCommaSeparator();
                return this.expect("}"), this.finalize(node, new Node.ObjectExpression(properties));
              }, Parser2.prototype.parseTemplateHead = function() {
                assert_1.assert(this.lookahead.head, "Template literal must start with a template head");
                var node = this.createNode(), token = this.nextToken(), raw = token.value, cooked = token.cooked;
                return this.finalize(node, new Node.TemplateElement({ raw, cooked }, token.tail));
              }, Parser2.prototype.parseTemplateElement = function() {
                this.lookahead.type !== 10 && this.throwUnexpectedToken();
                var node = this.createNode(), token = this.nextToken(), raw = token.value, cooked = token.cooked;
                return this.finalize(node, new Node.TemplateElement({ raw, cooked }, token.tail));
              }, Parser2.prototype.parseTemplateLiteral = function() {
                var node = this.createNode(), expressions = [], quasis = [], quasi = this.parseTemplateHead();
                for (quasis.push(quasi); !quasi.tail; )
                  expressions.push(this.parseExpression()), quasi = this.parseTemplateElement(), quasis.push(quasi);
                return this.finalize(node, new Node.TemplateLiteral(quasis, expressions));
              }, Parser2.prototype.reinterpretExpressionAsPattern = function(expr) {
                switch (expr.type) {
                  case syntax_1.Syntax.Identifier:
                  case syntax_1.Syntax.MemberExpression:
                  case syntax_1.Syntax.RestElement:
                  case syntax_1.Syntax.AssignmentPattern:
                    break;
                  case syntax_1.Syntax.SpreadElement:
                    expr.type = syntax_1.Syntax.RestElement, this.reinterpretExpressionAsPattern(expr.argument);
                    break;
                  case syntax_1.Syntax.ArrayExpression:
                    expr.type = syntax_1.Syntax.ArrayPattern;
                    for (var i = 0; i < expr.elements.length; i++)
                      expr.elements[i] !== null && this.reinterpretExpressionAsPattern(expr.elements[i]);
                    break;
                  case syntax_1.Syntax.ObjectExpression:
                    expr.type = syntax_1.Syntax.ObjectPattern;
                    for (var i = 0; i < expr.properties.length; i++)
                      this.reinterpretExpressionAsPattern(expr.properties[i].value);
                    break;
                  case syntax_1.Syntax.AssignmentExpression:
                    expr.type = syntax_1.Syntax.AssignmentPattern, delete expr.operator, this.reinterpretExpressionAsPattern(expr.left);
                    break;
                  default:
                    break;
                }
              }, Parser2.prototype.parseGroupExpression = function() {
                var expr;
                if (this.expect("("), this.match(")"))
                  this.nextToken(), this.match("=>") || this.expect("=>"), expr = {
                    type: ArrowParameterPlaceHolder,
                    params: [],
                    async: !1
                  };
                else {
                  var startToken = this.lookahead, params = [];
                  if (this.match("..."))
                    expr = this.parseRestElement(params), this.expect(")"), this.match("=>") || this.expect("=>"), expr = {
                      type: ArrowParameterPlaceHolder,
                      params: [expr],
                      async: !1
                    };
                  else {
                    var arrow = !1;
                    if (this.context.isBindingElement = !0, expr = this.inheritCoverGrammar(this.parseAssignmentExpression), this.match(",")) {
                      var expressions = [];
                      for (this.context.isAssignmentTarget = !1, expressions.push(expr); this.lookahead.type !== 2 && this.match(","); ) {
                        if (this.nextToken(), this.match(")")) {
                          this.nextToken();
                          for (var i = 0; i < expressions.length; i++)
                            this.reinterpretExpressionAsPattern(expressions[i]);
                          arrow = !0, expr = {
                            type: ArrowParameterPlaceHolder,
                            params: expressions,
                            async: !1
                          };
                        } else if (this.match("...")) {
                          this.context.isBindingElement || this.throwUnexpectedToken(this.lookahead), expressions.push(this.parseRestElement(params)), this.expect(")"), this.match("=>") || this.expect("=>"), this.context.isBindingElement = !1;
                          for (var i = 0; i < expressions.length; i++)
                            this.reinterpretExpressionAsPattern(expressions[i]);
                          arrow = !0, expr = {
                            type: ArrowParameterPlaceHolder,
                            params: expressions,
                            async: !1
                          };
                        } else
                          expressions.push(this.inheritCoverGrammar(this.parseAssignmentExpression));
                        if (arrow)
                          break;
                      }
                      arrow || (expr = this.finalize(this.startNode(startToken), new Node.SequenceExpression(expressions)));
                    }
                    if (!arrow) {
                      if (this.expect(")"), this.match("=>") && (expr.type === syntax_1.Syntax.Identifier && expr.name === "yield" && (arrow = !0, expr = {
                        type: ArrowParameterPlaceHolder,
                        params: [expr],
                        async: !1
                      }), !arrow)) {
                        if (this.context.isBindingElement || this.throwUnexpectedToken(this.lookahead), expr.type === syntax_1.Syntax.SequenceExpression)
                          for (var i = 0; i < expr.expressions.length; i++)
                            this.reinterpretExpressionAsPattern(expr.expressions[i]);
                        else
                          this.reinterpretExpressionAsPattern(expr);
                        var parameters = expr.type === syntax_1.Syntax.SequenceExpression ? expr.expressions : [expr];
                        expr = {
                          type: ArrowParameterPlaceHolder,
                          params: parameters,
                          async: !1
                        };
                      }
                      this.context.isBindingElement = !1;
                    }
                  }
                }
                return expr;
              }, Parser2.prototype.parseArguments = function() {
                this.expect("(");
                var args = [];
                if (!this.match(")"))
                  for (; ; ) {
                    var expr = this.match("...") ? this.parseSpreadElement() : this.isolateCoverGrammar(this.parseAssignmentExpression);
                    if (args.push(expr), this.match(")") || (this.expectCommaSeparator(), this.match(")")))
                      break;
                  }
                return this.expect(")"), args;
              }, Parser2.prototype.isIdentifierName = function(token) {
                return token.type === 3 || token.type === 4 || token.type === 1 || token.type === 5;
              }, Parser2.prototype.parseIdentifierName = function() {
                var node = this.createNode(), token = this.nextToken();
                return this.isIdentifierName(token) || this.throwUnexpectedToken(token), this.finalize(node, new Node.Identifier(token.value));
              }, Parser2.prototype.parseNewExpression = function() {
                var node = this.createNode(), id = this.parseIdentifierName();
                assert_1.assert(id.name === "new", "New expression must start with `new`");
                var expr;
                if (this.match("."))
                  if (this.nextToken(), this.lookahead.type === 3 && this.context.inFunctionBody && this.lookahead.value === "target") {
                    var property = this.parseIdentifierName();
                    expr = new Node.MetaProperty(id, property);
                  } else
                    this.throwUnexpectedToken(this.lookahead);
                else {
                  var callee = this.isolateCoverGrammar(this.parseLeftHandSideExpression), args = this.match("(") ? this.parseArguments() : [];
                  expr = new Node.NewExpression(callee, args), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                }
                return this.finalize(node, expr);
              }, Parser2.prototype.parseAsyncArgument = function() {
                var arg = this.parseAssignmentExpression();
                return this.context.firstCoverInitializedNameError = null, arg;
              }, Parser2.prototype.parseAsyncArguments = function() {
                this.expect("(");
                var args = [];
                if (!this.match(")"))
                  for (; ; ) {
                    var expr = this.match("...") ? this.parseSpreadElement() : this.isolateCoverGrammar(this.parseAsyncArgument);
                    if (args.push(expr), this.match(")") || (this.expectCommaSeparator(), this.match(")")))
                      break;
                  }
                return this.expect(")"), args;
              }, Parser2.prototype.parseLeftHandSideExpressionAllowCall = function() {
                var startToken = this.lookahead, maybeAsync = this.matchContextualKeyword("async"), previousAllowIn = this.context.allowIn;
                this.context.allowIn = !0;
                var expr;
                for (this.matchKeyword("super") && this.context.inFunctionBody ? (expr = this.createNode(), this.nextToken(), expr = this.finalize(expr, new Node.Super()), !this.match("(") && !this.match(".") && !this.match("[") && this.throwUnexpectedToken(this.lookahead)) : expr = this.inheritCoverGrammar(this.matchKeyword("new") ? this.parseNewExpression : this.parsePrimaryExpression); ; )
                  if (this.match(".")) {
                    this.context.isBindingElement = !1, this.context.isAssignmentTarget = !0, this.expect(".");
                    var property = this.parseIdentifierName();
                    expr = this.finalize(this.startNode(startToken), new Node.StaticMemberExpression(expr, property));
                  } else if (this.match("(")) {
                    var asyncArrow = maybeAsync && startToken.lineNumber === this.lookahead.lineNumber;
                    this.context.isBindingElement = !1, this.context.isAssignmentTarget = !1;
                    var args = asyncArrow ? this.parseAsyncArguments() : this.parseArguments();
                    if (expr = this.finalize(this.startNode(startToken), new Node.CallExpression(expr, args)), asyncArrow && this.match("=>")) {
                      for (var i = 0; i < args.length; ++i)
                        this.reinterpretExpressionAsPattern(args[i]);
                      expr = {
                        type: ArrowParameterPlaceHolder,
                        params: args,
                        async: !0
                      };
                    }
                  } else if (this.match("[")) {
                    this.context.isBindingElement = !1, this.context.isAssignmentTarget = !0, this.expect("[");
                    var property = this.isolateCoverGrammar(this.parseExpression);
                    this.expect("]"), expr = this.finalize(this.startNode(startToken), new Node.ComputedMemberExpression(expr, property));
                  } else if (this.lookahead.type === 10 && this.lookahead.head) {
                    var quasi = this.parseTemplateLiteral();
                    expr = this.finalize(this.startNode(startToken), new Node.TaggedTemplateExpression(expr, quasi));
                  } else
                    break;
                return this.context.allowIn = previousAllowIn, expr;
              }, Parser2.prototype.parseSuper = function() {
                var node = this.createNode();
                return this.expectKeyword("super"), !this.match("[") && !this.match(".") && this.throwUnexpectedToken(this.lookahead), this.finalize(node, new Node.Super());
              }, Parser2.prototype.parseLeftHandSideExpression = function() {
                assert_1.assert(this.context.allowIn, "callee of new expression always allow in keyword.");
                for (var node = this.startNode(this.lookahead), expr = this.matchKeyword("super") && this.context.inFunctionBody ? this.parseSuper() : this.inheritCoverGrammar(this.matchKeyword("new") ? this.parseNewExpression : this.parsePrimaryExpression); ; )
                  if (this.match("[")) {
                    this.context.isBindingElement = !1, this.context.isAssignmentTarget = !0, this.expect("[");
                    var property = this.isolateCoverGrammar(this.parseExpression);
                    this.expect("]"), expr = this.finalize(node, new Node.ComputedMemberExpression(expr, property));
                  } else if (this.match(".")) {
                    this.context.isBindingElement = !1, this.context.isAssignmentTarget = !0, this.expect(".");
                    var property = this.parseIdentifierName();
                    expr = this.finalize(node, new Node.StaticMemberExpression(expr, property));
                  } else if (this.lookahead.type === 10 && this.lookahead.head) {
                    var quasi = this.parseTemplateLiteral();
                    expr = this.finalize(node, new Node.TaggedTemplateExpression(expr, quasi));
                  } else
                    break;
                return expr;
              }, Parser2.prototype.parseUpdateExpression = function() {
                var expr, startToken = this.lookahead;
                if (this.match("++") || this.match("--")) {
                  var node = this.startNode(startToken), token = this.nextToken();
                  expr = this.inheritCoverGrammar(this.parseUnaryExpression), this.context.strict && expr.type === syntax_1.Syntax.Identifier && this.scanner.isRestrictedWord(expr.name) && this.tolerateError(messages_1.Messages.StrictLHSPrefix), this.context.isAssignmentTarget || this.tolerateError(messages_1.Messages.InvalidLHSInAssignment);
                  var prefix = !0;
                  expr = this.finalize(node, new Node.UpdateExpression(token.value, expr, prefix)), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                } else if (expr = this.inheritCoverGrammar(this.parseLeftHandSideExpressionAllowCall), !this.hasLineTerminator && this.lookahead.type === 7 && (this.match("++") || this.match("--"))) {
                  this.context.strict && expr.type === syntax_1.Syntax.Identifier && this.scanner.isRestrictedWord(expr.name) && this.tolerateError(messages_1.Messages.StrictLHSPostfix), this.context.isAssignmentTarget || this.tolerateError(messages_1.Messages.InvalidLHSInAssignment), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                  var operator = this.nextToken().value, prefix = !1;
                  expr = this.finalize(this.startNode(startToken), new Node.UpdateExpression(operator, expr, prefix));
                }
                return expr;
              }, Parser2.prototype.parseAwaitExpression = function() {
                var node = this.createNode();
                this.nextToken();
                var argument = this.parseUnaryExpression();
                return this.finalize(node, new Node.AwaitExpression(argument));
              }, Parser2.prototype.parseUnaryExpression = function() {
                var expr;
                if (this.match("+") || this.match("-") || this.match("~") || this.match("!") || this.matchKeyword("delete") || this.matchKeyword("void") || this.matchKeyword("typeof")) {
                  var node = this.startNode(this.lookahead), token = this.nextToken();
                  expr = this.inheritCoverGrammar(this.parseUnaryExpression), expr = this.finalize(node, new Node.UnaryExpression(token.value, expr)), this.context.strict && expr.operator === "delete" && expr.argument.type === syntax_1.Syntax.Identifier && this.tolerateError(messages_1.Messages.StrictDelete), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                } else this.context.await && this.matchContextualKeyword("await") ? expr = this.parseAwaitExpression() : expr = this.parseUpdateExpression();
                return expr;
              }, Parser2.prototype.parseExponentiationExpression = function() {
                var startToken = this.lookahead, expr = this.inheritCoverGrammar(this.parseUnaryExpression);
                if (expr.type !== syntax_1.Syntax.UnaryExpression && this.match("**")) {
                  this.nextToken(), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                  var left = expr, right = this.isolateCoverGrammar(this.parseExponentiationExpression);
                  expr = this.finalize(this.startNode(startToken), new Node.BinaryExpression("**", left, right));
                }
                return expr;
              }, Parser2.prototype.binaryPrecedence = function(token) {
                var op = token.value, precedence;
                return token.type === 7 ? precedence = this.operatorPrecedence[op] || 0 : token.type === 4 ? precedence = op === "instanceof" || this.context.allowIn && op === "in" ? 7 : 0 : precedence = 0, precedence;
              }, Parser2.prototype.parseBinaryExpression = function() {
                var startToken = this.lookahead, expr = this.inheritCoverGrammar(this.parseExponentiationExpression), token = this.lookahead, prec = this.binaryPrecedence(token);
                if (prec > 0) {
                  this.nextToken(), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                  for (var markers = [startToken, this.lookahead], left = expr, right = this.isolateCoverGrammar(this.parseExponentiationExpression), stack = [left, token.value, right], precedences = [prec]; prec = this.binaryPrecedence(this.lookahead), !(prec <= 0); ) {
                    for (; stack.length > 2 && prec <= precedences[precedences.length - 1]; ) {
                      right = stack.pop();
                      var operator = stack.pop();
                      precedences.pop(), left = stack.pop(), markers.pop();
                      var node = this.startNode(markers[markers.length - 1]);
                      stack.push(this.finalize(node, new Node.BinaryExpression(operator, left, right)));
                    }
                    stack.push(this.nextToken().value), precedences.push(prec), markers.push(this.lookahead), stack.push(this.isolateCoverGrammar(this.parseExponentiationExpression));
                  }
                  var i = stack.length - 1;
                  expr = stack[i];
                  for (var lastMarker = markers.pop(); i > 1; ) {
                    var marker = markers.pop(), lastLineStart = lastMarker && lastMarker.lineStart, node = this.startNode(marker, lastLineStart), operator = stack[i - 1];
                    expr = this.finalize(node, new Node.BinaryExpression(operator, stack[i - 2], expr)), i -= 2, lastMarker = marker;
                  }
                }
                return expr;
              }, Parser2.prototype.parseConditionalExpression = function() {
                var startToken = this.lookahead, expr = this.inheritCoverGrammar(this.parseBinaryExpression);
                if (this.match("?")) {
                  this.nextToken();
                  var previousAllowIn = this.context.allowIn;
                  this.context.allowIn = !0;
                  var consequent = this.isolateCoverGrammar(this.parseAssignmentExpression);
                  this.context.allowIn = previousAllowIn, this.expect(":");
                  var alternate = this.isolateCoverGrammar(this.parseAssignmentExpression);
                  expr = this.finalize(this.startNode(startToken), new Node.ConditionalExpression(expr, consequent, alternate)), this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                }
                return expr;
              }, Parser2.prototype.checkPatternParam = function(options, param) {
                switch (param.type) {
                  case syntax_1.Syntax.Identifier:
                    this.validateParam(options, param, param.name);
                    break;
                  case syntax_1.Syntax.RestElement:
                    this.checkPatternParam(options, param.argument);
                    break;
                  case syntax_1.Syntax.AssignmentPattern:
                    this.checkPatternParam(options, param.left);
                    break;
                  case syntax_1.Syntax.ArrayPattern:
                    for (var i = 0; i < param.elements.length; i++)
                      param.elements[i] !== null && this.checkPatternParam(options, param.elements[i]);
                    break;
                  case syntax_1.Syntax.ObjectPattern:
                    for (var i = 0; i < param.properties.length; i++)
                      this.checkPatternParam(options, param.properties[i].value);
                    break;
                  default:
                    break;
                }
                options.simple = options.simple && param instanceof Node.Identifier;
              }, Parser2.prototype.reinterpretAsCoverFormalsList = function(expr) {
                var params = [expr], options, asyncArrow = !1;
                switch (expr.type) {
                  case syntax_1.Syntax.Identifier:
                    break;
                  case ArrowParameterPlaceHolder:
                    params = expr.params, asyncArrow = expr.async;
                    break;
                  default:
                    return null;
                }
                options = {
                  simple: !0,
                  paramSet: {}
                };
                for (var i = 0; i < params.length; ++i) {
                  var param = params[i];
                  param.type === syntax_1.Syntax.AssignmentPattern ? param.right.type === syntax_1.Syntax.YieldExpression && (param.right.argument && this.throwUnexpectedToken(this.lookahead), param.right.type = syntax_1.Syntax.Identifier, param.right.name = "yield", delete param.right.argument, delete param.right.delegate) : asyncArrow && param.type === syntax_1.Syntax.Identifier && param.name === "await" && this.throwUnexpectedToken(this.lookahead), this.checkPatternParam(options, param), params[i] = param;
                }
                if (this.context.strict || !this.context.allowYield)
                  for (var i = 0; i < params.length; ++i) {
                    var param = params[i];
                    param.type === syntax_1.Syntax.YieldExpression && this.throwUnexpectedToken(this.lookahead);
                  }
                if (options.message === messages_1.Messages.StrictParamDupe) {
                  var token = this.context.strict ? options.stricted : options.firstRestricted;
                  this.throwUnexpectedToken(token, options.message);
                }
                return {
                  simple: options.simple,
                  params,
                  stricted: options.stricted,
                  firstRestricted: options.firstRestricted,
                  message: options.message
                };
              }, Parser2.prototype.parseAssignmentExpression = function() {
                var expr;
                if (!this.context.allowYield && this.matchKeyword("yield"))
                  expr = this.parseYieldExpression();
                else {
                  var startToken = this.lookahead, token = startToken;
                  if (expr = this.parseConditionalExpression(), token.type === 3 && token.lineNumber === this.lookahead.lineNumber && token.value === "async" && (this.lookahead.type === 3 || this.matchKeyword("yield"))) {
                    var arg = this.parsePrimaryExpression();
                    this.reinterpretExpressionAsPattern(arg), expr = {
                      type: ArrowParameterPlaceHolder,
                      params: [arg],
                      async: !0
                    };
                  }
                  if (expr.type === ArrowParameterPlaceHolder || this.match("=>")) {
                    this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1;
                    var isAsync = expr.async, list = this.reinterpretAsCoverFormalsList(expr);
                    if (list) {
                      this.hasLineTerminator && this.tolerateUnexpectedToken(this.lookahead), this.context.firstCoverInitializedNameError = null;
                      var previousStrict = this.context.strict, previousAllowStrictDirective = this.context.allowStrictDirective;
                      this.context.allowStrictDirective = list.simple;
                      var previousAllowYield = this.context.allowYield, previousAwait = this.context.await;
                      this.context.allowYield = !0, this.context.await = isAsync;
                      var node = this.startNode(startToken);
                      this.expect("=>");
                      var body = void 0;
                      if (this.match("{")) {
                        var previousAllowIn = this.context.allowIn;
                        this.context.allowIn = !0, body = this.parseFunctionSourceElements(), this.context.allowIn = previousAllowIn;
                      } else
                        body = this.isolateCoverGrammar(this.parseAssignmentExpression);
                      var expression = body.type !== syntax_1.Syntax.BlockStatement;
                      this.context.strict && list.firstRestricted && this.throwUnexpectedToken(list.firstRestricted, list.message), this.context.strict && list.stricted && this.tolerateUnexpectedToken(list.stricted, list.message), expr = isAsync ? this.finalize(node, new Node.AsyncArrowFunctionExpression(list.params, body, expression)) : this.finalize(node, new Node.ArrowFunctionExpression(list.params, body, expression)), this.context.strict = previousStrict, this.context.allowStrictDirective = previousAllowStrictDirective, this.context.allowYield = previousAllowYield, this.context.await = previousAwait;
                    }
                  } else if (this.matchAssign()) {
                    if (this.context.isAssignmentTarget || this.tolerateError(messages_1.Messages.InvalidLHSInAssignment), this.context.strict && expr.type === syntax_1.Syntax.Identifier) {
                      var id = expr;
                      this.scanner.isRestrictedWord(id.name) && this.tolerateUnexpectedToken(token, messages_1.Messages.StrictLHSAssignment), this.scanner.isStrictModeReservedWord(id.name) && this.tolerateUnexpectedToken(token, messages_1.Messages.StrictReservedWord);
                    }
                    this.match("=") ? this.reinterpretExpressionAsPattern(expr) : (this.context.isAssignmentTarget = !1, this.context.isBindingElement = !1), token = this.nextToken();
                    var operator = token.value, right = this.isolateCoverGrammar(this.parseAssignmentExpression);
                    expr = this.finalize(this.startNode(startToken), new Node.AssignmentExpression(operator, expr, right)), this.context.firstCoverInitializedNameError = null;
                  }
                }
                return expr;
              }, Parser2.prototype.parseExpression = function() {
                var startToken = this.lookahead, expr = this.isolateCoverGrammar(this.parseAssignmentExpression);
                if (this.match(",")) {
                  var expressions = [];
                  for (expressions.push(expr); this.lookahead.type !== 2 && this.match(","); )
                    this.nextToken(), expressions.push(this.isolateCoverGrammar(this.parseAssignmentExpression));
                  expr = this.finalize(this.startNode(startToken), new Node.SequenceExpression(expressions));
                }
                return expr;
              }, Parser2.prototype.parseStatementListItem = function() {
                var statement;
                if (this.context.isAssignmentTarget = !0, this.context.isBindingElement = !0, this.lookahead.type === 4)
                  switch (this.lookahead.value) {
                    case "export":
                      this.context.isModule || this.tolerateUnexpectedToken(this.lookahead, messages_1.Messages.IllegalExportDeclaration), statement = this.parseExportDeclaration();
                      break;
                    case "import":
                      this.context.isModule || this.tolerateUnexpectedToken(this.lookahead, messages_1.Messages.IllegalImportDeclaration), statement = this.parseImportDeclaration();
                      break;
                    case "const":
                      statement = this.parseLexicalDeclaration({ inFor: !1 });
                      break;
                    case "function":
                      statement = this.parseFunctionDeclaration();
                      break;
                    case "class":
                      statement = this.parseClassDeclaration();
                      break;
                    case "let":
                      statement = this.isLexicalDeclaration() ? this.parseLexicalDeclaration({ inFor: !1 }) : this.parseStatement();
                      break;
                    default:
                      statement = this.parseStatement();
                      break;
                  }
                else
                  statement = this.parseStatement();
                return statement;
              }, Parser2.prototype.parseBlock = function() {
                var node = this.createNode();
                this.expect("{");
                for (var block = []; !this.match("}"); )
                  block.push(this.parseStatementListItem());
                return this.expect("}"), this.finalize(node, new Node.BlockStatement(block));
              }, Parser2.prototype.parseLexicalBinding = function(kind, options) {
                var node = this.createNode(), params = [], id = this.parsePattern(params, kind);
                this.context.strict && id.type === syntax_1.Syntax.Identifier && this.scanner.isRestrictedWord(id.name) && this.tolerateError(messages_1.Messages.StrictVarName);
                var init = null;
                return kind === "const" ? !this.matchKeyword("in") && !this.matchContextualKeyword("of") && (this.match("=") ? (this.nextToken(), init = this.isolateCoverGrammar(this.parseAssignmentExpression)) : this.throwError(messages_1.Messages.DeclarationMissingInitializer, "const")) : (!options.inFor && id.type !== syntax_1.Syntax.Identifier || this.match("=")) && (this.expect("="), init = this.isolateCoverGrammar(this.parseAssignmentExpression)), this.finalize(node, new Node.VariableDeclarator(id, init));
              }, Parser2.prototype.parseBindingList = function(kind, options) {
                for (var list = [this.parseLexicalBinding(kind, options)]; this.match(","); )
                  this.nextToken(), list.push(this.parseLexicalBinding(kind, options));
                return list;
              }, Parser2.prototype.isLexicalDeclaration = function() {
                var state = this.scanner.saveState();
                this.scanner.scanComments();
                var next = this.scanner.lex();
                return this.scanner.restoreState(state), next.type === 3 || next.type === 7 && next.value === "[" || next.type === 7 && next.value === "{" || next.type === 4 && next.value === "let" || next.type === 4 && next.value === "yield";
              }, Parser2.prototype.parseLexicalDeclaration = function(options) {
                var node = this.createNode(), kind = this.nextToken().value;
                assert_1.assert(kind === "let" || kind === "const", "Lexical declaration must be either let or const");
                var declarations = this.parseBindingList(kind, options);
                return this.consumeSemicolon(), this.finalize(node, new Node.VariableDeclaration(declarations, kind));
              }, Parser2.prototype.parseBindingRestElement = function(params, kind) {
                var node = this.createNode();
                this.expect("...");
                var arg = this.parsePattern(params, kind);
                return this.finalize(node, new Node.RestElement(arg));
              }, Parser2.prototype.parseArrayPattern = function(params, kind) {
                var node = this.createNode();
                this.expect("[");
                for (var elements = []; !this.match("]"); )
                  if (this.match(","))
                    this.nextToken(), elements.push(null);
                  else {
                    if (this.match("...")) {
                      elements.push(this.parseBindingRestElement(params, kind));
                      break;
                    } else
                      elements.push(this.parsePatternWithDefault(params, kind));
                    this.match("]") || this.expect(",");
                  }
                return this.expect("]"), this.finalize(node, new Node.ArrayPattern(elements));
              }, Parser2.prototype.parsePropertyPattern = function(params, kind) {
                var node = this.createNode(), computed = !1, shorthand = !1, method = !1, key, value;
                if (this.lookahead.type === 3) {
                  var keyToken = this.lookahead;
                  key = this.parseVariableIdentifier();
                  var init = this.finalize(node, new Node.Identifier(keyToken.value));
                  if (this.match("=")) {
                    params.push(keyToken), shorthand = !0, this.nextToken();
                    var expr = this.parseAssignmentExpression();
                    value = this.finalize(this.startNode(keyToken), new Node.AssignmentPattern(init, expr));
                  } else this.match(":") ? (this.expect(":"), value = this.parsePatternWithDefault(params, kind)) : (params.push(keyToken), shorthand = !0, value = init);
                } else
                  computed = this.match("["), key = this.parseObjectPropertyKey(), this.expect(":"), value = this.parsePatternWithDefault(params, kind);
                return this.finalize(node, new Node.Property("init", key, computed, value, method, shorthand));
              }, Parser2.prototype.parseObjectPattern = function(params, kind) {
                var node = this.createNode(), properties = [];
                for (this.expect("{"); !this.match("}"); )
                  properties.push(this.parsePropertyPattern(params, kind)), this.match("}") || this.expect(",");
                return this.expect("}"), this.finalize(node, new Node.ObjectPattern(properties));
              }, Parser2.prototype.parsePattern = function(params, kind) {
                var pattern;
                return this.match("[") ? pattern = this.parseArrayPattern(params, kind) : this.match("{") ? pattern = this.parseObjectPattern(params, kind) : (this.matchKeyword("let") && (kind === "const" || kind === "let") && this.tolerateUnexpectedToken(this.lookahead, messages_1.Messages.LetInLexicalBinding), params.push(this.lookahead), pattern = this.parseVariableIdentifier(kind)), pattern;
              }, Parser2.prototype.parsePatternWithDefault = function(params, kind) {
                var startToken = this.lookahead, pattern = this.parsePattern(params, kind);
                if (this.match("=")) {
                  this.nextToken();
                  var previousAllowYield = this.context.allowYield;
                  this.context.allowYield = !0;
                  var right = this.isolateCoverGrammar(this.parseAssignmentExpression);
                  this.context.allowYield = previousAllowYield, pattern = this.finalize(this.startNode(startToken), new Node.AssignmentPattern(pattern, right));
                }
                return pattern;
              }, Parser2.prototype.parseVariableIdentifier = function(kind) {
                var node = this.createNode(), token = this.nextToken();
                return token.type === 4 && token.value === "yield" ? this.context.strict ? this.tolerateUnexpectedToken(token, messages_1.Messages.StrictReservedWord) : this.context.allowYield || this.throwUnexpectedToken(token) : token.type !== 3 ? this.context.strict && token.type === 4 && this.scanner.isStrictModeReservedWord(token.value) ? this.tolerateUnexpectedToken(token, messages_1.Messages.StrictReservedWord) : (this.context.strict || token.value !== "let" || kind !== "var") && this.throwUnexpectedToken(token) : (this.context.isModule || this.context.await) && token.type === 3 && token.value === "await" && this.tolerateUnexpectedToken(token), this.finalize(node, new Node.Identifier(token.value));
              }, Parser2.prototype.parseVariableDeclaration = function(options) {
                var node = this.createNode(), params = [], id = this.parsePattern(params, "var");
                this.context.strict && id.type === syntax_1.Syntax.Identifier && this.scanner.isRestrictedWord(id.name) && this.tolerateError(messages_1.Messages.StrictVarName);
                var init = null;
                return this.match("=") ? (this.nextToken(), init = this.isolateCoverGrammar(this.parseAssignmentExpression)) : id.type !== syntax_1.Syntax.Identifier && !options.inFor && this.expect("="), this.finalize(node, new Node.VariableDeclarator(id, init));
              }, Parser2.prototype.parseVariableDeclarationList = function(options) {
                var opt = { inFor: options.inFor }, list = [];
                for (list.push(this.parseVariableDeclaration(opt)); this.match(","); )
                  this.nextToken(), list.push(this.parseVariableDeclaration(opt));
                return list;
              }, Parser2.prototype.parseVariableStatement = function() {
                var node = this.createNode();
                this.expectKeyword("var");
                var declarations = this.parseVariableDeclarationList({ inFor: !1 });
                return this.consumeSemicolon(), this.finalize(node, new Node.VariableDeclaration(declarations, "var"));
              }, Parser2.prototype.parseEmptyStatement = function() {
                var node = this.createNode();
                return this.expect(";"), this.finalize(node, new Node.EmptyStatement());
              }, Parser2.prototype.parseExpressionStatement = function() {
                var node = this.createNode(), expr = this.parseExpression();
                return this.consumeSemicolon(), this.finalize(node, new Node.ExpressionStatement(expr));
              }, Parser2.prototype.parseIfClause = function() {
                return this.context.strict && this.matchKeyword("function") && this.tolerateError(messages_1.Messages.StrictFunction), this.parseStatement();
              }, Parser2.prototype.parseIfStatement = function() {
                var node = this.createNode(), consequent, alternate = null;
                this.expectKeyword("if"), this.expect("(");
                var test = this.parseExpression();
                return !this.match(")") && this.config.tolerant ? (this.tolerateUnexpectedToken(this.nextToken()), consequent = this.finalize(this.createNode(), new Node.EmptyStatement())) : (this.expect(")"), consequent = this.parseIfClause(), this.matchKeyword("else") && (this.nextToken(), alternate = this.parseIfClause())), this.finalize(node, new Node.IfStatement(test, consequent, alternate));
              }, Parser2.prototype.parseDoWhileStatement = function() {
                var node = this.createNode();
                this.expectKeyword("do");
                var previousInIteration = this.context.inIteration;
                this.context.inIteration = !0;
                var body = this.parseStatement();
                this.context.inIteration = previousInIteration, this.expectKeyword("while"), this.expect("(");
                var test = this.parseExpression();
                return !this.match(")") && this.config.tolerant ? this.tolerateUnexpectedToken(this.nextToken()) : (this.expect(")"), this.match(";") && this.nextToken()), this.finalize(node, new Node.DoWhileStatement(body, test));
              }, Parser2.prototype.parseWhileStatement = function() {
                var node = this.createNode(), body;
                this.expectKeyword("while"), this.expect("(");
                var test = this.parseExpression();
                if (!this.match(")") && this.config.tolerant)
                  this.tolerateUnexpectedToken(this.nextToken()), body = this.finalize(this.createNode(), new Node.EmptyStatement());
                else {
                  this.expect(")");
                  var previousInIteration = this.context.inIteration;
                  this.context.inIteration = !0, body = this.parseStatement(), this.context.inIteration = previousInIteration;
                }
                return this.finalize(node, new Node.WhileStatement(test, body));
              }, Parser2.prototype.parseForStatement = function() {
                var init = null, test = null, update = null, forIn = !0, left, right, node = this.createNode();
                if (this.expectKeyword("for"), this.expect("("), this.match(";"))
                  this.nextToken();
                else if (this.matchKeyword("var")) {
                  init = this.createNode(), this.nextToken();
                  var previousAllowIn = this.context.allowIn;
                  this.context.allowIn = !1;
                  var declarations = this.parseVariableDeclarationList({ inFor: !0 });
                  if (this.context.allowIn = previousAllowIn, declarations.length === 1 && this.matchKeyword("in")) {
                    var decl = declarations[0];
                    decl.init && (decl.id.type === syntax_1.Syntax.ArrayPattern || decl.id.type === syntax_1.Syntax.ObjectPattern || this.context.strict) && this.tolerateError(messages_1.Messages.ForInOfLoopInitializer, "for-in"), init = this.finalize(init, new Node.VariableDeclaration(declarations, "var")), this.nextToken(), left = init, right = this.parseExpression(), init = null;
                  } else declarations.length === 1 && declarations[0].init === null && this.matchContextualKeyword("of") ? (init = this.finalize(init, new Node.VariableDeclaration(declarations, "var")), this.nextToken(), left = init, right = this.parseAssignmentExpression(), init = null, forIn = !1) : (init = this.finalize(init, new Node.VariableDeclaration(declarations, "var")), this.expect(";"));
                } else if (this.matchKeyword("const") || this.matchKeyword("let")) {
                  init = this.createNode();
                  var kind = this.nextToken().value;
                  if (!this.context.strict && this.lookahead.value === "in")
                    init = this.finalize(init, new Node.Identifier(kind)), this.nextToken(), left = init, right = this.parseExpression(), init = null;
                  else {
                    var previousAllowIn = this.context.allowIn;
                    this.context.allowIn = !1;
                    var declarations = this.parseBindingList(kind, { inFor: !0 });
                    this.context.allowIn = previousAllowIn, declarations.length === 1 && declarations[0].init === null && this.matchKeyword("in") ? (init = this.finalize(init, new Node.VariableDeclaration(declarations, kind)), this.nextToken(), left = init, right = this.parseExpression(), init = null) : declarations.length === 1 && declarations[0].init === null && this.matchContextualKeyword("of") ? (init = this.finalize(init, new Node.VariableDeclaration(declarations, kind)), this.nextToken(), left = init, right = this.parseAssignmentExpression(), init = null, forIn = !1) : (this.consumeSemicolon(), init = this.finalize(init, new Node.VariableDeclaration(declarations, kind)));
                  }
                } else {
                  var initStartToken = this.lookahead, previousAllowIn = this.context.allowIn;
                  if (this.context.allowIn = !1, init = this.inheritCoverGrammar(this.parseAssignmentExpression), this.context.allowIn = previousAllowIn, this.matchKeyword("in"))
                    (!this.context.isAssignmentTarget || init.type === syntax_1.Syntax.AssignmentExpression) && this.tolerateError(messages_1.Messages.InvalidLHSInForIn), this.nextToken(), this.reinterpretExpressionAsPattern(init), left = init, right = this.parseExpression(), init = null;
                  else if (this.matchContextualKeyword("of"))
                    (!this.context.isAssignmentTarget || init.type === syntax_1.Syntax.AssignmentExpression) && this.tolerateError(messages_1.Messages.InvalidLHSInForLoop), this.nextToken(), this.reinterpretExpressionAsPattern(init), left = init, right = this.parseAssignmentExpression(), init = null, forIn = !1;
                  else {
                    if (this.match(",")) {
                      for (var initSeq = [init]; this.match(","); )
                        this.nextToken(), initSeq.push(this.isolateCoverGrammar(this.parseAssignmentExpression));
                      init = this.finalize(this.startNode(initStartToken), new Node.SequenceExpression(initSeq));
                    }
                    this.expect(";");
                  }
                }
                typeof left > "u" && (this.match(";") || (test = this.parseExpression()), this.expect(";"), this.match(")") || (update = this.parseExpression()));
                var body;
                if (!this.match(")") && this.config.tolerant)
                  this.tolerateUnexpectedToken(this.nextToken()), body = this.finalize(this.createNode(), new Node.EmptyStatement());
                else {
                  this.expect(")");
                  var previousInIteration = this.context.inIteration;
                  this.context.inIteration = !0, body = this.isolateCoverGrammar(this.parseStatement), this.context.inIteration = previousInIteration;
                }
                return typeof left > "u" ? this.finalize(node, new Node.ForStatement(init, test, update, body)) : forIn ? this.finalize(node, new Node.ForInStatement(left, right, body)) : this.finalize(node, new Node.ForOfStatement(left, right, body));
              }, Parser2.prototype.parseContinueStatement = function() {
                var node = this.createNode();
                this.expectKeyword("continue");
                var label = null;
                if (this.lookahead.type === 3 && !this.hasLineTerminator) {
                  var id = this.parseVariableIdentifier();
                  label = id;
                  var key = "$" + id.name;
                  Object.prototype.hasOwnProperty.call(this.context.labelSet, key) || this.throwError(messages_1.Messages.UnknownLabel, id.name);
                }
                return this.consumeSemicolon(), label === null && !this.context.inIteration && this.throwError(messages_1.Messages.IllegalContinue), this.finalize(node, new Node.ContinueStatement(label));
              }, Parser2.prototype.parseBreakStatement = function() {
                var node = this.createNode();
                this.expectKeyword("break");
                var label = null;
                if (this.lookahead.type === 3 && !this.hasLineTerminator) {
                  var id = this.parseVariableIdentifier(), key = "$" + id.name;
                  Object.prototype.hasOwnProperty.call(this.context.labelSet, key) || this.throwError(messages_1.Messages.UnknownLabel, id.name), label = id;
                }
                return this.consumeSemicolon(), label === null && !this.context.inIteration && !this.context.inSwitch && this.throwError(messages_1.Messages.IllegalBreak), this.finalize(node, new Node.BreakStatement(label));
              }, Parser2.prototype.parseReturnStatement = function() {
                this.context.inFunctionBody || this.tolerateError(messages_1.Messages.IllegalReturn);
                var node = this.createNode();
                this.expectKeyword("return");
                var hasArgument = !this.match(";") && !this.match("}") && !this.hasLineTerminator && this.lookahead.type !== 2 || this.lookahead.type === 8 || this.lookahead.type === 10, argument = hasArgument ? this.parseExpression() : null;
                return this.consumeSemicolon(), this.finalize(node, new Node.ReturnStatement(argument));
              }, Parser2.prototype.parseWithStatement = function() {
                this.context.strict && this.tolerateError(messages_1.Messages.StrictModeWith);
                var node = this.createNode(), body;
                this.expectKeyword("with"), this.expect("(");
                var object = this.parseExpression();
                return !this.match(")") && this.config.tolerant ? (this.tolerateUnexpectedToken(this.nextToken()), body = this.finalize(this.createNode(), new Node.EmptyStatement())) : (this.expect(")"), body = this.parseStatement()), this.finalize(node, new Node.WithStatement(object, body));
              }, Parser2.prototype.parseSwitchCase = function() {
                var node = this.createNode(), test;
                this.matchKeyword("default") ? (this.nextToken(), test = null) : (this.expectKeyword("case"), test = this.parseExpression()), this.expect(":");
                for (var consequent = []; !(this.match("}") || this.matchKeyword("default") || this.matchKeyword("case")); )
                  consequent.push(this.parseStatementListItem());
                return this.finalize(node, new Node.SwitchCase(test, consequent));
              }, Parser2.prototype.parseSwitchStatement = function() {
                var node = this.createNode();
                this.expectKeyword("switch"), this.expect("(");
                var discriminant = this.parseExpression();
                this.expect(")");
                var previousInSwitch = this.context.inSwitch;
                this.context.inSwitch = !0;
                var cases = [], defaultFound = !1;
                for (this.expect("{"); !this.match("}"); ) {
                  var clause = this.parseSwitchCase();
                  clause.test === null && (defaultFound && this.throwError(messages_1.Messages.MultipleDefaultsInSwitch), defaultFound = !0), cases.push(clause);
                }
                return this.expect("}"), this.context.inSwitch = previousInSwitch, this.finalize(node, new Node.SwitchStatement(discriminant, cases));
              }, Parser2.prototype.parseLabelledStatement = function() {
                var node = this.createNode(), expr = this.parseExpression(), statement;
                if (expr.type === syntax_1.Syntax.Identifier && this.match(":")) {
                  this.nextToken();
                  var id = expr, key = "$" + id.name;
                  Object.prototype.hasOwnProperty.call(this.context.labelSet, key) && this.throwError(messages_1.Messages.Redeclaration, "Label", id.name), this.context.labelSet[key] = !0;
                  var body = void 0;
                  if (this.matchKeyword("class"))
                    this.tolerateUnexpectedToken(this.lookahead), body = this.parseClassDeclaration();
                  else if (this.matchKeyword("function")) {
                    var token = this.lookahead, declaration = this.parseFunctionDeclaration();
                    this.context.strict ? this.tolerateUnexpectedToken(token, messages_1.Messages.StrictFunction) : declaration.generator && this.tolerateUnexpectedToken(token, messages_1.Messages.GeneratorInLegacyContext), body = declaration;
                  } else
                    body = this.parseStatement();
                  delete this.context.labelSet[key], statement = new Node.LabeledStatement(id, body);
                } else
                  this.consumeSemicolon(), statement = new Node.ExpressionStatement(expr);
                return this.finalize(node, statement);
              }, Parser2.prototype.parseThrowStatement = function() {
                var node = this.createNode();
                this.expectKeyword("throw"), this.hasLineTerminator && this.throwError(messages_1.Messages.NewlineAfterThrow);
                var argument = this.parseExpression();
                return this.consumeSemicolon(), this.finalize(node, new Node.ThrowStatement(argument));
              }, Parser2.prototype.parseCatchClause = function() {
                var node = this.createNode();
                this.expectKeyword("catch"), this.expect("("), this.match(")") && this.throwUnexpectedToken(this.lookahead);
                for (var params = [], param = this.parsePattern(params), paramMap = {}, i = 0; i < params.length; i++) {
                  var key = "$" + params[i].value;
                  Object.prototype.hasOwnProperty.call(paramMap, key) && this.tolerateError(messages_1.Messages.DuplicateBinding, params[i].value), paramMap[key] = !0;
                }
                this.context.strict && param.type === syntax_1.Syntax.Identifier && this.scanner.isRestrictedWord(param.name) && this.tolerateError(messages_1.Messages.StrictCatchVariable), this.expect(")");
                var body = this.parseBlock();
                return this.finalize(node, new Node.CatchClause(param, body));
              }, Parser2.prototype.parseFinallyClause = function() {
                return this.expectKeyword("finally"), this.parseBlock();
              }, Parser2.prototype.parseTryStatement = function() {
                var node = this.createNode();
                this.expectKeyword("try");
                var block = this.parseBlock(), handler = this.matchKeyword("catch") ? this.parseCatchClause() : null, finalizer = this.matchKeyword("finally") ? this.parseFinallyClause() : null;
                return !handler && !finalizer && this.throwError(messages_1.Messages.NoCatchOrFinally), this.finalize(node, new Node.TryStatement(block, handler, finalizer));
              }, Parser2.prototype.parseDebuggerStatement = function() {
                var node = this.createNode();
                return this.expectKeyword("debugger"), this.consumeSemicolon(), this.finalize(node, new Node.DebuggerStatement());
              }, Parser2.prototype.parseStatement = function() {
                var statement;
                switch (this.lookahead.type) {
                  case 1:
                  case 5:
                  case 6:
                  case 8:
                  case 10:
                  case 9:
                    statement = this.parseExpressionStatement();
                    break;
                  case 7:
                    var value = this.lookahead.value;
                    value === "{" ? statement = this.parseBlock() : value === "(" ? statement = this.parseExpressionStatement() : value === ";" ? statement = this.parseEmptyStatement() : statement = this.parseExpressionStatement();
                    break;
                  case 3:
                    statement = this.matchAsyncFunction() ? this.parseFunctionDeclaration() : this.parseLabelledStatement();
                    break;
                  case 4:
                    switch (this.lookahead.value) {
                      case "break":
                        statement = this.parseBreakStatement();
                        break;
                      case "continue":
                        statement = this.parseContinueStatement();
                        break;
                      case "debugger":
                        statement = this.parseDebuggerStatement();
                        break;
                      case "do":
                        statement = this.parseDoWhileStatement();
                        break;
                      case "for":
                        statement = this.parseForStatement();
                        break;
                      case "function":
                        statement = this.parseFunctionDeclaration();
                        break;
                      case "if":
                        statement = this.parseIfStatement();
                        break;
                      case "return":
                        statement = this.parseReturnStatement();
                        break;
                      case "switch":
                        statement = this.parseSwitchStatement();
                        break;
                      case "throw":
                        statement = this.parseThrowStatement();
                        break;
                      case "try":
                        statement = this.parseTryStatement();
                        break;
                      case "var":
                        statement = this.parseVariableStatement();
                        break;
                      case "while":
                        statement = this.parseWhileStatement();
                        break;
                      case "with":
                        statement = this.parseWithStatement();
                        break;
                      default:
                        statement = this.parseExpressionStatement();
                        break;
                    }
                    break;
                  default:
                    statement = this.throwUnexpectedToken(this.lookahead);
                }
                return statement;
              }, Parser2.prototype.parseFunctionSourceElements = function() {
                var node = this.createNode();
                this.expect("{");
                var body = this.parseDirectivePrologues(), previousLabelSet = this.context.labelSet, previousInIteration = this.context.inIteration, previousInSwitch = this.context.inSwitch, previousInFunctionBody = this.context.inFunctionBody;
                for (this.context.labelSet = {}, this.context.inIteration = !1, this.context.inSwitch = !1, this.context.inFunctionBody = !0; this.lookahead.type !== 2 && !this.match("}"); )
                  body.push(this.parseStatementListItem());
                return this.expect("}"), this.context.labelSet = previousLabelSet, this.context.inIteration = previousInIteration, this.context.inSwitch = previousInSwitch, this.context.inFunctionBody = previousInFunctionBody, this.finalize(node, new Node.BlockStatement(body));
              }, Parser2.prototype.validateParam = function(options, param, name) {
                var key = "$" + name;
                this.context.strict ? (this.scanner.isRestrictedWord(name) && (options.stricted = param, options.message = messages_1.Messages.StrictParamName), Object.prototype.hasOwnProperty.call(options.paramSet, key) && (options.stricted = param, options.message = messages_1.Messages.StrictParamDupe)) : options.firstRestricted || (this.scanner.isRestrictedWord(name) ? (options.firstRestricted = param, options.message = messages_1.Messages.StrictParamName) : this.scanner.isStrictModeReservedWord(name) ? (options.firstRestricted = param, options.message = messages_1.Messages.StrictReservedWord) : Object.prototype.hasOwnProperty.call(options.paramSet, key) && (options.stricted = param, options.message = messages_1.Messages.StrictParamDupe)), typeof Object.defineProperty == "function" ? Object.defineProperty(options.paramSet, key, { value: !0, enumerable: !0, writable: !0, configurable: !0 }) : options.paramSet[key] = !0;
              }, Parser2.prototype.parseRestElement = function(params) {
                var node = this.createNode();
                this.expect("...");
                var arg = this.parsePattern(params);
                return this.match("=") && this.throwError(messages_1.Messages.DefaultRestParameter), this.match(")") || this.throwError(messages_1.Messages.ParameterAfterRestParameter), this.finalize(node, new Node.RestElement(arg));
              }, Parser2.prototype.parseFormalParameter = function(options) {
                for (var params = [], param = this.match("...") ? this.parseRestElement(params) : this.parsePatternWithDefault(params), i = 0; i < params.length; i++)
                  this.validateParam(options, params[i], params[i].value);
                options.simple = options.simple && param instanceof Node.Identifier, options.params.push(param);
              }, Parser2.prototype.parseFormalParameters = function(firstRestricted) {
                var options;
                if (options = {
                  simple: !0,
                  params: [],
                  firstRestricted
                }, this.expect("("), !this.match(")"))
                  for (options.paramSet = {}; this.lookahead.type !== 2 && (this.parseFormalParameter(options), !(this.match(")") || (this.expect(","), this.match(")")))); )
                    ;
                return this.expect(")"), {
                  simple: options.simple,
                  params: options.params,
                  stricted: options.stricted,
                  firstRestricted: options.firstRestricted,
                  message: options.message
                };
              }, Parser2.prototype.matchAsyncFunction = function() {
                var match = this.matchContextualKeyword("async");
                if (match) {
                  var state = this.scanner.saveState();
                  this.scanner.scanComments();
                  var next = this.scanner.lex();
                  this.scanner.restoreState(state), match = state.lineNumber === next.lineNumber && next.type === 4 && next.value === "function";
                }
                return match;
              }, Parser2.prototype.parseFunctionDeclaration = function(identifierIsOptional) {
                var node = this.createNode(), isAsync = this.matchContextualKeyword("async");
                isAsync && this.nextToken(), this.expectKeyword("function");
                var isGenerator = isAsync ? !1 : this.match("*");
                isGenerator && this.nextToken();
                var message, id = null, firstRestricted = null;
                if (!identifierIsOptional || !this.match("(")) {
                  var token = this.lookahead;
                  id = this.parseVariableIdentifier(), this.context.strict ? this.scanner.isRestrictedWord(token.value) && this.tolerateUnexpectedToken(token, messages_1.Messages.StrictFunctionName) : this.scanner.isRestrictedWord(token.value) ? (firstRestricted = token, message = messages_1.Messages.StrictFunctionName) : this.scanner.isStrictModeReservedWord(token.value) && (firstRestricted = token, message = messages_1.Messages.StrictReservedWord);
                }
                var previousAllowAwait = this.context.await, previousAllowYield = this.context.allowYield;
                this.context.await = isAsync, this.context.allowYield = !isGenerator;
                var formalParameters = this.parseFormalParameters(firstRestricted), params = formalParameters.params, stricted = formalParameters.stricted;
                firstRestricted = formalParameters.firstRestricted, formalParameters.message && (message = formalParameters.message);
                var previousStrict = this.context.strict, previousAllowStrictDirective = this.context.allowStrictDirective;
                this.context.allowStrictDirective = formalParameters.simple;
                var body = this.parseFunctionSourceElements();
                return this.context.strict && firstRestricted && this.throwUnexpectedToken(firstRestricted, message), this.context.strict && stricted && this.tolerateUnexpectedToken(stricted, message), this.context.strict = previousStrict, this.context.allowStrictDirective = previousAllowStrictDirective, this.context.await = previousAllowAwait, this.context.allowYield = previousAllowYield, isAsync ? this.finalize(node, new Node.AsyncFunctionDeclaration(id, params, body)) : this.finalize(node, new Node.FunctionDeclaration(id, params, body, isGenerator));
              }, Parser2.prototype.parseFunctionExpression = function() {
                var node = this.createNode(), isAsync = this.matchContextualKeyword("async");
                isAsync && this.nextToken(), this.expectKeyword("function");
                var isGenerator = isAsync ? !1 : this.match("*");
                isGenerator && this.nextToken();
                var message, id = null, firstRestricted, previousAllowAwait = this.context.await, previousAllowYield = this.context.allowYield;
                if (this.context.await = isAsync, this.context.allowYield = !isGenerator, !this.match("(")) {
                  var token = this.lookahead;
                  id = !this.context.strict && !isGenerator && this.matchKeyword("yield") ? this.parseIdentifierName() : this.parseVariableIdentifier(), this.context.strict ? this.scanner.isRestrictedWord(token.value) && this.tolerateUnexpectedToken(token, messages_1.Messages.StrictFunctionName) : this.scanner.isRestrictedWord(token.value) ? (firstRestricted = token, message = messages_1.Messages.StrictFunctionName) : this.scanner.isStrictModeReservedWord(token.value) && (firstRestricted = token, message = messages_1.Messages.StrictReservedWord);
                }
                var formalParameters = this.parseFormalParameters(firstRestricted), params = formalParameters.params, stricted = formalParameters.stricted;
                firstRestricted = formalParameters.firstRestricted, formalParameters.message && (message = formalParameters.message);
                var previousStrict = this.context.strict, previousAllowStrictDirective = this.context.allowStrictDirective;
                this.context.allowStrictDirective = formalParameters.simple;
                var body = this.parseFunctionSourceElements();
                return this.context.strict && firstRestricted && this.throwUnexpectedToken(firstRestricted, message), this.context.strict && stricted && this.tolerateUnexpectedToken(stricted, message), this.context.strict = previousStrict, this.context.allowStrictDirective = previousAllowStrictDirective, this.context.await = previousAllowAwait, this.context.allowYield = previousAllowYield, isAsync ? this.finalize(node, new Node.AsyncFunctionExpression(id, params, body)) : this.finalize(node, new Node.FunctionExpression(id, params, body, isGenerator));
              }, Parser2.prototype.parseDirective = function() {
                var token = this.lookahead, node = this.createNode(), expr = this.parseExpression(), directive = expr.type === syntax_1.Syntax.Literal ? this.getTokenRaw(token).slice(1, -1) : null;
                return this.consumeSemicolon(), this.finalize(node, directive ? new Node.Directive(expr, directive) : new Node.ExpressionStatement(expr));
              }, Parser2.prototype.parseDirectivePrologues = function() {
                for (var firstRestricted = null, body = []; ; ) {
                  var token = this.lookahead;
                  if (token.type !== 8)
                    break;
                  var statement = this.parseDirective();
                  body.push(statement);
                  var directive = statement.directive;
                  if (typeof directive != "string")
                    break;
                  directive === "use strict" ? (this.context.strict = !0, firstRestricted && this.tolerateUnexpectedToken(firstRestricted, messages_1.Messages.StrictOctalLiteral), this.context.allowStrictDirective || this.tolerateUnexpectedToken(token, messages_1.Messages.IllegalLanguageModeDirective)) : !firstRestricted && token.octal && (firstRestricted = token);
                }
                return body;
              }, Parser2.prototype.qualifiedPropertyName = function(token) {
                switch (token.type) {
                  case 3:
                  case 8:
                  case 1:
                  case 5:
                  case 6:
                  case 4:
                    return !0;
                  case 7:
                    return token.value === "[";
                  default:
                    break;
                }
                return !1;
              }, Parser2.prototype.parseGetterMethod = function() {
                var node = this.createNode(), isGenerator = !1, previousAllowYield = this.context.allowYield;
                this.context.allowYield = !isGenerator;
                var formalParameters = this.parseFormalParameters();
                formalParameters.params.length > 0 && this.tolerateError(messages_1.Messages.BadGetterArity);
                var method = this.parsePropertyMethod(formalParameters);
                return this.context.allowYield = previousAllowYield, this.finalize(node, new Node.FunctionExpression(null, formalParameters.params, method, isGenerator));
              }, Parser2.prototype.parseSetterMethod = function() {
                var node = this.createNode(), isGenerator = !1, previousAllowYield = this.context.allowYield;
                this.context.allowYield = !isGenerator;
                var formalParameters = this.parseFormalParameters();
                formalParameters.params.length !== 1 ? this.tolerateError(messages_1.Messages.BadSetterArity) : formalParameters.params[0] instanceof Node.RestElement && this.tolerateError(messages_1.Messages.BadSetterRestParameter);
                var method = this.parsePropertyMethod(formalParameters);
                return this.context.allowYield = previousAllowYield, this.finalize(node, new Node.FunctionExpression(null, formalParameters.params, method, isGenerator));
              }, Parser2.prototype.parseGeneratorMethod = function() {
                var node = this.createNode(), isGenerator = !0, previousAllowYield = this.context.allowYield;
                this.context.allowYield = !0;
                var params = this.parseFormalParameters();
                this.context.allowYield = !1;
                var method = this.parsePropertyMethod(params);
                return this.context.allowYield = previousAllowYield, this.finalize(node, new Node.FunctionExpression(null, params.params, method, isGenerator));
              }, Parser2.prototype.isStartOfExpression = function() {
                var start = !0, value = this.lookahead.value;
                switch (this.lookahead.type) {
                  case 7:
                    start = value === "[" || value === "(" || value === "{" || value === "+" || value === "-" || value === "!" || value === "~" || value === "++" || value === "--" || value === "/" || value === "/=";
                    break;
                  case 4:
                    start = value === "class" || value === "delete" || value === "function" || value === "let" || value === "new" || value === "super" || value === "this" || value === "typeof" || value === "void" || value === "yield";
                    break;
                  default:
                    break;
                }
                return start;
              }, Parser2.prototype.parseYieldExpression = function() {
                var node = this.createNode();
                this.expectKeyword("yield");
                var argument = null, delegate = !1;
                if (!this.hasLineTerminator) {
                  var previousAllowYield = this.context.allowYield;
                  this.context.allowYield = !1, delegate = this.match("*"), delegate ? (this.nextToken(), argument = this.parseAssignmentExpression()) : this.isStartOfExpression() && (argument = this.parseAssignmentExpression()), this.context.allowYield = previousAllowYield;
                }
                return this.finalize(node, new Node.YieldExpression(argument, delegate));
              }, Parser2.prototype.parseClassElement = function(hasConstructor) {
                var token = this.lookahead, node = this.createNode(), kind = "", key = null, value = null, computed = !1, method = !1, isStatic = !1, isAsync = !1;
                if (this.match("*"))
                  this.nextToken();
                else {
                  computed = this.match("["), key = this.parseObjectPropertyKey();
                  var id = key;
                  if (id.name === "static" && (this.qualifiedPropertyName(this.lookahead) || this.match("*")) && (token = this.lookahead, isStatic = !0, computed = this.match("["), this.match("*") ? this.nextToken() : key = this.parseObjectPropertyKey()), token.type === 3 && !this.hasLineTerminator && token.value === "async") {
                    var punctuator = this.lookahead.value;
                    punctuator !== ":" && punctuator !== "(" && punctuator !== "*" && (isAsync = !0, token = this.lookahead, key = this.parseObjectPropertyKey(), token.type === 3 && token.value === "constructor" && this.tolerateUnexpectedToken(token, messages_1.Messages.ConstructorIsAsync));
                  }
                }
                var lookaheadPropertyKey = this.qualifiedPropertyName(this.lookahead);
                return token.type === 3 ? token.value === "get" && lookaheadPropertyKey ? (kind = "get", computed = this.match("["), key = this.parseObjectPropertyKey(), this.context.allowYield = !1, value = this.parseGetterMethod()) : token.value === "set" && lookaheadPropertyKey && (kind = "set", computed = this.match("["), key = this.parseObjectPropertyKey(), value = this.parseSetterMethod()) : token.type === 7 && token.value === "*" && lookaheadPropertyKey && (kind = "init", computed = this.match("["), key = this.parseObjectPropertyKey(), value = this.parseGeneratorMethod(), method = !0), !kind && key && this.match("(") && (kind = "init", value = isAsync ? this.parsePropertyMethodAsyncFunction() : this.parsePropertyMethodFunction(), method = !0), kind || this.throwUnexpectedToken(this.lookahead), kind === "init" && (kind = "method"), computed || (isStatic && this.isPropertyKey(key, "prototype") && this.throwUnexpectedToken(token, messages_1.Messages.StaticPrototype), !isStatic && this.isPropertyKey(key, "constructor") && ((kind !== "method" || !method || value && value.generator) && this.throwUnexpectedToken(token, messages_1.Messages.ConstructorSpecialMethod), hasConstructor.value ? this.throwUnexpectedToken(token, messages_1.Messages.DuplicateConstructor) : hasConstructor.value = !0, kind = "constructor")), this.finalize(node, new Node.MethodDefinition(key, computed, value, kind, isStatic));
              }, Parser2.prototype.parseClassElementList = function() {
                var body = [], hasConstructor = { value: !1 };
                for (this.expect("{"); !this.match("}"); )
                  this.match(";") ? this.nextToken() : body.push(this.parseClassElement(hasConstructor));
                return this.expect("}"), body;
              }, Parser2.prototype.parseClassBody = function() {
                var node = this.createNode(), elementList = this.parseClassElementList();
                return this.finalize(node, new Node.ClassBody(elementList));
              }, Parser2.prototype.parseClassDeclaration = function(identifierIsOptional) {
                var node = this.createNode(), previousStrict = this.context.strict;
                this.context.strict = !0, this.expectKeyword("class");
                var id = identifierIsOptional && this.lookahead.type !== 3 ? null : this.parseVariableIdentifier(), superClass = null;
                this.matchKeyword("extends") && (this.nextToken(), superClass = this.isolateCoverGrammar(this.parseLeftHandSideExpressionAllowCall));
                var classBody = this.parseClassBody();
                return this.context.strict = previousStrict, this.finalize(node, new Node.ClassDeclaration(id, superClass, classBody));
              }, Parser2.prototype.parseClassExpression = function() {
                var node = this.createNode(), previousStrict = this.context.strict;
                this.context.strict = !0, this.expectKeyword("class");
                var id = this.lookahead.type === 3 ? this.parseVariableIdentifier() : null, superClass = null;
                this.matchKeyword("extends") && (this.nextToken(), superClass = this.isolateCoverGrammar(this.parseLeftHandSideExpressionAllowCall));
                var classBody = this.parseClassBody();
                return this.context.strict = previousStrict, this.finalize(node, new Node.ClassExpression(id, superClass, classBody));
              }, Parser2.prototype.parseModule = function() {
                this.context.strict = !0, this.context.isModule = !0, this.scanner.isModule = !0;
                for (var node = this.createNode(), body = this.parseDirectivePrologues(); this.lookahead.type !== 2; )
                  body.push(this.parseStatementListItem());
                return this.finalize(node, new Node.Module(body));
              }, Parser2.prototype.parseScript = function() {
                for (var node = this.createNode(), body = this.parseDirectivePrologues(); this.lookahead.type !== 2; )
                  body.push(this.parseStatementListItem());
                return this.finalize(node, new Node.Script(body));
              }, Parser2.prototype.parseModuleSpecifier = function() {
                var node = this.createNode();
                this.lookahead.type !== 8 && this.throwError(messages_1.Messages.InvalidModuleSpecifier);
                var token = this.nextToken(), raw = this.getTokenRaw(token);
                return this.finalize(node, new Node.Literal(token.value, raw));
              }, Parser2.prototype.parseImportSpecifier = function() {
                var node = this.createNode(), imported, local;
                return this.lookahead.type === 3 ? (imported = this.parseVariableIdentifier(), local = imported, this.matchContextualKeyword("as") && (this.nextToken(), local = this.parseVariableIdentifier())) : (imported = this.parseIdentifierName(), local = imported, this.matchContextualKeyword("as") ? (this.nextToken(), local = this.parseVariableIdentifier()) : this.throwUnexpectedToken(this.nextToken())), this.finalize(node, new Node.ImportSpecifier(local, imported));
              }, Parser2.prototype.parseNamedImports = function() {
                this.expect("{");
                for (var specifiers = []; !this.match("}"); )
                  specifiers.push(this.parseImportSpecifier()), this.match("}") || this.expect(",");
                return this.expect("}"), specifiers;
              }, Parser2.prototype.parseImportDefaultSpecifier = function() {
                var node = this.createNode(), local = this.parseIdentifierName();
                return this.finalize(node, new Node.ImportDefaultSpecifier(local));
              }, Parser2.prototype.parseImportNamespaceSpecifier = function() {
                var node = this.createNode();
                this.expect("*"), this.matchContextualKeyword("as") || this.throwError(messages_1.Messages.NoAsAfterImportNamespace), this.nextToken();
                var local = this.parseIdentifierName();
                return this.finalize(node, new Node.ImportNamespaceSpecifier(local));
              }, Parser2.prototype.parseImportDeclaration = function() {
                this.context.inFunctionBody && this.throwError(messages_1.Messages.IllegalImportDeclaration);
                var node = this.createNode();
                this.expectKeyword("import");
                var src, specifiers = [];
                if (this.lookahead.type === 8)
                  src = this.parseModuleSpecifier();
                else {
                  if (this.match("{") ? specifiers = specifiers.concat(this.parseNamedImports()) : this.match("*") ? specifiers.push(this.parseImportNamespaceSpecifier()) : this.isIdentifierName(this.lookahead) && !this.matchKeyword("default") ? (specifiers.push(this.parseImportDefaultSpecifier()), this.match(",") && (this.nextToken(), this.match("*") ? specifiers.push(this.parseImportNamespaceSpecifier()) : this.match("{") ? specifiers = specifiers.concat(this.parseNamedImports()) : this.throwUnexpectedToken(this.lookahead))) : this.throwUnexpectedToken(this.nextToken()), !this.matchContextualKeyword("from")) {
                    var message = this.lookahead.value ? messages_1.Messages.UnexpectedToken : messages_1.Messages.MissingFromClause;
                    this.throwError(message, this.lookahead.value);
                  }
                  this.nextToken(), src = this.parseModuleSpecifier();
                }
                return this.consumeSemicolon(), this.finalize(node, new Node.ImportDeclaration(specifiers, src));
              }, Parser2.prototype.parseExportSpecifier = function() {
                var node = this.createNode(), local = this.parseIdentifierName(), exported = local;
                return this.matchContextualKeyword("as") && (this.nextToken(), exported = this.parseIdentifierName()), this.finalize(node, new Node.ExportSpecifier(local, exported));
              }, Parser2.prototype.parseExportDeclaration = function() {
                this.context.inFunctionBody && this.throwError(messages_1.Messages.IllegalExportDeclaration);
                var node = this.createNode();
                this.expectKeyword("export");
                var exportDeclaration;
                if (this.matchKeyword("default"))
                  if (this.nextToken(), this.matchKeyword("function")) {
                    var declaration = this.parseFunctionDeclaration(!0);
                    exportDeclaration = this.finalize(node, new Node.ExportDefaultDeclaration(declaration));
                  } else if (this.matchKeyword("class")) {
                    var declaration = this.parseClassDeclaration(!0);
                    exportDeclaration = this.finalize(node, new Node.ExportDefaultDeclaration(declaration));
                  } else if (this.matchContextualKeyword("async")) {
                    var declaration = this.matchAsyncFunction() ? this.parseFunctionDeclaration(!0) : this.parseAssignmentExpression();
                    exportDeclaration = this.finalize(node, new Node.ExportDefaultDeclaration(declaration));
                  } else {
                    this.matchContextualKeyword("from") && this.throwError(messages_1.Messages.UnexpectedToken, this.lookahead.value);
                    var declaration = this.match("{") ? this.parseObjectInitializer() : this.match("[") ? this.parseArrayInitializer() : this.parseAssignmentExpression();
                    this.consumeSemicolon(), exportDeclaration = this.finalize(node, new Node.ExportDefaultDeclaration(declaration));
                  }
                else if (this.match("*")) {
                  if (this.nextToken(), !this.matchContextualKeyword("from")) {
                    var message = this.lookahead.value ? messages_1.Messages.UnexpectedToken : messages_1.Messages.MissingFromClause;
                    this.throwError(message, this.lookahead.value);
                  }
                  this.nextToken();
                  var src = this.parseModuleSpecifier();
                  this.consumeSemicolon(), exportDeclaration = this.finalize(node, new Node.ExportAllDeclaration(src));
                } else if (this.lookahead.type === 4) {
                  var declaration = void 0;
                  switch (this.lookahead.value) {
                    case "let":
                    case "const":
                      declaration = this.parseLexicalDeclaration({ inFor: !1 });
                      break;
                    case "var":
                    case "class":
                    case "function":
                      declaration = this.parseStatementListItem();
                      break;
                    default:
                      this.throwUnexpectedToken(this.lookahead);
                  }
                  exportDeclaration = this.finalize(node, new Node.ExportNamedDeclaration(declaration, [], null));
                } else if (this.matchAsyncFunction()) {
                  var declaration = this.parseFunctionDeclaration();
                  exportDeclaration = this.finalize(node, new Node.ExportNamedDeclaration(declaration, [], null));
                } else {
                  var specifiers = [], source = null, isExportFromIdentifier = !1;
                  for (this.expect("{"); !this.match("}"); )
                    isExportFromIdentifier = isExportFromIdentifier || this.matchKeyword("default"), specifiers.push(this.parseExportSpecifier()), this.match("}") || this.expect(",");
                  if (this.expect("}"), this.matchContextualKeyword("from"))
                    this.nextToken(), source = this.parseModuleSpecifier(), this.consumeSemicolon();
                  else if (isExportFromIdentifier) {
                    var message = this.lookahead.value ? messages_1.Messages.UnexpectedToken : messages_1.Messages.MissingFromClause;
                    this.throwError(message, this.lookahead.value);
                  } else
                    this.consumeSemicolon();
                  exportDeclaration = this.finalize(node, new Node.ExportNamedDeclaration(null, specifiers, source));
                }
                return exportDeclaration;
              }, Parser2;
            })();
            exports2.Parser = Parser;
          },
          /* 9 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            function assert(condition, message) {
              if (!condition)
                throw new Error("ASSERT: " + message);
            }
            exports2.assert = assert;
          },
          /* 10 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var ErrorHandler = (function() {
              function ErrorHandler2() {
                this.errors = [], this.tolerant = !1;
              }
              return ErrorHandler2.prototype.recordError = function(error) {
                this.errors.push(error);
              }, ErrorHandler2.prototype.tolerate = function(error) {
                if (this.tolerant)
                  this.recordError(error);
                else
                  throw error;
              }, ErrorHandler2.prototype.constructError = function(msg, column) {
                var error = new Error(msg);
                try {
                  throw error;
                } catch (base) {
                  Object.create && Object.defineProperty && (error = Object.create(base), Object.defineProperty(error, "column", { value: column }));
                }
                return error;
              }, ErrorHandler2.prototype.createError = function(index, line, col, description) {
                var msg = "Line " + line + ": " + description, error = this.constructError(msg, col);
                return error.index = index, error.lineNumber = line, error.description = description, error;
              }, ErrorHandler2.prototype.throwError = function(index, line, col, description) {
                throw this.createError(index, line, col, description);
              }, ErrorHandler2.prototype.tolerateError = function(index, line, col, description) {
                var error = this.createError(index, line, col, description);
                if (this.tolerant)
                  this.recordError(error);
                else
                  throw error;
              }, ErrorHandler2;
            })();
            exports2.ErrorHandler = ErrorHandler;
          },
          /* 11 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 }), exports2.Messages = {
              BadGetterArity: "Getter must not have any formal parameters",
              BadSetterArity: "Setter must have exactly one formal parameter",
              BadSetterRestParameter: "Setter function argument must not be a rest parameter",
              ConstructorIsAsync: "Class constructor may not be an async method",
              ConstructorSpecialMethod: "Class constructor may not be an accessor",
              DeclarationMissingInitializer: "Missing initializer in %0 declaration",
              DefaultRestParameter: "Unexpected token =",
              DuplicateBinding: "Duplicate binding %0",
              DuplicateConstructor: "A class may only have one constructor",
              DuplicateProtoProperty: "Duplicate __proto__ fields are not allowed in object literals",
              ForInOfLoopInitializer: "%0 loop variable declaration may not have an initializer",
              GeneratorInLegacyContext: "Generator declarations are not allowed in legacy contexts",
              IllegalBreak: "Illegal break statement",
              IllegalContinue: "Illegal continue statement",
              IllegalExportDeclaration: "Unexpected token",
              IllegalImportDeclaration: "Unexpected token",
              IllegalLanguageModeDirective: "Illegal 'use strict' directive in function with non-simple parameter list",
              IllegalReturn: "Illegal return statement",
              InvalidEscapedReservedWord: "Keyword must not contain escaped characters",
              InvalidHexEscapeSequence: "Invalid hexadecimal escape sequence",
              InvalidLHSInAssignment: "Invalid left-hand side in assignment",
              InvalidLHSInForIn: "Invalid left-hand side in for-in",
              InvalidLHSInForLoop: "Invalid left-hand side in for-loop",
              InvalidModuleSpecifier: "Unexpected token",
              InvalidRegExp: "Invalid regular expression",
              LetInLexicalBinding: "let is disallowed as a lexically bound name",
              MissingFromClause: "Unexpected token",
              MultipleDefaultsInSwitch: "More than one default clause in switch statement",
              NewlineAfterThrow: "Illegal newline after throw",
              NoAsAfterImportNamespace: "Unexpected token",
              NoCatchOrFinally: "Missing catch or finally after try",
              ParameterAfterRestParameter: "Rest parameter must be last formal parameter",
              Redeclaration: "%0 '%1' has already been declared",
              StaticPrototype: "Classes may not have static property named prototype",
              StrictCatchVariable: "Catch variable may not be eval or arguments in strict mode",
              StrictDelete: "Delete of an unqualified identifier in strict mode.",
              StrictFunction: "In strict mode code, functions can only be declared at top level or inside a block",
              StrictFunctionName: "Function name may not be eval or arguments in strict mode",
              StrictLHSAssignment: "Assignment to eval or arguments is not allowed in strict mode",
              StrictLHSPostfix: "Postfix increment/decrement may not have eval or arguments operand in strict mode",
              StrictLHSPrefix: "Prefix increment/decrement may not have eval or arguments operand in strict mode",
              StrictModeWith: "Strict mode code may not include a with statement",
              StrictOctalLiteral: "Octal literals are not allowed in strict mode.",
              StrictParamDupe: "Strict mode function may not have duplicate parameter names",
              StrictParamName: "Parameter name eval or arguments is not allowed in strict mode",
              StrictReservedWord: "Use of future reserved word in strict mode",
              StrictVarName: "Variable name may not be eval or arguments in strict mode",
              TemplateOctalLiteral: "Octal literals are not allowed in template strings.",
              UnexpectedEOS: "Unexpected end of input",
              UnexpectedIdentifier: "Unexpected identifier",
              UnexpectedNumber: "Unexpected number",
              UnexpectedReserved: "Unexpected reserved word",
              UnexpectedString: "Unexpected string",
              UnexpectedTemplate: "Unexpected quasi %0",
              UnexpectedToken: "Unexpected token %0",
              UnexpectedTokenIllegal: "Unexpected token ILLEGAL",
              UnknownLabel: "Undefined label '%0'",
              UnterminatedRegExp: "Invalid regular expression: missing /"
            };
          },
          /* 12 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var assert_1 = __webpack_require__(9), character_1 = __webpack_require__(4), messages_1 = __webpack_require__(11);
            function hexValue(ch) {
              return "0123456789abcdef".indexOf(ch.toLowerCase());
            }
            function octalValue(ch) {
              return "01234567".indexOf(ch);
            }
            var Scanner = (function() {
              function Scanner2(code, handler) {
                this.source = code, this.errorHandler = handler, this.trackComment = !1, this.isModule = !1, this.length = code.length, this.index = 0, this.lineNumber = code.length > 0 ? 1 : 0, this.lineStart = 0, this.curlyStack = [];
              }
              return Scanner2.prototype.saveState = function() {
                return {
                  index: this.index,
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart
                };
              }, Scanner2.prototype.restoreState = function(state) {
                this.index = state.index, this.lineNumber = state.lineNumber, this.lineStart = state.lineStart;
              }, Scanner2.prototype.eof = function() {
                return this.index >= this.length;
              }, Scanner2.prototype.throwUnexpectedToken = function(message) {
                return message === void 0 && (message = messages_1.Messages.UnexpectedTokenIllegal), this.errorHandler.throwError(this.index, this.lineNumber, this.index - this.lineStart + 1, message);
              }, Scanner2.prototype.tolerateUnexpectedToken = function(message) {
                message === void 0 && (message = messages_1.Messages.UnexpectedTokenIllegal), this.errorHandler.tolerateError(this.index, this.lineNumber, this.index - this.lineStart + 1, message);
              }, Scanner2.prototype.skipSingleLineComment = function(offset) {
                var comments = [], start, loc;
                for (this.trackComment && (comments = [], start = this.index - offset, loc = {
                  start: {
                    line: this.lineNumber,
                    column: this.index - this.lineStart - offset
                  },
                  end: {}
                }); !this.eof(); ) {
                  var ch = this.source.charCodeAt(this.index);
                  if (++this.index, character_1.Character.isLineTerminator(ch)) {
                    if (this.trackComment) {
                      loc.end = {
                        line: this.lineNumber,
                        column: this.index - this.lineStart - 1
                      };
                      var entry = {
                        multiLine: !1,
                        slice: [start + offset, this.index - 1],
                        range: [start, this.index - 1],
                        loc
                      };
                      comments.push(entry);
                    }
                    return ch === 13 && this.source.charCodeAt(this.index) === 10 && ++this.index, ++this.lineNumber, this.lineStart = this.index, comments;
                  }
                }
                if (this.trackComment) {
                  loc.end = {
                    line: this.lineNumber,
                    column: this.index - this.lineStart
                  };
                  var entry = {
                    multiLine: !1,
                    slice: [start + offset, this.index],
                    range: [start, this.index],
                    loc
                  };
                  comments.push(entry);
                }
                return comments;
              }, Scanner2.prototype.skipMultiLineComment = function() {
                var comments = [], start, loc;
                for (this.trackComment && (comments = [], start = this.index - 2, loc = {
                  start: {
                    line: this.lineNumber,
                    column: this.index - this.lineStart - 2
                  },
                  end: {}
                }); !this.eof(); ) {
                  var ch = this.source.charCodeAt(this.index);
                  if (character_1.Character.isLineTerminator(ch))
                    ch === 13 && this.source.charCodeAt(this.index + 1) === 10 && ++this.index, ++this.lineNumber, ++this.index, this.lineStart = this.index;
                  else if (ch === 42) {
                    if (this.source.charCodeAt(this.index + 1) === 47) {
                      if (this.index += 2, this.trackComment) {
                        loc.end = {
                          line: this.lineNumber,
                          column: this.index - this.lineStart
                        };
                        var entry = {
                          multiLine: !0,
                          slice: [start + 2, this.index - 2],
                          range: [start, this.index],
                          loc
                        };
                        comments.push(entry);
                      }
                      return comments;
                    }
                    ++this.index;
                  } else
                    ++this.index;
                }
                if (this.trackComment) {
                  loc.end = {
                    line: this.lineNumber,
                    column: this.index - this.lineStart
                  };
                  var entry = {
                    multiLine: !0,
                    slice: [start + 2, this.index],
                    range: [start, this.index],
                    loc
                  };
                  comments.push(entry);
                }
                return this.tolerateUnexpectedToken(), comments;
              }, Scanner2.prototype.scanComments = function() {
                var comments;
                this.trackComment && (comments = []);
                for (var start = this.index === 0; !this.eof(); ) {
                  var ch = this.source.charCodeAt(this.index);
                  if (character_1.Character.isWhiteSpace(ch))
                    ++this.index;
                  else if (character_1.Character.isLineTerminator(ch))
                    ++this.index, ch === 13 && this.source.charCodeAt(this.index) === 10 && ++this.index, ++this.lineNumber, this.lineStart = this.index, start = !0;
                  else if (ch === 47)
                    if (ch = this.source.charCodeAt(this.index + 1), ch === 47) {
                      this.index += 2;
                      var comment = this.skipSingleLineComment(2);
                      this.trackComment && (comments = comments.concat(comment)), start = !0;
                    } else if (ch === 42) {
                      this.index += 2;
                      var comment = this.skipMultiLineComment();
                      this.trackComment && (comments = comments.concat(comment));
                    } else
                      break;
                  else if (start && ch === 45)
                    if (this.source.charCodeAt(this.index + 1) === 45 && this.source.charCodeAt(this.index + 2) === 62) {
                      this.index += 3;
                      var comment = this.skipSingleLineComment(3);
                      this.trackComment && (comments = comments.concat(comment));
                    } else
                      break;
                  else if (ch === 60 && !this.isModule)
                    if (this.source.slice(this.index + 1, this.index + 4) === "!--") {
                      this.index += 4;
                      var comment = this.skipSingleLineComment(4);
                      this.trackComment && (comments = comments.concat(comment));
                    } else
                      break;
                  else
                    break;
                }
                return comments;
              }, Scanner2.prototype.isFutureReservedWord = function(id) {
                switch (id) {
                  case "enum":
                  case "export":
                  case "import":
                  case "super":
                    return !0;
                  default:
                    return !1;
                }
              }, Scanner2.prototype.isStrictModeReservedWord = function(id) {
                switch (id) {
                  case "implements":
                  case "interface":
                  case "package":
                  case "private":
                  case "protected":
                  case "public":
                  case "static":
                  case "yield":
                  case "let":
                    return !0;
                  default:
                    return !1;
                }
              }, Scanner2.prototype.isRestrictedWord = function(id) {
                return id === "eval" || id === "arguments";
              }, Scanner2.prototype.isKeyword = function(id) {
                switch (id.length) {
                  case 2:
                    return id === "if" || id === "in" || id === "do";
                  case 3:
                    return id === "var" || id === "for" || id === "new" || id === "try" || id === "let";
                  case 4:
                    return id === "this" || id === "else" || id === "case" || id === "void" || id === "with" || id === "enum";
                  case 5:
                    return id === "while" || id === "break" || id === "catch" || id === "throw" || id === "const" || id === "yield" || id === "class" || id === "super";
                  case 6:
                    return id === "return" || id === "typeof" || id === "delete" || id === "switch" || id === "export" || id === "import";
                  case 7:
                    return id === "default" || id === "finally" || id === "extends";
                  case 8:
                    return id === "function" || id === "continue" || id === "debugger";
                  case 10:
                    return id === "instanceof";
                  default:
                    return !1;
                }
              }, Scanner2.prototype.codePointAt = function(i) {
                var cp = this.source.charCodeAt(i);
                if (cp >= 55296 && cp <= 56319) {
                  var second = this.source.charCodeAt(i + 1);
                  if (second >= 56320 && second <= 57343) {
                    var first2 = cp;
                    cp = (first2 - 55296) * 1024 + second - 56320 + 65536;
                  }
                }
                return cp;
              }, Scanner2.prototype.scanHexEscape = function(prefix) {
                for (var len = prefix === "u" ? 4 : 2, code = 0, i = 0; i < len; ++i)
                  if (!this.eof() && character_1.Character.isHexDigit(this.source.charCodeAt(this.index)))
                    code = code * 16 + hexValue(this.source[this.index++]);
                  else
                    return null;
                return String.fromCharCode(code);
              }, Scanner2.prototype.scanUnicodeCodePointEscape = function() {
                var ch = this.source[this.index], code = 0;
                for (ch === "}" && this.throwUnexpectedToken(); !this.eof() && (ch = this.source[this.index++], !!character_1.Character.isHexDigit(ch.charCodeAt(0))); )
                  code = code * 16 + hexValue(ch);
                return (code > 1114111 || ch !== "}") && this.throwUnexpectedToken(), character_1.Character.fromCodePoint(code);
              }, Scanner2.prototype.getIdentifier = function() {
                for (var start = this.index++; !this.eof(); ) {
                  var ch = this.source.charCodeAt(this.index);
                  if (ch === 92)
                    return this.index = start, this.getComplexIdentifier();
                  if (ch >= 55296 && ch < 57343)
                    return this.index = start, this.getComplexIdentifier();
                  if (character_1.Character.isIdentifierPart(ch))
                    ++this.index;
                  else
                    break;
                }
                return this.source.slice(start, this.index);
              }, Scanner2.prototype.getComplexIdentifier = function() {
                var cp = this.codePointAt(this.index), id = character_1.Character.fromCodePoint(cp);
                this.index += id.length;
                var ch;
                for (cp === 92 && (this.source.charCodeAt(this.index) !== 117 && this.throwUnexpectedToken(), ++this.index, this.source[this.index] === "{" ? (++this.index, ch = this.scanUnicodeCodePointEscape()) : (ch = this.scanHexEscape("u"), (ch === null || ch === "\\" || !character_1.Character.isIdentifierStart(ch.charCodeAt(0))) && this.throwUnexpectedToken()), id = ch); !this.eof() && (cp = this.codePointAt(this.index), !!character_1.Character.isIdentifierPart(cp)); )
                  ch = character_1.Character.fromCodePoint(cp), id += ch, this.index += ch.length, cp === 92 && (id = id.substr(0, id.length - 1), this.source.charCodeAt(this.index) !== 117 && this.throwUnexpectedToken(), ++this.index, this.source[this.index] === "{" ? (++this.index, ch = this.scanUnicodeCodePointEscape()) : (ch = this.scanHexEscape("u"), (ch === null || ch === "\\" || !character_1.Character.isIdentifierPart(ch.charCodeAt(0))) && this.throwUnexpectedToken()), id += ch);
                return id;
              }, Scanner2.prototype.octalToDecimal = function(ch) {
                var octal = ch !== "0", code = octalValue(ch);
                return !this.eof() && character_1.Character.isOctalDigit(this.source.charCodeAt(this.index)) && (octal = !0, code = code * 8 + octalValue(this.source[this.index++]), "0123".indexOf(ch) >= 0 && !this.eof() && character_1.Character.isOctalDigit(this.source.charCodeAt(this.index)) && (code = code * 8 + octalValue(this.source[this.index++]))), {
                  code,
                  octal
                };
              }, Scanner2.prototype.scanIdentifier = function() {
                var type, start = this.index, id = this.source.charCodeAt(start) === 92 ? this.getComplexIdentifier() : this.getIdentifier();
                if (id.length === 1 ? type = 3 : this.isKeyword(id) ? type = 4 : id === "null" ? type = 5 : id === "true" || id === "false" ? type = 1 : type = 3, type !== 3 && start + id.length !== this.index) {
                  var restore = this.index;
                  this.index = start, this.tolerateUnexpectedToken(messages_1.Messages.InvalidEscapedReservedWord), this.index = restore;
                }
                return {
                  type,
                  value: id,
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.scanPunctuator = function() {
                var start = this.index, str = this.source[this.index];
                switch (str) {
                  case "(":
                  case "{":
                    str === "{" && this.curlyStack.push("{"), ++this.index;
                    break;
                  case ".":
                    ++this.index, this.source[this.index] === "." && this.source[this.index + 1] === "." && (this.index += 2, str = "...");
                    break;
                  case "}":
                    ++this.index, this.curlyStack.pop();
                    break;
                  case ")":
                  case ";":
                  case ",":
                  case "[":
                  case "]":
                  case ":":
                  case "?":
                  case "~":
                    ++this.index;
                    break;
                  default:
                    str = this.source.substr(this.index, 4), str === ">>>=" ? this.index += 4 : (str = str.substr(0, 3), str === "===" || str === "!==" || str === ">>>" || str === "<<=" || str === ">>=" || str === "**=" ? this.index += 3 : (str = str.substr(0, 2), str === "&&" || str === "||" || str === "==" || str === "!=" || str === "+=" || str === "-=" || str === "*=" || str === "/=" || str === "++" || str === "--" || str === "<<" || str === ">>" || str === "&=" || str === "|=" || str === "^=" || str === "%=" || str === "<=" || str === ">=" || str === "=>" || str === "**" ? this.index += 2 : (str = this.source[this.index], "<>=!+-*%&|^/".indexOf(str) >= 0 && ++this.index)));
                }
                return this.index === start && this.throwUnexpectedToken(), {
                  type: 7,
                  value: str,
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.scanHexLiteral = function(start) {
                for (var num = ""; !this.eof() && character_1.Character.isHexDigit(this.source.charCodeAt(this.index)); )
                  num += this.source[this.index++];
                return num.length === 0 && this.throwUnexpectedToken(), character_1.Character.isIdentifierStart(this.source.charCodeAt(this.index)) && this.throwUnexpectedToken(), {
                  type: 6,
                  value: parseInt("0x" + num, 16),
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.scanBinaryLiteral = function(start) {
                for (var num = "", ch; !this.eof() && (ch = this.source[this.index], !(ch !== "0" && ch !== "1")); )
                  num += this.source[this.index++];
                return num.length === 0 && this.throwUnexpectedToken(), this.eof() || (ch = this.source.charCodeAt(this.index), (character_1.Character.isIdentifierStart(ch) || character_1.Character.isDecimalDigit(ch)) && this.throwUnexpectedToken()), {
                  type: 6,
                  value: parseInt(num, 2),
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.scanOctalLiteral = function(prefix, start) {
                var num = "", octal = !1;
                for (character_1.Character.isOctalDigit(prefix.charCodeAt(0)) ? (octal = !0, num = "0" + this.source[this.index++]) : ++this.index; !this.eof() && character_1.Character.isOctalDigit(this.source.charCodeAt(this.index)); )
                  num += this.source[this.index++];
                return !octal && num.length === 0 && this.throwUnexpectedToken(), (character_1.Character.isIdentifierStart(this.source.charCodeAt(this.index)) || character_1.Character.isDecimalDigit(this.source.charCodeAt(this.index))) && this.throwUnexpectedToken(), {
                  type: 6,
                  value: parseInt(num, 8),
                  octal,
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.isImplicitOctalLiteral = function() {
                for (var i = this.index + 1; i < this.length; ++i) {
                  var ch = this.source[i];
                  if (ch === "8" || ch === "9")
                    return !1;
                  if (!character_1.Character.isOctalDigit(ch.charCodeAt(0)))
                    return !0;
                }
                return !0;
              }, Scanner2.prototype.scanNumericLiteral = function() {
                var start = this.index, ch = this.source[start];
                assert_1.assert(character_1.Character.isDecimalDigit(ch.charCodeAt(0)) || ch === ".", "Numeric literal must start with a decimal digit or a decimal point");
                var num = "";
                if (ch !== ".") {
                  if (num = this.source[this.index++], ch = this.source[this.index], num === "0") {
                    if (ch === "x" || ch === "X")
                      return ++this.index, this.scanHexLiteral(start);
                    if (ch === "b" || ch === "B")
                      return ++this.index, this.scanBinaryLiteral(start);
                    if (ch === "o" || ch === "O")
                      return this.scanOctalLiteral(ch, start);
                    if (ch && character_1.Character.isOctalDigit(ch.charCodeAt(0)) && this.isImplicitOctalLiteral())
                      return this.scanOctalLiteral(ch, start);
                  }
                  for (; character_1.Character.isDecimalDigit(this.source.charCodeAt(this.index)); )
                    num += this.source[this.index++];
                  ch = this.source[this.index];
                }
                if (ch === ".") {
                  for (num += this.source[this.index++]; character_1.Character.isDecimalDigit(this.source.charCodeAt(this.index)); )
                    num += this.source[this.index++];
                  ch = this.source[this.index];
                }
                if (ch === "e" || ch === "E")
                  if (num += this.source[this.index++], ch = this.source[this.index], (ch === "+" || ch === "-") && (num += this.source[this.index++]), character_1.Character.isDecimalDigit(this.source.charCodeAt(this.index)))
                    for (; character_1.Character.isDecimalDigit(this.source.charCodeAt(this.index)); )
                      num += this.source[this.index++];
                  else
                    this.throwUnexpectedToken();
                return character_1.Character.isIdentifierStart(this.source.charCodeAt(this.index)) && this.throwUnexpectedToken(), {
                  type: 6,
                  value: parseFloat(num),
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.scanStringLiteral = function() {
                var start = this.index, quote = this.source[start];
                assert_1.assert(quote === "'" || quote === '"', "String literal must starts with a quote"), ++this.index;
                for (var octal = !1, str = ""; !this.eof(); ) {
                  var ch = this.source[this.index++];
                  if (ch === quote) {
                    quote = "";
                    break;
                  } else if (ch === "\\")
                    if (ch = this.source[this.index++], !ch || !character_1.Character.isLineTerminator(ch.charCodeAt(0)))
                      switch (ch) {
                        case "u":
                          if (this.source[this.index] === "{")
                            ++this.index, str += this.scanUnicodeCodePointEscape();
                          else {
                            var unescaped_1 = this.scanHexEscape(ch);
                            unescaped_1 === null && this.throwUnexpectedToken(), str += unescaped_1;
                          }
                          break;
                        case "x":
                          var unescaped = this.scanHexEscape(ch);
                          unescaped === null && this.throwUnexpectedToken(messages_1.Messages.InvalidHexEscapeSequence), str += unescaped;
                          break;
                        case "n":
                          str += `
`;
                          break;
                        case "r":
                          str += "\r";
                          break;
                        case "t":
                          str += "	";
                          break;
                        case "b":
                          str += "\b";
                          break;
                        case "f":
                          str += "\f";
                          break;
                        case "v":
                          str += "\v";
                          break;
                        case "8":
                        case "9":
                          str += ch, this.tolerateUnexpectedToken();
                          break;
                        default:
                          if (ch && character_1.Character.isOctalDigit(ch.charCodeAt(0))) {
                            var octToDec = this.octalToDecimal(ch);
                            octal = octToDec.octal || octal, str += String.fromCharCode(octToDec.code);
                          } else
                            str += ch;
                          break;
                      }
                    else
                      ++this.lineNumber, ch === "\r" && this.source[this.index] === `
` && ++this.index, this.lineStart = this.index;
                  else {
                    if (character_1.Character.isLineTerminator(ch.charCodeAt(0)))
                      break;
                    str += ch;
                  }
                }
                return quote !== "" && (this.index = start, this.throwUnexpectedToken()), {
                  type: 8,
                  value: str,
                  octal,
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.scanTemplate = function() {
                var cooked = "", terminated = !1, start = this.index, head = this.source[start] === "`", tail = !1, rawOffset = 2;
                for (++this.index; !this.eof(); ) {
                  var ch = this.source[this.index++];
                  if (ch === "`") {
                    rawOffset = 1, tail = !0, terminated = !0;
                    break;
                  } else if (ch === "$") {
                    if (this.source[this.index] === "{") {
                      this.curlyStack.push("${"), ++this.index, terminated = !0;
                      break;
                    }
                    cooked += ch;
                  } else if (ch === "\\")
                    if (ch = this.source[this.index++], character_1.Character.isLineTerminator(ch.charCodeAt(0)))
                      ++this.lineNumber, ch === "\r" && this.source[this.index] === `
` && ++this.index, this.lineStart = this.index;
                    else
                      switch (ch) {
                        case "n":
                          cooked += `
`;
                          break;
                        case "r":
                          cooked += "\r";
                          break;
                        case "t":
                          cooked += "	";
                          break;
                        case "u":
                          if (this.source[this.index] === "{")
                            ++this.index, cooked += this.scanUnicodeCodePointEscape();
                          else {
                            var restore = this.index, unescaped_2 = this.scanHexEscape(ch);
                            unescaped_2 !== null ? cooked += unescaped_2 : (this.index = restore, cooked += ch);
                          }
                          break;
                        case "x":
                          var unescaped = this.scanHexEscape(ch);
                          unescaped === null && this.throwUnexpectedToken(messages_1.Messages.InvalidHexEscapeSequence), cooked += unescaped;
                          break;
                        case "b":
                          cooked += "\b";
                          break;
                        case "f":
                          cooked += "\f";
                          break;
                        case "v":
                          cooked += "\v";
                          break;
                        default:
                          ch === "0" ? (character_1.Character.isDecimalDigit(this.source.charCodeAt(this.index)) && this.throwUnexpectedToken(messages_1.Messages.TemplateOctalLiteral), cooked += "\0") : character_1.Character.isOctalDigit(ch.charCodeAt(0)) ? this.throwUnexpectedToken(messages_1.Messages.TemplateOctalLiteral) : cooked += ch;
                          break;
                      }
                  else character_1.Character.isLineTerminator(ch.charCodeAt(0)) ? (++this.lineNumber, ch === "\r" && this.source[this.index] === `
` && ++this.index, this.lineStart = this.index, cooked += `
`) : cooked += ch;
                }
                return terminated || this.throwUnexpectedToken(), head || this.curlyStack.pop(), {
                  type: 10,
                  value: this.source.slice(start + 1, this.index - rawOffset),
                  cooked,
                  head,
                  tail,
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.testRegExp = function(pattern, flags) {
                var astralSubstitute = "\uFFFF", tmp = pattern, self = this;
                flags.indexOf("u") >= 0 && (tmp = tmp.replace(/\\u\{([0-9a-fA-F]+)\}|\\u([a-fA-F0-9]{4})/g, function($0, $1, $2) {
                  var codePoint = parseInt($1 || $2, 16);
                  return codePoint > 1114111 && self.throwUnexpectedToken(messages_1.Messages.InvalidRegExp), codePoint <= 65535 ? String.fromCharCode(codePoint) : astralSubstitute;
                }).replace(/[\uD800-\uDBFF][\uDC00-\uDFFF]/g, astralSubstitute));
                try {
                  RegExp(tmp);
                } catch {
                  this.throwUnexpectedToken(messages_1.Messages.InvalidRegExp);
                }
                try {
                  return new RegExp(pattern, flags);
                } catch {
                  return null;
                }
              }, Scanner2.prototype.scanRegExpBody = function() {
                var ch = this.source[this.index];
                assert_1.assert(ch === "/", "Regular expression literal must start with a slash");
                for (var str = this.source[this.index++], classMarker = !1, terminated = !1; !this.eof(); )
                  if (ch = this.source[this.index++], str += ch, ch === "\\")
                    ch = this.source[this.index++], character_1.Character.isLineTerminator(ch.charCodeAt(0)) && this.throwUnexpectedToken(messages_1.Messages.UnterminatedRegExp), str += ch;
                  else if (character_1.Character.isLineTerminator(ch.charCodeAt(0)))
                    this.throwUnexpectedToken(messages_1.Messages.UnterminatedRegExp);
                  else if (classMarker)
                    ch === "]" && (classMarker = !1);
                  else if (ch === "/") {
                    terminated = !0;
                    break;
                  } else ch === "[" && (classMarker = !0);
                return terminated || this.throwUnexpectedToken(messages_1.Messages.UnterminatedRegExp), str.substr(1, str.length - 2);
              }, Scanner2.prototype.scanRegExpFlags = function() {
                for (var str = "", flags = ""; !this.eof(); ) {
                  var ch = this.source[this.index];
                  if (!character_1.Character.isIdentifierPart(ch.charCodeAt(0)))
                    break;
                  if (++this.index, ch === "\\" && !this.eof())
                    if (ch = this.source[this.index], ch === "u") {
                      ++this.index;
                      var restore = this.index, char = this.scanHexEscape("u");
                      if (char !== null)
                        for (flags += char, str += "\\u"; restore < this.index; ++restore)
                          str += this.source[restore];
                      else
                        this.index = restore, flags += "u", str += "\\u";
                      this.tolerateUnexpectedToken();
                    } else
                      str += "\\", this.tolerateUnexpectedToken();
                  else
                    flags += ch, str += ch;
                }
                return flags;
              }, Scanner2.prototype.scanRegExp = function() {
                var start = this.index, pattern = this.scanRegExpBody(), flags = this.scanRegExpFlags(), value = this.testRegExp(pattern, flags);
                return {
                  type: 9,
                  value: "",
                  pattern,
                  flags,
                  regex: value,
                  lineNumber: this.lineNumber,
                  lineStart: this.lineStart,
                  start,
                  end: this.index
                };
              }, Scanner2.prototype.lex = function() {
                if (this.eof())
                  return {
                    type: 2,
                    value: "",
                    lineNumber: this.lineNumber,
                    lineStart: this.lineStart,
                    start: this.index,
                    end: this.index
                  };
                var cp = this.source.charCodeAt(this.index);
                return character_1.Character.isIdentifierStart(cp) ? this.scanIdentifier() : cp === 40 || cp === 41 || cp === 59 ? this.scanPunctuator() : cp === 39 || cp === 34 ? this.scanStringLiteral() : cp === 46 ? character_1.Character.isDecimalDigit(this.source.charCodeAt(this.index + 1)) ? this.scanNumericLiteral() : this.scanPunctuator() : character_1.Character.isDecimalDigit(cp) ? this.scanNumericLiteral() : cp === 96 || cp === 125 && this.curlyStack[this.curlyStack.length - 1] === "${" ? this.scanTemplate() : cp >= 55296 && cp < 57343 && character_1.Character.isIdentifierStart(this.codePointAt(this.index)) ? this.scanIdentifier() : this.scanPunctuator();
              }, Scanner2;
            })();
            exports2.Scanner = Scanner;
          },
          /* 13 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 }), exports2.TokenName = {}, exports2.TokenName[
              1
              /* BooleanLiteral */
            ] = "Boolean", exports2.TokenName[
              2
              /* EOF */
            ] = "<end>", exports2.TokenName[
              3
              /* Identifier */
            ] = "Identifier", exports2.TokenName[
              4
              /* Keyword */
            ] = "Keyword", exports2.TokenName[
              5
              /* NullLiteral */
            ] = "Null", exports2.TokenName[
              6
              /* NumericLiteral */
            ] = "Numeric", exports2.TokenName[
              7
              /* Punctuator */
            ] = "Punctuator", exports2.TokenName[
              8
              /* StringLiteral */
            ] = "String", exports2.TokenName[
              9
              /* RegularExpression */
            ] = "RegularExpression", exports2.TokenName[
              10
              /* Template */
            ] = "Template";
          },
          /* 14 */
          /***/
          function(module2, exports2) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 }), exports2.XHTMLEntities = {
              quot: '"',
              amp: "&",
              apos: "'",
              gt: ">",
              nbsp: "\xA0",
              iexcl: "\xA1",
              cent: "\xA2",
              pound: "\xA3",
              curren: "\xA4",
              yen: "\xA5",
              brvbar: "\xA6",
              sect: "\xA7",
              uml: "\xA8",
              copy: "\xA9",
              ordf: "\xAA",
              laquo: "\xAB",
              not: "\xAC",
              shy: "\xAD",
              reg: "\xAE",
              macr: "\xAF",
              deg: "\xB0",
              plusmn: "\xB1",
              sup2: "\xB2",
              sup3: "\xB3",
              acute: "\xB4",
              micro: "\xB5",
              para: "\xB6",
              middot: "\xB7",
              cedil: "\xB8",
              sup1: "\xB9",
              ordm: "\xBA",
              raquo: "\xBB",
              frac14: "\xBC",
              frac12: "\xBD",
              frac34: "\xBE",
              iquest: "\xBF",
              Agrave: "\xC0",
              Aacute: "\xC1",
              Acirc: "\xC2",
              Atilde: "\xC3",
              Auml: "\xC4",
              Aring: "\xC5",
              AElig: "\xC6",
              Ccedil: "\xC7",
              Egrave: "\xC8",
              Eacute: "\xC9",
              Ecirc: "\xCA",
              Euml: "\xCB",
              Igrave: "\xCC",
              Iacute: "\xCD",
              Icirc: "\xCE",
              Iuml: "\xCF",
              ETH: "\xD0",
              Ntilde: "\xD1",
              Ograve: "\xD2",
              Oacute: "\xD3",
              Ocirc: "\xD4",
              Otilde: "\xD5",
              Ouml: "\xD6",
              times: "\xD7",
              Oslash: "\xD8",
              Ugrave: "\xD9",
              Uacute: "\xDA",
              Ucirc: "\xDB",
              Uuml: "\xDC",
              Yacute: "\xDD",
              THORN: "\xDE",
              szlig: "\xDF",
              agrave: "\xE0",
              aacute: "\xE1",
              acirc: "\xE2",
              atilde: "\xE3",
              auml: "\xE4",
              aring: "\xE5",
              aelig: "\xE6",
              ccedil: "\xE7",
              egrave: "\xE8",
              eacute: "\xE9",
              ecirc: "\xEA",
              euml: "\xEB",
              igrave: "\xEC",
              iacute: "\xED",
              icirc: "\xEE",
              iuml: "\xEF",
              eth: "\xF0",
              ntilde: "\xF1",
              ograve: "\xF2",
              oacute: "\xF3",
              ocirc: "\xF4",
              otilde: "\xF5",
              ouml: "\xF6",
              divide: "\xF7",
              oslash: "\xF8",
              ugrave: "\xF9",
              uacute: "\xFA",
              ucirc: "\xFB",
              uuml: "\xFC",
              yacute: "\xFD",
              thorn: "\xFE",
              yuml: "\xFF",
              OElig: "\u0152",
              oelig: "\u0153",
              Scaron: "\u0160",
              scaron: "\u0161",
              Yuml: "\u0178",
              fnof: "\u0192",
              circ: "\u02C6",
              tilde: "\u02DC",
              Alpha: "\u0391",
              Beta: "\u0392",
              Gamma: "\u0393",
              Delta: "\u0394",
              Epsilon: "\u0395",
              Zeta: "\u0396",
              Eta: "\u0397",
              Theta: "\u0398",
              Iota: "\u0399",
              Kappa: "\u039A",
              Lambda: "\u039B",
              Mu: "\u039C",
              Nu: "\u039D",
              Xi: "\u039E",
              Omicron: "\u039F",
              Pi: "\u03A0",
              Rho: "\u03A1",
              Sigma: "\u03A3",
              Tau: "\u03A4",
              Upsilon: "\u03A5",
              Phi: "\u03A6",
              Chi: "\u03A7",
              Psi: "\u03A8",
              Omega: "\u03A9",
              alpha: "\u03B1",
              beta: "\u03B2",
              gamma: "\u03B3",
              delta: "\u03B4",
              epsilon: "\u03B5",
              zeta: "\u03B6",
              eta: "\u03B7",
              theta: "\u03B8",
              iota: "\u03B9",
              kappa: "\u03BA",
              lambda: "\u03BB",
              mu: "\u03BC",
              nu: "\u03BD",
              xi: "\u03BE",
              omicron: "\u03BF",
              pi: "\u03C0",
              rho: "\u03C1",
              sigmaf: "\u03C2",
              sigma: "\u03C3",
              tau: "\u03C4",
              upsilon: "\u03C5",
              phi: "\u03C6",
              chi: "\u03C7",
              psi: "\u03C8",
              omega: "\u03C9",
              thetasym: "\u03D1",
              upsih: "\u03D2",
              piv: "\u03D6",
              ensp: "\u2002",
              emsp: "\u2003",
              thinsp: "\u2009",
              zwnj: "\u200C",
              zwj: "\u200D",
              lrm: "\u200E",
              rlm: "\u200F",
              ndash: "\u2013",
              mdash: "\u2014",
              lsquo: "\u2018",
              rsquo: "\u2019",
              sbquo: "\u201A",
              ldquo: "\u201C",
              rdquo: "\u201D",
              bdquo: "\u201E",
              dagger: "\u2020",
              Dagger: "\u2021",
              bull: "\u2022",
              hellip: "\u2026",
              permil: "\u2030",
              prime: "\u2032",
              Prime: "\u2033",
              lsaquo: "\u2039",
              rsaquo: "\u203A",
              oline: "\u203E",
              frasl: "\u2044",
              euro: "\u20AC",
              image: "\u2111",
              weierp: "\u2118",
              real: "\u211C",
              trade: "\u2122",
              alefsym: "\u2135",
              larr: "\u2190",
              uarr: "\u2191",
              rarr: "\u2192",
              darr: "\u2193",
              harr: "\u2194",
              crarr: "\u21B5",
              lArr: "\u21D0",
              uArr: "\u21D1",
              rArr: "\u21D2",
              dArr: "\u21D3",
              hArr: "\u21D4",
              forall: "\u2200",
              part: "\u2202",
              exist: "\u2203",
              empty: "\u2205",
              nabla: "\u2207",
              isin: "\u2208",
              notin: "\u2209",
              ni: "\u220B",
              prod: "\u220F",
              sum: "\u2211",
              minus: "\u2212",
              lowast: "\u2217",
              radic: "\u221A",
              prop: "\u221D",
              infin: "\u221E",
              ang: "\u2220",
              and: "\u2227",
              or: "\u2228",
              cap: "\u2229",
              cup: "\u222A",
              int: "\u222B",
              there4: "\u2234",
              sim: "\u223C",
              cong: "\u2245",
              asymp: "\u2248",
              ne: "\u2260",
              equiv: "\u2261",
              le: "\u2264",
              ge: "\u2265",
              sub: "\u2282",
              sup: "\u2283",
              nsub: "\u2284",
              sube: "\u2286",
              supe: "\u2287",
              oplus: "\u2295",
              otimes: "\u2297",
              perp: "\u22A5",
              sdot: "\u22C5",
              lceil: "\u2308",
              rceil: "\u2309",
              lfloor: "\u230A",
              rfloor: "\u230B",
              loz: "\u25CA",
              spades: "\u2660",
              clubs: "\u2663",
              hearts: "\u2665",
              diams: "\u2666",
              lang: "\u27E8",
              rang: "\u27E9"
            };
          },
          /* 15 */
          /***/
          function(module2, exports2, __webpack_require__) {
            "use strict";
            Object.defineProperty(exports2, "__esModule", { value: !0 });
            var error_handler_1 = __webpack_require__(10), scanner_1 = __webpack_require__(12), token_1 = __webpack_require__(13), Reader = (function() {
              function Reader2() {
                this.values = [], this.curly = this.paren = -1;
              }
              return Reader2.prototype.beforeFunctionExpression = function(t) {
                return [
                  "(",
                  "{",
                  "[",
                  "in",
                  "typeof",
                  "instanceof",
                  "new",
                  "return",
                  "case",
                  "delete",
                  "throw",
                  "void",
                  // assignment operators
                  "=",
                  "+=",
                  "-=",
                  "*=",
                  "**=",
                  "/=",
                  "%=",
                  "<<=",
                  ">>=",
                  ">>>=",
                  "&=",
                  "|=",
                  "^=",
                  ",",
                  // binary/unary operators
                  "+",
                  "-",
                  "*",
                  "**",
                  "/",
                  "%",
                  "++",
                  "--",
                  "<<",
                  ">>",
                  ">>>",
                  "&",
                  "|",
                  "^",
                  "!",
                  "~",
                  "&&",
                  "||",
                  "?",
                  ":",
                  "===",
                  "==",
                  ">=",
                  "<=",
                  "<",
                  ">",
                  "!=",
                  "!=="
                ].indexOf(t) >= 0;
              }, Reader2.prototype.isRegexStart = function() {
                var previous = this.values[this.values.length - 1], regex = previous !== null;
                switch (previous) {
                  case "this":
                  case "]":
                    regex = !1;
                    break;
                  case ")":
                    var keyword = this.values[this.paren - 1];
                    regex = keyword === "if" || keyword === "while" || keyword === "for" || keyword === "with";
                    break;
                  case "}":
                    if (regex = !1, this.values[this.curly - 3] === "function") {
                      var check = this.values[this.curly - 4];
                      regex = check ? !this.beforeFunctionExpression(check) : !1;
                    } else if (this.values[this.curly - 4] === "function") {
                      var check = this.values[this.curly - 5];
                      regex = check ? !this.beforeFunctionExpression(check) : !0;
                    }
                    break;
                  default:
                    break;
                }
                return regex;
              }, Reader2.prototype.push = function(token) {
                token.type === 7 || token.type === 4 ? (token.value === "{" ? this.curly = this.values.length : token.value === "(" && (this.paren = this.values.length), this.values.push(token.value)) : this.values.push(null);
              }, Reader2;
            })(), Tokenizer = (function() {
              function Tokenizer2(code, config) {
                this.errorHandler = new error_handler_1.ErrorHandler(), this.errorHandler.tolerant = config ? typeof config.tolerant == "boolean" && config.tolerant : !1, this.scanner = new scanner_1.Scanner(code, this.errorHandler), this.scanner.trackComment = config ? typeof config.comment == "boolean" && config.comment : !1, this.trackRange = config ? typeof config.range == "boolean" && config.range : !1, this.trackLoc = config ? typeof config.loc == "boolean" && config.loc : !1, this.buffer = [], this.reader = new Reader();
              }
              return Tokenizer2.prototype.errors = function() {
                return this.errorHandler.errors;
              }, Tokenizer2.prototype.getNextToken = function() {
                if (this.buffer.length === 0) {
                  var comments = this.scanner.scanComments();
                  if (this.scanner.trackComment)
                    for (var i = 0; i < comments.length; ++i) {
                      var e = comments[i], value = this.scanner.source.slice(e.slice[0], e.slice[1]), comment = {
                        type: e.multiLine ? "BlockComment" : "LineComment",
                        value
                      };
                      this.trackRange && (comment.range = e.range), this.trackLoc && (comment.loc = e.loc), this.buffer.push(comment);
                    }
                  if (!this.scanner.eof()) {
                    var loc = void 0;
                    this.trackLoc && (loc = {
                      start: {
                        line: this.scanner.lineNumber,
                        column: this.scanner.index - this.scanner.lineStart
                      },
                      end: {}
                    });
                    var startRegex = this.scanner.source[this.scanner.index] === "/" && this.reader.isRegexStart(), token = startRegex ? this.scanner.scanRegExp() : this.scanner.lex();
                    this.reader.push(token);
                    var entry = {
                      type: token_1.TokenName[token.type],
                      value: this.scanner.source.slice(token.start, token.end)
                    };
                    if (this.trackRange && (entry.range = [token.start, token.end]), this.trackLoc && (loc.end = {
                      line: this.scanner.lineNumber,
                      column: this.scanner.index - this.scanner.lineStart
                    }, entry.loc = loc), token.type === 9) {
                      var pattern = token.pattern, flags = token.flags;
                      entry.regex = { pattern, flags };
                    }
                    this.buffer.push(entry);
                  }
                }
                return this.buffer.shift();
              }, Tokenizer2;
            })();
            exports2.Tokenizer = Tokenizer;
          }
          /******/
        ])
      );
    });
  }
});

// ../../node_modules/.pnpm/redeyed@2.1.1/node_modules/redeyed/redeyed.js
var require_redeyed = __commonJS({
  "../../node_modules/.pnpm/redeyed@2.1.1/node_modules/redeyed/redeyed.js"(exports, module) {
    init_cjs_shims();
    (function() {
      "use strict";
      var esprima, exportFn, toString2 = Object.prototype.toString;
      typeof module == "object" && typeof module.exports == "object" && typeof __require == "function" ? (esprima = require_esprima(), exportFn = function(redeyed) {
        module.exports = redeyed;
      }, bootstrap(esprima, exportFn)) : typeof define == "function" && define.amd ? define(["esprima"], function(esprima2) {
        return bootstrap(esprima2);
      }) : typeof window == "object" && (window.redeyed = bootstrap(window.esprima));
      function bootstrap(esprima2, exportFn2) {
        function isFunction(obj) {
          return toString2.call(obj) === "[object Function]";
        }
        function isString(obj) {
          return toString2.call(obj) === "[object String]";
        }
        function isObject(obj) {
          return toString2.call(obj) === "[object Object]";
        }
        function surroundWith(before, after) {
          return function(s) {
            return before + s + after;
          };
        }
        function isNonCircular(key) {
          return key !== "_parent";
        }
        function objectizeString(value) {
          var vals = value.split(":");
          if (vals.length === 0 || vals.length > 2)
            throw new Error(
              "illegal string config: " + value + `
Should be of format "before:after"`
            );
          return vals.length === 1 || vals[1].length === 0 ? vals.indexOf(":") < 0 ? { _before: vals[0] } : { _after: vals[0] } : { _before: vals[0], _after: vals[1] };
        }
        function objectize(node) {
          function resolve2(value, key) {
            if (value._parent) {
              if (value._parent._default && value._parent._default[key]) return value._parent._default[key];
              var root = value._parent._parent;
              if (root)
                return root._default ? root._default[key] : void 0;
            }
          }
          function process2(key) {
            var value = node[key];
            if (value && !isFunction(value)) {
              if (isString(value) && (node[key] = value = objectizeString(value)), value._parent = node, isObject(value)) {
                if (!value._before && !value._after) return objectize(value);
                value._before = value._before || resolve2(value, "_before"), value._after = value._after || resolve2(value, "_after");
                return;
              }
              throw new Error("nodes need to be either {String}, {Object} or {Function}." + value + " is neither.");
            }
          }
          node._default && process2("_default"), Object.keys(node).filter(function(key) {
            return isNonCircular(key) && node.hasOwnProperty(key) && key !== "_before" && key !== "_after" && key !== "_default";
          }).forEach(process2);
        }
        function functionize(node) {
          Object.keys(node).filter(function(key) {
            return isNonCircular(key) && node.hasOwnProperty(key);
          }).forEach(function(key) {
            var value = node[key];
            if (!isFunction(value) && isObject(value)) {
              if (!value._before && !value._after) return functionize(value);
              var before = value._before || "", after = value._after || "";
              return node[key] = surroundWith(before, after), node[key];
            }
          });
        }
        function normalize(root) {
          objectize(root), functionize(root);
        }
        function mergeTokensAndComments(tokens, comments) {
          var all = {};
          function addToAllByRangeStart(t) {
            all[t.range[0]] = t;
          }
          return tokens.forEach(addToAllByRangeStart), comments.forEach(addToAllByRangeStart), Object.keys(all).map(function(k) {
            return all[k];
          });
        }
        function redeyed(code, config, opts) {
          opts = opts || {};
          var parser4 = opts.parser || esprima2, jsx = !!opts.jsx, buildAst = jsx || !!opts.buildAst, hashbang = "", ast, tokens, comments, lastSplitEnd = 0, splits = [], transformedCode, all, info;
          code[0] === "#" && code[1] === "!" && (hashbang = code.substr(0, code.indexOf(`
`) + 1), code = Array.apply(0, Array(hashbang.length)).join(" ") + `
` + code.substr(hashbang.length)), buildAst ? (ast = parser4.parse(code, { tokens: !0, comment: !0, range: !0, loc: !0, tolerant: !0, jsx: !0 }), tokens = ast.tokens, comments = ast.comments) : (tokens = [], comments = [], parser4.tokenize(code, { range: !0, loc: !0, comment: !0 }, function(token2) {
            token2.type === "LineComment" ? (token2.type = "Line", comments.push(token2)) : token2.type === "BlockComment" ? (token2.type = "Block", comments.push(token2)) : (token2.type === "Identifier" && token2.value === "static" && (token2.type = "Keyword"), tokens.push(token2));
          })), normalize(config);
          function tokenIndex(tokens2, tkn, start2) {
            var current, rangeStart = tkn.range[0];
            for (current = start2; current < tokens2.length; current++)
              if (tokens2[current].range[0] === rangeStart) return current;
            throw new Error("Token %s not found at or after index: %d", tkn, start2);
          }
          function process2(surround2) {
            var result, currentIndex, nextIndex, skip = 0, splitEnd;
            return result = surround2(code.slice(start, end), info), isObject(result) ? (splits.push(result.replacement), currentIndex = info.tokenIndex, nextIndex = tokenIndex(info.tokens, result.skipPastToken, currentIndex), skip = nextIndex - currentIndex, splitEnd = skip > 0 ? tokens[nextIndex - 1].range[1] : end) : (splits.push(result), splitEnd = end), { skip, splitEnd };
          }
          function addSplit(start2, end2, surround2, info2) {
            var result, skip = 0;
            if (!(start2 >= end2))
              return surround2 ? (result = process2(surround2), skip = result.skip, lastSplitEnd = result.splitEnd) : (splits.push(code.slice(start2, end2)), lastSplitEnd = end2), skip;
          }
          all = mergeTokensAndComments(tokens, comments);
          for (var tokenIdx = 0; tokenIdx < all.length; tokenIdx++) {
            var token = all[tokenIdx], surroundForType = config[token.type], surround, start, end;
            surroundForType && (surround = surroundForType && surroundForType.hasOwnProperty(token.value) && surroundForType[token.value] && isFunction(surroundForType[token.value]) ? surroundForType[token.value] : surroundForType._default, start = token.range[0], end = token.range[1], addSplit(lastSplitEnd, start), info = { tokenIndex: tokenIdx, tokens: all, ast, code }, tokenIdx += addSplit(start, end, surround, info));
          }
          return lastSplitEnd < code.length && addSplit(lastSplitEnd, code.length), opts.nojoin || (transformedCode = splits.join(""), hashbang.length > 0 && (transformedCode = hashbang + transformedCode.substr(hashbang.length))), {
            ast,
            tokens,
            comments,
            splits,
            code: transformedCode
          };
        }
        return exportFn2 ? exportFn2(redeyed) : redeyed;
      }
    })();
  }
});

// ../../node_modules/.pnpm/ansicolors@0.3.2/node_modules/ansicolors/ansicolors.js
var require_ansicolors = __commonJS({
  "../../node_modules/.pnpm/ansicolors@0.3.2/node_modules/ansicolors/ansicolors.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var colorNums = {
      white: 37,
      black: 30,
      blue: 34,
      cyan: 36,
      green: 32,
      magenta: 35,
      red: 31,
      yellow: 33,
      brightBlack: 90,
      brightRed: 91,
      brightGreen: 92,
      brightYellow: 93,
      brightBlue: 94,
      brightMagenta: 95,
      brightCyan: 96,
      brightWhite: 97
    }, backgroundColorNums = {
      bgBlack: 40,
      bgRed: 41,
      bgGreen: 42,
      bgYellow: 43,
      bgBlue: 44,
      bgMagenta: 45,
      bgCyan: 46,
      bgWhite: 47,
      bgBrightBlack: 100,
      bgBrightRed: 101,
      bgBrightGreen: 102,
      bgBrightYellow: 103,
      bgBrightBlue: 104,
      bgBrightMagenta: 105,
      bgBrightCyan: 106,
      bgBrightWhite: 107
    }, open = {}, close = {}, colors = {};
    Object.keys(colorNums).forEach(function(k) {
      var o = open[k] = "\x1B[" + colorNums[k] + "m", c = close[k] = "\x1B[39m";
      colors[k] = function(s) {
        return o + s + c;
      };
    });
    Object.keys(backgroundColorNums).forEach(function(k) {
      var o = open[k] = "\x1B[" + backgroundColorNums[k] + "m", c = close[k] = "\x1B[49m";
      colors[k] = function(s) {
        return o + s + c;
      };
    });
    module.exports = colors;
    colors.open = open;
    colors.close = close;
  }
});

// ../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/themes/default.js
var require_default = __commonJS({
  "../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/themes/default.js"(exports, module) {
    init_cjs_shims();
    var colors = require_ansicolors();
    module.exports = {
      Boolean: {
        true: void 0,
        false: void 0,
        _default: colors.brightRed
      },
      Identifier: {
        undefined: colors.brightBlack,
        self: colors.brightRed,
        console: colors.blue,
        log: colors.blue,
        warn: colors.red,
        error: colors.brightRed,
        _default: colors.white
      },
      Null: {
        _default: colors.brightBlack
      },
      Numeric: {
        _default: colors.blue
      },
      String: {
        _default: function(s, info) {
          var nextToken = info.tokens[info.tokenIndex + 1];
          return nextToken && nextToken.type === "Punctuator" && nextToken.value === ":" ? colors.green(s) : colors.brightGreen(s);
        }
      },
      Keyword: {
        break: void 0,
        case: void 0,
        catch: colors.cyan,
        class: void 0,
        const: void 0,
        continue: void 0,
        debugger: void 0,
        default: void 0,
        delete: colors.red,
        do: void 0,
        else: void 0,
        enum: void 0,
        export: void 0,
        extends: void 0,
        finally: colors.cyan,
        for: void 0,
        function: void 0,
        if: void 0,
        implements: void 0,
        import: void 0,
        in: void 0,
        instanceof: void 0,
        let: void 0,
        new: colors.red,
        package: void 0,
        private: void 0,
        protected: void 0,
        public: void 0,
        return: colors.red,
        static: void 0,
        super: void 0,
        switch: void 0,
        this: colors.brightRed,
        throw: void 0,
        try: colors.cyan,
        typeof: void 0,
        var: colors.green,
        void: void 0,
        while: void 0,
        with: void 0,
        yield: void 0,
        _default: colors.brightBlue
      },
      Punctuator: {
        ";": colors.brightBlack,
        ".": colors.green,
        ",": colors.green,
        "{": colors.yellow,
        "}": colors.yellow,
        "(": colors.brightBlack,
        ")": colors.brightBlack,
        "[": colors.yellow,
        "]": colors.yellow,
        "<": void 0,
        ">": void 0,
        "+": void 0,
        "-": void 0,
        "*": void 0,
        "%": void 0,
        "&": void 0,
        "|": void 0,
        "^": void 0,
        "!": void 0,
        "~": void 0,
        "?": void 0,
        ":": void 0,
        "=": void 0,
        "<=": void 0,
        ">=": void 0,
        "==": void 0,
        "!=": void 0,
        "++": void 0,
        "--": void 0,
        "<<": void 0,
        ">>": void 0,
        "&&": void 0,
        "||": void 0,
        "+=": void 0,
        "-=": void 0,
        "*=": void 0,
        "%=": void 0,
        "&=": void 0,
        "|=": void 0,
        "^=": void 0,
        "/=": void 0,
        "=>": void 0,
        "**": void 0,
        "===": void 0,
        "!==": void 0,
        ">>>": void 0,
        "<<=": void 0,
        ">>=": void 0,
        "...": void 0,
        "**=": void 0,
        ">>>=": void 0,
        _default: colors.brightYellow
      },
      Line: {
        _default: colors.brightBlack
      },
      Block: {
        _default: colors.brightBlack
      },
      JSXAttribute: {
        _default: colors.magenta
      },
      JSXClosingElement: {
        _default: colors.magenta
      },
      JSXElement: {
        _default: colors.magenta
      },
      JSXEmptyExpression: {
        _default: colors.magenta
      },
      JSXExpressionContainer: {
        _default: colors.magenta
      },
      JSXIdentifier: {
        className: colors.blue,
        _default: colors.magenta
      },
      JSXMemberExpression: {
        _default: colors.magenta
      },
      JSXNamespacedName: {
        _default: colors.magenta
      },
      JSXOpeningElement: {
        _default: colors.magenta
      },
      JSXSpreadAttribute: {
        _default: colors.magenta
      },
      JSXText: {
        _default: colors.brightGreen
      },
      _default: void 0
    };
  }
});

// ../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/lib/highlight.js
var require_highlight = __commonJS({
  "../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/lib/highlight.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var redeyed = require_redeyed(), theme = require_default(), colors = require_ansicolors(), colorSurround = colors.brightBlack, surroundClose = "\x1B[39m";
    function trimEmptyLines(lines) {
      for (var line = lines.pop(); !line || !line.length; )
        line = lines.pop();
      line && lines.push(line);
    }
    function addLinenos(highlightedCode, firstline) {
      var highlightedLines = highlightedCode.split(`
`);
      trimEmptyLines(highlightedLines);
      var linesLen = highlightedLines.length, lines = [], totalDigits, lineno;
      function getDigits(n) {
        return n < 10 ? 1 : n < 100 ? 2 : n < 1e3 ? 3 : n < 1e4 ? 4 : 5;
      }
      function pad(n, totalDigits2) {
        var padDigits = totalDigits2 - getDigits(n);
        switch (padDigits) {
          case 0:
            return "" + n;
          case 1:
            return " " + n;
          case 2:
            return "  " + n;
          case 3:
            return "   " + n;
          case 4:
            return "    " + n;
          case 5:
            return "     " + n;
        }
      }
      totalDigits = getDigits(linesLen + firstline - 1);
      for (var i = 0; i < linesLen; i++)
        lineno = colorSurround(pad(i + firstline, totalDigits) + ": ").replace(surroundClose, ""), lines.push(lineno + highlightedLines[i]);
      return lines.join(`
`);
    }
    module.exports = function(code, opts) {
      opts = opts || {};
      try {
        var result = redeyed(code, opts.theme || theme, { jsx: !!opts.jsx }), firstline = opts.firstline && !isNaN(opts.firstline) ? opts.firstline : 1;
        return opts.linenos ? addLinenos(result.code, firstline) : result.code;
      } catch (e) {
        throw e.message = "Unable to perform highlight. The code contained syntax errors: " + e.message, e;
      }
    };
  }
});

// ../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/lib/highlightFile.js
var require_highlightFile = __commonJS({
  "../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/lib/highlightFile.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var fs = __require("fs"), highlight = require_highlight();
    function isFunction(obj) {
      return toString.call(obj) === "[object Function]";
    }
    module.exports = function(fullPath, opts, cb) {
      isFunction(opts) && (cb = opts, opts = {}), opts = opts || {}, fs.readFile(fullPath, "utf-8", function(err, code) {
        if (err) return cb(err);
        try {
          cb(null, highlight(code, opts));
        } catch (e) {
          cb(e);
        }
      });
    };
  }
});

// ../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/lib/highlightFileSync.js
var require_highlightFileSync = __commonJS({
  "../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/lib/highlightFileSync.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var fs = __require("fs"), highlight = require_highlight();
    module.exports = function(fullPath, opts) {
      var code = fs.readFileSync(fullPath, "utf-8");
      return opts = opts || {}, highlight(code, opts);
    };
  }
});

// ../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/cardinal.js
var require_cardinal = __commonJS({
  "../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/cardinal.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    module.exports = {
      highlight: require_highlight(),
      highlightFile: require_highlightFile(),
      highlightFileSync: require_highlightFileSync()
    };
  }
});

// ../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/themes/jq.js
var require_jq = __commonJS({
  "../../node_modules/.pnpm/cardinal@2.1.1/node_modules/cardinal/themes/jq.js"(exports, module) {
    init_cjs_shims();
    var colors = require_ansicolors();
    module.exports = {
      Boolean: {
        true: void 0,
        false: void 0,
        _default: colors.brightRed
      },
      Identifier: {
        undefined: colors.brightBlack,
        self: colors.brightRed,
        console: colors.blue,
        log: colors.blue,
        warn: colors.red,
        error: colors.brightRed,
        _default: colors.white
      },
      Null: {
        _default: colors.brightBlack
      },
      Numeric: {
        _default: colors.blue
      },
      String: {
        _default: function(s, info) {
          var nextToken = info.tokens[info.tokenIndex + 1];
          return nextToken && nextToken.type === "Punctuator" && nextToken.value === ":" ? colors.brightBlue(s) : colors.brightGreen(s);
        }
      },
      Keyword: {
        break: void 0,
        case: void 0,
        catch: colors.cyan,
        class: void 0,
        const: void 0,
        continue: void 0,
        debugger: void 0,
        default: void 0,
        delete: colors.red,
        do: void 0,
        else: void 0,
        enum: void 0,
        export: void 0,
        extends: void 0,
        finally: colors.cyan,
        for: void 0,
        function: void 0,
        if: void 0,
        implements: void 0,
        import: void 0,
        in: void 0,
        instanceof: void 0,
        let: void 0,
        new: colors.red,
        package: void 0,
        private: void 0,
        protected: void 0,
        public: void 0,
        return: colors.red,
        static: void 0,
        super: void 0,
        switch: void 0,
        this: colors.brightRed,
        throw: void 0,
        try: colors.cyan,
        typeof: void 0,
        var: colors.green,
        void: void 0,
        while: void 0,
        with: void 0,
        yield: void 0,
        _default: colors.brightBlue
      },
      Punctuator: {
        ";": colors.brightBlack,
        ".": colors.green,
        ",": colors.green,
        "{": colors.brightWhite,
        "}": colors.brightWhite,
        "(": colors.brightBlack,
        ")": colors.brightBlack,
        "[": colors.brightWhite,
        "]": colors.brightWhite,
        "<": void 0,
        ">": void 0,
        "+": void 0,
        "-": void 0,
        "*": void 0,
        "%": void 0,
        "&": void 0,
        "|": void 0,
        "^": void 0,
        "!": void 0,
        "~": void 0,
        "?": void 0,
        ":": void 0,
        "=": void 0,
        "<=": void 0,
        ">=": void 0,
        "==": void 0,
        "!=": void 0,
        "++": void 0,
        "--": void 0,
        "<<": void 0,
        ">>": void 0,
        "&&": void 0,
        "||": void 0,
        "+=": void 0,
        "-=": void 0,
        "*=": void 0,
        "%=": void 0,
        "&=": void 0,
        "|=": void 0,
        "^=": void 0,
        "/=": void 0,
        "=>": void 0,
        "**": void 0,
        "===": void 0,
        "!==": void 0,
        ">>>": void 0,
        "<<=": void 0,
        ">>=": void 0,
        "...": void 0,
        "**=": void 0,
        ">>>=": void 0,
        _default: colors.brightYellow
      },
      Line: {
        _default: colors.brightBlack
      },
      Block: {
        _default: colors.brightBlack
      },
      _default: void 0
    };
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/exit.js
var require_exit2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/exit.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.ExitError = void 0;
    var ExitError = class extends Error {
      code;
      error;
      ux;
      constructor(status, error) {
        let code = "EEXIT";
        super(error ? error.message : `${code}: ${status}`), this.error = error, this.ux = { exit: status }, this.code = code;
      }
    };
    exports.ExitError = ExitError;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/index.js
var require_cli_ux = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.write = exports.colorize = exports.Table = exports.ExitError = exports.config = exports.Config = exports.ActionBase = exports.warn = exports.wait = exports.url = exports.tree = exports.trace = exports.table = exports.styledObject = exports.styledJSON = exports.styledHeader = exports.prompt = exports.progress = exports.logToStderr = exports.log = exports.info = exports.flush = exports.exit = exports.error = exports.done = exports.debug = exports.confirm = exports.anykey = exports.annotation = exports.action = exports.ux = void 0;
    var chalk_1 = __importDefault(require_source()), node_util_1 = __require("node:util"), Errors = __importStar(require_errors()), config_1 = require_config2(), flush_1 = require_flush(), uxPrompt = __importStar(require_prompt()), styled = __importStar(require_styled()), wait_1 = __importDefault(require_wait()), write_1 = __importDefault(require_write()), hyperlinker = require_hyperlinker(), ux = class {
      static config = config_1.config;
      static get action() {
        return config_1.config.action;
      }
      static annotation(text, annotation2) {
        require_supports_hyperlinks().stdout ? this.log(`\x1B]1337;AddAnnotation=${text.length}|${annotation2}\x07${text}`) : this.log(text);
      }
      /**
       * "press anykey to continue"
       */
      static get anykey() {
        return uxPrompt.anykey;
      }
      static get confirm() {
        return uxPrompt.confirm;
      }
      static debug(format, ...args) {
        ["debug", "trace"].includes(this.config.outputLevel) && this.info((0, node_util_1.format)(format, ...args) + `
`);
      }
      static async done() {
        config_1.config.action.stop();
      }
      static async flush(ms = 1e4) {
        await (0, flush_1.flush)(ms);
      }
      static info(format, ...args) {
        write_1.default.stdout((0, node_util_1.format)(format, ...args) + `
`);
      }
      static log(format, ...args) {
        this.info(format || "", ...args);
      }
      static logToStderr(format, ...args) {
        write_1.default.stderr((0, node_util_1.format)(format, ...args) + `
`);
      }
      static get progress() {
        return styled.progress;
      }
      static get prompt() {
        return uxPrompt.prompt;
      }
      static styledHeader(header) {
        this.info(chalk_1.default.dim("=== ") + chalk_1.default.bold(header) + `
`);
      }
      static styledJSON(obj) {
        let json = JSON.stringify(obj, null, 2);
        if (!chalk_1.default.level) {
          this.info(json);
          return;
        }
        let cardinal = require_cardinal(), theme = require_jq();
        this.info(cardinal.highlight(json, { json: !0, theme }));
      }
      static styledObject(obj, keys) {
        this.info(styled.styledObject(obj, keys));
      }
      static get table() {
        return styled.Table.table;
      }
      static trace(format, ...args) {
        this.config.outputLevel === "trace" && this.info((0, node_util_1.format)(format, ...args) + `
`);
      }
      static get tree() {
        return styled.tree;
      }
      static url(text, uri, params = {}) {
        require_supports_hyperlinks().stdout ? this.log(hyperlinker(text, uri, params)) : this.log(uri);
      }
      static get wait() {
        return wait_1.default;
      }
    };
    exports.ux = ux;
    var { action, annotation, anykey, confirm, debug: debug2, done, flush, info, log, logToStderr, progress, prompt, styledHeader, styledJSON, styledObject, table, trace, tree, url, wait } = ux;
    exports.action = action;
    exports.annotation = annotation;
    exports.anykey = anykey;
    exports.confirm = confirm;
    exports.debug = debug2;
    exports.done = done;
    exports.flush = flush;
    exports.info = info;
    exports.log = log;
    exports.logToStderr = logToStderr;
    exports.progress = progress;
    exports.prompt = prompt;
    exports.styledHeader = styledHeader;
    exports.styledJSON = styledJSON;
    exports.styledObject = styledObject;
    exports.table = table;
    exports.trace = trace;
    exports.tree = tree;
    exports.url = url;
    exports.wait = wait;
    var { error, exit, warn } = Errors;
    exports.error = error;
    exports.exit = exit;
    exports.warn = warn;
    var uxProcessExitHandler = async () => {
      try {
        await ux.done();
      } catch (error2) {
        console.error(error2), process.exitCode = 1;
      }
    }, uxListener = process.listeners("exit").find((fn) => fn.name === uxProcessExitHandler.name);
    uxListener || process.once("exit", uxProcessExitHandler);
    var base_1 = require_base();
    Object.defineProperty(exports, "ActionBase", { enumerable: !0, get: function() {
      return base_1.ActionBase;
    } });
    var config_2 = require_config2();
    Object.defineProperty(exports, "Config", { enumerable: !0, get: function() {
      return config_2.Config;
    } });
    Object.defineProperty(exports, "config", { enumerable: !0, get: function() {
      return config_2.config;
    } });
    var exit_1 = require_exit2();
    Object.defineProperty(exports, "ExitError", { enumerable: !0, get: function() {
      return exit_1.ExitError;
    } });
    var styled_1 = require_styled();
    Object.defineProperty(exports, "Table", { enumerable: !0, get: function() {
      return styled_1.Table;
    } });
    var theme_1 = require_theme2();
    Object.defineProperty(exports, "colorize", { enumerable: !0, get: function() {
      return theme_1.colorize;
    } });
    var write_2 = require_write();
    Object.defineProperty(exports, "write", { enumerable: !0, get: function() {
      return __importDefault(write_2).default;
    } });
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/stream.js
var require_stream = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/stream.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.stderr = exports.stdout = void 0;
    var Stream = class {
      channel;
      constructor(channel) {
        this.channel = channel;
      }
      get isTTY() {
        return process[this.channel].isTTY;
      }
      emit(event, ...args) {
        return process[this.channel].emit(event, ...args);
      }
      getWindowSize() {
        return process[this.channel].getWindowSize();
      }
      on(event, listener) {
        return process[this.channel].on(event, listener), this;
      }
      once(event, listener) {
        return process[this.channel].once(event, listener), this;
      }
      read() {
        return process[this.channel].read();
      }
      write(data) {
        return process[this.channel].write(data);
      }
    };
    exports.stdout = new Stream("stdout");
    exports.stderr = new Stream("stderr");
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/performance.js
var require_performance = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/performance.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.Performance = exports.OCLIF_MARKER_OWNER = void 0;
    var node_perf_hooks_1 = __require("node:perf_hooks"), settings_1 = require_settings();
    exports.OCLIF_MARKER_OWNER = "@oclif/core";
    var Marker = class {
      owner;
      name;
      details;
      method;
      module;
      scope;
      stopped = !1;
      startMarker;
      stopMarker;
      constructor(owner, name, details = {}) {
        this.owner = owner, this.name = name, this.details = details, this.startMarker = `${this.name}-start`, this.stopMarker = `${this.name}-stop`;
        let [caller, scope] = name.split("#"), [module2, method] = caller.split(".");
        this.module = module2, this.method = method, this.scope = scope, node_perf_hooks_1.performance.mark(this.startMarker);
      }
      addDetails(details) {
        this.details = { ...this.details, ...details };
      }
      measure() {
        node_perf_hooks_1.performance.measure(this.name, this.startMarker, this.stopMarker);
      }
      stop() {
        this.stopped = !0, node_perf_hooks_1.performance.mark(this.stopMarker);
      }
    }, Performance = class _Performance {
      static _oclifPerf;
      /* Key: marker.owner */
      static _results = /* @__PURE__ */ new Map();
      /* Key: marker.name */
      static markers = /* @__PURE__ */ new Map();
      /**
       * Collect performance results into static Performance.results
       *
       * @returns Promise<void>
       */
      static async collect() {
        if (!_Performance.enabled || _Performance._results.size > 0)
          return;
        let markers = [..._Performance.markers.values()];
        if (markers.length !== 0) {
          for (let marker of markers.filter((m) => !m.stopped))
            marker.stop();
          return new Promise((resolve2) => {
            new node_perf_hooks_1.PerformanceObserver((items) => {
              for (let entry of items.getEntries()) {
                let marker = _Performance.markers.get(entry.name);
                if (marker) {
                  let result = {
                    details: marker.details,
                    duration: entry.duration,
                    method: marker.method,
                    module: marker.module,
                    name: entry.name,
                    scope: marker.scope
                  }, existing = _Performance._results.get(marker.owner) ?? [];
                  _Performance._results.set(marker.owner, [...existing, result]);
                }
              }
              let oclifResults = _Performance._results.get(exports.OCLIF_MARKER_OWNER) ?? [], command = oclifResults.find((r) => r.name.startsWith("config.runCommand")), commandLoadTime = command ? _Performance.getResult(exports.OCLIF_MARKER_OWNER, `plugin.findCommand#${command.details.plugin}.${command.details.command}`)?.duration ?? 0 : 0, pluginLoadTimes = Object.fromEntries(oclifResults.filter(({ name }) => name.startsWith("plugin.load#")).sort((a, b) => b.duration - a.duration).map(({ details, duration, scope }) => [scope, { details, duration }])), hookRunTimes = oclifResults.filter(({ name }) => name.startsWith("config.runHook#")).reduce((acc, perfResult) => {
                let event = perfResult.details.event;
                if (event)
                  acc[event] || (acc[event] = {}), acc[event][perfResult.scope] = perfResult.duration;
                else {
                  let event2 = perfResult.scope;
                  acc[event2] || (acc[event2] = {}), acc[event2].total = perfResult.duration;
                }
                return acc;
              }, {}), pluginLoadTimeByType = Object.fromEntries(oclifResults.filter(({ name }) => name.startsWith("config.loadPlugins#")).sort((a, b) => b.duration - a.duration).map(({ duration, scope }) => [scope, duration]));
              _Performance._oclifPerf = {
                hookRunTimes,
                "oclif.commandLoadMs": commandLoadTime,
                "oclif.commandRunMs": oclifResults.find(({ name }) => name.startsWith("config.runCommand#"))?.duration ?? 0,
                "oclif.configLoadMs": _Performance.getResult(exports.OCLIF_MARKER_OWNER, "config.load")?.duration ?? 0,
                "oclif.corePluginsLoadMs": pluginLoadTimeByType.core ?? 0,
                "oclif.initHookMs": hookRunTimes.init?.total ?? 0,
                "oclif.initMs": _Performance.getResult(exports.OCLIF_MARKER_OWNER, "main.run#init")?.duration ?? 0,
                "oclif.linkedPluginsLoadMs": pluginLoadTimeByType.link ?? 0,
                "oclif.postrunHookMs": hookRunTimes.postrun?.total ?? 0,
                "oclif.prerunHookMs": hookRunTimes.prerun?.total ?? 0,
                "oclif.runMs": _Performance.getResult(exports.OCLIF_MARKER_OWNER, "main.run")?.duration ?? 0,
                "oclif.userPluginsLoadMs": pluginLoadTimeByType.user ?? 0,
                pluginLoadTimes
              }, resolve2();
            }).observe({ buffered: !0, entryTypes: ["measure"] });
            for (let marker of markers)
              try {
                marker.measure();
              } catch {
              }
            node_perf_hooks_1.performance.clearMarks();
          });
        }
      }
      /**
       * Add debug logs for plugin loading performance
       * @returns void
       */
      static debug() {
        if (!_Performance.enabled)
          return;
        let oclifDebug = require_src()("oclif-perf"), processUpTime = (process.uptime() * 1e3).toFixed(4);
        oclifDebug("Process Uptime: %sms", processUpTime), oclifDebug("Oclif Time: %sms", _Performance.oclifPerf["oclif.runMs"].toFixed(4)), oclifDebug("Init Time: %sms", _Performance.oclifPerf["oclif.initMs"].toFixed(4)), oclifDebug("Config Load Time: %sms", _Performance.oclifPerf["oclif.configLoadMs"].toFixed(4)), oclifDebug("  \u2022 Root Plugin Load Time: %sms", _Performance.getResult(exports.OCLIF_MARKER_OWNER, "plugin.load#root")?.duration.toFixed(4) ?? 0), oclifDebug("  \u2022 Plugins Load Time: %sms", _Performance.getResult(exports.OCLIF_MARKER_OWNER, "config.loadAllPlugins")?.duration.toFixed(4) ?? 0), oclifDebug("  \u2022 Commands Load Time: %sms", _Performance.getResult(exports.OCLIF_MARKER_OWNER, "config.loadAllCommands")?.duration.toFixed(4) ?? 0), oclifDebug("Core Plugin Load Time: %sms", _Performance.oclifPerf["oclif.corePluginsLoadMs"].toFixed(4)), oclifDebug("User Plugin Load Time: %sms", _Performance.oclifPerf["oclif.userPluginsLoadMs"].toFixed(4)), oclifDebug("Linked Plugin Load Time: %sms", _Performance.oclifPerf["oclif.linkedPluginsLoadMs"].toFixed(4)), oclifDebug("Plugin Load Times:");
        for (let [plugin, result] of Object.entries(_Performance.oclifPerf.pluginLoadTimes))
          result.details.hasManifest ? oclifDebug(`  ${plugin}: ${result.duration.toFixed(4)}ms`) : oclifDebug(`  ${plugin}: ${result.duration.toFixed(4)}ms (no manifest!)`);
        oclifDebug("Hook Run Times:");
        for (let [event, runTimes] of Object.entries(_Performance.oclifPerf.hookRunTimes)) {
          oclifDebug(`  ${event}:`);
          for (let [plugin, duration] of Object.entries(runTimes))
            oclifDebug(`    ${plugin}: ${duration.toFixed(4)}ms`);
        }
        oclifDebug("Command Load Time: %sms", _Performance.oclifPerf["oclif.commandLoadMs"].toFixed(4)), oclifDebug("Command Run Time: %sms", _Performance.oclifPerf["oclif.commandRunMs"].toFixed(4)), _Performance.oclifPerf["oclif.configLoadMs"] > _Performance.oclifPerf["oclif.runMs"] && oclifDebug("! Config load time is greater than total oclif time. This might mean that Config was instantiated before oclif was run.");
        let nonCoreDebug = require_src()("non-oclif-perf"), nonCorePerf = _Performance.results;
        if (nonCorePerf.size > 0) {
          nonCoreDebug("Non-Core Performance Measurements:");
          for (let [owner, results] of nonCorePerf) {
            nonCoreDebug(`  ${owner}:`);
            for (let result of results)
              nonCoreDebug(`    ${result.name}: ${result.duration.toFixed(4)}ms`);
          }
        }
      }
      static get enabled() {
        return settings_1.settings.performanceEnabled ?? !1;
      }
      static getResult(owner, name) {
        return _Performance._results.get(owner)?.find((r) => r.name === name);
      }
      /**
       * Add a new performance marker
       *
       * @param owner An npm package like `@oclif/core` or `@salesforce/source-tracking`
       * @param name Name of the marker. Use `module.method#scope` format
       * @param details Arbitrary details to attach to the marker
       * @returns Marker instance
       */
      static mark(owner, name, details = {}) {
        if (!_Performance.enabled)
          return;
        let marker = new Marker(owner, name, details);
        return _Performance.markers.set(marker.name, marker), marker;
      }
      static get oclifPerf() {
        if (!_Performance.enabled)
          return {};
        if (_Performance._oclifPerf)
          return _Performance._oclifPerf;
        throw new Error("Perf results not available. Did you forget to call await Performance.collect()?");
      }
      /** returns a map of owner, PerfResult[].  Excludes oclif PerfResult, which you can get from oclifPerf */
      static get results() {
        return _Performance.enabled ? new Map([..._Performance._results.entries()].filter(([owner]) => owner !== exports.OCLIF_MARKER_OWNER)) : /* @__PURE__ */ new Map();
      }
    };
    exports.Performance = Performance;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/os.js
var require_os = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/os.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.getPlatform = exports.getHomeDir = void 0;
    var node_os_1 = __require("node:os");
    function getHomeDir() {
      return (0, node_os_1.homedir)();
    }
    exports.getHomeDir = getHomeDir;
    function getPlatform() {
      return (0, node_os_1.platform)();
    }
    exports.getPlatform = getPlatform;
  }
});

// ../../node_modules/.pnpm/balanced-match@4.0.4/node_modules/balanced-match/dist/commonjs/index.js
var require_commonjs = __commonJS({
  "../../node_modules/.pnpm/balanced-match@4.0.4/node_modules/balanced-match/dist/commonjs/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.range = exports.balanced = void 0;
    var balanced = (a, b, str) => {
      let ma = a instanceof RegExp ? maybeMatch(a, str) : a, mb = b instanceof RegExp ? maybeMatch(b, str) : b, r = ma !== null && mb != null && (0, exports.range)(ma, mb, str);
      return r && {
        start: r[0],
        end: r[1],
        pre: str.slice(0, r[0]),
        body: str.slice(r[0] + ma.length, r[1]),
        post: str.slice(r[1] + mb.length)
      };
    };
    exports.balanced = balanced;
    var maybeMatch = (reg, str) => {
      let m = str.match(reg);
      return m ? m[0] : null;
    }, range = (a, b, str) => {
      let begs, beg, left, right, result, ai = str.indexOf(a), bi = str.indexOf(b, ai + 1), i = ai;
      if (ai >= 0 && bi > 0) {
        if (a === b)
          return [ai, bi];
        for (begs = [], left = str.length; i >= 0 && !result; ) {
          if (i === ai)
            begs.push(i), ai = str.indexOf(a, i + 1);
          else if (begs.length === 1) {
            let r = begs.pop();
            r !== void 0 && (result = [r, bi]);
          } else
            beg = begs.pop(), beg !== void 0 && beg < left && (left = beg, right = bi), bi = str.indexOf(b, i + 1);
          i = ai < bi && ai >= 0 ? ai : bi;
        }
        begs.length && right !== void 0 && (result = [left, right]);
      }
      return result;
    };
    exports.range = range;
  }
});

// ../../node_modules/.pnpm/brace-expansion@5.0.4/node_modules/brace-expansion/dist/commonjs/index.js
var require_commonjs2 = __commonJS({
  "../../node_modules/.pnpm/brace-expansion@5.0.4/node_modules/brace-expansion/dist/commonjs/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.EXPANSION_MAX = void 0;
    exports.expand = expand;
    var balanced_match_1 = require_commonjs(), escSlash = "\0SLASH" + Math.random() + "\0", escOpen = "\0OPEN" + Math.random() + "\0", escClose = "\0CLOSE" + Math.random() + "\0", escComma = "\0COMMA" + Math.random() + "\0", escPeriod = "\0PERIOD" + Math.random() + "\0", escSlashPattern = new RegExp(escSlash, "g"), escOpenPattern = new RegExp(escOpen, "g"), escClosePattern = new RegExp(escClose, "g"), escCommaPattern = new RegExp(escComma, "g"), escPeriodPattern = new RegExp(escPeriod, "g"), slashPattern = /\\\\/g, openPattern = /\\{/g, closePattern = /\\}/g, commaPattern = /\\,/g, periodPattern = /\\\./g;
    exports.EXPANSION_MAX = 1e5;
    function numeric(str) {
      return isNaN(str) ? str.charCodeAt(0) : parseInt(str, 10);
    }
    function escapeBraces(str) {
      return str.replace(slashPattern, escSlash).replace(openPattern, escOpen).replace(closePattern, escClose).replace(commaPattern, escComma).replace(periodPattern, escPeriod);
    }
    function unescapeBraces(str) {
      return str.replace(escSlashPattern, "\\").replace(escOpenPattern, "{").replace(escClosePattern, "}").replace(escCommaPattern, ",").replace(escPeriodPattern, ".");
    }
    function parseCommaParts(str) {
      if (!str)
        return [""];
      let parts = [], m = (0, balanced_match_1.balanced)("{", "}", str);
      if (!m)
        return str.split(",");
      let { pre, body, post } = m, p = pre.split(",");
      p[p.length - 1] += "{" + body + "}";
      let postParts = parseCommaParts(post);
      return post.length && (p[p.length - 1] += postParts.shift(), p.push.apply(p, postParts)), parts.push.apply(parts, p), parts;
    }
    function expand(str, options = {}) {
      if (!str)
        return [];
      let { max = exports.EXPANSION_MAX } = options;
      return str.slice(0, 2) === "{}" && (str = "\\{\\}" + str.slice(2)), expand_(escapeBraces(str), max, !0).map(unescapeBraces);
    }
    function embrace(str) {
      return "{" + str + "}";
    }
    function isPadded(el) {
      return /^-?0\d/.test(el);
    }
    function lte(i, y) {
      return i <= y;
    }
    function gte(i, y) {
      return i >= y;
    }
    function expand_(str, max, isTop) {
      let expansions = [], m = (0, balanced_match_1.balanced)("{", "}", str);
      if (!m)
        return [str];
      let pre = m.pre, post = m.post.length ? expand_(m.post, max, !1) : [""];
      if (/\$$/.test(m.pre))
        for (let k = 0; k < post.length && k < max; k++) {
          let expansion = pre + "{" + m.body + "}" + post[k];
          expansions.push(expansion);
        }
      else {
        let isNumericSequence = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(m.body), isAlphaSequence = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(m.body), isSequence = isNumericSequence || isAlphaSequence, isOptions = m.body.indexOf(",") >= 0;
        if (!isSequence && !isOptions)
          return m.post.match(/,(?!,).*\}/) ? (str = m.pre + "{" + m.body + escClose + m.post, expand_(str, max, !0)) : [str];
        let n;
        if (isSequence)
          n = m.body.split(/\.\./);
        else if (n = parseCommaParts(m.body), n.length === 1 && n[0] !== void 0 && (n = expand_(n[0], max, !1).map(embrace), n.length === 1))
          return post.map((p) => m.pre + n[0] + p);
        let N;
        if (isSequence && n[0] !== void 0 && n[1] !== void 0) {
          let x = numeric(n[0]), y = numeric(n[1]), width = Math.max(n[0].length, n[1].length), incr = n.length === 3 && n[2] !== void 0 ? Math.abs(numeric(n[2])) : 1, test = lte;
          y < x && (incr *= -1, test = gte);
          let pad = n.some(isPadded);
          N = [];
          for (let i = x; test(i, y); i += incr) {
            let c;
            if (isAlphaSequence)
              c = String.fromCharCode(i), c === "\\" && (c = "");
            else if (c = String(i), pad) {
              let need = width - c.length;
              if (need > 0) {
                let z = new Array(need + 1).join("0");
                i < 0 ? c = "-" + z + c.slice(1) : c = z + c;
              }
            }
            N.push(c);
          }
        } else {
          N = [];
          for (let j = 0; j < n.length; j++)
            N.push.apply(N, expand_(n[j], max, !1));
        }
        for (let j = 0; j < N.length; j++)
          for (let k = 0; k < post.length && expansions.length < max; k++) {
            let expansion = pre + N[j] + post[k];
            (!isTop || isSequence || expansion) && expansions.push(expansion);
          }
      }
      return expansions;
    }
  }
});

// ../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/assert-valid-pattern.js
var require_assert_valid_pattern = __commonJS({
  "../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/assert-valid-pattern.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.assertValidPattern = void 0;
    var MAX_PATTERN_LENGTH = 1024 * 64, assertValidPattern = (pattern) => {
      if (typeof pattern != "string")
        throw new TypeError("invalid pattern");
      if (pattern.length > MAX_PATTERN_LENGTH)
        throw new TypeError("pattern is too long");
    };
    exports.assertValidPattern = assertValidPattern;
  }
});

// ../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/brace-expressions.js
var require_brace_expressions = __commonJS({
  "../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/brace-expressions.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.parseClass = void 0;
    var posixClasses = {
      "[:alnum:]": ["\\p{L}\\p{Nl}\\p{Nd}", !0],
      "[:alpha:]": ["\\p{L}\\p{Nl}", !0],
      "[:ascii:]": ["\\x00-\\x7f", !1],
      "[:blank:]": ["\\p{Zs}\\t", !0],
      "[:cntrl:]": ["\\p{Cc}", !0],
      "[:digit:]": ["\\p{Nd}", !0],
      "[:graph:]": ["\\p{Z}\\p{C}", !0, !0],
      "[:lower:]": ["\\p{Ll}", !0],
      "[:print:]": ["\\p{C}", !0],
      "[:punct:]": ["\\p{P}", !0],
      "[:space:]": ["\\p{Z}\\t\\r\\n\\v\\f", !0],
      "[:upper:]": ["\\p{Lu}", !0],
      "[:word:]": ["\\p{L}\\p{Nl}\\p{Nd}\\p{Pc}", !0],
      "[:xdigit:]": ["A-Fa-f0-9", !1]
    }, braceEscape = (s) => s.replace(/[[\]\\-]/g, "\\$&"), regexpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), rangesToString = (ranges) => ranges.join(""), parseClass = (glob2, position) => {
      let pos = position;
      if (glob2.charAt(pos) !== "[")
        throw new Error("not in a brace expression");
      let ranges = [], negs = [], i = pos + 1, sawStart = !1, uflag = !1, escaping = !1, negate = !1, endPos = pos, rangeStart = "";
      WHILE: for (; i < glob2.length; ) {
        let c = glob2.charAt(i);
        if ((c === "!" || c === "^") && i === pos + 1) {
          negate = !0, i++;
          continue;
        }
        if (c === "]" && sawStart && !escaping) {
          endPos = i + 1;
          break;
        }
        if (sawStart = !0, c === "\\" && !escaping) {
          escaping = !0, i++;
          continue;
        }
        if (c === "[" && !escaping) {
          for (let [cls, [unip, u, neg]] of Object.entries(posixClasses))
            if (glob2.startsWith(cls, i)) {
              if (rangeStart)
                return ["$.", !1, glob2.length - pos, !0];
              i += cls.length, neg ? negs.push(unip) : ranges.push(unip), uflag = uflag || u;
              continue WHILE;
            }
        }
        if (escaping = !1, rangeStart) {
          c > rangeStart ? ranges.push(braceEscape(rangeStart) + "-" + braceEscape(c)) : c === rangeStart && ranges.push(braceEscape(c)), rangeStart = "", i++;
          continue;
        }
        if (glob2.startsWith("-]", i + 1)) {
          ranges.push(braceEscape(c + "-")), i += 2;
          continue;
        }
        if (glob2.startsWith("-", i + 1)) {
          rangeStart = c, i += 2;
          continue;
        }
        ranges.push(braceEscape(c)), i++;
      }
      if (endPos < i)
        return ["", !1, 0, !1];
      if (!ranges.length && !negs.length)
        return ["$.", !1, glob2.length - pos, !0];
      if (negs.length === 0 && ranges.length === 1 && /^\\?.$/.test(ranges[0]) && !negate) {
        let r = ranges[0].length === 2 ? ranges[0].slice(-1) : ranges[0];
        return [regexpEscape(r), !1, endPos - pos, !1];
      }
      let sranges = "[" + (negate ? "^" : "") + rangesToString(ranges) + "]", snegs = "[" + (negate ? "" : "^") + rangesToString(negs) + "]";
      return [ranges.length && negs.length ? "(" + sranges + "|" + snegs + ")" : ranges.length ? sranges : snegs, uflag, endPos - pos, !0];
    };
    exports.parseClass = parseClass;
  }
});

// ../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/unescape.js
var require_unescape = __commonJS({
  "../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/unescape.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.unescape = void 0;
    var unescape = (s, { windowsPathsNoEscape = !1 } = {}) => windowsPathsNoEscape ? s.replace(/\[([^\/\\])\]/g, "$1") : s.replace(/((?!\\).|^)\[([^\/\\])\]/g, "$1$2").replace(/\\([^\/])/g, "$1");
    exports.unescape = unescape;
  }
});

// ../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/ast.js
var require_ast = __commonJS({
  "../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/ast.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.AST = void 0;
    var brace_expressions_js_1 = require_brace_expressions(), unescape_js_1 = require_unescape(), types = /* @__PURE__ */ new Set(["!", "?", "+", "*", "@"]), isExtglobType = (c) => types.has(c), startNoTraversal = "(?!(?:^|/)\\.\\.?(?:$|/))", startNoDot = "(?!\\.)", addPatternStart = /* @__PURE__ */ new Set(["[", "."]), justDots = /* @__PURE__ */ new Set(["..", "."]), reSpecials = new Set("().*{}+?[]^$\\!"), regExpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), qmark = "[^/]", star = qmark + "*?", starNoEmpty = qmark + "+?", AST = class _AST {
      type;
      #root;
      #hasMagic;
      #uflag = !1;
      #parts = [];
      #parent;
      #parentIndex;
      #negs;
      #filledNegs = !1;
      #options;
      #toString;
      // set to true if it's an extglob with no children
      // (which really means one child of '')
      #emptyExt = !1;
      constructor(type, parent, options = {}) {
        this.type = type, type && (this.#hasMagic = !0), this.#parent = parent, this.#root = this.#parent ? this.#parent.#root : this, this.#options = this.#root === this ? options : this.#root.#options, this.#negs = this.#root === this ? [] : this.#root.#negs, type === "!" && !this.#root.#filledNegs && this.#negs.push(this), this.#parentIndex = this.#parent ? this.#parent.#parts.length : 0;
      }
      get hasMagic() {
        if (this.#hasMagic !== void 0)
          return this.#hasMagic;
        for (let p of this.#parts)
          if (typeof p != "string" && (p.type || p.hasMagic))
            return this.#hasMagic = !0;
        return this.#hasMagic;
      }
      // reconstructs the pattern
      toString() {
        return this.#toString !== void 0 ? this.#toString : this.type ? this.#toString = this.type + "(" + this.#parts.map((p) => String(p)).join("|") + ")" : this.#toString = this.#parts.map((p) => String(p)).join("");
      }
      #fillNegs() {
        if (this !== this.#root)
          throw new Error("should only call on root");
        if (this.#filledNegs)
          return this;
        this.toString(), this.#filledNegs = !0;
        let n;
        for (; n = this.#negs.pop(); ) {
          if (n.type !== "!")
            continue;
          let p = n, pp = p.#parent;
          for (; pp; ) {
            for (let i = p.#parentIndex + 1; !pp.type && i < pp.#parts.length; i++)
              for (let part of n.#parts) {
                if (typeof part == "string")
                  throw new Error("string part in extglob AST??");
                part.copyIn(pp.#parts[i]);
              }
            p = pp, pp = p.#parent;
          }
        }
        return this;
      }
      push(...parts) {
        for (let p of parts)
          if (p !== "") {
            if (typeof p != "string" && !(p instanceof _AST && p.#parent === this))
              throw new Error("invalid part: " + p);
            this.#parts.push(p);
          }
      }
      toJSON() {
        let ret = this.type === null ? this.#parts.slice().map((p) => typeof p == "string" ? p : p.toJSON()) : [this.type, ...this.#parts.map((p) => p.toJSON())];
        return this.isStart() && !this.type && ret.unshift([]), this.isEnd() && (this === this.#root || this.#root.#filledNegs && this.#parent?.type === "!") && ret.push({}), ret;
      }
      isStart() {
        if (this.#root === this)
          return !0;
        if (!this.#parent?.isStart())
          return !1;
        if (this.#parentIndex === 0)
          return !0;
        let p = this.#parent;
        for (let i = 0; i < this.#parentIndex; i++) {
          let pp = p.#parts[i];
          if (!(pp instanceof _AST && pp.type === "!"))
            return !1;
        }
        return !0;
      }
      isEnd() {
        if (this.#root === this || this.#parent?.type === "!")
          return !0;
        if (!this.#parent?.isEnd())
          return !1;
        if (!this.type)
          return this.#parent?.isEnd();
        let pl = this.#parent ? this.#parent.#parts.length : 0;
        return this.#parentIndex === pl - 1;
      }
      copyIn(part) {
        typeof part == "string" ? this.push(part) : this.push(part.clone(this));
      }
      clone(parent) {
        let c = new _AST(this.type, parent);
        for (let p of this.#parts)
          c.copyIn(p);
        return c;
      }
      static #parseAST(str, ast, pos, opt) {
        let escaping = !1, inBrace = !1, braceStart = -1, braceNeg = !1;
        if (ast.type === null) {
          let i2 = pos, acc2 = "";
          for (; i2 < str.length; ) {
            let c = str.charAt(i2++);
            if (escaping || c === "\\") {
              escaping = !escaping, acc2 += c;
              continue;
            }
            if (inBrace) {
              i2 === braceStart + 1 ? (c === "^" || c === "!") && (braceNeg = !0) : c === "]" && !(i2 === braceStart + 2 && braceNeg) && (inBrace = !1), acc2 += c;
              continue;
            } else if (c === "[") {
              inBrace = !0, braceStart = i2, braceNeg = !1, acc2 += c;
              continue;
            }
            if (!opt.noext && isExtglobType(c) && str.charAt(i2) === "(") {
              ast.push(acc2), acc2 = "";
              let ext = new _AST(c, ast);
              i2 = _AST.#parseAST(str, ext, i2, opt), ast.push(ext);
              continue;
            }
            acc2 += c;
          }
          return ast.push(acc2), i2;
        }
        let i = pos + 1, part = new _AST(null, ast), parts = [], acc = "";
        for (; i < str.length; ) {
          let c = str.charAt(i++);
          if (escaping || c === "\\") {
            escaping = !escaping, acc += c;
            continue;
          }
          if (inBrace) {
            i === braceStart + 1 ? (c === "^" || c === "!") && (braceNeg = !0) : c === "]" && !(i === braceStart + 2 && braceNeg) && (inBrace = !1), acc += c;
            continue;
          } else if (c === "[") {
            inBrace = !0, braceStart = i, braceNeg = !1, acc += c;
            continue;
          }
          if (isExtglobType(c) && str.charAt(i) === "(") {
            part.push(acc), acc = "";
            let ext = new _AST(c, part);
            part.push(ext), i = _AST.#parseAST(str, ext, i, opt);
            continue;
          }
          if (c === "|") {
            part.push(acc), acc = "", parts.push(part), part = new _AST(null, ast);
            continue;
          }
          if (c === ")")
            return acc === "" && ast.#parts.length === 0 && (ast.#emptyExt = !0), part.push(acc), acc = "", ast.push(...parts, part), i;
          acc += c;
        }
        return ast.type = null, ast.#hasMagic = void 0, ast.#parts = [str.substring(pos - 1)], i;
      }
      static fromGlob(pattern, options = {}) {
        let ast = new _AST(null, void 0, options);
        return _AST.#parseAST(pattern, ast, 0, options), ast;
      }
      // returns the regular expression if there's magic, or the unescaped
      // string if not.
      toMMPattern() {
        if (this !== this.#root)
          return this.#root.toMMPattern();
        let glob2 = this.toString(), [re, body, hasMagic, uflag] = this.toRegExpSource();
        if (!(hasMagic || this.#hasMagic || this.#options.nocase && !this.#options.nocaseMagicOnly && glob2.toUpperCase() !== glob2.toLowerCase()))
          return body;
        let flags = (this.#options.nocase ? "i" : "") + (uflag ? "u" : "");
        return Object.assign(new RegExp(`^${re}$`, flags), {
          _src: re,
          _glob: glob2
        });
      }
      get options() {
        return this.#options;
      }
      // returns the string match, the regexp source, whether there's magic
      // in the regexp (so a regular expression is required) and whether or
      // not the uflag is needed for the regular expression (for posix classes)
      // TODO: instead of injecting the start/end at this point, just return
      // the BODY of the regexp, along with the start/end portions suitable
      // for binding the start/end in either a joined full-path makeRe context
      // (where we bind to (^|/), or a standalone matchPart context (where
      // we bind to ^, and not /).  Otherwise slashes get duped!
      //
      // In part-matching mode, the start is:
      // - if not isStart: nothing
      // - if traversal possible, but not allowed: ^(?!\.\.?$)
      // - if dots allowed or not possible: ^
      // - if dots possible and not allowed: ^(?!\.)
      // end is:
      // - if not isEnd(): nothing
      // - else: $
      //
      // In full-path matching mode, we put the slash at the START of the
      // pattern, so start is:
      // - if first pattern: same as part-matching mode
      // - if not isStart(): nothing
      // - if traversal possible, but not allowed: /(?!\.\.?(?:$|/))
      // - if dots allowed or not possible: /
      // - if dots possible and not allowed: /(?!\.)
      // end is:
      // - if last pattern, same as part-matching mode
      // - else nothing
      //
      // Always put the (?:$|/) on negated tails, though, because that has to be
      // there to bind the end of the negated pattern portion, and it's easier to
      // just stick it in now rather than try to inject it later in the middle of
      // the pattern.
      //
      // We can just always return the same end, and leave it up to the caller
      // to know whether it's going to be used joined or in parts.
      // And, if the start is adjusted slightly, can do the same there:
      // - if not isStart: nothing
      // - if traversal possible, but not allowed: (?:/|^)(?!\.\.?$)
      // - if dots allowed or not possible: (?:/|^)
      // - if dots possible and not allowed: (?:/|^)(?!\.)
      //
      // But it's better to have a simpler binding without a conditional, for
      // performance, so probably better to return both start options.
      //
      // Then the caller just ignores the end if it's not the first pattern,
      // and the start always gets applied.
      //
      // But that's always going to be $ if it's the ending pattern, or nothing,
      // so the caller can just attach $ at the end of the pattern when building.
      //
      // So the todo is:
      // - better detect what kind of start is needed
      // - return both flavors of starting pattern
      // - attach $ at the end of the pattern when creating the actual RegExp
      //
      // Ah, but wait, no, that all only applies to the root when the first pattern
      // is not an extglob. If the first pattern IS an extglob, then we need all
      // that dot prevention biz to live in the extglob portions, because eg
      // +(*|.x*) can match .xy but not .yx.
      //
      // So, return the two flavors if it's #root and the first child is not an
      // AST, otherwise leave it to the child AST to handle it, and there,
      // use the (?:^|/) style of start binding.
      //
      // Even simplified further:
      // - Since the start for a join is eg /(?!\.) and the start for a part
      // is ^(?!\.), we can just prepend (?!\.) to the pattern (either root
      // or start or whatever) and prepend ^ or / at the Regexp construction.
      toRegExpSource(allowDot) {
        let dot = allowDot ?? !!this.#options.dot;
        if (this.#root === this && this.#fillNegs(), !this.type) {
          let noEmpty = this.isStart() && this.isEnd(), src = this.#parts.map((p) => {
            let [re, _, hasMagic, uflag] = typeof p == "string" ? _AST.#parseGlob(p, this.#hasMagic, noEmpty) : p.toRegExpSource(allowDot);
            return this.#hasMagic = this.#hasMagic || hasMagic, this.#uflag = this.#uflag || uflag, re;
          }).join(""), start2 = "";
          if (this.isStart() && typeof this.#parts[0] == "string" && !(this.#parts.length === 1 && justDots.has(this.#parts[0]))) {
            let aps = addPatternStart, needNoTrav = (
              // dots are allowed, and the pattern starts with [ or .
              dot && aps.has(src.charAt(0)) || // the pattern starts with \., and then [ or .
              src.startsWith("\\.") && aps.has(src.charAt(2)) || // the pattern starts with \.\., and then [ or .
              src.startsWith("\\.\\.") && aps.has(src.charAt(4))
            ), needNoDot = !dot && !allowDot && aps.has(src.charAt(0));
            start2 = needNoTrav ? startNoTraversal : needNoDot ? startNoDot : "";
          }
          let end = "";
          return this.isEnd() && this.#root.#filledNegs && this.#parent?.type === "!" && (end = "(?:$|\\/)"), [
            start2 + src + end,
            (0, unescape_js_1.unescape)(src),
            this.#hasMagic = !!this.#hasMagic,
            this.#uflag
          ];
        }
        let repeated = this.type === "*" || this.type === "+", start = this.type === "!" ? "(?:(?!(?:" : "(?:", body = this.#partsToRegExp(dot);
        if (this.isStart() && this.isEnd() && !body && this.type !== "!") {
          let s = this.toString();
          return this.#parts = [s], this.type = null, this.#hasMagic = void 0, [s, (0, unescape_js_1.unescape)(this.toString()), !1, !1];
        }
        let bodyDotAllowed = !repeated || allowDot || dot || !startNoDot ? "" : this.#partsToRegExp(!0);
        bodyDotAllowed === body && (bodyDotAllowed = ""), bodyDotAllowed && (body = `(?:${body})(?:${bodyDotAllowed})*?`);
        let final = "";
        if (this.type === "!" && this.#emptyExt)
          final = (this.isStart() && !dot ? startNoDot : "") + starNoEmpty;
        else {
          let close = this.type === "!" ? (
            // !() must match something,but !(x) can match ''
            "))" + (this.isStart() && !dot && !allowDot ? startNoDot : "") + star + ")"
          ) : this.type === "@" ? ")" : this.type === "?" ? ")?" : this.type === "+" && bodyDotAllowed ? ")" : this.type === "*" && bodyDotAllowed ? ")?" : `)${this.type}`;
          final = start + body + close;
        }
        return [
          final,
          (0, unescape_js_1.unescape)(body),
          this.#hasMagic = !!this.#hasMagic,
          this.#uflag
        ];
      }
      #partsToRegExp(dot) {
        return this.#parts.map((p) => {
          if (typeof p == "string")
            throw new Error("string type in extglob ast??");
          let [re, _, _hasMagic, uflag] = p.toRegExpSource(dot);
          return this.#uflag = this.#uflag || uflag, re;
        }).filter((p) => !(this.isStart() && this.isEnd()) || !!p).join("|");
      }
      static #parseGlob(glob2, hasMagic, noEmpty = !1) {
        let escaping = !1, re = "", uflag = !1, inStar = !1;
        for (let i = 0; i < glob2.length; i++) {
          let c = glob2.charAt(i);
          if (escaping) {
            escaping = !1, re += (reSpecials.has(c) ? "\\" : "") + c, inStar = !1;
            continue;
          }
          if (c === "\\") {
            i === glob2.length - 1 ? re += "\\\\" : escaping = !0;
            continue;
          }
          if (c === "[") {
            let [src, needUflag, consumed, magic] = (0, brace_expressions_js_1.parseClass)(glob2, i);
            if (consumed) {
              re += src, uflag = uflag || needUflag, i += consumed - 1, hasMagic = hasMagic || magic, inStar = !1;
              continue;
            }
          }
          if (c === "*") {
            if (inStar)
              continue;
            inStar = !0, re += noEmpty && /^[*]+$/.test(glob2) ? starNoEmpty : star, hasMagic = !0;
            continue;
          } else
            inStar = !1;
          if (c === "?") {
            re += qmark, hasMagic = !0;
            continue;
          }
          re += regExpEscape(c);
        }
        return [re, (0, unescape_js_1.unescape)(glob2), !!hasMagic, uflag];
      }
    };
    exports.AST = AST;
  }
});

// ../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/escape.js
var require_escape = __commonJS({
  "../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/escape.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.escape = void 0;
    var escape = (s, { windowsPathsNoEscape = !1 } = {}) => windowsPathsNoEscape ? s.replace(/[?*()[\]]/g, "[$&]") : s.replace(/[?*()[\]\\]/g, "\\$&");
    exports.escape = escape;
  }
});

// ../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/index.js
var require_commonjs3 = __commonJS({
  "../../node_modules/.pnpm/minimatch@9.0.6/node_modules/minimatch/dist/commonjs/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.unescape = exports.escape = exports.AST = exports.Minimatch = exports.match = exports.makeRe = exports.braceExpand = exports.defaults = exports.filter = exports.GLOBSTAR = exports.sep = exports.minimatch = void 0;
    var brace_expansion_1 = require_commonjs2(), assert_valid_pattern_js_1 = require_assert_valid_pattern(), ast_js_1 = require_ast(), escape_js_1 = require_escape(), unescape_js_1 = require_unescape(), minimatch = (p, pattern, options = {}) => ((0, assert_valid_pattern_js_1.assertValidPattern)(pattern), !options.nocomment && pattern.charAt(0) === "#" ? !1 : new Minimatch(pattern, options).match(p));
    exports.minimatch = minimatch;
    var starDotExtRE = /^\*+([^+@!?\*\[\(]*)$/, starDotExtTest = (ext2) => (f) => !f.startsWith(".") && f.endsWith(ext2), starDotExtTestDot = (ext2) => (f) => f.endsWith(ext2), starDotExtTestNocase = (ext2) => (ext2 = ext2.toLowerCase(), (f) => !f.startsWith(".") && f.toLowerCase().endsWith(ext2)), starDotExtTestNocaseDot = (ext2) => (ext2 = ext2.toLowerCase(), (f) => f.toLowerCase().endsWith(ext2)), starDotStarRE = /^\*+\.\*+$/, starDotStarTest = (f) => !f.startsWith(".") && f.includes("."), starDotStarTestDot = (f) => f !== "." && f !== ".." && f.includes("."), dotStarRE = /^\.\*+$/, dotStarTest = (f) => f !== "." && f !== ".." && f.startsWith("."), starRE = /^\*+$/, starTest = (f) => f.length !== 0 && !f.startsWith("."), starTestDot = (f) => f.length !== 0 && f !== "." && f !== "..", qmarksRE = /^\?+([^+@!?\*\[\(]*)?$/, qmarksTestNocase = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExt([$0]);
      return ext2 ? (ext2 = ext2.toLowerCase(), (f) => noext(f) && f.toLowerCase().endsWith(ext2)) : noext;
    }, qmarksTestNocaseDot = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExtDot([$0]);
      return ext2 ? (ext2 = ext2.toLowerCase(), (f) => noext(f) && f.toLowerCase().endsWith(ext2)) : noext;
    }, qmarksTestDot = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExtDot([$0]);
      return ext2 ? (f) => noext(f) && f.endsWith(ext2) : noext;
    }, qmarksTest = ([$0, ext2 = ""]) => {
      let noext = qmarksTestNoExt([$0]);
      return ext2 ? (f) => noext(f) && f.endsWith(ext2) : noext;
    }, qmarksTestNoExt = ([$0]) => {
      let len = $0.length;
      return (f) => f.length === len && !f.startsWith(".");
    }, qmarksTestNoExtDot = ([$0]) => {
      let len = $0.length;
      return (f) => f.length === len && f !== "." && f !== "..";
    }, defaultPlatform = typeof process == "object" && process ? typeof process.env == "object" && process.env && process.env.__MINIMATCH_TESTING_PLATFORM__ || process.platform : "posix", path3 = {
      win32: { sep: "\\" },
      posix: { sep: "/" }
    };
    exports.sep = defaultPlatform === "win32" ? path3.win32.sep : path3.posix.sep;
    exports.minimatch.sep = exports.sep;
    exports.GLOBSTAR = /* @__PURE__ */ Symbol("globstar **");
    exports.minimatch.GLOBSTAR = exports.GLOBSTAR;
    var qmark = "[^/]", star = qmark + "*?", twoStarDot = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?", twoStarNoDot = "(?:(?!(?:\\/|^)\\.).)*?", filter = (pattern, options = {}) => (p) => (0, exports.minimatch)(p, pattern, options);
    exports.filter = filter;
    exports.minimatch.filter = exports.filter;
    var ext = (a, b = {}) => Object.assign({}, a, b), defaults = (def) => {
      if (!def || typeof def != "object" || !Object.keys(def).length)
        return exports.minimatch;
      let orig = exports.minimatch;
      return Object.assign((p, pattern, options = {}) => orig(p, pattern, ext(def, options)), {
        Minimatch: class extends orig.Minimatch {
          constructor(pattern, options = {}) {
            super(pattern, ext(def, options));
          }
          static defaults(options) {
            return orig.defaults(ext(def, options)).Minimatch;
          }
        },
        AST: class extends orig.AST {
          /* c8 ignore start */
          constructor(type, parent, options = {}) {
            super(type, parent, ext(def, options));
          }
          /* c8 ignore stop */
          static fromGlob(pattern, options = {}) {
            return orig.AST.fromGlob(pattern, ext(def, options));
          }
        },
        unescape: (s, options = {}) => orig.unescape(s, ext(def, options)),
        escape: (s, options = {}) => orig.escape(s, ext(def, options)),
        filter: (pattern, options = {}) => orig.filter(pattern, ext(def, options)),
        defaults: (options) => orig.defaults(ext(def, options)),
        makeRe: (pattern, options = {}) => orig.makeRe(pattern, ext(def, options)),
        braceExpand: (pattern, options = {}) => orig.braceExpand(pattern, ext(def, options)),
        match: (list, pattern, options = {}) => orig.match(list, pattern, ext(def, options)),
        sep: orig.sep,
        GLOBSTAR: exports.GLOBSTAR
      });
    };
    exports.defaults = defaults;
    exports.minimatch.defaults = exports.defaults;
    var braceExpand = (pattern, options = {}) => ((0, assert_valid_pattern_js_1.assertValidPattern)(pattern), options.nobrace || !/\{(?:(?!\{).)*\}/.test(pattern) ? [pattern] : (0, brace_expansion_1.expand)(pattern));
    exports.braceExpand = braceExpand;
    exports.minimatch.braceExpand = exports.braceExpand;
    var makeRe = (pattern, options = {}) => new Minimatch(pattern, options).makeRe();
    exports.makeRe = makeRe;
    exports.minimatch.makeRe = exports.makeRe;
    var match = (list, pattern, options = {}) => {
      let mm = new Minimatch(pattern, options);
      return list = list.filter((f) => mm.match(f)), mm.options.nonull && !list.length && list.push(pattern), list;
    };
    exports.match = match;
    exports.minimatch.match = exports.match;
    var globMagic = /[?*]|[+@!]\(.*?\)|\[|\]/, regExpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), Minimatch = class {
      options;
      set;
      pattern;
      windowsPathsNoEscape;
      nonegate;
      negate;
      comment;
      empty;
      preserveMultipleSlashes;
      partial;
      globSet;
      globParts;
      nocase;
      isWindows;
      platform;
      windowsNoMagicRoot;
      regexp;
      constructor(pattern, options = {}) {
        (0, assert_valid_pattern_js_1.assertValidPattern)(pattern), options = options || {}, this.options = options, this.pattern = pattern, this.platform = options.platform || defaultPlatform, this.isWindows = this.platform === "win32", this.windowsPathsNoEscape = !!options.windowsPathsNoEscape || options.allowWindowsEscape === !1, this.windowsPathsNoEscape && (this.pattern = this.pattern.replace(/\\/g, "/")), this.preserveMultipleSlashes = !!options.preserveMultipleSlashes, this.regexp = null, this.negate = !1, this.nonegate = !!options.nonegate, this.comment = !1, this.empty = !1, this.partial = !!options.partial, this.nocase = !!this.options.nocase, this.windowsNoMagicRoot = options.windowsNoMagicRoot !== void 0 ? options.windowsNoMagicRoot : !!(this.isWindows && this.nocase), this.globSet = [], this.globParts = [], this.set = [], this.make();
      }
      hasMagic() {
        if (this.options.magicalBraces && this.set.length > 1)
          return !0;
        for (let pattern of this.set)
          for (let part of pattern)
            if (typeof part != "string")
              return !0;
        return !1;
      }
      debug(..._) {
      }
      make() {
        let pattern = this.pattern, options = this.options;
        if (!options.nocomment && pattern.charAt(0) === "#") {
          this.comment = !0;
          return;
        }
        if (!pattern) {
          this.empty = !0;
          return;
        }
        this.parseNegate(), this.globSet = [...new Set(this.braceExpand())], options.debug && (this.debug = (...args) => console.error(...args)), this.debug(this.pattern, this.globSet);
        let rawGlobParts = this.globSet.map((s) => this.slashSplit(s));
        this.globParts = this.preprocess(rawGlobParts), this.debug(this.pattern, this.globParts);
        let set = this.globParts.map((s, _, __) => {
          if (this.isWindows && this.windowsNoMagicRoot) {
            let isUNC = s[0] === "" && s[1] === "" && (s[2] === "?" || !globMagic.test(s[2])) && !globMagic.test(s[3]), isDrive = /^[a-z]:/i.test(s[0]);
            if (isUNC)
              return [...s.slice(0, 4), ...s.slice(4).map((ss) => this.parse(ss))];
            if (isDrive)
              return [s[0], ...s.slice(1).map((ss) => this.parse(ss))];
          }
          return s.map((ss) => this.parse(ss));
        });
        if (this.debug(this.pattern, set), this.set = set.filter((s) => s.indexOf(!1) === -1), this.isWindows)
          for (let i = 0; i < this.set.length; i++) {
            let p = this.set[i];
            p[0] === "" && p[1] === "" && this.globParts[i][2] === "?" && typeof p[3] == "string" && /^[a-z]:$/i.test(p[3]) && (p[2] = "?");
          }
        this.debug(this.pattern, this.set);
      }
      // various transforms to equivalent pattern sets that are
      // faster to process in a filesystem walk.  The goal is to
      // eliminate what we can, and push all ** patterns as far
      // to the right as possible, even if it increases the number
      // of patterns that we have to process.
      preprocess(globParts) {
        if (this.options.noglobstar)
          for (let i = 0; i < globParts.length; i++)
            for (let j = 0; j < globParts[i].length; j++)
              globParts[i][j] === "**" && (globParts[i][j] = "*");
        let { optimizationLevel = 1 } = this.options;
        return optimizationLevel >= 2 ? (globParts = this.firstPhasePreProcess(globParts), globParts = this.secondPhasePreProcess(globParts)) : optimizationLevel >= 1 ? globParts = this.levelOneOptimize(globParts) : globParts = this.adjascentGlobstarOptimize(globParts), globParts;
      }
      // just get rid of adjascent ** portions
      adjascentGlobstarOptimize(globParts) {
        return globParts.map((parts) => {
          let gs = -1;
          for (; (gs = parts.indexOf("**", gs + 1)) !== -1; ) {
            let i = gs;
            for (; parts[i + 1] === "**"; )
              i++;
            i !== gs && parts.splice(gs, i - gs);
          }
          return parts;
        });
      }
      // get rid of adjascent ** and resolve .. portions
      levelOneOptimize(globParts) {
        return globParts.map((parts) => (parts = parts.reduce((set, part) => {
          let prev = set[set.length - 1];
          return part === "**" && prev === "**" ? set : part === ".." && prev && prev !== ".." && prev !== "." && prev !== "**" ? (set.pop(), set) : (set.push(part), set);
        }, []), parts.length === 0 ? [""] : parts));
      }
      levelTwoFileOptimize(parts) {
        Array.isArray(parts) || (parts = this.slashSplit(parts));
        let didSomething = !1;
        do {
          if (didSomething = !1, !this.preserveMultipleSlashes) {
            for (let i = 1; i < parts.length - 1; i++) {
              let p = parts[i];
              i === 1 && p === "" && parts[0] === "" || (p === "." || p === "") && (didSomething = !0, parts.splice(i, 1), i--);
            }
            parts[0] === "." && parts.length === 2 && (parts[1] === "." || parts[1] === "") && (didSomething = !0, parts.pop());
          }
          let dd = 0;
          for (; (dd = parts.indexOf("..", dd + 1)) !== -1; ) {
            let p = parts[dd - 1];
            p && p !== "." && p !== ".." && p !== "**" && (didSomething = !0, parts.splice(dd - 1, 2), dd -= 2);
          }
        } while (didSomething);
        return parts.length === 0 ? [""] : parts;
      }
      // First phase: single-pattern processing
      // <pre> is 1 or more portions
      // <rest> is 1 or more portions
      // <p> is any portion other than ., .., '', or **
      // <e> is . or ''
      //
      // **/.. is *brutal* for filesystem walking performance, because
      // it effectively resets the recursive walk each time it occurs,
      // and ** cannot be reduced out by a .. pattern part like a regexp
      // or most strings (other than .., ., and '') can be.
      //
      // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
      // <pre>/<e>/<rest> -> <pre>/<rest>
      // <pre>/<p>/../<rest> -> <pre>/<rest>
      // **/**/<rest> -> **/<rest>
      //
      // **/*/<rest> -> */**/<rest> <== not valid because ** doesn't follow
      // this WOULD be allowed if ** did follow symlinks, or * didn't
      firstPhasePreProcess(globParts) {
        let didSomething = !1;
        do {
          didSomething = !1;
          for (let parts of globParts) {
            let gs = -1;
            for (; (gs = parts.indexOf("**", gs + 1)) !== -1; ) {
              let gss = gs;
              for (; parts[gss + 1] === "**"; )
                gss++;
              gss > gs && parts.splice(gs + 1, gss - gs);
              let next = parts[gs + 1], p = parts[gs + 2], p2 = parts[gs + 3];
              if (next !== ".." || !p || p === "." || p === ".." || !p2 || p2 === "." || p2 === "..")
                continue;
              didSomething = !0, parts.splice(gs, 1);
              let other = parts.slice(0);
              other[gs] = "**", globParts.push(other), gs--;
            }
            if (!this.preserveMultipleSlashes) {
              for (let i = 1; i < parts.length - 1; i++) {
                let p = parts[i];
                i === 1 && p === "" && parts[0] === "" || (p === "." || p === "") && (didSomething = !0, parts.splice(i, 1), i--);
              }
              parts[0] === "." && parts.length === 2 && (parts[1] === "." || parts[1] === "") && (didSomething = !0, parts.pop());
            }
            let dd = 0;
            for (; (dd = parts.indexOf("..", dd + 1)) !== -1; ) {
              let p = parts[dd - 1];
              if (p && p !== "." && p !== ".." && p !== "**") {
                didSomething = !0;
                let splin = dd === 1 && parts[dd + 1] === "**" ? ["."] : [];
                parts.splice(dd - 1, 2, ...splin), parts.length === 0 && parts.push(""), dd -= 2;
              }
            }
          }
        } while (didSomething);
        return globParts;
      }
      // second phase: multi-pattern dedupes
      // {<pre>/*/<rest>,<pre>/<p>/<rest>} -> <pre>/*/<rest>
      // {<pre>/<rest>,<pre>/<rest>} -> <pre>/<rest>
      // {<pre>/**/<rest>,<pre>/<rest>} -> <pre>/**/<rest>
      //
      // {<pre>/**/<rest>,<pre>/**/<p>/<rest>} -> <pre>/**/<rest>
      // ^-- not valid because ** doens't follow symlinks
      secondPhasePreProcess(globParts) {
        for (let i = 0; i < globParts.length - 1; i++)
          for (let j = i + 1; j < globParts.length; j++) {
            let matched = this.partsMatch(globParts[i], globParts[j], !this.preserveMultipleSlashes);
            if (matched) {
              globParts[i] = [], globParts[j] = matched;
              break;
            }
          }
        return globParts.filter((gs) => gs.length);
      }
      partsMatch(a, b, emptyGSMatch = !1) {
        let ai = 0, bi = 0, result = [], which = "";
        for (; ai < a.length && bi < b.length; )
          if (a[ai] === b[bi])
            result.push(which === "b" ? b[bi] : a[ai]), ai++, bi++;
          else if (emptyGSMatch && a[ai] === "**" && b[bi] === a[ai + 1])
            result.push(a[ai]), ai++;
          else if (emptyGSMatch && b[bi] === "**" && a[ai] === b[bi + 1])
            result.push(b[bi]), bi++;
          else if (a[ai] === "*" && b[bi] && (this.options.dot || !b[bi].startsWith(".")) && b[bi] !== "**") {
            if (which === "b")
              return !1;
            which = "a", result.push(a[ai]), ai++, bi++;
          } else if (b[bi] === "*" && a[ai] && (this.options.dot || !a[ai].startsWith(".")) && a[ai] !== "**") {
            if (which === "a")
              return !1;
            which = "b", result.push(b[bi]), ai++, bi++;
          } else
            return !1;
        return a.length === b.length && result;
      }
      parseNegate() {
        if (this.nonegate)
          return;
        let pattern = this.pattern, negate = !1, negateOffset = 0;
        for (let i = 0; i < pattern.length && pattern.charAt(i) === "!"; i++)
          negate = !negate, negateOffset++;
        negateOffset && (this.pattern = pattern.slice(negateOffset)), this.negate = negate;
      }
      // set partial to true to test if, for example,
      // "/a/b" matches the start of "/*/b/*/d"
      // Partial means, if you run out of file before you run
      // out of pattern, then that's fine, as long as all
      // the parts match.
      matchOne(file, pattern, partial = !1) {
        let options = this.options;
        if (this.isWindows) {
          let fileDrive = typeof file[0] == "string" && /^[a-z]:$/i.test(file[0]), fileUNC = !fileDrive && file[0] === "" && file[1] === "" && file[2] === "?" && /^[a-z]:$/i.test(file[3]), patternDrive = typeof pattern[0] == "string" && /^[a-z]:$/i.test(pattern[0]), patternUNC = !patternDrive && pattern[0] === "" && pattern[1] === "" && pattern[2] === "?" && typeof pattern[3] == "string" && /^[a-z]:$/i.test(pattern[3]), fdi = fileUNC ? 3 : fileDrive ? 0 : void 0, pdi = patternUNC ? 3 : patternDrive ? 0 : void 0;
          if (typeof fdi == "number" && typeof pdi == "number") {
            let [fd, pd] = [file[fdi], pattern[pdi]];
            fd.toLowerCase() === pd.toLowerCase() && (pattern[pdi] = fd, pdi > fdi ? pattern = pattern.slice(pdi) : fdi > pdi && (file = file.slice(fdi)));
          }
        }
        let { optimizationLevel = 1 } = this.options;
        optimizationLevel >= 2 && (file = this.levelTwoFileOptimize(file)), this.debug("matchOne", this, { file, pattern }), this.debug("matchOne", file.length, pattern.length);
        for (var fi = 0, pi = 0, fl = file.length, pl = pattern.length; fi < fl && pi < pl; fi++, pi++) {
          this.debug("matchOne loop");
          var p = pattern[pi], f = file[fi];
          if (this.debug(pattern, p, f), p === !1)
            return !1;
          if (p === exports.GLOBSTAR) {
            this.debug("GLOBSTAR", [pattern, p, f]);
            var fr = fi, pr = pi + 1;
            if (pr === pl) {
              for (this.debug("** at the end"); fi < fl; fi++)
                if (file[fi] === "." || file[fi] === ".." || !options.dot && file[fi].charAt(0) === ".")
                  return !1;
              return !0;
            }
            for (; fr < fl; ) {
              var swallowee = file[fr];
              if (this.debug(`
globstar while`, file, fr, pattern, pr, swallowee), this.matchOne(file.slice(fr), pattern.slice(pr), partial))
                return this.debug("globstar found match!", fr, fl, swallowee), !0;
              if (swallowee === "." || swallowee === ".." || !options.dot && swallowee.charAt(0) === ".") {
                this.debug("dot detected!", file, fr, pattern, pr);
                break;
              }
              this.debug("globstar swallow a segment, and continue"), fr++;
            }
            return !!(partial && (this.debug(`
>>> no match, partial?`, file, fr, pattern, pr), fr === fl));
          }
          let hit;
          if (typeof p == "string" ? (hit = f === p, this.debug("string match", p, f, hit)) : (hit = p.test(f), this.debug("pattern match", p, f, hit)), !hit)
            return !1;
        }
        if (fi === fl && pi === pl)
          return !0;
        if (fi === fl)
          return partial;
        if (pi === pl)
          return fi === fl - 1 && file[fi] === "";
        throw new Error("wtf?");
      }
      braceExpand() {
        return (0, exports.braceExpand)(this.pattern, this.options);
      }
      parse(pattern) {
        (0, assert_valid_pattern_js_1.assertValidPattern)(pattern);
        let options = this.options;
        if (pattern === "**")
          return exports.GLOBSTAR;
        if (pattern === "")
          return "";
        let m, fastTest = null;
        (m = pattern.match(starRE)) ? fastTest = options.dot ? starTestDot : starTest : (m = pattern.match(starDotExtRE)) ? fastTest = (options.nocase ? options.dot ? starDotExtTestNocaseDot : starDotExtTestNocase : options.dot ? starDotExtTestDot : starDotExtTest)(m[1]) : (m = pattern.match(qmarksRE)) ? fastTest = (options.nocase ? options.dot ? qmarksTestNocaseDot : qmarksTestNocase : options.dot ? qmarksTestDot : qmarksTest)(m) : (m = pattern.match(starDotStarRE)) ? fastTest = options.dot ? starDotStarTestDot : starDotStarTest : (m = pattern.match(dotStarRE)) && (fastTest = dotStarTest);
        let re = ast_js_1.AST.fromGlob(pattern, this.options).toMMPattern();
        return fastTest && typeof re == "object" && Reflect.defineProperty(re, "test", { value: fastTest }), re;
      }
      makeRe() {
        if (this.regexp || this.regexp === !1)
          return this.regexp;
        let set = this.set;
        if (!set.length)
          return this.regexp = !1, this.regexp;
        let options = this.options, twoStar = options.noglobstar ? star : options.dot ? twoStarDot : twoStarNoDot, flags = new Set(options.nocase ? ["i"] : []), re = set.map((pattern) => {
          let pp = pattern.map((p) => {
            if (p instanceof RegExp)
              for (let f of p.flags.split(""))
                flags.add(f);
            return typeof p == "string" ? regExpEscape(p) : p === exports.GLOBSTAR ? exports.GLOBSTAR : p._src;
          });
          return pp.forEach((p, i) => {
            let next = pp[i + 1], prev = pp[i - 1];
            p !== exports.GLOBSTAR || prev === exports.GLOBSTAR || (prev === void 0 ? next !== void 0 && next !== exports.GLOBSTAR ? pp[i + 1] = "(?:\\/|" + twoStar + "\\/)?" + next : pp[i] = twoStar : next === void 0 ? pp[i - 1] = prev + "(?:\\/|" + twoStar + ")?" : next !== exports.GLOBSTAR && (pp[i - 1] = prev + "(?:\\/|\\/" + twoStar + "\\/)" + next, pp[i + 1] = exports.GLOBSTAR));
          }), pp.filter((p) => p !== exports.GLOBSTAR).join("/");
        }).join("|"), [open, close] = set.length > 1 ? ["(?:", ")"] : ["", ""];
        re = "^" + open + re + close + "$", this.negate && (re = "^(?!" + re + ").+$");
        try {
          this.regexp = new RegExp(re, [...flags].join(""));
        } catch {
          this.regexp = !1;
        }
        return this.regexp;
      }
      slashSplit(p) {
        return this.preserveMultipleSlashes ? p.split("/") : this.isWindows && /^\/\/[^\/]+/.test(p) ? ["", ...p.split(/\/+/)] : p.split(/\/+/);
      }
      match(f, partial = this.partial) {
        if (this.debug("match", f, this.pattern), this.comment)
          return !1;
        if (this.empty)
          return f === "";
        if (f === "/" && partial)
          return !0;
        let options = this.options;
        this.isWindows && (f = f.split("\\").join("/"));
        let ff = this.slashSplit(f);
        this.debug(this.pattern, "split", ff);
        let set = this.set;
        this.debug(this.pattern, "set", set);
        let filename = ff[ff.length - 1];
        if (!filename)
          for (let i = ff.length - 2; !filename && i >= 0; i--)
            filename = ff[i];
        for (let i = 0; i < set.length; i++) {
          let pattern = set[i], file = ff;
          if (options.matchBase && pattern.length === 1 && (file = [filename]), this.matchOne(file, pattern, partial))
            return options.flipNegate ? !0 : !this.negate;
        }
        return options.flipNegate ? !1 : this.negate;
      }
      static defaults(def) {
        return exports.minimatch.defaults(def).Minimatch;
      }
    };
    exports.Minimatch = Minimatch;
    var ast_js_2 = require_ast();
    Object.defineProperty(exports, "AST", { enumerable: !0, get: function() {
      return ast_js_2.AST;
    } });
    var escape_js_2 = require_escape();
    Object.defineProperty(exports, "escape", { enumerable: !0, get: function() {
      return escape_js_2.escape;
    } });
    var unescape_js_2 = require_unescape();
    Object.defineProperty(exports, "unescape", { enumerable: !0, get: function() {
      return unescape_js_2.unescape;
    } });
    exports.minimatch.AST = ast_js_1.AST;
    exports.minimatch.Minimatch = Minimatch;
    exports.minimatch.escape = escape_js_1.escape;
    exports.minimatch.unescape = unescape_js_1.unescape;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/aggregate-flags.js
var require_aggregate_flags = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/aggregate-flags.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.aggregateFlags = void 0;
    var flags_1 = require_flags(), json = (0, flags_1.boolean)({
      description: "Format output as json.",
      helpGroup: "GLOBAL"
    });
    function aggregateFlags(flags, baseFlags, enableJsonFlag) {
      let combinedFlags = { ...baseFlags, ...flags };
      return enableJsonFlag ? { json, ...combinedFlags } : combinedFlags;
    }
    exports.aggregateFlags = aggregateFlags;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/cache-command.js
var require_cache_command = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/cache-command.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.cacheCommand = void 0;
    var aggregate_flags_1 = require_aggregate_flags(), cache_default_value_1 = require_cache_default_value(), ensure_arg_object_1 = require_ensure_arg_object(), util_1 = require_util();
    function mergePrototype(result, cmd) {
      let proto = Object.getPrototypeOf(cmd), filteredProto = (0, util_1.pickBy)(proto, (v) => v !== void 0);
      return Object.keys(proto).length > 0 ? mergePrototype({ ...filteredProto, ...result }, proto) : result;
    }
    async function cacheFlags(cmdFlags, respectNoCacheDefault) {
      let promises = Object.entries(cmdFlags).map(async ([name, flag]) => [
        name,
        {
          aliases: flag.aliases,
          char: flag.char,
          charAliases: flag.charAliases,
          dependsOn: flag.dependsOn,
          deprecateAliases: flag.deprecateAliases,
          deprecated: flag.deprecated,
          description: flag.description,
          env: flag.env,
          exclusive: flag.exclusive,
          helpGroup: flag.helpGroup,
          helpLabel: flag.helpLabel,
          hidden: flag.hidden,
          name,
          noCacheDefault: flag.noCacheDefault,
          relationships: flag.relationships,
          required: flag.required,
          summary: flag.summary,
          ...flag.type === "boolean" ? {
            allowNo: flag.allowNo,
            type: flag.type
          } : {
            default: await (0, cache_default_value_1.cacheDefaultValue)(flag, respectNoCacheDefault),
            delimiter: flag.delimiter,
            hasDynamicHelp: typeof flag.defaultHelp == "function",
            helpValue: flag.helpValue,
            multiple: flag.multiple,
            options: flag.options,
            type: flag.type
          }
        }
      ]);
      return Object.fromEntries(await Promise.all(promises));
    }
    async function cacheArgs(cmdArgs, respectNoCacheDefault) {
      let promises = Object.entries(cmdArgs).map(async ([name, arg]) => [
        name,
        {
          default: await (0, cache_default_value_1.cacheDefaultValue)(arg, respectNoCacheDefault),
          description: arg.description,
          hidden: arg.hidden,
          name,
          noCacheDefault: arg.noCacheDefault,
          options: arg.options,
          required: arg.required
        }
      ]);
      return Object.fromEntries(await Promise.all(promises));
    }
    async function cacheCommand(uncachedCmd, plugin, respectNoCacheDefault = !1) {
      let cmd = mergePrototype(uncachedCmd, uncachedCmd), uncachedFlags = cmd.flags ?? cmd._flags, uncachedBaseFlags = cmd.baseFlags ?? cmd._baseFlags, [flags, args] = await Promise.all([
        await cacheFlags((0, aggregate_flags_1.aggregateFlags)(uncachedFlags, uncachedBaseFlags, cmd.enableJsonFlag), respectNoCacheDefault),
        await cacheArgs((0, ensure_arg_object_1.ensureArgObject)(cmd.args), respectNoCacheDefault)
      ]), stdProperties = {
        aliases: cmd.aliases ?? [],
        args,
        deprecateAliases: cmd.deprecateAliases,
        deprecationOptions: cmd.deprecationOptions,
        description: cmd.description,
        // Support both `examples` and `example` for backwards compatibility.
        examples: cmd.examples ?? cmd.example,
        flags,
        hasDynamicHelp: Object.values(flags).some((f) => f.hasDynamicHelp),
        hidden: cmd.hidden,
        hiddenAliases: cmd.hiddenAliases ?? [],
        id: cmd.id,
        pluginAlias: plugin && plugin.alias,
        pluginName: plugin && plugin.name,
        pluginType: plugin && plugin.type,
        state: cmd.state,
        strict: cmd.strict,
        summary: cmd.summary,
        usage: cmd.usage
      }, ignoreCommandProperties = [
        "plugin",
        "_flags",
        "_enableJsonFlag",
        "_globalFlags",
        "_baseFlags",
        "baseFlags",
        "_--",
        "_base"
      ], stdKeysAndIgnored = /* @__PURE__ */ new Set([...Object.keys(stdProperties), ...ignoreCommandProperties]), keysToAdd = Object.keys(cmd).filter((property) => !stdKeysAndIgnored.has(property)), additionalProperties = Object.fromEntries(keysToAdd.map((key) => [key, cmd[key]]));
      return { ...stdProperties, ...additionalProperties };
    }
    exports.cacheCommand = cacheCommand;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/find-root.js
var require_find_root = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/util/find-root.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.findRoot = void 0;
    var debug_1 = __importDefault(require_src()), node_path_1 = __require("node:path"), fs_1 = require_fs(), debug2 = (0, debug_1.default)("find-root");
    function* up(from) {
      for (; (0, node_path_1.dirname)(from) !== from; )
        yield from, from = (0, node_path_1.dirname)(from);
      yield from;
    }
    async function findPluginRoot(root, name) {
      if (debug2.extend(name ?? "root-plugin")(`Finding root starting at ${root}`), name) {
        for (let next of up(root))
          if (next.endsWith((0, node_path_1.basename)(name)))
            return debug2.extend(name)("Found root based on plugin name!"), next;
      }
      for (let next of up(root))
        if (!((0, node_path_1.basename)((0, node_path_1.dirname)(next)) === "bin" && ["dev", "dev.cmd", "dev.js", "run", "run.cmd", "run.js"].includes((0, node_path_1.basename)(next))))
          try {
            let cur = (0, node_path_1.join)(next, "package.json");
            if (debug2.extend(name ?? "root-plugin")(`Checking ${cur}`), await (0, fs_1.safeReadJson)(cur))
              return debug2.extend(name ?? "root-plugin")("Found root by traversing up from starting point!"), (0, node_path_1.dirname)(cur);
          } catch {
          }
    }
    async function findRootLegacy(name, root) {
      debug2.extend(name ?? "root-plugin")("Finding root using legacy method");
      for (let next of up(root)) {
        let cur;
        if (name) {
          if (cur = (0, node_path_1.join)(next, "node_modules", name, "package.json"), await (0, fs_1.safeReadJson)(cur))
            return (0, node_path_1.dirname)(cur);
          if ((await (0, fs_1.safeReadJson)((0, node_path_1.join)(next, "package.json")))?.name === name)
            return next;
        } else if (cur = (0, node_path_1.join)(next, "package.json"), await (0, fs_1.safeReadJson)(cur))
          return (0, node_path_1.dirname)(cur);
      }
    }
    var pnp;
    function maybeRequirePnpApi(root) {
      if (pnp)
        return pnp;
      try {
        return pnp = __require(__require.resolve("pnpapi", { paths: [root] })), pnp;
      } catch {
      }
    }
    var getKey = (locator) => JSON.stringify(locator), isPeerDependency = (pkg, parentPkg, name) => getKey(pkg?.packageDependencies.get(name)) === getKey(parentPkg?.packageDependencies.get(name));
    function findPnpRoot(name, root) {
      if (maybeRequirePnpApi(root), !pnp)
        return;
      debug2.extend(name)("Finding root for using pnp method");
      let seen = /* @__PURE__ */ new Set(), traverseDependencyTree = (locator, parentPkg) => {
        let key = getKey(locator);
        if (seen.has(key))
          return;
        let pkg = pnp.getPackageInformation(locator);
        if (locator.name === name)
          return pkg.packageLocation;
        seen.add(key);
        for (let [name2, referencish] of pkg.packageDependencies) {
          if (referencish === null || parentPkg !== null && isPeerDependency(pkg, parentPkg, name2))
            continue;
          let childLocator = pnp.getLocator(name2, referencish), foundSomething = traverseDependencyTree(childLocator, pkg);
          if (foundSomething)
            return foundSomething;
        }
        seen.delete(key);
      };
      for (let locator of pnp.getDependencyTreeRoots()) {
        let foundSomething = traverseDependencyTree(locator);
        if (foundSomething)
          return foundSomething;
      }
    }
    async function findRoot(name, root) {
      if (name) {
        debug2.extend(name)(`Finding root using ${root}`);
        let pkgPath;
        try {
          pkgPath = __require.resolve(name, { paths: [root] }), debug2.extend(name)("Found starting point with require.resolve");
        } catch {
          debug2.extend(name)("require.resolve could not find plugin starting point");
        }
        if (pkgPath) {
          let found3 = await findPluginRoot((0, node_path_1.dirname)(pkgPath), name);
          if (found3)
            return debug2.extend(name)(`Found root at ${found3}`), found3;
        }
        let found2 = process.versions.pnp ? findPnpRoot(name, root) : await findRootLegacy(name, root);
        return debug2.extend(name)(found2 ? `Found root at ${found2}` : "No root found!"), found2;
      }
      debug2.extend("root-plugin")(`Finding root plugin using ${root}`);
      let found = await findPluginRoot(root);
      return debug2.extend("root-plugin")(found ? `Found root at ${found}` : "No root found!"), found;
    }
    exports.findRoot = findRoot;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/plugin.js
var require_plugin = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/plugin.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.Plugin = void 0;
    var globby_1 = __importDefault(require_globby()), node_path_1 = __require("node:path"), node_util_1 = __require("node:util"), cache_1 = __importDefault(require_cache()), errors_1 = require_errors(), module_loader_1 = require_module_loader(), performance_1 = require_performance(), symbols_1 = require_symbols(), cache_command_1 = require_cache_command(), find_root_1 = require_find_root(), fs_1 = require_fs(), util_1 = require_util(), ts_path_1 = require_ts_path(), util_2 = require_util2(), _pjson = cache_1.default.getInstance().get("@oclif/core");
    function topicsToArray(input, base) {
      return input ? (base = base ? `${base}:` : "", Array.isArray(input) ? [...input, input.flatMap((t) => topicsToArray(t.subtopics, `${base}${t.name}`))] : Object.keys(input).flatMap((k) => (input[k].name = k, [{ ...input[k], name: `${base}${k}` }, ...topicsToArray(input[k].subtopics, `${base}${input[k].name}`)]))) : [];
    }
    var cachedCommandCanBeUsed = (manifest, id) => !!(manifest?.commands[id] && "isESM" in manifest.commands[id] && "relativePath" in manifest.commands[id]), searchForCommandClass = (cmd) => typeof cmd.run == "function" ? cmd : cmd.default && cmd.default.run ? cmd.default : Object.values(cmd).find((cmd2) => typeof cmd2.run == "function"), ensureCommandClass = (cmd) => {
      if (cmd && typeof cmd.run == "function")
        return cmd;
    }, GLOB_PATTERNS = [
      "**/*.+(js|cjs|mjs|ts|tsx|mts|cts)",
      "!**/*.+(d.ts|test.ts|test.js|spec.ts|spec.js|d.mts|d.cts)?(x)"
    ];
    function processCommandIds(files) {
      return files.map((file) => {
        let p = (0, node_path_1.parse)(file), topics = p.dir.split("/"), command = p.name !== "index" && p.name, id = [...topics, command].filter(Boolean).join(":");
        return id === "" ? symbols_1.SINGLE_COMMAND_CLI_SYMBOL : id;
      });
    }
    function determineCommandDiscoveryOptions(commandDiscovery, defaultCmdId) {
      if (commandDiscovery) {
        if (typeof commandDiscovery == "string" && defaultCmdId)
          return { strategy: "single", target: commandDiscovery };
        if (typeof commandDiscovery == "string")
          return { globPatterns: GLOB_PATTERNS, strategy: "pattern", target: commandDiscovery };
        if (!commandDiscovery.target)
          throw new errors_1.CLIError("`oclif.commandDiscovery.target` is required.");
        if (!commandDiscovery.strategy)
          throw new errors_1.CLIError("`oclif.commandDiscovery.strategy` is required.");
        return commandDiscovery.strategy === "explicit" && !commandDiscovery.identifier && (commandDiscovery.identifier = "default"), commandDiscovery;
      }
    }
    function determineHookOptions(hook) {
      return typeof hook == "string" ? { identifier: "default", target: hook } : hook.identifier ? hook : { ...hook, identifier: "default" };
    }
    var Plugin = class {
      options;
      alias;
      alreadyLoaded = !1;
      children = [];
      commandIDs = [];
      // This will be initialized in the _manifest() method, which gets called in the load() method.
      commands;
      commandsDir;
      hasManifest = !1;
      hooks;
      isRoot = !1;
      manifest;
      moduleType;
      name;
      parent;
      pjson;
      root;
      tag;
      type;
      valid = !1;
      version;
      _base = `${_pjson.name}@${_pjson.version}`;
      // eslint-disable-next-line new-cap
      _debug = (0, util_2.Debug)();
      commandCache;
      commandDiscoveryOpts;
      flexibleTaxonomy;
      constructor(options) {
        this.options = options;
      }
      get topics() {
        return topicsToArray(this.pjson.oclif.topics || {});
      }
      async findCommand(id, opts = {}) {
        let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `plugin.findCommand#${this.name}.${id}`, {
          id,
          plugin: this.name
        }), cmd = await (async () => {
          if (this.commandDiscoveryOpts?.strategy === "pattern") {
            let commandsDir = await this.getCommandsDir();
            if (!commandsDir)
              return;
            let module2, isESM, filePath;
            try {
              ({ filePath, isESM, module: module2 } = cachedCommandCanBeUsed(this.manifest, id) ? await (0, module_loader_1.loadWithDataFromManifest)(this.manifest.commands[id], this.root) : await (0, module_loader_1.loadWithData)(this, (0, node_path_1.join)(commandsDir ?? this.pjson.oclif.commands, ...id.split(":")))), this._debug(isESM ? "(import)" : "(require)", filePath);
            } catch (error) {
              if (!opts.must && error.code === "MODULE_NOT_FOUND")
                return;
              throw error;
            }
            let cmd2 = searchForCommandClass(module2);
            return cmd2 ? (cmd2.id = id, cmd2.plugin = this, cmd2.isESM = isESM, cmd2.relativePath = (0, node_path_1.relative)(this.root, filePath || "").split(node_path_1.sep), cmd2) : void 0;
          }
          if (this.commandDiscoveryOpts?.strategy === "single" || this.commandDiscoveryOpts?.strategy === "explicit") {
            let commandCache = await this.loadCommandsFromTarget(), cmd2 = ensureCommandClass(commandCache?.[id]);
            return cmd2 ? (cmd2.id = id, cmd2.plugin = this, cmd2) : void 0;
          }
        })();
        return !cmd && opts.must && (0, errors_1.error)(`command ${id} not found`), marker?.stop(), cmd;
      }
      async load() {
        this.type = this.options.type ?? "core", this.tag = this.options.tag, this.isRoot = this.options.isRoot ?? !1, this.options.parent && (this.parent = this.options.parent);
        let root = this.options.pjson && this.options.isRoot ? this.options.root : this.type === "link" && !this.parent ? this.options.root : await (0, find_root_1.findRoot)(this.options.name, this.options.root);
        if (!root)
          throw new errors_1.CLIError(`could not find package.json with ${(0, node_util_1.inspect)(this.options)}`);
        if (this.root = root, this._debug(`loading ${this.type} plugin from ${root}`), this.pjson = this.options.pjson ?? await (0, fs_1.readJson)((0, node_path_1.join)(root, "package.json")), this.flexibleTaxonomy = this.options?.flexibleTaxonomy || this.pjson.oclif?.flexibleTaxonomy || !1, this.moduleType = this.pjson.type === "module" ? "module" : "commonjs", this.name = this.pjson.name, this.alias = this.options.name ?? this.pjson.name, !this.name)
          throw new errors_1.CLIError(`no name in package.json (${root})`);
        this._debug = (0, util_2.Debug)(this.name), this.version = this.pjson.version, this.pjson.oclif ? this.valid = !0 : this.pjson.oclif = this.pjson["cli-engine"] || {}, this.hooks = Object.fromEntries(Object.entries(this.pjson.oclif.hooks ?? {}).map(([k, v]) => [
          k,
          (0, util_1.castArray)(v).map((v2) => determineHookOptions(v2))
        ])), this.commandDiscoveryOpts = determineCommandDiscoveryOptions(this.pjson.oclif?.commands, this.pjson.oclif?.default), this._debug("command discovery options", this.commandDiscoveryOpts), this.manifest = await this._manifest(), this.commands = Object.entries(this.manifest.commands).map(([id, c]) => ({
          ...c,
          load: async () => this.findCommand(id, { must: !0 }),
          pluginAlias: this.alias,
          pluginType: c.pluginType === "jit" ? "jit" : this.type
        })).sort((a, b) => a.id.localeCompare(b.id));
      }
      async _manifest() {
        let ignoreManifest = !!this.options.ignoreManifest, errorOnManifestCreate = !!this.options.errorOnManifestCreate, respectNoCacheDefault = !!this.options.respectNoCacheDefault, readManifest = async (dotfile = !1) => {
          try {
            let p = (0, node_path_1.join)(this.root, `${dotfile ? "." : ""}oclif.manifest.json`), manifest2 = await (0, fs_1.readJson)(p);
            if (!process.env.OCLIF_NEXT_VERSION && manifest2.version.split("-")[0] !== this.version.split("-")[0])
              process.emitWarning(`Mismatched version in ${this.name} plugin manifest. Expected: ${this.version} Received: ${manifest2.version}
This usually means you have an oclif.manifest.json file that should be deleted in development. This file should be automatically generated when publishing.`);
            else
              return this._debug("using manifest from", p), this.hasManifest = !0, manifest2;
          } catch (error) {
            if (error.code === "ENOENT") {
              if (!dotfile)
                return readManifest(!0);
            } else
              this.warn(error, "readManifest");
          }
        }, marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `plugin.manifest#${this.name}`, { plugin: this.name });
        if (!ignoreManifest) {
          let manifest2 = await readManifest();
          if (manifest2)
            return marker?.addDetails({ commandCount: Object.keys(manifest2.commands).length, fromCache: !0 }), marker?.stop(), this.commandIDs = Object.keys(manifest2.commands), manifest2;
        }
        this.commandIDs = await this.getCommandIDs();
        let manifest = {
          commands: (await Promise.all(this.commandIDs.map(async (id) => {
            try {
              let found = await this.findCommand(id, { must: !0 }), cached = await (0, cache_command_1.cacheCommand)(found, this, respectNoCacheDefault);
              if (cached.id = id, this.flexibleTaxonomy) {
                let permutations = (0, util_2.getCommandIdPermutations)(id), aliasPermutations = cached.aliases.flatMap((a) => (0, util_2.getCommandIdPermutations)(a));
                return [id, { ...cached, aliasPermutations, permutations }];
              }
              return [id, cached];
            } catch (error) {
              let scope = `findCommand (${id})`;
              if (!errorOnManifestCreate)
                this.warn(error, scope);
              else
                throw this.addErrorScope(error, scope);
            }
          }))).filter((f) => !!f).reduce((commands, [id, c]) => (commands[id] = c, commands), {}),
          version: this.version
        };
        return marker?.addDetails({ commandCount: Object.keys(manifest.commands).length, fromCache: !1 }), marker?.stop(), manifest;
      }
      addErrorScope(err, scope) {
        return err.name = err.name ?? (0, node_util_1.inspect)(err).trim(), err.detail = (0, util_1.compact)([
          err.detail,
          `module: ${this._base}`,
          scope && `task: ${scope}`,
          `plugin: ${this.name}`,
          `root: ${this.root}`,
          ...err.code ? [`code: ${err.code}`] : [],
          ...err.message ? [`message: ${err.message}`] : [],
          "See more details with DEBUG=*"
        ]).join(`
`), err;
      }
      async getCommandIDs() {
        let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `plugin.getCommandIDs#${this.name}`, { plugin: this.name }), ids;
        switch (this.commandDiscoveryOpts?.strategy) {
          case "explicit": {
            ids = await this.getCommandIdsFromTarget() ?? [];
            break;
          }
          case "pattern": {
            ids = await this.getCommandIdsFromPattern();
            break;
          }
          case "single": {
            ids = await this.getCommandIdsFromTarget() ?? [];
            break;
          }
          default:
            ids = [];
        }
        return this._debug("found commands", ids), marker?.addDetails({ count: ids.length }), marker?.stop(), ids;
      }
      async getCommandIdsFromPattern() {
        let commandsDir = await this.getCommandsDir();
        if (!commandsDir)
          return [];
        this._debug(`loading IDs from ${commandsDir}`);
        let files = await (0, globby_1.default)(this.commandDiscoveryOpts?.globPatterns ?? GLOB_PATTERNS, { cwd: commandsDir });
        return processCommandIds(files);
      }
      async getCommandIdsFromTarget() {
        if (await this.loadCommandsFromTarget())
          return Object.entries(await this.loadCommandsFromTarget() ?? []).filter(([, cmd]) => ensureCommandClass(cmd)).map(([id]) => id);
      }
      async getCommandsDir() {
        return this.commandsDir ? this.commandsDir : (this.commandsDir = await (0, ts_path_1.tsPath)(this.root, this.commandDiscoveryOpts?.target, this), this.commandsDir);
      }
      async loadCommandsFromTarget() {
        if (this.commandCache)
          return this.commandCache;
        if (this.commandDiscoveryOpts?.strategy === "explicit" && this.commandDiscoveryOpts.target) {
          let filePath = await (0, ts_path_1.tsPath)(this.root, this.commandDiscoveryOpts.target, this), module2 = await (0, module_loader_1.load)(this, filePath);
          return this.commandCache = module2[this.commandDiscoveryOpts?.identifier ?? "default"] ?? {}, this.commandCache;
        }
        if (this.commandDiscoveryOpts?.strategy === "single" && this.commandDiscoveryOpts.target) {
          let filePath = await (0, ts_path_1.tsPath)(this.root, this.commandDiscoveryOpts?.target ?? this.root, this), module2 = await (0, module_loader_1.load)(this, filePath);
          return this.commandCache = { [symbols_1.SINGLE_COMMAND_CLI_SYMBOL]: searchForCommandClass(module2) }, this.commandCache;
        }
      }
      warn(err, scope) {
        typeof err == "string" && (err = new Error(err));
        let warning = this.addErrorScope(err, scope);
        process.emitWarning(warning.name, warning);
      }
    };
    exports.Plugin = Plugin;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/plugin-loader.js
var require_plugin_loader = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/plugin-loader.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var minimatch_1 = require_commonjs3(), node_path_1 = __require("node:path"), performance_1 = require_performance(), fs_1 = require_fs(), util_1 = require_util(), Plugin = __importStar(require_plugin()), util_2 = require_util2(), debug2 = (0, util_2.Debug)();
    function findMatchingDependencies(dependencies, patterns) {
      return Object.keys(dependencies).filter((p) => patterns.some((w) => (0, minimatch_1.minimatch)(p, w)));
    }
    var PluginLoader = class {
      options;
      errors = [];
      plugins = /* @__PURE__ */ new Map();
      pluginsProvided = !1;
      constructor(options) {
        this.options = options, options.plugins && (this.pluginsProvided = !0, this.plugins = Array.isArray(options.plugins) ? new Map(options.plugins.map((p) => [p.name, p])) : options.plugins);
      }
      async loadChildren(opts) {
        return (!this.pluginsProvided || opts.force) && (await this.loadUserPlugins(opts), await this.loadDevPlugins(opts), await this.loadCorePlugins(opts)), { errors: this.errors, plugins: this.plugins };
      }
      async loadRoot({ pjson }) {
        let rootPlugin;
        if (this.pluginsProvided) {
          let plugins = [...this.plugins.values()];
          rootPlugin = plugins.find((p) => p.root === this.options.root) ?? plugins[0];
        } else {
          let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, "plugin.load#root");
          rootPlugin = new Plugin.Plugin({ isRoot: !0, pjson, root: this.options.root }), await rootPlugin.load(), marker?.addDetails({
            commandCount: rootPlugin.commands.length,
            hasManifest: rootPlugin.hasManifest ?? !1,
            name: rootPlugin.name,
            topicCount: rootPlugin.topics.length,
            type: rootPlugin.type,
            usesMain: !!rootPlugin.pjson.main
          }), marker?.stop();
        }
        return this.plugins.set(rootPlugin.name, rootPlugin), rootPlugin;
      }
      async loadCorePlugins(opts) {
        let { plugins: corePlugins } = opts.rootPlugin.pjson.oclif;
        if (corePlugins) {
          let plugins = findMatchingDependencies(opts.rootPlugin.pjson.dependencies ?? {}, corePlugins);
          await this.loadPlugins(opts.rootPlugin.root, "core", plugins);
        }
        let { core: pluginAdditionsCore, path: path3 } = opts.pluginAdditions ?? { core: [] };
        if (pluginAdditionsCore)
          if (path3) {
            let pjson = await (0, fs_1.readJson)((0, node_path_1.join)(path3, "package.json")), plugins = findMatchingDependencies(pjson.dependencies ?? {}, pluginAdditionsCore);
            await this.loadPlugins(path3, "core", plugins);
          } else {
            let plugins = findMatchingDependencies(opts.rootPlugin.pjson.dependencies ?? {}, pluginAdditionsCore);
            await this.loadPlugins(opts.rootPlugin.root, "core", plugins);
          }
      }
      async loadDevPlugins(opts) {
        if (opts.devPlugins !== !1) {
          if ((0, util_1.isProd)())
            return;
          try {
            let { devPlugins } = opts.rootPlugin.pjson.oclif;
            if (devPlugins) {
              let allDeps = { ...opts.rootPlugin.pjson.dependencies, ...opts.rootPlugin.pjson.devDependencies }, plugins = findMatchingDependencies(allDeps ?? {}, devPlugins);
              await this.loadPlugins(opts.rootPlugin.root, "dev", plugins);
            }
            let { dev: pluginAdditionsDev, path: path3 } = opts.pluginAdditions ?? { core: [] };
            if (pluginAdditionsDev)
              if (path3) {
                let pjson = await (0, fs_1.readJson)((0, node_path_1.join)(path3, "package.json")), allDeps = { ...pjson.dependencies, ...pjson.devDependencies }, plugins = findMatchingDependencies(allDeps ?? {}, pluginAdditionsDev);
                await this.loadPlugins(path3, "dev", plugins);
              } else {
                let allDeps = { ...opts.rootPlugin.pjson.dependencies, ...opts.rootPlugin.pjson.devDependencies }, plugins = findMatchingDependencies(allDeps ?? {}, pluginAdditionsDev);
                await this.loadPlugins(opts.rootPlugin.root, "dev", plugins);
              }
          } catch (error) {
            process.emitWarning(error);
          }
        }
      }
      async loadPlugins(root, type, plugins, parent) {
        if (!plugins || plugins.length === 0)
          return;
        let mark = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `config.loadPlugins#${type}`);
        debug2("loading plugins", plugins), await Promise.all((plugins || []).map(async (plugin) => {
          try {
            let name = typeof plugin == "string" ? plugin : plugin.name, opts = {
              name,
              root,
              type
            };
            if (typeof plugin != "string" && (opts.tag = plugin.tag || opts.tag, opts.root = plugin.root || opts.root, opts.url = plugin.url), parent && (opts.parent = parent), this.plugins.has(name))
              return;
            let pluginMarker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `plugin.load#${name}`), instance = new Plugin.Plugin(opts);
            if (await instance.load(), pluginMarker?.addDetails({
              commandCount: instance.commands.length,
              hasManifest: instance.hasManifest,
              name: instance.name,
              topicCount: instance.topics.length,
              type: instance.type,
              usesMain: !!instance.pjson.main
            }), pluginMarker?.stop(), this.plugins.set(instance.name, instance), parent && (instance.parent = parent, parent.children || (parent.children = []), parent.children.push(instance)), instance.pjson.oclif.plugins) {
              let allDeps = type === "dev" ? { ...instance.pjson.dependencies, ...instance.pjson.devDependencies } : instance.pjson.dependencies, plugins2 = findMatchingDependencies(allDeps ?? {}, instance.pjson.oclif.plugins);
              await this.loadPlugins(instance.root, type, plugins2, instance);
            }
          } catch (error) {
            this.errors.push(error);
          }
        })), mark?.addDetails({ pluginCount: plugins.length }), mark?.stop();
      }
      async loadUserPlugins(opts) {
        if (opts.userPlugins !== !1)
          try {
            let userPJSONPath = (0, node_path_1.join)(opts.dataDir, "package.json");
            debug2("reading user plugins pjson %s", userPJSONPath);
            let pjson = await (0, fs_1.readJson)(userPJSONPath);
            pjson.oclif || (pjson.oclif = { schema: 1 }), pjson.oclif.plugins || (pjson.oclif.plugins = []), await this.loadPlugins(userPJSONPath, "user", pjson.oclif.plugins.filter((p) => p.type === "user")), await this.loadPlugins(userPJSONPath, "link", pjson.oclif.plugins.filter((p) => p.type === "link"));
          } catch (error) {
            error.code !== "ENOENT" && process.emitWarning(error);
          }
      }
    };
    exports.default = PluginLoader;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/config.js
var require_config3 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/config.js"(exports, module) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.Config = void 0;
    var ejs = __importStar(require_ejs()), is_wsl_1 = __importDefault(require_is_wsl()), node_os_1 = __require("node:os"), node_path_1 = __require("node:path"), node_url_1 = __require("node:url"), cache_1 = __importDefault(require_cache()), cli_ux_1 = require_cli_ux(), theme_1 = require_theme2(), errors_1 = require_errors(), util_1 = require_util3(), module_loader_1 = require_module_loader(), performance_1 = require_performance(), settings_1 = require_settings(), fs_1 = require_fs(), os_1 = require_os(), util_2 = require_util(), plugin_loader_1 = __importDefault(require_plugin_loader()), ts_path_1 = require_ts_path(), util_3 = require_util2(), debug2 = (0, util_3.Debug)(), _pjson = cache_1.default.getInstance().get("@oclif/core"), BASE = `${_pjson.name}@${_pjson.version}`, ROOT_ONLY_HOOKS = /* @__PURE__ */ new Set(["preparse"]);
    function channelFromVersion(version) {
      let m = version.match(/[^-]+(?:-([^.]+))?/);
      return m && m[1] || "stable";
    }
    function isConfig(o) {
      return o && !!o._base;
    }
    var Permutations = class extends Map {
      validPermutations = /* @__PURE__ */ new Map();
      add(permutation, commandId) {
        this.validPermutations.set(permutation, commandId);
        for (let id of (0, util_3.collectUsableIds)([permutation]))
          this.has(id) ? this.set(id, this.get(id).add(commandId)) : this.set(id, /* @__PURE__ */ new Set([commandId]));
      }
      get(key) {
        return super.get(key) ?? /* @__PURE__ */ new Set();
      }
      getAllValid() {
        return [...this.validPermutations.keys()];
      }
      getValid(key) {
        return this.validPermutations.get(key);
      }
      hasValid(key) {
        return this.validPermutations.has(key);
      }
    }, Config = class _Config {
      options;
      arch;
      bin;
      binAliases;
      binPath;
      cacheDir;
      channel;
      configDir;
      dataDir;
      debug = 0;
      dirname;
      errlog;
      flexibleTaxonomy;
      home;
      isSingleCommandCLI = !1;
      name;
      npmRegistry;
      nsisCustomization;
      pjson;
      platform;
      plugins = /* @__PURE__ */ new Map();
      root;
      shell;
      theme;
      topicSeparator = ":";
      userAgent;
      userPJSON;
      valid;
      version;
      warned = !1;
      windows;
      _base = BASE;
      _commandIDs;
      _commands = /* @__PURE__ */ new Map();
      _topics = /* @__PURE__ */ new Map();
      commandPermutations = new Permutations();
      pluginLoader;
      rootPlugin;
      topicPermutations = new Permutations();
      constructor(options) {
        this.options = options;
      }
      static async load(opts = module.filename || __dirname) {
        if (typeof opts == "string" && opts.startsWith("file://") && (opts = (0, node_url_1.fileURLToPath)(opts)), typeof opts == "string" && (opts = { root: opts }), isConfig(opts)) {
          if (BASE !== opts._base) {
            debug2(`reloading config from ${opts._base} to ${BASE}`);
            let config2 = new _Config({ ...opts.options, plugins: opts.plugins });
            return await config2.load(), config2;
          }
          return opts;
        }
        let config = new _Config(opts);
        return await config.load(), config;
      }
      static get rootPlugin() {
        return this.rootPlugin;
      }
      get commandIDs() {
        return this._commandIDs ? this._commandIDs : (this._commandIDs = this.commands.map((c) => c.id), this._commandIDs);
      }
      get commands() {
        return [...this._commands.values()];
      }
      get isProd() {
        return (0, util_2.isProd)();
      }
      get topics() {
        return [...this._topics.values()];
      }
      get versionDetails() {
        let [cliVersion, architecture, nodeVersion] = this.userAgent.split(" ");
        return {
          architecture,
          cliVersion,
          nodeVersion,
          osVersion: `${(0, node_os_1.type)()} ${(0, node_os_1.release)()}`,
          pluginVersions: Object.fromEntries([...this.plugins.values()].map((p) => [p.name, { root: p.root, type: p.type, version: p.version }])),
          rootPath: this.root,
          shell: this.shell
        };
      }
      dir(category) {
        let base = process.env[`XDG_${category.toUpperCase()}_HOME`] || this.windows && process.env.LOCALAPPDATA || (0, node_path_1.join)(this.home, category === "data" ? ".local/share" : "." + category);
        return (0, node_path_1.join)(base, this.dirname);
      }
      findCommand(id, opts = {}) {
        let lookupId = this.getCmdLookupId(id), command = this._commands.get(lookupId);
        return opts.must && !command && (0, errors_1.error)(`command ${lookupId} not found`), command;
      }
      /**
       * Find all command ids that include the provided command id.
       *
       * For example, if the command ids are:
       * - foo:bar:baz
       * - one:two:three
       *
       * `bar` would return `foo:bar:baz`
       *
       * @param partialCmdId string
       * @param argv string[] process.argv containing the flags and arguments provided by the user
       * @returns string[]
       */
      findMatches(partialCmdId, argv) {
        let flags = argv.filter((arg) => !(0, util_1.getHelpFlagAdditions)(this).includes(arg) && arg.startsWith("-")).map((a) => a.replaceAll("-", ""));
        return [...this.commandPermutations.get(partialCmdId)].map((k) => this._commands.get(k)).filter((command) => {
          let cmdFlags = Object.entries(command.flags).flatMap(([flag, def]) => def.char ? [def.char, flag] : [flag]);
          return flags.every((f) => cmdFlags.includes(f));
        });
      }
      findTopic(name, opts = {}) {
        let lookupId = this.getTopicLookupId(name), topic = this._topics.get(lookupId);
        if (topic)
          return topic;
        if (opts.must)
          throw new Error(`topic ${name} not found`);
      }
      /**
       * Returns an array of all command ids. If flexible taxonomy is enabled then all permutations will be appended to the array.
       * @returns string[]
       */
      getAllCommandIDs() {
        return this.getAllCommands().map((c) => c.id);
      }
      /**
       * Returns an array of all commands. If flexible taxonomy is enabled then all permutations will be appended to the array.
       * @returns Command.Loadable[]
       */
      getAllCommands() {
        let commands = [...this._commands.values()], validPermutations = [...this.commandPermutations.getAllValid()];
        for (let permutation of validPermutations)
          if (!this._commands.has(permutation)) {
            let cmd = this._commands.get(this.getCmdLookupId(permutation));
            commands.push({ ...cmd, id: permutation });
          }
        return commands;
      }
      getPluginsList() {
        return [...this.plugins.values()];
      }
      // eslint-disable-next-line complexity
      async load() {
        settings_1.settings.performanceEnabled = (settings_1.settings.performanceEnabled === void 0 ? this.options.enablePerf : settings_1.settings.performanceEnabled) ?? !1;
        let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, "config.load");
        this.pluginLoader = new plugin_loader_1.default({ plugins: this.options.plugins, root: this.options.root }), this.rootPlugin = await this.pluginLoader.loadRoot({ pjson: this.options.pjson });
        let cache2 = cache_1.default.getInstance();
        if (cache2.set("rootPlugin", this.rootPlugin), cache2.set("exitCodes", this.rootPlugin.pjson.oclif.exitCodes ?? {}), this.root = this.rootPlugin.root, this.pjson = this.rootPlugin.pjson, this.plugins.set(this.rootPlugin.name, this.rootPlugin), this.root = this.rootPlugin.root, this.pjson = this.rootPlugin.pjson, this.name = this.pjson.name, this.version = this.options.version || this.pjson.version || "0.0.0", this.channel = this.options.channel || channelFromVersion(this.version), this.valid = this.rootPlugin.valid, this.arch = (0, node_os_1.arch)() === "ia32" ? "x86" : (0, node_os_1.arch)(), this.platform = is_wsl_1.default ? "wsl" : (0, os_1.getPlatform)(), this.windows = this.platform === "win32", this.bin = this.pjson.oclif.bin || this.name, this.binAliases = this.pjson.oclif.binAliases, this.nsisCustomization = this.pjson.oclif.nsisCustomization, this.dirname = this.pjson.oclif.dirname || this.name, this.flexibleTaxonomy = this.pjson.oclif.flexibleTaxonomy || !1, this.pjson.oclif.topicSeparator && [" ", ":"].includes(this.pjson.oclif.topicSeparator) && (this.topicSeparator = this.pjson.oclif.topicSeparator), this.platform === "win32" && (this.dirname = this.dirname.replace("/", "\\")), this.userAgent = `${this.name}/${this.version} ${this.platform}-${this.arch} node-${process.version}`, this.shell = this._shell(), this.debug = this._debug(), this.home = process.env.HOME || this.windows && this.windowsHome() || (0, os_1.getHomeDir)() || (0, node_os_1.tmpdir)(), this.cacheDir = this.scopedEnvVar("CACHE_DIR") || this.macosCacheDir() || this.dir("cache"), this.configDir = this.scopedEnvVar("CONFIG_DIR") || this.dir("config"), this.dataDir = this.scopedEnvVar("DATA_DIR") || this.dir("data"), this.errlog = (0, node_path_1.join)(this.cacheDir, "error.log"), this.binPath = this.scopedEnvVar("BINPATH"), this.npmRegistry = this.scopedEnvVar("NPM_REGISTRY") || this.pjson.oclif.npmRegistry, !this.scopedEnvVarTrue("DISABLE_THEME")) {
          let { theme } = await this.loadThemes();
          this.theme = theme;
        }
        this.pjson.oclif.update = this.pjson.oclif.update || {}, this.pjson.oclif.update.node = this.pjson.oclif.update.node || {};
        let s3 = this.pjson.oclif.update.s3 || {};
        this.pjson.oclif.update.s3 = s3, s3.bucket = this.scopedEnvVar("S3_BUCKET") || s3.bucket, s3.bucket && !s3.host && (s3.host = `https://${s3.bucket}.s3.amazonaws.com`), s3.templates = {
          ...s3.templates,
          target: {
            baseDir: "<%- bin %>",
            manifest: "<%- channel === 'stable' ? '' : 'channels/' + channel + '/' %><%- platform %>-<%- arch %>",
            unversioned: "<%- channel === 'stable' ? '' : 'channels/' + channel + '/' %><%- bin %>-<%- platform %>-<%- arch %><%- ext %>",
            versioned: "<%- channel === 'stable' ? '' : 'channels/' + channel + '/' %><%- bin %>-v<%- version %>/<%- bin %>-v<%- version %>-<%- platform %>-<%- arch %><%- ext %>",
            ...s3.templates && s3.templates.target
          },
          vanilla: {
            baseDir: "<%- bin %>",
            manifest: "<%- channel === 'stable' ? '' : 'channels/' + channel + '/' %>version",
            unversioned: "<%- channel === 'stable' ? '' : 'channels/' + channel + '/' %><%- bin %><%- ext %>",
            versioned: "<%- channel === 'stable' ? '' : 'channels/' + channel + '/' %><%- bin %>-v<%- version %>/<%- bin %>-v<%- version %><%- ext %>",
            ...s3.templates && s3.templates.vanilla
          }
        }, this.isSingleCommandCLI = !!(this.pjson.oclif.default || typeof this.pjson.oclif.commands != "string" && this.pjson.oclif.commands?.strategy === "single" && this.pjson.oclif.commands?.target), await this.loadPluginsAndCommands(), debug2("config done"), marker?.addDetails({
          commandPermutations: this.commands.length,
          commands: [...this.plugins.values()].reduce((acc, p) => acc + p.commands.length, 0),
          plugins: this.plugins.size,
          topics: this.topics.length
        }), marker?.stop();
      }
      async loadPluginsAndCommands(opts) {
        let pluginsMarker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, "config.loadAllPlugins"), { errors, plugins } = await this.pluginLoader.loadChildren({
          dataDir: this.dataDir,
          devPlugins: this.options.devPlugins,
          force: opts?.force ?? !1,
          pluginAdditions: this.options.pluginAdditions,
          rootPlugin: this.rootPlugin,
          userPlugins: this.options.userPlugins
        });
        this.plugins = plugins, pluginsMarker?.stop();
        let commandsMarker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, "config.loadAllCommands");
        for (let plugin of this.plugins.values())
          this.loadCommands(plugin), this.loadTopics(plugin);
        commandsMarker?.stop();
        for (let error of errors)
          this.warn(error);
      }
      async loadThemes() {
        let defaultThemeFile = this.pjson.oclif.theme ? (0, node_path_1.resolve)(this.root, this.pjson.oclif.theme) : this.pjson.oclif.theme, userThemeFile = (0, node_path_1.resolve)(this.configDir, "theme.json"), [defaultTheme, userTheme] = await Promise.all([
          defaultThemeFile ? await (0, fs_1.safeReadJson)(defaultThemeFile) : void 0,
          await (0, fs_1.safeReadJson)(userThemeFile)
        ]), merged = { ...defaultTheme, ...userTheme };
        return {
          // Point to the user file if it exists, otherwise use the default file.
          // This doesn't really serve a purpose to anyone but removing it would be a breaking change.
          file: userTheme ? userThemeFile : defaultThemeFile,
          theme: Object.keys(merged).length > 0 ? (0, theme_1.parseTheme)(merged) : void 0
        };
      }
      macosCacheDir() {
        return this.platform === "darwin" && (0, node_path_1.join)(this.home, "Library", "Caches", this.dirname) || void 0;
      }
      async runCommand(id, argv = [], cachedCommand = null) {
        let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `config.runCommand#${id}`);
        debug2("runCommand %s %o", id, argv);
        let c = cachedCommand ?? this.findCommand(id);
        if (!c) {
          let matches = this.flexibleTaxonomy ? this.findMatches(id, argv) : [], hookResult = this.flexibleTaxonomy && matches.length > 0 ? await this.runHook("command_incomplete", { argv, id, matches }) : await this.runHook("command_not_found", { argv, id });
          if (hookResult.successes[0])
            return hookResult.successes[0].result;
          throw hookResult.failures[0] ? hookResult.failures[0].error : new errors_1.CLIError(`command ${id} not found`);
        }
        if (this.isJitPluginCommand(c)) {
          let pluginName = c.pluginName, pluginVersion = this.pjson.oclif.jitPlugins[pluginName], jitResult = await this.runHook("jit_plugin_not_installed", {
            argv,
            command: c,
            id,
            pluginName,
            pluginVersion
          });
          if (jitResult.failures[0])
            throw jitResult.failures[0].error;
          if (jitResult.successes[0])
            await this.loadPluginsAndCommands({ force: !0 }), c = this.findCommand(id) ?? c;
          else {
            let result2 = await this.runHook("command_not_found", { argv, id });
            if (result2.successes[0])
              return result2.successes[0].result;
            throw result2.failures[0] ? result2.failures[0].error : new errors_1.CLIError(`command ${id} not found`);
          }
        }
        let command = await c.load();
        await this.runHook("prerun", { Command: command, argv });
        let result = await command.run(argv, this);
        if (c.id === "plugins:uninstall")
          for (let arg of argv)
            this.plugins.delete(arg);
        return await this.runHook("postrun", { Command: command, argv, result }), marker?.addDetails({ command: id, plugin: c.pluginName }), marker?.stop(), result;
      }
      async runHook(event, opts, timeout, captureErrors) {
        let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `config.runHook#${event}`);
        debug2("start %s hook", event);
        let search = (m) => typeof m == "function" ? m : m.default && typeof m.default == "function" ? m.default : Object.values(m).find((m2) => typeof m2 == "function"), withTimeout = async (ms, promise) => {
          let id, timeout2 = new Promise((_, reject) => {
            id = setTimeout(() => {
              reject(new Error(`Timed out after ${ms} ms.`));
            }, ms).unref();
          });
          return Promise.race([promise, timeout2]).then((result) => (clearTimeout(id), result));
        }, final = {
          failures: [],
          successes: []
        }, promises = (ROOT_ONLY_HOOKS.has(event) ? [this.rootPlugin] : [...this.plugins.values()]).map(async (p) => {
          let debug3 = require_src()([this.bin, p.name, "hooks", event].join(":")), context = {
            config: this,
            debug: debug3,
            error(message, options = {}) {
              (0, errors_1.error)(message, options);
            },
            exit(code = 0) {
              (0, errors_1.exit)(code);
            },
            log(message, ...args) {
              cli_ux_1.ux.info(message, ...args);
            },
            warn(message) {
              (0, errors_1.warn)(message);
            }
          }, hooks = p.hooks[event] || [];
          for (let hook of hooks) {
            let marker2 = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `config.runHook#${p.name}(${hook.target})`);
            try {
              let { filePath, isESM, module: module2 } = await (0, module_loader_1.loadWithData)(p, await (0, ts_path_1.tsPath)(p.root, hook.target, p));
              debug3("start", isESM ? "(import)" : "(require)", filePath);
              let hookFn = module2[hook.identifier] ?? (hook.identifier === "default" ? search(module2) : void 0);
              if (!hookFn) {
                debug3("No hook found for hook definition:", hook);
                continue;
              }
              let result = timeout ? await withTimeout(timeout, hookFn.call(context, { ...opts, config: this, context })) : await hookFn.call(context, { ...opts, config: this, context });
              final.successes.push({ plugin: p, result }), p.name === "@oclif/plugin-legacy" && event === "init" && this.insertLegacyPlugins(result), debug3("done");
            } catch (error) {
              if (final.failures.push({ error, plugin: p }), debug3(error), !captureErrors && error.oclif?.exit !== void 0 && error.oclif?.exit !== 0 && error.code !== "MODULE_NOT_FOUND")
                throw error;
            }
            marker2?.addDetails({
              event,
              hook: hook.target,
              plugin: p.name
            }), marker2?.stop();
          }
        });
        return await Promise.all(promises), debug2("%s hook done", event), marker?.stop(), final;
      }
      s3Key(type, ext, options = {}) {
        typeof ext == "object" ? options = ext : ext && (options.ext = ext);
        let template = this.pjson.oclif.update.s3.templates[options.platform ? "target" : "vanilla"][type] ?? "";
        return ejs.render(template, { ...this, ...options });
      }
      s3Url(key) {
        let { host } = this.pjson.oclif.update.s3;
        if (!host)
          throw new Error("no s3 host is set");
        let url = new node_url_1.URL(host);
        return url.pathname = (0, node_path_1.join)(url.pathname, key), url.toString();
      }
      scopedEnvVar(k) {
        return process.env[this.scopedEnvVarKeys(k).find((k2) => process.env[k2])];
      }
      /**
       * this DOES NOT account for bin aliases, use scopedEnvVarKeys instead which will account for bin aliases
       * @param {string} k, the unscoped key you want to get the value for
       * @returns {string} returns the env var key
       */
      scopedEnvVarKey(k) {
        return [this.bin, k].map((p) => p.replaceAll("@", "").replaceAll(/[/-]/g, "_")).join("_").toUpperCase();
      }
      /**
       * gets the scoped env var keys for a given key, including bin aliases
       * @param {string} k, the env key e.g. 'debug'
       * @returns {string[]} e.g. ['SF_DEBUG', 'SFDX_DEBUG']
       */
      scopedEnvVarKeys(k) {
        return [this.bin, ...this.binAliases ?? []].filter(Boolean).map((alias) => [alias.replaceAll("@", "").replaceAll(/[/-]/g, "_"), k].join("_").toUpperCase());
      }
      scopedEnvVarTrue(k) {
        let v = this.scopedEnvVar(k);
        return v === "1" || v === "true";
      }
      warn(err, scope) {
        if (!this.warned) {
          if (typeof err == "string") {
            process.emitWarning(err);
            return;
          }
          if (err instanceof Error) {
            let modifiedErr = err;
            modifiedErr.name = `${err.name} Plugin: ${this.name}`, modifiedErr.detail = (0, util_2.compact)([
              err.detail,
              `module: ${this._base}`,
              scope && `task: ${scope}`,
              `plugin: ${this.name}`,
              `root: ${this.root}`,
              "See more details with DEBUG=*"
            ]).join(`
`), process.emitWarning(err);
            return;
          }
          process.emitWarning("Config.warn expected either a string or Error, but instead received an object"), err.name = `${err.name} Plugin: ${this.name}`, err.detail = (0, util_2.compact)([
            err.detail,
            `module: ${this._base}`,
            scope && `task: ${scope}`,
            `plugin: ${this.name}`,
            `root: ${this.root}`,
            "See more details with DEBUG=*"
          ]).join(`
`), process.emitWarning(JSON.stringify(err));
        }
      }
      windowsHome() {
        return this.windowsHomedriveHome() || this.windowsUserprofileHome();
      }
      windowsHomedriveHome() {
        return process.env.HOMEDRIVE && process.env.HOMEPATH && (0, node_path_1.join)(process.env.HOMEDRIVE, process.env.HOMEPATH);
      }
      windowsUserprofileHome() {
        return process.env.USERPROFILE;
      }
      _debug() {
        if (this.scopedEnvVarTrue("DEBUG"))
          return 1;
        try {
          let { enabled } = require_src()(this.bin);
          if (enabled)
            return 1;
        } catch {
        }
        return 0;
      }
      _shell() {
        let shellPath, { COMSPEC } = process.env, SHELL = process.env.SHELL ?? (0, node_os_1.userInfo)().shell?.split(node_path_1.sep)?.pop();
        return SHELL ? shellPath = SHELL.split("/") : this.windows && COMSPEC ? shellPath = COMSPEC.split(/\\|\//) : shellPath = ["unknown"], shellPath.at(-1) ?? "unknown";
      }
      /**
       * This method is responsible for locating the correct plugin to use for a named command id
       * It searches the {Config} registered commands to match either the raw command id or the command alias
       * It is possible that more than one command will be found. This is due the ability of two distinct plugins to
       * create the same command or command alias.
       *
       * In the case of more than one found command, the function will select the command based on the order in which
       * the plugin is included in the package.json `oclif.plugins` list. The command that occurs first in the list
       * is selected as the command to run.
       *
       * Commands can also be present from either an install or a link. When a command is one of these and a core plugin
       * is present, this function defers to the core plugin.
       *
       * If there is not a core plugin command present, this function will return the first
       * plugin as discovered (will not change the order)
       *
       * @param commands commands to determine the priority of
       * @returns command instance {Command.Loadable} or undefined
       */
      determinePriority(commands) {
        let oclifPlugins = this.pjson.oclif?.plugins ?? [];
        return commands.sort((a, b) => {
          let pluginAliasA = a.pluginAlias ?? "A-Cannot-Find-This", pluginAliasB = b.pluginAlias ?? "B-Cannot-Find-This", aIndex = oclifPlugins.indexOf(pluginAliasA), bIndex = oclifPlugins.indexOf(pluginAliasB);
          return a.pluginType === "core" && b.pluginType === "core" ? aIndex - bIndex : b.pluginType === "core" && a.pluginType !== "core" ? 1 : a.pluginType === "core" && b.pluginType !== "core" ? -1 : a.pluginType === "jit" && b.pluginType !== "jit" ? 1 : b.pluginType === "jit" && a.pluginType !== "jit" ? -1 : 0;
        })[0];
      }
      getCmdLookupId(id) {
        return this._commands.has(id) ? id : this.commandPermutations.hasValid(id) ? this.commandPermutations.getValid(id) : id;
      }
      getTopicLookupId(id) {
        return this._topics.has(id) ? id : this.topicPermutations.hasValid(id) ? this.topicPermutations.getValid(id) : id;
      }
      /**
       * Insert legacy plugins
       *
       * Replace invalid CLI plugins (cli-engine plugins, mostly Heroku) loaded via `this.loadPlugins`
       * with oclif-compatible ones returned by @oclif/plugin-legacy init hook.
       *
       * @param plugins array of oclif-compatible plugins
       * @returns void
       */
      insertLegacyPlugins(plugins) {
        for (let plugin of plugins) {
          this.plugins.set(plugin.name, plugin);
          for (let cmd of plugin.commands ?? []) {
            this._commands.delete(cmd.id);
            for (let alias of [...cmd.aliases ?? [], ...cmd.hiddenAliases ?? []])
              this._commands.delete(alias);
          }
          this.loadCommands(plugin);
        }
      }
      isJitPluginCommand(c) {
        return Object.keys(this.pjson.oclif.jitPlugins ?? {}).includes(c.pluginName ?? "") && !!(c?.pluginName && !this.plugins.has(c.pluginName));
      }
      loadCommands(plugin) {
        let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `config.loadCommands#${plugin.name}`, { plugin: plugin.name });
        for (let command of plugin.commands) {
          if (this._commands.has(command.id)) {
            let prioritizedCommand = this.determinePriority([this._commands.get(command.id), command]);
            this._commands.set(prioritizedCommand.id, prioritizedCommand);
          } else
            this._commands.set(command.id, command);
          let permutations = this.flexibleTaxonomy && command.permutations === void 0 ? (0, util_3.getCommandIdPermutations)(command.id) : command.permutations ?? [command.id];
          for (let permutation of permutations)
            this.commandPermutations.add(permutation, command.id);
          let handleAlias = (alias, hidden = !1) => {
            if (this._commands.has(alias)) {
              let prioritizedCommand = this.determinePriority([this._commands.get(alias), command]);
              this._commands.set(alias, { ...prioritizedCommand, id: alias });
            } else
              this._commands.set(alias, { ...command, hidden, id: alias });
            let aliasPermutations = this.flexibleTaxonomy && command.aliasPermutations === void 0 ? (0, util_3.getCommandIdPermutations)(alias) : command.permutations ?? [alias];
            for (let permutation of aliasPermutations)
              this.commandPermutations.add(permutation, command.id);
          };
          for (let alias of command.aliases ?? [])
            handleAlias(alias);
          for (let alias of command.hiddenAliases ?? [])
            handleAlias(alias, !0);
        }
        marker?.addDetails({ commandCount: plugin.commands.length }), marker?.stop();
      }
      loadTopics(plugin) {
        let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, `config.loadTopics#${plugin.name}`, { plugin: plugin.name });
        for (let topic of (0, util_2.compact)(plugin.topics)) {
          let existing = this._topics.get(topic.name);
          existing ? (existing.description = topic.description || existing.description, existing.hidden = existing.hidden || topic.hidden) : this._topics.set(topic.name, topic);
          let permutations = this.flexibleTaxonomy ? (0, util_3.getCommandIdPermutations)(topic.name) : [topic.name];
          for (let permutation of permutations)
            this.topicPermutations.add(permutation, topic.name);
        }
        for (let c of plugin.commands.filter((c2) => !c2.hidden)) {
          let parts = c.id.split(":");
          for (; parts.length > 0; ) {
            let name = parts.join(":");
            name && !this._topics.has(name) && this._topics.set(name, { description: c.summary || c.description, name }), parts.pop();
          }
        }
        marker?.stop();
      }
    };
    exports.Config = Config;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/index.js
var require_config4 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/config/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.tsPath = exports.Plugin = exports.Config = void 0;
    var config_1 = require_config3();
    Object.defineProperty(exports, "Config", { enumerable: !0, get: function() {
      return config_1.Config;
    } });
    var plugin_1 = require_plugin();
    Object.defineProperty(exports, "Plugin", { enumerable: !0, get: function() {
      return plugin_1.Plugin;
    } });
    var ts_path_1 = require_ts_path();
    Object.defineProperty(exports, "tsPath", { enumerable: !0, get: function() {
      return ts_path_1.tsPath;
    } });
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/list.js
var require_list = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/cli-ux/list.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.renderList = void 0;
    var screen_1 = require_screen(), util_1 = require_util(), wordwrap = require_wordwrap();
    function linewrap(length, s) {
      return wordwrap(length, screen_1.stdtermwidth, {
        skipScheme: "ansi-color"
      })(s).trim();
    }
    function renderList(items) {
      if (items.length === 0)
        return "";
      let maxLength = (0, util_1.maxBy)(items, (item) => item[0].length)?.[0].length ?? 0;
      return items.map((i) => {
        let left = i[0], right = i[1];
        return right ? (left = left.padEnd(maxLength), right = linewrap(maxLength + 2, right), `${left}  ${right}`) : left;
      }).join(`
`);
    }
    exports.renderList = renderList;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/errors.js
var require_errors2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/errors.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.FailedFlagValidationError = exports.ArgInvalidOptionError = exports.FlagInvalidOptionError = exports.NonExistentFlagsError = exports.UnexpectedArgsError = exports.RequiredArgsError = exports.InvalidArgsSpecError = exports.CLIParseError = exports.CLIError = void 0;
    var chalk_1 = __importDefault(require_source()), cache_1 = __importDefault(require_cache()), list_1 = require_list(), errors_1 = require_errors(), util_1 = require_util(), errors_2 = require_errors();
    Object.defineProperty(exports, "CLIError", { enumerable: !0, get: function() {
      return errors_2.CLIError;
    } });
    var CLIParseError = class extends errors_1.CLIError {
      parse;
      showHelp = !1;
      constructor(options) {
        options.message += `
See more help with --help`, super(options.message, { exit: options.exit }), this.parse = options.parse;
      }
    };
    exports.CLIParseError = CLIParseError;
    var InvalidArgsSpecError = class extends CLIParseError {
      args;
      constructor({ args, exit, parse: parse2 }) {
        let message = "Invalid argument spec", namedArgs = Object.values(args).filter((a) => a.name);
        if (namedArgs.length > 0) {
          let list = (0, list_1.renderList)(namedArgs.map((a) => [`${a.name} (${a.required ? "required" : "optional"})`, a.description]));
          message += `:
${list}`;
        }
        super({ exit: cache_1.default.getInstance().get("exitCodes")?.invalidArgsSpec ?? exit, message, parse: parse2 }), this.args = args;
      }
    };
    exports.InvalidArgsSpecError = InvalidArgsSpecError;
    var RequiredArgsError = class extends CLIParseError {
      args;
      constructor({ args, exit, flagsWithMultiple, parse: parse2 }) {
        let message = `Missing ${args.length} required arg${args.length === 1 ? "" : "s"}`, namedArgs = args.filter((a) => a.name);
        if (namedArgs.length > 0) {
          let list = (0, list_1.renderList)(namedArgs.map((a) => {
            let description = a.options ? `(${a.options.join("|")}) ${a.description}` : a.description;
            return [a.name, description];
          }));
          message += `:
${list}`;
        }
        if (flagsWithMultiple?.length) {
          let flags = flagsWithMultiple.map((f) => `--${f}`).join(", ");
          message += `

Note: ${flags} allow${flagsWithMultiple.length === 1 ? "s" : ""} multiple values. Because of this you need to provide all arguments before providing ${flagsWithMultiple.length === 1 ? "that flag" : "those flags"}.`, message += `
Alternatively, you can use "--" to signify the end of the flags and the beginning of arguments.`;
        }
        super({ exit: cache_1.default.getInstance().get("exitCodes")?.requiredArgs ?? exit, message, parse: parse2 }), this.args = args, this.showHelp = !0;
      }
    };
    exports.RequiredArgsError = RequiredArgsError;
    var UnexpectedArgsError = class extends CLIParseError {
      args;
      constructor({ args, exit, parse: parse2 }) {
        let message = `Unexpected argument${args.length === 1 ? "" : "s"}: ${args.join(", ")}`;
        super({ exit: cache_1.default.getInstance().get("exitCodes")?.unexpectedArgs ?? exit, message, parse: parse2 }), this.args = args, this.showHelp = !0;
      }
    };
    exports.UnexpectedArgsError = UnexpectedArgsError;
    var NonExistentFlagsError = class extends CLIParseError {
      flags;
      constructor({ exit, flags, parse: parse2 }) {
        let message = `Nonexistent flag${flags.length === 1 ? "" : "s"}: ${flags.join(", ")}`;
        super({ exit: cache_1.default.getInstance().get("exitCodes")?.nonExistentFlag ?? exit, message, parse: parse2 }), this.flags = flags, this.showHelp = !0;
      }
    };
    exports.NonExistentFlagsError = NonExistentFlagsError;
    var FlagInvalidOptionError = class extends CLIParseError {
      constructor(flag, input) {
        let message = `Expected --${flag.name}=${input} to be one of: ${flag.options.join(", ")}`;
        super({ message, parse: {} });
      }
    };
    exports.FlagInvalidOptionError = FlagInvalidOptionError;
    var ArgInvalidOptionError = class extends CLIParseError {
      constructor(arg, input) {
        let message = `Expected ${input} to be one of: ${arg.options.join(", ")}`;
        super({ message, parse: {} });
      }
    };
    exports.ArgInvalidOptionError = ArgInvalidOptionError;
    var FailedFlagValidationError = class extends CLIParseError {
      constructor({ exit, failed, parse: parse2 }) {
        let reasons = failed.map((r) => r.reason), deduped = (0, util_1.uniq)(reasons), message = `The following ${deduped.length === 1 ? "error" : "errors"} occurred:
  ${chalk_1.default.dim(deduped.join(`
  `))}`;
        super({ exit: cache_1.default.getInstance().get("exitCodes")?.failedFlagValidation ?? exit, message, parse: parse2 });
      }
    };
    exports.FailedFlagValidationError = FailedFlagValidationError;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/parse.js
var require_parse = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/parse.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.Parser = exports.readStdin = void 0;
    var node_readline_1 = __require("node:readline"), cache_1 = __importDefault(require_cache()), util_1 = require_util(), errors_1 = require_errors2(), debug2;
    try {
      debug2 = process.env.CLI_FLAGS_DEBUG === "1" ? require_src()("../parser") : () => {
      };
    } catch {
      debug2 = () => {
      };
    }
    var readStdin = async () => {
      let { stdin, stdout } = process;
      return stdin.isTTY ? null : global.oclif?.stdinCache ? (debug2("resolved stdin from global cache", global.oclif.stdinCache), global.oclif.stdinCache) : new Promise((resolve2) => {
        let result = "", ac = new AbortController(), { signal } = ac, timeout = setTimeout(() => ac.abort(), 10), rl = (0, node_readline_1.createInterface)({
          input: stdin,
          output: stdout,
          terminal: !1
        });
        rl.on("line", (line) => {
          result += line;
        }), rl.once("close", () => {
          clearTimeout(timeout), debug2("resolved from stdin", result), global.oclif = { ...global.oclif, stdinCache: result }, resolve2(result);
        }), signal.addEventListener("abort", () => {
          debug2("stdin aborted"), clearTimeout(timeout), rl.close(), resolve2(null);
        }, { once: !0 });
      });
    };
    exports.readStdin = readStdin;
    function isNegativeNumber(input) {
      return /^-\d/g.test(input);
    }
    var validateOptions = (flag, input) => {
      if (flag.options && !flag.options.includes(input))
        throw new errors_1.FlagInvalidOptionError(flag, input);
      return input;
    }, Parser = class {
      input;
      argv;
      booleanFlags;
      context;
      currentFlag;
      flagAliases;
      raw = [];
      constructor(input) {
        this.input = input, this.context = input.context ?? {}, this.argv = [...input.argv], this._setNames(), this.booleanFlags = (0, util_1.pickBy)(input.flags, (f) => f.type === "boolean"), this.flagAliases = Object.fromEntries(Object.values(input.flags).flatMap((flag) => [...flag.aliases ?? [], ...flag.charAliases ?? []].map((a) => [a, flag])));
      }
      async parse() {
        this._debugInput();
        let parseFlag = async (arg) => {
          let { isLong, name } = this.findFlag(arg);
          if (!name) {
            let i = arg.indexOf("=");
            if (i !== -1) {
              let sliced = arg.slice(i + 1);
              this.argv.unshift(sliced);
              let equalsParsed = await parseFlag(arg.slice(0, i));
              return equalsParsed || this.argv.shift(), equalsParsed;
            }
            return !1;
          }
          let flag = this.input.flags[name];
          if (flag.type === "option") {
            if (!flag.multiple && this.raw.some((o) => o.type === "flag" && o.flag === name))
              throw new errors_1.CLIError(`Flag --${name} can only be specified once`);
            this.currentFlag = flag;
            let input = isLong || arg.length < 3 ? this.argv.shift() : arg.slice(arg[2] === "=" ? 3 : 2);
            if (flag.allowStdin === "only" && input !== "-" && input !== void 0 && !this.findFlag(input).name)
              throw new errors_1.CLIError(`Flag --${name} can only be read from stdin. The value must be "-" or not provided at all.`);
            if (flag.allowStdin && input === "-" || flag.allowStdin === "only") {
              let stdin = await (0, exports.readStdin)();
              stdin && (input = stdin.trim());
            }
            if (typeof input != "string" || this.findFlag(input).name)
              throw flag.options ? new errors_1.CLIError(`Flag --${name} expects one of these values: ${flag.options.join(", ")}`) : new errors_1.CLIError(`Flag --${name} expects a value`);
            this.raw.push({ flag: flag.name, input, type: "flag" });
          } else
            this.raw.push({ flag: flag.name, input: arg, type: "flag" }), !isLong && arg.length > 2 && this.argv.unshift(`-${arg.slice(2)}`);
          return !0;
        }, parsingFlags = !0, nonExistentFlags = [], dashdash = !1, originalArgv = [...this.argv];
        for (; this.argv.length > 0; ) {
          let input = this.argv.shift();
          if (parsingFlags && input.startsWith("-") && input !== "-") {
            if (this.input["--"] !== !1 && input === "--") {
              parsingFlags = !1;
              continue;
            }
            if (await parseFlag(input))
              continue;
            if (input === "--") {
              dashdash = !0;
              continue;
            }
            if (this.input["--"] !== !1 && !isNegativeNumber(input)) {
              nonExistentFlags.push(input);
              continue;
            }
          }
          if (parsingFlags && this.currentFlag && this.currentFlag.multiple && !this.currentFlag.multipleNonGreedy) {
            this.raw.push({ flag: this.currentFlag.name, input, type: "flag" });
            continue;
          }
          let arg = Object.keys(this.input.args)[this._argTokens.length];
          this.raw.push({ arg, input, type: "arg" });
        }
        let [{ args, argv }, { flags, metadata }] = await Promise.all([this._args(), this._flags()]);
        this._debugOutput(argv, args, flags);
        let unsortedArgv = dashdash ? [...argv, ...nonExistentFlags, "--"] : [...argv, ...nonExistentFlags];
        return {
          args,
          argv: unsortedArgv.sort((a, b) => originalArgv.indexOf(a) - originalArgv.indexOf(b)),
          flags,
          metadata,
          nonExistentFlags,
          raw: this.raw
        };
      }
      async _args() {
        let argv = [], args = {}, tokens = this._argTokens, stdinRead = !1, ctx = this.context;
        for (let [name, arg] of Object.entries(this.input.args)) {
          let token = tokens.find((t) => t.arg === name);
          if (ctx.token = token, token) {
            if (arg.options && !arg.options.includes(token.input))
              throw new errors_1.ArgInvalidOptionError(arg, token.input);
            let parsed = await arg.parse(token.input, ctx, arg);
            argv.push(parsed), args[token.arg] = parsed;
          } else if (!arg.ignoreStdin && !stdinRead) {
            let stdin = await (0, exports.readStdin)();
            if (stdin) {
              stdin = stdin.trim();
              let parsed = await arg.parse(stdin, ctx, arg);
              argv.push(parsed), args[name] = parsed;
            }
            stdinRead = !0;
          }
          if (!args[name] && (arg.default || arg.default === !1))
            if (typeof arg.default == "function") {
              let f = await arg.default();
              argv.push(f), args[name] = f;
            } else
              argv.push(arg.default), args[name] = arg.default;
        }
        for (let token of tokens)
          args[token.arg] === void 0 && argv.push(token.input);
        return { args, argv };
      }
      get _argTokens() {
        return this.raw.filter((o) => o.type === "arg");
      }
      _debugInput() {
        debug2("input: %s", this.argv.join(" "));
        let args = Object.keys(this.input.args);
        args.length > 0 && debug2("available args: %s", args.join(" ")), Object.keys(this.input.flags).length !== 0 && debug2("available flags: %s", Object.keys(this.input.flags).map((f) => `--${f}`).join(" "));
      }
      _debugOutput(args, flags, argv) {
        argv.length > 0 && debug2("argv: %o", argv), Object.keys(args).length > 0 && debug2("args: %o", args), Object.keys(flags).length > 0 && debug2("flags: %o", flags);
      }
      async _flags() {
        let parseFlagOrThrowError = async (input, flag, context, token) => {
          if (!flag.parse)
            return input;
          let ctx = {
            ...context,
            error: context?.error,
            exit: context?.exit,
            log: context?.log,
            logToStderr: context?.logToStderr,
            token,
            warn: context?.warn
          };
          try {
            return flag.type === "boolean" ? await flag.parse(input, ctx, flag) : await flag.parse(input, ctx, flag);
          } catch (error) {
            throw error.message = `Parsing --${flag.name} 
	${error.message}
See more help with --help`, cache_1.default.getInstance().get("exitCodes")?.failedFlagParsing && (error.oclif = { exit: cache_1.default.getInstance().get("exitCodes")?.failedFlagParsing }), error;
          }
        }, addValueFunction = (fws) => {
          if (fws.tokens?.length) {
            if (fws.inputFlag.flag.type === "boolean" && (0, util_1.last)(fws.tokens)?.input)
              return {
                ...fws,
                valueFunction: async (i) => parseFlagOrThrowError((0, util_1.last)(i.tokens)?.input !== `--no-${i.inputFlag.name}`, i.inputFlag.flag, this.context, (0, util_1.last)(i.tokens))
              };
            if (fws.inputFlag.flag.type === "option" && fws.inputFlag.flag.delimiter && fws.inputFlag.flag.multiple)
              return {
                ...fws,
                valueFunction: async (i) => (await Promise.all((i.tokens ?? []).flatMap((token) => token.input.split(i.inputFlag.flag.delimiter ?? ",")).map((v) => v.trim().replace(/^"(.*)"$/, "$1").replace(/^'(.*)'$/, "$1")).map(async (v) => parseFlagOrThrowError(v, i.inputFlag.flag, this.context, {
                  ...(0, util_1.last)(i.tokens),
                  input: v
                })))).map((v) => validateOptions(i.inputFlag.flag, v))
              };
            if (fws.inputFlag.flag.type === "option" && fws.inputFlag.flag.multiple)
              return {
                ...fws,
                valueFunction: async (i) => Promise.all((fws.tokens ?? []).map((token) => parseFlagOrThrowError(validateOptions(i.inputFlag.flag, token.input), i.inputFlag.flag, this.context, token)))
              };
            if (fws.inputFlag.flag.type === "option")
              return {
                ...fws,
                valueFunction: async (i) => parseFlagOrThrowError(validateOptions(i.inputFlag.flag, (0, util_1.last)(fws.tokens)?.input), i.inputFlag.flag, this.context, (0, util_1.last)(fws.tokens))
              };
          }
          if (fws.inputFlag.flag.env && process.env[fws.inputFlag.flag.env]) {
            let valueFromEnv = process.env[fws.inputFlag.flag.env];
            if (fws.inputFlag.flag.type === "option" && valueFromEnv)
              return {
                ...fws,
                valueFunction: async (i) => parseFlagOrThrowError(validateOptions(i.inputFlag.flag, valueFromEnv), i.inputFlag.flag, this.context)
              };
            if (fws.inputFlag.flag.type === "boolean")
              return {
                ...fws,
                valueFunction: async (i) => (0, util_1.isTruthy)(process.env[i.inputFlag.flag.env] ?? "false")
              };
          }
          return typeof fws.inputFlag.flag.default !== void 0 ? {
            ...fws,
            metadata: { setFromDefault: !0 },
            valueFunction: typeof fws.inputFlag.flag.default == "function" ? (i, allFlags = {}) => fws.inputFlag.flag.default({ flags: allFlags, options: i.inputFlag.flag }) : async () => fws.inputFlag.flag.default
          } : fws;
        }, addHelpFunction = (fws) => fws.inputFlag.flag.type === "option" && fws.inputFlag.flag.defaultHelp ? {
          ...fws,
          helpFunction: typeof fws.inputFlag.flag.defaultHelp == "function" ? (i, flags, ...context) => (
            // @ts-expect-error flag type isn't specific enough to know defaultHelp will definitely be there
            i.inputFlag.flag.defaultHelp({ flags, options: i.inputFlag }, ...context)
          ) : (
            // @ts-expect-error flag type isn't specific enough to know defaultHelp will definitely be there
            (i) => i.inputFlag.flag.defaultHelp
          )
        } : fws, addDefaultHelp = async (fwsArray) => {
          let valueReferenceForHelp = fwsArrayToObject(flagsWithAllValues.filter((fws) => !fws.metadata?.setFromDefault));
          return Promise.all(fwsArray.map(async (fws) => {
            try {
              if (fws.helpFunction)
                return {
                  ...fws,
                  metadata: {
                    ...fws.metadata,
                    defaultHelp: await fws.helpFunction?.(fws, valueReferenceForHelp, this.context)
                  }
                };
            } catch {
            }
            return fws;
          }));
        }, fwsArrayToObject = (fwsArray) => Object.fromEntries(fwsArray.filter((fws) => fws.value !== void 0).map((fws) => [fws.inputFlag.name, fws.value])), flagTokenMap = this.mapAndValidateFlags(), flagsWithValues = await Promise.all(Object.entries(this.input.flags).filter(([name, flag]) => flag.type === "boolean" || flag.env || flag.default !== void 0 || "defaultHelp" in flag || flagTokenMap.has(name)).map(([name, flag]) => ({ inputFlag: { flag, name }, tokens: flagTokenMap.get(name) })).map((fws) => addValueFunction(fws)).filter((fws) => fws.valueFunction !== void 0).map((fws) => addHelpFunction(fws)).map(async (fws) => fws.metadata?.setFromDefault ? fws : { ...fws, value: await fws.valueFunction?.(fws) })), valueReference = fwsArrayToObject(flagsWithValues.filter((fws) => !fws.metadata?.setFromDefault)), flagsWithAllValues = await Promise.all(flagsWithValues.map(async (fws) => fws.metadata?.setFromDefault ? { ...fws, value: await fws.valueFunction?.(fws, valueReference) } : fws)), finalFlags = flagsWithAllValues.some((fws) => typeof fws.helpFunction == "function") ? await addDefaultHelp(flagsWithAllValues) : flagsWithAllValues;
        return {
          flags: fwsArrayToObject(finalFlags),
          metadata: {
            flags: Object.fromEntries(finalFlags.filter((fws) => fws.metadata).map((fws) => [fws.inputFlag.name, fws.metadata]))
          }
        };
      }
      _setNames() {
        for (let k of Object.keys(this.input.flags))
          this.input.flags[k].name = k;
        for (let k of Object.keys(this.input.args))
          this.input.args[k].name = k;
      }
      findFlag(arg) {
        let isLong = arg.startsWith("--"), short = isLong ? !1 : arg.startsWith("-"), name = isLong ? this.findLongFlag(arg) : short ? this.findShortFlag(arg) : void 0;
        return { isLong, name };
      }
      findLongFlag(arg) {
        let name = arg.slice(2);
        if (this.input.flags[name])
          return name;
        if (this.flagAliases[name])
          return this.flagAliases[name].name;
        if (arg.startsWith("--no-")) {
          let flag = this.booleanFlags[arg.slice(5)];
          if (flag && flag.allowNo)
            return flag.name;
        }
      }
      findShortFlag([_, char]) {
        return this.flagAliases[char] ? this.flagAliases[char].name : Object.keys(this.input.flags).find((k) => this.input.flags[k].char === char && char !== void 0 && this.input.flags[k].char !== void 0);
      }
      mapAndValidateFlags() {
        let flagTokenMap = /* @__PURE__ */ new Map();
        for (let token of this.raw.filter((o) => o.type === "flag")) {
          if (!(token.flag in this.input.flags))
            throw new errors_1.CLIError(`Unexpected flag ${token.flag}`);
          let existing = flagTokenMap.get(token.flag) ?? [];
          flagTokenMap.set(token.flag, [...existing, token]);
        }
        return flagTokenMap;
      }
    };
    exports.Parser = Parser;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/validate.js
var require_validate = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/validate.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.validate = void 0;
    var util_1 = require_util(), errors_1 = require_errors2();
    async function validate(parse2) {
      let cachedResolvedFlags;
      function validateArgs() {
        if (parse2.output.nonExistentFlags?.length > 0)
          throw new errors_1.NonExistentFlagsError({
            flags: parse2.output.nonExistentFlags,
            parse: parse2
          });
        let maxArgs = Object.keys(parse2.input.args).length;
        if (parse2.input.strict && parse2.output.argv.length > maxArgs) {
          let extras = parse2.output.argv.slice(maxArgs);
          throw new errors_1.UnexpectedArgsError({
            args: extras,
            parse: parse2
          });
        }
        let missingRequiredArgs = [], hasOptional = !1;
        for (let [name, arg] of Object.entries(parse2.input.args)) {
          if (!arg.required)
            hasOptional = !0;
          else if (hasOptional)
            throw new errors_1.InvalidArgsSpecError({
              args: parse2.input.args,
              parse: parse2
            });
          arg.required && parse2.output.args[name] === void 0 && missingRequiredArgs.push(arg);
        }
        if (missingRequiredArgs.length > 0) {
          let flagsWithMultiple = Object.entries(parse2.input.flags).filter(([_, flagDef]) => flagDef.type === "option" && !!flagDef.multiple).map(([name]) => name);
          throw new errors_1.RequiredArgsError({
            args: missingRequiredArgs,
            flagsWithMultiple,
            parse: parse2
          });
        }
      }
      async function validateFlags() {
        let promises = Object.entries(parse2.input.flags).flatMap(([name, flag]) => parse2.output.flags[name] !== void 0 ? [
          ...flag.relationships ? validateRelationships(name, flag) : [],
          ...flag.dependsOn ? [validateDependsOn(name, flag.dependsOn)] : [],
          ...flag.exclusive ? [validateExclusive(name, flag.exclusive)] : [],
          ...flag.exactlyOne ? [validateExactlyOne(name, flag.exactlyOne)] : []
        ] : flag.required ? [{ name, reason: `Missing required flag ${name}`, status: "failed", validationFn: "required" }] : flag.exactlyOne && flag.exactlyOne.length > 0 ? [validateAcrossFlags(flag)] : []), failed = (await Promise.all(promises)).filter((r) => r.status === "failed");
        if (failed.length > 0)
          throw new errors_1.FailedFlagValidationError({
            failed,
            parse: parse2
          });
      }
      async function resolveFlags(flags) {
        if (cachedResolvedFlags)
          return cachedResolvedFlags;
        let promises = flags.map(async (flag) => typeof flag == "string" ? [flag, parse2.output.flags[flag]] : await flag.when(parse2.output.flags) ? [flag.name, parse2.output.flags[flag.name]] : null), resolved = await Promise.all(promises);
        return cachedResolvedFlags = Object.fromEntries(resolved.filter((r) => r !== null)), cachedResolvedFlags;
      }
      let getPresentFlags = (flags) => Object.keys(flags).filter((key) => key !== void 0);
      function validateAcrossFlags(flag) {
        let base = { name: flag.name, validationFn: "validateAcrossFlags" };
        if (Object.entries(parse2.input.flags).map((entry) => entry[0]).filter((flagName) => parse2.output.flags[flagName] !== void 0).filter((flagName) => flag.exactlyOne && flag.exactlyOne.includes(flagName)).length === 0) {
          let reason = `Exactly one of the following must be provided: ${(0, util_1.uniq)(flag.exactlyOne?.map((flag2) => `--${flag2}`) ?? []).join(", ")}`;
          return { ...base, reason, status: "failed" };
        }
        return { ...base, status: "success" };
      }
      async function validateExclusive(name, flags) {
        let base = { name, validationFn: "validateExclusive" }, resolved = await resolveFlags(flags), keys = getPresentFlags(resolved);
        for (let flag of keys)
          if (!(parse2.output.metadata.flags && parse2.output.metadata.flags[flag]?.setFromDefault) && !(parse2.output.metadata.flags && parse2.output.metadata.flags[name]?.setFromDefault) && parse2.output.flags[flag] !== void 0) {
            let flagValue = parse2.output.metadata.flags?.[flag]?.defaultHelp ?? parse2.output.flags[flag];
            return {
              ...base,
              reason: `--${flag}=${flagValue} cannot also be provided when using --${name}`,
              status: "failed"
            };
          }
        return { ...base, status: "success" };
      }
      async function validateExactlyOne(name, flags) {
        let base = { name, validationFn: "validateExactlyOne" }, resolved = await resolveFlags(flags), keys = getPresentFlags(resolved);
        for (let flag of keys)
          if (flag !== name && parse2.output.flags[flag] !== void 0)
            return { ...base, reason: `--${flag} cannot also be provided when using --${name}`, status: "failed" };
        return { ...base, status: "success" };
      }
      async function validateDependsOn(name, flags) {
        let base = { name, validationFn: "validateDependsOn" }, resolved = await resolveFlags(flags);
        if (!Object.values(resolved).every((val) => val !== void 0)) {
          let formattedFlags = Object.keys(resolved).map((f) => `--${f}`).join(", ");
          return {
            ...base,
            reason: `All of the following must be provided when using --${name}: ${formattedFlags}`,
            status: "failed"
          };
        }
        return { ...base, status: "success" };
      }
      async function validateSome(name, flags) {
        let base = { name, validationFn: "validateSome" }, resolved = await resolveFlags(flags);
        if (!Object.values(resolved).some(Boolean)) {
          let formattedFlags = Object.keys(resolved).map((f) => `--${f}`).join(", ");
          return {
            ...base,
            reason: `One of the following must be provided when using --${name}: ${formattedFlags}`,
            status: "failed"
          };
        }
        return { ...base, status: "success" };
      }
      function validateRelationships(name, flag) {
        return (flag.relationships ?? []).map((relationship) => {
          switch (relationship.type) {
            case "all":
              return validateDependsOn(name, relationship.flags);
            case "some":
              return validateSome(name, relationship.flags);
            case "none":
              return validateExclusive(name, relationship.flags);
            default:
              throw new Error(`Unknown relationship type: ${relationship.type}`);
          }
        });
      }
      return validateArgs(), validateFlags();
    }
    exports.validate = validate;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/help.js
var require_help2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/help.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.flagUsages = exports.flagUsage = void 0;
    var chalk_1 = __importDefault(require_source()), util_1 = require_util();
    function flagUsage(flag, options = {}) {
      let label = [];
      flag.helpLabel ? label.push(flag.helpLabel) : (flag.char && label.push(`-${flag.char}`), flag.name && label.push(` --${flag.name}`));
      let usage = flag.type === "option" ? ` ${flag.name.toUpperCase()}` : "", description = flag.summary || flag.description || "";
      return options.displayRequired && flag.required && (description = `(required) ${description}`), description = description ? chalk_1.default.dim(description) : void 0, [` ${label.join(",").trim()}${usage}`, description];
    }
    exports.flagUsage = flagUsage;
    function flagUsages(flags, options = {}) {
      return flags.length === 0 ? [] : (0, util_1.sortBy)(flags, (f) => [f.char ? -1 : 1, f.char, f.name]).map((f) => flagUsage(f, options));
    }
    exports.flagUsages = flagUsages;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/index.js
var require_parser = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/parser/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.parse = exports.flagUsages = void 0;
    var parse_1 = require_parse(), validate_1 = require_validate(), help_1 = require_help2();
    Object.defineProperty(exports, "flagUsages", { enumerable: !0, get: function() {
      return help_1.flagUsages;
    } });
    async function parse2(argv, options) {
      let input = {
        "--": options["--"],
        args: options.args ?? {},
        argv,
        context: options.context,
        flags: options.flags ?? {},
        strict: options.strict !== !1
      }, output = await new parse_1.Parser(input).parse();
      return await (0, validate_1.validate)({ input, output }), output;
    }
    exports.parse = parse2;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/command.js
var require_command2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/command.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.Command = void 0;
    var chalk_1 = __importDefault(require_source()), node_url_1 = __require("node:url"), node_util_1 = __require("node:util"), cache_1 = __importDefault(require_cache()), cli_ux_1 = require_cli_ux(), config_1 = require_config4(), Errors = __importStar(require_errors()), util_1 = require_util3(), Parser = __importStar(require_parser()), aggregate_flags_1 = require_aggregate_flags(), ids_1 = require_ids(), util_2 = require_util(), pjson = cache_1.default.getInstance().get("@oclif/core");
    process.stdout.on("error", (err) => {
      if (!(err && err.code === "EPIPE"))
        throw err;
    });
    var Command = class {
      argv;
      config;
      /** An array of aliases for this command. */
      static aliases = [];
      /** An order-dependent object of arguments for the command */
      static args = {};
      static baseFlags;
      /**
       * Emit deprecation warning when a command alias is used
       */
      static deprecateAliases;
      static deprecationOptions;
      /**
       * A full description of how to use the command.
       *
       * If no summary, the first line of the description will be used as the summary.
       */
      static description;
      static enableJsonFlag = !1;
      /**
       * An array of examples to show at the end of the command's help.
       *
       * IF only a string is provided, it will try to look for a line that starts
       * with the cmd.bin as the example command and the rest as the description.
       * If found, the command will be formatted appropriately.
       *
       * ```
       * EXAMPLES:
       *   A description of a particular use case.
       *
       *     $ <%= config.bin => command flags
       * ```
       */
      static examples;
      /** A hash of flags for the command */
      static flags;
      static hasDynamicHelp = !1;
      static help;
      /** Hide the command from help */
      static hidden;
      /** An array of aliases for this command that are hidden from help. */
      static hiddenAliases = [];
      /** A command ID, used mostly in error or verbose reporting. */
      static id;
      static plugin;
      static pluginAlias;
      static pluginName;
      static pluginType;
      /** Mark the command as a given state (e.g. beta or deprecated) in help */
      static state;
      /** When set to false, allows a variable amount of arguments */
      static strict = !0;
      /**
       * The tweet-sized description for your class, used in a parent-commands
       * sub-command listing and as the header for the command help.
       */
      static summary;
      /**
       * An override string (or strings) for the default usage documentation.
       */
      static usage;
      debug;
      id;
      static _base = `${pjson.name}@${pjson.version}`;
      constructor(argv, config) {
        this.argv = argv, this.config = config, this.id = this.ctor.id;
        try {
          this.debug = require_src()(this.id ? `${this.config.bin}:${this.id}` : this.config.bin);
        } catch {
          this.debug = () => {
          };
        }
      }
      /**
       * instantiate and run the command
       *
       * @param {Command.Class} this - the command class
       * @param {string[]} argv argv
       * @param {LoadOptions} opts options
       * @returns {Promise<unknown>} result
       */
      static async run(argv, opts) {
        argv || (argv = process.argv.slice(2)), typeof opts == "string" && opts.startsWith("file://") && (opts = (0, node_url_1.fileURLToPath)(opts));
        let config = await config_1.Config.load(opts || __require.main?.filename || __dirname), cmd = new this(argv, config);
        if (!cmd.id) {
          let id = cmd.constructor.name.toLowerCase();
          cmd.id = id, cmd.ctor.id = id;
        }
        return cmd._run();
      }
      get ctor() {
        return this.constructor;
      }
      async catch(err) {
        if (process.exitCode = process.exitCode ?? err.exitCode ?? 1, this.jsonEnabled())
          this.logJson(this.toErrorJson(err));
        else {
          if (!err.message)
            throw err;
          try {
            cli_ux_1.ux.action.stop(chalk_1.default.bold.red("!"));
          } catch {
          }
          throw err;
        }
      }
      error(input, options = {}) {
        return Errors.error(input, options);
      }
      exit(code = 0) {
        Errors.exit(code);
      }
      async finally(_) {
        try {
          let { config } = Errors;
          config.errorLogger && await config.errorLogger.flush();
        } catch (error) {
          console.error(error);
        }
      }
      async init() {
        this.debug("init version: %s argv: %o", this.ctor._base, this.argv), this.config.debug && (Errors.config.debug = !0), this.config.errlog && (Errors.config.errlog = this.config.errlog);
        let g = global;
        g["http-call"] = g["http-call"] || {}, g["http-call"].userAgent = this.config.userAgent, this.warnIfCommandDeprecated();
      }
      /**
       * Determine if the command is being run with the --json flag in a command that supports it.
       *
       * @returns {boolean} true if the command supports json and the --json flag is present
       */
      jsonEnabled() {
        if (!this.ctor.enableJsonFlag)
          return !1;
        if (this.config.scopedEnvVar?.("CONTENT_TYPE")?.toLowerCase() === "json")
          return !0;
        let passThroughIndex = this.argv.indexOf("--"), jsonIndex = this.argv.indexOf("--json");
        return passThroughIndex === -1 ? (
          // If '--' is not present, then check for `--json` in this.argv
          jsonIndex > -1
        ) : (
          // If '--' is present, return true only the --json flag exists and is before the '--'
          jsonIndex > -1 && jsonIndex < passThroughIndex
        );
      }
      log(message = "", ...args) {
        this.jsonEnabled() || (message = typeof message == "string" ? message : (0, node_util_1.inspect)(message), cli_ux_1.ux.info(message, ...args));
      }
      logJson(json) {
        cli_ux_1.ux.styledJSON(json);
      }
      logToStderr(message = "", ...args) {
        this.jsonEnabled() || (message = typeof message == "string" ? message : (0, node_util_1.inspect)(message), cli_ux_1.ux.logToStderr(message, ...args));
      }
      async parse(options, argv = this.argv) {
        options || (options = this.ctor);
        let opts = {
          context: this,
          ...options,
          flags: (0, aggregate_flags_1.aggregateFlags)(options.flags, options.baseFlags, options.enableJsonFlag)
        }, hookResult = await this.config.runHook("preparse", { argv: [...argv], options: opts }), argvToParse = hookResult.successes?.length ? hookResult.successes.find((s) => s.plugin.root === cache_1.default.getInstance().get("rootPlugin")?.root)?.result ?? argv : argv;
        this.argv = [...argvToParse];
        let results = await Parser.parse(argvToParse, opts);
        return this.warnIfFlagDeprecated(results.flags ?? {}), results;
      }
      toErrorJson(err) {
        return { error: err };
      }
      toSuccessJson(result) {
        return result;
      }
      warn(input) {
        return this.jsonEnabled() || Errors.warn(input), input;
      }
      warnIfCommandDeprecated() {
        let [id] = (0, util_1.normalizeArgv)(this.config);
        if (this.ctor.deprecateAliases && this.ctor.aliases.includes(id)) {
          let cmdName = (0, ids_1.toConfiguredId)(this.ctor.id, this.config), aliasName = (0, ids_1.toConfiguredId)(id, this.config);
          this.warn((0, util_1.formatCommandDeprecationWarning)(aliasName, { to: cmdName }));
        }
        if (this.ctor.state === "deprecated") {
          let cmdName = (0, ids_1.toConfiguredId)(this.ctor.id, this.config);
          this.warn((0, util_1.formatCommandDeprecationWarning)(cmdName, this.ctor.deprecationOptions));
        }
      }
      warnIfFlagDeprecated(flags) {
        let allFlags = (0, aggregate_flags_1.aggregateFlags)(this.ctor.flags, this.ctor.baseFlags, this.ctor.enableJsonFlag);
        for (let flag of Object.keys(flags)) {
          let flagDef = allFlags[flag], deprecated = flagDef?.deprecated;
          if (deprecated && this.warn((0, util_1.formatFlagDeprecationWarning)(flag, deprecated)), flagDef?.deprecateAliases) {
            let aliases = (0, util_2.uniq)([...flagDef?.aliases ?? [], ...flagDef?.charAliases ?? []]).map((a) => a.length === 1 ? `-${a}` : `--${a}`);
            if (aliases.length === 0)
              return;
            let foundAliases = aliases.filter((alias) => this.argv.some((a) => a.startsWith(alias)));
            for (let alias of foundAliases) {
              let preferredUsage = `--${flagDef?.name}`;
              flagDef?.char && (preferredUsage += ` | -${flagDef?.char}`), this.warn((0, util_1.formatFlagDeprecationWarning)(alias, { to: preferredUsage }));
            }
          }
        }
      }
      async _run() {
        let err, result;
        try {
          this.removeEnvVar("REDIRECTED"), await this.init(), result = await this.run();
        } catch (error) {
          err = error, await this.catch(error);
        } finally {
          await this.finally(err);
        }
        return result && this.jsonEnabled() && this.logJson(this.toSuccessJson(result)), result;
      }
      removeEnvVar(envVar) {
        let keys = [];
        try {
          keys.push(...this.config.scopedEnvVarKeys(envVar));
        } catch {
          keys.push(this.config.scopedEnvVarKey(envVar));
        }
        keys.map((key) => delete process.env[key]);
      }
    };
    exports.Command = Command;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/main.js
var require_main = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/main.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.run = exports.versionAddition = exports.helpAddition = void 0;
    var node_url_1 = __require("node:url"), cache_1 = __importDefault(require_cache()), cli_ux_1 = require_cli_ux(), config_1 = require_config4(), help_1 = require_help(), performance_1 = require_performance(), symbols_1 = require_symbols(), debug2 = require_src()("oclif:main"), helpAddition = (argv, config) => {
      if (argv.length === 0 && !config.isSingleCommandCLI)
        return !0;
      let mergedHelpFlags = (0, help_1.getHelpFlagAdditions)(config);
      for (let arg of argv) {
        if (mergedHelpFlags.includes(arg))
          return !0;
        if (arg === "--")
          return !1;
      }
      return !1;
    };
    exports.helpAddition = helpAddition;
    var versionAddition = (argv, config) => {
      let additionalVersionFlags = config?.pjson.oclif.additionalVersionFlags ?? [];
      return !![...(/* @__PURE__ */ new Set(["--version", ...additionalVersionFlags])).values()].includes(argv[0]);
    };
    exports.versionAddition = versionAddition;
    async function run(argv, options) {
      let marker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, "main.run"), initMarker = performance_1.Performance.mark(performance_1.OCLIF_MARKER_OWNER, "main.run#init"), collectPerf = async () => {
        marker?.stop(), initMarker?.stopped || initMarker?.stop(), await performance_1.Performance.collect(), performance_1.Performance.debug();
      }, showHelp = async (argv2) => {
        let Help = await (0, help_1.loadHelpClass)(config);
        await new Help(config, config.pjson.oclif.helpOptions ?? config.pjson.helpOptions).showHelp(argv2);
      };
      debug2(`process.execPath: ${process.execPath}`), debug2(`process.execArgv: ${process.execArgv}`), debug2("process.argv: %O", process.argv), argv = argv ?? process.argv.slice(2), options && (typeof options == "string" && options.startsWith("file://") || options instanceof node_url_1.URL) && (options = (0, node_url_1.fileURLToPath)(options));
      let config = await config_1.Config.load(options ?? __require.main?.filename ?? __dirname);
      cache_1.default.getInstance().set("config", config), config.isSingleCommandCLI && (argv = [symbols_1.SINGLE_COMMAND_CLI_SYMBOL, ...argv]);
      let [id, ...argvSlice] = (0, help_1.normalizeArgv)(config, argv);
      if (await config.runHook("init", { argv: argvSlice, id }), (0, exports.versionAddition)(argv, config)) {
        cli_ux_1.ux.log(config.userAgent), await collectPerf();
        return;
      }
      if ((0, exports.helpAddition)(argv, config)) {
        await showHelp(argv), await collectPerf();
        return;
      }
      let cmd = config.findCommand(id);
      if (!cmd && (config.flexibleTaxonomy ? null : config.findTopic(id))) {
        await showHelp([id]), await collectPerf();
        return;
      }
      initMarker?.stop();
      try {
        return await config.runCommand(id, argvSlice, cmd);
      } finally {
        await collectPerf();
      }
    }
    exports.run = run;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/execute.js
var require_execute = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/execute.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.execute = void 0;
    var flush_1 = require_flush(), errors_1 = require_errors(), handle_1 = require_handle(), main_1 = require_main(), settings_1 = require_settings();
    async function execute(options) {
      if (!options.dir && !options.loadOptions)
        throw new errors_1.CLIError("dir or loadOptions is required.");
      return options.development && (process.env.NODE_ENV = "development", settings_1.settings.debug = !0), (0, main_1.run)(options.args ?? process.argv.slice(2), options.loadOptions ?? options.dir).then(async (result) => ((0, flush_1.flush)(), result)).catch(async (error) => (0, handle_1.handle)(error));
    }
    exports.execute = execute;
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/interfaces/index.js
var require_interfaces = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/interfaces/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
  }
});

// ../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/index.js
var require_lib2 = __commonJS({
  "../../node_modules/.pnpm/@oclif+core@3.26.5/node_modules/@oclif/core/lib/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k);
      var desc = Object.getOwnPropertyDescriptor(m, k);
      (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) && (desc = { enumerable: !0, get: function() {
        return m[k];
      } }), Object.defineProperty(o, k2, desc);
    }) : (function(o, m, k, k2) {
      k2 === void 0 && (k2 = k), o[k2] = m[k];
    })), __setModuleDefault = exports && exports.__setModuleDefault || (Object.create ? (function(o, v) {
      Object.defineProperty(o, "default", { enumerable: !0, value: v });
    }) : function(o, v) {
      o.default = v;
    }), __importStar = exports && exports.__importStar || function(mod) {
      if (mod && mod.__esModule) return mod;
      var result = {};
      if (mod != null) for (var k in mod) k !== "default" && Object.prototype.hasOwnProperty.call(mod, k) && __createBinding(result, mod, k);
      return __setModuleDefault(result, mod), result;
    }, __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.toStandardizedId = exports.toConfiguredId = exports.settings = exports.Performance = exports.Parser = exports.ModuleLoader = exports.run = exports.Interfaces = exports.loadHelpClass = exports.HelpBase = exports.Help = exports.CommandHelp = exports.Flags = exports.execute = exports.handle = exports.Errors = exports.Plugin = exports.Config = exports.Command = exports.stdout = exports.stderr = exports.flush = exports.ux = exports.Args = void 0;
    var write_1 = __importDefault(require_write());
    function checkCWD() {
      try {
        process.cwd();
      } catch (error) {
        error.code === "ENOENT" && write_1.default.stderr(`WARNING: current directory does not exist
`);
      }
    }
    checkCWD();
    exports.Args = __importStar(require_args());
    exports.ux = __importStar(require_cli_ux());
    var flush_1 = require_flush();
    Object.defineProperty(exports, "flush", { enumerable: !0, get: function() {
      return flush_1.flush;
    } });
    var stream_1 = require_stream();
    Object.defineProperty(exports, "stderr", { enumerable: !0, get: function() {
      return stream_1.stderr;
    } });
    Object.defineProperty(exports, "stdout", { enumerable: !0, get: function() {
      return stream_1.stdout;
    } });
    var command_1 = require_command2();
    Object.defineProperty(exports, "Command", { enumerable: !0, get: function() {
      return command_1.Command;
    } });
    var config_1 = require_config4();
    Object.defineProperty(exports, "Config", { enumerable: !0, get: function() {
      return config_1.Config;
    } });
    Object.defineProperty(exports, "Plugin", { enumerable: !0, get: function() {
      return config_1.Plugin;
    } });
    exports.Errors = __importStar(require_errors());
    var handle_1 = require_handle();
    Object.defineProperty(exports, "handle", { enumerable: !0, get: function() {
      return handle_1.handle;
    } });
    var execute_1 = require_execute();
    Object.defineProperty(exports, "execute", { enumerable: !0, get: function() {
      return execute_1.execute;
    } });
    exports.Flags = __importStar(require_flags());
    var help_1 = require_help();
    Object.defineProperty(exports, "CommandHelp", { enumerable: !0, get: function() {
      return help_1.CommandHelp;
    } });
    Object.defineProperty(exports, "Help", { enumerable: !0, get: function() {
      return help_1.Help;
    } });
    Object.defineProperty(exports, "HelpBase", { enumerable: !0, get: function() {
      return help_1.HelpBase;
    } });
    Object.defineProperty(exports, "loadHelpClass", { enumerable: !0, get: function() {
      return help_1.loadHelpClass;
    } });
    exports.Interfaces = __importStar(require_interfaces());
    var main_1 = require_main();
    Object.defineProperty(exports, "run", { enumerable: !0, get: function() {
      return main_1.run;
    } });
    exports.ModuleLoader = __importStar(require_module_loader());
    exports.Parser = __importStar(require_parser());
    var performance_1 = require_performance();
    Object.defineProperty(exports, "Performance", { enumerable: !0, get: function() {
      return performance_1.Performance;
    } });
    var settings_1 = require_settings();
    Object.defineProperty(exports, "settings", { enumerable: !0, get: function() {
      return settings_1.settings;
    } });
    var ids_1 = require_ids();
    Object.defineProperty(exports, "toConfiguredId", { enumerable: !0, get: function() {
      return ids_1.toConfiguredId;
    } });
    Object.defineProperty(exports, "toStandardizedId", { enumerable: !0, get: function() {
      return ids_1.toStandardizedId;
    } });
  }
});

// ../../node_modules/.pnpm/@kwsites+file-exists@1.1.1/node_modules/@kwsites/file-exists/dist/src/index.js
var require_src2 = __commonJS({
  "../../node_modules/.pnpm/@kwsites+file-exists@1.1.1/node_modules/@kwsites/file-exists/dist/src/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    var __importDefault = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { default: mod };
    };
    Object.defineProperty(exports, "__esModule", { value: !0 });
    var fs_1 = __require("fs"), debug_1 = __importDefault(require_src()), log = debug_1.default("@kwsites/file-exists");
    function check(path3, isFile, isDirectory2) {
      log("checking %s", path3);
      try {
        let stat = fs_1.statSync(path3);
        return stat.isFile() && isFile ? (log("[OK] path represents a file"), !0) : stat.isDirectory() && isDirectory2 ? (log("[OK] path represents a directory"), !0) : (log("[FAIL] path represents something other than a file or directory"), !1);
      } catch (e) {
        if (e.code === "ENOENT")
          return log("[FAIL] path is not accessible: %o", e), !1;
        throw log("[FATAL] %o", e), e;
      }
    }
    function exists2(path3, type = exports.READABLE) {
      return check(path3, (type & exports.FILE) > 0, (type & exports.FOLDER) > 0);
    }
    exports.exists = exists2;
    exports.FILE = 1;
    exports.FOLDER = 2;
    exports.READABLE = exports.FILE + exports.FOLDER;
  }
});

// ../../node_modules/.pnpm/@kwsites+file-exists@1.1.1/node_modules/@kwsites/file-exists/dist/index.js
var require_dist = __commonJS({
  "../../node_modules/.pnpm/@kwsites+file-exists@1.1.1/node_modules/@kwsites/file-exists/dist/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    function __export2(m) {
      for (var p in m) exports.hasOwnProperty(p) || (exports[p] = m[p]);
    }
    Object.defineProperty(exports, "__esModule", { value: !0 });
    __export2(require_src2());
  }
});

// ../../node_modules/.pnpm/@kwsites+promise-deferred@1.1.1/node_modules/@kwsites/promise-deferred/dist/index.js
var require_dist2 = __commonJS({
  "../../node_modules/.pnpm/@kwsites+promise-deferred@1.1.1/node_modules/@kwsites/promise-deferred/dist/index.js"(exports) {
    "use strict";
    init_cjs_shims();
    Object.defineProperty(exports, "__esModule", { value: !0 });
    exports.createDeferred = exports.deferred = void 0;
    function deferred2() {
      let done, fail, status = "pending";
      return {
        promise: new Promise((_done, _fail) => {
          done = _done, fail = _fail;
        }),
        done(result) {
          status === "pending" && (status = "resolved", done(result));
        },
        fail(error) {
          status === "pending" && (status = "rejected", fail(error));
        },
        get fulfilled() {
          return status !== "pending";
        },
        get status() {
          return status;
        }
      };
    }
    exports.deferred = deferred2;
    exports.createDeferred = deferred2;
    exports.default = deferred2;
  }
});

// ../../node_modules/.pnpm/ignore@6.0.2/node_modules/ignore/index.js
var require_ignore = __commonJS({
  "../../node_modules/.pnpm/ignore@6.0.2/node_modules/ignore/index.js"(exports, module) {
    init_cjs_shims();
    function makeArray(subject) {
      return Array.isArray(subject) ? subject : [subject];
    }
    var EMPTY = "", SPACE = " ", ESCAPE = "\\", REGEX_TEST_BLANK_LINE = /^\s+$/, REGEX_INVALID_TRAILING_BACKSLASH = /(?:[^\\]|^)\\$/, REGEX_REPLACE_LEADING_EXCAPED_EXCLAMATION = /^\\!/, REGEX_REPLACE_LEADING_EXCAPED_HASH = /^\\#/, REGEX_SPLITALL_CRLF = /\r?\n/g, REGEX_TEST_INVALID_PATH = /^\.*\/|^\.+$/, SLASH = "/", TMP_KEY_IGNORE = "node-ignore";
    typeof Symbol < "u" && (TMP_KEY_IGNORE = /* @__PURE__ */ Symbol.for("node-ignore"));
    var KEY_IGNORE = TMP_KEY_IGNORE, define2 = (object, key, value) => Object.defineProperty(object, key, { value }), REGEX_REGEXP_RANGE = /([0-z])-([0-z])/g, RETURN_FALSE = () => !1, sanitizeRange = (range) => range.replace(
      REGEX_REGEXP_RANGE,
      (match, from, to) => from.charCodeAt(0) <= to.charCodeAt(0) ? match : EMPTY
    ), cleanRangeBackSlash = (slashes) => {
      let { length } = slashes;
      return slashes.slice(0, length - length % 2);
    }, REPLACERS = [
      [
        // remove BOM
        // TODO:
        // Other similar zero-width characters?
        /^\uFEFF/,
        () => EMPTY
      ],
      // > Trailing spaces are ignored unless they are quoted with backslash ("\")
      [
        // (a\ ) -> (a )
        // (a  ) -> (a)
        // (a ) -> (a)
        // (a \ ) -> (a  )
        /((?:\\\\)*?)(\\?\s+)$/,
        (_, m1, m2) => m1 + (m2.indexOf("\\") === 0 ? SPACE : EMPTY)
      ],
      // replace (\ ) with ' '
      // (\ ) -> ' '
      // (\\ ) -> '\\ '
      // (\\\ ) -> '\\ '
      [
        /(\\+?)\s/g,
        (_, m1) => {
          let { length } = m1;
          return m1.slice(0, length - length % 2) + SPACE;
        }
      ],
      // Escape metacharacters
      // which is written down by users but means special for regular expressions.
      // > There are 12 characters with special meanings:
      // > - the backslash \,
      // > - the caret ^,
      // > - the dollar sign $,
      // > - the period or dot .,
      // > - the vertical bar or pipe symbol |,
      // > - the question mark ?,
      // > - the asterisk or star *,
      // > - the plus sign +,
      // > - the opening parenthesis (,
      // > - the closing parenthesis ),
      // > - and the opening square bracket [,
      // > - the opening curly brace {,
      // > These special characters are often called "metacharacters".
      [
        /[\\$.|*+(){^]/g,
        (match) => `\\${match}`
      ],
      [
        // > a question mark (?) matches a single character
        /(?!\\)\?/g,
        () => "[^/]"
      ],
      // leading slash
      [
        // > A leading slash matches the beginning of the pathname.
        // > For example, "/*.c" matches "cat-file.c" but not "mozilla-sha1/sha1.c".
        // A leading slash matches the beginning of the pathname
        /^\//,
        () => "^"
      ],
      // replace special metacharacter slash after the leading slash
      [
        /\//g,
        () => "\\/"
      ],
      [
        // > A leading "**" followed by a slash means match in all directories.
        // > For example, "**/foo" matches file or directory "foo" anywhere,
        // > the same as pattern "foo".
        // > "**/foo/bar" matches file or directory "bar" anywhere that is directly
        // >   under directory "foo".
        // Notice that the '*'s have been replaced as '\\*'
        /^\^*\\\*\\\*\\\//,
        // '**/foo' <-> 'foo'
        () => "^(?:.*\\/)?"
      ],
      // starting
      [
        // there will be no leading '/'
        //   (which has been replaced by section "leading slash")
        // If starts with '**', adding a '^' to the regular expression also works
        /^(?=[^^])/,
        function() {
          return /\/(?!$)/.test(this) ? "^" : "(?:^|\\/)";
        }
      ],
      // two globstars
      [
        // Use lookahead assertions so that we could match more than one `'/**'`
        /\\\/\\\*\\\*(?=\\\/|$)/g,
        // Zero, one or several directories
        // should not use '*', or it will be replaced by the next replacer
        // Check if it is not the last `'/**'`
        (_, index, str) => index + 6 < str.length ? "(?:\\/[^\\/]+)*" : "\\/.+"
      ],
      // normal intermediate wildcards
      [
        // Never replace escaped '*'
        // ignore rule '\*' will match the path '*'
        // 'abc.*/' -> go
        // 'abc.*'  -> skip this rule,
        //    coz trailing single wildcard will be handed by [trailing wildcard]
        /(^|[^\\]+)(\\\*)+(?=.+)/g,
        // '*.js' matches '.js'
        // '*.js' doesn't match 'abc'
        (_, p1, p2) => {
          let unescaped = p2.replace(/\\\*/g, "[^\\/]*");
          return p1 + unescaped;
        }
      ],
      [
        // unescape, revert step 3 except for back slash
        // For example, if a user escape a '\\*',
        // after step 3, the result will be '\\\\\\*'
        /\\\\\\(?=[$.|*+(){^])/g,
        () => ESCAPE
      ],
      [
        // '\\\\' -> '\\'
        /\\\\/g,
        () => ESCAPE
      ],
      [
        // > The range notation, e.g. [a-zA-Z],
        // > can be used to match one of the characters in a range.
        // `\` is escaped by step 3
        /(\\)?\[([^\]/]*?)(\\*)($|\])/g,
        (match, leadEscape, range, endEscape, close) => leadEscape === ESCAPE ? `\\[${range}${cleanRangeBackSlash(endEscape)}${close}` : close === "]" && endEscape.length % 2 === 0 ? `[${sanitizeRange(range)}${endEscape}]` : "[]"
      ],
      // ending
      [
        // 'js' will not match 'js.'
        // 'ab' will not match 'abc'
        /(?:[^*])$/,
        // WTF!
        // https://git-scm.com/docs/gitignore
        // changes in [2.22.1](https://git-scm.com/docs/gitignore/2.22.1)
        // which re-fixes #24, #38
        // > If there is a separator at the end of the pattern then the pattern
        // > will only match directories, otherwise the pattern can match both
        // > files and directories.
        // 'js*' will not match 'a.js'
        // 'js/' will not match 'a.js'
        // 'js' will match 'a.js' and 'a.js/'
        (match) => /\/$/.test(match) ? `${match}$` : `${match}(?=$|\\/$)`
      ],
      // trailing wildcard
      [
        /(\^|\\\/)?\\\*$/,
        (_, p1) => `${p1 ? `${p1}[^/]+` : "[^/]*"}(?=$|\\/$)`
      ]
    ], regexCache = /* @__PURE__ */ Object.create(null), makeRegex = (pattern, ignoreCase) => {
      let source = regexCache[pattern];
      return source || (source = REPLACERS.reduce(
        (prev, [matcher, replacer]) => prev.replace(matcher, replacer.bind(pattern)),
        pattern
      ), regexCache[pattern] = source), ignoreCase ? new RegExp(source, "i") : new RegExp(source);
    }, isString = (subject) => typeof subject == "string", checkPattern = (pattern) => pattern && isString(pattern) && !REGEX_TEST_BLANK_LINE.test(pattern) && !REGEX_INVALID_TRAILING_BACKSLASH.test(pattern) && pattern.indexOf("#") !== 0, splitPattern = (pattern) => pattern.split(REGEX_SPLITALL_CRLF), IgnoreRule = class {
      constructor(origin, pattern, negative, regex) {
        this.origin = origin, this.pattern = pattern, this.negative = negative, this.regex = regex;
      }
    }, createRule = (pattern, ignoreCase) => {
      let origin = pattern, negative = !1;
      pattern.indexOf("!") === 0 && (negative = !0, pattern = pattern.substr(1)), pattern = pattern.replace(REGEX_REPLACE_LEADING_EXCAPED_EXCLAMATION, "!").replace(REGEX_REPLACE_LEADING_EXCAPED_HASH, "#");
      let regex = makeRegex(pattern, ignoreCase);
      return new IgnoreRule(
        origin,
        pattern,
        negative,
        regex
      );
    }, throwError = (message, Ctor) => {
      throw new Ctor(message);
    }, checkPath = (path3, originalPath, doThrow) => isString(path3) ? path3 ? checkPath.isNotRelative(path3) ? doThrow(
      `path should be a \`path.relative()\`d string, but got "${originalPath}"`,
      RangeError
    ) : !0 : doThrow("path must not be empty", TypeError) : doThrow(
      `path must be a string, but got \`${originalPath}\``,
      TypeError
    ), isNotRelative = (path3) => REGEX_TEST_INVALID_PATH.test(path3);
    checkPath.isNotRelative = isNotRelative;
    checkPath.convert = (p) => p;
    var Ignore = class {
      constructor({
        ignorecase = !0,
        ignoreCase = ignorecase,
        allowRelativePaths = !1
      } = {}) {
        define2(this, KEY_IGNORE, !0), this._rules = [], this._ignoreCase = ignoreCase, this._allowRelativePaths = allowRelativePaths, this._initCache();
      }
      _initCache() {
        this._ignoreCache = /* @__PURE__ */ Object.create(null), this._testCache = /* @__PURE__ */ Object.create(null);
      }
      _addPattern(pattern) {
        if (pattern && pattern[KEY_IGNORE]) {
          this._rules = this._rules.concat(pattern._rules), this._added = !0;
          return;
        }
        if (checkPattern(pattern)) {
          let rule = createRule(pattern, this._ignoreCase);
          this._added = !0, this._rules.push(rule);
        }
      }
      // @param {Array<string> | string | Ignore} pattern
      add(pattern) {
        return this._added = !1, makeArray(
          isString(pattern) ? splitPattern(pattern) : pattern
        ).forEach(this._addPattern, this), this._added && this._initCache(), this;
      }
      // legacy
      addPattern(pattern) {
        return this.add(pattern);
      }
      //          |           ignored : unignored
      // negative |   0:0   |   0:1   |   1:0   |   1:1
      // -------- | ------- | ------- | ------- | --------
      //     0    |  TEST   |  TEST   |  SKIP   |    X
      //     1    |  TESTIF |  SKIP   |  TEST   |    X
      // - SKIP: always skip
      // - TEST: always test
      // - TESTIF: only test if checkUnignored
      // - X: that never happen
      // @param {boolean} whether should check if the path is unignored,
      //   setting `checkUnignored` to `false` could reduce additional
      //   path matching.
      // @returns {TestResult} true if a file is ignored
      _testOne(path3, checkUnignored) {
        let ignored = !1, unignored = !1;
        return this._rules.forEach((rule) => {
          let { negative } = rule;
          if (unignored === negative && ignored !== unignored || negative && !ignored && !unignored && !checkUnignored)
            return;
          rule.regex.test(path3) && (ignored = !negative, unignored = negative);
        }), {
          ignored,
          unignored
        };
      }
      // @returns {TestResult}
      _test(originalPath, cache2, checkUnignored, slices) {
        let path3 = originalPath && checkPath.convert(originalPath);
        return checkPath(
          path3,
          originalPath,
          this._allowRelativePaths ? RETURN_FALSE : throwError
        ), this._t(path3, cache2, checkUnignored, slices);
      }
      _t(path3, cache2, checkUnignored, slices) {
        if (path3 in cache2)
          return cache2[path3];
        if (slices || (slices = path3.split(SLASH)), slices.pop(), !slices.length)
          return cache2[path3] = this._testOne(path3, checkUnignored);
        let parent = this._t(
          slices.join(SLASH) + SLASH,
          cache2,
          checkUnignored,
          slices
        );
        return cache2[path3] = parent.ignored ? parent : this._testOne(path3, checkUnignored);
      }
      ignores(path3) {
        return this._test(path3, this._ignoreCache, !1).ignored;
      }
      createFilter() {
        return (path3) => !this.ignores(path3);
      }
      filter(paths) {
        return makeArray(paths).filter(this.createFilter());
      }
      // @returns {TestResult}
      test(path3) {
        return this._test(path3, this._testCache, !0);
      }
    }, factory = (options) => new Ignore(options), isPathValid = (path3) => checkPath(path3 && checkPath.convert(path3), path3, RETURN_FALSE);
    factory.isPathValid = isPathValid;
    factory.default = factory;
    module.exports = factory;
    if (
      // Detect `process` so that it can run in browsers.
      typeof process < "u" && (process.env && process.env.IGNORE_TEST_WIN32 || process.platform === "win32")
    ) {
      let makePosix = (str) => /^\\\\\?\\/.test(str) || /["<>|\u0000-\u001F]+/u.test(str) ? str : str.replace(/\\/g, "/");
      checkPath.convert = makePosix;
      let REGIX_IS_WINDOWS_PATH_ABSOLUTE = /^[a-z]:\//i;
      checkPath.isNotRelative = (path3) => REGIX_IS_WINDOWS_PATH_ABSOLUTE.test(path3) || isNotRelative(path3);
    }
  }
});

// ../../node_modules/.pnpm/dotenv@16.4.7/node_modules/dotenv/package.json
var require_package2 = __commonJS({
  "../../node_modules/.pnpm/dotenv@16.4.7/node_modules/dotenv/package.json"(exports, module) {
    module.exports = {
      name: "dotenv",
      version: "16.4.7",
      description: "Loads environment variables from .env file",
      main: "lib/main.js",
      types: "lib/main.d.ts",
      exports: {
        ".": {
          types: "./lib/main.d.ts",
          require: "./lib/main.js",
          default: "./lib/main.js"
        },
        "./config": "./config.js",
        "./config.js": "./config.js",
        "./lib/env-options": "./lib/env-options.js",
        "./lib/env-options.js": "./lib/env-options.js",
        "./lib/cli-options": "./lib/cli-options.js",
        "./lib/cli-options.js": "./lib/cli-options.js",
        "./package.json": "./package.json"
      },
      scripts: {
        "dts-check": "tsc --project tests/types/tsconfig.json",
        lint: "standard",
        pretest: "npm run lint && npm run dts-check",
        test: "tap run --allow-empty-coverage --disable-coverage --timeout=60000",
        "test:coverage": "tap run --show-full-coverage --timeout=60000 --coverage-report=lcov",
        prerelease: "npm test",
        release: "standard-version"
      },
      repository: {
        type: "git",
        url: "git://github.com/motdotla/dotenv.git"
      },
      funding: "https://dotenvx.com",
      keywords: [
        "dotenv",
        "env",
        ".env",
        "environment",
        "variables",
        "config",
        "settings"
      ],
      readmeFilename: "README.md",
      license: "BSD-2-Clause",
      devDependencies: {
        "@types/node": "^18.11.3",
        decache: "^4.6.2",
        sinon: "^14.0.1",
        standard: "^17.0.0",
        "standard-version": "^9.5.0",
        tap: "^19.2.0",
        typescript: "^4.8.4"
      },
      engines: {
        node: ">=12"
      },
      browser: {
        fs: !1
      }
    };
  }
});

// ../../node_modules/.pnpm/dotenv@16.4.7/node_modules/dotenv/lib/main.js
var require_main2 = __commonJS({
  "../../node_modules/.pnpm/dotenv@16.4.7/node_modules/dotenv/lib/main.js"(exports, module) {
    init_cjs_shims();
    var fs = __require("fs"), path3 = __require("path"), os2 = __require("os"), crypto = __require("crypto"), packageJson = require_package2(), version = packageJson.version, LINE = /(?:^|^)\s*(?:export\s+)?([\w.-]+)(?:\s*=\s*?|:\s+?)(\s*'(?:\\'|[^'])*'|\s*"(?:\\"|[^"])*"|\s*`(?:\\`|[^`])*`|[^#\r\n]+)?\s*(?:#.*)?(?:$|$)/mg;
    function parse2(src) {
      let obj = {}, lines = src.toString();
      lines = lines.replace(/\r\n?/mg, `
`);
      let match;
      for (; (match = LINE.exec(lines)) != null; ) {
        let key = match[1], value = match[2] || "";
        value = value.trim();
        let maybeQuote = value[0];
        value = value.replace(/^(['"`])([\s\S]*)\1$/mg, "$2"), maybeQuote === '"' && (value = value.replace(/\\n/g, `
`), value = value.replace(/\\r/g, "\r")), obj[key] = value;
      }
      return obj;
    }
    function _parseVault(options) {
      let vaultPath = _vaultPath(options), result = DotenvModule.configDotenv({ path: vaultPath });
      if (!result.parsed) {
        let err = new Error(`MISSING_DATA: Cannot parse ${vaultPath} for an unknown reason`);
        throw err.code = "MISSING_DATA", err;
      }
      let keys = _dotenvKey(options).split(","), length = keys.length, decrypted;
      for (let i = 0; i < length; i++)
        try {
          let key = keys[i].trim(), attrs = _instructions(result, key);
          decrypted = DotenvModule.decrypt(attrs.ciphertext, attrs.key);
          break;
        } catch (error) {
          if (i + 1 >= length)
            throw error;
        }
      return DotenvModule.parse(decrypted);
    }
    function _log(message) {
      console.log(`[dotenv@${version}][INFO] ${message}`);
    }
    function _warn(message) {
      console.log(`[dotenv@${version}][WARN] ${message}`);
    }
    function _debug(message) {
      console.log(`[dotenv@${version}][DEBUG] ${message}`);
    }
    function _dotenvKey(options) {
      return options && options.DOTENV_KEY && options.DOTENV_KEY.length > 0 ? options.DOTENV_KEY : process.env.DOTENV_KEY && process.env.DOTENV_KEY.length > 0 ? process.env.DOTENV_KEY : "";
    }
    function _instructions(result, dotenvKey) {
      let uri;
      try {
        uri = new URL(dotenvKey);
      } catch (error) {
        if (error.code === "ERR_INVALID_URL") {
          let err = new Error("INVALID_DOTENV_KEY: Wrong format. Must be in valid uri format like dotenv://:key_1234@dotenvx.com/vault/.env.vault?environment=development");
          throw err.code = "INVALID_DOTENV_KEY", err;
        }
        throw error;
      }
      let key = uri.password;
      if (!key) {
        let err = new Error("INVALID_DOTENV_KEY: Missing key part");
        throw err.code = "INVALID_DOTENV_KEY", err;
      }
      let environment = uri.searchParams.get("environment");
      if (!environment) {
        let err = new Error("INVALID_DOTENV_KEY: Missing environment part");
        throw err.code = "INVALID_DOTENV_KEY", err;
      }
      let environmentKey = `DOTENV_VAULT_${environment.toUpperCase()}`, ciphertext = result.parsed[environmentKey];
      if (!ciphertext) {
        let err = new Error(`NOT_FOUND_DOTENV_ENVIRONMENT: Cannot locate environment ${environmentKey} in your .env.vault file.`);
        throw err.code = "NOT_FOUND_DOTENV_ENVIRONMENT", err;
      }
      return { ciphertext, key };
    }
    function _vaultPath(options) {
      let possibleVaultPath = null;
      if (options && options.path && options.path.length > 0)
        if (Array.isArray(options.path))
          for (let filepath of options.path)
            fs.existsSync(filepath) && (possibleVaultPath = filepath.endsWith(".vault") ? filepath : `${filepath}.vault`);
        else
          possibleVaultPath = options.path.endsWith(".vault") ? options.path : `${options.path}.vault`;
      else
        possibleVaultPath = path3.resolve(process.cwd(), ".env.vault");
      return fs.existsSync(possibleVaultPath) ? possibleVaultPath : null;
    }
    function _resolveHome(envPath) {
      return envPath[0] === "~" ? path3.join(os2.homedir(), envPath.slice(1)) : envPath;
    }
    function _configVault(options) {
      _log("Loading env from encrypted .env.vault");
      let parsed = DotenvModule._parseVault(options), processEnv = process.env;
      return options && options.processEnv != null && (processEnv = options.processEnv), DotenvModule.populate(processEnv, parsed, options), { parsed };
    }
    function configDotenv(options) {
      let dotenvPath = path3.resolve(process.cwd(), ".env"), encoding = "utf8", debug2 = !!(options && options.debug);
      options && options.encoding ? encoding = options.encoding : debug2 && _debug("No encoding is specified. UTF-8 is used by default");
      let optionPaths = [dotenvPath];
      if (options && options.path)
        if (!Array.isArray(options.path))
          optionPaths = [_resolveHome(options.path)];
        else {
          optionPaths = [];
          for (let filepath of options.path)
            optionPaths.push(_resolveHome(filepath));
        }
      let lastError, parsedAll = {};
      for (let path4 of optionPaths)
        try {
          let parsed = DotenvModule.parse(fs.readFileSync(path4, { encoding }));
          DotenvModule.populate(parsedAll, parsed, options);
        } catch (e) {
          debug2 && _debug(`Failed to load ${path4} ${e.message}`), lastError = e;
        }
      let processEnv = process.env;
      return options && options.processEnv != null && (processEnv = options.processEnv), DotenvModule.populate(processEnv, parsedAll, options), lastError ? { parsed: parsedAll, error: lastError } : { parsed: parsedAll };
    }
    function config(options) {
      if (_dotenvKey(options).length === 0)
        return DotenvModule.configDotenv(options);
      let vaultPath = _vaultPath(options);
      return vaultPath ? DotenvModule._configVault(options) : (_warn(`You set DOTENV_KEY but you are missing a .env.vault file at ${vaultPath}. Did you forget to build it?`), DotenvModule.configDotenv(options));
    }
    function decrypt(encrypted, keyStr) {
      let key = Buffer.from(keyStr.slice(-64), "hex"), ciphertext = Buffer.from(encrypted, "base64"), nonce = ciphertext.subarray(0, 12), authTag = ciphertext.subarray(-16);
      ciphertext = ciphertext.subarray(12, -16);
      try {
        let aesgcm = crypto.createDecipheriv("aes-256-gcm", key, nonce);
        return aesgcm.setAuthTag(authTag), `${aesgcm.update(ciphertext)}${aesgcm.final()}`;
      } catch (error) {
        let isRange = error instanceof RangeError, invalidKeyLength = error.message === "Invalid key length", decryptionFailed = error.message === "Unsupported state or unable to authenticate data";
        if (isRange || invalidKeyLength) {
          let err = new Error("INVALID_DOTENV_KEY: It must be 64 characters long (or more)");
          throw err.code = "INVALID_DOTENV_KEY", err;
        } else if (decryptionFailed) {
          let err = new Error("DECRYPTION_FAILED: Please check your DOTENV_KEY");
          throw err.code = "DECRYPTION_FAILED", err;
        } else
          throw error;
      }
    }
    function populate(processEnv, parsed, options = {}) {
      let debug2 = !!(options && options.debug), override = !!(options && options.override);
      if (typeof parsed != "object") {
        let err = new Error("OBJECT_REQUIRED: Please check the processEnv argument being passed to populate");
        throw err.code = "OBJECT_REQUIRED", err;
      }
      for (let key of Object.keys(parsed))
        Object.prototype.hasOwnProperty.call(processEnv, key) ? (override === !0 && (processEnv[key] = parsed[key]), debug2 && _debug(override === !0 ? `"${key}" is already defined and WAS overwritten` : `"${key}" is already defined and was NOT overwritten`)) : processEnv[key] = parsed[key];
    }
    var DotenvModule = {
      configDotenv,
      _configVault,
      _parseVault,
      config,
      decrypt,
      parse: parse2,
      populate
    };
    module.exports.configDotenv = DotenvModule.configDotenv;
    module.exports._configVault = DotenvModule._configVault;
    module.exports._parseVault = DotenvModule._parseVault;
    module.exports.config = DotenvModule.config;
    module.exports.decrypt = DotenvModule.decrypt;
    module.exports.parse = DotenvModule.parse;
    module.exports.populate = DotenvModule.populate;
    module.exports = DotenvModule;
  }
});

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/import-utils.js
init_cjs_shims();
import { createRequire } from "node:module";
import { pathToFileURL } from "node:url";
var require2 = createRequire(import.meta.url);
async function importVite(root) {
  let vitePath = require2.resolve(
    "vite",
    process.env.SHOPIFY_UNIT_TEST ? void 0 : { paths: [root] }
  ), vitePackageJson = await findUpAndReadPackageJson(vitePath), viteNodeIndexFile = vitePackageJson.content.exports?.["."];
  typeof viteNodeIndexFile != "string" && (viteNodeIndexFile = viteNodeIndexFile?.import), typeof viteNodeIndexFile != "string" && (viteNodeIndexFile = viteNodeIndexFile.default);
  let viteNodePath = joinPath(
    dirname(vitePackageJson.path),
    viteNodeIndexFile
  );
  return import(pathToFileURL(viteNodePath).href);
}
function importLocal(packageName, path3) {
  let realPath = require2.resolve(
    packageName,
    process.env.SHOPIFY_UNIT_TEST ? void 0 : { paths: [path3] }
  );
  return import(pathToFileURL(realPath).href);
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/request-events.js
init_cjs_shims();
import path from "node:path";
import { EventEmitter } from "node:events";
import { ReadableStream } from "node:stream/web";

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphiql-url.js
init_cjs_shims();
function getGraphiQLUrl({
  host = "",
  graphql
}) {
  let url = `${host.endsWith("/") ? host.slice(0, -1) : host}/graphiql`;
  if (graphql) {
    let { query, variables } = graphql;
    typeof variables != "string" && (variables = JSON.stringify(variables)), url += `?query=${encodeURIComponent(query)}${variables ? `&variables=${encodeURIComponent(variables)}` : ""}`, graphql.schema && (url += `&schema=${graphql.schema}`);
  }
  return url;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/request-events.js
var import_source_map_support = __toESM(require_source_map_support(), 1), H2O_BINDING_NAME = "H2O_LOG_EVENT", ResponseConstructor;
function setConstructors(constructors) {
  ResponseConstructor = constructors.Response;
}
var DEV_ROUTES = /* @__PURE__ */ new Set([
  "/graphiql",
  "/graphiql/customer-account.schema.json",
  "/subrequest-profiler",
  "/debug-network-server"
]), EVENT_MAP = {
  request: "Request",
  subrequest: "Sub request"
};
async function getRequestInfo(request) {
  let data = await request.json();
  return {
    ...data,
    requestId: data.requestId ?? "",
    eventType: data.eventType || "unknown",
    endTime: data.endTime || Date.now(),
    purpose: data.purpose === "prefetch" ? "(prefetch)" : "",
    cacheStatus: data.cacheStatus ?? "",
    graphql: data.graphql ? JSON.parse(data.graphql) : null
  };
}
var eventEmitter = new EventEmitter(), eventHistory = [];
function createResponse(main = "ok", init) {
  return new ResponseConstructor(main, init);
}
function clearHistory(request) {
  return eventHistory.length = 0, createResponse();
}
function createLogRequestEvent(options) {
  return async function(request) {
    let url = new URL(request.url);
    if (DEV_ROUTES.has(url.pathname))
      return createResponse();
    let {
      url: displayUrl,
      displayName: displayNameData,
      eventType,
      purpose,
      graphql,
      stackInfo,
      ...data
    } = await getRequestInfo(request), graphiqlLink = "", descriptionUrl = request.url, displayName = displayNameData;
    eventType === "subrequest" && (displayName = displayName || graphql?.query.match(/(query|mutation)\s+(\w+)/)?.[0]?.replace(/\s+/, " "), descriptionUrl = displayUrl || request.url, graphql && (graphiqlLink = getGraphiQLUrl({ graphql })));
    let stackLine = null, stackLink = null;
    if (stackInfo?.file) {
      options?.transformLocation && (stackInfo.file = options.transformLocation(stackInfo.file));
      let { source, line, column } = (0, import_source_map_support.mapSourcePosition)({
        source: stackInfo.file,
        line: stackInfo.line ?? 0,
        column: stackInfo.column ?? 0
      });
      stackLine = `${source}:${line}:${column + 1}`, stackLink = `vscode://${path.join("file", stackLine)}`, stackLine = stackLine.split(path.sep + "app" + path.sep)[1] ?? stackLine, stackInfo.func && (stackLine = `${stackInfo.func.replace(/\d+$/, "")} (${stackLine})`);
    }
    let event = {
      event: EVENT_MAP[eventType] || eventType,
      data: JSON.stringify({
        ...data,
        displayName,
        url: `${purpose} ${descriptionUrl}`.trim(),
        graphiqlLink,
        stackLine,
        stackLink
      })
    };
    return eventHistory.push(event), eventHistory.length > 100 && eventHistory.shift(), eventEmitter.emit("request", event), createResponse();
  };
}
function streamRequestEvents(request) {
  let stream = new ReadableStream({
    start(controller) {
      let encoder = new TextEncoder(), enqueueEvent = ({ event = "message", data }) => {
        controller.enqueue(encoder.encode(`event: ${event}
`)), controller.enqueue(encoder.encode(`data: ${data}

`));
      };
      eventHistory.forEach(enqueueEvent), eventEmitter.addListener("request", enqueueEvent);
      let closed = !1;
      function close() {
        closed || (closed = !0, request.signal.removeEventListener("abort", close), eventEmitter.removeListener("request", enqueueEvent), controller.close());
      }
      if (request.signal.addEventListener("abort", close), request.signal.aborted) return close();
    }
  });
  return createResponse(stream, {
    headers: {
      "Content-Type": "text/event-stream",
      "Cache-Control": "no-store",
      Connection: "keep-alive"
    }
  });
}
function handleDebugNetworkRequest(request) {
  return request.method === "DELETE" ? clearHistory() : streamRequestEvents(request);
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/mini-oxygen/common.js
init_cjs_shims();
var SUBREQUEST_PROFILER_ENDPOINT = "/debug-network-server";
function handleMiniOxygenImportFail() {
  throw new AbortError(
    "Could not load MiniOxygen.",
    "Please make sure you have `@shopify/mini-oxygen` installed."
  );
}
function logRequestLine({
  request,
  response,
  meta
}) {
  try {
    let url = new URL(request.url);
    if (!meta.durationMs && meta.startTimeMs && meta.endTimeMs && (meta.durationMs = meta.endTimeMs - meta.startTimeMs), DEV_ROUTES.has(url.pathname) || url.pathname === "/favicon.ico") return;
    let isDataRequest = url.searchParams.has("_data"), route = request.url.replace(url.origin, ""), info = "", type = "render";
    if (isDataRequest) {
      type = request.method === "GET" ? "loader" : "action";
      let dataParam = url.searchParams.get("_data")?.replace("routes/", "");
      route = url.pathname, info = `[${dataParam}]`;
    }
    let colorizeStatus = response.status < 300 ? outputToken.green : response.status < 400 ? outputToken.cyan : outputToken.errorText;
    outputInfo(
      outputContent`${request.method.padStart(6)}  ${colorizeStatus(
        String(response.status)
      )}  ${outputToken.italic(type.padEnd(7, " "))} ${route} ${meta.durationMs && meta.durationMs > 0 ? source_default.dim(` ${meta.durationMs}ms`) : ""}${info ? "  " + source_default.dim(info) : ""}${request.headers.purpose === "prefetch" ? source_default.italic(source_default.dim("  prefetch")) : ""}`
    );
  } catch {
    request && response?.status && outputInfo(`${request.method} ${response.status} ${request.url}`);
  }
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/dev-shared.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/commands/hydrogen/customer-account/push.js
init_cjs_shims();
var import_core3 = __toESM(require_lib2(), 1);

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/commands/hydrogen/link.js
init_cjs_shims();
var import_core2 = __toESM(require_lib2(), 1);

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/flags.js
init_cjs_shims();
var import_core = __toESM(require_lib2(), 1);

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/index.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/assets.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/build.js
init_cjs_shims();
import { fileURLToPath } from "node:url";
import { existsSync } from "node:fs";

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/process.js
init_cjs_shims();
import { exec } from "node:child_process";
import { promisify } from "node:util";
var execAsync = promisify(exec);
function supressNodeExperimentalWarnings() {
  let warningListener = process.listeners("warning")[0];
  warningListener && (process.removeAllListeners("warning"), process.prependListener("warning", (warning) => {
    warning.name != "ExperimentalWarning" && warningListener(warning);
  }));
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/build.js
var monorepoPackagesPath = new URL("../../..", import.meta.url).pathname, isHydrogenMonorepo = (() => {
  try {
    let skeletonPath = joinPath(
      dirname(monorepoPackagesPath),
      "templates",
      "skeleton"
    );
    return existsSync(skeletonPath);
  } catch {
    return !1;
  }
})(), hydrogenPackagesPath = isHydrogenMonorepo ? monorepoPackagesPath : void 0, ASSETS_DIR_PREFIX = "assets/hydrogen", ASSETS_STARTER_DIR = "starter", ASSETS_STARTER_DIR_ROUTES = "routes", pkgJsonPath;
async function getPkgJsonPath() {
  if (pkgJsonPath ??= await findPathUp("package.json", {
    cwd: fileURLToPath(import.meta.url),
    type: "file"
  }), !pkgJsonPath)
    throw new AbortError(
      "Could not find assets directory",
      "Please report this error."
    );
  return pkgJsonPath;
}
async function getAssetsDir(feature, ...subpaths) {
  return joinPath(
    dirname(await getPkgJsonPath()),
    process.env.SHOPIFY_UNIT_TEST ? "assets" : `dist/${ASSETS_DIR_PREFIX}`,
    feature ?? "",
    ...subpaths
  );
}
async function getTemplateAppFile(filepath, root) {
  root ??= await getStarterDir();
  let url = new URL(
    `${root}/app${filepath ? `/${filepath}` : ""}`,
    import.meta.url
  );
  return url.protocol === "file:" ? fileURLToPath(url) : url.toString();
}
function getStarterDir(useSource = !!process.env.SHOPIFY_UNIT_TEST) {
  return useSource ? getSkeletonSourceDir() : getAssetsDir(ASSETS_STARTER_DIR);
}
function getSkeletonSourceDir() {
  if (!isHydrogenMonorepo)
    throw new AbortError(
      "Trying to use skeleton source dir outside of Hydrogen monorepo.",
      "Please report this error."
    );
  return joinPath(dirname(monorepoPackagesPath), "templates", "skeleton");
}
function getSkeletonNodeModules() {
  return joinPath(getSkeletonSourceDir(), "node_modules");
}
async function getRepoNodeModules() {
  if (isHydrogenMonorepo)
    return getSkeletonNodeModules();
  let { stdout } = await execAsync("npm root");
  return stdout.trim();
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/assets.js
var SETUP_CSS_STRATEGIES = [
  "tailwind",
  "vanilla-extract",
  "css-modules",
  "postcss"
];
async function copyAssets(feature, assets, rootDirectory, replacer = (content, filename) => content) {
  let setupAssetsPath = await getAssetsDir(feature);
  return Promise.all(
    Object.entries(assets).map(async ([source, destination]) => {
      let content = await readFile(joinPath(setupAssetsPath, source));
      await writeFile(
        joinPath(rootDirectory, destination),
        replacer(content, source)
      );
    })
  );
}
async function canWriteFiles(assetMap, directory, force) {
  let fileExistPromises = Object.values(assetMap).map(
    (file) => fileExists(joinPath(directory, file)).then(
      (exists2) => exists2 ? file : null
    )
  ), existingFiles = (await Promise.all(fileExistPromises)).filter(
    Boolean
  );
  return !(existingFiles.length > 0 && !force && !await renderConfirmationPrompt({
    message: `Some files already exist (${existingFiles.join(
      ", "
    )}). Overwrite?`,
    defaultValue: !1
  }));
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/tailwind.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/file.js
init_cjs_shims();
import { readdir } from "node:fs/promises";

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/format-code.js
init_cjs_shims();
init_prettier();
import * as FS from "fs/promises";
import * as Path from "path";
var DEFAULT_PRETTIER_CONFIG = {
  arrowParens: "always",
  singleQuote: !0,
  bracketSpacing: !1,
  trailingComma: "all"
};
async function getCodeFormatOptions(filePath = process.cwd()) {
  let pathToUse = (await FS.lstat(filePath)).isFile() ? filePath : Path.resolve(filePath, "prettier.file");
  try {
    return await resolveConfig(pathToUse) || DEFAULT_PRETTIER_CONFIG;
  } catch {
    return DEFAULT_PRETTIER_CONFIG;
  }
}
async function formatCode(content, config = DEFAULT_PRETTIER_CONFIG, filePath = "") {
  let ext = extname(filePath);
  return format2(content, {
    // Specify the TypeScript parser for ts/tsx files. Otherwise
    // we need to use the babel parser instead of the default parser,
    // because prettier will print a warning.
    parser: ext === ".tsx" || ext === ".ts" ? "typescript" : "babel",
    ...config
  });
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/file.js
async function replaceFileContent(filepath, formatConfig, replacer) {
  let content = await replacer(await readFile(filepath));
  if (typeof content == "string")
    return formatConfig && (content = await formatCode(content, formatConfig, filepath)), writeFile(filepath, content);
}
var DEFAULT_EXTENSIONS = [
  "tsx",
  "ts",
  "jsx",
  "js",
  "mjs",
  "cjs"
];
async function findFileWithExtension(directory, fileBase, extensions = DEFAULT_EXTENSIONS) {
  let dirFiles = await readdir(directory);
  if (dirFiles.includes(fileBase)) {
    let filepath = resolvePath(directory, fileBase);
    if (!await isDirectory(filepath))
      return { filepath };
    for (let extension of ["ts", "js"]) {
      let filepath2 = resolvePath(directory, `${fileBase}/index.${extension}`);
      if (await fileExists(resolvePath(directory, filepath2)))
        return { filepath: filepath2, extension, astType: extension };
    }
  } else
    for (let extension of extensions) {
      let filename = `${fileBase}.${extension}`;
      if (dirFiles.includes(filename)) {
        let astType = extension === "mjs" || extension === "cjs" ? "js" : extension;
        return { filepath: resolvePath(directory, filename), extension, astType };
      }
    }
  return {};
}
var MANAGED_PACKAGE_JSON_KEYS = Object.freeze([
  "dependencies",
  "devDependencies",
  "peerDependencies"
]);
async function mergePackageJson(sourceDir, targetDir, options) {
  let targetPkgJson = await readAndParsePackageJson(
    joinPath(targetDir, "package.json")
  ), sourcePkgJson = await readAndParsePackageJson(
    joinPath(sourceDir, "package.json")
  ), ignoredKeys = /* @__PURE__ */ new Set(["comment", ...options?.ignoredKeys ?? []]), unmanagedKeys = Object.keys(sourcePkgJson).filter(
    (key) => !MANAGED_PACKAGE_JSON_KEYS.includes(key)
  );
  for (let key of unmanagedKeys) {
    if (ignoredKeys.has(key)) continue;
    let sourceValue = sourcePkgJson[key], targetValue = targetPkgJson[key], newValue = Array.isArray(sourceValue) && Array.isArray(targetValue) ? [...targetValue, ...sourceValue] : typeof sourceValue == "object" && typeof targetValue == "object" ? { ...targetValue, ...sourceValue } : sourceValue;
    targetPkgJson[key] = newValue;
  }
  let remixVersion = Object.entries(targetPkgJson.dependencies || {}).find(
    ([dep]) => dep.startsWith("@remix-run/")
  )?.[1];
  for (let key of MANAGED_PACKAGE_JSON_KEYS)
    ignoredKeys.has(key) || sourcePkgJson[key] && (targetPkgJson[key] = [
      .../* @__PURE__ */ new Set([
        ...Object.keys(targetPkgJson[key] ?? {}),
        ...Object.keys(sourcePkgJson[key] ?? {})
      ])
    ].sort().reduce(
      (acc, dep) => {
        let version = sourcePkgJson[key]?.[dep] ?? targetPkgJson[key]?.[dep];
        return dep.startsWith("@remix-run/") && remixVersion && (version = remixVersion), acc[dep] = version, acc;
      },
      {}
    ));
  await writePackageJSON(
    targetDir,
    options?.onResult?.(targetPkgJson) ?? targetPkgJson
  );
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/replacers.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/ast.js
init_cjs_shims();
async function importLangAstGrep(lang) {
  let astGrep = await import("@ast-grep/napi");
  if (!(lang in astGrep))
    throw new Error(`Wrong language for AST: ${lang}`);
  return astGrep[lang];
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/replacers.js
async function replaceRootLinks(appDirectory, formatConfig, importer) {
  let { filepath, astType } = await findFileWithExtension(appDirectory, "root");
  if (!filepath || !astType)
    throw new AbortError(`Could not find root file in ${appDirectory}`);
  await replaceFileContent(filepath, formatConfig, async (content) => {
    let importStatement = `import ${importer.isDefault ? importer.name : `{${importer.name}}`} from '${(importer.isAbsolute ? "" : "./") + importer.path}';`;
    if (content.includes(importStatement.split("from")[0]))
      return;
    let root = (await importLangAstGrep(astType)).parse(content).root(), importNodes = root.findAll({ rule: { kind: "import_statement" } }).reverse(), lastImportNode = importNodes.find((node) => node.text().includes(".css")) || importNodes.shift(), layoutStyleNode = root.find({
      rule: {
        kind: "jsx_element",
        regex: "resetStyles",
        has: {
          kind: "jsx_opening_element",
          has: {
            kind: "identifier",
            pattern: "link"
          }
        }
      }
    });
    if (!lastImportNode || !layoutStyleNode)
      throw new AbortError(
        'Could not find a "links" export in root file. Please add one and try again.'
      );
    let lastImportContent = lastImportNode.text(), layoutStyleNodeContent = layoutStyleNode.text(), newLinkNode = importer.isConditional ? `{${importer.name} && <link rel="stylesheet" href={${importer.name}}></link>}` : `<link rel="stylesheet" href={${importer.name}}></link>`;
    return content.replace(lastImportContent, lastImportContent + `
` + importStatement).replace(
      layoutStyleNodeContent,
      newLinkNode + `
` + layoutStyleNodeContent
    );
  });
}
async function injectVitePlugin(rootDirectory, formatConfig, importer, pluginOptions) {
  let { filepath, astType } = await findFileWithExtension(
    rootDirectory,
    "vite.config"
  );
  if (!filepath || !astType)
    throw new AbortError(`Could not find vite.config file in ${rootDirectory}`);
  await replaceFileContent(filepath, formatConfig, async (content) => {
    let importStatement = `import ${importer.isDefault ? importer.name : `{${importer.name}}`} from '${importer.path}';`;
    if (new RegExp(`['"]${importer.path.replace("/", "\\/")}['"]`).test(content))
      return;
    let root = (await importLangAstGrep(astType)).parse(content).root(), lastImportNode = root.findAll({ rule: { kind: "import_statement" } }).pop(), vitePluginListNode = root.find(vitePluginListRule);
    if (!lastImportNode || !vitePluginListNode)
      throw new AbortError(
        'Could not find a "plugins" key in Vite config file. Please add one and try again.'
      );
    let lastImportContent = lastImportNode.text(), linksExportReturnContent = vitePluginListNode.text(), newVitePluginItem = `${importer.name}(${pluginOptions ? JSON.stringify(pluginOptions) : ""})`;
    return content.replace(lastImportContent, lastImportContent + `
` + importStatement).replace(
      linksExportReturnContent,
      linksExportReturnContent.replace("[", `[${newVitePluginItem},`)
    );
  });
}
var vitePluginListRule = {
  rule: {
    // An array
    pattern: "[$$$]",
    inside: {
      // directly in the value part of a `plugins` key
      kind: "pair",
      stopBy: "neighbor",
      has: {
        field: "key",
        regex: "^plugins$",
        stopBy: "neighbor"
      },
      inside: {
        // directly inside an object (the Vite config object)
        kind: "object",
        stopBy: "neighbor",
        // that is exported but is not inside another object
        // e.g. `export default {something:{plugins:[]}}`
        // doesn't match, but `export default {plugins:[]}` does.
        // And `export default defineConfig({plugins:[]})` matches too.
        all: [
          {
            inside: {
              kind: "export_statement",
              regex: "export default",
              stopBy: "end"
            }
          },
          {
            not: {
              inside: {
                kind: "object",
                stopBy: "end"
              }
            }
          }
        ]
      }
    }
  }
};

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/tailwind.js
var tailwindCssPath = "styles/tailwind.css";
async function setupTailwind({ rootDirectory, appDirectory }, force = !1) {
  let relativeAppDirectory = relativePath(rootDirectory, appDirectory), assetMap = {
    "tailwind.css": joinPath(relativeAppDirectory, tailwindCssPath)
  };
  if (!await canWriteFiles(assetMap, appDirectory, force)) {
    outputInfo(
      "Skipping CSS setup as some files already exist. You may use `--force` or `-f` to override it."
    );
    return;
  }
  return {
    workPromise: Promise.all([
      mergePackageJson(await getAssetsDir("tailwind"), rootDirectory),
      copyAssets("tailwind", assetMap, rootDirectory),
      getCodeFormatOptions(rootDirectory).then(
        (formatConfig) => Promise.all([
          replaceRootLinks(appDirectory, formatConfig, {
            name: "tailwindCss",
            path: `${tailwindCssPath}?url`,
            isDefault: !0
          }),
          injectVitePlugin(rootDirectory, formatConfig, {
            name: "tailwindcss",
            path: "@tailwindcss/vite",
            isDefault: !0
          })
        ])
      )
    ]),
    generatedAssets: Object.values(assetMap),
    needsInstallDeps: !0
  };
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/vanilla-extract.js
init_cjs_shims();
async function setupVanillaExtract({
  rootDirectory
}) {
  return {
    workPromise: Promise.all([
      mergePackageJson(await getAssetsDir("vanilla-extract"), rootDirectory),
      getCodeFormatOptions(rootDirectory).then(
        (formatConfig) => injectVitePlugin(rootDirectory, formatConfig, {
          path: "@vanilla-extract/vite-plugin",
          name: "vanillaExtractPlugin",
          isDefault: !1
        })
      )
    ]),
    generatedAssets: [],
    needsInstallDeps: !0
  };
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/css/index.js
var STYLING_CHOICES = [...SETUP_CSS_STRATEGIES, "none"], CSS_STRATEGY_NAME_MAP = {
  tailwind: "Tailwind v4",
  "vanilla-extract": "Vanilla Extract",
  "css-modules": "CSS Modules",
  postcss: "PostCSS"
}, CSS_STRATEGY_HELP_URL_MAP = {
  postcss: "https://vitejs.dev/guide/features.html#postcss",
  "css-modules": "https://vitejs.dev/guide/features.html#css-modules",
  "vanilla-extract": "https://vanilla-extract.style/documentation/styling/",
  tailwind: "https://tailwindcss.com/docs/configuration"
};
function setupCssStrategy(strategy, options, force) {
  switch (strategy) {
    case "tailwind":
      return setupTailwind(options, force);
    case "vanilla-extract":
      return setupVanillaExtract(options);
    case "postcss":
    case "css-modules":
      return {
        workPromise: Promise.resolve(),
        generatedAssets: [],
        needsInstallDeps: !1
      };
    default:
      throw new Error("Unknown strategy");
  }
}
async function renderCssPrompt(options) {
  let cssStrategies = Object.entries({
    ...CSS_STRATEGY_NAME_MAP,
    ...options?.extraChoices
  });
  return renderSelectPrompt({
    message: "Select a styling library",
    ...options,
    choices: cssStrategies.map(([value, label]) => ({
      value,
      label
    })),
    defaultValue: "tailwind"
  });
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/i18n/index.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/i18n/replacers.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/transpile/index.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/transpile/file.js
init_cjs_shims();
async function transpileFile(code, filepath, keepTypes = !0) {
  let { transpileTs } = await import("./morph-32Y5BUJM.js");
  return transpileTs(code, filepath, keepTypes);
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/transpile/project.js
init_cjs_shims();
var DEFAULT_JS_CONFIG = {
  checkJs: !1,
  target: "ES2022",
  module: "ES2022",
  moduleResolution: "bundler",
  baseUrl: ".",
  paths: {
    "~/*": ["app/*"]
  }
}, JS_CONFIG_KEYS = /* @__PURE__ */ new Set([
  "noLib",
  "target",
  "module",
  "moduleResolution",
  "checkJs",
  "experimentalDecorators",
  "allowSyntheticDefaultImports",
  "baseUrl",
  "paths",
  ...Object.keys(DEFAULT_JS_CONFIG)
]);
function convertConfigToJS(tsConfig, keepTypes = !1) {
  let jsConfig = {
    compilerOptions: { ...DEFAULT_JS_CONFIG }
  };
  if (tsConfig.include && (jsConfig.include = tsConfig.include.filter((s) => keepTypes || !s.endsWith(".d.ts")).map((s) => s.replace(/(?<!\.d)\.ts(x?)$/, ".js$1"))), tsConfig.compilerOptions)
    for (let key of JS_CONFIG_KEYS)
      tsConfig.compilerOptions[key] !== void 0 && (jsConfig.compilerOptions[key] = tsConfig.compilerOptions[key]);
  return jsConfig;
}
async function transpileProject(projectDir, keepTypes = !0) {
  let routesPath = joinPath(projectDir, "app/routes.ts");
  if (await fileExists(routesPath)) {
    let replacedRoutesFileContent = (await readFile(routesPath)).replace(
      "./layout.tsx",
      "./layout.jsx"
    );
    await writeFile(routesPath, replacedRoutesFileContent);
  }
  let entries = await glob("**/*.+(ts|tsx)", {
    absolute: !0,
    cwd: projectDir,
    dot: !0,
    ignore: ["**/node_modules/**"]
  }), formatConfig = await getCodeFormatOptions();
  for (let entry of entries) {
    if (entry.endsWith(".d.ts")) {
      keepTypes || await removeFile(entry);
      continue;
    }
    let tsx = await readFile(entry), mjs = await formatCode(
      await transpileFile(tsx, entry, keepTypes),
      formatConfig
    );
    await removeFile(entry), await writeFile(entry.replace(/\.ts(x?)$/, ".js$1"), mjs);
  }
  try {
    let remixConfigPath = joinPath(projectDir, "remix.config.js"), remixConfig = await readFile(remixConfigPath);
    remixConfig = remixConfig.replace(/\/server\.ts/gim, "/server.js"), await writeFile(remixConfigPath, remixConfig);
  } catch (error) {
    outputDebug(
      `Could not change TS extensions in remix.config.js:
` + error.stack
    );
  }
  try {
    let tsConfigPath = joinPath(projectDir, "tsconfig.json"), tsConfigWithComments = await readFile(tsConfigPath), jsConfig = convertConfigToJS(
      JSON.parse(tsConfigWithComments.replace(/^\s*\/\/.*$/gm, "")),
      keepTypes
    );
    await removeFile(tsConfigPath), await writeFile(
      joinPath(projectDir, "jsconfig.json"),
      JSON.stringify(jsConfig, null, 2)
    );
  } catch (error) {
    outputDebug(
      `Could not transpile tsconfig.json:
` + error.stack
    );
  }
  try {
    let pkgJson = JSON.parse(
      await readFile(joinPath(projectDir, "package.json"))
    );
    delete pkgJson.scripts.typecheck;
    let typegenPattern = /react-router typegen\s*&&\s*/g;
    for (let [key, value] of Object.entries(pkgJson.scripts || {}))
      typeof value == "string" && value.includes("react-router typegen") && (pkgJson.scripts[key] = value.replace(typegenPattern, ""));
    if (!keepTypes) {
      delete pkgJson.devDependencies.typescript, delete pkgJson.devDependencies["@shopify/oxygen-workers-types"];
      for (let key of Object.keys(pkgJson.devDependencies))
        key.startsWith("@types/") && delete pkgJson.devDependencies[key];
      let codegenFlag = /\s*--codegen/;
      pkgJson.scripts?.dev && (pkgJson.scripts.dev = pkgJson.scripts.dev.replace(codegenFlag, "")), pkgJson.scripts?.build && (pkgJson.scripts.build = pkgJson.scripts.build.replace(codegenFlag, ""));
    }
    await writeFile(
      joinPath(projectDir, "package.json"),
      JSON.stringify(pkgJson, null, 2)
    );
  } catch (error) {
    outputDebug(
      `Could not remove TS dependencies from package.json:
` + error.stack
    );
  }
  try {
    let { filepath = joinPath(projectDir, ".eslintrc.cjs") } = await findFileWithExtension(projectDir, ".eslintrc", ["cjs", "js"]), eslintrc = await readFile(filepath);
    keepTypes || (eslintrc = eslintrc.replace(/\/\*\*[\s*]+@type.+\s+\*\/\s?/gim, "")), eslintrc = eslintrc.replace(/\s*,?\s*['"`]plugin:hydrogen\/typescript['"`]/gim, "").replace(/\s+['"`]@typescript-eslint\/.+,/gim, ""), await writeFile(filepath, await formatCode(eslintrc, formatConfig));
  } catch (error) {
    outputDebug(
      `Could not remove TS rules from .eslintrc:
` + error.stack
    );
  }
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/i18n/replacers.js
async function replaceContextI18n({
  rootDirectory,
  contextCreate = joinPath("app", "lib", "context.ts")
}, formatConfig, i18nStrategyFilePath) {
  let createContextMethodName = "createAppLoadContext", { filepath, astType } = await findContextCreateFile({
    rootDirectory,
    contextCreate
  }), localeExtractImpl = await readFile(i18nStrategyFilePath), i18nFileFinalPath = await replaceI18nStrategy(
    { rootDirectory, contextCreate },
    formatConfig,
    i18nStrategyFilePath
  );
  await replaceFileContent(filepath, formatConfig, async (content) => {
    let root = (await importLangAstGrep(astType)).parse(content).root(), requestIdentifierName = root.find({
      rule: {
        kind: "identifier",
        inside: {
          kind: "formal_parameters",
          stopBy: "end",
          inside: {
            kind: "function_declaration",
            stopBy: "end",
            has: {
              kind: "identifier",
              regex: `^${createContextMethodName}$`
            }
          }
        }
      }
    })?.text() ?? "request", i18nFunctionName = localeExtractImpl.match(
      /^(export )?function (\w+)/m
    )?.[2];
    if (!i18nFunctionName)
      throw new Error("Could not find the i18n function name");
    let i18nFunctionCall = `${i18nFunctionName}(${requestIdentifierName})`, hydrogenImportPath = "@shopify/hydrogen", hydrogenImportName = "createHydrogenContext", importSpecifier = root.find({
      rule: {
        kind: "import_specifier",
        inside: {
          kind: "import_statement",
          stopBy: "end",
          has: {
            kind: "string_fragment",
            stopBy: "end",
            regex: `^${hydrogenImportPath}$`
          }
        },
        has: {
          kind: "identifier",
          regex: `^${hydrogenImportName}`
          // could be appended with " as ..."
        }
      }
    }), [importName, importAlias] = importSpecifier?.text().split(/\s+as\s+/) || [];
    if (importName = importAlias ?? importName, !importName)
      throw new AbortError(
        `Could not find a Hydrogen import in ${contextCreate}`,
        `Please import "${hydrogenImportName}" from "${hydrogenImportPath}"`
      );
    let argumentObject = root.find({
      rule: {
        kind: "object",
        inside: {
          kind: "arguments",
          inside: {
            kind: "call_expression",
            has: {
              kind: "identifier",
              regex: importName
            }
          }
        }
      }
    });
    if (!argumentObject)
      throw new AbortError(
        `Could not find a Hydrogen client instantiation with an inline object as argument in ${contextCreate}`,
        `Please add a call to ${importName}({...})`
      );
    let i18nValue = argumentObject.find({
      rule: {
        kind: "property_identifier",
        regex: "^i18n$"
      }
    })?.next()?.next();
    if (i18nValue) {
      if (i18nValue.text().includes(i18nFunctionName))
        throw new AbortError(
          "An i18n strategy is already set up.",
          `Remove the existing i18n property in the ${importName}({...}) call to set up a new one.`
        );
      let { start, end } = i18nValue.range();
      content = content.slice(0, start.index) + i18nFunctionCall + content.slice(end.index);
    } else {
      let { end } = argumentObject.range(), firstPart = content.slice(0, end.index - 1);
      content = firstPart + ((/,\s*$/.test(firstPart) ? "" : ",") + `i18n: ${i18nFunctionCall}`) + content.slice(end.index - 1);
    }
    let lastImportRange = root.findAll({ rule: { kind: "import_statement" } }).pop()?.range() ?? {
      end: { index: 0 }
    }, i18nFunctionImport = joinPath(
      "~",
      relativePath(
        joinPath(rootDirectory, "app"),
        i18nFileFinalPath.slice(0, -extname(i18nFileFinalPath).length)
      )
    );
    return content = content.slice(0, lastImportRange.end.index) + `import {getLocaleFromRequest} from "${i18nFunctionImport}";` + content.slice(lastImportRange.end.index), content;
  });
}
async function replaceI18nStrategy({
  rootDirectory,
  contextCreate = joinPath("app", "lib", "context.ts")
}, formatConfig, i18nStrategyFilePath) {
  let isJs = contextCreate?.endsWith(".js") || !1, i18nPath = joinPath(
    rootDirectory,
    dirname(contextCreate),
    isJs ? "i18n.js" : "i18n.ts"
  );
  if (await fileExists(i18nPath))
    throw new AbortError(
      `${i18nPath} already exist. Renamed or remove the existing file before continue.`
    );
  return await copyFile(i18nStrategyFilePath, i18nPath), await replaceFileContent(i18nPath, formatConfig, async (content) => (content = content.replace(/\.\/mock-i18n-types\.js/, "@shopify/hydrogen"), isJs && (content = await transpileFile(content, i18nStrategyFilePath)), content)), i18nPath;
}
async function findContextCreateFile({
  rootDirectory,
  contextCreate = joinPath("app", "lib", "context.ts")
}) {
  let match = contextCreate.match(/\.([jt]sx?)$/)?.[1], { filepath, astType } = match ? { filepath: resolvePath(rootDirectory, contextCreate), astType: match } : await findFileWithExtension(rootDirectory, joinPath(contextCreate));
  if (!filepath || !astType)
    throw new AbortError(
      `Could not find a context create file at ${resolvePath(
        rootDirectory,
        contextCreate
      )}`
    );
  return { filepath, astType };
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/i18n/index.js
var SETUP_I18N_STRATEGIES = [
  "subfolders",
  "domains",
  "subdomains"
], I18N_STRATEGY_NAME_MAP = {
  subfolders: "Subfolders (example.com/fr-ca/...)",
  subdomains: "Subdomains (de.example.com/...)",
  domains: "Top-level domains (example.jp/...)"
}, I18N_CHOICES = [...SETUP_I18N_STRATEGIES, "none"];
async function setupI18nStrategy(strategy, options) {
  let templatePath = await getAssetsDir("i18n", `${strategy}.ts`);
  if (!await fileExists(templatePath))
    throw new Error("Unknown strategy");
  let formatConfig = await getCodeFormatOptions(options.rootDirectory);
  await replaceContextI18n(options, formatConfig, templatePath);
}
async function renderI18nPrompt(options) {
  let i18nStrategies = Object.entries({
    ...I18N_STRATEGY_NAME_MAP,
    ...options?.extraChoices
  });
  return renderSelectPrompt({
    message: "Select a URL structure to support multiple markets",
    ...options,
    choices: i18nStrategies.map(([value, label]) => ({
      value,
      label
    }))
  });
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/flags.js
var DEFAULT_APP_PORT = 3e3, DEFAULT_INSPECTOR_PORT = 9229, commonFlags = {
  path: {
    path: import_core.Flags.string({
      description: "The path to the directory of the Hydrogen storefront. Defaults to the current directory where the command is run.",
      env: "SHOPIFY_HYDROGEN_FLAG_PATH"
    })
  },
  port: {
    port: import_core.Flags.integer({
      description: `The port to run the server on. Defaults to ${DEFAULT_APP_PORT}.`,
      env: "SHOPIFY_HYDROGEN_FLAG_PORT"
    })
  },
  force: {
    force: import_core.Flags.boolean({
      description: "Overwrites the destination directory and files if they already exist.",
      env: "SHOPIFY_HYDROGEN_FLAG_FORCE",
      char: "f"
    })
  },
  shop: {
    shop: import_core.Flags.string({
      char: "s",
      description: "Shop URL. It can be the shop prefix (janes-apparel) or the full myshopify.com URL (janes-apparel.myshopify.com, https://janes-apparel.myshopify.com).",
      env: "SHOPIFY_SHOP",
      parse: async (input) => normalizeStoreFqdn(input)
    })
  },
  installDeps: {
    "install-deps": import_core.Flags.boolean({
      description: "Auto installs dependencies using the active package manager.",
      env: "SHOPIFY_HYDROGEN_FLAG_INSTALL_DEPS",
      allowNo: !0
    })
  },
  env: {
    env: import_core.Flags.string({
      description: "Specifies the environment to perform the operation using its handle. Fetch the handle using the `env list` command.",
      exclusive: ["env-branch"]
    })
  },
  /**
   * @deprecated use `env` instead.
   */
  envBranch: {
    "env-branch": import_core.Flags.string({
      description: "Specifies the environment to perform the operation using its Git branch name.",
      env: "SHOPIFY_HYDROGEN_ENVIRONMENT_BRANCH",
      deprecated: {
        to: "env",
        message: "--env-branch is deprecated. Use --env instead."
      }
    })
  },
  envFile: {
    "env-file": import_core.Flags.string({
      description: "Path to an environment file to override existing environment variables. Defaults to the '.env' located in your project path `--path`.",
      required: !1,
      default: ".env"
    })
  },
  sourcemap: {
    sourcemap: import_core.Flags.boolean({
      description: "Controls whether server sourcemaps are generated. Default to `true`. Deactivate `--no-sourcemaps`.",
      env: "SHOPIFY_HYDROGEN_FLAG_SOURCEMAP",
      default: !0,
      allowNo: !0
    })
  },
  codegen: {
    codegen: import_core.Flags.boolean({
      description: "Automatically generates GraphQL types for your project\u2019s Storefront API queries.",
      required: !1
    }),
    "codegen-config-path": import_core.Flags.string({
      description: "Specifies a path to a codegen configuration file. Defaults to `<root>/codegen.ts` if this file exists.",
      required: !1,
      dependsOn: ["codegen"]
    })
  },
  styling: {
    styling: import_core.Flags.string({
      description: `Sets the styling strategy to use. One of ${STYLING_CHOICES.map(
        (item) => `\`${item}\``
      ).join(", ")}.`,
      choices: STYLING_CHOICES,
      env: "SHOPIFY_HYDROGEN_FLAG_STYLING"
    })
  },
  markets: {
    markets: import_core.Flags.string({
      description: `Sets the URL structure to support multiple markets. Must be one of: ${I18N_CHOICES.map(
        (item) => `\`${item}\``
      ).join(", ")}. Example: \`--markets subfolders\`.`,
      choices: I18N_CHOICES,
      env: "SHOPIFY_HYDROGEN_FLAG_I18N"
    })
  },
  shortcut: {
    shortcut: import_core.Flags.boolean({
      description: "Creates a global h2 shortcut for Shopify CLI using shell aliases. Deactivate with `--no-shortcut`.",
      env: "SHOPIFY_HYDROGEN_FLAG_SHORTCUT",
      allowNo: !0
    })
  },
  debug: {
    debug: import_core.Flags.boolean({
      description: "Enables inspector connections to the server with a debugger such as Visual Studio Code or Chrome DevTools.",
      env: "SHOPIFY_HYDROGEN_FLAG_DEBUG"
    })
  },
  inspectorPort: {
    "inspector-port": import_core.Flags.integer({
      description: `The port where the inspector is available. Defaults to ${DEFAULT_INSPECTOR_PORT}.`,
      env: "SHOPIFY_HYDROGEN_FLAG_INSPECTOR_PORT"
    })
  },
  entry: {
    entry: import_core.Flags.string({
      description: "Entry file for the worker. Defaults to `./server`.",
      env: "SHOPIFY_HYDROGEN_FLAG_ENTRY"
    })
  },
  lockfileCheck: {
    "lockfile-check": import_core.Flags.boolean({
      allowNo: !0,
      default: !0,
      description: "Checks that there is exactly one valid lockfile in the project. Defaults to `true`. Deactivate with `--no-lockfile-check`.",
      env: "SHOPIFY_HYDROGEN_FLAG_LOCKFILE_CHECK"
    })
  },
  disableRouteWarning: {
    "disable-route-warning": import_core.Flags.boolean({
      description: "Disables any warnings about missing standard routes.",
      env: "SHOPIFY_HYDROGEN_FLAG_DISABLE_ROUTE_WARNING"
    })
  },
  customerAccountPush: {
    "customer-account-push": import_core.Flags.boolean({
      description: "Use tunneling for local development and push the tunneling domain to admin. Required to use Customer Account API's OAuth flow",
      required: !1,
      env: "SHOPIFY_HYDROGEN_FLAG_CUSTOMER_ACCOUNT_PUSH"
    })
  },
  verbose: {
    verbose: import_core.Flags.boolean({
      description: "Outputs more information about the command's execution.",
      required: !1,
      env: "SHOPIFY_HYDROGEN_FLAG_VERBOSE"
    })
  }
};
function flagsToCamelObject(obj) {
  return Object.entries(obj).reduce((acc, [key, value]) => (acc[camelize(key)] = value, acc), {});
}
function parseProcessFlags(processArgv, flagMap = {}) {
  let [, , ...args] = processArgv, options = {};
  for (let i = 0; i < args.length; i++) {
    let arg = args[i], nextArg = args[i + 1];
    if (arg?.startsWith("-")) {
      let key = arg.replace(/^\-{1,2}/, ""), value = !nextArg || nextArg.startsWith("-") ? !0 : nextArg;
      value === !0 && key.startsWith("no-") && (value = !1, key = key.replace("no-", "")), options[flagMap[key] || key] = value;
    }
  }
  return flagsToCamelObject(options);
}
function overrideFlag(flags, extra) {
  return Object.entries(extra).reduce(
    (acc, [key, value]) => (acc[key] = { ...flags[key], ...value }, acc),
    { ...flags }
  );
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/link-storefront.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/client.js
init_cjs_shims();
async function adminRequest(query, session, variables) {
  let api = "Admin", url = `https://${session.storeFqdn}/admin/api/unstable/graphql.json`;
  try {
    return await graphqlRequest({
      query,
      api,
      url,
      token: session.token,
      variables
    });
  } catch (error) {
    let errors = error.errors;
    throw errors?.some?.((error2) => error2.message.includes("app is not installed")) ? new AbortError(
      "Hydrogen sales channel isn't installed",
      "Install the Hydrogen sales channel on your store to start creating and linking Hydrogen storefronts: https://apps.shopify.com/hydrogen"
    ) : errors?.some?.(
      (error2) => error2.message.includes(
        "Access denied for hydrogenStorefrontCreate field"
      )
    ) ? new AbortError("Couldn't connect storefront to Shopify", [
      "Common reasons for this error include:",
      {
        list: {
          items: [
            "The Hydrogen sales channel isn't installed on the store.",
            "You don't have the required account permission to manage apps or channels.",
            "You're trying to connect to an ineligible store type (Trial, Development store)"
          ]
        }
      }
    ]) : error;
  }
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/gid.js
init_cjs_shims();
var GID_REGEXP = /gid:\/\/shopify\/\w*\/(\d+)/;
function parseGid(gid) {
  let matches = GID_REGEXP.exec(gid);
  if (matches && matches[1] !== void 0)
    return matches[1];
  throw new AbortError(`Invalid Global ID: ${gid}`);
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/link-storefront.js
var LinkStorefrontQuery = `#graphql
  query LinkStorefront {
    hydrogenStorefronts {
      id
      title
      productionUrl
    }
  }
`;
async function getStorefronts(adminSession) {
  let { hydrogenStorefronts } = await adminRequest(
    LinkStorefrontQuery,
    adminSession
  );
  return hydrogenStorefronts.map((storefront) => ({
    ...storefront,
    parsedId: parseGid(storefront.id)
  }));
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/shopify-config.js
init_cjs_shims();
var SHOPIFY_DIR = ".shopify", SHOPIFY_DIR_PROJECT = "project.json";
async function resetConfig(root) {
  let filePath = resolvePath(root, SHOPIFY_DIR, SHOPIFY_DIR_PROJECT);
  await fileExists(filePath) && await writeFile(filePath, JSON.stringify({}));
}
async function getConfig(root) {
  let filePath = resolvePath(root, SHOPIFY_DIR, SHOPIFY_DIR_PROJECT);
  return await fileExists(filePath) ? JSON.parse(await readFile(filePath)) : {};
}
async function setUserAccount(root, { shop, shopName, email }) {
  let filePath = resolvePath(root, SHOPIFY_DIR, SHOPIFY_DIR_PROJECT), existingConfig = {};
  await fileExists(filePath) ? existingConfig = JSON.parse(await readFile(filePath)) : await mkdir(dirname(filePath));
  let newConfig = {
    ...existingConfig,
    shop,
    shopName,
    email
  };
  return await writeFile(filePath, JSON.stringify(newConfig)), await ensureShopifyGitIgnore(root), newConfig;
}
async function setStorefront(root, { id, title }) {
  try {
    let filePath = resolvePath(root, SHOPIFY_DIR, SHOPIFY_DIR_PROJECT), config = {
      ...JSON.parse(await readFile(filePath)),
      storefront: { id, title }
    };
    return await writeFile(filePath, JSON.stringify(config)), await ensureShopifyGitIgnore(root), config;
  } catch {
    throw new AbortError("Project configuration could not be found.");
  }
}
async function unsetStorefront(root) {
  try {
    let filePath = resolvePath(root, SHOPIFY_DIR, SHOPIFY_DIR_PROJECT), config = {
      ...JSON.parse(
        await readFile(filePath)
      ),
      storefront: void 0
    };
    return await writeFile(filePath, JSON.stringify(config)), await ensureShopifyGitIgnore(root), config;
  } catch {
    throw new AbortError("Project configuration could not be found.");
  }
}
async function ensureShopifyGitIgnore(root) {
  try {
    let gitIgnoreFilePath = resolvePath(root, ".gitignore"), gitIgnoreContents = await fileExists(gitIgnoreFilePath) ? await readFile(gitIgnoreFilePath) : "";
    return gitIgnoreContents.includes(".shopify") ? !1 : (gitIgnoreContents.length > 0 && (gitIgnoreContents += `
`), gitIgnoreContents += `${SHOPIFY_DIR}\r
`, await writeFile(gitIgnoreFilePath, gitIgnoreContents), !0);
  } catch {
    return !1;
  }
}
async function setCustomerAccountConfig(root, customerAccountConfig) {
  try {
    let filePath = resolvePath(root, SHOPIFY_DIR, SHOPIFY_DIR_PROJECT), existingConfig = JSON.parse(await readFile(filePath)), config = {
      ...existingConfig,
      storefront: {
        ...existingConfig.storefront,
        customerAccountConfig
      }
    };
    return await writeFile(filePath, JSON.stringify(config)), await ensureShopifyGitIgnore(root), config;
  } catch {
    throw new AbortError("Project configuration could not be found.");
  }
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/create-storefront.js
init_cjs_shims();
var CreateStorefrontMutation = `#graphql
  mutation CreateStorefront($title: String!) {
    hydrogenStorefrontCreate(title: $title) {
      hydrogenStorefront {
        id
        title
        productionUrl
      }
      userErrors {
        code
        field
        message
      }
      jobId
    }
  }
`;
async function createStorefront(adminSession, title) {
  let {
    hydrogenStorefrontCreate: { hydrogenStorefront, userErrors, jobId }
  } = await adminRequest(
    CreateStorefrontMutation,
    adminSession,
    { title }
  );
  if (!hydrogenStorefront || !jobId || userErrors.length > 0) {
    let errorMessages = userErrors.map(({ message }) => message).join(", ");
    throw new AbortError("Could not create storefront. " + errorMessages);
  }
  return { jobId, storefront: hydrogenStorefront };
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/fetch-job.js
init_cjs_shims();
var FetchJobQuery = `#graphql
  query FetchJob($id: ID!) {
    hydrogenStorefrontJob(id: $id) {
      id
      done
      errors {
        code
        message
      }
    }
  }
`;
async function fetchJob(adminSession, jobId) {
  let { hydrogenStorefrontJob } = await adminRequest(
    FetchJobQuery,
    adminSession,
    { id: jobId }
  );
  return hydrogenStorefrontJob;
}
function waitForJob(adminSession, jobId) {
  return new Promise((resolve2, reject) => {
    let interval = setInterval(async () => {
      let job = await fetchJob(adminSession, jobId);
      if (job.errors.length > 0)
        return clearInterval(interval), reject();
      if (job.done)
        return clearInterval(interval), resolve2();
    }, 500);
  });
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/string.js
init_cjs_shims();
function titleize(name = "") {
  return name.replace(/[\W_]+/g, " ").split(" ").filter((word) => word.length > 0).map(
    (word) => word.charAt(0).toUpperCase() + word.slice(1).toLowerCase()
  ).join(" ");
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/shell.js
init_cjs_shims();
import os from "node:os";
var ALIAS_NAME = "h2", isWindows = () => os.platform() === "win32", isGitBash = () => !!process.env.MINGW_PREFIX;
function resolveFromHome(filepath) {
  return filepath[0] === "~" ? joinPath(os.homedir(), filepath.slice(1)) : filepath;
}
function homeFileExists(filepath) {
  try {
    return fileExists(resolveFromHome(filepath));
  } catch {
    return !1;
  }
}
async function supportsShell(shell) {
  try {
    return await execAsync(`which ${shell}`), !0;
  } catch {
    return !1;
  }
}
function getShellAliasDefinitionFile(shell) {
  return shell === "bash" ? "~/.bashrc" : shell === "zsh" ? "~/.zshrc" : `~/.config/fish/functions/${ALIAS_NAME}.fish`;
}
async function hasAliasDefinition(aliasName, shell) {
  let filepath = getShellAliasDefinitionFile(shell);
  try {
    return shell === "fish" ? await homeFileExists(filepath) : !!(await execAsync(
      `grep 'alias ${aliasName}' ${resolveFromHome(filepath)}`
    )).stdout;
  } catch {
    return !1;
  }
}
async function shellWriteFile(shell, content, append2 = !1) {
  let filepath = getShellAliasDefinitionFile(shell);
  content = `"${content}"`, content = content.replaceAll(`
`, "\\n"), isWindows() || (content = content.replaceAll("$", "\\$"));
  try {
    return await execAsync(
      `printf ${content} ${append2 ? ">>" : ">"} ${resolveFromHome(filepath)}`
    ), !0;
  } catch (error) {
    return outputDebug(
      `Could not create or modify ${filepath}:
` + error.stack
    ), !1;
  }
}
async function shellWriteAlias(shell, aliasName, content) {
  return await supportsShell(shell) ? await hasAliasDefinition(aliasName, shell) ? !0 : await shellWriteFile(shell, content, shell !== "fish") : !1;
}
async function shellRunScript(script, shellBin) {
  try {
    return await execAsync(script, { shell: shellBin }), !0;
  } catch (error) {
    return outputDebug(
      `Could not run shell script for ${shellBin}:
` + error.stack
    ), !1;
  }
}
function isKnownUnixShell(shell) {
  return ["zsh", "bash", "fish"].includes(shell);
}
async function hasCliAlias() {
  try {
    if (isWindows() && !isGitBash())
      await execAsync(`Get-Alias -Name ${ALIAS_NAME}`);
    else {
      let shell = os.userInfo().shell?.split("/").pop() ?? "bash";
      return isKnownUnixShell(shell) ? await hasAliasDefinition(ALIAS_NAME, shell) : !1;
    }
    return !0;
  } catch {
    return !1;
  }
}
async function createPlatformShortcut() {
  return isWindows() && !isGitBash() ? await createShortcutsForWindows() : await createShortcutsForUnix();
}
var BASH_ZSH_COMMAND = `
# Shopify Hydrogen alias to local projects
alias ${ALIAS_NAME}='$(npm prefix -s)/node_modules/.bin/shopify hydrogen'
`, FISH_FUNCTION = `
function ${ALIAS_NAME} --wraps='shopify hydrogen' --description 'Shortcut for the Hydrogen CLI'
   set npmPrefix (npm prefix -s)
   $npmPrefix/node_modules/.bin/shopify hydrogen $argv
end
`;
async function createShortcutsForUnix() {
  let shells = [];
  return await shellWriteAlias("zsh", ALIAS_NAME, BASH_ZSH_COMMAND) && shells.push("zsh"), await shellWriteAlias("bash", ALIAS_NAME, BASH_ZSH_COMMAND) && shells.push("bash"), await shellWriteAlias("fish", ALIAS_NAME, FISH_FUNCTION) && shells.push("fish"), shells;
}
var PS_FUNCTION = `function Invoke-Local-H2 {$npmPrefix = npm prefix -s; Invoke-Expression "$npmPrefix\\node_modules\\.bin\\shopify.ps1 hydrogen $Args"}; Set-Alias -Name ${ALIAS_NAME} -Value Invoke-Local-H2`, PS_APPEND_PROFILE_COMMAND = `
if (!(Test-Path -Path $PROFILE)) {
  New-Item -ItemType File -Path $PROFILE -Force
}

$profileContent = Get-Content -Path $PROFILE
if (!$profileContent -or $profileContent -NotLike '*Invoke-Local-H2*') {
  Add-Content -Path $PROFILE -Value '${PS_FUNCTION}'
}
`;
async function createShortcutsForWindows() {
  let shells = [];
  return await shellRunScript(PS_APPEND_PROFILE_COMMAND, "powershell.exe") && shells.push("PowerShell"), await shellRunScript(PS_APPEND_PROFILE_COMMAND, "pwsh.exe") && shells.push("PowerShell 7+"), shells;
}
async function getCliCommand(directory = process.cwd(), forcePkgManager) {
  if (await hasCliAlias())
    return ALIAS_NAME;
  let cli = "npx", pkgManager = forcePkgManager ?? await getPackageManager(directory).catch(() => null);
  return (pkgManager === "bun" || pkgManager === "pnpm" || pkgManager === "yarn") && (cli = pkgManager), `${cli} shopify hydrogen`;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/auth.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/business-platform/user-account.js
init_cjs_shims();
var CurrentUserAccountQuery = `#graphql
  query currentUserAccount {
    currentUserAccount {
      email
      organizations(first: 100) {
        edges {
          node {
            id
            name
            categories(handles: STORES) {
              destinations(first: 100) {
                edges {
                  node {
                    ...destinationFields
                  }
                }
                pageInfo {
                  endCursor
                  hasNextPage
                }
              }
            }
          }
        }
        pageInfo {
          endCursor
          hasNextPage
        }
      }
      orphanDestinations {
        categories(handles: STORES) {
          destinations(first: 100) {
            edges {
              node {
                ...destinationFields
              }
            }
            pageInfo {
              endCursor
              hasNextPage
            }
          }
        }
      }
    }
  }

  fragment destinationFields on Destination {
    name
    status
    webUrl
  }
`;
async function getUserAccount(token) {
  let { currentUserAccount } = await businessPlatformRequest(
    CurrentUserAccountQuery,
    token
  );
  if (!currentUserAccount)
    throw new AbortError("Unable to get current user account");
  return {
    email: currentUserAccount.email,
    activeShops: [
      ...currentUserAccount.organizations?.edges?.flatMap(
        (edge) => edge.node?.categories.flatMap(
          (category) => category?.destinations?.edges?.map((edge2) => edge2.node)
        )
      ) ?? [],
      ...currentUserAccount.orphanDestinations?.categories?.flatMap(
        (category) => category.destinations?.edges?.map((edge) => edge.node)
      ) ?? []
    ].filter(({ status }) => status === "ACTIVE").map(({ name, webUrl }) => ({
      name,
      fqdn: webUrl.replace(/^https?:\/\//, "").replace(/\/admin$/, "")
    }))
  };
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/log.js
init_cjs_shims();
var originalConsole = { ...console }, methodsReplaced = /* @__PURE__ */ new Set(), addedReplacers = /* @__PURE__ */ new Set(), messageReplacers = [];
function addMessageReplacers(key, ...items) {
  addedReplacers.has(key) || (addedReplacers.add(key), messageReplacers.push(...items));
}
var printedMessages = /* @__PURE__ */ new Set();
function debounceMessage(args, debounceFor) {
  let key = args.map((item) => {
    let message = item?.message ?? item;
    return typeof message == "string" ? message : "";
  }).filter(Boolean).join("");
  return printedMessages.has(key) ? !0 : (printedMessages.add(key), debounceFor !== !0 && setTimeout(() => printedMessages.delete(key), debounceFor ?? 1e3), !1);
}
function warningDebouncer([first2]) {
  return typeof first2 == "string" && // Show these warnings only once.
  /\[h2:(warn|info):(createStorefrontClient|createCustomerAccountClient)\]/.test(
    first2
  ) ? !0 : void 0;
}
function injectLogReplacer(method, debouncer) {
  methodsReplaced.has(method) || (methodsReplaced.add(method), console[method] = (...args) => {
    if (debouncer !== !1 && debounceMessage(args, debouncer?.(args)))
      return;
    let replacers = messageReplacers.reduce((acc, [matcher, replacer]) => (matcher(args, acc.length) && acc.push(replacer), acc), []);
    if (replacers.length === 0) return originalConsole[method](...args);
    let result = replacers.reduce(
      (resultArgs, replacer) => resultArgs && replacer(resultArgs),
      args
    );
    if (result) return originalConsole[method](...result);
  });
}
function muteDevLogs({ workerReload } = {}) {
  injectLogReplacer("log"), injectLogReplacer("error"), injectLogReplacer("warn", warningDebouncer), injectLogReplacer("debug", !1);
  let isFirstWorkerReload = !0;
  addMessageReplacers("dev-node", [
    ([first2]) => typeof first2 == "string" && first2.includes("[mf:"),
    (args) => {
      let first2 = args[0];
      if (workerReload !== !1 && first2.includes("Worker reloaded")) {
        if (isFirstWorkerReload) {
          isFirstWorkerReload = !1;
          return;
        }
        return [first2.replace("[mf:inf] ", "\u{1F504} ") + `
`, ...args.slice(1)];
      }
      first2.includes("[mf:err]");
    }
  ]), addMessageReplacers(
    "dev-workerd",
    // Workerd logs
    [
      ([first2]) => typeof first2 == "string" && /^\x1B\[31m(workerd\/|stack:( (0|[a-f\d]{4,})){3,})/.test(first2),
      () => {
      }
    ],
    [
      ([first2]) => typeof first2 == "string" && /\$LLVM_SYMBOLIZER|recursive isolate lock/.test(first2),
      () => {
      }
    ],
    // Non-actionable warnings:
    [
      ([first2]) => typeof first2 == "string" && /^A promise rejection/i.test(first2),
      () => {
      }
    ],
    [
      ([first2]) => typeof first2 == "string" && /resolved to multiple addresses/i.test(first2),
      // For win32
      () => {
      }
    ],
    // Non-actionable errors:
    [
      ([first2]) => {
        let message = first2?.message ?? first2;
        return typeof message == "string" && /^Network connection lost/i.test(message);
      },
      () => {
      }
    ]
  );
  let isLastLineBlank = !1, isLastLineRequestLog = !1;
  addMessageReplacers(
    "dev-vite",
    // Vite logs
    [
      // This log must come from Rollup and does not go through Vite's customLogger
      ([first2]) => typeof first2 == "string" && /^Sourcemap for .*@remix-run/i.test(first2),
      () => {
      }
    ],
    [
      // Log generated from Workerd HMR connection to Vite
      ([first2]) => typeof first2 == "string" && /^\[vite\] (connected|program reload)/i.test(first2),
      () => {
      }
    ],
    [
      // Log that gets entangled with our initial dev logs
      ([first2]) => typeof first2 == "string" && /^Re-optimizing dependencies because/i.test(first2),
      () => {
      }
    ],
    [
      // This error is fixed in new Remix versions:
      // https://github.com/remix-run/remix/pull/9194
      ([first2]) => {
        let message = first2?.message ?? first2;
        return /virtual-routes/.test(message) && /(Failed to load url|Could not resolve module for file)/i.test(
          message
        );
      },
      () => {
      }
    ],
    [
      // This log must come from Rollup and does not go through Vite's customLogger
      ([first2]) => typeof first2 == "string" && /^Generated an empty chunk:/i.test(first2),
      () => {
      }
    ],
    [
      // Log new lines between Request logs and other logs
      ([first2], existingMatches) => {
        if (existingMatches === 0 && typeof first2 == "string") {
          let isRequestLog = /^\s+[A-Z]+\s+\d{3}\s+[a-z]+\s+\//.test(
            // Clear ANSI colors before matching
            first2.replace(/\u001b\[.*?m/g, "")
          );
          !isLastLineBlank && (isRequestLog && !isLastLineRequestLog || !isRequestLog && isLastLineRequestLog) && process.stdout.write(`
`), isLastLineRequestLog = isRequestLog, isLastLineBlank = /\n$/.test(first2);
        }
        return !1;
      },
      (params) => params
    ]
  );
  let processStderrWrite = process.stderr.write, timeout = setTimeout(() => {
    process.stderr.write = processStderrWrite;
  }, 5e3);
  process.stderr.write = (...args) => typeof args[0] == "string" && args[0].includes("Could not find ts-node") ? (clearTimeout(timeout), process.stderr.write = processStderrWrite, !1) : processStderrWrite.apply(process.stderr, args);
}
function enhanceAuthLogs(hideInitialLog = !1) {
  injectLogReplacer("log", warningDebouncer), addMessageReplacers(
    "auth",
    [
      ([first2]) => hideInitialLog && typeof first2 == "string" && first2.includes("To run this command,"),
      ([first2]) => {
      }
    ],
    [
      ([first2]) => typeof first2 == "string" && first2.includes("Logged in."),
      () => {
        process.stdout.write(ansi_escapes_default.eraseLines(hideInitialLog ? 5 : 6));
      }
    ]
  );
}
function enhanceH2Logs(options) {
  injectLogReplacer("error"), injectLogReplacer("warn", warningDebouncer), injectLogReplacer("log", warningDebouncer), injectLogReplacer("info", warningDebouncer), addMessageReplacers("h2-warn", [
    ([first2]) => {
      let message = first2?.message ?? first2;
      return typeof message == "string" && message.includes("[h2:");
    },
    (args) => {
      let firstArg = args[0], errorObject = typeof firstArg == "object" && firstArg.stack ? firstArg : void 0, stringArg = errorObject?.message ?? firstArg;
      stringArg.startsWith("[h2:info:createStorefrontClient]") && stringArg.includes("defaulting to mock.shop") && (stringArg += "\nRun `h2 link` to link your store.");
      let [, type, scope, message] = stringArg.match(/\[h2:([^:]+):([^\]]+)\]\s+(.*)$/ims) || [];
      if (!type || !scope || !message) return args;
      let headline = `In Hydrogen's \`${scope.trim()}\`:

`, lines = message.split(`
`), lastLine = lines.at(-1) ?? "", hasLinks = /https?:\/\//.test(lastLine), hasCommands = /`h2 [^`]+`/.test(lastLine);
      if (hasCommands && lastLine && (lastLine = lastLine.replace(
        /`(h2) ([^`]+)`/g,
        source_default.magentaBright(`\`${options.cliCommand ?? "$1"} $2\``)
      )), (hasLinks || hasCommands) && lines.pop(), type === "error" || errorObject) {
        let tryMessage = hasLinks || hasCommands ? lastLine : void 0, stack = errorObject?.stack, cause = errorObject?.cause;
        if (typeof cause == "string")
          try {
            cause = JSON.parse(cause);
          } catch {
          }
        if (typeof cause != "string" && cause?.graphql?.query) {
          let link = getGraphiQLUrl({
            host: options.host,
            graphql: cause.graphql
          }), [, queryType, queryName] = cause.graphql.query.match(/(query|mutation)\s+(\w+)/) || [];
          tryMessage = (tryMessage ? `${tryMessage}

` : "") + outputContent`To debug the ${queryType || "query"}${queryName ? ` \`${source_default.whiteBright(queryName)}\`` : ""}, try it in ${outputToken.link(source_default.bold("GraphiQL"), link)}.`.value;
        }
        let stackLines = stack?.split(`
`) ?? [], isAppLine = (line) => line.includes(options.rootDirectory) && !line.includes("node_modules"), firstAppLineIndex = stackLines.findIndex(isAppLine), lastAppLineIndex = stackLines.length - [...stackLines].reverse().findIndex(isAppLine);
        firstAppLineIndex > 0 && lastAppLineIndex > firstAppLineIndex && (stack = [
          stackLines[0],
          // Error message
          ...stackLines.slice(firstAppLineIndex, lastAppLineIndex)
          // App code
        ].join(`
`).trim() || void 0);
        let error = new BugError(
          headline + source_default.bold(lines.join(`
`).replace(" - Request", `
Request`)),
          tryMessage
        );
        error.cause = cause, error.stack = stack, renderFatalError(error);
        return;
      }
      let reference;
      if (hasLinks) {
        reference = [];
        for (let link of lastLine.matchAll(/https?:\/\/[^\s]+/g))
          reference.push(link[0]);
      }
      (type === "warn" ? renderWarning : renderInfo)({
        body: headline + source_default.bold(lines.join(`
`)),
        reference,
        nextSteps: hasCommands ? [lastLine] : void 0
      });
    }
  ]);
}
function setH2OVerbose() {
  !process.env.DEBUG || process.env.DEBUG === "*" ? process.env.DEBUG = "h2:*,o2:*" : process.env.DEBUG += ",h2:*,o2:*";
}
function isH2Verbose() {
  return !!(process.env.DEBUG === "*" || process.env.DEBUG?.includes("h2:*"));
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/auth.js
async function logout2(root) {
  await logout(), await resetConfig(root);
}
async function login(root, shop) {
  enhanceAuthLogs(!1);
  let forcePrompt = shop === !0, existingConfig = root ? await getConfig(root) : {}, { email, shopName } = existingConfig;
  if (typeof shop != "string" && (shop = existingConfig.shop), shop && (shop = await normalizeStoreFqdn(shop)), !shop || !shopName || !email || forcePrompt || shop !== existingConfig.shop) {
    let token = await ensureAuthenticatedBusinessPlatform().catch(() => {
      throw new AbortError(
        "Unable to authenticate with Shopify. Please report this issue."
      );
    }), userAccount = await getUserAccount(token), selected = !forcePrompt && shop && userAccount.activeShops.find(({ fqdn }) => shop === fqdn) || await renderSelectPrompt({
      message: "Select a shop to log in to",
      choices: userAccount.activeShops.map(({ name, fqdn }) => ({
        label: `${name} (${fqdn})`,
        value: { name, fqdn }
      }))
    });
    shop = selected.fqdn, shopName = selected.name, email = userAccount.email;
  }
  let session = await ensureAuthenticatedAdmin(shop).catch(() => {
    throw new AbortError("Unable to authenticate with Shopify", void 0, [
      `Ensure the shop that you specified is correct (you are trying to use: ${shop})`
    ]);
  }), config = root ? await setUserAccount(root, { shop, shopName, email }) : { shop, shopName, email };
  return { session, config };
}
function renderLoginSuccess(config) {
  renderSuccess({
    headline: "Shopify authentication complete",
    body: [
      "You are logged in to",
      { userInput: config.shopName ?? config.shop ?? "your store" },
      ...config.email ? ["as", { userInput: config.email }] : []
    ]
  });
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/onboarding/common.js
init_cjs_shims();
import { symlink, readdir as readdir3 } from "node:fs/promises";

// ../cli-kit/dist/public/node/git.js
init_cjs_shims();

// ../../node_modules/.pnpm/simple-git@3.27.0/node_modules/simple-git/dist/esm/index.js
init_cjs_shims();
var import_file_exists = __toESM(require_dist(), 1), import_debug = __toESM(require_src(), 1), import_promise_deferred = __toESM(require_dist2(), 1), import_promise_deferred2 = __toESM(require_dist2(), 1);
import { spawn } from "child_process";
import { EventEmitter as EventEmitter2 } from "node:events";
var __defProp = Object.defineProperty, __defProps = Object.defineProperties, __getOwnPropDesc = Object.getOwnPropertyDescriptor, __getOwnPropDescs = Object.getOwnPropertyDescriptors, __getOwnPropNames = Object.getOwnPropertyNames, __getOwnPropSymbols = Object.getOwnPropertySymbols, __hasOwnProp = Object.prototype.hasOwnProperty, __propIsEnum = Object.prototype.propertyIsEnumerable, __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: !0, configurable: !0, writable: !0, value }) : obj[key] = value, __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    __hasOwnProp.call(b, prop) && __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b))
      __propIsEnum.call(b, prop) && __defNormalProp(a, prop, b[prop]);
  return a;
}, __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b)), __esm = (fn, res) => function() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
}, __commonJS2 = (cb, mod) => function() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
}, __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: !0 });
}, __copyProps = (to, from, except, desc) => {
  if (from && typeof from == "object" || typeof from == "function")
    for (let key of __getOwnPropNames(from))
      !__hasOwnProp.call(to, key) && key !== except && __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  return to;
}, __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: !0 }), mod), __async = (__this, __arguments, generator) => new Promise((resolve2, reject) => {
  var fulfilled = (value) => {
    try {
      step(generator.next(value));
    } catch (e) {
      reject(e);
    }
  }, rejected = (value) => {
    try {
      step(generator.throw(value));
    } catch (e) {
      reject(e);
    }
  }, step = (x) => x.done ? resolve2(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
  step((generator = generator.apply(__this, __arguments)).next());
});
function pathspec(...paths) {
  let key = new String(paths);
  return cache.set(key, paths), key;
}
function isPathSpec(path3) {
  return path3 instanceof String && cache.has(path3);
}
function toPaths(pathSpec) {
  return cache.get(pathSpec) || [];
}
var cache, init_pathspec = __esm({
  "src/lib/args/pathspec.ts"() {
    "use strict";
    cache = /* @__PURE__ */ new WeakMap();
  }
}), GitError, init_git_error = __esm({
  "src/lib/errors/git-error.ts"() {
    "use strict";
    GitError = class extends Error {
      constructor(task, message) {
        super(message), this.task = task, Object.setPrototypeOf(this, new.target.prototype);
      }
    };
  }
}), GitResponseError, init_git_response_error = __esm({
  "src/lib/errors/git-response-error.ts"() {
    "use strict";
    init_git_error(), GitResponseError = class extends GitError {
      constructor(git, message) {
        super(void 0, message || String(git)), this.git = git;
      }
    };
  }
}), TaskConfigurationError, init_task_configuration_error = __esm({
  "src/lib/errors/task-configuration-error.ts"() {
    "use strict";
    init_git_error(), TaskConfigurationError = class extends GitError {
      constructor(message) {
        super(void 0, message);
      }
    };
  }
});
function asFunction(source) {
  return typeof source == "function" ? source : NOOP;
}
function isUserFunction(source) {
  return typeof source == "function" && source !== NOOP;
}
function splitOn(input, char) {
  let index = input.indexOf(char);
  return index <= 0 ? [input, ""] : [input.substr(0, index), input.substr(index + 1)];
}
function first(input, offset = 0) {
  return isArrayLike(input) && input.length > offset ? input[offset] : void 0;
}
function last(input, offset = 0) {
  if (isArrayLike(input) && input.length > offset)
    return input[input.length - 1 - offset];
}
function isArrayLike(input) {
  return !!(input && typeof input.length == "number");
}
function toLinesWithContent(input = "", trimmed2 = !0, separator = `
`) {
  return input.split(separator).reduce((output, line) => {
    let lineContent = trimmed2 ? line.trim() : line;
    return lineContent && output.push(lineContent), output;
  }, []);
}
function forEachLineWithContent(input, callback) {
  return toLinesWithContent(input, !0).map((line) => callback(line));
}
function folderExists(path3) {
  return (0, import_file_exists.exists)(path3, import_file_exists.FOLDER);
}
function append(target, item) {
  return Array.isArray(target) ? target.includes(item) || target.push(item) : target.add(item), item;
}
function including(target, item) {
  return Array.isArray(target) && !target.includes(item) && target.push(item), target;
}
function remove(target, item) {
  if (Array.isArray(target)) {
    let index = target.indexOf(item);
    index >= 0 && target.splice(index, 1);
  } else
    target.delete(item);
  return item;
}
function asArray(source) {
  return Array.isArray(source) ? source : [source];
}
function asCamelCase(str) {
  return str.replace(/[\s-]+(.)/g, (_all, chr) => chr.toUpperCase());
}
function asStringArray(source) {
  return asArray(source).map(String);
}
function asNumber(source, onNaN = 0) {
  if (source == null)
    return onNaN;
  let num = parseInt(source, 10);
  return isNaN(num) ? onNaN : num;
}
function prefixedArray(input, prefix) {
  let output = [];
  for (let i = 0, max = input.length; i < max; i++)
    output.push(prefix, input[i]);
  return output;
}
function bufferToString(input) {
  return (Array.isArray(input) ? Buffer.concat(input) : input).toString("utf-8");
}
function pick(source, properties) {
  return Object.assign(
    {},
    ...properties.map((property) => property in source ? { [property]: source[property] } : {})
  );
}
function delay(duration = 0) {
  return new Promise((done) => setTimeout(done, duration));
}
function orVoid(input) {
  if (input !== !1)
    return input;
}
var NULL, NOOP, objectToString, init_util = __esm({
  "src/lib/utils/util.ts"() {
    "use strict";
    NULL = "\0", NOOP = () => {
    }, objectToString = Object.prototype.toString.call.bind(Object.prototype.toString);
  }
});
function filterType(input, filter, def) {
  return filter(input) ? input : arguments.length > 2 ? def : void 0;
}
function filterPrimitives(input, omit) {
  let type = isPathSpec(input) ? "string" : typeof input;
  return /number|string|boolean/.test(type) && (!omit || !omit.includes(type));
}
function filterPlainObject(input) {
  return !!input && objectToString(input) === "[object Object]";
}
function filterFunction(input) {
  return typeof input == "function";
}
var filterArray, filterString, filterStringArray, filterStringOrStringArray, filterHasLength, init_argument_filters = __esm({
  "src/lib/utils/argument-filters.ts"() {
    "use strict";
    init_util(), init_pathspec(), filterArray = (input) => Array.isArray(input), filterString = (input) => typeof input == "string", filterStringArray = (input) => Array.isArray(input) && input.every(filterString), filterStringOrStringArray = (input) => filterString(input) || Array.isArray(input) && input.every(filterString), filterHasLength = (input) => input == null || "number|boolean|function".includes(typeof input) ? !1 : Array.isArray(input) || typeof input == "string" || typeof input.length == "number";
  }
}), ExitCodes, init_exit_codes = __esm({
  "src/lib/utils/exit-codes.ts"() {
    "use strict";
    ExitCodes = /* @__PURE__ */ ((ExitCodes2) => (ExitCodes2[ExitCodes2.SUCCESS = 0] = "SUCCESS", ExitCodes2[ExitCodes2.ERROR = 1] = "ERROR", ExitCodes2[ExitCodes2.NOT_FOUND = -2] = "NOT_FOUND", ExitCodes2[ExitCodes2.UNCLEAN = 128] = "UNCLEAN", ExitCodes2))(ExitCodes || {});
  }
}), GitOutputStreams, init_git_output_streams = __esm({
  "src/lib/utils/git-output-streams.ts"() {
    "use strict";
    GitOutputStreams = class {
      constructor(stdOut, stdErr) {
        this.stdOut = stdOut, this.stdErr = stdErr;
      }
      asStrings() {
        return new GitOutputStreams(this.stdOut.toString("utf8"), this.stdErr.toString("utf8"));
      }
    };
  }
}), LineParser, RemoteLineParser, init_line_parser = __esm({
  "src/lib/utils/line-parser.ts"() {
    "use strict";
    LineParser = class {
      constructor(regExp, useMatches) {
        this.matches = [], this.parse = (line, target) => (this.resetMatches(), this._regExp.every((reg, index) => this.addMatch(reg, index, line(index))) ? this.useMatches(target, this.prepareMatches()) !== !1 : !1), this._regExp = Array.isArray(regExp) ? regExp : [regExp], useMatches && (this.useMatches = useMatches);
      }
      useMatches(target, match) {
        throw new Error("LineParser:useMatches not implemented");
      }
      resetMatches() {
        this.matches.length = 0;
      }
      prepareMatches() {
        return this.matches;
      }
      addMatch(reg, index, line) {
        let matched = line && reg.exec(line);
        return matched && this.pushMatch(index, matched), !!matched;
      }
      pushMatch(_index, matched) {
        this.matches.push(...matched.slice(1));
      }
    }, RemoteLineParser = class extends LineParser {
      addMatch(reg, index, line) {
        return /^remote:\s/.test(String(line)) && super.addMatch(reg, index, line);
      }
      pushMatch(index, matched) {
        (index > 0 || matched.length > 1) && super.pushMatch(index, matched);
      }
    };
  }
});
function createInstanceConfig(...options) {
  let baseDir = process.cwd(), config = Object.assign(
    __spreadValues({ baseDir }, defaultOptions),
    ...options.filter((o) => typeof o == "object" && o)
  );
  return config.baseDir = config.baseDir || baseDir, config.trimmed = config.trimmed === !0, config;
}
var defaultOptions, init_simple_git_options = __esm({
  "src/lib/utils/simple-git-options.ts"() {
    "use strict";
    defaultOptions = {
      binary: "git",
      maxConcurrentProcesses: 5,
      config: [],
      trimmed: !1
    };
  }
});
function appendTaskOptions(options, commands = []) {
  return filterPlainObject(options) ? Object.keys(options).reduce((commands2, key) => {
    let value = options[key];
    return isPathSpec(value) ? commands2.push(value) : filterPrimitives(value, ["boolean"]) ? commands2.push(key + "=" + value) : commands2.push(key), commands2;
  }, commands) : commands;
}
function getTrailingOptions(args, initialPrimitive = 0, objectOnly = !1) {
  let command = [];
  for (let i = 0, max = initialPrimitive < 0 ? args.length : initialPrimitive; i < max; i++)
    "string|number".includes(typeof args[i]) && command.push(String(args[i]));
  return appendTaskOptions(trailingOptionsArgument(args), command), objectOnly || command.push(...trailingArrayArgument(args)), command;
}
function trailingArrayArgument(args) {
  let hasTrailingCallback = typeof last(args) == "function";
  return filterType(last(args, hasTrailingCallback ? 1 : 0), filterArray, []);
}
function trailingOptionsArgument(args) {
  let hasTrailingCallback = filterFunction(last(args));
  return filterType(last(args, hasTrailingCallback ? 1 : 0), filterPlainObject);
}
function trailingFunctionArgument(args, includeNoop = !0) {
  let callback = asFunction(last(args));
  return includeNoop || isUserFunction(callback) ? callback : void 0;
}
var init_task_options = __esm({
  "src/lib/utils/task-options.ts"() {
    "use strict";
    init_argument_filters(), init_util(), init_pathspec();
  }
});
function callTaskParser(parser4, streams) {
  return parser4(streams.stdOut, streams.stdErr);
}
function parseStringResponse(result, parsers12, texts, trim = !0) {
  return asArray(texts).forEach((text) => {
    for (let lines = toLinesWithContent(text, trim), i = 0, max = lines.length; i < max; i++) {
      let line = (offset = 0) => {
        if (!(i + offset >= max))
          return lines[i + offset];
      };
      parsers12.some(({ parse: parse2 }) => parse2(line, result));
    }
  }), result;
}
var init_task_parser = __esm({
  "src/lib/utils/task-parser.ts"() {
    "use strict";
    init_util();
  }
}), utils_exports = {};
__export(utils_exports, {
  ExitCodes: () => ExitCodes,
  GitOutputStreams: () => GitOutputStreams,
  LineParser: () => LineParser,
  NOOP: () => NOOP,
  NULL: () => NULL,
  RemoteLineParser: () => RemoteLineParser,
  append: () => append,
  appendTaskOptions: () => appendTaskOptions,
  asArray: () => asArray,
  asCamelCase: () => asCamelCase,
  asFunction: () => asFunction,
  asNumber: () => asNumber,
  asStringArray: () => asStringArray,
  bufferToString: () => bufferToString,
  callTaskParser: () => callTaskParser,
  createInstanceConfig: () => createInstanceConfig,
  delay: () => delay,
  filterArray: () => filterArray,
  filterFunction: () => filterFunction,
  filterHasLength: () => filterHasLength,
  filterPlainObject: () => filterPlainObject,
  filterPrimitives: () => filterPrimitives,
  filterString: () => filterString,
  filterStringArray: () => filterStringArray,
  filterStringOrStringArray: () => filterStringOrStringArray,
  filterType: () => filterType,
  first: () => first,
  folderExists: () => folderExists,
  forEachLineWithContent: () => forEachLineWithContent,
  getTrailingOptions: () => getTrailingOptions,
  including: () => including,
  isUserFunction: () => isUserFunction,
  last: () => last,
  objectToString: () => objectToString,
  orVoid: () => orVoid,
  parseStringResponse: () => parseStringResponse,
  pick: () => pick,
  prefixedArray: () => prefixedArray,
  remove: () => remove,
  splitOn: () => splitOn,
  toLinesWithContent: () => toLinesWithContent,
  trailingFunctionArgument: () => trailingFunctionArgument,
  trailingOptionsArgument: () => trailingOptionsArgument
});
var init_utils = __esm({
  "src/lib/utils/index.ts"() {
    "use strict";
    init_argument_filters(), init_exit_codes(), init_git_output_streams(), init_line_parser(), init_simple_git_options(), init_task_options(), init_task_parser(), init_util();
  }
}), check_is_repo_exports = {};
__export(check_is_repo_exports, {
  CheckRepoActions: () => CheckRepoActions,
  checkIsBareRepoTask: () => checkIsBareRepoTask,
  checkIsRepoRootTask: () => checkIsRepoRootTask,
  checkIsRepoTask: () => checkIsRepoTask
});
function checkIsRepoTask(action) {
  switch (action) {
    case "bare":
      return checkIsBareRepoTask();
    case "root":
      return checkIsRepoRootTask();
  }
  return {
    commands: ["rev-parse", "--is-inside-work-tree"],
    format: "utf-8",
    onError,
    parser
  };
}
function checkIsRepoRootTask() {
  return {
    commands: ["rev-parse", "--git-dir"],
    format: "utf-8",
    onError,
    parser(path3) {
      return /^\.(git)?$/.test(path3.trim());
    }
  };
}
function checkIsBareRepoTask() {
  return {
    commands: ["rev-parse", "--is-bare-repository"],
    format: "utf-8",
    onError,
    parser
  };
}
function isNotRepoMessage(error) {
  return /(Not a git repository|Kein Git-Repository)/i.test(String(error));
}
var CheckRepoActions, onError, parser, init_check_is_repo = __esm({
  "src/lib/tasks/check-is-repo.ts"() {
    "use strict";
    init_utils(), CheckRepoActions = /* @__PURE__ */ ((CheckRepoActions2) => (CheckRepoActions2.BARE = "bare", CheckRepoActions2.IN_TREE = "tree", CheckRepoActions2.IS_REPO_ROOT = "root", CheckRepoActions2))(CheckRepoActions || {}), onError = ({ exitCode }, error, done, fail) => {
      if (exitCode === 128 && isNotRepoMessage(error))
        return done(Buffer.from("false"));
      fail(error);
    }, parser = (text) => text.trim() === "true";
  }
});
function cleanSummaryParser(dryRun, text) {
  let summary = new CleanResponse(dryRun), regexp = dryRun ? dryRunRemovalRegexp : removalRegexp;
  return toLinesWithContent(text).forEach((line) => {
    let removed = line.replace(regexp, "");
    summary.paths.push(removed), (isFolderRegexp.test(removed) ? summary.folders : summary.files).push(removed);
  }), summary;
}
var CleanResponse, removalRegexp, dryRunRemovalRegexp, isFolderRegexp, init_CleanSummary = __esm({
  "src/lib/responses/CleanSummary.ts"() {
    "use strict";
    init_utils(), CleanResponse = class {
      constructor(dryRun) {
        this.dryRun = dryRun, this.paths = [], this.files = [], this.folders = [];
      }
    }, removalRegexp = /^[a-z]+\s*/i, dryRunRemovalRegexp = /^[a-z]+\s+[a-z]+\s*/i, isFolderRegexp = /\/$/;
  }
}), task_exports = {};
__export(task_exports, {
  EMPTY_COMMANDS: () => EMPTY_COMMANDS,
  adhocExecTask: () => adhocExecTask,
  configurationErrorTask: () => configurationErrorTask,
  isBufferTask: () => isBufferTask,
  isEmptyTask: () => isEmptyTask,
  straightThroughBufferTask: () => straightThroughBufferTask,
  straightThroughStringTask: () => straightThroughStringTask
});
function adhocExecTask(parser4) {
  return {
    commands: EMPTY_COMMANDS,
    format: "empty",
    parser: parser4
  };
}
function configurationErrorTask(error) {
  return {
    commands: EMPTY_COMMANDS,
    format: "empty",
    parser() {
      throw typeof error == "string" ? new TaskConfigurationError(error) : error;
    }
  };
}
function straightThroughStringTask(commands, trimmed2 = !1) {
  return {
    commands,
    format: "utf-8",
    parser(text) {
      return trimmed2 ? String(text).trim() : text;
    }
  };
}
function straightThroughBufferTask(commands) {
  return {
    commands,
    format: "buffer",
    parser(buffer) {
      return buffer;
    }
  };
}
function isBufferTask(task) {
  return task.format === "buffer";
}
function isEmptyTask(task) {
  return task.format === "empty" || !task.commands.length;
}
var EMPTY_COMMANDS, init_task = __esm({
  "src/lib/tasks/task.ts"() {
    "use strict";
    init_task_configuration_error(), EMPTY_COMMANDS = [];
  }
}), clean_exports = {};
__export(clean_exports, {
  CONFIG_ERROR_INTERACTIVE_MODE: () => CONFIG_ERROR_INTERACTIVE_MODE,
  CONFIG_ERROR_MODE_REQUIRED: () => CONFIG_ERROR_MODE_REQUIRED,
  CONFIG_ERROR_UNKNOWN_OPTION: () => CONFIG_ERROR_UNKNOWN_OPTION,
  CleanOptions: () => CleanOptions,
  cleanTask: () => cleanTask,
  cleanWithOptionsTask: () => cleanWithOptionsTask,
  isCleanOptionsArray: () => isCleanOptionsArray
});
function cleanWithOptionsTask(mode, customArgs) {
  let { cleanMode, options, valid } = getCleanOptions(mode);
  return cleanMode ? valid.options ? (options.push(...customArgs), options.some(isInteractiveMode) ? configurationErrorTask(CONFIG_ERROR_INTERACTIVE_MODE) : cleanTask(cleanMode, options)) : configurationErrorTask(CONFIG_ERROR_UNKNOWN_OPTION + JSON.stringify(mode)) : configurationErrorTask(CONFIG_ERROR_MODE_REQUIRED);
}
function cleanTask(mode, customArgs) {
  return {
    commands: ["clean", `-${mode}`, ...customArgs],
    format: "utf-8",
    parser(text) {
      return cleanSummaryParser(mode === "n", text);
    }
  };
}
function isCleanOptionsArray(input) {
  return Array.isArray(input) && input.every((test) => CleanOptionValues.has(test));
}
function getCleanOptions(input) {
  let cleanMode, options = [], valid = { cleanMode: !1, options: !0 };
  return input.replace(/[^a-z]i/g, "").split("").forEach((char) => {
    isCleanMode(char) ? (cleanMode = char, valid.cleanMode = !0) : valid.options = valid.options && isKnownOption(options[options.length] = `-${char}`);
  }), {
    cleanMode,
    options,
    valid
  };
}
function isCleanMode(cleanMode) {
  return cleanMode === "f" || cleanMode === "n";
}
function isKnownOption(option) {
  return /^-[a-z]$/i.test(option) && CleanOptionValues.has(option.charAt(1));
}
function isInteractiveMode(option) {
  return /^-[^\-]/.test(option) ? option.indexOf("i") > 0 : option === "--interactive";
}
var CONFIG_ERROR_INTERACTIVE_MODE, CONFIG_ERROR_MODE_REQUIRED, CONFIG_ERROR_UNKNOWN_OPTION, CleanOptions, CleanOptionValues, init_clean = __esm({
  "src/lib/tasks/clean.ts"() {
    "use strict";
    init_CleanSummary(), init_utils(), init_task(), CONFIG_ERROR_INTERACTIVE_MODE = "Git clean interactive mode is not supported", CONFIG_ERROR_MODE_REQUIRED = 'Git clean mode parameter ("n" or "f") is required', CONFIG_ERROR_UNKNOWN_OPTION = "Git clean unknown option found in: ", CleanOptions = /* @__PURE__ */ ((CleanOptions2) => (CleanOptions2.DRY_RUN = "n", CleanOptions2.FORCE = "f", CleanOptions2.IGNORED_INCLUDED = "x", CleanOptions2.IGNORED_ONLY = "X", CleanOptions2.EXCLUDING = "e", CleanOptions2.QUIET = "q", CleanOptions2.RECURSIVE = "d", CleanOptions2))(CleanOptions || {}), CleanOptionValues = /* @__PURE__ */ new Set([
      "i",
      ...asStringArray(Object.values(CleanOptions))
    ]);
  }
});
function configListParser(text) {
  let config = new ConfigList();
  for (let item of configParser(text))
    config.addValue(item.file, String(item.key), item.value);
  return config;
}
function configGetParser(text, key) {
  let value = null, values = [], scopes = /* @__PURE__ */ new Map();
  for (let item of configParser(text, key))
    item.key === key && (values.push(value = item.value), scopes.has(item.file) || scopes.set(item.file, []), scopes.get(item.file).push(value));
  return {
    key,
    paths: Array.from(scopes.keys()),
    scopes,
    value,
    values
  };
}
function configFilePath(filePath) {
  return filePath.replace(/^(file):/, "");
}
function* configParser(text, requestedKey = null) {
  let lines = text.split("\0");
  for (let i = 0, max = lines.length - 1; i < max; ) {
    let file = configFilePath(lines[i++]), value = lines[i++], key = requestedKey;
    if (value.includes(`
`)) {
      let line = splitOn(value, `
`);
      key = line[0], value = line[1];
    }
    yield { file, key, value };
  }
}
var ConfigList, init_ConfigList = __esm({
  "src/lib/responses/ConfigList.ts"() {
    "use strict";
    init_utils(), ConfigList = class {
      constructor() {
        this.files = [], this.values = /* @__PURE__ */ Object.create(null);
      }
      get all() {
        return this._all || (this._all = this.files.reduce((all, file) => Object.assign(all, this.values[file]), {})), this._all;
      }
      addFile(file) {
        if (!(file in this.values)) {
          let latest = last(this.files);
          this.values[file] = latest ? Object.create(this.values[latest]) : {}, this.files.push(file);
        }
        return this.values[file];
      }
      addValue(file, key, value) {
        let values = this.addFile(file);
        values.hasOwnProperty(key) ? Array.isArray(values[key]) ? values[key].push(value) : values[key] = [values[key], value] : values[key] = value, this._all = void 0;
      }
    };
  }
});
function asConfigScope(scope, fallback) {
  return typeof scope == "string" && GitConfigScope.hasOwnProperty(scope) ? scope : fallback;
}
function addConfigTask(key, value, append2, scope) {
  let commands = ["config", `--${scope}`];
  return append2 && commands.push("--add"), commands.push(key, value), {
    commands,
    format: "utf-8",
    parser(text) {
      return text;
    }
  };
}
function getConfigTask(key, scope) {
  let commands = ["config", "--null", "--show-origin", "--get-all", key];
  return scope && commands.splice(1, 0, `--${scope}`), {
    commands,
    format: "utf-8",
    parser(text) {
      return configGetParser(text, key);
    }
  };
}
function listConfigTask(scope) {
  let commands = ["config", "--list", "--show-origin", "--null"];
  return scope && commands.push(`--${scope}`), {
    commands,
    format: "utf-8",
    parser(text) {
      return configListParser(text);
    }
  };
}
function config_default() {
  return {
    addConfig(key, value, ...rest) {
      return this._runTask(
        addConfigTask(
          key,
          value,
          rest[0] === !0,
          asConfigScope(
            rest[1],
            "local"
            /* local */
          )
        ),
        trailingFunctionArgument(arguments)
      );
    },
    getConfig(key, scope) {
      return this._runTask(
        getConfigTask(key, asConfigScope(scope, void 0)),
        trailingFunctionArgument(arguments)
      );
    },
    listConfig(...rest) {
      return this._runTask(
        listConfigTask(asConfigScope(rest[0], void 0)),
        trailingFunctionArgument(arguments)
      );
    }
  };
}
var GitConfigScope, init_config = __esm({
  "src/lib/tasks/config.ts"() {
    "use strict";
    init_ConfigList(), init_utils(), GitConfigScope = /* @__PURE__ */ ((GitConfigScope2) => (GitConfigScope2.system = "system", GitConfigScope2.global = "global", GitConfigScope2.local = "local", GitConfigScope2.worktree = "worktree", GitConfigScope2))(GitConfigScope || {});
  }
});
function isDiffNameStatus(input) {
  return diffNameStatus.has(input);
}
var DiffNameStatus, diffNameStatus, init_diff_name_status = __esm({
  "src/lib/tasks/diff-name-status.ts"() {
    "use strict";
    DiffNameStatus = /* @__PURE__ */ ((DiffNameStatus2) => (DiffNameStatus2.ADDED = "A", DiffNameStatus2.COPIED = "C", DiffNameStatus2.DELETED = "D", DiffNameStatus2.MODIFIED = "M", DiffNameStatus2.RENAMED = "R", DiffNameStatus2.CHANGED = "T", DiffNameStatus2.UNMERGED = "U", DiffNameStatus2.UNKNOWN = "X", DiffNameStatus2.BROKEN = "B", DiffNameStatus2))(DiffNameStatus || {}), diffNameStatus = new Set(Object.values(DiffNameStatus));
  }
});
function grepQueryBuilder(...params) {
  return new GrepQuery().param(...params);
}
function parseGrep(grep) {
  let paths = /* @__PURE__ */ new Set(), results = {};
  return forEachLineWithContent(grep, (input) => {
    let [path3, line, preview] = input.split(NULL);
    paths.add(path3), (results[path3] = results[path3] || []).push({
      line: asNumber(line),
      path: path3,
      preview
    });
  }), {
    paths,
    results
  };
}
function grep_default() {
  return {
    grep(searchTerm) {
      let then = trailingFunctionArgument(arguments), options = getTrailingOptions(arguments);
      for (let option of disallowedOptions)
        if (options.includes(option))
          return this._runTask(
            configurationErrorTask(`git.grep: use of "${option}" is not supported.`),
            then
          );
      typeof searchTerm == "string" && (searchTerm = grepQueryBuilder().param(searchTerm));
      let commands = ["grep", "--null", "-n", "--full-name", ...options, ...searchTerm];
      return this._runTask(
        {
          commands,
          format: "utf-8",
          parser(stdOut) {
            return parseGrep(stdOut);
          }
        },
        then
      );
    }
  };
}
var disallowedOptions, Query, _a, GrepQuery, init_grep = __esm({
  "src/lib/tasks/grep.ts"() {
    "use strict";
    init_utils(), init_task(), disallowedOptions = ["-h"], Query = /* @__PURE__ */ Symbol("grepQuery"), GrepQuery = class {
      constructor() {
        this[_a] = [];
      }
      *[(_a = Query, Symbol.iterator)]() {
        for (let query of this[Query])
          yield query;
      }
      and(...and) {
        return and.length && this[Query].push("--and", "(", ...prefixedArray(and, "-e"), ")"), this;
      }
      param(...param) {
        return this[Query].push(...prefixedArray(param, "-e")), this;
      }
    };
  }
}), reset_exports = {};
__export(reset_exports, {
  ResetMode: () => ResetMode,
  getResetMode: () => getResetMode,
  resetTask: () => resetTask
});
function resetTask(mode, customArgs) {
  let commands = ["reset"];
  return isValidResetMode(mode) && commands.push(`--${mode}`), commands.push(...customArgs), straightThroughStringTask(commands);
}
function getResetMode(mode) {
  if (isValidResetMode(mode))
    return mode;
  switch (typeof mode) {
    case "string":
    case "undefined":
      return "soft";
  }
}
function isValidResetMode(mode) {
  return ResetModes.includes(mode);
}
var ResetMode, ResetModes, init_reset = __esm({
  "src/lib/tasks/reset.ts"() {
    "use strict";
    init_task(), ResetMode = /* @__PURE__ */ ((ResetMode2) => (ResetMode2.MIXED = "mixed", ResetMode2.SOFT = "soft", ResetMode2.HARD = "hard", ResetMode2.MERGE = "merge", ResetMode2.KEEP = "keep", ResetMode2))(ResetMode || {}), ResetModes = Array.from(Object.values(ResetMode));
  }
});
function createLog() {
  return (0, import_debug.default)("simple-git");
}
function prefixedLogger(to, prefix, forward) {
  return !prefix || !String(prefix).replace(/\s*/, "") ? forward ? (message, ...args) => {
    to(message, ...args), forward(message, ...args);
  } : to : (message, ...args) => {
    to(`%s ${message}`, prefix, ...args), forward && forward(message, ...args);
  };
}
function childLoggerName(name, childDebugger, { namespace: parentNamespace }) {
  if (typeof name == "string")
    return name;
  let childNamespace = childDebugger && childDebugger.namespace || "";
  return childNamespace.startsWith(parentNamespace) ? childNamespace.substr(parentNamespace.length + 1) : childNamespace || parentNamespace;
}
function createLogger(label, verbose, initialStep, infoDebugger = createLog()) {
  let labelPrefix = label && `[${label}]` || "", spawned = [], debugDebugger = typeof verbose == "string" ? infoDebugger.extend(verbose) : verbose, key = childLoggerName(filterType(verbose, filterString), debugDebugger, infoDebugger);
  return step(initialStep);
  function sibling(name, initial) {
    return append(
      spawned,
      createLogger(label, key.replace(/^[^:]+/, name), initial, infoDebugger)
    );
  }
  function step(phase) {
    let stepPrefix = phase && `[${phase}]` || "", debug2 = debugDebugger && prefixedLogger(debugDebugger, stepPrefix) || NOOP, info = prefixedLogger(infoDebugger, `${labelPrefix} ${stepPrefix}`, debug2);
    return Object.assign(debugDebugger ? debug2 : info, {
      label,
      sibling,
      info,
      step
    });
  }
}
var init_git_logger = __esm({
  "src/lib/git-logger.ts"() {
    "use strict";
    init_utils(), import_debug.default.formatters.L = (value) => String(filterHasLength(value) ? value.length : "-"), import_debug.default.formatters.B = (value) => Buffer.isBuffer(value) ? value.toString("utf8") : objectToString(value);
  }
}), _TasksPendingQueue, TasksPendingQueue, init_tasks_pending_queue = __esm({
  "src/lib/runners/tasks-pending-queue.ts"() {
    "use strict";
    init_git_error(), init_git_logger(), _TasksPendingQueue = class {
      constructor(logLabel = "GitExecutor") {
        this.logLabel = logLabel, this._queue = /* @__PURE__ */ new Map();
      }
      withProgress(task) {
        return this._queue.get(task);
      }
      createProgress(task) {
        let name = _TasksPendingQueue.getName(task.commands[0]), logger = createLogger(this.logLabel, name);
        return {
          task,
          logger,
          name
        };
      }
      push(task) {
        let progress = this.createProgress(task);
        return progress.logger("Adding task to the queue, commands = %o", task.commands), this._queue.set(task, progress), progress;
      }
      fatal(err) {
        for (let [task, { logger }] of Array.from(this._queue.entries()))
          task === err.task ? (logger.info("Failed %o", err), logger(
            "Fatal exception, any as-yet un-started tasks run through this executor will not be attempted"
          )) : logger.info(
            "A fatal exception occurred in a previous task, the queue has been purged: %o",
            err.message
          ), this.complete(task);
        if (this._queue.size !== 0)
          throw new Error(`Queue size should be zero after fatal: ${this._queue.size}`);
      }
      complete(task) {
        this.withProgress(task) && this._queue.delete(task);
      }
      attempt(task) {
        let progress = this.withProgress(task);
        if (!progress)
          throw new GitError(void 0, "TasksPendingQueue: attempt called for an unknown task");
        return progress.logger("Starting task"), progress;
      }
      static getName(name = "empty") {
        return `task:${name}:${++_TasksPendingQueue.counter}`;
      }
    }, TasksPendingQueue = _TasksPendingQueue, TasksPendingQueue.counter = 0;
  }
});
function pluginContext(task, commands) {
  return {
    method: first(task.commands) || "",
    commands
  };
}
function onErrorReceived(target, logger) {
  return (err) => {
    logger("[ERROR] child process exception %o", err), target.push(Buffer.from(String(err.stack), "ascii"));
  };
}
function onDataReceived(target, name, logger, output) {
  return (buffer) => {
    logger("%s received %L bytes", name, buffer), output("%B", buffer), target.push(buffer);
  };
}
var GitExecutorChain, init_git_executor_chain = __esm({
  "src/lib/runners/git-executor-chain.ts"() {
    "use strict";
    init_git_error(), init_task(), init_utils(), init_tasks_pending_queue(), GitExecutorChain = class {
      constructor(_executor, _scheduler, _plugins) {
        this._executor = _executor, this._scheduler = _scheduler, this._plugins = _plugins, this._chain = Promise.resolve(), this._queue = new TasksPendingQueue();
      }
      get cwd() {
        return this._cwd || this._executor.cwd;
      }
      set cwd(cwd2) {
        this._cwd = cwd2;
      }
      get env() {
        return this._executor.env;
      }
      get outputHandler() {
        return this._executor.outputHandler;
      }
      chain() {
        return this;
      }
      push(task) {
        return this._queue.push(task), this._chain = this._chain.then(() => this.attemptTask(task));
      }
      attemptTask(task) {
        return __async(this, null, function* () {
          let onScheduleComplete = yield this._scheduler.next(), onQueueComplete = () => this._queue.complete(task);
          try {
            let { logger } = this._queue.attempt(task);
            return yield isEmptyTask(task) ? this.attemptEmptyTask(task, logger) : this.attemptRemoteTask(task, logger);
          } catch (e) {
            throw this.onFatalException(task, e);
          } finally {
            onQueueComplete(), onScheduleComplete();
          }
        });
      }
      onFatalException(task, e) {
        let gitError = e instanceof GitError ? Object.assign(e, { task }) : new GitError(task, e && String(e));
        return this._chain = Promise.resolve(), this._queue.fatal(gitError), gitError;
      }
      attemptRemoteTask(task, logger) {
        return __async(this, null, function* () {
          let binary = this._plugins.exec("spawn.binary", "", pluginContext(task, task.commands)), args = this._plugins.exec(
            "spawn.args",
            [...task.commands],
            pluginContext(task, task.commands)
          ), raw = yield this.gitResponse(
            task,
            binary,
            args,
            this.outputHandler,
            logger.step("SPAWN")
          ), outputStreams = yield this.handleTaskData(task, args, raw, logger.step("HANDLE"));
          return logger("passing response to task's parser as a %s", task.format), isBufferTask(task) ? callTaskParser(task.parser, outputStreams) : callTaskParser(task.parser, outputStreams.asStrings());
        });
      }
      attemptEmptyTask(task, logger) {
        return __async(this, null, function* () {
          return logger("empty task bypassing child process to call to task's parser"), task.parser(this);
        });
      }
      handleTaskData(task, args, result, logger) {
        let { exitCode, rejection, stdOut, stdErr } = result;
        return new Promise((done, fail) => {
          logger("Preparing to handle process response exitCode=%d stdOut=", exitCode);
          let { error } = this._plugins.exec(
            "task.error",
            { error: rejection },
            __spreadValues(__spreadValues({}, pluginContext(task, args)), result)
          );
          if (error && task.onError)
            return logger.info("exitCode=%s handling with custom error handler"), task.onError(
              result,
              error,
              (newStdOut) => {
                logger.info("custom error handler treated as success"), logger("custom error returned a %s", objectToString(newStdOut)), done(
                  new GitOutputStreams(
                    Array.isArray(newStdOut) ? Buffer.concat(newStdOut) : newStdOut,
                    Buffer.concat(stdErr)
                  )
                );
              },
              fail
            );
          if (error)
            return logger.info(
              "handling as error: exitCode=%s stdErr=%s rejection=%o",
              exitCode,
              stdErr.length,
              rejection
            ), fail(error);
          logger.info("retrieving task output complete"), done(new GitOutputStreams(Buffer.concat(stdOut), Buffer.concat(stdErr)));
        });
      }
      gitResponse(task, command, args, outputHandler, logger) {
        return __async(this, null, function* () {
          let outputLogger = logger.sibling("output"), spawnOptions = this._plugins.exec(
            "spawn.options",
            {
              cwd: this.cwd,
              env: this.env,
              windowsHide: !0
            },
            pluginContext(task, task.commands)
          );
          return new Promise((done) => {
            let stdOut = [], stdErr = [];
            logger.info("%s %o", command, args), logger("%O", spawnOptions);
            let rejection = this._beforeSpawn(task, args);
            if (rejection)
              return done({
                stdOut,
                stdErr,
                exitCode: 9901,
                rejection
              });
            this._plugins.exec("spawn.before", void 0, __spreadProps(__spreadValues({}, pluginContext(task, args)), {
              kill(reason) {
                rejection = reason || rejection;
              }
            }));
            let spawned = spawn(command, args, spawnOptions);
            spawned.stdout.on(
              "data",
              onDataReceived(stdOut, "stdOut", logger, outputLogger.step("stdOut"))
            ), spawned.stderr.on(
              "data",
              onDataReceived(stdErr, "stdErr", logger, outputLogger.step("stdErr"))
            ), spawned.on("error", onErrorReceived(stdErr, logger)), outputHandler && (logger("Passing child process stdOut/stdErr to custom outputHandler"), outputHandler(command, spawned.stdout, spawned.stderr, [...args])), this._plugins.exec("spawn.after", void 0, __spreadProps(__spreadValues({}, pluginContext(task, args)), {
              spawned,
              close(exitCode, reason) {
                done({
                  stdOut,
                  stdErr,
                  exitCode,
                  rejection: rejection || reason
                });
              },
              kill(reason) {
                spawned.killed || (rejection = reason, spawned.kill("SIGINT"));
              }
            }));
          });
        });
      }
      _beforeSpawn(task, args) {
        let rejection;
        return this._plugins.exec("spawn.before", void 0, __spreadProps(__spreadValues({}, pluginContext(task, args)), {
          kill(reason) {
            rejection = reason || rejection;
          }
        })), rejection;
      }
    };
  }
}), git_executor_exports = {};
__export(git_executor_exports, {
  GitExecutor: () => GitExecutor
});
var GitExecutor, init_git_executor = __esm({
  "src/lib/runners/git-executor.ts"() {
    "use strict";
    init_git_executor_chain(), GitExecutor = class {
      constructor(cwd2, _scheduler, _plugins) {
        this.cwd = cwd2, this._scheduler = _scheduler, this._plugins = _plugins, this._chain = new GitExecutorChain(this, this._scheduler, this._plugins);
      }
      chain() {
        return new GitExecutorChain(this, this._scheduler, this._plugins);
      }
      push(task) {
        return this._chain.push(task);
      }
    };
  }
});
function taskCallback(task, response, callback = NOOP) {
  let onSuccess = (data) => {
    callback(null, data);
  }, onError2 = (err) => {
    err?.task === task && callback(
      err instanceof GitResponseError ? addDeprecationNoticeToError(err) : err,
      void 0
    );
  };
  response.then(onSuccess, onError2);
}
function addDeprecationNoticeToError(err) {
  let log = (name) => {
    console.warn(
      `simple-git deprecation notice: accessing GitResponseError.${name} should be GitResponseError.git.${name}, this will no longer be available in version 3`
    ), log = NOOP;
  };
  return Object.create(err, Object.getOwnPropertyNames(err.git).reduce(descriptorReducer, {}));
  function descriptorReducer(all, name) {
    return name in err || (all[name] = {
      enumerable: !1,
      configurable: !1,
      get() {
        return log(name), err.git[name];
      }
    }), all;
  }
}
var init_task_callback = __esm({
  "src/lib/task-callback.ts"() {
    "use strict";
    init_git_response_error(), init_utils();
  }
});
function changeWorkingDirectoryTask(directory, root) {
  return adhocExecTask((instance) => {
    if (!folderExists(directory))
      throw new Error(`Git.cwd: cannot change to non-directory "${directory}"`);
    return (root || instance).cwd = directory;
  });
}
var init_change_working_directory = __esm({
  "src/lib/tasks/change-working-directory.ts"() {
    "use strict";
    init_utils(), init_task();
  }
});
function checkoutTask(args) {
  let commands = ["checkout", ...args];
  return commands[1] === "-b" && commands.includes("-B") && (commands[1] = remove(commands, "-B")), straightThroughStringTask(commands);
}
function checkout_default() {
  return {
    checkout() {
      return this._runTask(
        checkoutTask(getTrailingOptions(arguments, 1)),
        trailingFunctionArgument(arguments)
      );
    },
    checkoutBranch(branchName, startPoint) {
      return this._runTask(
        checkoutTask(["-b", branchName, startPoint, ...getTrailingOptions(arguments)]),
        trailingFunctionArgument(arguments)
      );
    },
    checkoutLocalBranch(branchName) {
      return this._runTask(
        checkoutTask(["-b", branchName, ...getTrailingOptions(arguments)]),
        trailingFunctionArgument(arguments)
      );
    }
  };
}
var init_checkout = __esm({
  "src/lib/tasks/checkout.ts"() {
    "use strict";
    init_utils(), init_task();
  }
});
function countObjectsResponse() {
  return {
    count: 0,
    garbage: 0,
    inPack: 0,
    packs: 0,
    prunePackable: 0,
    size: 0,
    sizeGarbage: 0,
    sizePack: 0
  };
}
function count_objects_default() {
  return {
    countObjects() {
      return this._runTask({
        commands: ["count-objects", "--verbose"],
        format: "utf-8",
        parser(stdOut) {
          return parseStringResponse(countObjectsResponse(), [parser2], stdOut);
        }
      });
    }
  };
}
var parser2, init_count_objects = __esm({
  "src/lib/tasks/count-objects.ts"() {
    "use strict";
    init_utils(), parser2 = new LineParser(
      /([a-z-]+): (\d+)$/,
      (result, [key, value]) => {
        let property = asCamelCase(key);
        result.hasOwnProperty(property) && (result[property] = asNumber(value));
      }
    );
  }
});
function parseCommitResult(stdOut) {
  return parseStringResponse({
    author: null,
    branch: "",
    commit: "",
    root: !1,
    summary: {
      changes: 0,
      insertions: 0,
      deletions: 0
    }
  }, parsers, stdOut);
}
var parsers, init_parse_commit = __esm({
  "src/lib/parsers/parse-commit.ts"() {
    "use strict";
    init_utils(), parsers = [
      new LineParser(/^\[([^\s]+)( \([^)]+\))? ([^\]]+)/, (result, [branch, root, commit]) => {
        result.branch = branch, result.commit = commit, result.root = !!root;
      }),
      new LineParser(/\s*Author:\s(.+)/i, (result, [author]) => {
        let parts = author.split("<"), email = parts.pop();
        !email || !email.includes("@") || (result.author = {
          email: email.substr(0, email.length - 1),
          name: parts.join("<").trim()
        });
      }),
      new LineParser(
        /(\d+)[^,]*(?:,\s*(\d+)[^,]*)(?:,\s*(\d+))/g,
        (result, [changes, insertions, deletions]) => {
          result.summary.changes = parseInt(changes, 10) || 0, result.summary.insertions = parseInt(insertions, 10) || 0, result.summary.deletions = parseInt(deletions, 10) || 0;
        }
      ),
      new LineParser(
        /^(\d+)[^,]*(?:,\s*(\d+)[^(]+\(([+-]))?/,
        (result, [changes, lines, direction]) => {
          result.summary.changes = parseInt(changes, 10) || 0;
          let count = parseInt(lines, 10) || 0;
          direction === "-" ? result.summary.deletions = count : direction === "+" && (result.summary.insertions = count);
        }
      )
    ];
  }
});
function commitTask(message, files, customArgs) {
  return {
    commands: [
      "-c",
      "core.abbrev=40",
      "commit",
      ...prefixedArray(message, "-m"),
      ...files,
      ...customArgs
    ],
    format: "utf-8",
    parser: parseCommitResult
  };
}
function commit_default() {
  return {
    commit(message, ...rest) {
      let next = trailingFunctionArgument(arguments), task = rejectDeprecatedSignatures(message) || commitTask(
        asArray(message),
        asArray(filterType(rest[0], filterStringOrStringArray, [])),
        [...filterType(rest[1], filterArray, []), ...getTrailingOptions(arguments, 0, !0)]
      );
      return this._runTask(task, next);
    }
  };
  function rejectDeprecatedSignatures(message) {
    return !filterStringOrStringArray(message) && configurationErrorTask(
      "git.commit: requires the commit message to be supplied as a string/string[]"
    );
  }
}
var init_commit = __esm({
  "src/lib/tasks/commit.ts"() {
    "use strict";
    init_parse_commit(), init_utils(), init_task();
  }
});
function first_commit_default() {
  return {
    firstCommit() {
      return this._runTask(
        straightThroughStringTask(["rev-list", "--max-parents=0", "HEAD"], !0),
        trailingFunctionArgument(arguments)
      );
    }
  };
}
var init_first_commit = __esm({
  "src/lib/tasks/first-commit.ts"() {
    "use strict";
    init_utils(), init_task();
  }
});
function hashObjectTask(filePath, write) {
  let commands = ["hash-object", filePath];
  return write && commands.push("-w"), straightThroughStringTask(commands, !0);
}
var init_hash_object = __esm({
  "src/lib/tasks/hash-object.ts"() {
    "use strict";
    init_task();
  }
});
function parseInit(bare, path3, text) {
  let response = String(text).trim(), result;
  if (result = initResponseRegex.exec(response))
    return new InitSummary(bare, path3, !1, result[1]);
  if (result = reInitResponseRegex.exec(response))
    return new InitSummary(bare, path3, !0, result[1]);
  let gitDir = "", tokens = response.split(" ");
  for (; tokens.length; )
    if (tokens.shift() === "in") {
      gitDir = tokens.join(" ");
      break;
    }
  return new InitSummary(bare, path3, /^re/i.test(response), gitDir);
}
var InitSummary, initResponseRegex, reInitResponseRegex, init_InitSummary = __esm({
  "src/lib/responses/InitSummary.ts"() {
    "use strict";
    InitSummary = class {
      constructor(bare, path3, existing, gitDir) {
        this.bare = bare, this.path = path3, this.existing = existing, this.gitDir = gitDir;
      }
    }, initResponseRegex = /^Init.+ repository in (.+)$/, reInitResponseRegex = /^Rein.+ in (.+)$/;
  }
});
function hasBareCommand(command) {
  return command.includes(bareCommand);
}
function initTask(bare = !1, path3, customArgs) {
  let commands = ["init", ...customArgs];
  return bare && !hasBareCommand(commands) && commands.splice(1, 0, bareCommand), {
    commands,
    format: "utf-8",
    parser(text) {
      return parseInit(commands.includes("--bare"), path3, text);
    }
  };
}
var bareCommand, init_init = __esm({
  "src/lib/tasks/init.ts"() {
    "use strict";
    init_InitSummary(), bareCommand = "--bare";
  }
});
function logFormatFromCommand(customArgs) {
  for (let i = 0; i < customArgs.length; i++) {
    let format = logFormatRegex.exec(customArgs[i]);
    if (format)
      return `--${format[1]}`;
  }
  return "";
}
function isLogFormat(customArg) {
  return logFormatRegex.test(customArg);
}
var logFormatRegex, init_log_format = __esm({
  "src/lib/args/log-format.ts"() {
    "use strict";
    logFormatRegex = /^--(stat|numstat|name-only|name-status)(=|$)/;
  }
}), DiffSummary, init_DiffSummary = __esm({
  "src/lib/responses/DiffSummary.ts"() {
    "use strict";
    DiffSummary = class {
      constructor() {
        this.changed = 0, this.deletions = 0, this.insertions = 0, this.files = [];
      }
    };
  }
});
function getDiffParser(format = "") {
  let parser4 = diffSummaryParsers[format];
  return (stdOut) => parseStringResponse(new DiffSummary(), parser4, stdOut, !1);
}
var statParser, numStatParser, nameOnlyParser, nameStatusParser, diffSummaryParsers, init_parse_diff_summary = __esm({
  "src/lib/parsers/parse-diff-summary.ts"() {
    "use strict";
    init_log_format(), init_DiffSummary(), init_diff_name_status(), init_utils(), statParser = [
      new LineParser(
        /^(.+)\s+\|\s+(\d+)(\s+[+\-]+)?$/,
        (result, [file, changes, alterations = ""]) => {
          result.files.push({
            file: file.trim(),
            changes: asNumber(changes),
            insertions: alterations.replace(/[^+]/g, "").length,
            deletions: alterations.replace(/[^-]/g, "").length,
            binary: !1
          });
        }
      ),
      new LineParser(
        /^(.+) \|\s+Bin ([0-9.]+) -> ([0-9.]+) ([a-z]+)/,
        (result, [file, before, after]) => {
          result.files.push({
            file: file.trim(),
            before: asNumber(before),
            after: asNumber(after),
            binary: !0
          });
        }
      ),
      new LineParser(
        /(\d+) files? changed\s*((?:, \d+ [^,]+){0,2})/,
        (result, [changed, summary]) => {
          let inserted = /(\d+) i/.exec(summary), deleted = /(\d+) d/.exec(summary);
          result.changed = asNumber(changed), result.insertions = asNumber(inserted?.[1]), result.deletions = asNumber(deleted?.[1]);
        }
      )
    ], numStatParser = [
      new LineParser(
        /(\d+)\t(\d+)\t(.+)$/,
        (result, [changesInsert, changesDelete, file]) => {
          let insertions = asNumber(changesInsert), deletions = asNumber(changesDelete);
          result.changed++, result.insertions += insertions, result.deletions += deletions, result.files.push({
            file,
            changes: insertions + deletions,
            insertions,
            deletions,
            binary: !1
          });
        }
      ),
      new LineParser(/-\t-\t(.+)$/, (result, [file]) => {
        result.changed++, result.files.push({
          file,
          after: 0,
          before: 0,
          binary: !0
        });
      })
    ], nameOnlyParser = [
      new LineParser(/(.+)$/, (result, [file]) => {
        result.changed++, result.files.push({
          file,
          changes: 0,
          insertions: 0,
          deletions: 0,
          binary: !1
        });
      })
    ], nameStatusParser = [
      new LineParser(
        /([ACDMRTUXB])([0-9]{0,3})\t(.[^\t]*)(\t(.[^\t]*))?$/,
        (result, [status, similarity, from, _to, to]) => {
          result.changed++, result.files.push({
            file: to ?? from,
            changes: 0,
            insertions: 0,
            deletions: 0,
            binary: !1,
            status: orVoid(isDiffNameStatus(status) && status),
            from: orVoid(!!to && from !== to && from),
            similarity: asNumber(similarity)
          });
        }
      )
    ], diffSummaryParsers = {
      "": statParser,
      "--stat": statParser,
      "--numstat": numStatParser,
      "--name-status": nameStatusParser,
      "--name-only": nameOnlyParser
    };
  }
});
function lineBuilder(tokens, fields) {
  return fields.reduce(
    (line, field, index) => (line[field] = tokens[index] || "", line),
    /* @__PURE__ */ Object.create({ diff: null })
  );
}
function createListLogSummaryParser(splitter = SPLITTER, fields = defaultFieldNames, logFormat = "") {
  let parseDiffResult = getDiffParser(logFormat);
  return function(stdOut) {
    let all = toLinesWithContent(
      stdOut.trim(),
      !1,
      START_BOUNDARY
    ).map(function(item) {
      let lineDetail = item.split(COMMIT_BOUNDARY), listLogLine = lineBuilder(lineDetail[0].split(splitter), fields);
      return lineDetail.length > 1 && lineDetail[1].trim() && (listLogLine.diff = parseDiffResult(lineDetail[1])), listLogLine;
    });
    return {
      all,
      latest: all.length && all[0] || null,
      total: all.length
    };
  };
}
var START_BOUNDARY, COMMIT_BOUNDARY, SPLITTER, defaultFieldNames, init_parse_list_log_summary = __esm({
  "src/lib/parsers/parse-list-log-summary.ts"() {
    "use strict";
    init_utils(), init_parse_diff_summary(), init_log_format(), START_BOUNDARY = "\xF2\xF2\xF2\xF2\xF2\xF2 ", COMMIT_BOUNDARY = " \xF2\xF2", SPLITTER = " \xF2 ", defaultFieldNames = ["hash", "date", "message", "refs", "author_name", "author_email"];
  }
}), diff_exports = {};
__export(diff_exports, {
  diffSummaryTask: () => diffSummaryTask,
  validateLogFormatConfig: () => validateLogFormatConfig
});
function diffSummaryTask(customArgs) {
  let logFormat = logFormatFromCommand(customArgs), commands = ["diff"];
  return logFormat === "" && (logFormat = "--stat", commands.push("--stat=4096")), commands.push(...customArgs), validateLogFormatConfig(commands) || {
    commands,
    format: "utf-8",
    parser: getDiffParser(logFormat)
  };
}
function validateLogFormatConfig(customArgs) {
  let flags = customArgs.filter(isLogFormat);
  if (flags.length > 1)
    return configurationErrorTask(
      `Summary flags are mutually exclusive - pick one of ${flags.join(",")}`
    );
  if (flags.length && customArgs.includes("-z"))
    return configurationErrorTask(
      `Summary flag ${flags} parsing is not compatible with null termination option '-z'`
    );
}
var init_diff = __esm({
  "src/lib/tasks/diff.ts"() {
    "use strict";
    init_log_format(), init_parse_diff_summary(), init_task();
  }
});
function prettyFormat(format, splitter) {
  let fields = [], formatStr = [];
  return Object.keys(format).forEach((field) => {
    fields.push(field), formatStr.push(String(format[field]));
  }), [fields, formatStr.join(splitter)];
}
function userOptions(input) {
  return Object.keys(input).reduce((out, key) => (key in excludeOptions || (out[key] = input[key]), out), {});
}
function parseLogOptions(opt = {}, customArgs = []) {
  let splitter = filterType(opt.splitter, filterString, SPLITTER), format = !filterPrimitives(opt.format) && opt.format ? opt.format : {
    hash: "%H",
    date: opt.strictDate === !1 ? "%ai" : "%aI",
    message: "%s",
    refs: "%D",
    body: opt.multiLine ? "%B" : "%b",
    author_name: opt.mailMap !== !1 ? "%aN" : "%an",
    author_email: opt.mailMap !== !1 ? "%aE" : "%ae"
  }, [fields, formatStr] = prettyFormat(format, splitter), suffix = [], command = [
    `--pretty=format:${START_BOUNDARY}${formatStr}${COMMIT_BOUNDARY}`,
    ...customArgs
  ], maxCount = opt.n || opt["max-count"] || opt.maxCount;
  if (maxCount && command.push(`--max-count=${maxCount}`), opt.from || opt.to) {
    let rangeOperator = opt.symmetric !== !1 ? "..." : "..";
    suffix.push(`${opt.from || ""}${rangeOperator}${opt.to || ""}`);
  }
  return filterString(opt.file) && command.push("--follow", pathspec(opt.file)), appendTaskOptions(userOptions(opt), command), {
    fields,
    splitter,
    commands: [...command, ...suffix]
  };
}
function logTask(splitter, fields, customArgs) {
  let parser4 = createListLogSummaryParser(splitter, fields, logFormatFromCommand(customArgs));
  return {
    commands: ["log", ...customArgs],
    format: "utf-8",
    parser: parser4
  };
}
function log_default() {
  return {
    log(...rest) {
      let next = trailingFunctionArgument(arguments), options = parseLogOptions(
        trailingOptionsArgument(arguments),
        filterType(arguments[0], filterArray)
      ), task = rejectDeprecatedSignatures(...rest) || validateLogFormatConfig(options.commands) || createLogTask(options);
      return this._runTask(task, next);
    }
  };
  function createLogTask(options) {
    return logTask(options.splitter, options.fields, options.commands);
  }
  function rejectDeprecatedSignatures(from, to) {
    return filterString(from) && filterString(to) && configurationErrorTask(
      "git.log(string, string) should be replaced with git.log({ from: string, to: string })"
    );
  }
}
var excludeOptions, init_log = __esm({
  "src/lib/tasks/log.ts"() {
    "use strict";
    init_log_format(), init_pathspec(), init_parse_list_log_summary(), init_utils(), init_task(), init_diff(), excludeOptions = /* @__PURE__ */ ((excludeOptions2) => (excludeOptions2[excludeOptions2["--pretty"] = 0] = "--pretty", excludeOptions2[excludeOptions2["max-count"] = 1] = "max-count", excludeOptions2[excludeOptions2.maxCount = 2] = "maxCount", excludeOptions2[excludeOptions2.n = 3] = "n", excludeOptions2[excludeOptions2.file = 4] = "file", excludeOptions2[excludeOptions2.format = 5] = "format", excludeOptions2[excludeOptions2.from = 6] = "from", excludeOptions2[excludeOptions2.to = 7] = "to", excludeOptions2[excludeOptions2.splitter = 8] = "splitter", excludeOptions2[excludeOptions2.symmetric = 9] = "symmetric", excludeOptions2[excludeOptions2.mailMap = 10] = "mailMap", excludeOptions2[excludeOptions2.multiLine = 11] = "multiLine", excludeOptions2[excludeOptions2.strictDate = 12] = "strictDate", excludeOptions2))(excludeOptions || {});
  }
}), MergeSummaryConflict, MergeSummaryDetail, init_MergeSummary = __esm({
  "src/lib/responses/MergeSummary.ts"() {
    "use strict";
    MergeSummaryConflict = class {
      constructor(reason, file = null, meta) {
        this.reason = reason, this.file = file, this.meta = meta;
      }
      toString() {
        return `${this.file}:${this.reason}`;
      }
    }, MergeSummaryDetail = class {
      constructor() {
        this.conflicts = [], this.merges = [], this.result = "success";
      }
      get failed() {
        return this.conflicts.length > 0;
      }
      get reason() {
        return this.result;
      }
      toString() {
        return this.conflicts.length ? `CONFLICTS: ${this.conflicts.join(", ")}` : "OK";
      }
    };
  }
}), PullSummary, PullFailedSummary, init_PullSummary = __esm({
  "src/lib/responses/PullSummary.ts"() {
    "use strict";
    PullSummary = class {
      constructor() {
        this.remoteMessages = {
          all: []
        }, this.created = [], this.deleted = [], this.files = [], this.deletions = {}, this.insertions = {}, this.summary = {
          changes: 0,
          deletions: 0,
          insertions: 0
        };
      }
    }, PullFailedSummary = class {
      constructor() {
        this.remote = "", this.hash = {
          local: "",
          remote: ""
        }, this.branch = {
          local: "",
          remote: ""
        }, this.message = "";
      }
      toString() {
        return this.message;
      }
    };
  }
});
function objectEnumerationResult(remoteMessages) {
  return remoteMessages.objects = remoteMessages.objects || {
    compressing: 0,
    counting: 0,
    enumerating: 0,
    packReused: 0,
    reused: { count: 0, delta: 0 },
    total: { count: 0, delta: 0 }
  };
}
function asObjectCount(source) {
  let count = /^\s*(\d+)/.exec(source), delta = /delta (\d+)/i.exec(source);
  return {
    count: asNumber(count && count[1] || "0"),
    delta: asNumber(delta && delta[1] || "0")
  };
}
var remoteMessagesObjectParsers, init_parse_remote_objects = __esm({
  "src/lib/parsers/parse-remote-objects.ts"() {
    "use strict";
    init_utils(), remoteMessagesObjectParsers = [
      new RemoteLineParser(
        /^remote:\s*(enumerating|counting|compressing) objects: (\d+),/i,
        (result, [action, count]) => {
          let key = action.toLowerCase(), enumeration = objectEnumerationResult(result.remoteMessages);
          Object.assign(enumeration, { [key]: asNumber(count) });
        }
      ),
      new RemoteLineParser(
        /^remote:\s*(enumerating|counting|compressing) objects: \d+% \(\d+\/(\d+)\),/i,
        (result, [action, count]) => {
          let key = action.toLowerCase(), enumeration = objectEnumerationResult(result.remoteMessages);
          Object.assign(enumeration, { [key]: asNumber(count) });
        }
      ),
      new RemoteLineParser(
        /total ([^,]+), reused ([^,]+), pack-reused (\d+)/i,
        (result, [total, reused, packReused]) => {
          let objects = objectEnumerationResult(result.remoteMessages);
          objects.total = asObjectCount(total), objects.reused = asObjectCount(reused), objects.packReused = asNumber(packReused);
        }
      )
    ];
  }
});
function parseRemoteMessages(_stdOut, stdErr) {
  return parseStringResponse({ remoteMessages: new RemoteMessageSummary() }, parsers2, stdErr);
}
var parsers2, RemoteMessageSummary, init_parse_remote_messages = __esm({
  "src/lib/parsers/parse-remote-messages.ts"() {
    "use strict";
    init_utils(), init_parse_remote_objects(), parsers2 = [
      new RemoteLineParser(/^remote:\s*(.+)$/, (result, [text]) => (result.remoteMessages.all.push(text.trim()), !1)),
      ...remoteMessagesObjectParsers,
      new RemoteLineParser(
        [/create a (?:pull|merge) request/i, /\s(https?:\/\/\S+)$/],
        (result, [pullRequestUrl]) => {
          result.remoteMessages.pullRequestUrl = pullRequestUrl;
        }
      ),
      new RemoteLineParser(
        [/found (\d+) vulnerabilities.+\(([^)]+)\)/i, /\s(https?:\/\/\S+)$/],
        (result, [count, summary, url]) => {
          result.remoteMessages.vulnerabilities = {
            count: asNumber(count),
            summary,
            url
          };
        }
      )
    ], RemoteMessageSummary = class {
      constructor() {
        this.all = [];
      }
    };
  }
});
function parsePullErrorResult(stdOut, stdErr) {
  let pullError = parseStringResponse(new PullFailedSummary(), errorParsers, [stdOut, stdErr]);
  return pullError.message && pullError;
}
var FILE_UPDATE_REGEX, SUMMARY_REGEX, ACTION_REGEX, parsers3, errorParsers, parsePullDetail, parsePullResult, init_parse_pull = __esm({
  "src/lib/parsers/parse-pull.ts"() {
    "use strict";
    init_PullSummary(), init_utils(), init_parse_remote_messages(), FILE_UPDATE_REGEX = /^\s*(.+?)\s+\|\s+\d+\s*(\+*)(-*)/, SUMMARY_REGEX = /(\d+)\D+((\d+)\D+\(\+\))?(\D+(\d+)\D+\(-\))?/, ACTION_REGEX = /^(create|delete) mode \d+ (.+)/, parsers3 = [
      new LineParser(FILE_UPDATE_REGEX, (result, [file, insertions, deletions]) => {
        result.files.push(file), insertions && (result.insertions[file] = insertions.length), deletions && (result.deletions[file] = deletions.length);
      }),
      new LineParser(SUMMARY_REGEX, (result, [changes, , insertions, , deletions]) => insertions !== void 0 || deletions !== void 0 ? (result.summary.changes = +changes || 0, result.summary.insertions = +insertions || 0, result.summary.deletions = +deletions || 0, !0) : !1),
      new LineParser(ACTION_REGEX, (result, [action, file]) => {
        append(result.files, file), append(action === "create" ? result.created : result.deleted, file);
      })
    ], errorParsers = [
      new LineParser(/^from\s(.+)$/i, (result, [remote]) => {
        result.remote = remote;
      }),
      new LineParser(/^fatal:\s(.+)$/, (result, [message]) => {
        result.message = message;
      }),
      new LineParser(
        /([a-z0-9]+)\.\.([a-z0-9]+)\s+(\S+)\s+->\s+(\S+)$/,
        (result, [hashLocal, hashRemote, branchLocal, branchRemote]) => {
          result.branch.local = branchLocal, result.hash.local = hashLocal, result.branch.remote = branchRemote, result.hash.remote = hashRemote;
        }
      )
    ], parsePullDetail = (stdOut, stdErr) => parseStringResponse(new PullSummary(), parsers3, [stdOut, stdErr]), parsePullResult = (stdOut, stdErr) => Object.assign(
      new PullSummary(),
      parsePullDetail(stdOut, stdErr),
      parseRemoteMessages(stdOut, stdErr)
    );
  }
}), parsers4, parseMergeResult, parseMergeDetail, init_parse_merge = __esm({
  "src/lib/parsers/parse-merge.ts"() {
    "use strict";
    init_MergeSummary(), init_utils(), init_parse_pull(), parsers4 = [
      new LineParser(/^Auto-merging\s+(.+)$/, (summary, [autoMerge]) => {
        summary.merges.push(autoMerge);
      }),
      new LineParser(/^CONFLICT\s+\((.+)\): Merge conflict in (.+)$/, (summary, [reason, file]) => {
        summary.conflicts.push(new MergeSummaryConflict(reason, file));
      }),
      new LineParser(
        /^CONFLICT\s+\((.+\/delete)\): (.+) deleted in (.+) and/,
        (summary, [reason, file, deleteRef]) => {
          summary.conflicts.push(new MergeSummaryConflict(reason, file, { deleteRef }));
        }
      ),
      new LineParser(/^CONFLICT\s+\((.+)\):/, (summary, [reason]) => {
        summary.conflicts.push(new MergeSummaryConflict(reason, null));
      }),
      new LineParser(/^Automatic merge failed;\s+(.+)$/, (summary, [result]) => {
        summary.result = result;
      })
    ], parseMergeResult = (stdOut, stdErr) => Object.assign(parseMergeDetail(stdOut, stdErr), parsePullResult(stdOut, stdErr)), parseMergeDetail = (stdOut) => parseStringResponse(new MergeSummaryDetail(), parsers4, stdOut);
  }
});
function mergeTask(customArgs) {
  return customArgs.length ? {
    commands: ["merge", ...customArgs],
    format: "utf-8",
    parser(stdOut, stdErr) {
      let merge = parseMergeResult(stdOut, stdErr);
      if (merge.failed)
        throw new GitResponseError(merge);
      return merge;
    }
  } : configurationErrorTask("Git.merge requires at least one option");
}
var init_merge = __esm({
  "src/lib/tasks/merge.ts"() {
    "use strict";
    init_git_response_error(), init_parse_merge(), init_task();
  }
});
function pushResultPushedItem(local, remote, status) {
  let deleted = status.includes("deleted"), tag = status.includes("tag") || /^refs\/tags/.test(local), alreadyUpdated = !status.includes("new");
  return {
    deleted,
    tag,
    branch: !tag,
    new: !alreadyUpdated,
    alreadyUpdated,
    local,
    remote
  };
}
var parsers5, parsePushResult, parsePushDetail, init_parse_push = __esm({
  "src/lib/parsers/parse-push.ts"() {
    "use strict";
    init_utils(), init_parse_remote_messages(), parsers5 = [
      new LineParser(/^Pushing to (.+)$/, (result, [repo]) => {
        result.repo = repo;
      }),
      new LineParser(/^updating local tracking ref '(.+)'/, (result, [local]) => {
        result.ref = __spreadProps(__spreadValues({}, result.ref || {}), {
          local
        });
      }),
      new LineParser(/^[=*-]\s+([^:]+):(\S+)\s+\[(.+)]$/, (result, [local, remote, type]) => {
        result.pushed.push(pushResultPushedItem(local, remote, type));
      }),
      new LineParser(
        /^Branch '([^']+)' set up to track remote branch '([^']+)' from '([^']+)'/,
        (result, [local, remote, remoteName]) => {
          result.branch = __spreadProps(__spreadValues({}, result.branch || {}), {
            local,
            remote,
            remoteName
          });
        }
      ),
      new LineParser(
        /^([^:]+):(\S+)\s+([a-z0-9]+)\.\.([a-z0-9]+)$/,
        (result, [local, remote, from, to]) => {
          result.update = {
            head: {
              local,
              remote
            },
            hash: {
              from,
              to
            }
          };
        }
      )
    ], parsePushResult = (stdOut, stdErr) => {
      let pushDetail = parsePushDetail(stdOut, stdErr), responseDetail = parseRemoteMessages(stdOut, stdErr);
      return __spreadValues(__spreadValues({}, pushDetail), responseDetail);
    }, parsePushDetail = (stdOut, stdErr) => parseStringResponse({ pushed: [] }, parsers5, [stdOut, stdErr]);
  }
}), push_exports = {};
__export(push_exports, {
  pushTagsTask: () => pushTagsTask,
  pushTask: () => pushTask
});
function pushTagsTask(ref = {}, customArgs) {
  return append(customArgs, "--tags"), pushTask(ref, customArgs);
}
function pushTask(ref = {}, customArgs) {
  let commands = ["push", ...customArgs];
  return ref.branch && commands.splice(1, 0, ref.branch), ref.remote && commands.splice(1, 0, ref.remote), remove(commands, "-v"), append(commands, "--verbose"), append(commands, "--porcelain"), {
    commands,
    format: "utf-8",
    parser: parsePushResult
  };
}
var init_push = __esm({
  "src/lib/tasks/push.ts"() {
    "use strict";
    init_parse_push(), init_utils();
  }
});
function show_default() {
  return {
    showBuffer() {
      let commands = ["show", ...getTrailingOptions(arguments, 1)];
      return commands.includes("--binary") || commands.splice(1, 0, "--binary"), this._runTask(
        straightThroughBufferTask(commands),
        trailingFunctionArgument(arguments)
      );
    },
    show() {
      let commands = ["show", ...getTrailingOptions(arguments, 1)];
      return this._runTask(
        straightThroughStringTask(commands),
        trailingFunctionArgument(arguments)
      );
    }
  };
}
var init_show = __esm({
  "src/lib/tasks/show.ts"() {
    "use strict";
    init_utils(), init_task();
  }
}), fromPathRegex, FileStatusSummary, init_FileStatusSummary = __esm({
  "src/lib/responses/FileStatusSummary.ts"() {
    "use strict";
    fromPathRegex = /^(.+)\0(.+)$/, FileStatusSummary = class {
      constructor(path3, index, working_dir) {
        if (this.path = path3, this.index = index, this.working_dir = working_dir, index === "R" || working_dir === "R") {
          let detail = fromPathRegex.exec(path3) || [null, path3, path3];
          this.from = detail[2] || "", this.path = detail[1] || "";
        }
      }
    };
  }
});
function renamedFile(line) {
  let [to, from] = line.split(NULL);
  return {
    from: from || to,
    to
  };
}
function parser3(indexX, indexY, handler) {
  return [`${indexX}${indexY}`, handler];
}
function conflicts(indexX, ...indexY) {
  return indexY.map((y) => parser3(indexX, y, (result, file) => append(result.conflicted, file)));
}
function splitLine(result, lineStr) {
  let trimmed2 = lineStr.trim();
  switch (" ") {
    case trimmed2.charAt(2):
      return data(trimmed2.charAt(0), trimmed2.charAt(1), trimmed2.substr(3));
    case trimmed2.charAt(1):
      return data(" ", trimmed2.charAt(0), trimmed2.substr(2));
    default:
      return;
  }
  function data(index, workingDir, path3) {
    let raw = `${index}${workingDir}`, handler = parsers6.get(raw);
    handler && handler(result, path3), raw !== "##" && raw !== "!!" && result.files.push(new FileStatusSummary(path3, index, workingDir));
  }
}
var StatusSummary, parsers6, parseStatusSummary, init_StatusSummary = __esm({
  "src/lib/responses/StatusSummary.ts"() {
    "use strict";
    init_utils(), init_FileStatusSummary(), StatusSummary = class {
      constructor() {
        this.not_added = [], this.conflicted = [], this.created = [], this.deleted = [], this.ignored = void 0, this.modified = [], this.renamed = [], this.files = [], this.staged = [], this.ahead = 0, this.behind = 0, this.current = null, this.tracking = null, this.detached = !1, this.isClean = () => !this.files.length;
      }
    }, parsers6 = new Map([
      parser3(
        " ",
        "A",
        (result, file) => append(result.created, file)
      ),
      parser3(
        " ",
        "D",
        (result, file) => append(result.deleted, file)
      ),
      parser3(
        " ",
        "M",
        (result, file) => append(result.modified, file)
      ),
      parser3(
        "A",
        " ",
        (result, file) => append(result.created, file) && append(result.staged, file)
      ),
      parser3(
        "A",
        "M",
        (result, file) => append(result.created, file) && append(result.staged, file) && append(result.modified, file)
      ),
      parser3(
        "D",
        " ",
        (result, file) => append(result.deleted, file) && append(result.staged, file)
      ),
      parser3(
        "M",
        " ",
        (result, file) => append(result.modified, file) && append(result.staged, file)
      ),
      parser3(
        "M",
        "M",
        (result, file) => append(result.modified, file) && append(result.staged, file)
      ),
      parser3("R", " ", (result, file) => {
        append(result.renamed, renamedFile(file));
      }),
      parser3("R", "M", (result, file) => {
        let renamed = renamedFile(file);
        append(result.renamed, renamed), append(result.modified, renamed.to);
      }),
      parser3("!", "!", (_result, _file) => {
        append(_result.ignored = _result.ignored || [], _file);
      }),
      parser3(
        "?",
        "?",
        (result, file) => append(result.not_added, file)
      ),
      ...conflicts(
        "A",
        "A",
        "U"
        /* UNMERGED */
      ),
      ...conflicts(
        "D",
        "D",
        "U"
        /* UNMERGED */
      ),
      ...conflicts(
        "U",
        "A",
        "D",
        "U"
        /* UNMERGED */
      ),
      [
        "##",
        (result, line) => {
          let aheadReg = /ahead (\d+)/, behindReg = /behind (\d+)/, currentReg = /^(.+?(?=(?:\.{3}|\s|$)))/, trackingReg = /\.{3}(\S*)/, onEmptyBranchReg = /\son\s([\S]+)$/, regexResult;
          regexResult = aheadReg.exec(line), result.ahead = regexResult && +regexResult[1] || 0, regexResult = behindReg.exec(line), result.behind = regexResult && +regexResult[1] || 0, regexResult = currentReg.exec(line), result.current = regexResult && regexResult[1], regexResult = trackingReg.exec(line), result.tracking = regexResult && regexResult[1], regexResult = onEmptyBranchReg.exec(line), result.current = regexResult && regexResult[1] || result.current, result.detached = /\(no branch\)/.test(line);
        }
      ]
    ]), parseStatusSummary = function(text) {
      let lines = text.split(NULL), status = new StatusSummary();
      for (let i = 0, l = lines.length; i < l; ) {
        let line = lines[i++].trim();
        line && (line.charAt(0) === "R" && (line += NULL + (lines[i++] || "")), splitLine(status, line));
      }
      return status;
    };
  }
});
function statusTask(customArgs) {
  return {
    format: "utf-8",
    commands: [
      "status",
      "--porcelain",
      "-b",
      "-u",
      "--null",
      ...customArgs.filter((arg) => !ignoredOptions.includes(arg))
    ],
    parser(text) {
      return parseStatusSummary(text);
    }
  };
}
var ignoredOptions, init_status = __esm({
  "src/lib/tasks/status.ts"() {
    "use strict";
    init_StatusSummary(), ignoredOptions = ["--null", "-z"];
  }
});
function versionResponse(major = 0, minor = 0, patch = 0, agent = "", installed = !0) {
  return Object.defineProperty(
    {
      major,
      minor,
      patch,
      agent,
      installed
    },
    "toString",
    {
      value() {
        return `${this.major}.${this.minor}.${this.patch}`;
      },
      configurable: !1,
      enumerable: !1
    }
  );
}
function notInstalledResponse() {
  return versionResponse(0, 0, 0, "", !1);
}
function version_default() {
  return {
    version() {
      return this._runTask({
        commands: ["--version"],
        format: "utf-8",
        parser: versionParser,
        onError(result, error, done, fail) {
          if (result.exitCode === -2)
            return done(Buffer.from(NOT_INSTALLED));
          fail(error);
        }
      });
    }
  };
}
function versionParser(stdOut) {
  return stdOut === NOT_INSTALLED ? notInstalledResponse() : parseStringResponse(versionResponse(0, 0, 0, stdOut), parsers7, stdOut);
}
var NOT_INSTALLED, parsers7, init_version = __esm({
  "src/lib/tasks/version.ts"() {
    "use strict";
    init_utils(), NOT_INSTALLED = "installed=false", parsers7 = [
      new LineParser(
        /version (\d+)\.(\d+)\.(\d+)(?:\s*\((.+)\))?/,
        (result, [major, minor, patch, agent = ""]) => {
          Object.assign(
            result,
            versionResponse(asNumber(major), asNumber(minor), asNumber(patch), agent)
          );
        }
      ),
      new LineParser(
        /version (\d+)\.(\d+)\.(\D+)(.+)?$/,
        (result, [major, minor, patch, agent = ""]) => {
          Object.assign(result, versionResponse(asNumber(major), asNumber(minor), patch, agent));
        }
      )
    ];
  }
}), simple_git_api_exports = {};
__export(simple_git_api_exports, {
  SimpleGitApi: () => SimpleGitApi
});
var SimpleGitApi, init_simple_git_api = __esm({
  "src/lib/simple-git-api.ts"() {
    "use strict";
    init_task_callback(), init_change_working_directory(), init_checkout(), init_count_objects(), init_commit(), init_config(), init_first_commit(), init_grep(), init_hash_object(), init_init(), init_log(), init_merge(), init_push(), init_show(), init_status(), init_task(), init_version(), init_utils(), SimpleGitApi = class {
      constructor(_executor) {
        this._executor = _executor;
      }
      _runTask(task, then) {
        let chain = this._executor.chain(), promise = chain.push(task);
        return then && taskCallback(task, promise, then), Object.create(this, {
          then: { value: promise.then.bind(promise) },
          catch: { value: promise.catch.bind(promise) },
          _executor: { value: chain }
        });
      }
      add(files) {
        return this._runTask(
          straightThroughStringTask(["add", ...asArray(files)]),
          trailingFunctionArgument(arguments)
        );
      }
      cwd(directory) {
        let next = trailingFunctionArgument(arguments);
        return typeof directory == "string" ? this._runTask(changeWorkingDirectoryTask(directory, this._executor), next) : typeof directory?.path == "string" ? this._runTask(
          changeWorkingDirectoryTask(
            directory.path,
            directory.root && this._executor || void 0
          ),
          next
        ) : this._runTask(
          configurationErrorTask("Git.cwd: workingDirectory must be supplied as a string"),
          next
        );
      }
      hashObject(path3, write) {
        return this._runTask(
          hashObjectTask(path3, write === !0),
          trailingFunctionArgument(arguments)
        );
      }
      init(bare) {
        return this._runTask(
          initTask(bare === !0, this._executor.cwd, getTrailingOptions(arguments)),
          trailingFunctionArgument(arguments)
        );
      }
      merge() {
        return this._runTask(
          mergeTask(getTrailingOptions(arguments)),
          trailingFunctionArgument(arguments)
        );
      }
      mergeFromTo(remote, branch) {
        return filterString(remote) && filterString(branch) ? this._runTask(
          mergeTask([remote, branch, ...getTrailingOptions(arguments)]),
          trailingFunctionArgument(arguments, !1)
        ) : this._runTask(
          configurationErrorTask(
            "Git.mergeFromTo requires that the 'remote' and 'branch' arguments are supplied as strings"
          )
        );
      }
      outputHandler(handler) {
        return this._executor.outputHandler = handler, this;
      }
      push() {
        let task = pushTask(
          {
            remote: filterType(arguments[0], filterString),
            branch: filterType(arguments[1], filterString)
          },
          getTrailingOptions(arguments)
        );
        return this._runTask(task, trailingFunctionArgument(arguments));
      }
      stash() {
        return this._runTask(
          straightThroughStringTask(["stash", ...getTrailingOptions(arguments)]),
          trailingFunctionArgument(arguments)
        );
      }
      status() {
        return this._runTask(
          statusTask(getTrailingOptions(arguments)),
          trailingFunctionArgument(arguments)
        );
      }
    }, Object.assign(
      SimpleGitApi.prototype,
      checkout_default(),
      commit_default(),
      config_default(),
      count_objects_default(),
      first_commit_default(),
      grep_default(),
      log_default(),
      show_default(),
      version_default()
    );
  }
}), scheduler_exports = {};
__export(scheduler_exports, {
  Scheduler: () => Scheduler
});
var createScheduledTask, Scheduler, init_scheduler = __esm({
  "src/lib/runners/scheduler.ts"() {
    "use strict";
    init_utils(), init_git_logger(), createScheduledTask = /* @__PURE__ */ (() => {
      let id = 0;
      return () => {
        id++;
        let { promise, done } = (0, import_promise_deferred.createDeferred)();
        return {
          promise,
          done,
          id
        };
      };
    })(), Scheduler = class {
      constructor(concurrency = 2) {
        this.concurrency = concurrency, this.logger = createLogger("", "scheduler"), this.pending = [], this.running = [], this.logger("Constructed, concurrency=%s", concurrency);
      }
      schedule() {
        if (!this.pending.length || this.running.length >= this.concurrency) {
          this.logger(
            "Schedule attempt ignored, pending=%s running=%s concurrency=%s",
            this.pending.length,
            this.running.length,
            this.concurrency
          );
          return;
        }
        let task = append(this.running, this.pending.shift());
        this.logger("Attempting id=%s", task.id), task.done(() => {
          this.logger("Completing id=", task.id), remove(this.running, task), this.schedule();
        });
      }
      next() {
        let { promise, id } = append(this.pending, createScheduledTask());
        return this.logger("Scheduling id=%s", id), this.schedule(), promise;
      }
    };
  }
}), apply_patch_exports = {};
__export(apply_patch_exports, {
  applyPatchTask: () => applyPatchTask
});
function applyPatchTask(patches, customArgs) {
  return straightThroughStringTask(["apply", ...customArgs, ...patches]);
}
var init_apply_patch = __esm({
  "src/lib/tasks/apply-patch.ts"() {
    "use strict";
    init_task();
  }
});
function branchDeletionSuccess(branch, hash) {
  return {
    branch,
    hash,
    success: !0
  };
}
function branchDeletionFailure(branch) {
  return {
    branch,
    hash: null,
    success: !1
  };
}
var BranchDeletionBatch, init_BranchDeleteSummary = __esm({
  "src/lib/responses/BranchDeleteSummary.ts"() {
    "use strict";
    BranchDeletionBatch = class {
      constructor() {
        this.all = [], this.branches = {}, this.errors = [];
      }
      get success() {
        return !this.errors.length;
      }
    };
  }
});
function hasBranchDeletionError(data, processExitCode) {
  return processExitCode === 1 && deleteErrorRegex.test(data);
}
var deleteSuccessRegex, deleteErrorRegex, parsers8, parseBranchDeletions, init_parse_branch_delete = __esm({
  "src/lib/parsers/parse-branch-delete.ts"() {
    "use strict";
    init_BranchDeleteSummary(), init_utils(), deleteSuccessRegex = /(\S+)\s+\(\S+\s([^)]+)\)/, deleteErrorRegex = /^error[^']+'([^']+)'/m, parsers8 = [
      new LineParser(deleteSuccessRegex, (result, [branch, hash]) => {
        let deletion = branchDeletionSuccess(branch, hash);
        result.all.push(deletion), result.branches[branch] = deletion;
      }),
      new LineParser(deleteErrorRegex, (result, [branch]) => {
        let deletion = branchDeletionFailure(branch);
        result.errors.push(deletion), result.all.push(deletion), result.branches[branch] = deletion;
      })
    ], parseBranchDeletions = (stdOut, stdErr) => parseStringResponse(new BranchDeletionBatch(), parsers8, [stdOut, stdErr]);
  }
}), BranchSummaryResult, init_BranchSummary = __esm({
  "src/lib/responses/BranchSummary.ts"() {
    "use strict";
    BranchSummaryResult = class {
      constructor() {
        this.all = [], this.branches = {}, this.current = "", this.detached = !1;
      }
      push(status, detached, name, commit, label) {
        status === "*" && (this.detached = detached, this.current = name), this.all.push(name), this.branches[name] = {
          current: status === "*",
          linkedWorkTree: status === "+",
          name,
          commit,
          label
        };
      }
    };
  }
});
function branchStatus(input) {
  return input ? input.charAt(0) : "";
}
function parseBranchSummary(stdOut) {
  return parseStringResponse(new BranchSummaryResult(), parsers9, stdOut);
}
var parsers9, init_parse_branch = __esm({
  "src/lib/parsers/parse-branch.ts"() {
    "use strict";
    init_BranchSummary(), init_utils(), parsers9 = [
      new LineParser(
        /^([*+]\s)?\((?:HEAD )?detached (?:from|at) (\S+)\)\s+([a-z0-9]+)\s(.*)$/,
        (result, [current, name, commit, label]) => {
          result.push(branchStatus(current), !0, name, commit, label);
        }
      ),
      new LineParser(
        new RegExp("^([*+]\\s)?(\\S+)\\s+([a-z0-9]+)\\s?(.*)$", "s"),
        (result, [current, name, commit, label]) => {
          result.push(branchStatus(current), !1, name, commit, label);
        }
      )
    ];
  }
}), branch_exports = {};
__export(branch_exports, {
  branchLocalTask: () => branchLocalTask,
  branchTask: () => branchTask,
  containsDeleteBranchCommand: () => containsDeleteBranchCommand,
  deleteBranchTask: () => deleteBranchTask,
  deleteBranchesTask: () => deleteBranchesTask
});
function containsDeleteBranchCommand(commands) {
  let deleteCommands = ["-d", "-D", "--delete"];
  return commands.some((command) => deleteCommands.includes(command));
}
function branchTask(customArgs) {
  let isDelete = containsDeleteBranchCommand(customArgs), commands = ["branch", ...customArgs];
  return commands.length === 1 && commands.push("-a"), commands.includes("-v") || commands.splice(1, 0, "-v"), {
    format: "utf-8",
    commands,
    parser(stdOut, stdErr) {
      return isDelete ? parseBranchDeletions(stdOut, stdErr).all[0] : parseBranchSummary(stdOut);
    }
  };
}
function branchLocalTask() {
  return {
    format: "utf-8",
    commands: ["branch", "-v"],
    parser: parseBranchSummary
  };
}
function deleteBranchesTask(branches, forceDelete = !1) {
  return {
    format: "utf-8",
    commands: ["branch", "-v", forceDelete ? "-D" : "-d", ...branches],
    parser(stdOut, stdErr) {
      return parseBranchDeletions(stdOut, stdErr);
    },
    onError({ exitCode, stdOut }, error, done, fail) {
      if (!hasBranchDeletionError(String(error), exitCode))
        return fail(error);
      done(stdOut);
    }
  };
}
function deleteBranchTask(branch, forceDelete = !1) {
  let task = {
    format: "utf-8",
    commands: ["branch", "-v", forceDelete ? "-D" : "-d", branch],
    parser(stdOut, stdErr) {
      return parseBranchDeletions(stdOut, stdErr).branches[branch];
    },
    onError({ exitCode, stdErr, stdOut }, error, _, fail) {
      if (!hasBranchDeletionError(String(error), exitCode))
        return fail(error);
      throw new GitResponseError(
        task.parser(bufferToString(stdOut), bufferToString(stdErr)),
        String(error)
      );
    }
  };
  return task;
}
var init_branch = __esm({
  "src/lib/tasks/branch.ts"() {
    "use strict";
    init_git_response_error(), init_parse_branch_delete(), init_parse_branch(), init_utils();
  }
}), parseCheckIgnore, init_CheckIgnore = __esm({
  "src/lib/responses/CheckIgnore.ts"() {
    "use strict";
    parseCheckIgnore = (text) => text.split(/\n/g).map((line) => line.trim()).filter((file) => !!file);
  }
}), check_ignore_exports = {};
__export(check_ignore_exports, {
  checkIgnoreTask: () => checkIgnoreTask
});
function checkIgnoreTask(paths) {
  return {
    commands: ["check-ignore", ...paths],
    format: "utf-8",
    parser: parseCheckIgnore
  };
}
var init_check_ignore = __esm({
  "src/lib/tasks/check-ignore.ts"() {
    "use strict";
    init_CheckIgnore();
  }
}), clone_exports = {};
__export(clone_exports, {
  cloneMirrorTask: () => cloneMirrorTask,
  cloneTask: () => cloneTask
});
function disallowedCommand(command) {
  return /^--upload-pack(=|$)/.test(command);
}
function cloneTask(repo, directory, customArgs) {
  let commands = ["clone", ...customArgs];
  return filterString(repo) && commands.push(repo), filterString(directory) && commands.push(directory), commands.find(disallowedCommand) ? configurationErrorTask("git.fetch: potential exploit argument blocked.") : straightThroughStringTask(commands);
}
function cloneMirrorTask(repo, directory, customArgs) {
  return append(customArgs, "--mirror"), cloneTask(repo, directory, customArgs);
}
var init_clone = __esm({
  "src/lib/tasks/clone.ts"() {
    "use strict";
    init_task(), init_utils();
  }
});
function parseFetchResult(stdOut, stdErr) {
  return parseStringResponse({
    raw: stdOut,
    remote: null,
    branches: [],
    tags: [],
    updated: [],
    deleted: []
  }, parsers10, [stdOut, stdErr]);
}
var parsers10, init_parse_fetch = __esm({
  "src/lib/parsers/parse-fetch.ts"() {
    "use strict";
    init_utils(), parsers10 = [
      new LineParser(/From (.+)$/, (result, [remote]) => {
        result.remote = remote;
      }),
      new LineParser(/\* \[new branch]\s+(\S+)\s*-> (.+)$/, (result, [name, tracking]) => {
        result.branches.push({
          name,
          tracking
        });
      }),
      new LineParser(/\* \[new tag]\s+(\S+)\s*-> (.+)$/, (result, [name, tracking]) => {
        result.tags.push({
          name,
          tracking
        });
      }),
      new LineParser(/- \[deleted]\s+\S+\s*-> (.+)$/, (result, [tracking]) => {
        result.deleted.push({
          tracking
        });
      }),
      new LineParser(
        /\s*([^.]+)\.\.(\S+)\s+(\S+)\s*-> (.+)$/,
        (result, [from, to, name, tracking]) => {
          result.updated.push({
            name,
            tracking,
            to,
            from
          });
        }
      )
    ];
  }
}), fetch_exports = {};
__export(fetch_exports, {
  fetchTask: () => fetchTask
});
function disallowedCommand2(command) {
  return /^--upload-pack(=|$)/.test(command);
}
function fetchTask(remote, branch, customArgs) {
  let commands = ["fetch", ...customArgs];
  return remote && branch && commands.push(remote, branch), commands.find(disallowedCommand2) ? configurationErrorTask("git.fetch: potential exploit argument blocked.") : {
    commands,
    format: "utf-8",
    parser: parseFetchResult
  };
}
var init_fetch = __esm({
  "src/lib/tasks/fetch.ts"() {
    "use strict";
    init_parse_fetch(), init_task();
  }
});
function parseMoveResult(stdOut) {
  return parseStringResponse({ moves: [] }, parsers11, stdOut);
}
var parsers11, init_parse_move = __esm({
  "src/lib/parsers/parse-move.ts"() {
    "use strict";
    init_utils(), parsers11 = [
      new LineParser(/^Renaming (.+) to (.+)$/, (result, [from, to]) => {
        result.moves.push({ from, to });
      })
    ];
  }
}), move_exports = {};
__export(move_exports, {
  moveTask: () => moveTask
});
function moveTask(from, to) {
  return {
    commands: ["mv", "-v", ...asArray(from), to],
    format: "utf-8",
    parser: parseMoveResult
  };
}
var init_move = __esm({
  "src/lib/tasks/move.ts"() {
    "use strict";
    init_parse_move(), init_utils();
  }
}), pull_exports = {};
__export(pull_exports, {
  pullTask: () => pullTask
});
function pullTask(remote, branch, customArgs) {
  let commands = ["pull", ...customArgs];
  return remote && branch && commands.splice(1, 0, remote, branch), {
    commands,
    format: "utf-8",
    parser(stdOut, stdErr) {
      return parsePullResult(stdOut, stdErr);
    },
    onError(result, _error, _done, fail) {
      let pullError = parsePullErrorResult(
        bufferToString(result.stdOut),
        bufferToString(result.stdErr)
      );
      if (pullError)
        return fail(new GitResponseError(pullError));
      fail(_error);
    }
  };
}
var init_pull = __esm({
  "src/lib/tasks/pull.ts"() {
    "use strict";
    init_git_response_error(), init_parse_pull(), init_utils();
  }
});
function parseGetRemotes(text) {
  let remotes = {};
  return forEach(text, ([name]) => remotes[name] = { name }), Object.values(remotes);
}
function parseGetRemotesVerbose(text) {
  let remotes = {};
  return forEach(text, ([name, url, purpose]) => {
    remotes.hasOwnProperty(name) || (remotes[name] = {
      name,
      refs: { fetch: "", push: "" }
    }), purpose && url && (remotes[name].refs[purpose.replace(/[^a-z]/g, "")] = url);
  }), Object.values(remotes);
}
function forEach(text, handler) {
  forEachLineWithContent(text, (line) => handler(line.split(/\s+/)));
}
var init_GetRemoteSummary = __esm({
  "src/lib/responses/GetRemoteSummary.ts"() {
    "use strict";
    init_utils();
  }
}), remote_exports = {};
__export(remote_exports, {
  addRemoteTask: () => addRemoteTask,
  getRemotesTask: () => getRemotesTask,
  listRemotesTask: () => listRemotesTask,
  remoteTask: () => remoteTask,
  removeRemoteTask: () => removeRemoteTask
});
function addRemoteTask(remoteName, remoteRepo, customArgs) {
  return straightThroughStringTask(["remote", "add", ...customArgs, remoteName, remoteRepo]);
}
function getRemotesTask(verbose) {
  let commands = ["remote"];
  return verbose && commands.push("-v"), {
    commands,
    format: "utf-8",
    parser: verbose ? parseGetRemotesVerbose : parseGetRemotes
  };
}
function listRemotesTask(customArgs) {
  let commands = [...customArgs];
  return commands[0] !== "ls-remote" && commands.unshift("ls-remote"), straightThroughStringTask(commands);
}
function remoteTask(customArgs) {
  let commands = [...customArgs];
  return commands[0] !== "remote" && commands.unshift("remote"), straightThroughStringTask(commands);
}
function removeRemoteTask(remoteName) {
  return straightThroughStringTask(["remote", "remove", remoteName]);
}
var init_remote = __esm({
  "src/lib/tasks/remote.ts"() {
    "use strict";
    init_GetRemoteSummary(), init_task();
  }
}), stash_list_exports = {};
__export(stash_list_exports, {
  stashListTask: () => stashListTask
});
function stashListTask(opt = {}, customArgs) {
  let options = parseLogOptions(opt), commands = ["stash", "list", ...options.commands, ...customArgs], parser4 = createListLogSummaryParser(
    options.splitter,
    options.fields,
    logFormatFromCommand(commands)
  );
  return validateLogFormatConfig(commands) || {
    commands,
    format: "utf-8",
    parser: parser4
  };
}
var init_stash_list = __esm({
  "src/lib/tasks/stash-list.ts"() {
    "use strict";
    init_log_format(), init_parse_list_log_summary(), init_diff(), init_log();
  }
}), sub_module_exports = {};
__export(sub_module_exports, {
  addSubModuleTask: () => addSubModuleTask,
  initSubModuleTask: () => initSubModuleTask,
  subModuleTask: () => subModuleTask,
  updateSubModuleTask: () => updateSubModuleTask
});
function addSubModuleTask(repo, path3) {
  return subModuleTask(["add", repo, path3]);
}
function initSubModuleTask(customArgs) {
  return subModuleTask(["init", ...customArgs]);
}
function subModuleTask(customArgs) {
  let commands = [...customArgs];
  return commands[0] !== "submodule" && commands.unshift("submodule"), straightThroughStringTask(commands);
}
function updateSubModuleTask(customArgs) {
  return subModuleTask(["update", ...customArgs]);
}
var init_sub_module = __esm({
  "src/lib/tasks/sub-module.ts"() {
    "use strict";
    init_task();
  }
});
function singleSorted(a, b) {
  let aIsNum = isNaN(a), bIsNum = isNaN(b);
  return aIsNum !== bIsNum ? aIsNum ? 1 : -1 : aIsNum ? sorted(a, b) : 0;
}
function sorted(a, b) {
  return a === b ? 0 : a > b ? 1 : -1;
}
function trimmed(input) {
  return input.trim();
}
function toNumber(input) {
  return typeof input == "string" && parseInt(input.replace(/^\D+/g, ""), 10) || 0;
}
var TagList, parseTagList, init_TagList = __esm({
  "src/lib/responses/TagList.ts"() {
    "use strict";
    TagList = class {
      constructor(all, latest) {
        this.all = all, this.latest = latest;
      }
    }, parseTagList = function(data, customSort = !1) {
      let tags = data.split(`
`).map(trimmed).filter(Boolean);
      customSort || tags.sort(function(tagA, tagB) {
        let partsA = tagA.split("."), partsB = tagB.split(".");
        if (partsA.length === 1 || partsB.length === 1)
          return singleSorted(toNumber(partsA[0]), toNumber(partsB[0]));
        for (let i = 0, l = Math.max(partsA.length, partsB.length); i < l; i++) {
          let diff = sorted(toNumber(partsA[i]), toNumber(partsB[i]));
          if (diff)
            return diff;
        }
        return 0;
      });
      let latest = customSort ? tags[0] : [...tags].reverse().find((tag) => tag.indexOf(".") >= 0);
      return new TagList(tags, latest);
    };
  }
}), tag_exports = {};
__export(tag_exports, {
  addAnnotatedTagTask: () => addAnnotatedTagTask,
  addTagTask: () => addTagTask,
  tagListTask: () => tagListTask
});
function tagListTask(customArgs = []) {
  let hasCustomSort = customArgs.some((option) => /^--sort=/.test(option));
  return {
    format: "utf-8",
    commands: ["tag", "-l", ...customArgs],
    parser(text) {
      return parseTagList(text, hasCustomSort);
    }
  };
}
function addTagTask(name) {
  return {
    format: "utf-8",
    commands: ["tag", name],
    parser() {
      return { name };
    }
  };
}
function addAnnotatedTagTask(name, tagMessage) {
  return {
    format: "utf-8",
    commands: ["tag", "-a", "-m", tagMessage, name],
    parser() {
      return { name };
    }
  };
}
var init_tag = __esm({
  "src/lib/tasks/tag.ts"() {
    "use strict";
    init_TagList();
  }
}), require_git = __commonJS2({
  "src/git.js"(exports, module) {
    "use strict";
    var { GitExecutor: GitExecutor2 } = (init_git_executor(), __toCommonJS(git_executor_exports)), { SimpleGitApi: SimpleGitApi2 } = (init_simple_git_api(), __toCommonJS(simple_git_api_exports)), { Scheduler: Scheduler2 } = (init_scheduler(), __toCommonJS(scheduler_exports)), { configurationErrorTask: configurationErrorTask2 } = (init_task(), __toCommonJS(task_exports)), {
      asArray: asArray2,
      filterArray: filterArray2,
      filterPrimitives: filterPrimitives2,
      filterString: filterString2,
      filterStringOrStringArray: filterStringOrStringArray2,
      filterType: filterType2,
      getTrailingOptions: getTrailingOptions2,
      trailingFunctionArgument: trailingFunctionArgument2,
      trailingOptionsArgument: trailingOptionsArgument2
    } = (init_utils(), __toCommonJS(utils_exports)), { applyPatchTask: applyPatchTask2 } = (init_apply_patch(), __toCommonJS(apply_patch_exports)), {
      branchTask: branchTask2,
      branchLocalTask: branchLocalTask2,
      deleteBranchesTask: deleteBranchesTask2,
      deleteBranchTask: deleteBranchTask2
    } = (init_branch(), __toCommonJS(branch_exports)), { checkIgnoreTask: checkIgnoreTask2 } = (init_check_ignore(), __toCommonJS(check_ignore_exports)), { checkIsRepoTask: checkIsRepoTask2 } = (init_check_is_repo(), __toCommonJS(check_is_repo_exports)), { cloneTask: cloneTask2, cloneMirrorTask: cloneMirrorTask2 } = (init_clone(), __toCommonJS(clone_exports)), { cleanWithOptionsTask: cleanWithOptionsTask2, isCleanOptionsArray: isCleanOptionsArray2 } = (init_clean(), __toCommonJS(clean_exports)), { diffSummaryTask: diffSummaryTask2 } = (init_diff(), __toCommonJS(diff_exports)), { fetchTask: fetchTask2 } = (init_fetch(), __toCommonJS(fetch_exports)), { moveTask: moveTask2 } = (init_move(), __toCommonJS(move_exports)), { pullTask: pullTask2 } = (init_pull(), __toCommonJS(pull_exports)), { pushTagsTask: pushTagsTask2 } = (init_push(), __toCommonJS(push_exports)), {
      addRemoteTask: addRemoteTask2,
      getRemotesTask: getRemotesTask2,
      listRemotesTask: listRemotesTask2,
      remoteTask: remoteTask2,
      removeRemoteTask: removeRemoteTask2
    } = (init_remote(), __toCommonJS(remote_exports)), { getResetMode: getResetMode2, resetTask: resetTask2 } = (init_reset(), __toCommonJS(reset_exports)), { stashListTask: stashListTask2 } = (init_stash_list(), __toCommonJS(stash_list_exports)), {
      addSubModuleTask: addSubModuleTask2,
      initSubModuleTask: initSubModuleTask2,
      subModuleTask: subModuleTask2,
      updateSubModuleTask: updateSubModuleTask2
    } = (init_sub_module(), __toCommonJS(sub_module_exports)), { addAnnotatedTagTask: addAnnotatedTagTask2, addTagTask: addTagTask2, tagListTask: tagListTask2 } = (init_tag(), __toCommonJS(tag_exports)), { straightThroughBufferTask: straightThroughBufferTask2, straightThroughStringTask: straightThroughStringTask2 } = (init_task(), __toCommonJS(task_exports));
    function Git2(options, plugins) {
      this._plugins = plugins, this._executor = new GitExecutor2(
        options.baseDir,
        new Scheduler2(options.maxConcurrentProcesses),
        plugins
      ), this._trimmed = options.trimmed;
    }
    (Git2.prototype = Object.create(SimpleGitApi2.prototype)).constructor = Git2, Git2.prototype.customBinary = function(command) {
      return this._plugins.reconfigure("binary", command), this;
    }, Git2.prototype.env = function(name, value) {
      return arguments.length === 1 && typeof name == "object" ? this._executor.env = name : (this._executor.env = this._executor.env || {})[name] = value, this;
    }, Git2.prototype.stashList = function(options) {
      return this._runTask(
        stashListTask2(
          trailingOptionsArgument2(arguments) || {},
          filterArray2(options) && options || []
        ),
        trailingFunctionArgument2(arguments)
      );
    };
    function createCloneTask(api, task, repoPath, localPath) {
      return typeof repoPath != "string" ? configurationErrorTask2(`git.${api}() requires a string 'repoPath'`) : task(repoPath, filterType2(localPath, filterString2), getTrailingOptions2(arguments));
    }
    Git2.prototype.clone = function() {
      return this._runTask(
        createCloneTask("clone", cloneTask2, ...arguments),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.mirror = function() {
      return this._runTask(
        createCloneTask("mirror", cloneMirrorTask2, ...arguments),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.mv = function(from, to) {
      return this._runTask(moveTask2(from, to), trailingFunctionArgument2(arguments));
    }, Git2.prototype.checkoutLatestTag = function(then) {
      var git = this;
      return this.pull(function() {
        git.tags(function(err, tags) {
          git.checkout(tags.latest, then);
        });
      });
    }, Git2.prototype.pull = function(remote, branch, options, then) {
      return this._runTask(
        pullTask2(
          filterType2(remote, filterString2),
          filterType2(branch, filterString2),
          getTrailingOptions2(arguments)
        ),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.fetch = function(remote, branch) {
      return this._runTask(
        fetchTask2(
          filterType2(remote, filterString2),
          filterType2(branch, filterString2),
          getTrailingOptions2(arguments)
        ),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.silent = function(silence) {
      return console.warn(
        "simple-git deprecation notice: git.silent: logging should be configured using the `debug` library / `DEBUG` environment variable, this will be an error in version 3"
      ), this;
    }, Git2.prototype.tags = function(options, then) {
      return this._runTask(
        tagListTask2(getTrailingOptions2(arguments)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.rebase = function() {
      return this._runTask(
        straightThroughStringTask2(["rebase", ...getTrailingOptions2(arguments)]),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.reset = function(mode) {
      return this._runTask(
        resetTask2(getResetMode2(mode), getTrailingOptions2(arguments)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.revert = function(commit) {
      let next = trailingFunctionArgument2(arguments);
      return typeof commit != "string" ? this._runTask(configurationErrorTask2("Commit must be a string"), next) : this._runTask(
        straightThroughStringTask2(["revert", ...getTrailingOptions2(arguments, 0, !0), commit]),
        next
      );
    }, Git2.prototype.addTag = function(name) {
      let task = typeof name == "string" ? addTagTask2(name) : configurationErrorTask2("Git.addTag requires a tag name");
      return this._runTask(task, trailingFunctionArgument2(arguments));
    }, Git2.prototype.addAnnotatedTag = function(tagName, tagMessage) {
      return this._runTask(
        addAnnotatedTagTask2(tagName, tagMessage),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.deleteLocalBranch = function(branchName, forceDelete, then) {
      return this._runTask(
        deleteBranchTask2(branchName, typeof forceDelete == "boolean" ? forceDelete : !1),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.deleteLocalBranches = function(branchNames, forceDelete, then) {
      return this._runTask(
        deleteBranchesTask2(branchNames, typeof forceDelete == "boolean" ? forceDelete : !1),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.branch = function(options, then) {
      return this._runTask(
        branchTask2(getTrailingOptions2(arguments)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.branchLocal = function(then) {
      return this._runTask(branchLocalTask2(), trailingFunctionArgument2(arguments));
    }, Git2.prototype.raw = function(commands) {
      let createRestCommands = !Array.isArray(commands), command = [].slice.call(createRestCommands ? arguments : commands, 0);
      for (let i = 0; i < command.length && createRestCommands; i++)
        if (!filterPrimitives2(command[i])) {
          command.splice(i, command.length - i);
          break;
        }
      command.push(...getTrailingOptions2(arguments, 0, !0));
      var next = trailingFunctionArgument2(arguments);
      return command.length ? this._runTask(straightThroughStringTask2(command, this._trimmed), next) : this._runTask(
        configurationErrorTask2("Raw: must supply one or more command to execute"),
        next
      );
    }, Git2.prototype.submoduleAdd = function(repo, path3, then) {
      return this._runTask(addSubModuleTask2(repo, path3), trailingFunctionArgument2(arguments));
    }, Git2.prototype.submoduleUpdate = function(args, then) {
      return this._runTask(
        updateSubModuleTask2(getTrailingOptions2(arguments, !0)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.submoduleInit = function(args, then) {
      return this._runTask(
        initSubModuleTask2(getTrailingOptions2(arguments, !0)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.subModule = function(options, then) {
      return this._runTask(
        subModuleTask2(getTrailingOptions2(arguments)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.listRemote = function() {
      return this._runTask(
        listRemotesTask2(getTrailingOptions2(arguments)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.addRemote = function(remoteName, remoteRepo, then) {
      return this._runTask(
        addRemoteTask2(remoteName, remoteRepo, getTrailingOptions2(arguments)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.removeRemote = function(remoteName, then) {
      return this._runTask(removeRemoteTask2(remoteName), trailingFunctionArgument2(arguments));
    }, Git2.prototype.getRemotes = function(verbose, then) {
      return this._runTask(getRemotesTask2(verbose === !0), trailingFunctionArgument2(arguments));
    }, Git2.prototype.remote = function(options, then) {
      return this._runTask(
        remoteTask2(getTrailingOptions2(arguments)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.tag = function(options, then) {
      let command = getTrailingOptions2(arguments);
      return command[0] !== "tag" && command.unshift("tag"), this._runTask(straightThroughStringTask2(command), trailingFunctionArgument2(arguments));
    }, Git2.prototype.updateServerInfo = function(then) {
      return this._runTask(
        straightThroughStringTask2(["update-server-info"]),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.pushTags = function(remote, then) {
      let task = pushTagsTask2(
        { remote: filterType2(remote, filterString2) },
        getTrailingOptions2(arguments)
      );
      return this._runTask(task, trailingFunctionArgument2(arguments));
    }, Git2.prototype.rm = function(files) {
      return this._runTask(
        straightThroughStringTask2(["rm", "-f", ...asArray2(files)]),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.rmKeepLocal = function(files) {
      return this._runTask(
        straightThroughStringTask2(["rm", "--cached", ...asArray2(files)]),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.catFile = function(options, then) {
      return this._catFile("utf-8", arguments);
    }, Git2.prototype.binaryCatFile = function() {
      return this._catFile("buffer", arguments);
    }, Git2.prototype._catFile = function(format, args) {
      var handler = trailingFunctionArgument2(args), command = ["cat-file"], options = args[0];
      if (typeof options == "string")
        return this._runTask(
          configurationErrorTask2("Git.catFile: options must be supplied as an array of strings"),
          handler
        );
      Array.isArray(options) && command.push.apply(command, options);
      let task = format === "buffer" ? straightThroughBufferTask2(command) : straightThroughStringTask2(command);
      return this._runTask(task, handler);
    }, Git2.prototype.diff = function(options, then) {
      let task = filterString2(options) ? configurationErrorTask2(
        "git.diff: supplying options as a single string is no longer supported, switch to an array of strings"
      ) : straightThroughStringTask2(["diff", ...getTrailingOptions2(arguments)]);
      return this._runTask(task, trailingFunctionArgument2(arguments));
    }, Git2.prototype.diffSummary = function() {
      return this._runTask(
        diffSummaryTask2(getTrailingOptions2(arguments, 1)),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.applyPatch = function(patches) {
      let task = filterStringOrStringArray2(patches) ? applyPatchTask2(asArray2(patches), getTrailingOptions2([].slice.call(arguments, 1))) : configurationErrorTask2(
        "git.applyPatch requires one or more string patches as the first argument"
      );
      return this._runTask(task, trailingFunctionArgument2(arguments));
    }, Git2.prototype.revparse = function() {
      let commands = ["rev-parse", ...getTrailingOptions2(arguments, !0)];
      return this._runTask(
        straightThroughStringTask2(commands, !0),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.clean = function(mode, options, then) {
      let usingCleanOptionsArray = isCleanOptionsArray2(mode), cleanMode = usingCleanOptionsArray && mode.join("") || filterType2(mode, filterString2) || "", customArgs = getTrailingOptions2([].slice.call(arguments, usingCleanOptionsArray ? 1 : 0));
      return this._runTask(
        cleanWithOptionsTask2(cleanMode, customArgs),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.exec = function(then) {
      let task = {
        commands: [],
        format: "utf-8",
        parser() {
          typeof then == "function" && then();
        }
      };
      return this._runTask(task);
    }, Git2.prototype.clearQueue = function() {
      return this;
    }, Git2.prototype.checkIgnore = function(pathnames, then) {
      return this._runTask(
        checkIgnoreTask2(asArray2(filterType2(pathnames, filterStringOrStringArray2, []))),
        trailingFunctionArgument2(arguments)
      );
    }, Git2.prototype.checkIsRepo = function(checkType, then) {
      return this._runTask(
        checkIsRepoTask2(filterType2(checkType, filterString2)),
        trailingFunctionArgument2(arguments)
      );
    }, module.exports = Git2;
  }
});
init_pathspec();
init_git_error();
var GitConstructError = class extends GitError {
  constructor(config, message) {
    super(void 0, message), this.config = config;
  }
};
init_git_error();
init_git_error();
var GitPluginError = class extends GitError {
  constructor(task, plugin, message) {
    super(task, message), this.task = task, this.plugin = plugin, Object.setPrototypeOf(this, new.target.prototype);
  }
};
init_git_response_error();
init_task_configuration_error();
init_check_is_repo();
init_clean();
init_config();
init_diff_name_status();
init_grep();
init_reset();
function abortPlugin(signal) {
  return signal ? [{
    type: "spawn.before",
    action(_data, context) {
      signal.aborted && context.kill(new GitPluginError(void 0, "abort", "Abort already signaled"));
    }
  }, {
    type: "spawn.after",
    action(_data, context) {
      function kill() {
        context.kill(new GitPluginError(void 0, "abort", "Abort signal received"));
      }
      signal.addEventListener("abort", kill), context.spawned.on("close", () => signal.removeEventListener("abort", kill));
    }
  }] : void 0;
}
function isConfigSwitch(arg) {
  return typeof arg == "string" && arg.trim().toLowerCase() === "-c";
}
function preventProtocolOverride(arg, next) {
  if (isConfigSwitch(arg) && /^\s*protocol(.[a-z]+)?.allow/.test(next))
    throw new GitPluginError(
      void 0,
      "unsafe",
      "Configuring protocol.allow is not permitted without enabling allowUnsafeExtProtocol"
    );
}
function preventUploadPack(arg, method) {
  if (/^\s*--(upload|receive)-pack/.test(arg))
    throw new GitPluginError(
      void 0,
      "unsafe",
      "Use of --upload-pack or --receive-pack is not permitted without enabling allowUnsafePack"
    );
  if (method === "clone" && /^\s*-u\b/.test(arg))
    throw new GitPluginError(
      void 0,
      "unsafe",
      "Use of clone with option -u is not permitted without enabling allowUnsafePack"
    );
  if (method === "push" && /^\s*--exec\b/.test(arg))
    throw new GitPluginError(
      void 0,
      "unsafe",
      "Use of push with option --exec is not permitted without enabling allowUnsafePack"
    );
}
function blockUnsafeOperationsPlugin({
  allowUnsafeProtocolOverride = !1,
  allowUnsafePack = !1
} = {}) {
  return {
    type: "spawn.args",
    action(args, context) {
      return args.forEach((current, index) => {
        let next = index < args.length ? args[index + 1] : "";
        allowUnsafeProtocolOverride || preventProtocolOverride(current, next), allowUnsafePack || preventUploadPack(current, context.method);
      }), args;
    }
  };
}
init_utils();
function commandConfigPrefixingPlugin(configuration) {
  let prefix = prefixedArray(configuration, "-c");
  return {
    type: "spawn.args",
    action(data) {
      return [...prefix, ...data];
    }
  };
}
init_utils();
var never = (0, import_promise_deferred2.deferred)().promise;
function completionDetectionPlugin({
  onClose = !0,
  onExit = 50
} = {}) {
  function createEvents() {
    let exitCode = -1, events = {
      close: (0, import_promise_deferred2.deferred)(),
      closeTimeout: (0, import_promise_deferred2.deferred)(),
      exit: (0, import_promise_deferred2.deferred)(),
      exitTimeout: (0, import_promise_deferred2.deferred)()
    }, result = Promise.race([
      onClose === !1 ? never : events.closeTimeout.promise,
      onExit === !1 ? never : events.exitTimeout.promise
    ]);
    return configureTimeout(onClose, events.close, events.closeTimeout), configureTimeout(onExit, events.exit, events.exitTimeout), {
      close(code) {
        exitCode = code, events.close.done();
      },
      exit(code) {
        exitCode = code, events.exit.done();
      },
      get exitCode() {
        return exitCode;
      },
      result
    };
  }
  function configureTimeout(flag, event, timeout) {
    flag !== !1 && (flag === !0 ? event.promise : event.promise.then(() => delay(flag))).then(timeout.done);
  }
  return {
    type: "spawn.after",
    action(_0, _1) {
      return __async(this, arguments, function* (_data, { spawned, close }) {
        var _a3, _b;
        let events = createEvents(), deferClose = !0, quickClose = () => {
          deferClose = !1;
        };
        (_a3 = spawned.stdout) == null || _a3.on("data", quickClose), (_b = spawned.stderr) == null || _b.on("data", quickClose), spawned.on("error", quickClose), spawned.on("close", (code) => events.close(code)), spawned.on("exit", (code) => events.exit(code));
        try {
          yield events.result, deferClose && (yield delay(50)), close(events.exitCode);
        } catch (err) {
          close(events.exitCode, err);
        }
      });
    }
  };
}
init_utils();
var WRONG_NUMBER_ERR = "Invalid value supplied for custom binary, requires a single string or an array containing either one or two strings", WRONG_CHARS_ERR = "Invalid value supplied for custom binary, restricted characters must be removed or supply the unsafe.allowUnsafeCustomBinary option";
function isBadArgument(arg) {
  return !arg || !/^([a-z]:)?([a-z0-9/.\\_-]+)$/i.test(arg);
}
function toBinaryConfig(input, allowUnsafe) {
  if (input.length < 1 || input.length > 2)
    throw new GitPluginError(void 0, "binary", WRONG_NUMBER_ERR);
  if (input.some(isBadArgument))
    if (allowUnsafe)
      console.warn(WRONG_CHARS_ERR);
    else
      throw new GitPluginError(void 0, "binary", WRONG_CHARS_ERR);
  let [binary, prefix] = input;
  return {
    binary,
    prefix
  };
}
function customBinaryPlugin(plugins, input = ["git"], allowUnsafe = !1) {
  let config = toBinaryConfig(asArray(input), allowUnsafe);
  plugins.on("binary", (input2) => {
    config = toBinaryConfig(asArray(input2), allowUnsafe);
  }), plugins.append("spawn.binary", () => config.binary), plugins.append("spawn.args", (data) => config.prefix ? [config.prefix, ...data] : data);
}
init_git_error();
function isTaskError(result) {
  return !!(result.exitCode && result.stdErr.length);
}
function getErrorMessage(result) {
  return Buffer.concat([...result.stdOut, ...result.stdErr]);
}
function errorDetectionHandler(overwrite = !1, isError = isTaskError, errorMessage = getErrorMessage) {
  return (error, result) => !overwrite && error || !isError(result) ? error : errorMessage(result);
}
function errorDetectionPlugin(config) {
  return {
    type: "task.error",
    action(data, context) {
      let error = config(data.error, {
        stdErr: context.stdErr,
        stdOut: context.stdOut,
        exitCode: context.exitCode
      });
      return Buffer.isBuffer(error) ? { error: new GitError(void 0, error.toString("utf-8")) } : {
        error
      };
    }
  };
}
init_utils();
var PluginStore = class {
  constructor() {
    this.plugins = /* @__PURE__ */ new Set(), this.events = new EventEmitter2();
  }
  on(type, listener) {
    this.events.on(type, listener);
  }
  reconfigure(type, data) {
    this.events.emit(type, data);
  }
  append(type, action) {
    let plugin = append(this.plugins, { type, action });
    return () => this.plugins.delete(plugin);
  }
  add(plugin) {
    let plugins = [];
    return asArray(plugin).forEach((plugin2) => plugin2 && this.plugins.add(append(plugins, plugin2))), () => {
      plugins.forEach((plugin2) => this.plugins.delete(plugin2));
    };
  }
  exec(type, data, context) {
    let output = data, contextual = Object.freeze(Object.create(context));
    for (let plugin of this.plugins)
      plugin.type === type && (output = plugin.action(output, contextual));
    return output;
  }
};
init_utils();
function progressMonitorPlugin(progress) {
  let progressCommand = "--progress", progressMethods = ["checkout", "clone", "fetch", "pull", "push"];
  return [{
    type: "spawn.args",
    action(args, context) {
      return progressMethods.includes(context.method) ? including(args, progressCommand) : args;
    }
  }, {
    type: "spawn.after",
    action(_data, context) {
      var _a2;
      context.commands.includes(progressCommand) && ((_a2 = context.spawned.stderr) == null || _a2.on("data", (chunk) => {
        let message = /^([\s\S]+?):\s*(\d+)% \((\d+)\/(\d+)\)/.exec(chunk.toString("utf8"));
        message && progress({
          method: context.method,
          stage: progressEventStage(message[1]),
          progress: asNumber(message[2]),
          processed: asNumber(message[3]),
          total: asNumber(message[4])
        });
      }));
    }
  }];
}
function progressEventStage(input) {
  return String(input.toLowerCase().split(" ", 1)) || "unknown";
}
init_utils();
function spawnOptionsPlugin(spawnOptions) {
  let options = pick(spawnOptions, ["uid", "gid"]);
  return {
    type: "spawn.options",
    action(data) {
      return __spreadValues(__spreadValues({}, options), data);
    }
  };
}
function timeoutPlugin({
  block,
  stdErr = !0,
  stdOut = !0
}) {
  if (block > 0)
    return {
      type: "spawn.after",
      action(_data, context) {
        var _a2, _b;
        let timeout;
        function wait() {
          timeout && clearTimeout(timeout), timeout = setTimeout(kill, block);
        }
        function stop() {
          var _a3, _b2;
          (_a3 = context.spawned.stdout) == null || _a3.off("data", wait), (_b2 = context.spawned.stderr) == null || _b2.off("data", wait), context.spawned.off("exit", stop), context.spawned.off("close", stop), timeout && clearTimeout(timeout);
        }
        function kill() {
          stop(), context.kill(new GitPluginError(void 0, "timeout", "block timeout reached"));
        }
        stdOut && ((_a2 = context.spawned.stdout) == null || _a2.on("data", wait)), stdErr && ((_b = context.spawned.stderr) == null || _b.on("data", wait)), context.spawned.on("exit", stop), context.spawned.on("close", stop), wait();
      }
    };
}
init_pathspec();
function suffixPathsPlugin() {
  return {
    type: "spawn.args",
    action(data) {
      let prefix = [], suffix;
      function append2(args) {
        (suffix = suffix || []).push(...args);
      }
      for (let i = 0; i < data.length; i++) {
        let param = data[i];
        if (isPathSpec(param)) {
          append2(toPaths(param));
          continue;
        }
        if (param === "--") {
          append2(
            data.slice(i + 1).flatMap((item) => isPathSpec(item) && toPaths(item) || item)
          );
          break;
        }
        prefix.push(param);
      }
      return suffix ? [...prefix, "--", ...suffix.map(String)] : prefix;
    }
  };
}
init_utils();
var Git = require_git();
function gitInstanceFactory(baseDir, options) {
  var _a2;
  let plugins = new PluginStore(), config = createInstanceConfig(
    baseDir && (typeof baseDir == "string" ? { baseDir } : baseDir) || {},
    options
  );
  if (!folderExists(config.baseDir))
    throw new GitConstructError(
      config,
      "Cannot use simple-git on a directory that does not exist"
    );
  return Array.isArray(config.config) && plugins.add(commandConfigPrefixingPlugin(config.config)), plugins.add(blockUnsafeOperationsPlugin(config.unsafe)), plugins.add(suffixPathsPlugin()), plugins.add(completionDetectionPlugin(config.completion)), config.abort && plugins.add(abortPlugin(config.abort)), config.progress && plugins.add(progressMonitorPlugin(config.progress)), config.timeout && plugins.add(timeoutPlugin(config.timeout)), config.spawnOptions && plugins.add(spawnOptionsPlugin(config.spawnOptions)), plugins.add(errorDetectionPlugin(errorDetectionHandler(!0))), config.errors && plugins.add(errorDetectionPlugin(config.errors)), customBinaryPlugin(plugins, config.binary, (_a2 = config.unsafe) == null ? void 0 : _a2.allowUnsafeCustomBinary), new Git(config, plugins);
}
init_git_response_error();
var esm_default = gitInstanceFactory;

// ../cli-kit/dist/public/node/git.js
var import_ignore = __toESM(require_ignore());
async function initializeGitRepository(directory, initialBranch = "main") {
  outputDebug(outputContent`Initializing git repository at ${outputToken.path(directory)}...`), await ensureGitIsPresentOrAbort(), await withGit({ directory }, async (repo) => {
    await repo.init(), await repo.checkoutLocalBranch(initialBranch);
  });
}
async function checkIfIgnoredInGitRepository(directory, files) {
  return withGit({ directory }, (repo) => repo.checkIgnore(files));
}
async function downloadGitRepository(cloneOptions) {
  return runWithTimer("cmd_all_timing_network_ms")(async () => {
    let { repoUrl, destination, progressUpdater, shallow, latestTag } = cloneOptions;
    if (outputDebug(outputContent`Git-cloning repository ${repoUrl} into ${outputToken.path(destination)}...`), await ensureGitIsPresentOrAbort(), await fileExists(destination)) {
      if (!await isDirectory(destination))
        throw new AbortError(outputContent`Can't clone to ${outputToken.path(destination)}`, "The path exists but isn't a directory.");
      if ((await glob(["*", ".*"], {
        cwd: destination,
        deep: 1,
        onlyFiles: !1
      })).length > 0)
        throw new AbortError(outputContent`Directory ${outputToken.path(destination)} already exists and is not empty`, outputContent`Choose a different name or remove the existing directory first.`);
    }
    let [repository, branch] = repoUrl.split("#"), options = { "--recurse-submodules": null };
    if (branch && latestTag)
      throw new AbortError("Error cloning the repository. Git can't clone the latest release with a 'branch'.");
    if (branch && (options["--branch"] = branch), shallow && latestTag)
      throw new AbortError("Error cloning the repository. Git can't clone the latest release with the 'shallow' property.");
    shallow && (options["--depth"] = 1);
    let simpleGitOptions = {
      progress: ({ stage, progress: progress2, processed, total }) => {
        let updateString = `${stage}, ${processed}/${total} objects (${progress2}% complete)`;
        progressUpdater && progressUpdater(updateString);
      },
      ...!isTerminalInteractive() && { config: ["core.askpass=true"] }
    };
    try {
      await esm_default(simpleGitOptions).clone(repository, destination, options), latestTag && await withGit({ directory: destination }, async (localGitRepository) => {
        let latestTag2 = await getLocalLatestTag(localGitRepository, repoUrl);
        await localGitRepository.checkout(latestTag2);
      });
    } catch (err) {
      if (err instanceof Error) {
        let abortError = new AbortError(err.message);
        throw abortError.stack = err.stack, abortError;
      }
      throw err;
    }
  });
}
async function getLocalLatestTag(repository, repoUrl) {
  let latest = (await repository.tags()).latest;
  if (!latest)
    throw new AbortError(`Couldn't obtain the most recent tag of the repository ${repoUrl}`);
  return latest;
}
async function getLatestGitCommit(directory) {
  let logs = await withGit({ directory }, (repo) => repo.log({ maxCount: 1 }));
  if (!logs.latest)
    throw new AbortError("Must have at least one commit to run command", outputContent`Run ${outputToken.genericShellCommand("git commit -m 'Initial commit'")} to create your first commit.`);
  return logs.latest;
}
async function addAllToGitFromDirectory(directory) {
  await withGit({ directory }, (repo) => repo.raw("add", "--all"));
}
async function createGitCommit(message, options) {
  let commitOptions = options?.author ? { "--author": options.author } : void 0;
  return (await withGit({ directory: options?.directory }, (repo) => repo.commit(message, commitOptions))).commit;
}
async function ensureGitIsPresentOrAbort() {
  if (!await hasGit())
    throw new AbortError("Git is necessary in the environment to continue", outputContent`Install ${outputToken.link("git", "https://git-scm.com/book/en/v2/Getting-Started-Installing-Git")}`);
}
var OutsideGitDirectoryError = class extends AbortError {
};
async function ensureInsideGitDirectory(directory) {
  if (!await insideGitDirectory(directory))
    throw new OutsideGitDirectoryError(`${outputToken.path(directory || cwd())} is not a Git directory`);
}
async function insideGitDirectory(directory) {
  return withGit({ directory }, (repo) => repo.checkIsRepo());
}
var GitDirectoryNotCleanError = class extends AbortError {
};
async function ensureIsClean(directory) {
  if (!await isClean(directory))
    throw new GitDirectoryNotCleanError(`${outputToken.path(directory || cwd())} is not a clean Git directory`);
}
async function isClean(directory) {
  return (await withGit({ directory }, (git) => git.status())).isClean();
}
async function removeGitRemote(directory, remoteName = "origin") {
  outputDebug(outputContent`Removing git remote ${remoteName} from ${outputToken.path(directory)}...`), await ensureGitIsPresentOrAbort(), await withGit({ directory }, async (repo) => {
    if (!(await repo.getRemotes()).some((remote) => remote.name === remoteName)) {
      outputDebug(outputContent`Remote ${remoteName} does not exist, no action needed`);
      return;
    }
    await repo.removeRemote(remoteName);
  });
}
async function withGit({ directory }, callback) {
  let repo = esm_default({ baseDir: directory });
  try {
    return await callback(repo);
  } catch (err) {
    if (err instanceof Error) {
      let abortError = new AbortError(err.message);
      throw abortError.stack = err.stack, abortError;
    }
    throw err;
  }
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/routes/generate.js
init_cjs_shims();
import { readdir as readdir2 } from "node:fs/promises";

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/remix-version-interop.js
init_cjs_shims();
import { createRequire as createRequire2 } from "module";
function convertRouteToV1(route) {
  return route.replace(/(^|\.)_index$/, "$1index").replace(/\.(?!\w+\])/g, "/");
}
function isV1RouteConventionInstalled() {
  try {
    return createRequire2(import.meta.url).resolve("@remix-run/v1-route-convention/package.json"), !0;
  } catch {
    return !1;
  }
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/remix-config.js
init_cjs_shims();
import { createRequire as createRequire3 } from "node:module";
import path2 from "node:path";

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/remix-version-check.js
init_cjs_shims();

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/vite-config.js
init_cjs_shims();
var REMIX_COMPILER_ERROR_MESSAGE = "Classic Remix Compiler projects are no longer supported, please upgrade to Vite by running 'npx shopify hydrogen setup vite'";
async function hasViteConfig(root) {
  return !!(await findFileWithExtension(root, "vite.config")).filepath;
}
async function isViteProject(root) {
  let isVite = await hasViteConfig(root);
  return isVite && await hasRemixConfigFile(root) && renderWarning({
    headline: "Both Vite and Remix config files found.",
    body: "The remix.config.js file is not used in Vite projects. Please remove it to avoid conflicts."
  }), isVite;
}
async function getViteConfig(root, ssrEntryFlag) {
  let vite = await importVite(root), command = "build", mode = process.env.NODE_ENV || "production", maybeConfig = await vite.loadConfigFromFile(
    { command, mode, isSsrBuild: !0 },
    void 0,
    root
  );
  if (!maybeConfig || !maybeConfig.path)
    throw new Error("No Vite config found");
  let resolvedViteConfig = await vite.resolveConfig(
    { root, build: { ssr: !0 } },
    command,
    mode,
    mode
  ), { appDirectory, serverBuildFile, routes } = getReactRouterOrRemixConfigFromVite(resolvedViteConfig), serverOutDir = resolvedViteConfig.build.outDir, clientOutDir = serverOutDir.replace(/server$/, "client"), rollupOutput = resolvedViteConfig.build.rollupOptions.output, { entryFileNames } = (Array.isArray(rollupOutput) ? rollupOutput[0] : rollupOutput) ?? {}, serverOutFile = joinPath(
    serverOutDir,
    typeof entryFileNames == "string" ? entryFileNames : serverBuildFile ?? "index.js"
  ), ssrEntry = ssrEntryFlag ?? resolvedViteConfig.build.ssr, resolvedSsrEntry = resolvePath(
    resolvedViteConfig.root,
    typeof ssrEntry == "string" ? ssrEntry : "server"
  );
  return {
    clientOutDir,
    serverOutDir,
    serverOutFile,
    resolvedViteConfig,
    userViteConfig: maybeConfig.config,
    remixConfig: {
      routes: routes ?? {},
      appDirectory: appDirectory ?? joinPath(resolvedViteConfig.root, "app"),
      rootDirectory: resolvedViteConfig.root,
      serverEntryPoint: (await findFileWithExtension(
        dirname(resolvedSsrEntry),
        basename(resolvedSsrEntry)
      )).filepath || resolvedSsrEntry
    }
  };
}
function getReactRouterOrRemixConfigFromVite(viteConfig) {
  try {
    return getReactRouterConfigFromVite(viteConfig);
  } catch {
    return getRemixConfigFromVite(viteConfig);
  }
}
function getReactRouterConfigFromVite(viteConfig) {
  if (!viteConfig.__reactRouterPluginContext)
    throw new Error("Could not resolve React Router config");
  let { appDirectory, serverBuildFile, routes } = viteConfig.__reactRouterPluginContext.reactRouterConfig;
  return {
    appDirectory,
    serverBuildFile,
    routes
  };
}
function getRemixConfigFromVite(viteConfig) {
  let { remixConfig } = findHydrogenPlugin(viteConfig)?.api?.getPluginOptions() ?? {};
  return remixConfig ? {
    appDirectory: remixConfig.appDirectory,
    serverBuildFile: remixConfig.serverBuildFile,
    routes: remixConfig.routes
  } : {};
}
function findPlugin(config, name) {
  return config.plugins.find((plugin) => plugin.name === name);
}
function findHydrogenPlugin(config) {
  return findPlugin(config, "hydrogen:main");
}
function findOxygenPlugin(config) {
  return findPlugin(config, "oxygen:main");
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/remix-config.js
async function hasRemixConfigFile(root) {
  return !!(await findFileWithExtension(root, "remix.config")).filepath;
}
async function isClassicProject(root) {
  return await isViteProject(root) ? !1 : hasRemixConfigFile(root);
}
var BUILD_DIR = "dist", CLIENT_SUBDIR = "client", WORKER_SUBDIR = "worker";
function getProjectPaths(appPath) {
  let root = appPath ?? process.cwd(), publicPath = path2.join(root, "public"), buildPath = path2.join(root, BUILD_DIR), buildPathClient = path2.join(buildPath, CLIENT_SUBDIR), buildPathWorkerFile = path2.join(buildPath, WORKER_SUBDIR, "index.js");
  return {
    root,
    buildPath,
    buildPathClient,
    buildPathWorkerFile,
    publicPath
  };
}
function getRawRemixConfig(root) {
  return findFileWithExtension(root, "remix.config").then(({ filepath }) => {
    if (!filepath) throw new AbortError("No remix.config.js file found.");
    return createRequire3(import.meta.url)(filepath);
  });
}
async function getRemixConfig(root, mode = process.env.NODE_ENV) {
  if (!await isViteProject(root))
    throw new AbortError(REMIX_COMPILER_ERROR_MESSAGE);
  return (await getViteConfig(root)).remixConfig;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/setups/routes/generate.js
var NO_LOCALE_PATTERNS = [/robots\.txt/], ROUTE_MAP = {
  home: ["_index", "$"],
  page: "pages*",
  cart: ["cart", "cart.$lines", "discount.$code"],
  products: "products*",
  collections: "collections*",
  policies: "policies*",
  blogs: "blogs*",
  account: "account*",
  search: ["search", "api.predictive-search"],
  robots: "[robots.txt]",
  sitemap: ["[sitemap.xml]", "sitemap.$type.$page[.xml]"],
  tokenlessApi: "api.$version.[graphql.json]"
}, allRouteTemplateFiles = [];
async function getResolvedRoutes(routeKeys = Object.keys(ROUTE_MAP)) {
  allRouteTemplateFiles.length === 0 && (allRouteTemplateFiles = (await readdir2(await getTemplateAppFile(ASSETS_STARTER_DIR_ROUTES))).map((item) => item.replace(/\.tsx?$/, "")));
  let routeGroups = {}, resolvedRouteFiles = [];
  for (let key of routeKeys) {
    routeGroups[key] = [];
    let value = ROUTE_MAP[key];
    if (!value)
      throw new AbortError(
        `No route found for ${key}. Try one of ${ALL_ROUTE_CHOICES.join()}.`
      );
    let routes = Array.isArray(value) ? value : [value];
    for (let route of routes) {
      let routePrefix = route.replace("*", "");
      routeGroups[key].push(
        ...allRouteTemplateFiles.filter((file) => file.startsWith(routePrefix))
      );
    }
    resolvedRouteFiles.push(...routeGroups[key]);
  }
  return { routeGroups, resolvedRouteFiles };
}
var ALL_ROUTE_CHOICES = [...Object.keys(ROUTE_MAP), "all"];
async function generateRoutes(options, remixConfig) {
  let { routeGroups, resolvedRouteFiles } = options.routeName === "all" ? await getResolvedRoutes() : await getResolvedRoutes([options.routeName]), { rootDirectory, appDirectory } = remixConfig || await getRemixConfig(options.directory), routesArray = resolvedRouteFiles.flatMap(
    (item) => ASSETS_STARTER_DIR_ROUTES + "/" + item
  ), formatOptions = await getCodeFormatOptions(rootDirectory), routesDirectory = joinPath(appDirectory, ASSETS_STARTER_DIR_ROUTES), localePrefix = await getLocalePrefix(routesDirectory, options), typescript = !!(options.typescript ?? await fileExists(joinPath(rootDirectory, "tsconfig.json"))), routes = [];
  for (let route of routesArray)
    routes.push(
      await generateProjectFile(route, {
        ...options,
        typescript,
        localePrefix,
        rootDirectory,
        appDirectory,
        formatOptions
      })
    );
  return localePrefix && await copyLocaleNamelessRoute({
    typescript,
    localePrefix,
    routesDirectory,
    formatOptions,
    adapter: options.adapter
  }), {
    routes,
    routeGroups,
    isTypescript: typescript,
    formatOptions
  };
}
async function getLocalePrefix(routesDirectory, { localePrefix, routeName, v1RouteConvention }) {
  if (localePrefix) return localePrefix;
  if (localePrefix !== void 0 || routeName === "all") return;
  let existingFiles = await readdir2(routesDirectory).catch(() => []), coreRouteWithLocaleRE = v1RouteConvention ? /^\(\$(\w+)\)$/ : /^\(\$(\w+)\)\.(_index|\$|cart).[jt]sx?$/, coreRouteWithLocale = existingFiles.find(
    (file) => coreRouteWithLocaleRE.test(file)
  );
  if (coreRouteWithLocale)
    return coreRouteWithLocale.match(coreRouteWithLocaleRE)?.[1];
}
async function generateProjectFile(routeFrom, {
  rootDirectory,
  appDirectory,
  typescript,
  force,
  adapter,
  templatesRoot,
  formatOptions,
  localePrefix,
  v1RouteConvention = !1,
  signal,
  overwriteFileDeps = !0
}) {
  templatesRoot ??= await getStarterDir();
  let extension = (routeFrom.match(/(\.[jt]sx?)$/) ?? [])[1] ?? ".tsx";
  routeFrom = routeFrom.replace(extension, "");
  let routeDestinationPath = joinPath(
    appDirectory,
    getDestinationRoute(routeFrom, localePrefix, { v1RouteConvention }) + (typescript ? extension : extension.replace(".ts", ".js"))
  ), result = {
    operation: "created",
    sourceRoute: routeFrom,
    destinationRoute: relativizePath(routeDestinationPath, rootDirectory)
  };
  if (!force && await fileExists(routeDestinationPath)) {
    if (!await renderConfirmationPrompt({
      message: `The file ${result.destinationRoute} already exists. Do you want to replace it?`,
      defaultValue: !1,
      confirmationMessage: "Yes",
      cancellationMessage: "No",
      abortSignal: signal
    })) return { ...result, operation: "skipped" };
    result.operation = "replaced";
  }
  let routeTemplatePath = await getTemplateAppFile(
    routeFrom + extension,
    templatesRoot
  ), allFilesToGenerate = await findRouteDependencies(
    routeTemplatePath,
    await getTemplateAppFile("", templatesRoot)
  );
  for (let filePath of allFilesToGenerate) {
    let destinationPath = filePath.startsWith(ASSETS_STARTER_DIR_ROUTES + "/") ? routeDestinationPath : joinPath(
      appDirectory,
      filePath.replace(/\.ts(x?)$/, `.${typescript ? "ts$1" : "js$1"}`)
    );
    if (!overwriteFileDeps && await fileExists(destinationPath))
      continue;
    await fileExists(dirname(destinationPath)) || await mkdir(dirname(destinationPath));
    let templateAppFilePath = await getTemplateAppFile(
      filePath,
      templatesRoot
    );
    if (!/\.[jt]sx?$/.test(filePath)) {
      await copyFile(templateAppFilePath, destinationPath);
      continue;
    }
    let templateContent = await readFile(templateAppFilePath);
    typescript || (templateContent = await transpileFile(
      templateContent,
      templateAppFilePath
    )), adapter && (templateContent = replaceAdapters(templateContent, adapter)), templateContent = await formatCode(
      templateContent,
      formatOptions,
      destinationPath
    ), await writeFile(destinationPath, templateContent);
  }
  return result;
}
function replaceAdapters(templateContent, adapter) {
  return templateContent.replace(/@shopify\/remix-oxygen/g, adapter);
}
function getDestinationRoute(routeFrom, localePrefix, options) {
  let routePath = routeFrom.replace(ASSETS_STARTER_DIR_ROUTES + "/", ""), filePrefix = localePrefix && !NO_LOCALE_PATTERNS.some((pattern) => pattern.test(routePath)) ? `($${localePrefix})` + (options.v1RouteConvention ? "/" : ".") : "";
  return ASSETS_STARTER_DIR_ROUTES + "/" + filePrefix + // The template file uses the v2 route convention, so we need to convert
  // it to v1 if the user is not using v2.
  (options.v1RouteConvention ? convertRouteToV1(routePath) : routePath);
}
async function findRouteDependencies(routeFilePath, appDirectory) {
  let filesToCheck = /* @__PURE__ */ new Set([routeFilePath]), fileDependencies = /* @__PURE__ */ new Set([relativePath(appDirectory, routeFilePath)]);
  for (let filePath of filesToCheck) {
    let importMatches = (await readFile(filePath, { encoding: "utf8" })).matchAll(/^(import|export)\s+.*?\s+from\s+['"](.*?)['"];?$/gims);
    for (let [, , match] of importMatches) {
      if (!match || !/^(\.|~)/.test(match) || (match = match.replace(/\?[a-z.]+$/, ""), match = match.replace(
        "~",
        // import from '~/components/...'
        relativePath(dirname(filePath), appDirectory) || "."
      ), match.includes("/+types/")))
        continue;
      let resolvedMatchPath = resolvePath(dirname(filePath), match), absoluteFilepath = (await findFileWithExtension(
        dirname(resolvedMatchPath),
        basename(resolvedMatchPath)
      )).filepath || resolvedMatchPath;
      absoluteFilepath.includes(`/${ASSETS_STARTER_DIR_ROUTES}/`) || (fileDependencies.add(relativePath(appDirectory, absoluteFilepath)), /\.[jt]sx?$/.test(absoluteFilepath) && filesToCheck.add(absoluteFilepath));
    }
  }
  return [...fileDependencies];
}
function copyLocaleNamelessRoute({
  typescript,
  localePrefix,
  ...options
}) {
  return copyRouteTemplate({
    ...options,
    typescript,
    templateName: "locale-check.ts",
    routeName: `($${localePrefix})${typescript ? ".tsx" : ".jsx"}`
  });
}
async function copyRouteTemplate({
  templateName,
  routeName,
  routesDirectory,
  formatOptions,
  typescript,
  adapter
}) {
  let routePath = joinPath(routesDirectory, routeName);
  if (await fileExists(routePath)) return;
  let templatePath = await getAssetsDir("routes", templateName);
  if (!await fileExists(templatePath))
    throw new Error("Unknown strategy");
  let templateContent = await readFile(templatePath);
  adapter && (templateContent = replaceAdapters(templateContent, adapter)), typescript || (templateContent = await transpileFile(templateContent, templatePath)), templateContent = await formatCode(templateContent, formatOptions, routePath), await writeFile(routePath, templateContent);
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/onboarding/common.js
var LANGUAGES = {
  js: "JavaScript",
  ts: "TypeScript"
};
async function handleI18n(controller, cliCommand, flagI18n) {
  let selection = flagI18n ?? await renderI18nPrompt({
    abortSignal: controller.signal,
    extraChoices: {
      none: "Set up later " + source_default.dim(`(run \`${cliCommand} setup markets\`)`)
    }
  }), i18nStrategy = selection === "none" ? void 0 : selection;
  return {
    i18nStrategy,
    setupI18n: async (options) => {
      i18nStrategy && await setupI18nStrategy(i18nStrategy, options);
    }
  };
}
function handleRouteGeneration(controller) {
  return {
    setupRoutes: async (directory, language, options) => (await generateRoutes(
      {
        routeName: "all",
        directory,
        force: !0,
        typescript: language === "ts",
        localePrefix: options?.i18nStrategy === "subfolders" ? "locale" : !1,
        signal: controller.signal,
        ...options
      },
      {
        rootDirectory: directory,
        appDirectory: joinPath(directory, "app")
      }
    )).routeGroups
  };
}
function generateProjectEntries(options) {
  return Promise.all(
    ["routes.ts", "root", "entry.server", "entry.client", "../server.ts"].map(
      (filename) => generateProjectFile(filename, options)
    )
  );
}
async function handleCliShortcut(controller, cliCommand, flagShortcut) {
  return cliCommand === ALIAS_NAME ? {} : flagShortcut ?? await renderConfirmationPrompt({
    confirmationMessage: "Yes",
    cancellationMessage: "No",
    message: [
      "Create a global",
      { command: ALIAS_NAME },
      "alias to run commands instead of",
      { command: cliCommand },
      "?"
    ],
    abortSignal: controller.signal
  }) ? {
    createShortcut: async () => {
      try {
        return (await createPlatformShortcut()).length > 0;
      } catch (error) {
        return outputDebug(
          "Failed to create shortcut." + (error?.stack ?? error?.message ?? error)
        ), !1;
      }
    },
    showShortcutBanner: () => renderInfo({
      body: `You'll need to restart your terminal session to make \`${ALIAS_NAME}\` alias available.`
    })
  } : {};
}
async function handleStorefrontLink(controller) {
  enhanceAuthLogs(!0);
  let { session, config } = await login();
  renderLoginSuccess(config);
  let storefronts = await getStorefronts(session), selectedStorefront = await handleStorefrontSelection(storefronts), title;
  return selectedStorefront ? title = selectedStorefront.title : title = await renderTextPrompt({
    message: "New storefront name",
    defaultValue: titleize(config.shopName),
    abortSignal: controller.signal
  }), {
    ...config,
    id: selectedStorefront?.id,
    title,
    session
  };
}
async function handleStorefrontSelection(storefronts) {
  let choices = [
    {
      label: "Create a new storefront",
      value: null
    },
    ...storefronts.map(({ id, title, productionUrl }) => ({
      label: `${title} (${productionUrl})`,
      value: id
    }))
  ];
  if (choices.length === 1)
    return;
  let storefrontId = await renderSelectPrompt({
    message: "Select a Hydrogen storefront to link",
    choices
  });
  return storefrontId ? storefronts.find(({ id }) => id === storefrontId) : void 0;
}
async function handleProjectLocation({
  storefrontInfo,
  controller,
  force,
  path: flagPath
}) {
  let storefrontDirectory = storefrontInfo && hyphenate(storefrontInfo.title), location = flagPath ?? storefrontDirectory ?? await renderTextPrompt({
    message: "Where would you like to create your storefront?",
    defaultValue: "hydrogen-storefront",
    abortSignal: controller.signal
  }), directory = resolvePath(process.cwd(), location);
  if (await projectExists(directory)) {
    if (!force && storefrontDirectory && (location = await renderTextPrompt({
      message: `There's already a folder called \`${storefrontDirectory}\`. Where do you want to create the app?`,
      defaultValue: storefrontDirectory,
      abortSignal: controller.signal
    }), directory = resolvePath(process.cwd(), location), await projectExists(directory) || (force = !0)), !force && !await renderConfirmationPrompt({
      message: `The directory ${source_default.cyan(
        location
      )} is not empty. Do you want to delete the existing files and continue?`,
      defaultValue: !1,
      abortSignal: controller.signal
    })) {
      renderInfo({
        body: `Destination path ${source_default.cyan(
          location
        )} already exists and is not an empty directory. You may use \`--force\` or \`-f\` to override it.`
      });
      return;
    }
    await rmdir(directory, { force: !0 });
  }
  return {
    name: basename(location),
    location,
    // User input. E.g. "./hydrogen-storefront"
    directory,
    // Absolute path to location
    storefrontTitle: storefrontInfo?.title
  };
}
async function handleLanguage(projectDir, controller, flagLanguage) {
  let language = flagLanguage ?? await renderSelectPrompt({
    message: "Select a language",
    choices: [
      { label: "JavaScript", value: "js" },
      { label: "TypeScript", value: "ts" }
    ],
    defaultValue: "js",
    abortSignal: controller.signal
  });
  return {
    language,
    async transpileProject() {
      language !== "ts" && await transpileProject(projectDir);
    }
  };
}
async function handleCssStrategy(projectDir, controller, flagStyling) {
  let selection = flagStyling ?? await renderCssPrompt({
    abortSignal: controller.signal,
    extraChoices: { none: "Skip and set up later" }
  }), cssStrategy = selection === "none" ? void 0 : selection;
  return {
    cssStrategy,
    async setupCss() {
      if (cssStrategy) {
        if (cssStrategy === "postcss" || cssStrategy === "css-modules")
          return;
        let result = await setupCssStrategy(
          cssStrategy,
          {
            rootDirectory: projectDir,
            appDirectory: joinPath(projectDir, "app")
            // Default value in new projects
          },
          !0
        );
        result && await result.workPromise;
      }
    }
  };
}
async function handleDependencies(projectDir, controller, packageManagerFromFlag, shouldInstallDeps) {
  let detectedPackageManager = packageManagerFromFlag ?? packageManagerFromUserAgent(), actualPackageManager = "npm";
  if (shouldInstallDeps !== !1)
    if (detectedPackageManager === "unknown") {
      let result = await renderSelectPrompt({
        message: "Select package manager to install dependencies",
        choices: [
          { label: "NPM", value: "npm" },
          { label: "PNPM", value: "pnpm" },
          { label: "Yarn v1", value: "yarn" },
          { label: "Skip and install later", value: "no" }
        ],
        defaultValue: "npm",
        abortSignal: controller.signal
      });
      result === "no" ? shouldInstallDeps = !1 : (actualPackageManager = result, shouldInstallDeps = !0);
    } else shouldInstallDeps === void 0 && (actualPackageManager = detectedPackageManager, shouldInstallDeps = await renderConfirmationPrompt({
      message: `Install dependencies with ${detectedPackageManager}?`,
      confirmationMessage: "Yes",
      cancellationMessage: "No",
      abortSignal: controller.signal
    }));
  return isHydrogenMonorepo && (await copyFile(
    joinPath(getSkeletonSourceDir(), ".npmrc"),
    joinPath(projectDir, ".npmrc")
  ).catch(() => {
  }), shouldInstallDeps || await symlink(
    await getRepoNodeModules(),
    joinPath(projectDir, "node_modules")
  ).catch(() => {
  })), {
    packageManager: actualPackageManager,
    shouldInstallDeps,
    installDeps: shouldInstallDeps ? async () => {
      await installNodeModules({
        directory: projectDir,
        packageManager: actualPackageManager,
        args: [],
        signal: controller.signal
      });
    } : () => {
    }
  };
}
var gitIgnoreContent = `
node_modules
/.cache
/build
/dist
/public/build
/.mf
.env
.shopify
.react-router
`.slice(1);
async function createInitialCommit(directory) {
  try {
    return await initializeGitRepository(directory), await writeFile(joinPath(directory, ".gitignore"), gitIgnoreContent), process.env.NODE_ENV === "test" && process.env.CI && (await execAsync('git config --global user.name "hydrogen"'), await execAsync('git config --global user.email "hydrogen@shopify.com"')), commitAll(directory, "Scaffold Storefront");
  } catch (error) {
    outputDebug(
      `Failed to initialize Git.
` + (error?.stack ?? error?.message ?? error)
    );
  }
}
async function commitAll(directory, message) {
  try {
    await addAllToGitFromDirectory(directory), await createGitCommit(message, { directory });
  } catch (error) {
    outputDebug(
      `Failed to commit code.
` + (error?.stack ?? error?.message ?? error)
    );
  }
}
async function renderProjectReady(project, {
  language,
  packageManager,
  depsInstalled,
  cssStrategy,
  cliCommand,
  routes,
  i18n,
  depsError,
  i18nError,
  routesError
}) {
  let hasErrors = !!(depsError || i18nError || routesError), bodyLines = [];
  project.storefrontTitle && bodyLines.push(["Shopify", project.storefrontTitle]), language && bodyLines.push(["Language", LANGUAGES[language]]), cssStrategy && bodyLines.push(["Styling", CSS_STRATEGY_NAME_MAP[cssStrategy]]), !i18nError && i18n && bodyLines.push(["Markets", I18N_STRATEGY_NAME_MAP[i18n].split(" (")[0]]);
  let routeSummary = "";
  if (!routesError && routes && Object.keys(routes).length) {
    bodyLines.push(["Routes", ""]);
    for (let [routeName, routePaths] of Object.entries(routes)) {
      routePaths = Array.isArray(routePaths) ? routePaths : [routePaths];
      let urls = [
        ...new Set(routePaths.map((item) => "/" + normalizeRoutePath(item)))
      ].sort();
      urls.length > 2 && (urls = [...new Set(urls.map((url) => url.split("/")[1] ?? ""))].map((item) => "/" + item + "/*")), routeSummary += `
    \u2022 ${capitalize(routeName)} ${source_default.dim(
        "(" + urls.join(" & ") + ")"
      )}`;
    }
  }
  let padMin = 1 + bodyLines.reduce((max, [label]) => Math.max(max, label.length), 0);
  (hasErrors ? renderWarning : renderSuccess)({
    headline: "Storefront setup complete" + (hasErrors ? " with errors (see warnings below)." : "!"),
    body: bodyLines.map(
      ([label, value]) => `  ${(label + ":").padEnd(padMin, " ")}  ${source_default.dim(value)}`
    ).join(`
`) + routeSummary,
    // Use `customSections` instead of `nextSteps` and `references`
    // here to enforce a newline between title and items.
    customSections: [
      hasErrors && {
        title: `Warnings
`,
        body: [
          {
            list: {
              items: [
                depsError && [
                  "Failed to install dependencies:",
                  { subdued: depsError.message }
                ],
                i18nError && [
                  "Failed to scaffold Markets:",
                  { subdued: i18nError.message }
                ],
                routesError && [
                  "Failed to scaffold routes:",
                  { subdued: routesError.message }
                ]
              ].filter((step) => !!step)
            }
          }
        ]
      },
      {
        title: `Next steps
`,
        body: [
          {
            list: {
              items: [
                [
                  "Run",
                  {
                    command: [
                      project.directory === process.cwd() ? void 0 : `cd ${project.location.replace(/^\.\//, "")}`,
                      depsInstalled ? void 0 : `${packageManager} install`,
                      currentProcessIsGlobal() ? "npm run dev" : formatPackageManagerCommand(packageManager, "dev")
                    ].filter(Boolean).join(" && ")
                  }
                ]
              ].filter((step) => !!step)
            }
          }
        ]
      }
    ].filter((step) => !!step)
  });
}
function createAbortHandler(controller, project) {
  return async function(error) {
    if (controller.abort(), await Promise.resolve(), project?.directory && await rmdir(project.directory, { force: !0 }).catch(() => {
    }), renderFatalError(
      new AbortError(
        "Failed to initialize project: " + (error?.message ?? ""),
        error?.tryMessage ?? error?.stack
      )
    ), process.env.SHOPIFY_UNIT_TEST && process.exit.name !== "spy")
      throw console.error("Error during test before process.exit:", error), error;
    process.exit(1);
  };
}
async function projectExists(projectDir) {
  return await fileExists(projectDir) && await isDirectory(projectDir) && (await readdir3(projectDir)).length > 0;
}
function normalizeRoutePath(routePath) {
  return routePath.replace(/(^|\.)_index$/, "").replace(/((^|\.)[^\.]+)_\./g, "$1.").replace(/\.(?!\w+\])/g, "/").replace(/\$$/g, ":catchAll").replace(/\$/g, ":").replace(/[\[\]]/g, "").replace(/:\w*Handle/i, ":handle");
}
function generateRandomName() {
  function getRandomElement(arr) {
    return arr[Math.floor(Math.random() * arr.length)];
  }
  let geographicalFeature = getRandomElement([
    "Bay",
    "Bend",
    "Cape",
    "Cliff",
    "Cove",
    "Creek",
    "Dale",
    "Dune",
    "Fjord",
    "Glade",
    "Gulf",
    "Hill",
    "Isle",
    "Knoll",
    "Lake",
    "Loch",
    "Mesa",
    "Peak",
    "Pond",
    "Quay",
    "Reef",
    "Ridge",
    "Rise",
    "River",
    "Road",
    "Shore",
    "Strait",
    "Stream",
    "Vale",
    "Valley",
    "View",
    "Vista"
  ]);
  return `${getRandomElement([
    "Crimson",
    "Azure",
    "Coral",
    "Fuchsia",
    "Indigo",
    "Ivory",
    "Lavender",
    "Lime",
    "Magenta",
    "Maroon",
    "Orchid",
    "Peach",
    "Plum",
    "Quartz",
    "Salmon",
    "Teal",
    "Turquoise",
    "Violet",
    "Yellow",
    "Ebony",
    "Jade",
    "Lilac",
    "Mint",
    "Onyx",
    "Pearl",
    "Ruby",
    "Sapphire",
    "Topaz"
  ])} ${geographicalFeature}`;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/commands/hydrogen/link.js
var Link = class _Link extends base_command_default {
  static descriptionWithMarkdown = `Links your local development environment to a remote Hydrogen storefront. You can link an unlimited number of development environments to a single Hydrogen storefront.

  Linking to a Hydrogen storefront enables you to run [dev](https://shopify.dev/docs/api/shopify-cli/hydrogen/hydrogen-dev) and automatically inject your linked Hydrogen storefront's environment variables directly into the server runtime.

  After you run the \`link\` command, you can access the [env list](https://shopify.dev/docs/api/shopify-cli/hydrogen/hydrogen-env-list), [env pull](https://shopify.dev/docs/api/shopify-cli/hydrogen/hydrogen-env-pull), and [unlink](https://shopify.dev/docs/api/shopify-cli/hydrogen/hydrogen-unlink) commands.`;
  static description = "Link a local project to one of your shop's Hydrogen storefronts.";
  static flags = {
    ...commonFlags.force,
    ...commonFlags.path,
    storefront: import_core2.Flags.string({
      description: `The name of a Hydrogen Storefront (e.g. "Jane's Apparel")`,
      env: "SHOPIFY_HYDROGEN_STOREFRONT"
    })
  };
  async run() {
    let { flags } = await this.parse(_Link);
    await runLink(flags);
  }
};
async function runLink({
  force,
  path: root = process.cwd(),
  storefront: flagStorefront
}) {
  let [{ session, config }, cliCommand] = await Promise.all([
    login(root),
    getCliCommand()
  ]), linkedStore = await linkStorefront(root, session, config, {
    force,
    flagStorefront,
    cliCommand
  });
  linkedStore && renderSuccess({
    body: [{ userInput: linkedStore.title }, "is now linked"],
    nextSteps: [
      [
        "Run",
        { command: `${cliCommand} dev` },
        "to start your local development server and start building"
      ]
    ]
  });
}
async function linkStorefront(root, session, config, {
  force = !1,
  flagStorefront,
  cliCommand,
  storefronts
}) {
  if (!config.shop)
    throw new AbortError("No shop found in local config, login first.");
  if (config.storefront?.id && !force && !await renderConfirmationPrompt({
    message: `Your project is currently linked to ${config.storefront.title}. Do you want to link to a different Hydrogen storefront on Shopify?`
  }))
    return;
  storefronts || (storefronts = await getStorefronts(session));
  let selectedStorefront;
  if (flagStorefront) {
    if (selectedStorefront = storefronts.find(
      ({ title }) => title === flagStorefront
    ), !selectedStorefront) {
      renderWarning({
        headline: `Couldn't find ${flagStorefront}`,
        body: [
          "There's no storefront matching",
          { userInput: flagStorefront },
          "on your",
          { userInput: config.shop },
          "shop. To see all available Hydrogen storefronts, run",
          {
            command: `${cliCommand} list`
          }
        ]
      });
      return;
    }
  } else
    selectedStorefront = await handleStorefrontSelection(storefronts), selectedStorefront || (selectedStorefront = await createNewStorefront(
      root,
      session,
      storefronts
    ));
  return await setStorefront(root, selectedStorefront), selectedStorefront;
}
async function createNewStorefront(root, session, storefronts) {
  let projectDirectory = basename(root), defaultProjectName = titleize(projectDirectory);
  storefronts.some(
    ({ title }) => title === defaultProjectName
  ) && (defaultProjectName = generateRandomName());
  let projectName = await renderTextPrompt({
    message: "New storefront name",
    defaultValue: defaultProjectName
  }), storefront, jobId;
  if (await renderTasks([
    {
      title: "Creating storefront",
      task: async () => {
        let result = await createStorefront(session, projectName);
        storefront = result.storefront, jobId = result.jobId;
      }
    },
    {
      title: "Creating API tokens",
      task: async () => {
        try {
          await waitForJob(session, jobId);
        } catch {
          storefront = void 0;
        }
      },
      skip: () => !jobId
    }
  ]), !storefront)
    throw new AbortError(
      "Unknown error ocurred. Please try again or contact support if the error persists."
    );
  return storefront;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/customer-application-update.js
init_cjs_shims();
var CUSTOMER_APPLICATION_URLS_REPLACE = `#graphql
  mutation CustomerApplicationUrlsReplace($storefrontId: ID!, $urlsReplaceInput: HydrogenStorefrontCustomerApplicationUrlsReplaceInput!) {
    hydrogenStorefrontCustomerApplicationUrlsReplace(
      storefrontId: $storefrontId,
      urlsReplaceInput: $urlsReplaceInput,
    ) {
      success
      userErrors {
        code
        field
        message
      }
    }
  }
`;
async function replaceCustomerApplicationUrls(adminSession, storefrontId, urlsReplaceInput) {
  let {
    hydrogenStorefrontCustomerApplicationUrlsReplace: { success, userErrors }
  } = await adminRequest(
    CUSTOMER_APPLICATION_URLS_REPLACE,
    adminSession,
    { storefrontId, urlsReplaceInput }
  );
  return { success, userErrors };
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/commands/hydrogen/customer-account/push.js
var CustomerAccountPush = class _CustomerAccountPush extends base_command_default {
  static description = "Push project configuration to admin";
  static flags = {
    ...commonFlags.path,
    "storefront-id": import_core3.Flags.string({
      description: "The id of the storefront the configuration should be pushed to. Must start with 'gid://shopify/HydrogenStorefront/'"
    }),
    "dev-origin": import_core3.Flags.string({
      description: "The development domain of your application.",
      required: !0
    }),
    "relative-redirect-uri": import_core3.Flags.string({
      description: "The relative url of allowed callback url for Customer Account API OAuth flow. Default is '/account/authorize'"
    }),
    "relative-logout-uri": import_core3.Flags.string({
      description: "The relative url of allowed url that will be redirected to post-logout for Customer Account API OAuth flow. Default to nothing."
    })
  };
  async run() {
    let { flags } = await this.parse(_CustomerAccountPush);
    await runCustomerAccountPush({ ...flagsToCamelObject(flags) });
  }
};
async function runCustomerAccountPush({
  path: root = process.cwd(),
  storefrontId: storefrontIdFromFlag,
  devOrigin,
  redirectUriRelativeUrl = "/account/authorize",
  logoutUriRelativeUrl
}) {
  let storefrontId = await getStorefrontId(root, storefrontIdFromFlag);
  try {
    if (!storefrontId)
      throw new Error(
        "No storefrontId was found in --storefront-id flag or .shopify/project.json"
      );
    let redirectUri = redirectUriRelativeUrl ? new URL(redirectUriRelativeUrl, devOrigin).toString() : devOrigin, javascriptOrigin = devOrigin, logoutUri = logoutUriRelativeUrl ? new URL(logoutUriRelativeUrl, devOrigin).toString() : devOrigin;
    if (!redirectUri && !javascriptOrigin && !logoutUri)
      return;
    let { session, config } = await login(root), customerAccountConfig = config?.storefront?.customerAccountConfig, { success, userErrors } = await replaceCustomerApplicationUrls(
      session,
      storefrontId,
      {
        redirectUri: {
          add: redirectUri ? [redirectUri] : void 0,
          removeRegex: customerAccountConfig?.redirectUri
        },
        javascriptOrigin: {
          add: javascriptOrigin ? [javascriptOrigin] : void 0,
          removeRegex: customerAccountConfig?.javascriptOrigin
        },
        logoutUris: {
          add: logoutUri ? [logoutUri] : void 0,
          removeRegex: customerAccountConfig?.logoutUri
        }
      }
    );
    if (!success || userErrors.length) {
      let error = new Error(
        "Customer Account Application setup update fail."
      );
      throw error.userErrors = userErrors, error;
    }
    return await setCustomerAccountConfig(root, {
      redirectUri,
      javascriptOrigin,
      logoutUri
    }), () => cleanupCustomerApplicationUrls(session, storefrontId, {
      redirectUri,
      javascriptOrigin,
      logoutUri
    });
  } catch (error) {
    let confidentialAccessFound = !1, errors = error?.userErrors?.length ? error.userErrors.map(
      (value) => (/Javascript origin is not allowed for this application type/i.test(
        value.message
      ) && (confidentialAccessFound = !0), `${value.field}: ${value.message}`)
    ) : error.message ? [error.message] : [], nextSteps = [
      {
        link: {
          label: "Manually update application setup",
          url: "https://shopify.dev/docs/custom-storefronts/building-with-the-customer-account-api/hydrogen#update-the-application-setup"
        }
      }
    ];
    throw confidentialAccessFound && nextSteps.unshift({
      link: {
        label: "Enable Public access for Hydrogen",
        url: "https://shopify.dev/docs/custom-storefronts/building-with-the-customer-account-api/getting-started#step-1-enable-customer-account-api-access"
      }
    }), new AbortError(
      "Customer Account Application setup update fail.",
      errors.length ? [
        {
          list: {
            title: "The following error occurs during update:",
            items: errors.map(
              (value, index) => `${index != 0 ? `

` : ""}${value}`
            )
          }
        }
      ] : void 0,
      nextSteps
    );
  }
}
async function cleanupCustomerApplicationUrls(session, storefrontId, customerAccountConfig = {}) {
  Object.values(customerAccountConfig).some(Boolean) && (outputDebug(
    `Cleaning up Customer Application url "${customerAccountConfig.redirectUri}"`
  ), await replaceCustomerApplicationUrls(session, storefrontId, {
    redirectUri: { removeRegex: customerAccountConfig?.redirectUri },
    javascriptOrigin: { removeRegex: customerAccountConfig?.javascriptOrigin },
    logoutUris: { removeRegex: customerAccountConfig?.logoutUri }
  }).catch((error) => {
    outputDebug(
      `Failed to clean up Customer Application url "${customerAccountConfig.redirectUri}":
${error?.message}`
    );
  }));
}
async function getStorefrontId(root, storefrontIdFromFlag) {
  if (storefrontIdFromFlag) return storefrontIdFromFlag;
  let { session, config } = await login(root);
  if (config.storefront?.id) return config.storefront.id;
  let cliCommand = await getCliCommand();
  return await linkStorefront(root, session, config, {
    cliCommand
  }) ? (await getConfig(root)).storefront?.id : void 0;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/environment-variables.js
init_cjs_shims();

// ../cli-kit/dist/public/node/dot-env.js
init_cjs_shims();
var import_dotenv = __toESM(require_main2());
async function readAndParseDotEnv(path3) {
  if (outputDebug(outputContent`Reading the .env file at ${outputToken.path(path3)}`), !await fileExists(path3))
    throw new AbortError(`The environment file at ${path3} does not exist.`);
  let content = await readFile(path3);
  return {
    path: path3,
    variables: (0, import_dotenv.parse)(content)
  };
}
function patchEnvFile(envFileContent, updatedValues) {
  let outputLines = [], envFileLines = envFileContent === null ? [] : envFileContent.split(`
`), alreadyPresentKeys = [], multilineVariable;
  for (let line of envFileLines) {
    if (multilineVariable) {
      if (line.endsWith(multilineVariable.quote)) {
        let lineToWrite2 = createDotEnvFileLine(multilineVariable.key, multilineVariable.value + line.slice(0, -1), multilineVariable.quote), newValue = updatedValues[multilineVariable.key];
        newValue && (alreadyPresentKeys.push(multilineVariable.key), lineToWrite2 = createDotEnvFileLine(multilineVariable.key, newValue)), outputLines.push(lineToWrite2), multilineVariable = void 0;
      } else
        multilineVariable.value += `${line}
`;
      continue;
    }
    let match = line.match(/^([^=:#]+?)[=:](.*)/), lineToWrite = line;
    if (match) {
      let key = match[1].trim(), value = (match[2] || "").trim();
      if (/^["'`]/.test(value) && !value.endsWith(value[0])) {
        multilineVariable = {
          key,
          value: `${value.slice(1)}
`,
          quote: value[0]
        };
        continue;
      }
      let newValue = updatedValues[key];
      newValue && (alreadyPresentKeys.push(key), lineToWrite = createDotEnvFileLine(key, newValue));
    }
    outputLines.push(lineToWrite);
  }
  if (multilineVariable)
    throw new AbortError(`Multi-line environment variable '${multilineVariable.key}' is not properly enclosed.`);
  for (let [patchKey, updatedValue] of Object.entries(updatedValues))
    alreadyPresentKeys.includes(patchKey) || outputLines.push(createDotEnvFileLine(patchKey, updatedValue));
  return outputLines.join(`
`);
}
function createDotEnvFileLine(key, value, quote) {
  if (quote)
    return `${key}=${quote}${value}${quote}`;
  if (value?.includes(`
`)) {
    let quoteCharacter = ['"', "'", "`"].find((char) => !value.includes(char));
    if (!quoteCharacter)
      throw new AbortError(`The environment file patch has an env value that can't be surrounded by quotes: ${value}`);
    return `${key}=${quoteCharacter}${value}${quoteCharacter}`;
  }
  return `${key}=${value}`;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/pull-variables.js
init_cjs_shims();
var PullVariablesQuery = `#graphql
  query PullVariables($id: ID!, $handle: String) {
    hydrogenStorefront(id: $id) {
      id
      environmentVariables(handle: $handle) {
        id
        isSecret
        readOnly
        key
        value
      }
    }
  }
`;
async function getStorefrontEnvVariables(adminSession, storefrontId, envHandle) {
  let { hydrogenStorefront } = await adminRequest(
    PullVariablesQuery,
    adminSession,
    {
      id: storefrontId,
      handle: envHandle
    }
  );
  return hydrogenStorefront;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/graphql/admin/list-environments.js
init_cjs_shims();
var ListEnvironmentsQuery = `#graphql
  query ListStorefronts($id: ID!) {
    hydrogenStorefront(id: $id) {
      id
      productionUrl
      environments {
        branch
        createdAt
        id
        name
        handle
        type
        url
      }
    }
  }
`;
async function getStorefrontEnvironments(adminSession, storefrontId) {
  let { hydrogenStorefront } = await adminRequest(
    ListEnvironmentsQuery,
    adminSession,
    { id: storefrontId }
  );
  return hydrogenStorefront;
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/common.js
init_cjs_shims();
function orderEnvironmentsBySafety(environments) {
  return [
    ...environments.filter((environment) => environment.type === "PREVIEW"),
    ...environments.filter((environment) => environment.type === "CUSTOM"),
    ...environments.filter((environment) => environment.type === "PRODUCTION")
  ];
}
function createEnvironmentCliChoiceLabel(name, handle, branch) {
  let metadataStringified = Object.entries({ handle, branch }).reduce((acc, [key, val]) => (val && acc.push(`${key}: ${val}`), acc), []).join(", ");
  return `${name} ${source_default.dim(`(${metadataStringified})`)}`;
}
function findEnvironmentOrThrow(environments, envHandle) {
  let matchedEnvironment = environments.find(
    ({ handle }) => handle === envHandle
  );
  if (!matchedEnvironment)
    throw environmentNotFound("handle", envHandle);
  return matchedEnvironment;
}
function findEnvironmentByBranchOrThrow(environments, branch) {
  let matchedEnvironment = environments.find(({ branch: b }) => b === branch);
  if (!matchedEnvironment)
    throw environmentNotFound("branch", branch);
  return matchedEnvironment;
}
function environmentNotFound(criterion, value) {
  return new AbortError(
    "Environment not found",
    `We could not find an environment matching the ${criterion} '${value}'.`,
    [
      [
        "Run",
        { command: "env list" },
        "to view a list of available environments."
      ]
    ]
  );
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/environment-variables.js
var createEmptyRemoteVars = () => ({
  remoteVariables: {},
  remoteSecrets: {}
});
async function getAllEnvironmentVariables({
  root,
  envHandle,
  envBranch,
  envFile,
  fetchRemote = !0,
  localVariables: inlineLocalVariables
}) {
  let [{ remoteVariables, remoteSecrets }, { variables: localVariables }] = await Promise.all([
    // Get remote vars
    fetchRemote ? getRemoteVariables(root, envHandle, envBranch).catch(
      (error) => (renderWarning({
        headline: "Failed to load environment variables from Shopify. The development server will still start, but the following error occurred:",
        body: [error.message, error.tryMessage, error.nextSteps].filter(Boolean).join(`

`)
      }), createEmptyRemoteVars())
    ) : createEmptyRemoteVars(),
    // Get local vars
    inlineLocalVariables ? { variables: inlineLocalVariables } : getLocalVariables(root, envFile)
  ]), remoteSecretKeys = Object.keys(remoteSecrets), remotePublicKeys = Object.keys(remoteVariables), localKeys = Object.keys(localVariables);
  function logInjectedVariables() {
    (localKeys.length > 0 || remotePublicKeys.length + remoteSecretKeys.length > 0) && (outputInfo(`
Environment variables injected into MiniOxygen:
`), outputInfo(
      linesToColumns([
        ...remotePublicKeys.filter((key) => !localKeys.includes(key)).map((key) => [key, "from Oxygen"]),
        ...localKeys.map((key) => [key, `from local ${envFile}`]),
        // Ensure secret variables always get added to the bottom of the list
        ...remoteSecretKeys.filter((key) => !localKeys.includes(key)).map((key) => [
          source_default.dim(key),
          source_default.dim("from Oxygen (Marked as secret)")
        ])
      ])
    ));
  }
  return {
    logInjectedVariables,
    remoteVariables,
    remoteSecrets,
    localVariables,
    allVariables: {
      ...remoteSecrets,
      ...remoteVariables,
      ...localVariables
    }
  };
}
async function getRemoteVariables(root, envHandle, envBranch) {
  let { session, config } = await login(root);
  if (envBranch) {
    let environments = (await getStorefrontEnvironments(session, config.storefront.id))?.environments || [];
    envHandle = findEnvironmentByBranchOrThrow(environments, envBranch).handle;
  }
  let envVariables = (await getStorefrontEnvVariables(session, config.storefront.id, envHandle))?.environmentVariables || [], remoteVariables = {}, remoteSecrets = {};
  for (let { key, value, isSecret } of envVariables)
    isSecret ? remoteSecrets[key] = value : remoteVariables[key] = value;
  return { remoteVariables, remoteSecrets };
}
async function getLocalVariables(root, envFile) {
  let dotEnvPath = resolvePath(root, envFile);
  return await fileExists(dotEnvPath).then(
    (exists2) => exists2 ? readAndParseDotEnv(dotEnvPath) : { variables: {} }
  );
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/tunneling.js
init_cjs_shims();
async function startTunnelPlugin(config, port, provider) {
  let hooks = await fanoutHooks(config, "tunnel_start", { port, provider }), first2 = Object.values(hooks).filter(
    (tunnelResponse) => !tunnelResponse?.isErr() || tunnelResponse.error.type !== "invalid-provider"
  )[0];
  if (!first2)
    throw new BugError(`We couldn't find the ${provider} tunnel plugin`);
  if (first2.isErr())
    throw new AbortError(
      `${provider} failed to start the tunnel.
${first2.error.message}`
    );
  return first2.value;
}
async function pollTunnelURL(tunnelClient) {
  return new Promise((resolve2, reject) => {
    let retries = 0, pollTunnelStatus = async () => {
      let result = tunnelClient.getTunnelStatus();
      if (outputDebug(
        `Polling tunnel status for ${tunnelClient.provider} (attempt ${retries}): ${result.status}`
      ), result.status === "error")
        return reject(new AbortError(result.message, result.tryMessage));
      result.status === "connected" ? resolve2(result.url) : (retries += 1, startPolling());
    }, startPolling = () => {
      setTimeout(pollTunnelStatus, 500);
    };
    pollTunnelStatus();
  });
}

// ../../node_modules/.pnpm/@shopify+cli-hydrogen@11.1.10_@graphql-codegen+cli@5.0.4_@parcel+watcher@2.5.1_@types+n_224b49f683ddd0894a6efb2678941bc0/node_modules/@shopify/cli-hydrogen/dist/lib/dev-shared.js
function isMockShop(envVariables) {
  return envVariables.PUBLIC_STORE_DOMAIN === "mock.shop" || // We fallback to mock.shop if the env var is falsy.
  // When it's undefined, it might be overwritten by remote variables.
  envVariables.PUBLIC_STORE_DOMAIN === "";
}
function notifyIssueWithTunnelAndMockShop(cliCommand) {
  renderInfo({
    headline: "Using mock.shop with `--customer-account-push` flag is not supported",
    body: "The functionalities of this flag are disabled.",
    nextSteps: [
      "You may continue knowing Customer Account API (/account) interactions will fail.",
      [
        "Or run",
        { command: `${cliCommand} env pull` },
        "to link to your store credentials."
      ]
    ]
  });
}
function getDevConfigInBackground(root, customerAccountPushFlag, envFile) {
  return getLocalVariables(root, envFile).then(
    async ({ variables: localVariables }) => {
      let customerAccountPush = customerAccountPushFlag && !isMockShop(localVariables);
      customerAccountPush && await getStorefrontId(root);
      let { shop, storefront } = await getConfig(root), storefrontId = storefront?.id;
      return {
        storefrontId,
        customerAccountPush,
        fetchRemote: !!shop && !!storefrontId,
        localVariables,
        storefrontTitle: storefront?.title
      };
    }
  );
}
var TUNNEL_DOMAIN = Object.freeze({
  ORIGINAL: ".trycloudflare.com",
  REBRANDED: ".tryhydrogen.dev"
});
async function startTunnelAndPushConfig(root, cliConfig, port, storefrontId) {
  outputInfo(`
Starting tunnel...
`);
  let tunnel = await startTunnelPlugin(cliConfig, port, "cloudflare"), host = await pollTunnelURL(tunnel).then(
    (host2) => (
      // Replace branded tunnel domain:
      host2.replace(TUNNEL_DOMAIN.ORIGINAL, TUNNEL_DOMAIN.REBRANDED)
    )
  ), cleanup = await runCustomerAccountPush({
    path: root,
    devOrigin: host,
    storefrontId
  }).catch((error) => {
    error instanceof AbortError && renderInfo({
      headline: "Customer Account Application setup update fail.",
      body: error.tryMessage || void 0,
      nextSteps: error.nextSteps
    });
  });
  return { host, cleanup };
}
function getDebugBannerLine(publicInspectorPort) {
  let isVSCode = process.env.TERM_PROGRAM === "vscode", debuggingDocsLink = "https://h2o.fyi/debugging/server-code" + (isVSCode ? "#visual-studio-code" : "#step-2-attach-a-debugger");
  return outputContent`Debugging enabled on port ${String(
    publicInspectorPort
  )}.\nAttach a ${outputToken.link(
    source_default.yellow(isVSCode ? "VSCode debugger" : "debugger"),
    debuggingDocsLink
  )} or open DevTools in http://localhost:${String(publicInspectorPort)}.`.value;
}
function getUtilityBannerlines(host) {
  return host = host.endsWith("/") ? host.slice(0, -1) : host, [
    `View GraphiQL API browser: 
${getGraphiQLUrl({
      host
    })}`,
    `View server network requests: 
${host}/subrequest-profiler`
  ].map((value, index) => ({
    subdued: `${index === 0 ? "" : `

`}${value}`
  }));
}

export {
  require_commonjs3 as require_commonjs,
  require_ignore,
  initializeGitRepository,
  checkIfIgnoredInGitRepository,
  downloadGitRepository,
  getLatestGitCommit,
  ensureInsideGitDirectory,
  insideGitDirectory,
  GitDirectoryNotCleanError,
  ensureIsClean,
  isClean,
  removeGitRemote,
  require_lib2 as require_lib,
  execAsync,
  supressNodeExperimentalWarnings,
  isHydrogenMonorepo,
  hydrogenPackagesPath,
  getAssetsDir,
  getTemplateAppFile,
  getStarterDir,
  getSkeletonSourceDir,
  SETUP_CSS_STRATEGIES,
  getCodeFormatOptions,
  formatCode,
  replaceFileContent,
  mergePackageJson,
  importLangAstGrep,
  STYLING_CHOICES,
  CSS_STRATEGY_NAME_MAP,
  CSS_STRATEGY_HELP_URL_MAP,
  setupCssStrategy,
  renderCssPrompt,
  SETUP_I18N_STRATEGIES,
  I18N_STRATEGY_NAME_MAP,
  I18N_CHOICES,
  setupI18nStrategy,
  renderI18nPrompt,
  DEFAULT_APP_PORT,
  DEFAULT_INSPECTOR_PORT,
  commonFlags,
  flagsToCamelObject,
  parseProcessFlags,
  overrideFlag,
  importVite,
  importLocal,
  isClassicProject,
  getProjectPaths,
  getRawRemixConfig,
  getRemixConfig,
  REMIX_COMPILER_ERROR_MESSAGE,
  hasViteConfig,
  isViteProject,
  getViteConfig,
  findHydrogenPlugin,
  findOxygenPlugin,
  adminRequest,
  parseGid,
  getStorefronts,
  getConfig,
  setUserAccount,
  setStorefront,
  unsetStorefront,
  createStorefront,
  waitForJob,
  ALIAS_NAME,
  createPlatformShortcut,
  getCliCommand,
  muteDevLogs,
  enhanceAuthLogs,
  enhanceH2Logs,
  setH2OVerbose,
  isH2Verbose,
  logout2 as logout,
  login,
  renderLoginSuccess,
  isV1RouteConventionInstalled,
  ALL_ROUTE_CHOICES,
  generateRoutes,
  LANGUAGES,
  handleI18n,
  handleRouteGeneration,
  generateProjectEntries,
  handleCliShortcut,
  handleStorefrontLink,
  handleProjectLocation,
  handleLanguage,
  handleCssStrategy,
  handleDependencies,
  createInitialCommit,
  commitAll,
  renderProjectReady,
  createAbortHandler,
  Link,
  linkStorefront,
  CustomerAccountPush,
  H2O_BINDING_NAME,
  setConstructors,
  createLogRequestEvent,
  handleDebugNetworkRequest,
  SUBREQUEST_PROFILER_ENDPOINT,
  handleMiniOxygenImportFail,
  logRequestLine,
  readAndParseDotEnv,
  patchEnvFile,
  createDotEnvFileLine,
  orderEnvironmentsBySafety,
  createEnvironmentCliChoiceLabel,
  findEnvironmentOrThrow,
  findEnvironmentByBranchOrThrow,
  getStorefrontEnvVariables,
  getStorefrontEnvironments,
  getAllEnvironmentVariables,
  isMockShop,
  notifyIssueWithTunnelAndMockShop,
  getDevConfigInBackground,
  TUNNEL_DOMAIN,
  startTunnelAndPushConfig,
  getDebugBannerLine,
  getUtilityBannerlines
};
//# sourceMappingURL=chunk-ZN7BY463.js.map
